# Contexts and current context

Pack: dump-cooloo9871
Source topic: source-q01 (Contexts)

Lab namespace: `${CKA_SIM_LAB_NS}`

Use kubectl against the live lab state seeded by this drill. Create a ConfigMap named `q01-answer` in the lab namespace with these data keys:

- `contexts`
- `currentContext`
- `noKubectlSource`

Use commands to derive the values; do not read or modify files under `/tmp/cka-sim` for the answer.
