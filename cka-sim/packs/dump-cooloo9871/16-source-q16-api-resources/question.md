# Identify namespaces and namespaced API resources

Pack: dump-cooloo9871
Source topic: source-q16 (Namespaces and API resources)

Lab namespace: `${CKA_SIM_LAB_NS}`

Use kubectl against the live lab state seeded by this drill. Create a ConfigMap named `q16-answer` in the lab namespace with these data keys:

- `namespace`
- `namespacedKind`

Use commands to derive the values; do not read or modify files under `/tmp/cka-sim` for the answer.
