pack:
  id: cluster-architecture
  domain: cluster-architecture
  weight: 25
  description: "Cluster Architecture 25% domain pack (PACK-04) -- full v1.35 Tracker coverage + CONCERNS.md content replacements (PSS + CRI-dockerd)."
questions:
  - id: cluster-architecture-rbac-viewer
    path: 01-rbac-viewer
    estimatedMinutes: 8
  # BEGIN phase-05 new questions (P09-P15 append below this line -- one row per plan, idempotent)
  # END phase-05 new questions
