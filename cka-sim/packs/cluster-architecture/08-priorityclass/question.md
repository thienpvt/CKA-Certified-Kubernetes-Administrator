# PriorityClass globalDefault

Two PriorityClasses, `q08-critical` and `q08-batch`, are part of this drill.

Fix the state so exactly one PriorityClass has `globalDefault: true`.

Do not delete either PriorityClass. Both resources must still exist after your fix.
