#!/bin/bash
# cka-sim list — show packs, blueprints, history (Phase 1: stub)

set -euo pipefail

printf 'list — no packs registered yet (phase 4+ populates packs/)\n'
printf 'list — no blueprints registered yet (phase 7+ populates exams/)\n'
printf 'list — no session history yet (phase 7+ writes ~/.cka-sim/history/)\n'
