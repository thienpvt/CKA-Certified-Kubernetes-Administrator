{
  "note": "Placeholder fixture for read_node_worker happy-path unit case. Represents the response shape the helper expects from `kubectl get nodes -l '!node-role.kubernetes.io/control-plane' -o jsonpath='{.items[0].metadata.name}'`.",
  "expected_stdout": "worker-1"
}
