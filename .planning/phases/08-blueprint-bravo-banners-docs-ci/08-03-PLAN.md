---
plan: 08-03
phase: 08
title: "Full documentation (README, AUTHORING, SCHEMA, CONTRIBUTING)"
wave: 2
depends_on: [08-01]
requirements: [DOC-01, DOC-02, DOC-03, DOC-04]
files_modified:
  - cka-sim/README.md
  - cka-sim/AUTHORING.md
  - cka-sim/SCHEMA.md
  - CONTRIBUTING.md
autonomous: yes
---

# Plan 08-03: Documentation

## Objective

Deliver the four documentation deliverables: full quickstart README, expanded authoring guide, YAML schema reference, and CONTRIBUTING section for exam-sim question authoring.

## Tasks

### Task 1: Rewrite `cka-sim/README.md` (DOC-01)

Replace the placeholder with a full quickstart. Structure:

1. **Title + one-line description** — "cka-sim — CKA Exam Simulator"
2. **Overview** — bash-only, kubectl-driven, runs on candidate's 1+2 kubeadm cluster, v1.35 syllabus
3. **Quickstart** — step-by-step:
   - `cka-sim bootstrap` (SSH setup, env)
   - `cka-sim doctor` (health check)
   - `cka-sim drill <domain>` (single question practice)
   - `cka-sim exam blueprint-alpha` (timed 2h mock)
   - `cka-sim score` (view last report)
   - `cka-sim list history` (all sessions)
4. **Architecture** — directory tree showing lib/, packs/, exams/, scripts/, tests/, traps/
5. **Domain Packs** — table of 5 packs with question counts
6. **Mock Exams** — blueprint-alpha and blueprint-bravo descriptions
7. **Development** — how to run tests (`scripts/test.sh`), lint (`scripts/lint-packs.sh`), validate (`scripts/validate-local.sh`)
8. **Disclaimer** — "Not real CKA exam content; independently authored."

### Task 2: Expand `cka-sim/AUTHORING.md` (DOC-02)

Keep existing Phase 3 content as the foundation. Expand with:

- **Style guide** — question.md writing conventions (PSI style, imperative voice, no spoilers)
- **Schema deep-dive** — metadata.yaml field-by-field with examples
- **Coverage-matrix workflow** — how to add a question and update coverage.yaml
- **CI integration** — what lint-packs.sh checks, how to run locally
- **Trap registration flow** — adding a new trap to catalog.yaml + writing the detector

### Task 3: Create `cka-sim/SCHEMA.md` (DOC-03)

New file. YAML schema documentation for:

1. **Question `metadata.yaml`** — all fields with types, constraints, examples
2. **Pack `manifest.yaml`** — pack-level fields + questions array schema
3. **Exam `manifest.yaml`** — exam-level fields + questions array schema
4. **`traps/catalog.yaml`** — per-entry schema (8 fields)

Use fenced YAML blocks for examples. Reference lint-packs.sh pass letters for enforcement.

### Task 4: Extend `CONTRIBUTING.md` (DOC-04)

Append a new section "## Authoring Exam-Sim Questions" after the existing content. Cover:

- Prerequisites (working 1+2 cluster, shellcheck installed)
- Step-by-step: create directory, write 6 files, register traps, update coverage.yaml, run lint
- Link to `cka-sim/AUTHORING.md` for the full guide
- Link to `cka-sim/SCHEMA.md` for YAML schemas

Do NOT modify any existing content in CONTRIBUTING.md.

## Verification

- `cka-sim/README.md` contains "bootstrap", "doctor", "drill", "exam", "score" in quickstart section
- `cka-sim/AUTHORING.md` contains "Style guide", "Coverage-matrix", "Trap registration"
- `cka-sim/SCHEMA.md` contains all 4 schema sections
- `CONTRIBUTING.md` ends with the new "Authoring Exam-Sim Questions" section
- No broken relative links
