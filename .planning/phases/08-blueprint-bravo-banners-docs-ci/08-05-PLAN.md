---
plan: 08-05
phase: 08
title: "Phase 8 verification + lint pass"
wave: 3
depends_on: [08-01, 08-02, 08-03, 08-04]
requirements: []
files_modified:
  - .planning/phases/08-blueprint-bravo-banners-docs-ci/08-VERIFICATION.md
autonomous: yes
---

# Plan 08-05: Phase Verification

## Objective

Run all automated checks against the Phase 8 deliverables and produce a verification report.

## Tasks

### Task 1: Run lint-packs.sh (validates blueprint-bravo manifest)

```bash
bash cka-sim/scripts/lint-packs.sh
```

Pass H validates:
- 17 questions in manifest
- Weighting fields present with correct values
- All (pack, slug) pairs resolve to existing directories
- No duplicate slugs
- estimatedMinutes sum in budget range
- Disclaimer present
- README contains disclaimer

### Task 2: Run test.sh (full test suite)

```bash
bash cka-sim/scripts/test.sh
```

Ensures no regressions from Phase 8 changes.

### Task 3: Run validate-local.sh (shellcheck + yamllint)

```bash
bash cka-sim/scripts/validate-local.sh
```

Validates all new .sh and .yaml files pass shellcheck and yamllint.

### Task 4: Verify banners

Check that:
- `README.md` line 1 starts with `> **Note:**`
- `exercises/README.md` line 1 starts with `> **Note:**`
- `mock-exams/README.md` line 1 starts with `> **Note:**`
- Existing content below banners is unchanged (git diff shows only additions at top)

### Task 5: Verify documentation completeness

Check that:
- `cka-sim/README.md` contains quickstart sections
- `cka-sim/AUTHORING.md` has expanded sections
- `cka-sim/SCHEMA.md` exists with 4 schema sections
- `CONTRIBUTING.md` has "Authoring Exam-Sim Questions" section

### Task 6: Write 08-VERIFICATION.md

Produce the verification report with pass/fail for each check.

## Verification

All checks pass → phase is complete.
