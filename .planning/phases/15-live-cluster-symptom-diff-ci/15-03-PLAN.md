---
phase: 15-live-cluster-symptom-diff-ci
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - cka-sim/packs/workloads-scheduling/01-deployment-requests/expected-symptom.yaml
  - cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/expected-symptom.yaml
  - cka-sim/packs/workloads-scheduling/03-configmap-secret-env-volume/expected-symptom.yaml
  - cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/expected-symptom.yaml
  - cka-sim/packs/workloads-scheduling/05-daemonset/expected-symptom.yaml
  - cka-sim/packs/workloads-scheduling/06-static-pod/expected-symptom.yaml
  - cka-sim/packs/workloads-scheduling/07-native-sidecar/expected-symptom.yaml
  - cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/expected-symptom.yaml
autonomous: true
requirements: [CI-01]
must_haves:
  truths:
    - "All 8 workloads-scheduling questions ship an expected-symptom.yaml derived from question.md's claimed symptom — NOT from running setup.sh."
    - "Each file yamllint-clean under validate.yml ruleset; parses via python3 yaml.safe_load; uses ${CKA_SIM_LAB_NS} substitution."
    - "Open-world: only fields the question text claims appear under expect:. Resources whose presence is the only claim use expect: {}."
  artifacts:
    - path: cka-sim/packs/workloads-scheduling/01-deployment-requests/expected-symptom.yaml
      provides: "Deployment load-app exists, Available; pod spec lacks resources.requests (the candidate fix); ServiceAccount load-app-sa absent."
      contains: "question: workloads-scheduling-deployment-requests"
    - path: cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/expected-symptom.yaml
      provides: "Deployment web exists, Available, image=nginx:1.25 (pre-update state)."
      contains: "question: workloads-scheduling-rolling-update-rollback"
    - path: cka-sim/packs/workloads-scheduling/03-configmap-secret-env-volume/expected-symptom.yaml
      provides: "ConfigMap q03-app-config + Secret q03-app-secret present; Pod q03-app candidate-authored (absent)."
      contains: "question: workloads-scheduling-configmap-secret-env-volume"
    - path: cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/expected-symptom.yaml
      provides: "Deployment q04-load present with cpu/memory requests; HPA q04-load candidate-authored (absent)."
      contains: "question: workloads-scheduling-hpa-metrics-server"
    - path: cka-sim/packs/workloads-scheduling/05-daemonset/expected-symptom.yaml
      provides: "DaemonSet q05-node-agent candidate-authored (absent at setup)."
      contains: "question: workloads-scheduling-daemonset"
    - path: cka-sim/packs/workloads-scheduling/06-static-pod/expected-symptom.yaml
      provides: "Mirror Pod q06-static-nginx-node-01 candidate-authored (absent at setup); namespace state only."
      contains: "question: workloads-scheduling-static-pod"
    - path: cka-sim/packs/workloads-scheduling/07-native-sidecar/expected-symptom.yaml
      provides: "Deployment q07-app present, sidecar declared as a regular container in spec.containers (the wrong-place state the candidate fixes)."
      contains: "question: workloads-scheduling-native-sidecar"
    - path: cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/expected-symptom.yaml
      provides: "Deployment q08-gpu-sim present; replicas Pending (NoSchedule taint blocks them) until candidate adds toleration + nodeSelector."
      contains: "question: workloads-scheduling-nodeselector-affinity-taints"
  key_links:
    - from: cka-sim/packs/workloads-scheduling/<NN>/expected-symptom.yaml
      to: cka-sim/packs/workloads-scheduling/<NN>/question.md
      via: "expect: fields encode literal symptom claims (Available / Pending / image / replica counts)."
      pattern: "question: workloads-scheduling-"
---

<objective>
Ship expected-symptom.yaml for all 8 workloads-scheduling questions. Files derive from question.md's symptom claims. After this plan: lint-question-symptom.sh walks 8 workloads-scheduling YAMLs and exits 0 on a live cluster.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-live-cluster-symptom-diff-ci/15-CONTEXT.md
@cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md
@cka-sim/packs/workloads-scheduling/01-deployment-requests/question.md
@cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh
@cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/question.md
@cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/setup.sh
@cka-sim/packs/workloads-scheduling/03-configmap-secret-env-volume/question.md
@cka-sim/packs/workloads-scheduling/03-configmap-secret-env-volume/setup.sh
@cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/question.md
@cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/setup.sh
@cka-sim/packs/workloads-scheduling/05-daemonset/question.md
@cka-sim/packs/workloads-scheduling/05-daemonset/setup.sh
@cka-sim/packs/workloads-scheduling/06-static-pod/question.md
@cka-sim/packs/workloads-scheduling/06-static-pod/setup.sh
@cka-sim/packs/workloads-scheduling/07-native-sidecar/question.md
@cka-sim/packs/workloads-scheduling/07-native-sidecar/setup.sh
@cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/question.md
@cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/setup.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: workloads-scheduling/01-deployment-requests/expected-symptom.yaml</name>
  <files>cka-sim/packs/workloads-scheduling/01-deployment-requests/expected-symptom.yaml</files>
  <action>
    Question.md says: Deployment load-app exists; pods do NOT yet use a dedicated SA; pods do NOT yet declare resources.requests. SA load-app-sa is candidate-created.

    ```yaml
    # workloads-scheduling/01-deployment-requests — symptom: load-app Deployment
    # exists, Available, but pod template has no SA + no resources.requests
    # (those are the candidate fixes). load-app-sa SA is candidate-authored.
    question: workloads-scheduling-deployment-requests
    namespace: ${CKA_SIM_LAB_NS}
    resources:
      - kind: deploy
        name: load-app
        namespace: ${CKA_SIM_LAB_NS}
        expect:
          status.conditions[?(@.type=="Available")].status: "True"
          spec.template.spec.containers[0].image: nginx:1.27
    absent_resources:
      - kind: serviceaccount
        name: load-app-sa
        namespace: ${CKA_SIM_LAB_NS}
    ```

    Note: `spec.template.spec.containers[0].image` uses indexed-array dot form. The lint script's translator (plan 01 Task 2) converts `a.b.containers[0].image` to jq `.a.b.containers[0].image`; jq accepts numeric-indexed `.containers[0]` natively.
  </action>
  <verify>
    <automated>python3 -c 'import yaml; yaml.safe_load(open("cka-sim/packs/workloads-scheduling/01-deployment-requests/expected-symptom.yaml"))'</automated>
  </verify>
  <acceptance_criteria>
    - YAML valid.
    - Deployment Available + image claims present: `grep -c 'Available")\].status: "True"' ...01.../expected-symptom.yaml` returns 1; `grep -c 'image: nginx:1.27' ...01.../expected-symptom.yaml` returns 1.
    - absent_resources serviceaccount: `grep -c 'kind: serviceaccount' ...01.../expected-symptom.yaml` returns 1.
    - yamllint-clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: workloads-scheduling/02-rolling-update-rollback/expected-symptom.yaml</name>
  <files>cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/expected-symptom.yaml</files>
  <action>
    ```yaml
    # workloads-scheduling/02-rolling-update-rollback — symptom: web Deployment
    # exists, Available, on nginx:1.25 (pre-update state).
    question: workloads-scheduling-rolling-update-rollback
    namespace: ${CKA_SIM_LAB_NS}
    resources:
      - kind: deploy
        name: web
        namespace: ${CKA_SIM_LAB_NS}
        expect:
          status.conditions[?(@.type=="Available")].status: "True"
          spec.template.spec.containers[0].image: nginx:1.25
    ```
  </action>
  <verify>
    <automated>python3 -c 'import yaml; yaml.safe_load(open("cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/expected-symptom.yaml"))'</automated>
  </verify>
  <acceptance_criteria>
    - YAML valid; image: nginx:1.25 claim present; yamllint-clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: workloads-scheduling/03-configmap-secret-env-volume/expected-symptom.yaml</name>
  <files>cka-sim/packs/workloads-scheduling/03-configmap-secret-env-volume/expected-symptom.yaml</files>
  <action>
    Question.md: ConfigMap q03-app-config + Secret q03-app-secret already exist; Pod q03-app is candidate-authored.

    ```yaml
    # workloads-scheduling/03-configmap-secret-env-volume — symptom: ConfigMap
    # q03-app-config + Secret q03-app-secret pre-exist; Pod q03-app is
    # candidate-authored.
    question: workloads-scheduling-configmap-secret-env-volume
    namespace: ${CKA_SIM_LAB_NS}
    resources:
      - kind: configmap
        name: q03-app-config
        namespace: ${CKA_SIM_LAB_NS}
        expect: {}
      - kind: secret
        name: q03-app-secret
        namespace: ${CKA_SIM_LAB_NS}
        expect: {}
    absent_resources:
      - kind: pod
        name: q03-app
        namespace: ${CKA_SIM_LAB_NS}
    ```
  </action>
  <verify>
    <automated>python3 -c 'import yaml; yaml.safe_load(open("cka-sim/packs/workloads-scheduling/03-configmap-secret-env-volume/expected-symptom.yaml"))'</automated>
  </verify>
  <acceptance_criteria>
    - YAML valid; configmap + secret presence + pod absent; yamllint-clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: workloads-scheduling/04-hpa-metrics-server/expected-symptom.yaml</name>
  <files>cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/expected-symptom.yaml</files>
  <action>
    Question.md: Deployment q04-load with CPU+memory requests already set; kubectl top fails (metrics-server is candidate-installed); HPA candidate-authored.

    ```yaml
    # workloads-scheduling/04-hpa-metrics-server — symptom: q04-load
    # Deployment exists with cpu/memory requests; HPA q04-load is
    # candidate-authored.
    question: workloads-scheduling-hpa-metrics-server
    namespace: ${CKA_SIM_LAB_NS}
    resources:
      - kind: deploy
        name: q04-load
        namespace: ${CKA_SIM_LAB_NS}
        expect:
          status.conditions[?(@.type=="Available")].status: "True"
    absent_resources:
      - kind: hpa
        name: q04-load
        namespace: ${CKA_SIM_LAB_NS}
    ```
  </action>
  <verify>
    <automated>python3 -c 'import yaml; yaml.safe_load(open("cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/expected-symptom.yaml"))'</automated>
  </verify>
  <acceptance_criteria>
    - YAML valid; HPA absent claim; yamllint-clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: workloads-scheduling/05-daemonset/expected-symptom.yaml</name>
  <files>cka-sim/packs/workloads-scheduling/05-daemonset/expected-symptom.yaml</files>
  <action>
    Question.md: candidate creates DaemonSet q05-node-agent. setup.sh seeds nothing in this ns beyond the ns itself.

    ```yaml
    # workloads-scheduling/05-daemonset — symptom: lab ns exists; q05-node-agent
    # DaemonSet is candidate-authored (absent at setup).
    question: workloads-scheduling-daemonset
    namespace: ${CKA_SIM_LAB_NS}
    resources:
      - kind: namespace
        name: ${CKA_SIM_LAB_NS}
        expect:
          status.phase: Active
    absent_resources:
      - kind: daemonset
        name: q05-node-agent
        namespace: ${CKA_SIM_LAB_NS}
    ```
  </action>
  <verify>
    <automated>python3 -c 'import yaml; yaml.safe_load(open("cka-sim/packs/workloads-scheduling/05-daemonset/expected-symptom.yaml"))'</automated>
  </verify>
  <acceptance_criteria>
    - YAML valid; DaemonSet absent_resources claim; yamllint-clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: workloads-scheduling/06-static-pod/expected-symptom.yaml</name>
  <files>cka-sim/packs/workloads-scheduling/06-static-pod/expected-symptom.yaml</files>
  <action>
    Question.md: candidate SSHs to node-01 + drops a static pod manifest. Mirror Pod q06-static-nginx-node-01 in default ns is the post-candidate state. At setup, the mirror pod does NOT exist.

    Static-pod questions interact with the host filesystem on a worker node. In CI (kind cluster, no SSH access), the candidate's setup just verifies the namespace exists; the mirror pod absence is the meaningful claim.

    ```yaml
    # workloads-scheduling/06-static-pod — symptom: lab ns exists; q06-static-nginx
    # mirror pod is candidate-authored on node-01. Setup state has no mirror pod.
    # Note: kind clusters in CI have no SSH path to a node; the lint here only
    # verifies the symptom (mirror pod absent at setup), not the candidate fix.
    question: workloads-scheduling-static-pod
    namespace: ${CKA_SIM_LAB_NS}
    resources:
      - kind: namespace
        name: ${CKA_SIM_LAB_NS}
        expect:
          status.phase: Active
    absent_resources:
      - kind: pod
        name: q06-static-nginx-node-01
        namespace: default
    ```
  </action>
  <verify>
    <automated>python3 -c 'import yaml; yaml.safe_load(open("cka-sim/packs/workloads-scheduling/06-static-pod/expected-symptom.yaml"))'</automated>
  </verify>
  <acceptance_criteria>
    - YAML valid; mirror-pod absent_resources claim; yamllint-clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 7: workloads-scheduling/07-native-sidecar/expected-symptom.yaml</name>
  <files>cka-sim/packs/workloads-scheduling/07-native-sidecar/expected-symptom.yaml</files>
  <action>
    Question.md: Deployment q07-app exists; sidecar log-tailer currently declared in spec.containers (wrong place for v1.35 native sidecar). Candidate moves it to spec.initContainers with restartPolicy: Always.

    ```yaml
    # workloads-scheduling/07-native-sidecar — symptom: q07-app Deployment
    # has app + log-tailer both in spec.containers (pre-fix state). Candidate
    # moves log-tailer to spec.initContainers with restartPolicy: Always.
    question: workloads-scheduling-native-sidecar
    namespace: ${CKA_SIM_LAB_NS}
    resources:
      - kind: deploy
        name: q07-app
        namespace: ${CKA_SIM_LAB_NS}
        expect:
          status.conditions[?(@.type=="Available")].status: "True"
          spec.template.spec.containers[0].image: nginx:1.27
          spec.template.spec.containers[1].image: busybox:1.36
    ```

    Note: claims TWO containers in spec.containers (app + log-tailer at index 0 and 1). After candidate fix the second container moves out, but the post-setup state has both there.
  </action>
  <verify>
    <automated>python3 -c 'import yaml; yaml.safe_load(open("cka-sim/packs/workloads-scheduling/07-native-sidecar/expected-symptom.yaml"))'</automated>
  </verify>
  <acceptance_criteria>
    - YAML valid; both container images claimed; yamllint-clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 8: workloads-scheduling/08-nodeselector-affinity-taints/expected-symptom.yaml</name>
  <files>cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/expected-symptom.yaml</files>
  <action>
    Question.md: Deployment q08-gpu-sim exists; first non-control-plane worker is tainted gpu=true:NoSchedule but does NOT carry label gpu=true. Replicas stay Pending until candidate fixes the deployment.

    ```yaml
    # workloads-scheduling/08-nodeselector-affinity-taints — symptom: q08-gpu-sim
    # Deployment exists; first non-CP worker is tainted gpu=true:NoSchedule but
    # does not carry the gpu=true label. Replicas Pending until candidate adds
    # toleration + nodeSelector.
    question: workloads-scheduling-nodeselector-affinity-taints
    namespace: ${CKA_SIM_LAB_NS}
    resources:
      - kind: deploy
        name: q08-gpu-sim
        namespace: ${CKA_SIM_LAB_NS}
        expect:
          status.conditions[?(@.type=="Available")].status: "False"
    ```

    The Deployment is Available=False because pods can't be scheduled; this is the question's claimed symptom.
  </action>
  <verify>
    <automated>python3 -c 'import yaml; yaml.safe_load(open("cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/expected-symptom.yaml"))'</automated>
  </verify>
  <acceptance_criteria>
    - YAML valid; Available=False claim; yamllint-clean.
  </acceptance_criteria>
</task>

</tasks>

<verification>
After 8 tasks: each YAML parses; yamllint-clean across the validate.yml ruleset; all 8 derive from question.md (NOT setup.sh) claims; live-cluster end-to-end deferred to plan 07 if no local cluster.
</verification>

<success_criteria>
- 8 expected-symptom.yaml files shipped, one per workloads-scheduling question.
- All schema-conformant + yamllint-clean.
- No file outside the 8 listed is touched.
</success_criteria>

<output>
Create `.planning/phases/15-live-cluster-symptom-diff-ci/15-03-SUMMARY.md` with files-shipped + 1-line per question.
</output>
