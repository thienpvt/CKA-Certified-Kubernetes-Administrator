---
phase: 15-live-cluster-symptom-diff-ci
plan: 05
type: execute
wave: 2
depends_on: [01]
files_modified:
  - cka-sim/packs/cluster-architecture/01-rbac-viewer/expected-symptom.yaml
  - cka-sim/packs/cluster-architecture/02-etcd-backup-restore/expected-symptom.yaml
  - cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/expected-symptom.yaml
  - cka-sim/packs/cluster-architecture/04-pss-enforce/expected-symptom.yaml
  - cka-sim/packs/cluster-architecture/05-audit-policy/expected-symptom.yaml
  - cka-sim/packs/cluster-architecture/06-crd-basics/expected-symptom.yaml
  - cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/expected-symptom.yaml
  - cka-sim/packs/cluster-architecture/08-priorityclass/expected-symptom.yaml
autonomous: true
requirements: [CI-01]
must_haves:
  truths:
    - "All 8 cluster-architecture questions ship an expected-symptom.yaml derived from question.md's claimed symptom — NOT from running setup.sh."
    - "Sandbox/file-only questions (02 etcd, 03 kubeadm-upgrade, 04 pss-enforce, 05 audit-policy, 07 cri-dockerd) operate on the candidate's local filesystem; their kubectl-visible setup state is just the lab ns. Open-world coverage handles the rest."
    - "Each file yamllint-clean under validate.yml ruleset; parses via python3 yaml.safe_load; uses ${CKA_SIM_LAB_NS} substitution where lab-scoped resources are referenced."
  artifacts:
    - path: cka-sim/packs/cluster-architecture/01-rbac-viewer/expected-symptom.yaml
      provides: "ServiceAccount viewer + Role pod-viewer + RoleBinding viewer-binding present (Role's rules are wrong — candidate fixes them)."
      contains: "question: cluster-architecture-rbac-viewer"
    - path: cka-sim/packs/cluster-architecture/02-etcd-backup-restore/expected-symptom.yaml
      provides: "Lab ns Active. Filesystem-only question; no kubectl-visible state beyond ns."
      contains: "question: cluster-architecture-etcd-backup-restore"
    - path: cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/expected-symptom.yaml
      provides: "Lab ns Active. Filesystem-only question."
      contains: "question: cluster-architecture-kubeadm-upgrade"
    - path: cka-sim/packs/cluster-architecture/04-pss-enforce/expected-symptom.yaml
      provides: "Lab ns Active and labelled with pod-security.kubernetes.io/enforce=restricted + enforce-version=v1.35."
      contains: "question: cluster-architecture-pss-enforce"
    - path: cka-sim/packs/cluster-architecture/05-audit-policy/expected-symptom.yaml
      provides: "Lab ns Active. Filesystem-only question (audit policy YAML edit)."
      contains: "question: cluster-architecture-audit-policy"
    - path: cka-sim/packs/cluster-architecture/06-crd-basics/expected-symptom.yaml
      provides: "Lab ns Active. CRD q06widgets.cka-sim.io is candidate-authored (absent)."
      contains: "question: cluster-architecture-crd-basics"
    - path: cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/expected-symptom.yaml
      provides: "Lab ns Active. Filesystem-only question (kubeadm-flags.env edit)."
      contains: "question: cluster-architecture-cri-dockerd-endpoint"
    - path: cka-sim/packs/cluster-architecture/08-priorityclass/expected-symptom.yaml
      provides: "PriorityClass q08-critical + q08-batch present, both globalDefault=false."
      contains: "question: cluster-architecture-priorityclass"
  key_links:
    - from: cka-sim/packs/cluster-architecture/<NN>/expected-symptom.yaml
      to: cka-sim/packs/cluster-architecture/<NN>/question.md
      via: "expect: fields encode literal symptom claims (or just lab-ns Active for filesystem-only questions)."
      pattern: "question: cluster-architecture-"
---

<objective>
Ship expected-symptom.yaml for all 8 cluster-architecture questions. Several are filesystem-only — their kubectl-visible state is minimal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-live-cluster-symptom-diff-ci/15-CONTEXT.md
@cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md
@cka-sim/packs/cluster-architecture/01-rbac-viewer/question.md
@cka-sim/packs/cluster-architecture/01-rbac-viewer/setup.sh
@cka-sim/packs/cluster-architecture/02-etcd-backup-restore/question.md
@cka-sim/packs/cluster-architecture/02-etcd-backup-restore/setup.sh
@cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/question.md
@cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/setup.sh
@cka-sim/packs/cluster-architecture/04-pss-enforce/question.md
@cka-sim/packs/cluster-architecture/04-pss-enforce/setup.sh
@cka-sim/packs/cluster-architecture/05-audit-policy/question.md
@cka-sim/packs/cluster-architecture/05-audit-policy/setup.sh
@cka-sim/packs/cluster-architecture/06-crd-basics/question.md
@cka-sim/packs/cluster-architecture/06-crd-basics/setup.sh
@cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/question.md
@cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/setup.sh
@cka-sim/packs/cluster-architecture/08-priorityclass/question.md
@cka-sim/packs/cluster-architecture/08-priorityclass/setup.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: cluster-architecture/01-rbac-viewer/expected-symptom.yaml</name>
  <files>cka-sim/packs/cluster-architecture/01-rbac-viewer/expected-symptom.yaml</files>
  <action>
    ```yaml
    # cluster-architecture/01-rbac-viewer — symptom: viewer SA + pod-viewer Role
    # + viewer-binding RoleBinding all present, but Role rules don't permit the
    # right verbs (candidate fixes that). Presence is the symptom-diff claim.
    question: cluster-architecture-rbac-viewer
    namespace: ${CKA_SIM_LAB_NS}
    resources:
      - kind: serviceaccount
        name: viewer
        namespace: ${CKA_SIM_LAB_NS}
        expect: {}
      - kind: role
        name: pod-viewer
        namespace: ${CKA_SIM_LAB_NS}
        expect: {}
      - kind: rolebinding
        name: viewer-binding
        namespace: ${CKA_SIM_LAB_NS}
        expect: {}
    ```
  </action>
  <verify>
    <automated>python3 -c 'import yaml; yaml.safe_load(open("cka-sim/packs/cluster-architecture/01-rbac-viewer/expected-symptom.yaml"))'</automated>
  </verify>
  <acceptance_criteria>
    - YAML valid; 3 RBAC resources present; yamllint-clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: cluster-architecture/02-etcd-backup-restore/expected-symptom.yaml</name>
  <files>cka-sim/packs/cluster-architecture/02-etcd-backup-restore/expected-symptom.yaml</files>
  <action>
    Filesystem-only — sandbox at /tmp/q02-etcd-backup. Lab ns is the only kubectl state.

    ```yaml
    # cluster-architecture/02-etcd-backup-restore — filesystem-only question
    # (sandbox /tmp/q02-etcd-backup). Lab ns is the only kubectl-visible state.
    question: cluster-architecture-etcd-backup-restore
    namespace: ${CKA_SIM_LAB_NS}
    resources:
      - kind: namespace
        name: ${CKA_SIM_LAB_NS}
        expect:
          status.phase: Active
    ```
  </action>
  <verify>
    <automated>python3 -c 'import yaml; yaml.safe_load(open("cka-sim/packs/cluster-architecture/02-etcd-backup-restore/expected-symptom.yaml"))'</automated>
  </verify>
  <acceptance_criteria>
    - YAML valid; ns Active; yamllint-clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: cluster-architecture/03-kubeadm-upgrade/expected-symptom.yaml</name>
  <files>cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/expected-symptom.yaml</files>
  <action>
    Filesystem-only — sandbox at /tmp/q03-kubeadm-upgrade.

    ```yaml
    # cluster-architecture/03-kubeadm-upgrade — filesystem-only question
    # (sandbox /tmp/q03-kubeadm-upgrade). No kubectl-visible state beyond ns.
    question: cluster-architecture-kubeadm-upgrade
    namespace: ${CKA_SIM_LAB_NS}
    resources:
      - kind: namespace
        name: ${CKA_SIM_LAB_NS}
        expect:
          status.phase: Active
    ```
  </action>
  <verify>
    <automated>python3 -c 'import yaml; yaml.safe_load(open("cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/expected-symptom.yaml"))'</automated>
  </verify>
  <acceptance_criteria>
    - YAML valid; ns Active; yamllint-clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: cluster-architecture/04-pss-enforce/expected-symptom.yaml</name>
  <files>cka-sim/packs/cluster-architecture/04-pss-enforce/expected-symptom.yaml</files>
  <action>
    Question.md: lab namespace is labelled `pod-security.kubernetes.io/enforce=restricted` and `pod-security.kubernetes.io/enforce-version=v1.35`. These ARE kubectl-visible namespace labels.

    ```yaml
    # cluster-architecture/04-pss-enforce — symptom: lab ns is labelled for
    # restricted Pod Security at v1.35 (the question's stated condition).
    # Filesystem-only candidate work on /tmp/q04-pss-enforce/.
    question: cluster-architecture-pss-enforce
    namespace: ${CKA_SIM_LAB_NS}
    resources:
      - kind: namespace
        name: ${CKA_SIM_LAB_NS}
        expect:
          status.phase: Active
          metadata.labels.pod-security\.kubernetes\.io/enforce: restricted
          metadata.labels.pod-security\.kubernetes\.io/enforce-version: v1.35
    ```

    The label keys contain dots and slashes; jq's dot-form requires escaping the dot in the key name. The lint script's translator passes the path as-is to jq via `.metadata.labels."pod-security.kubernetes.io/enforce"`. Document a small extension in the executor: if a path segment contains an escaped dot (`\.`) or a slash, quote it in the jq query. Implementation note for the planner: this is the only file in this plan that exercises that extension; if the executor finds the translator can't handle it, the fallback is to drop those two `metadata.labels.*` claims and keep only `status.phase: Active`. The label-correctness check then moves to plan 07's CI synthetic regression test (which can encode it via a different mechanism — direct jq from the lint script).

    DEFENSIVE FALLBACK if executor sees translator complications: replace the two metadata.labels.* lines with a single comment `# label assertion deferred — see translator note in plan 07` and just keep `status.phase: Active`. The plan still ships a valid YAML.
  </action>
  <verify>
    <automated>python3 -c 'import yaml; yaml.safe_load(open("cka-sim/packs/cluster-architecture/04-pss-enforce/expected-symptom.yaml"))'</automated>
  </verify>
  <acceptance_criteria>
    - YAML valid; ns Active claim present; yamllint-clean.
    - Either both label assertions OR a deferred-comment with status.phase only — both are acceptable per the defensive fallback.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: cluster-architecture/05-audit-policy/expected-symptom.yaml</name>
  <files>cka-sim/packs/cluster-architecture/05-audit-policy/expected-symptom.yaml</files>
  <action>
    ```yaml
    # cluster-architecture/05-audit-policy — filesystem-only question
    # (sandbox /tmp/q05-audit-policy/policy.yaml).
    question: cluster-architecture-audit-policy
    namespace: ${CKA_SIM_LAB_NS}
    resources:
      - kind: namespace
        name: ${CKA_SIM_LAB_NS}
        expect:
          status.phase: Active
    ```
  </action>
  <verify>
    <automated>python3 -c 'import yaml; yaml.safe_load(open("cka-sim/packs/cluster-architecture/05-audit-policy/expected-symptom.yaml"))'</automated>
  </verify>
  <acceptance_criteria>
    - YAML valid; ns Active; yamllint-clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: cluster-architecture/06-crd-basics/expected-symptom.yaml</name>
  <files>cka-sim/packs/cluster-architecture/06-crd-basics/expected-symptom.yaml</files>
  <action>
    Question.md: candidate creates CRD q06widgets.cka-sim.io and one Q06Widget custom resource. CRDs are cluster-scoped; symptom-diff doesn't include them in the allow-list (CONTEXT specifics — no `customresourcedefinition` kind in the 21-allow-list).

    Encode only ns Active + the absent_resources for the CRD by listing it as a `namespace`-scoped widget? No — Q06Widget is in the lab ns. Without `customresourcedefinition` in the allow-list, the CRD itself can't be checked. Use absent_resources only for the in-namespace CR — and skip the CRD assertion (preflight, not symptom).

    Actually the plan doesn't have a Q06Widget kind in the allow-list either — Q06Widget is a custom resource, kind name varies. Best move: ns Active only.

    ```yaml
    # cluster-architecture/06-crd-basics — symptom: lab ns Active. CRD
    # q06widgets.cka-sim.io is candidate-authored; CRDs and Q06Widget custom
    # resources are not in the symptom-diff allow-list, so we encode only ns.
    question: cluster-architecture-crd-basics
    namespace: ${CKA_SIM_LAB_NS}
    resources:
      - kind: namespace
        name: ${CKA_SIM_LAB_NS}
        expect:
          status.phase: Active
    ```
  </action>
  <verify>
    <automated>python3 -c 'import yaml; yaml.safe_load(open("cka-sim/packs/cluster-architecture/06-crd-basics/expected-symptom.yaml"))'</automated>
  </verify>
  <acceptance_criteria>
    - YAML valid; ns Active; yamllint-clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 7: cluster-architecture/07-cri-dockerd-endpoint/expected-symptom.yaml</name>
  <files>cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/expected-symptom.yaml</files>
  <action>
    Filesystem-only — sandbox /tmp/q07-kubelet-flags/.

    ```yaml
    # cluster-architecture/07-cri-dockerd-endpoint — filesystem-only question
    # (sandbox /tmp/q07-kubelet-flags/).
    question: cluster-architecture-cri-dockerd-endpoint
    namespace: ${CKA_SIM_LAB_NS}
    resources:
      - kind: namespace
        name: ${CKA_SIM_LAB_NS}
        expect:
          status.phase: Active
    ```
  </action>
  <verify>
    <automated>python3 -c 'import yaml; yaml.safe_load(open("cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/expected-symptom.yaml"))'</automated>
  </verify>
  <acceptance_criteria>
    - YAML valid; ns Active; yamllint-clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 8: cluster-architecture/08-priorityclass/expected-symptom.yaml</name>
  <files>cka-sim/packs/cluster-architecture/08-priorityclass/expected-symptom.yaml</files>
  <action>
    Question.md: q08-critical + q08-batch PriorityClasses both exist, neither has globalDefault=true.

    ```yaml
    # cluster-architecture/08-priorityclass — symptom: q08-critical + q08-batch
    # both exist, both globalDefault=false.
    question: cluster-architecture-priorityclass
    resources:
      - kind: priorityclass
        name: q08-critical
        expect:
          globalDefault: "false"
      - kind: priorityclass
        name: q08-batch
        expect:
          globalDefault: "false"
    ```

    Note: PriorityClass.globalDefault is a top-level boolean. jq returns booleans as `true`/`false` strings when piped through `tostring`; the lint script normalises both expected and actual to strings before comparing (per plan 01 schema: "expected jsonpath: expected_value_string"). The YAML uses the quoted string `"false"` to make this explicit.
  </action>
  <verify>
    <automated>python3 -c 'import yaml; yaml.safe_load(open("cka-sim/packs/cluster-architecture/08-priorityclass/expected-symptom.yaml"))'</automated>
  </verify>
  <acceptance_criteria>
    - YAML valid; both PriorityClasses listed with globalDefault=false; yamllint-clean.
  </acceptance_criteria>
</task>

</tasks>

<verification>
8 YAMLs parse; yamllint-clean; derive from question.md.
</verification>

<success_criteria>
- 8 expected-symptom.yaml shipped.
- No file outside the 8 listed is touched.
</success_criteria>

<output>
Create `.planning/phases/15-live-cluster-symptom-diff-ci/15-05-SUMMARY.md`.
</output>
