---
phase: 12-trap-coverage-lint-orphan-cleanup
plan: 03
type: execute
wave: 1
depends_on: [12-01-PLAN]
files_modified:
  - cka-sim/packs/storage/03-access-modes-reclaim/metadata.yaml
autonomous: true
requirements: [BUG-M02]
must_haves:
  truths:
    - "cka-sim/packs/storage/03-access-modes-reclaim/metadata.yaml `traps:` list contains exactly two entries in this order: `pv-accessmodes-mismatch`, `reclaim-policy-retain-when-delete-required`. The orphan `pvc-wrong-storageclass` is removed (no `record_trap` call site for it in the sibling grade.sh)."
    - "All other top-level fields are unchanged: `id: storage-access-modes-reclaim`, `domain: storage`, `estimatedMinutes: 9`, `verified_against: \"1.35\"`, both k8s-doc references with their target URLs and notes (Access modes matrix + Reclaim policy semantics)."
    - "After this trim, running the lint from plan 12-01 on storage/03 reports `storage/03-access-modes-reclaim: trap coverage OK` (no orphan err for this question)."
    - "The catalog entry `pvc-wrong-storageclass` STAYS in `cka-sim/traps/catalog.yaml` — it's still referenced by storage/02 and storage/04. This phase only edits per-question pointers."
    - "No grade.sh / setup.sh / reset.sh / question.md / ref-solution.sh changes — only metadata.yaml is touched."
  artifacts:
    - path: cka-sim/packs/storage/03-access-modes-reclaim/metadata.yaml
      provides: "Trimmed `traps:` list — two declared traps (`pv-accessmodes-mismatch`, `reclaim-policy-retain-when-delete-required`) matching the two `record_trap` calls in grade.sh."
      contains: "reclaim-policy-retain-when-delete-required"
  key_links:
    - from: cka-sim/packs/storage/03-access-modes-reclaim/metadata.yaml
      to: cka-sim/packs/storage/03-access-modes-reclaim/grade.sh
      via: declared `traps:` list MUST cover all `record_trap <id>` call sites in sibling grade.sh; lint-trap-coverage.sh enforces
      pattern: 'cka_sim::grade::record_trap (pv-accessmodes-mismatch|reclaim-policy-retain-when-delete-required)'
    - from: cka-sim/packs/storage/03-access-modes-reclaim/metadata.yaml
      to: cka-sim/traps/catalog.yaml
      via: dropped id `pvc-wrong-storageclass` remains in catalog (still used by storage/02 + storage/04)
      pattern: '^  - id: pvc-wrong-storageclass$'
---

<objective>
Fix BUG-M02 — drop the orphan `pvc-wrong-storageclass` from `cka-sim/packs/storage/03-access-modes-reclaim/metadata.yaml`. The grader records two traps (`pv-accessmodes-mismatch` + `reclaim-policy-retain-when-delete-required`) but the metadata declares three. The third (`pvc-wrong-storageclass`) is incorrect for this question — the question uses manual PV binding (`storageClassName=manual`), not a dynamic SC, so the storageclass-mismatch trap has no honest detector path here. (See 04-REVIEW.md WR-05: this trap was already removed from the grader as part of trap-realignment work; metadata was missed.)

Per CONTEXT.md: keep `pv-accessmodes-mismatch` + `reclaim-policy-retain-when-delete-required`. Remove `pvc-wrong-storageclass` from this question's pointer list. Catalog entry stays.

Output: a 2-line `traps:` block (two bullets), the lint from plan 12-01 reports `storage/03-access-modes-reclaim: trap coverage OK`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-trap-coverage-lint-orphan-cleanup/12-CONTEXT.md
@.planning/REQUIREMENTS.md
@cka-sim/packs/storage/03-access-modes-reclaim/metadata.yaml
@cka-sim/packs/storage/03-access-modes-reclaim/grade.sh
@cka-sim/scripts/lint-trap-coverage.sh

<interfaces>
Current cka-sim/packs/storage/03-access-modes-reclaim/metadata.yaml `traps:` block (lines 5-8):
```yaml
traps:
  - pv-accessmodes-mismatch
  - pvc-wrong-storageclass
  - reclaim-policy-retain-when-delete-required
```

Current cka-sim/packs/storage/03-access-modes-reclaim/grade.sh records exactly two traps (lines 52, 63):
```bash
cka_sim::grade::record_trap pv-accessmodes-mismatch
cka_sim::grade::record_trap reclaim-policy-retain-when-delete-required
```

No dynamic-form `record_trap "$var"` in this grade.sh — lint will perform a strict static check after the trim.

Note: WR-05 in 04-REVIEW.md (Phase 04) explicitly removed the `pvc-wrong-storageclass` detector from this grader because it described a StorageClass-level RWO limitation, but this question uses manual PV binding (`storageClassName=manual`), not a dynamic RWO-only SC. The metadata entry was the residue. Trim now closes the loop.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trim orphan from storage/03-access-modes-reclaim/metadata.yaml</name>
  <read_first>
    - cka-sim/packs/storage/03-access-modes-reclaim/metadata.yaml (current 16-line file)
    - cka-sim/packs/storage/03-access-modes-reclaim/grade.sh (confirms only `pv-accessmodes-mismatch` + `reclaim-policy-retain-when-delete-required` are recorded)
    - .planning/phases/12-trap-coverage-lint-orphan-cleanup/12-CONTEXT.md (BUG-M02 decision: drop pvc-wrong-storageclass, keep the other two)
  </read_first>
  <files>cka-sim/packs/storage/03-access-modes-reclaim/metadata.yaml</files>
  <action>
    Edit cka-sim/packs/storage/03-access-modes-reclaim/metadata.yaml. Remove line 7 (`  - pvc-wrong-storageclass`).

    BEFORE (lines 5-8):
    ```yaml
    traps:
      - pv-accessmodes-mismatch
      - pvc-wrong-storageclass
      - reclaim-policy-retain-when-delete-required
    ```

    AFTER (lines 5-7):
    ```yaml
    traps:
      - pv-accessmodes-mismatch
      - reclaim-policy-retain-when-delete-required
    ```

    Order preserved: `pv-accessmodes-mismatch` first, `reclaim-policy-retain-when-delete-required` second (matches grade.sh detector order: line 52 then line 63).

    Do NOT change anything else. The `id`, `domain`, `estimatedMinutes`, `verified_against`, and the two `references:` entries (k8s-doc Access modes matrix + Reclaim policy semantics) all stay verbatim.

    LF line endings. No CRLF. No trailing whitespace on any line. Final newline at EOF preserved.
  </action>
  <verify>
    <automated>grep -c '^  - ' cka-sim/packs/storage/03-access-modes-reclaim/metadata.yaml</automated>
  </verify>
  <acceptance_criteria>
    - The `traps:` list has exactly two entries in the expected order: `awk '/^traps:$/,/^[a-zA-Z]/' cka-sim/packs/storage/03-access-modes-reclaim/metadata.yaml` shows `traps:` then `  - pv-accessmodes-mismatch` then `  - reclaim-policy-retain-when-delete-required` then `references:`.
    - Both kept entries present: `grep -c '^  - pv-accessmodes-mismatch$' cka-sim/packs/storage/03-access-modes-reclaim/metadata.yaml` returns 1; `grep -c '^  - reclaim-policy-retain-when-delete-required$' cka-sim/packs/storage/03-access-modes-reclaim/metadata.yaml` returns 1.
    - The orphan is gone: `grep -c '^  - pvc-wrong-storageclass$' cka-sim/packs/storage/03-access-modes-reclaim/metadata.yaml` returns 0.
    - Identity fields untouched: `grep -c '^id: storage-access-modes-reclaim$' cka-sim/packs/storage/03-access-modes-reclaim/metadata.yaml` returns 1; `grep -c '^domain: storage$' cka-sim/packs/storage/03-access-modes-reclaim/metadata.yaml` returns 1; `grep -c '^estimatedMinutes: 9$' cka-sim/packs/storage/03-access-modes-reclaim/metadata.yaml` returns 1; `grep -c '^verified_against: "1.35"$' cka-sim/packs/storage/03-access-modes-reclaim/metadata.yaml` returns 1.
    - References block untouched: `grep -c 'Access modes matrix' cka-sim/packs/storage/03-access-modes-reclaim/metadata.yaml` returns 1; `grep -c 'Reclaim policy semantics' cka-sim/packs/storage/03-access-modes-reclaim/metadata.yaml` returns 1; `grep -c 'kubernetes.io/docs/concepts/storage/persistent-volumes/#access-modes' cka-sim/packs/storage/03-access-modes-reclaim/metadata.yaml` returns 1; `grep -c 'kubernetes.io/docs/concepts/storage/persistent-volumes/#reclaiming' cka-sim/packs/storage/03-access-modes-reclaim/metadata.yaml` returns 1.
    - `pvc-wrong-storageclass` is still in the catalog (used by storage/02 + storage/04): `grep -c '^  - id: pvc-wrong-storageclass$' cka-sim/traps/catalog.yaml` returns 1.
    - Lint from plan 12-01 reports this question as clean: `bash cka-sim/scripts/lint-trap-coverage.sh 2>&1 | grep -c 'storage/03-access-modes-reclaim: trap coverage OK'` returns 1; the same run NO LONGER prints `storage/03-access-modes-reclaim/metadata.yaml.* trap 'pvc-wrong-storageclass'`.
    - No file outside metadata.yaml was changed: `git diff --name-only -- cka-sim/packs/storage/03-access-modes-reclaim/ | sort` lists only `cka-sim/packs/storage/03-access-modes-reclaim/metadata.yaml`.
  </acceptance_criteria>
  <done>storage/03 metadata.yaml `traps:` list trimmed to two entries matching the two grade.sh detectors; lint from plan 12-01 reports the question clean; identity, references, and catalog all unchanged.</done>
</task>

</tasks>

<verification>
After the task completes:

1. `bash cka-sim/scripts/lint-trap-coverage.sh` no longer lists storage/03 in the err output. The `OK` line for storage/03-access-modes-reclaim appears.
2. `bash cka-sim/scripts/test.sh` continues to pass — pack-level lints accept the trimmed metadata since the kept ids are still in the catalog.
3. The dropped trap id `pvc-wrong-storageclass` remains in cka-sim/traps/catalog.yaml — verified by `grep -c '^  - id: pvc-wrong-storageclass$' cka-sim/traps/catalog.yaml` returning 1.
4. storage/02 (BUG-M01, plan 12-02) and storage/04 (BUG-M03, plan 12-04) trims are independent — this plan can land before, after, or in parallel with them. Lint orphan count after this plan alone (vs HEAD): 4 → 3.
</verification>

<success_criteria>
- `cka-sim/packs/storage/03-access-modes-reclaim/metadata.yaml` `traps:` list contains exactly two bullets in this order: `pv-accessmodes-mismatch`, `reclaim-policy-retain-when-delete-required`.
- The lint from plan 12-01 no longer flags storage/03 as having an orphan trap.
- catalog.yaml is unchanged; the dropped id is still present (referenced by other questions).
- No file other than the one targeted metadata.yaml is modified.
</success_criteria>

<output>
After completion, create `.planning/phases/12-trap-coverage-lint-orphan-cleanup/12-03-SUMMARY.md` documenting:
- File modified: cka-sim/packs/storage/03-access-modes-reclaim/metadata.yaml
- Trap entry dropped: pvc-wrong-storageclass
- Trap entries kept: pv-accessmodes-mismatch, reclaim-policy-retain-when-delete-required
- Lint result for storage/03 after trim: trap coverage OK
- Cross-reference: WR-05 (Phase 04) removed the detector from grade.sh; this plan closes the loop on the metadata pointer
</output>
