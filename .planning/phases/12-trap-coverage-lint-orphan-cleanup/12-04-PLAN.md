---
phase: 12-trap-coverage-lint-orphan-cleanup
plan: 04
type: execute
wave: 1
depends_on: [12-01-PLAN]
files_modified:
  - cka-sim/packs/storage/04-csi-volumesnapshot/metadata.yaml
autonomous: true
requirements: [BUG-M03]
must_haves:
  truths:
    - "cka-sim/packs/storage/04-csi-volumesnapshot/metadata.yaml `traps:` list contains exactly two entries in this order: `csi-snapshot-wrong-driver`, `pvc-wrong-storageclass`. The orphan `reclaim-policy-delete-data-loss` is removed (no `record_trap` call site for it in the sibling grade.sh and no candidate signal exists in this question)."
    - "All other top-level fields are unchanged: `id: storage-csi-volumesnapshot`, `domain: storage`, `estimatedMinutes: 9`, `verified_against: \"1.35\"`, both k8s-doc references with their target URLs and notes (VolumeSnapshot + VolumeSnapshotClass concepts + local-path-provisioner)."
    - "After this trim, running the lint from plan 12-01 on storage/04 reports `storage/04-csi-volumesnapshot: trap coverage OK` (no orphan err for this question)."
    - "The catalog entry `reclaim-policy-delete-data-loss` STAYS in `cka-sim/traps/catalog.yaml` — this phase only edits per-question pointers (per CONTEXT.md decision)."
    - "No grade.sh / setup.sh / reset.sh / question.md / ref-solution.sh changes — only metadata.yaml is touched. The grade.sh comment at lines 57-62 (which already explains why no detector exists for this trap in this question) remains accurate."
  artifacts:
    - path: cka-sim/packs/storage/04-csi-volumesnapshot/metadata.yaml
      provides: "Trimmed `traps:` list — two declared traps (`csi-snapshot-wrong-driver`, `pvc-wrong-storageclass`) matching the two `record_trap` calls in grade.sh."
      contains: "csi-snapshot-wrong-driver"
  key_links:
    - from: cka-sim/packs/storage/04-csi-volumesnapshot/metadata.yaml
      to: cka-sim/packs/storage/04-csi-volumesnapshot/grade.sh
      via: declared `traps:` list MUST cover all `record_trap <id>` call sites in sibling grade.sh; lint-trap-coverage.sh enforces
      pattern: 'cka_sim::grade::record_trap (csi-snapshot-wrong-driver|pvc-wrong-storageclass)'
    - from: cka-sim/packs/storage/04-csi-volumesnapshot/metadata.yaml
      to: cka-sim/traps/catalog.yaml
      via: dropped id `reclaim-policy-delete-data-loss` remains in catalog (still part of shared taxonomy)
      pattern: '^  - id: reclaim-policy-delete-data-loss$'
---

<objective>
Fix BUG-M03 — drop the orphan `reclaim-policy-delete-data-loss` from `cka-sim/packs/storage/04-csi-volumesnapshot/metadata.yaml`. The grader records two traps (`csi-snapshot-wrong-driver` + `pvc-wrong-storageclass`) but the metadata declares three. The third has no detector here because, as the existing comment in grade.sh (lines 57-62) explains, "the VSC itself has no durable intent field" — there is no candidate signal that distinguishes Retain-vs-Delete intent in this exercise. The catalog entry is still meaningful as shared taxonomy; only the per-question pointer is wrong.

Per CONTEXT.md: keep `csi-snapshot-wrong-driver` + `pvc-wrong-storageclass`. Remove `reclaim-policy-delete-data-loss` from this question's pointer list. Catalog entry stays.

Output: a 2-line `traps:` block (two bullets), the lint from plan 12-01 reports `storage/04-csi-volumesnapshot: trap coverage OK`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-trap-coverage-lint-orphan-cleanup/12-CONTEXT.md
@.planning/REQUIREMENTS.md
@cka-sim/packs/storage/04-csi-volumesnapshot/metadata.yaml
@cka-sim/packs/storage/04-csi-volumesnapshot/grade.sh
@cka-sim/scripts/lint-trap-coverage.sh

<interfaces>
Current cka-sim/packs/storage/04-csi-volumesnapshot/metadata.yaml `traps:` block (lines 5-8):
```yaml
traps:
  - csi-snapshot-wrong-driver
  - pvc-wrong-storageclass
  - reclaim-policy-delete-data-loss
```

Current cka-sim/packs/storage/04-csi-volumesnapshot/grade.sh records exactly two traps (lines 45, 54):
```bash
cka_sim::grade::record_trap csi-snapshot-wrong-driver
cka_sim::grade::record_trap pvc-wrong-storageclass
```

Both are literal-form calls — no dynamic `record_trap "$var"` in this grader. The lint will perform a strict static check after the trim.

The grade.sh contains an explicit comment (lines 57-62) documenting why no detector exists for `reclaim-policy-delete-data-loss` in this question:
```
# Note: the third metadata-declared trap (reclaim-policy-delete-data-loss)
# has no grade.sh detector -- it is conceptual/documentation-only, listed
# in metadata.yaml for PACK-06 trap-seed completeness. The same pattern
# is used by storage/05 (declares 3 traps, detects 2). A reliable live
# detector for Retain-vs-Delete intent would need a candidate signal this
# question does not supply (the VSC itself has no durable intent field).
```

That comment becomes mildly stale once the metadata pointer is trimmed (it now describes a former state, not the current one). Per CONTEXT.md decision the comment is NOT touched in this phase — leaving it accurate-but-historical avoids creeping the patch beyond the metadata trim and is acceptable because the comment is explanatory only and does not gate any behaviour. (A future cleanup phase can refresh the wording; out of scope for v1.0.1.)

CONTEXT.md note about "storage/04 uses record_trap \"$hit\"" appears to be a CONTEXT-author transcription slip — the actual dynamic-id callers per `grep -rn 'record_trap "\$' cka-sim/packs/` are workloads-scheduling/01..03/05, storage/01/05/06, cluster-architecture/01/04, troubleshooting/01, services-networking/01. storage/04's grade.sh uses LITERAL ids, so the lint applies the strict static check here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trim orphan from storage/04-csi-volumesnapshot/metadata.yaml</name>
  <read_first>
    - cka-sim/packs/storage/04-csi-volumesnapshot/metadata.yaml (current 16-line file)
    - cka-sim/packs/storage/04-csi-volumesnapshot/grade.sh (confirms only `csi-snapshot-wrong-driver` + `pvc-wrong-storageclass` are recorded; the lines 57-62 comment explains why the third was metadata-only)
    - .planning/phases/12-trap-coverage-lint-orphan-cleanup/12-CONTEXT.md (BUG-M03 decision: drop reclaim-policy-delete-data-loss, keep the other two)
  </read_first>
  <files>cka-sim/packs/storage/04-csi-volumesnapshot/metadata.yaml</files>
  <action>
    Edit cka-sim/packs/storage/04-csi-volumesnapshot/metadata.yaml. Remove line 8 (`  - reclaim-policy-delete-data-loss`).

    BEFORE (lines 5-8):
    ```yaml
    traps:
      - csi-snapshot-wrong-driver
      - pvc-wrong-storageclass
      - reclaim-policy-delete-data-loss
    ```

    AFTER (lines 5-7):
    ```yaml
    traps:
      - csi-snapshot-wrong-driver
      - pvc-wrong-storageclass
    ```

    Order preserved: `csi-snapshot-wrong-driver` first, `pvc-wrong-storageclass` second (matches grade.sh detector order: line 45 then line 54).

    Do NOT change anything else. The `id`, `domain`, `estimatedMinutes`, `verified_against`, and the two `references:` entries (k8s-doc VolumeSnapshot concepts + local-path-provisioner) all stay verbatim.

    Do NOT touch grade.sh — the lines 57-62 comment is explanatory and not load-bearing; refreshing its wording is deferred (out of scope per CONTEXT.md "metadata.yaml only" decision).

    LF line endings. No CRLF. No trailing whitespace on any line. Final newline at EOF preserved.
  </action>
  <verify>
    <automated>grep -c '^  - ' cka-sim/packs/storage/04-csi-volumesnapshot/metadata.yaml</automated>
  </verify>
  <acceptance_criteria>
    - The `traps:` list has exactly two entries in the expected order: `awk '/^traps:$/,/^[a-zA-Z]/' cka-sim/packs/storage/04-csi-volumesnapshot/metadata.yaml` shows `traps:` then `  - csi-snapshot-wrong-driver` then `  - pvc-wrong-storageclass` then `references:`.
    - Both kept entries present: `grep -c '^  - csi-snapshot-wrong-driver$' cka-sim/packs/storage/04-csi-volumesnapshot/metadata.yaml` returns 1; `grep -c '^  - pvc-wrong-storageclass$' cka-sim/packs/storage/04-csi-volumesnapshot/metadata.yaml` returns 1.
    - The orphan is gone: `grep -c '^  - reclaim-policy-delete-data-loss$' cka-sim/packs/storage/04-csi-volumesnapshot/metadata.yaml` returns 0.
    - Identity fields untouched: `grep -c '^id: storage-csi-volumesnapshot$' cka-sim/packs/storage/04-csi-volumesnapshot/metadata.yaml` returns 1; `grep -c '^domain: storage$' cka-sim/packs/storage/04-csi-volumesnapshot/metadata.yaml` returns 1; `grep -c '^estimatedMinutes: 9$' cka-sim/packs/storage/04-csi-volumesnapshot/metadata.yaml` returns 1; `grep -c '^verified_against: "1.35"$' cka-sim/packs/storage/04-csi-volumesnapshot/metadata.yaml` returns 1.
    - References block untouched: `grep -c 'VolumeSnapshot + VolumeSnapshotClass concepts' cka-sim/packs/storage/04-csi-volumesnapshot/metadata.yaml` returns 1; `grep -c 'local-path-provisioner StorageClass used by the lab PVC' cka-sim/packs/storage/04-csi-volumesnapshot/metadata.yaml` returns 1; `grep -c 'kubernetes.io/docs/concepts/storage/volume-snapshots/' cka-sim/packs/storage/04-csi-volumesnapshot/metadata.yaml` returns 1; `grep -c 'github.com/rancher/local-path-provisioner' cka-sim/packs/storage/04-csi-volumesnapshot/metadata.yaml` returns 1.
    - `reclaim-policy-delete-data-loss` is still in the catalog (shared taxonomy): `grep -c '^  - id: reclaim-policy-delete-data-loss$' cka-sim/traps/catalog.yaml` returns 1.
    - grade.sh is untouched: `git diff --name-only -- cka-sim/packs/storage/04-csi-volumesnapshot/grade.sh | wc -l` returns 0.
    - Lint from plan 12-01 reports this question as clean: `bash cka-sim/scripts/lint-trap-coverage.sh 2>&1 | grep -c 'storage/04-csi-volumesnapshot: trap coverage OK'` returns 1; the same run NO LONGER prints `storage/04-csi-volumesnapshot/metadata.yaml.* trap 'reclaim-policy-delete-data-loss'`.
    - No file outside metadata.yaml was changed: `git diff --name-only -- cka-sim/packs/storage/04-csi-volumesnapshot/ | sort` lists only `cka-sim/packs/storage/04-csi-volumesnapshot/metadata.yaml`.
  </acceptance_criteria>
  <done>storage/04 metadata.yaml `traps:` list trimmed to two entries matching the two grade.sh detectors; lint from plan 12-01 reports the question clean; identity, references, grade.sh, and catalog all unchanged.</done>
</task>

</tasks>

<verification>
After the task completes:

1. `bash cka-sim/scripts/lint-trap-coverage.sh` no longer lists storage/04 in the err output. The `OK` line for storage/04-csi-volumesnapshot appears.
2. `bash cka-sim/scripts/test.sh` continues to pass — pack-level lints accept the trimmed metadata since the kept ids are still in the catalog.
3. The dropped trap id `reclaim-policy-delete-data-loss` remains in cka-sim/traps/catalog.yaml — verified by `grep -c '^  - id: reclaim-policy-delete-data-loss$' cka-sim/traps/catalog.yaml` returning 1.
4. storage/02 (BUG-M01, plan 12-02) and storage/03 (BUG-M02, plan 12-03) trims are independent — this plan can land before, after, or in parallel with them. Lint orphan count after this plan alone (vs HEAD): 4 → 3.
5. After all three trim plans (12-02, 12-03, 12-04) land, the lint exits 0 across the full pack tree — that final-green confirmation is plan 12-05's job.
</verification>

<success_criteria>
- `cka-sim/packs/storage/04-csi-volumesnapshot/metadata.yaml` `traps:` list contains exactly two bullets in this order: `csi-snapshot-wrong-driver`, `pvc-wrong-storageclass`.
- The lint from plan 12-01 no longer flags storage/04 as having an orphan trap.
- catalog.yaml is unchanged; the dropped id is still present in shared taxonomy.
- grade.sh is untouched (the lines 57-62 comment about the missing detector is left as historical context — refresh deferred).
- No file other than the one targeted metadata.yaml is modified.
</success_criteria>

<output>
After completion, create `.planning/phases/12-trap-coverage-lint-orphan-cleanup/12-04-SUMMARY.md` documenting:
- File modified: cka-sim/packs/storage/04-csi-volumesnapshot/metadata.yaml
- Trap entry dropped: reclaim-policy-delete-data-loss
- Trap entries kept: csi-snapshot-wrong-driver, pvc-wrong-storageclass
- Lint result for storage/04 after trim: trap coverage OK
- Note on the grade.sh lines 57-62 comment: left as historical context (refresh deferred per CONTEXT.md scoping)
</output>
