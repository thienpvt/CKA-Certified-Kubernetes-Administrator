---
phase: 12-trap-coverage-lint-orphan-cleanup
plan: 02
type: execute
wave: 1
depends_on: [12-01-PLAN]
files_modified:
  - cka-sim/packs/storage/02-storageclass-dynamic/metadata.yaml
autonomous: true
requirements: [BUG-M01]
must_haves:
  truths:
    - "cka-sim/packs/storage/02-storageclass-dynamic/metadata.yaml `traps:` list contains exactly one entry: `pvc-wrong-storageclass`. The two orphans `pvc-accessmode-rwx-on-rwo-sc` and `hostpath-pv-without-nodeaffinity` are removed (no `record_trap` call site for either in the sibling grade.sh)."
    - "All other top-level fields are unchanged: `id: storage-storageclass-dynamic`, `domain: storage`, `estimatedMinutes: 7`, `verified_against: \"1.35\"`, both k8s-doc references with their target URLs and notes."
    - "After this trim, running the lint from plan 12-01 on storage/02 reports `storage/02-storageclass-dynamic: trap coverage OK` (no orphan err for this question). The two trims for storage/03 and storage/04 land in parallel plans 12-03 and 12-04."
    - "The catalog entries `pvc-accessmode-rwx-on-rwo-sc` and `hostpath-pv-without-nodeaffinity` STAY in `cka-sim/traps/catalog.yaml` — this phase only edits per-question pointers (per CONTEXT.md decision)."
    - "No grade.sh / setup.sh / reset.sh / question.md / ref-solution.sh changes — only metadata.yaml is touched."
  artifacts:
    - path: cka-sim/packs/storage/02-storageclass-dynamic/metadata.yaml
      provides: "Trimmed `traps:` list — one declared trap (`pvc-wrong-storageclass`) matching the sole `record_trap` call in grade.sh."
      contains: "pvc-wrong-storageclass"
  key_links:
    - from: cka-sim/packs/storage/02-storageclass-dynamic/metadata.yaml
      to: cka-sim/packs/storage/02-storageclass-dynamic/grade.sh
      via: declared `traps:` list MUST cover all `record_trap <id>` call sites in sibling grade.sh; lint-trap-coverage.sh enforces
      pattern: 'cka_sim::grade::record_trap pvc-wrong-storageclass'
    - from: cka-sim/packs/storage/02-storageclass-dynamic/metadata.yaml
      to: cka-sim/traps/catalog.yaml
      via: trap ids declared here must exist in catalog (lint-packs.sh enforces); catalog entries for the dropped ids stay intact
      pattern: '^  - id: pvc-wrong-storageclass$'
---

<objective>
Fix BUG-M01 — drop the two orphan trap entries in `cka-sim/packs/storage/02-storageclass-dynamic/metadata.yaml` whose ids have no matching `record_trap` call in the sibling grade.sh. The grader records exactly one trap (`pvc-wrong-storageclass`) but the metadata declares three; the other two (`pvc-accessmode-rwx-on-rwo-sc`, `hostpath-pv-without-nodeaffinity`) are vestigial from earlier seeded-trap-completeness work and now read as documentation noise.

Per CONTEXT.md: keep `pvc-wrong-storageclass`. Remove the other two from this question's pointer list. Catalog entries stay (they're still used by other questions and the shared taxonomy).

Output: a 1-line `traps:` block under `traps:` (one bullet), the lint from plan 12-01 reports `storage/02-storageclass-dynamic: trap coverage OK`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-trap-coverage-lint-orphan-cleanup/12-CONTEXT.md
@.planning/REQUIREMENTS.md
@cka-sim/packs/storage/02-storageclass-dynamic/metadata.yaml
@cka-sim/packs/storage/02-storageclass-dynamic/grade.sh
@cka-sim/scripts/lint-trap-coverage.sh

<interfaces>
Current cka-sim/packs/storage/02-storageclass-dynamic/metadata.yaml `traps:` block (lines 5-8):
```yaml
traps:
  - pvc-wrong-storageclass
  - pvc-accessmode-rwx-on-rwo-sc
  - hostpath-pv-without-nodeaffinity
```

Current cka-sim/packs/storage/02-storageclass-dynamic/grade.sh records exactly one trap (line 36):
```bash
cka_sim::grade::record_trap pvc-wrong-storageclass
```

No dynamic-form `record_trap "$var"` in this grade.sh — lint will perform a strict static check after the trim.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trim orphans from storage/02-storageclass-dynamic/metadata.yaml</name>
  <read_first>
    - cka-sim/packs/storage/02-storageclass-dynamic/metadata.yaml (current 16-line file)
    - cka-sim/packs/storage/02-storageclass-dynamic/grade.sh (confirms only `pvc-wrong-storageclass` is recorded)
    - .planning/phases/12-trap-coverage-lint-orphan-cleanup/12-CONTEXT.md (BUG-M01 decision: drop 2, keep 1)
  </read_first>
  <files>cka-sim/packs/storage/02-storageclass-dynamic/metadata.yaml</files>
  <action>
    Edit cka-sim/packs/storage/02-storageclass-dynamic/metadata.yaml. Remove lines 7 and 8 of the current file (`  - pvc-accessmode-rwx-on-rwo-sc` and `  - hostpath-pv-without-nodeaffinity`).

    BEFORE (lines 5-8):
    ```yaml
    traps:
      - pvc-wrong-storageclass
      - pvc-accessmode-rwx-on-rwo-sc
      - hostpath-pv-without-nodeaffinity
    ```

    AFTER (lines 5-6):
    ```yaml
    traps:
      - pvc-wrong-storageclass
    ```

    Do NOT change anything else. The `id`, `domain`, `estimatedMinutes`, `verified_against`, and the two `references:` entries (k8s-doc StorageClass concepts + Dynamic volume provisioning) all stay verbatim.

    LF line endings. No CRLF. No trailing whitespace on any line. Final newline at EOF preserved.
  </action>
  <verify>
    <automated>grep -c '^  - ' cka-sim/packs/storage/02-storageclass-dynamic/metadata.yaml</automated>
  </verify>
  <acceptance_criteria>
    - The `traps:` list has exactly one entry: `grep -A2 '^traps:$' cka-sim/packs/storage/02-storageclass-dynamic/metadata.yaml` shows `traps:` followed by `  - pvc-wrong-storageclass` followed by either `references:` or end-of-block.
    - `pvc-wrong-storageclass` is preserved: `grep -c '^  - pvc-wrong-storageclass$' cka-sim/packs/storage/02-storageclass-dynamic/metadata.yaml` returns 1.
    - The two orphans are gone: `grep -c '^  - pvc-accessmode-rwx-on-rwo-sc$' cka-sim/packs/storage/02-storageclass-dynamic/metadata.yaml` returns 0; `grep -c '^  - hostpath-pv-without-nodeaffinity$' cka-sim/packs/storage/02-storageclass-dynamic/metadata.yaml` returns 0.
    - Identity fields untouched: `grep -c '^id: storage-storageclass-dynamic$' cka-sim/packs/storage/02-storageclass-dynamic/metadata.yaml` returns 1; `grep -c '^domain: storage$' cka-sim/packs/storage/02-storageclass-dynamic/metadata.yaml` returns 1; `grep -c '^estimatedMinutes: 7$' cka-sim/packs/storage/02-storageclass-dynamic/metadata.yaml` returns 1; `grep -c '^verified_against: "1.35"$' cka-sim/packs/storage/02-storageclass-dynamic/metadata.yaml` returns 1.
    - References block untouched: `grep -c 'StorageClass concepts' cka-sim/packs/storage/02-storageclass-dynamic/metadata.yaml` returns 1; `grep -c 'Dynamic volume provisioning' cka-sim/packs/storage/02-storageclass-dynamic/metadata.yaml` returns 1; `grep -c 'kubernetes.io/docs/concepts/storage/storage-classes/' cka-sim/packs/storage/02-storageclass-dynamic/metadata.yaml` returns 1; `grep -c 'kubernetes.io/docs/concepts/storage/dynamic-provisioning/' cka-sim/packs/storage/02-storageclass-dynamic/metadata.yaml` returns 1.
    - Catalog is untouched: `git diff --name-only -- cka-sim/traps/catalog.yaml | wc -l` returns 0.
    - Lint from plan 12-01 reports this question as clean: `bash cka-sim/scripts/lint-trap-coverage.sh 2>&1 | grep -c 'storage/02-storageclass-dynamic: trap coverage OK'` returns 1; the same run NO LONGER prints `storage/02-storageclass-dynamic/metadata.yaml.* trap 'pvc-accessmode-rwx-on-rwo-sc'` or `... trap 'hostpath-pv-without-nodeaffinity'` orphan errors.
    - The lint's overall orphan count drops from 4 to 2 after this plan lands (storage/03 and storage/04 still each contribute 1 until plans 12-03 and 12-04 land): `bash cka-sim/scripts/lint-trap-coverage.sh 2>&1 | grep -c '2 trap-coverage error(s)'` returns 1. (Note: this acceptance assumes plans 12-03 and 12-04 have NOT yet landed; if they have, the count is lower — planner reruns the lint at execute time and updates the SUMMARY accordingly.)
    - No file outside metadata.yaml was changed: `git diff --name-only -- cka-sim/packs/storage/02-storageclass-dynamic/ | sort` lists only `cka-sim/packs/storage/02-storageclass-dynamic/metadata.yaml`.
  </acceptance_criteria>
  <done>storage/02 metadata.yaml `traps:` list trimmed to one entry; lint from plan 12-01 reports the question clean; identity, references, and catalog all unchanged.</done>
</task>

</tasks>

<verification>
After the task completes:

1. `bash cka-sim/scripts/lint-trap-coverage.sh` exits 1 (still — storage/03 and storage/04 orphans remain until their plans land), but NO LONGER lists storage/02 in the err output. The `OK` line for storage/02-storageclass-dynamic appears.
2. `bash cka-sim/scripts/test.sh` continues to pass — pack-level lints (lint-packs.sh) accept the trimmed metadata since `pvc-wrong-storageclass` is still in the catalog.
3. The two dropped trap ids (`pvc-accessmode-rwx-on-rwo-sc`, `hostpath-pv-without-nodeaffinity`) remain in cka-sim/traps/catalog.yaml — verified by `grep -c '^  - id: pvc-accessmode-rwx-on-rwo-sc$' cka-sim/traps/catalog.yaml` returning 1 and similarly for hostpath-pv-without-nodeaffinity.
</verification>

<success_criteria>
- `cka-sim/packs/storage/02-storageclass-dynamic/metadata.yaml` `traps:` list contains exactly one bullet: `pvc-wrong-storageclass`.
- The lint from plan 12-01 no longer flags storage/02 as having orphan traps.
- catalog.yaml is unchanged.
- No file other than the one targeted metadata.yaml is modified.
</success_criteria>

<output>
After completion, create `.planning/phases/12-trap-coverage-lint-orphan-cleanup/12-02-SUMMARY.md` documenting:
- File modified: cka-sim/packs/storage/02-storageclass-dynamic/metadata.yaml
- Trap entries dropped: pvc-accessmode-rwx-on-rwo-sc, hostpath-pv-without-nodeaffinity
- Trap entry kept: pvc-wrong-storageclass
- Lint result for storage/02 after trim: trap coverage OK
- Confirmation that catalog.yaml entries for dropped ids still exist (other questions / shared taxonomy)
</output>
