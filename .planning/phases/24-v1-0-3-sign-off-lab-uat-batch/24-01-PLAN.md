---
phase: 24-v1-0-3-sign-off-lab-uat-batch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cka-sim/scripts/uat-v103.sh
autonomous: true
requirements: []

must_haves:
  truths:
    - "uat-v103.sh is syntactically valid bash (bash -n exits 0)"
    - "uat-v103.sh mirrors uat-phase18-21.sh helper shape (report/skip/score_of/trap_count/reset_q/prep_baseline)"
    - "uat-v103.sh has 5 sub-checks covering DRILL-NS-01, AUDIT-W&S06, LINT-01, BLG-06, BLG-07"
    - "uat-v103.sh skips gracefully when no live cluster (kubectl cluster-info gate)"
    - "uat-v103.sh file mode is 100755 in git index"
  artifacts:
    - path: "cka-sim/scripts/uat-v103.sh"
      provides: "v1.0.3 milestone UAT driver"
      contains: "PASS=0 FAIL=0 TOTAL=0 SKIP=0"
  key_links:
    - from: "cka-sim/scripts/uat-v103.sh"
      to: "cka-sim/lib/colors.sh"
      via: "source"
      pattern: 'source.*lib/colors\.sh'
    - from: "cka-sim/scripts/uat-v103.sh"
      to: "cka-sim/lib/baseline.sh"
      via: "source"
      pattern: 'source.*lib/baseline\.sh'
---

<objective>
Author the v1.0.3 milestone UAT driver `cka-sim/scripts/uat-v103.sh`.

Purpose: Provides a single executable script that re-verifies all 5 v1.0.3 fixes on the lab cluster (or skips gracefully when no cluster is reachable). This is the evidence-gathering tool for the milestone audit doc.

Output: `cka-sim/scripts/uat-v103.sh` with executable bit set in git index.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-v1-0-3-sign-off-lab-uat-batch/24-CONTEXT.md
@cka-sim/scripts/uat-phase18-21.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author uat-v103.sh UAT driver</name>
  <files>cka-sim/scripts/uat-v103.sh</files>
  <read_first>
    - cka-sim/scripts/uat-phase18-21.sh (FULL — canonical shape to mirror exactly)
    - cka-sim/lib/colors.sh (confirm sourcing path)
    - cka-sim/lib/baseline.sh (confirm prep_baseline contract)
  </read_first>
  <action>
Create `cka-sim/scripts/uat-v103.sh` mirroring `uat-phase18-21.sh` structure byte-for-byte in its preamble and helpers. Specifics:

PREAMBLE:
- Shebang `#!/bin/bash`
- Header comment: "Phase 24 UAT — v1.0.3 Sign-Off (DRILL-NS-01, AUDIT-W&S06, LINT-01, BLG-06, BLG-07)"
- `set -uo pipefail`
- `CKA_SIM_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"` + export
- Source `lib/colors.sh`, `lib/log.sh`, `lib/baseline.sh`
- Counter vars: `PASS=0 FAIL=0 TOTAL=0 SKIP=0`

HELPERS (copy verbatim from uat-phase18-21.sh):
- `report()` — name + rc, increments PASS or FAIL + TOTAL
- `skip()` — name + reason, increments SKIP + TOTAL
- `score_of()` — grep SCORE line, sed extract
- `trap_count()` — grep -c "Trap "
- `reset_q()` — bash reset.sh + sleep 3
- `prep_baseline()` — ns + qid args, export vars, mkdir, capture

CLUSTER GATE (top-level, before sub-checks):
- `HAVE_CLUSTER=0; kubectl cluster-info >/dev/null 2>&1 && HAVE_CLUSTER=1`

BANNER:
- Box-draw banner matching uat-phase18-21.sh style

SUB-CHECKS (5 sections):

1. DRILL-NS-01 — Namespace render smoke:
   - If `HAVE_CLUSTER=0`, skip "DRILL-NS-01" "no live cluster".
   - Else: run `bash "$CKA_SIM_ROOT/bin/cka-sim" drill storage 1 --dry-run 2>/dev/null` (capture stdout). If `--dry-run` not available, use `bash "$CKA_SIM_ROOT/lib/cmd/drill.sh"` with appropriate env to render prompt only. Fallback: grep drill.sh for the parameter-expansion idiom presence.
   - Assert: output contains `cka-sim-storage-01` (resolved ns). Assert: output does NOT contain literal `${CKA_SIM_LAB_NS}`.
   - `report "DRILL-NS-01 namespace render (no literal \${CKA_SIM_LAB_NS})" $rc`

2. AUDIT-W&S06 — Audit skip gate:
   - Run `bash "$CKA_SIM_ROOT/bin/cka-sim" audit workloads-scheduling/06-static-pod 2>&1` (capture output).
   - Assert: output contains "SKIPPED" (case-insensitive grep -i). Assert: exit code is 0.
   - If HAVE_CLUSTER=0 AND audit can run without cluster (it reads metadata.yaml only for skip check): still attempt. If it requires cluster, skip.
   - `report "AUDIT-W&S06 audit emits SKIPPED + exits 0" $rc`
   - Bonus: if HAVE_CLUSTER=1, run `bash "$CKA_SIM_ROOT/bin/cka-sim" drill workloads-scheduling 6` to confirm drill-mode still works (score >=0/N via ref-solution round-trip). Report as separate sub-check.

3. LINT-01 — Regression test fires on live cluster:
   - If `HAVE_CLUSTER=0`, skip "LINT-01" "no live cluster".
   - Else: run `bash "$CKA_SIM_ROOT/tests/cases/symptom-diff-regression.sh" 2>&1` (capture output + rc).
   - Assert: rc != 0 (the test SHOULD fail — it deliberately mutates expected-symptom.yaml to prove lint catches drift).
   - Assert: output contains "expected" and "got" (the diff citation).
   - `report "LINT-01 regression test exits non-zero on live cluster" $rc`

4. BLG-06 — GHA shellcheck confirmation (deferred/noted):
   - This is a GHA-only check. The driver cannot run GHA locally.
   - `skip "BLG-06" "GHA confirmation — verify via git push + validate.yml run ID in step6-results.txt"`
   - Add a comment: "# BLG-06: Confirmed OOB via GHA push. Record run ID in cka-sim/current-tests/step6-results.txt"

5. BLG-07 — GHA bash-tests confirmation (deferred/noted):
   - Same as BLG-06 — GHA-only.
   - `skip "BLG-07" "GHA confirmation — verify via git push + bash-tests job in step6-results.txt"`
   - Add a comment: "# BLG-07: Confirmed OOB via GHA push. Record run ID in cka-sim/current-tests/step6-results.txt"

SUMMARY (copy shape from uat-phase18-21.sh):
- Box-draw summary line: "Results: $PASS passed, $FAIL failed, $SKIP skipped (of $TOTAL)"
- If FAIL==0 && SKIP matches expected (2 GHA skips + any cluster-absent skips): print guidance to record in milestone audit.
- `if (( FAIL > 0 )); then exit 1; fi; exit 0`
  </action>
  <acceptance_criteria>
    - File exists at cka-sim/scripts/uat-v103.sh
    - bash -n cka-sim/scripts/uat-v103.sh exits 0
    - File contains all 6 helper functions (report, skip, score_of, trap_count, reset_q, prep_baseline)
    - File has 5 labeled sub-check sections (DRILL-NS-01, AUDIT-W&S06, LINT-01, BLG-06, BLG-07)
    - File sources lib/colors.sh, lib/log.sh, lib/baseline.sh
  </acceptance_criteria>
  <verify>
    <automated>bash -n cka-sim/scripts/uat-v103.sh && grep -c "^report\|^skip\|^score_of\|^trap_count\|^reset_q\|^prep_baseline" cka-sim/scripts/uat-v103.sh | grep -q "^6$" && grep -c "DRILL-NS-01\|AUDIT-W&S06\|LINT-01\|BLG-06\|BLG-07" cka-sim/scripts/uat-v103.sh | grep -qE "^[5-9]"</automated>
  </verify>
  <done>uat-v103.sh is syntactically valid, contains all 6 helpers mirroring uat-phase18-21.sh, has 5 sub-check sections, sources the 3 required libs, and uses kubectl cluster-info gate for skip logic.</done>
</task>

<task type="auto">
  <name>Task 2: Set executable bit in git index</name>
  <files>cka-sim/scripts/uat-v103.sh</files>
  <read_first>
    - (none — straightforward git operation)
  </read_first>
  <action>
Run `git add cka-sim/scripts/uat-v103.sh` then `git update-index --chmod=+x cka-sim/scripts/uat-v103.sh`. Verify with `git ls-files -s cka-sim/scripts/uat-v103.sh` — must show `100755`.
  </action>
  <acceptance_criteria>
    - git ls-files -s shows 100755 for cka-sim/scripts/uat-v103.sh
  </acceptance_criteria>
  <verify>
    <automated>git ls-files -s cka-sim/scripts/uat-v103.sh | grep -q "^100755"</automated>
  </verify>
  <done>File mode is 100755 in git index.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT driver -> cluster | Driver runs kubectl commands against live cluster; untrusted cluster state |
| UAT driver -> cka-sim scripts | Sources lib files; trusts repo-local code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | T (Tampering) | uat-v103.sh cluster-info gate | accept | Driver is a verification tool run by the operator on their own cluster; no untrusted input crosses the boundary |
| T-24-02 | I (Info Disclosure) | uat-v103.sh output | accept | Output is local terminal only; no secrets in PASS/FAIL counters |
</threat_model>

<verification>
- `bash -n cka-sim/scripts/uat-v103.sh` exits 0 (syntax valid)
- `git ls-files -s cka-sim/scripts/uat-v103.sh` shows 100755
- grep confirms 6 helper functions and 5 sub-check sections present
</verification>

<success_criteria>
uat-v103.sh exists, is syntactically valid, mirrors uat-phase18-21.sh helper shape, covers all 5 v1.0.3 REQs as sub-checks (3 live-cluster + 2 GHA-deferred skips), and has executable bit set in git index.
</success_criteria>

<output>
After completion, create `.planning/phases/24-v1-0-3-sign-off-lab-uat-batch/24-01-SUMMARY.md`
</output>
