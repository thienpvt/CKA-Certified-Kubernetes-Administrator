---
phase: 07.1-grading-honesty-rebuild
plan: 12
authored: 2026-05-16
consumer: 07.1-13-PLAN.md (VERIFICATION.md author)
---

# Phase 07.1 Plan 12 — Audit-Escape Inventory

Per CONTEXT D-22: graders whose candidate-driven work cannot be observed via the kubectl-resource baseline schema (file-edits, node-side actions, ConfigMap content changes that mirror server state) are flagged audit-escape. They are NOT refactored to use `assert_changed_since_setup` / `assert_resource_candidate_authored` / `assert_generation_delta_ge` because the helpers do not apply.

## Troubleshooting pack — Q01..Q06

| Slug | Status | Reason | Disposition |
|------|--------|--------|-------------|
| 01-deploy-svc-mismatch | refactored | Setup-collision on Service + Deployment existence assertions; behavioural endpoints_nonempty already correctly fails under trap. | weight=0 demotion of 2 existence checks; kept endpoints_nonempty + trap detectors. |
| 02-netpol-dns-egress | refactored | Setup-collision on 3 resource_exists (default-deny-egress NP, allow-web-to-api NP, api-svc). DNS/reachability behavioural checks already correctly fail under default-deny. | weight=0 demotion of 3 existence checks; added `assert_changed_since_setup networkpolicy allow-web-to-api`. |
| 03-coredns-resolution | refactored | Setup-collision on 4 resource_exists + 1 field_eq (dnsPolicy=None). DNS-resolution behavioural checks already correctly fail (invalid upstream + bad subPath). | weight=0 demotion of 5 assertions; added `assert_changed_since_setup` on ConfigMap (rv-only signal) and Deployment. |
| 04-debug-node | audit-escape | Candidate work is `kubectl debug node/<worker>` + write to `/tmp/q04-debug-node/answer.txt`. Baseline schema captures only kubectl resources; no file-tracking. The answer.txt content match against live `nodeInfo.kernelVersion` is candidate-required by setup design (setup writes empty answer.txt). | No change to grade.sh logic — all assertions already correctly require candidate work. Annotated with D-22 comment. |
| 05-static-pod-manifest | audit-escape | Candidate work is pure file-edit to `/tmp/q05-staticpod/manifest.yaml`. Setup writes a YAML with tab-indent + image-tag traps. All assertions (file exists, parseable, kind=Pod, dry-run validates) require candidate work to pass. | No change to grade.sh logic. Annotated with D-22 comment. |
| 06-broken-kubelet | audit-escape | Candidate work is pure file-edit to `/tmp/q06-kubelet-flags/kubeadm-flags.env` (proxy for node-side edits via SSH). Setup writes file with wrong CRI endpoint flags. All assertions (file exists, parseable, regex match correct endpoint) require candidate work to pass. | No change to grade.sh logic. Annotated with D-22 comment. |

## Summary

- **3 refactored** (01, 02, 03) — Wave 2 playbook applied: existence/field-eq leak demoted to weight=0; behavioural checks retained; `assert_changed_since_setup` added where the candidate must edit a setup-authored resource (NP allow-web-to-api, ConfigMap q03-coredns-corefile, Deployment q03-coredns).
- **3 audit-escape** (04, 05, 06) — All file-edit + node-action questions. The assertions correctly require candidate work because setup writes files in known-broken state and grade.sh asserts known-fixed content/structure.

## Plan 13 (VERIFICATION.md) consumption

Plan 13 must include these audit-escape questions in the verification matrix with disposition `audit-escape: file-baseline gap` so the empty-submission-scores-0/N invariant is not falsely claimed for Qs 04, 05, 06. Their xfail-style regression tests assert the current scoring contract (empty-submission scores depend on setup having written the broken state — see test cases under `cka-sim/tests/grading-honesty/troubleshooting__04..06`).

## RESEARCH backing

- RESEARCH Q5: troubleshooting questions are typically LOW risk because broken-state-gone check is candidate-driven by definition; the risk leak is in the working-state-present check potentially passing on setup-state. Confirmed for Q01-03 (working-state-present leaks fixed) and ruled out for Q04-06 (no kubectl-resource working-state to check; only file-content).
- CONTEXT D-22: audit-escape is the canonical disposition for file-baseline gaps. The grading-honesty schema (baseline.json captured kubectl resources only) does not extend to filesystem paths.
