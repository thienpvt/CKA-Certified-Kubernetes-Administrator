---
phase: 07.1-grading-honesty-rebuild
plan: 11
type: execute
wave: 5
depends_on: [07.1-07]
files_modified:
  - cka-sim/packs/cluster-architecture/01-rbac-viewer/grade.sh
  - cka-sim/packs/cluster-architecture/01-rbac-viewer/reset.sh
  - cka-sim/packs/cluster-architecture/02-etcd-backup-restore/grade.sh
  - cka-sim/packs/cluster-architecture/02-etcd-backup-restore/reset.sh
  - cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/grade.sh
  - cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/reset.sh
  - cka-sim/packs/cluster-architecture/05-audit-policy/grade.sh
  - cka-sim/packs/cluster-architecture/05-audit-policy/reset.sh
  - cka-sim/packs/cluster-architecture/06-crd-basics/grade.sh
  - cka-sim/packs/cluster-architecture/06-crd-basics/reset.sh
  - cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/grade.sh
  - cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/reset.sh
  - cka-sim/packs/cluster-architecture/08-priorityclass/grade.sh
  - cka-sim/packs/cluster-architecture/08-priorityclass/reset.sh
  - cka-sim/tests/grading-honesty/cluster-architecture__01-rbac-viewer.sh
  - cka-sim/tests/grading-honesty/cluster-architecture__02-etcd-backup-restore.sh
  - cka-sim/tests/grading-honesty/cluster-architecture__03-kubeadm-upgrade.sh
  - cka-sim/tests/grading-honesty/cluster-architecture__05-audit-policy.sh
  - cka-sim/tests/grading-honesty/cluster-architecture__06-crd-basics.sh
  - cka-sim/tests/grading-honesty/cluster-architecture__07-cri-dockerd-endpoint.sh
  - cka-sim/tests/grading-honesty/cluster-architecture__08-priorityclass.sh
autonomous: true
requirements: [AUDIT-01]
must_haves:
  truths:
    - "Empty submission of every CA Q (01,02,03,05,06,07,08) scores 0/N raw points"
    - "Reference-solution path scores N/N for every Q"
    - "File-edit and node-action questions properly handle audit-escape per RESEARCH Q3 limitations"
    - "Every CA reset.sh removes /tmp/cka-sim/<slug>/"
  artifacts:
    - path: cka-sim/tests/grading-honesty/cluster-architecture__*.sh
      provides: "7 CA regression test cases"
    - path: cka-sim/tests/fixtures/grading-honesty/cluster-architecture__*/
      provides: "7 × 2 × 2 = 28 CA fixture files"
  key_links:
    - from: cluster-architecture grade.sh files
      to: lib/grade.sh delta helpers
      via: assert_changed_since_setup OR assert_resource_candidate_authored
      pattern: 'cka_sim::grade::assert_(changed_since_setup|resource_candidate_authored)'
---

<objective>
Audit + fix 7 cluster-architecture questions (01, 02, 03, 05, 06, 07, 08 — Q04 already in Plan 06). Pack-specific risk: many CA questions are file-edit (etcd config, kubeadm-flags.env, audit-policy.yaml) or node-action (kubeadm upgrade) — kubectl-stub fixtures may not cover non-API-server checks. Audit-escape candidates likely.

Verify slug names by `ls cka-sim/packs/cluster-architecture/` before authoring; correct frontmatter if drift.

Output: Per-Q grade.sh + reset.sh fixes + 7 test cases + 28 fixture files (with audit-escape annotations where kubectl-stub is insufficient).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-CONTEXT.md
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-RESEARCH.md
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-06-SUMMARY.md
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-07-SUMMARY.md
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-08-SUMMARY.md
@cka-sim/lib/grade.sh
@cka-sim/lib/baseline.sh

<interfaces>
RESEARCH Q5 risk predictions: most CA questions are file-edit or kubectl-action; LOW risk unless setup pre-seeds the target.

Per-Q expected patterns:
- 01-rbac-viewer: candidate creates Role + RoleBinding → assert_resource_candidate_authored.
- 02-etcd-backup-restore: candidate creates snapshot file + restores → file-baseline (out of D-03 scope) → audit-escape likely.
- 03-kubeadm-upgrade: node-action; check via kubectl version → kubectl-stub OK with version fixture.
- 05-audit-policy: candidate edits /etc/kubernetes/audit-policy.yaml → file-edit, audit-escape likely (similar to Q04 with file-baseline gap).
- 06-crd-basics: candidate creates CRD → assert_resource_candidate_authored crd <name>.
- 07-cri-dockerd-endpoint: candidate edits /var/lib/kubelet/kubeadm-flags.env → file-edit, audit-escape likely.
- 08-priorityclass: candidate creates PriorityClass (cluster-scoped) → assert_resource_candidate_authored priorityclass <name>.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify slug names + audit + fix 7 CA graders</name>
  <read_first>
    - `ls cka-sim/packs/cluster-architecture/` — confirm exact slugs (frontmatter is best-guess)
    - 7 grade.sh + setup.sh + ref-solution.sh files
    - cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh (Plan 06's audit-escape pattern)
    - Plans 06, 08, 09 SUMMARYs
  </read_first>
  <action>
    Step 1 — verify slug names; correct frontmatter paths if needed.

    Step 2 — for each question, apply Plan 08 Task 1 procedure. Per-Q expectations:
    - 01-rbac-viewer: replace Role/RoleBinding existence asserts with assert_resource_candidate_authored.
    - 02-etcd-backup-restore: candidate creates snapshot file → file-baseline gap → audit-escape per D-22; demote setup-state asserts to weight=0; flag for v1.x.
    - 03-kubeadm-upgrade: kubectl version checks generally cover candidate work; verify no leaks.
    - 05-audit-policy: file-edit on /etc/kubernetes/audit-policy.yaml → audit-escape; demote + flag.
    - 06-crd-basics: assert_resource_candidate_authored crd <name>.
    - 07-cri-dockerd-endpoint: file-edit on /var/lib/kubelet/kubeadm-flags.env → audit-escape; demote + flag. Note: detect_kubelet_runtime_flag_in_kubeconfig + detect_removed_container_runtime_flag traps fire on text content (Plan 01 tagged setup-allowed) → those still record traps without ownership gate, which is the desired behavior here.
    - 08-priorityclass: assert_resource_candidate_authored priorityclass <name>.

    Step 3 — append `rm -rf /tmp/cka-sim/<slug>/` to each reset.sh.

    Step 4 — file-baseline-gap audit-escape Qs (likely 02, 05, 07): document each in 07.1-11-AUDIT-ESCAPE.md (Plan 13 input). Write the audit-escape note: "<slug>: candidate work is file-edit; current baseline schema (D-03) tracks K8s API resources only; v1.x scope expansion needed for file-mtime + sha256 baseline support."
  </action>
  <verify>
    <automated>for q in $(ls cka-sim/packs/cluster-architecture/ | grep -v '^04-' | grep -E '^[0-9]'); do bash -n cka-sim/packs/cluster-architecture/$q/grade.sh && bash -n cka-sim/packs/cluster-architecture/$q/reset.sh && grep -q "rm -rf /tmp/cka-sim/$q" cka-sim/packs/cluster-architecture/$q/reset.sh; done</automated>
  </verify>
  <acceptance_criteria>
    - 7 grade.sh files have `# Phase 07.1 AUDIT-01` header comment.
    - 7 reset.sh files contain `rm -rf /tmp/cka-sim/<slug>/`.
    - All 7 grade.sh pass bash -n.
    - File-baseline audit-escape Qs (02,05,07 expected) annotated with `# Phase 07.1 D-22 audit-escape: file-baseline gap`.
    - Audit-escape doc 07.1-11-AUDIT-ESCAPE.md committed.
  </acceptance_criteria>
  <done>7 CA graders audited + fixed; file-baseline gaps annotated.</done>
</task>

<task type="auto">
  <name>Task 2: Capture fixtures + write 7 CA test cases</name>
  <read_first>
    - cka-sim/scripts/capture-grading-fixtures.sh (Plan 07)
    - Plan 06's cluster-architecture__04-pss-enforce.sh (audit-escape test pattern)
  </read_first>
  <action>
    Same procedure as Plans 08-10 Task 2. Per-Q:
    - K8s-API questions (01, 03, 06, 08): standard kubectl-stub fixtures + standard test asserting empty=0/max + ref=max/max.
    - File-edit audit-escape questions (02, 05, 07): xfail-style test asserting current state — empty submission scores 0 because demoted-to-weight-0 setup-state asserts contribute nothing AND the file-edit assertion can't be replicated under kubectl stub. The test asserts SCORE: 0/0 (no scoring assertions remain) AND records audit-escape rationale in the test file's header comment.

    Run test.sh; all 7 cases pass (or xfail-pass for audit-escape Qs).
  </action>
  <verify>
    <automated>bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - 7 test case files with `cluster-architecture__` prefix.
    - 28 fixture files committed.
    - bash cka-sim/scripts/test.sh exits 0.
    - File-baseline audit-escape Qs (02,05,07) explicitly marked.
  </acceptance_criteria>
  <done>CA pack regressions caught in CI for all 8 questions (Q04 in Plan 06 + 7 here).</done>
</task>

</tasks>

<verification>
- 7 CA graders audited; 28 fixtures + 7 tests; audit-escape gaps documented.
</verification>

<success_criteria>
- All 8 CA questions covered.
- File-baseline gap (D-22 audit-escape) feeds Plan 13's VERIFICATION.md.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-11-SUMMARY.md`. Include audit-escape inventory.
</output>
