---
phase: 07.1-grading-honesty-rebuild
plan: 02
subsystem: grading-wiring
tags: [baseline, traps, runner-hooks, tdd, ownership-gate]
dependency_graph:
  requires: [lib/baseline.sh, grade-honesty-helpers]
  provides: [ownership-gated-detectors, runner-baseline-hooks, reset-tmp-lint]
  affects: [lib/traps.sh, lib/cmd/drill.sh, lib/cmd/exam.sh, scripts/lint-packs.sh]
tech_stack:
  added: []
  patterns: [ownership-gate-before-detection, capture-failure-abort, per-question-baseline-path]
key_files:
  created:
    - cka-sim/tests/cases/traps_ownership_gate.sh
    - cka-sim/tests/cases/baseline_runner_hook.sh
    - cka-sim/tests/fixtures/grading-honesty/ownership-gate/baseline.json
    - cka-sim/tests/fixtures/grading-honesty/ownership-gate/pod-setup-owned.json
    - cka-sim/tests/fixtures/grading-honesty/ownership-gate/pod-candidate-modified.json
  modified:
    - cka-sim/lib/traps.sh
    - cka-sim/lib/cmd/drill.sh
    - cka-sim/lib/cmd/exam.sh
    - cka-sim/scripts/lint-packs.sh
decisions:
  - "5 per-resource detectors gated (not 6): detect_pvc_wrong_storageclass does not exist in codebase"
  - "Ownership gate pattern: is_candidate_modified || return 0 as first line after arg parsing"
  - "Capture failure = hard abort (die in drill, return 1 + flag in exam) — no degrade-to-no-baseline"
  - "lint-packs Pass I is warn-only (ENFORCE_RESET_TMP_CLEANUP=0) until Wave 5 lands cleanup"
metrics:
  duration_seconds: 523
  completed: "2026-05-15T17:19:33Z"
  tasks_completed: 2
  tasks_total: 2
  files_created: 5
  files_modified: 4
---

# Phase 07.1 Plan 02: Ownership Gate + Runner Baseline Hooks Summary

Wired baseline primitives from Plan 01 into the runtime: 5 per-resource trap detectors now gate on baseline ownership, drill.sh and exam.sh capture baselines after setup.sh, and lint-packs.sh warns on missing /tmp/cka-sim/ cleanup in reset.sh files.

## Commits

| Task | Type | Hash | Description |
|------|------|------|-------------|
| 1 | test | 42709a0 | RED: 2 failing test cases + 3 fixture files for ownership gate + runner hooks |
| 2 | feat | d71d1d9 | GREEN: 5 detectors gated, drill.sh + exam.sh hooks, lint Pass I |

## What Was Built

1. **Ownership gate in traps.sh** — 5 per-resource detectors (detect_default_sa_used, detect_missing_dns_egress, detect_hostpath_pv_without_nodeaffinity, detect_rbac_viewer_role_mismatch, detect_service_label_mismatch) now call `cka_sim::baseline::is_candidate_modified` before their existing logic. If the resource is unchanged since baseline, the detector returns empty (no trap fired). 5 text-based detectors remain unchanged.

2. **drill.sh baseline hook** — Sources lib/baseline.sh, exports CKA_SIM_BASELINE_PATH after setup.sh, calls `cka_sim::baseline::capture`, aborts with `die` on capture failure. Cleanup removes /tmp/cka-sim/<question-id>/.

3. **exam.sh baseline hook** — Sources lib/baseline.sh, captures per-question baseline in setup_question() after setup.sh succeeds, flags question on capture failure. batch_grade() re-exports CKA_SIM_BASELINE_PATH per question before each grade.sh invocation.

4. **lint-packs.sh Pass I** — Walks all reset.sh files checking for /tmp/cka-sim/ cleanup. Warn-only mode (ENFORCE_RESET_TMP_CLEANUP=0) until Wave 5 lands the cleanup lines across all packs.

## TDD Gate Compliance

- RED gate: `test(07.1-02)` commit 42709a0 — 2 new cases fail, 42 existing pass.
- GREEN gate: `feat(07.1-02)` commit d71d1d9 — all 44 cases pass.
- No refactor commit needed (code was clean on first pass).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] detect_pvc_wrong_storageclass does not exist**
- **Found during:** Task 2 Step 1
- **Issue:** Plan listed 6 per-resource detectors to gate, but detect_pvc_wrong_storageclass is not present in traps.sh (grep returned empty).
- **Fix:** Gated the 5 detectors that exist. No code change needed — the plan's "if it exists" conditional applies.
- **Files modified:** None (no-op deviation).

## Verification Results

- `bash cka-sim/scripts/test.sh` exits 0 (all 44 cases pass)
- `bash cka-sim/scripts/lint-packs.sh` exits 0 (warn-only for reset.sh cleanup)
- 5 ownership gate calls in traps.sh: `grep -c 'is_candidate_modified' cka-sim/lib/traps.sh` = 5
- 0 ownership gates in text-based detectors (verified via sed extraction)
- drill.sh: capture after setup.sh (line ordering verified by baseline_runner_hook.sh)
- exam.sh: capture in setup_question + re-export in batch_grade (verified by test assertions)
- Capture failure = abort in both modes (die in drill, return 1 in exam)

## Known Stubs

None — all functions are fully wired with real logic.

## Self-Check: PASSED

All files exist. All commits verified in git log.
