---
phase: 07.1-grading-honesty-rebuild
plan: 06
type: execute
wave: 3
depends_on: [07.1-02]
files_modified:
  - cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh
  - cka-sim/packs/cluster-architecture/04-pss-enforce/reset.sh
autonomous: true
requirements: [FIX-01]
must_haves:
  truths:
    - "Empty submission of cluster-architecture/04-pss-enforce scores 0/N raw points (5 setup-state assertions demoted to weight 0)"
    - "Reference-solution path adds at least 1 candidate-authored Pod q04-candidate, scoring at least 1 point"
    - "Audit-escape note (D-22) documented in 07.1-VERIFICATION.md (Plan 13) — pedagogy-thin question, flagged for v1.x rebuild"
    - "reset.sh removes /tmp/cka-sim/04-pss-enforce/ AND /tmp/q04-pss-enforce/"
  artifacts:
    - path: cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh
      provides: "5 setup-state assertions weight=0; new candidate-authored Pod assertion"
      contains: "cka_sim::grade::assert_resource_candidate_authored pod q04-candidate"
    - path: cka-sim/packs/cluster-architecture/04-pss-enforce/reset.sh
      contains: "rm -rf /tmp/cka-sim"
  key_links:
    - from: grade.sh
      to: lib/grade.sh
      via: assert_resource_candidate_authored call
      pattern: 'assert_resource_candidate_authored pod q04-candidate'
---

<objective>
Apply RESEARCH Q4 fix to cluster-architecture/04-pss-enforce. RESEARCH found ALL 5 current assertions are setup-state with NO candidate-work assertion (UAT under-reported the leak as 1/5; structural reality is 5/5). Demote 5 setup-state assertions to weight=0; add `assert_resource_candidate_authored pod q04-candidate` for the candidate's actual deliverable.

Per CONTEXT D-22 audit-escape: this question is pedagogy-thin and may need a deeper rebuild in v1.x. Flag in 07.1-VERIFICATION.md (Plan 13). Phase 07.1 minimum: empty submission scores 0; ref-solution scores at least 1.

Purpose: D-16 — RESEARCH Q4 escalation (UAT 1/5 → actual 5/5 leak); audit-escape candidate.

Output: 5 setup-state assertions with weight=0; 1 candidate-authored Pod assertion; reset.sh wipes both /tmp/ paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-CONTEXT.md
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-RESEARCH.md
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-01-SUMMARY.md
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-02-SUMMARY.md
@cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh
@cka-sim/packs/cluster-architecture/04-pss-enforce/setup.sh
@cka-sim/packs/cluster-architecture/04-pss-enforce/reset.sh
@cka-sim/packs/cluster-architecture/04-pss-enforce/ref-solution.sh
@cka-sim/packs/cluster-architecture/04-pss-enforce/question.md
@cka-sim/lib/grade.sh

<interfaces>
From Plan 01: `cka_sim::grade::assert_resource_candidate_authored <kind> <name> [-n <ns>] [<weight>]` — passes iff resource exists in cluster AND NOT in baseline.

From Plan 01 helpers: weight=0 is honored (still updates message arrays + ok/err output, but does NOT increment CKA_SIM_GRADE_TOTAL or CKA_SIM_GRADE_PASSED). Verify by reading lib/grade.sh existing helpers — if weight=0 IS NOT supported by existing helpers, refactor to skip accumulator math when weight==0.

Existing assertions (grade.sh lines 19-51):
- A1+A2: namespace PSS labels (setup-applied) — leak.
- A3: admission log grep (setup-created) — leak.
- A4: Deployment q04-compliant exists (setup-created) — leak.
- A5: Deployment q04-compliant has ready replicas (setup-waited) — leak.

Question flow (question.md): candidate edits /tmp/q04-pss-enforce/candidate-violator.yaml to remove trap triggers, then `kubectl apply -f` it. The applied resource lands as Pod `q04-candidate` (verify by reading ref-solution.sh). Trap detectors record traps for the wrong-wording text but DO NOT add scoring points — that is the gap RESEARCH Q4 identifies.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Demote 5 assertions to weight=0; add candidate-authored Pod assertion</name>
  <read_first>
    - cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh (full file)
    - cka-sim/packs/cluster-architecture/04-pss-enforce/ref-solution.sh (confirm Pod q04-candidate is the candidate's deliverable)
    - cka-sim/packs/cluster-architecture/04-pss-enforce/question.md (confirm candidate's task)
    - cka-sim/lib/grade.sh (verify assert_field_eq + assert_resource_exists weight=0 honored — if not, the manual `CKA_SIM_GRADE_TOTAL=$(( + weight ))` lines must be guarded)
  </read_first>
  <action>
    Step 1 — verify weight=0 path in lib/grade.sh helpers. If existing helpers always increment TOTAL by weight (treating weight=0 as no-op for passes too), the demotion is mechanical: pass `0` as the weight argument. If the helpers also do NOT increment when weight=0, no further change needed. Read lib/grade.sh; if any helper has `CKA_SIM_GRADE_TOTAL=$(( CKA_SIM_GRADE_TOTAL + weight ))` and weight=0 is honored arithmetically (TOTAL unchanged), the demotion just works. Confirm before proceeding.

    Step 2 — modify grade.sh:

    Demote A1 + A2 by appending `0` (weight argument) to existing calls:
    ```
    cka_sim::grade::assert_field_eq namespace "$CKA_SIM_LAB_NS" \
      '{.metadata.labels.pod-security\.kubernetes\.io/enforce}' restricted 0
    cka_sim::grade::assert_field_eq namespace "$CKA_SIM_LAB_NS" \
      '{.metadata.labels.pod-security\.kubernetes\.io/enforce-version}' v1.35 0
    ```

    Demote A3 (the inline grep block at lines 29-36) — the inline pattern `CKA_SIM_GRADE_TOTAL=$(( + 1 ))` must be removed; convert to ok/err message-only output. Pattern:
    ```
    if grep -qE '(would violate|violates) PodSecurity "(privileged|baseline|restricted):(v1\.[0-9]+|latest)":' "$log_file" 2>/dev/null; then
      ok "admission log uses v1.25+ PodSecurity wording (precondition; no points)"
    else
      err "admission log missing expected PodSecurity wording (precondition)"
    fi
    ```

    Demote A4 (assert_resource_exists deployment q04-compliant): append weight `0`:
    `cka_sim::grade::assert_resource_exists deployment q04-compliant -n "$CKA_SIM_LAB_NS" 0`

    Demote A5 (inline readyReplicas check at lines 41-51) — same pattern as A3 (remove TOTAL/PASSED math, leave ok/err). The condition stays as written; just no accumulator increment.

    Add a new scoring assertion AFTER the 5 demoted preconditions:
    ```
    # Phase 07.1 D-16 — score the candidate's actual deliverable.
    # Q04 is pedagogy-thin (audit-escape per CONTEXT D-22): without this assertion,
    # all 5 preconditions are setup-state. Candidate's task per question.md ends
    # with kubectl apply -f /tmp/q04-pss-enforce/candidate-violator.yaml, producing
    # Pod q04-candidate.
    cka_sim::grade::assert_resource_candidate_authored pod q04-candidate -n "$CKA_SIM_LAB_NS"
    ```

    Trap detector block (lines 53-63 — pss-error-string-mismatch + psp-fictional-pod-label-exemption) — KEEP unchanged. These are text-based detectors tagged setup-allowed in Plan 01, fire freely.

    `cka_sim::grade::emit_result` stays as last call.

    Step 3 — annotate the file with a header comment:
    ```
    # Phase 07.1 D-16 / D-22: this question's grader is structurally thin —
    # 5 preconditions are setup-state (weight=0); 1 scoring assertion checks the
    # candidate-authored Pod q04-candidate. Flagged for v1.x rebuild in 07.1-VERIFICATION.md.
    ```
  </action>
  <verify>
    <automated>bash -n cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh && grep -c 'assert_resource_candidate_authored pod q04-candidate' cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh</automated>
  </verify>
  <acceptance_criteria>
    - grade.sh contains `assert_resource_candidate_authored pod q04-candidate -n "$CKA_SIM_LAB_NS"` exactly once.
    - 5 preconditions remain (2× assert_field_eq, 1× assert_resource_exists, 2× inline) but with weight=0 OR (for inline) without accumulator increment.
    - bash -n passes.
    - 2 text-based trap detectors (pss-error-string-mismatch, psp-fictional-pod-label-exemption) preserved.
    - cka_sim::grade::emit_result is the last call.
    - Header comment notes the audit-escape status.
  </acceptance_criteria>
  <done>5 setup-state assertions weight=0; candidate-authored Pod assertion added; audit-escape annotated.</done>
</task>

<task type="auto">
  <name>Task 2: Extend reset.sh /tmp cleanup (TWO paths)</name>
  <read_first>
    - cka-sim/packs/cluster-architecture/04-pss-enforce/reset.sh (likely already removes /tmp/q04-pss-enforce/ from Phase 5)
  </read_first>
  <action>
    Add `rm -rf /tmp/cka-sim/04-pss-enforce/` to reset.sh. Place near the existing `rm -rf /tmp/q04-pss-enforce/` line (read file first to find it). Both paths must be cleaned: the new baseline dir AND the existing sandbox dir.
  </action>
  <verify>
    <automated>bash -n cka-sim/packs/cluster-architecture/04-pss-enforce/reset.sh && grep -c 'rm -rf /tmp/cka-sim/04-pss-enforce' cka-sim/packs/cluster-architecture/04-pss-enforce/reset.sh</automated>
  </verify>
  <acceptance_criteria>
    - reset.sh contains BOTH `rm -rf /tmp/cka-sim/04-pss-enforce/` AND `rm -rf /tmp/q04-pss-enforce/` (preserve existing).
    - bash -n passes.
  </acceptance_criteria>
  <done>reset.sh cleans both baseline and sandbox dirs.</done>
</task>

</tasks>

<verification>
- 5 assertions demoted; 1 scoring assertion added.
- reset.sh cleans both /tmp paths.
- Audit-escape annotated for Plan 13's VERIFICATION.md.
</verification>

<success_criteria>
- Empty submission predicted score: 0/1.
- Ref-solution predicted score: 1/1.
- Plan 13 receives audit-escape note for v1.x followup.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-06-SUMMARY.md` with audit-escape note for Plan 13's VERIFICATION.md to consume.
</output>
