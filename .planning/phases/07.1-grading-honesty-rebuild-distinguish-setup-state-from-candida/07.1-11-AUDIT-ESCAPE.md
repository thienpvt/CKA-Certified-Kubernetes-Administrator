---
phase: 07.1-grading-honesty-rebuild
plan: 11
pack: cluster-architecture
escape_type: file-baseline-gap
input_for: 07.1-13
---

# Phase 07.1 Plan 11 — cluster-architecture audit-escape inventory

7 CA questions audited under AUDIT-01 (Q04 covered by Plan 06). Three Qs flagged
as D-22 audit-escape (file-baseline gap): the grader reads filesystem state that
`lib/baseline.sh` (D-03 K8s-API-only baseline) cannot track for setup-state vs
candidate-state delta. Mitigations applied where possible (demote setup-state
asserts to weight=0); structural fix deferred to v1.x scope expansion (file-mtime
+ sha256 baseline support). Plan 13 VERIFICATION.md consumes this inventory.

## Escapes

### cluster-architecture/02-etcd-backup-restore

- **Candidate work:** write `snapshot.db`, populate `restored-data/member/wal`,
  embed `ETCDCTL_API=3` + correct `--data-dir` in `apply-script.sh`.
- **Setup state:** `apply-script.sh` with TODO comments, `.cka-sim-sentinel`.
- **Grader probes:** all filesystem (`/tmp/q02-etcd-backup/...`).
- **Why audit-escape:** lib/baseline.sh tracks K8s API resources only. The
  grader cannot ask "was this file modified since setup?" without
  file-mtime + sha256 baseline support.
- **Mitigation in this plan:** no demotion needed — setup writes none of the
  files that scoring asserts probe, so empty submission naturally scores 0/3.
- **v1.x scope expansion needed:** file-mtime + sha256 baseline support so the
  harness can verify candidate authored vs setup pre-seeded the score files.

### cluster-architecture/05-audit-policy

- **Candidate work:** edit `/tmp/q05-audit-policy/policy.yaml` to add valid
  `rules[].level` and `omitStages` per audit Policy v1 schema.
- **Setup state:** `policy.yaml` with `apiVersion`/`kind`/`rules` scaffold but
  no `rules[].level` (broken state).
- **Grader probes:** filesystem (`-s "$policy"`, python YAML parse, structure
  validation, rules count).
- **Why audit-escape:** lib/baseline.sh schema (D-03) tracks K8s API resources
  only; file-mtime + sha256 baseline support needed to verify candidate
  authorship of `policy.yaml`.
- **Mitigation in this plan:** "file exists" and "has at least one rule"
  demoted to **weight=0** (setup-owned). "structure valid" remains weight=1
  (only assertion that proves candidate work).
- **v1.x scope expansion needed:** file-mtime + sha256 baseline support so
  setup-state assertions can be re-promoted under genuine ownership gating.

### cluster-architecture/07-cri-dockerd-endpoint

- **Candidate work:** rewrite `/tmp/q07-kubelet-flags/kubeadm-flags.env` to use
  `--container-runtime-endpoint=unix:///run/cri-dockerd.sock` (replacing the
  deprecated `--container-runtime=remote` setup-seeded broken state).
- **Setup state:** `kubeadm-flags.env` with the broken `--container-runtime=remote`
  flag (Plan 01 tagged setup-allowed) and an empty `kubelet.conf`.
- **Grader probes:** filesystem (`-s "$flags"`, `grep` for endpoint shape,
  `awk` parsing, trap detectors on text content).
- **Why audit-escape:** lib/baseline.sh schema (D-03) tracks K8s API resources
  only; file-mtime + sha256 baseline support needed to verify candidate
  authorship of the kubeadm-flags.env edits.
- **Mitigation in this plan:** "kubeadm-flags.env exists" demoted to **weight=0**
  (setup-owned). "correct CRI endpoint present" remains weight=1. Trap detectors
  fire on text content without an ownership gate — this is the desired behavior
  (Plan 01 tagged these traps setup-allowed; they're negative scoring, not
  positive credit).
- **v1.x scope expansion needed:** file-mtime + sha256 baseline support so
  setup-state assertions can be re-promoted under genuine ownership gating.

## Non-escapes (covered by standard mechanisms)

| Slug                     | Pattern                              | Owner gate |
| ------------------------ | ------------------------------------ | ---------- |
| 01-rbac-viewer           | candidate modifies Role.rules verbs  | `assert_changed_since_setup role pod-viewer` + `assert_can_i` |
| 03-kubeadm-upgrade       | candidate writes content into empty files | content greps fail on empty setup state (no demotion needed) |
| 06-crd-basics            | candidate creates CRD + Q06Widget    | `assert_resource_candidate_authored crd q06widgets.cka-sim.io` |
| 08-priorityclass         | candidate flips globalDefault flag   | `assert_changed_since_setup priorityclass q08-critical` + cluster-wide invariant |

## Summary

- **3 audit-escape Qs** (02, 05, 07) → file-baseline gap → v1.x file-mtime + sha256 baseline.
- **4 standard Qs** (01, 03, 06, 08) → ownership gating applied via baseline.sh delta helpers.
- **Q04** owned by Plan 06 (PSS-enforce; namespace-label gating).
