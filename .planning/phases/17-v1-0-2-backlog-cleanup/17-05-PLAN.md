---
phase: 17-v1-0-2-backlog-cleanup
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/validate.yml
  - cka-sim/scripts/validate-local.sh
autonomous: false
requirements: [BLG-06]
must_haves:
  truths:
    - "BLG-06 root cause assessment: Phase 15 enabled the GHA shellcheck job (validate-local.sh pass 2) on Linux for the first time. Local Windows hosts skip pass 2 (shellcheck not installed), so first-run reds surfaced only in CI. The actual finding count and codes are unknown without running shellcheck against the corpus — that requires Linux + shellcheck."
    - "Two acceptable fixes per CONTEXT D-17: (1) inline `# shellcheck disable=<code>` directives with one-line justifications for each finding (preferred — keeps per-finding accountability); (2) GHA workflow tweak to allow validate-local.sh to skip the shellcheck pass while keeping yamllint pass — temporary, with a TODO referencing this phase to close the debt. The fix shape depends on the actual finding count: if findings are <10, prefer (1); if findings are >=10 and dominated by SC2086/SC2155-style style nits, prefer (1) but in batches; if findings reveal real bugs, fix the bugs at root cause and add inline disables only for false positives."
    - "Because this plan cannot run shellcheck on the Windows host (binary not installed), it ships a CI-only investigation step: a small workflow change that (a) makes the shellcheck pass non-blocking initially via `continue-on-error: true` so we can land the rest of v1.0.2 without being gated on triage, AND (b) prints the shellcheck output to the GHA log for offline triage. The full remediation lands in a follow-up commit on this branch after the first GHA run reveals the findings."
    - "validate-local.sh is also tweaked so the shellcheck pass can be skipped via an env var `CKA_SIM_SKIP_SHELLCHECK=1` for local Linux runs without shellcheck installed, keeping yamllint pass active. This is the minimum-impact change; the script's current behaviour is preserved when the env var is unset."
    - "autonomous: false — this plan involves a CI workflow edit and a follow-up triage step that requires Linux+shellcheck output to land the per-finding fixes. The orchestrator should pause for the operator to fetch GHA logs, triage, and apply per-finding fixes (or merge a follow-up commit) before marking BLG-06 closed."
  artifacts:
    - path: .github/workflows/validate.yml
      provides: "shellcheck job acquires `continue-on-error: true` so first-run reds don't block the rest of v1.0.2 merge. A new step prints the validate-local.sh stderr output to the GHA log even on failure for offline triage."
      contains: "continue-on-error: true"
    - path: cka-sim/scripts/validate-local.sh
      provides: "Adds an opt-in skip via CKA_SIM_SKIP_SHELLCHECK=1 env var for local Linux runs without shellcheck installed. yamllint pass continues to run regardless."
      contains: "CKA_SIM_SKIP_SHELLCHECK"
  key_links:
    - from: .github/workflows/validate.yml
      to: cka-sim/scripts/validate-local.sh
      via: "GHA shellcheck job invokes validate-local.sh; the env var override and continue-on-error are coordinated"
      pattern: "validate-local.sh"
---

<objective>
Close BLG-06 (CI shellcheck job red on Linux) — partial close in this plan, full close in a follow-up commit. The shellcheck binary is not installed on this Windows host, so the actual findings cannot be triaged here.

Plan 17-05 lands the minimum CI-side scaffolding to:
1. Make the shellcheck job non-blocking (`continue-on-error: true`) so the rest of v1.0.2 (Phase 18 forensic re-audit) can ship without waiting on triage.
2. Print the shellcheck output to the GHA log for offline triage.
3. Add a `CKA_SIM_SKIP_SHELLCHECK=1` opt-out in `validate-local.sh` for local Linux runs without shellcheck.

The full per-finding remediation (inline `# shellcheck disable=<code>` directives or root-cause fixes) lands in a follow-up commit on this same branch after the first GHA run reveals the actual findings. The plan is `autonomous: false` — orchestrator should pause for the operator after this plan lands and the GHA log is fetched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-v1-0-2-backlog-cleanup/17-CONTEXT.md
@.github/workflows/validate.yml
@cka-sim/scripts/validate-local.sh

<interfaces>
- The shellcheck job in validate.yml is named `shellcheck` (also runs yamllint). Its single step `Run validate-local` invokes `bash cka-sim/scripts/validate-local.sh`.
- The script runs yamllint (pass 1) then shellcheck (pass 2). Errors from either pass are accumulated and exit 1 if any.
- The new env var lets a Linux runner without shellcheck installed still verify yamllint locally; CI machines have shellcheck installed (apt-get install in the workflow), so the var should be UNSET there to keep the gate active.
- `continue-on-error: true` at the job level lets the workflow as a whole stay green if the shellcheck job fails, while still surfacing the failure in the PR check list. This is the standard pattern for tracking but not blocking on a known-debt CI gate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add continue-on-error to shellcheck job + print output to GHA log</name>
  <read_first>
    - .github/workflows/validate.yml (full — find the shellcheck job)
  </read_first>
  <files>.github/workflows/validate.yml</files>
  <action>
    Find the `shellcheck:` job (the one that runs `cka-sim/scripts/validate-local.sh`). Make two edits:

    1. Add `continue-on-error: true` at the job level, immediately after the `name:` line:
       ```yaml
         shellcheck:
           name: ShellCheck + yamllint (cka-sim)
           runs-on: ubuntu-latest
           continue-on-error: true   # BLG-06: temporary; remove after per-finding triage lands
           steps:
             ...
       ```

    2. After the existing `Run validate-local` step, add an `if: always()` step that re-runs shellcheck explicitly and prints to the log so triage can read findings even when the script's own stderr is buried:
       ```yaml
             - name: Print shellcheck findings (BLG-06 triage)
               if: always()
               run: |
                 echo "::group::shellcheck findings"
                 find cka-sim -name '*.sh' -print0 | xargs -0 -r shellcheck -x -s bash || true
                 echo "::endgroup::"
       ```

    The `if: always()` ensures triage output appears even if the prior step failed. The `|| true` prevents the triage step itself from failing.

    Do NOT add `continue-on-error: true` to the other validate.yml jobs (yamllint-job, bash-tests, symptom-diff). They remain blocking.

    LF line endings.
  </action>
  <verify>
    <automated>grep -A3 '^  shellcheck:' .github/workflows/validate.yml | grep -c 'continue-on-error: true'</automated>
  </verify>
  <acceptance_criteria>
    - `continue-on-error: true` added at the shellcheck job level: `grep -A4 '^  shellcheck:' .github/workflows/validate.yml | grep -c 'continue-on-error: true'` returns 1.
    - BLG-06 tag in the comment: `grep -c 'BLG-06' .github/workflows/validate.yml` returns >= 1.
    - Triage step added: `grep -c 'Print shellcheck findings (BLG-06 triage)' .github/workflows/validate.yml` returns 1.
    - Triage uses `if: always()`: `grep -B1 -A1 'Print shellcheck findings' .github/workflows/validate.yml | grep -c 'if: always' ` returns 1.
    - Other jobs NOT marked continue-on-error: `grep -cE '^[[:space:]]+continue-on-error: true' .github/workflows/validate.yml` returns 1 (only the shellcheck job).
    - YAML still parses: `python3 -c 'import yaml,sys; yaml.safe_load(open(sys.argv[1]))' .github/workflows/validate.yml` exits 0.
  </acceptance_criteria>
  <done>Shellcheck job is non-blocking; findings print to the GHA log on every run for triage.</done>
</task>

<task type="auto">
  <name>Task 2: Add CKA_SIM_SKIP_SHELLCHECK opt-out to validate-local.sh</name>
  <read_first>
    - cka-sim/scripts/validate-local.sh (full)
  </read_first>
  <files>cka-sim/scripts/validate-local.sh</files>
  <action>
    Wrap the existing pass 2 (shellcheck) block in a guard that respects an opt-out env var. Replace the existing `info "pass 2: shellcheck — cka-sim/**/*.sh"` block (lines ~28-35) with:

    ```bash
    # Pass 2: shellcheck — opt out via CKA_SIM_SKIP_SHELLCHECK=1 for hosts without shellcheck installed.
    if [[ -n "${CKA_SIM_SKIP_SHELLCHECK:-}" ]]; then
      warn "pass 2: shellcheck SKIPPED (CKA_SIM_SKIP_SHELLCHECK is set)"
    elif ! command -v shellcheck >/dev/null 2>&1; then
      warn "pass 2: shellcheck SKIPPED (binary not in PATH; install via apt/brew/choco to enable)"
    else
      info "pass 2: shellcheck — cka-sim/**/*.sh"
      while IFS= read -r f; do
        if ! shellcheck -x -s bash "$f" 2>/dev/null; then
          err "shellcheck FAIL: $f"
          errors=$(( errors + 1 ))
        fi
      done < <(find "$CKA_SIM_ROOT" -name '*.sh')
    fi
    ```

    The new behaviour:
    - `CKA_SIM_SKIP_SHELLCHECK=1` → warn-skip pass 2 (yamllint still runs).
    - shellcheck binary missing → warn-skip pass 2 (graceful local-Linux dev).
    - Otherwise → existing pass 2 behaviour preserved (shellcheck runs, fails accumulate).

    Pass 1 (yamllint) is untouched. The final exit-code logic is untouched.

    LF line endings.
  </action>
  <verify>
    <automated>bash -n cka-sim/scripts/validate-local.sh && grep -c 'CKA_SIM_SKIP_SHELLCHECK' cka-sim/scripts/validate-local.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n cka-sim/scripts/validate-local.sh` exits 0.
    - Env var honoured: `grep -c 'CKA_SIM_SKIP_SHELLCHECK' cka-sim/scripts/validate-local.sh` returns >= 2 (test + warn message).
    - Binary-missing fallback present: `grep -c 'binary not in PATH' cka-sim/scripts/validate-local.sh` returns 1.
    - Pass 1 (yamllint) untouched: `grep -c 'pass 1: yamllint' cka-sim/scripts/validate-local.sh` returns 1.
    - Pass 2 still runs shellcheck when available: `grep -c 'shellcheck -x -s bash' cka-sim/scripts/validate-local.sh` returns 1.
    - Functional check on this Windows host (no shellcheck): `bash cka-sim/scripts/validate-local.sh 2>&1 | head -5` shows the binary-missing warn line, then the script exits.
  </acceptance_criteria>
  <done>validate-local.sh skips pass 2 gracefully when shellcheck is missing or opted out; pass 1 (yamllint) still runs; CI behaviour unchanged when shellcheck is installed.</done>
</task>

<task type="auto">
  <name>Task 3: Document the follow-up triage step in plan SUMMARY (no code change)</name>
  <read_first>
    - .planning/phases/17-v1-0-2-backlog-cleanup/17-CONTEXT.md (D-16, D-17, D-18)
  </read_first>
  <files>(verification + handoff documentation only — written into SUMMARY when plan executes)</files>
  <action>
    The SUMMARY for this plan documents the follow-up handoff:

    1. After this plan is committed, push the branch and let GHA run.
    2. Open the `shellcheck` job log in the failed GHA run; copy the `Print shellcheck findings (BLG-06 triage)` step output.
    3. Triage:
       - For each finding, decide: real bug (fix in code) vs over-strict lint (add `# shellcheck disable=<code>` inline with one-line justification).
       - Group by file; commit one fix per file with message `fix(17-05): <file>: <SC-codes triaged>`.
    4. After all findings are addressed, remove `continue-on-error: true` from the shellcheck job in validate.yml and remove the BLG-06 comment. Commit with `refactor(17-05): re-enable shellcheck gate after BLG-06 triage`.
    5. Re-push; the shellcheck job should now exit 0.

    The SUMMARY also notes that this plan's `autonomous: false` flag means the orchestrator should pause after the plan lands so the operator can fetch GHA logs and proceed with the triage commits.

    Document this all in `.planning/phases/17-v1-0-2-backlog-cleanup/17-05-SUMMARY.md`.
  </action>
  <verify>
    <automated>true</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY documents the 5-step follow-up triage flow.
    - SUMMARY notes the autonomous: false handoff.
    - SUMMARY records that `continue-on-error: true` is intentionally a temporary scaffold to be removed after triage.
  </acceptance_criteria>
  <done>Triage path is documented; the operator knows the next concrete steps after this plan lands.</done>
</task>

</tasks>

<verification>
1. `.github/workflows/validate.yml` parses; shellcheck job is non-blocking; triage step prints output.
2. `cka-sim/scripts/validate-local.sh` parses; shellcheck pass skips gracefully when binary missing or env var set; yamllint pass unchanged.
3. On no-cluster Windows host, `bash cka-sim/scripts/validate-local.sh` runs pass 1 (yamllint) and skips pass 2 with the binary-missing warn.
4. The follow-up triage flow is documented in 17-05-SUMMARY.md.
</verification>

<success_criteria>
- BLG-06 partially closed: CI scaffolding lands; first-run reds no longer block v1.0.2 merge.
- Full BLG-06 close (per-finding remediation + re-enable gate) is a follow-up commit, NOT in this plan.
- `autonomous: false` flag pauses the orchestrator for the operator handoff.
</success_criteria>

<output>
After completion, create `.planning/phases/17-v1-0-2-backlog-cleanup/17-05-SUMMARY.md` documenting:
- The CI-side scaffolding (continue-on-error + triage step + env-var opt-out).
- Why this plan does not close BLG-06 fully (shellcheck not installed locally; per-finding triage requires GHA log).
- The 5-step follow-up triage flow.
- Whether the orchestrator paused for operator handoff.
</output>
