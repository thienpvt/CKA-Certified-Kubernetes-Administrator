---
phase: 17-v1-0-2-backlog-cleanup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - cka-sim/tests/fixtures/grading-honesty/storage__02-storageclass-dynamic/post-ref-solution/baseline.json
  - cka-sim/tests/fixtures/grading-honesty/workloads-scheduling__05-daemonset/post-ref-solution/.fixtures.json
autonomous: true
requirements: [BLG-05]
must_haves:
  truths:
    - "BLG-05 storage/02-storageclass-dynamic root cause: post-ref-solution/baseline.json incorrectly contains the candidate-authored StorageClass `fast-ssd` in both the `resources` array and `resource_list`. The Phase 07.1 grading-honesty contract is that baseline.json captures the cluster state BEFORE the candidate (or ref-solution) modifies anything — so post-ref-solution baseline must be IDENTICAL to post-setup baseline. Fix: rewrite post-ref-solution/baseline.json to match post-setup/baseline.json byte-for-byte (only app-cache PVC, no fast-ssd)."
    - "BLG-05 workloads-scheduling/05-daemonset root cause: post-ref-solution/.fixtures.json is missing the `get daemonset q05-node-agent ... -o jsonpath={.status.numberReady}` entry. The grader's assertion 2 reads numberReady, gets empty (stub default for unmapped argv is rc=1 empty stdout), comparison fails, score is 3/4 instead of 4/4. Fix: add the missing fixture entry returning numberReady=1 (matches the existing desiredNumberScheduled=1 — both are 1-node ready in the stub)."
    - "After both fixes, `bash cka-sim/scripts/test.sh` exits 0 with zero red cases (the 2 BLG-05 reds close). Test suite count unchanged (no new cases added in this plan; fixtures are not separate cases)."
    - "The candidate-state baseline contract is preserved: every other post-ref-solution/baseline.json across cka-sim/tests/fixtures/grading-honesty/ continues to match its sibling post-setup/baseline.json. Quick spot-check across 3 other fixtures confirms the contract."
  artifacts:
    - path: cka-sim/tests/fixtures/grading-honesty/storage__02-storageclass-dynamic/post-ref-solution/baseline.json
      provides: "Rewritten to match post-setup baseline byte-for-byte: only app-cache PVC in resources/resource_list, no StorageClass entries. Restores the Phase 07.1 grading-honesty contract."
      contains: "persistentvolumeclaim/app-cache"
    - path: cka-sim/tests/fixtures/grading-honesty/workloads-scheduling__05-daemonset/post-ref-solution/.fixtures.json
      provides: "Adds the missing `get daemonset q05-node-agent -n ... -o jsonpath={.status.numberReady}` entry returning '1' so the rolled-out assertion (numberReady == desiredNumberScheduled) passes."
      contains: "numberReady"
  key_links:
    - from: cka-sim/tests/fixtures/grading-honesty/storage__02-storageclass-dynamic/post-ref-solution/baseline.json
      to: cka-sim/tests/fixtures/grading-honesty/storage__02-storageclass-dynamic/post-setup/baseline.json
      via: "Both must be byte-identical — baseline captures pre-work state; ref-solution work happens AFTER baseline capture"
      pattern: "app-cache"
    - from: cka-sim/tests/fixtures/grading-honesty/workloads-scheduling__05-daemonset/post-ref-solution/.fixtures.json
      to: cka-sim/packs/workloads-scheduling/05-daemonset/grade.sh
      via: "grade.sh assertion 2 reads numberReady; the fixture must answer it for the case to pass"
      pattern: "numberReady"
---

<objective>
Close BLG-05: the two pre-existing unit-suite reds carried from v1.0.1.

- `storage__02-storageclass-dynamic`: ref-solution scores 0/1 instead of 1/1 because the post-ref-solution baseline.json wrongly contains `fast-ssd` (making `assert_resource_candidate_authored` see it as pre-existing).
- `workloads-scheduling__05-daemonset`: ref-solution scores 3/4 instead of 4/4 because the kubectl stub's `.fixtures.json` is missing the `numberReady` jsonpath entry.

Both fixes restore the v1.0.1 unit suite to fully green. The test cases themselves (`tests/grading-honesty/*.sh`) remain unchanged — the expected scores are authoritative; the fixtures must align.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-v1-0-2-backlog-cleanup/17-CONTEXT.md
@cka-sim/tests/fixtures/grading-honesty/storage__02-storageclass-dynamic/post-setup/baseline.json
@cka-sim/tests/fixtures/grading-honesty/storage__02-storageclass-dynamic/post-ref-solution/baseline.json
@cka-sim/tests/fixtures/grading-honesty/workloads-scheduling__05-daemonset/post-ref-solution/.fixtures.json
@cka-sim/packs/storage/02-storageclass-dynamic/grade.sh
@cka-sim/packs/workloads-scheduling/05-daemonset/grade.sh
@cka-sim/tests/grading-honesty/storage__02-storageclass-dynamic.sh
@cka-sim/tests/grading-honesty/workloads-scheduling__05-daemonset.sh

<interfaces>
- The Phase 07.1 grading-honesty contract: baseline.json captures cluster state BEFORE any candidate work. The grader's `assert_resource_candidate_authored` reads the baseline's `resource_list` and asserts the named resource is NOT present (i.e. the candidate created it after baseline capture).
- post-setup baseline = state after `setup.sh` runs. post-ref-solution baseline SHOULD be IDENTICAL because the runner captures baseline once, then the candidate (or ref-solution) does work; the grader reads the same baseline regardless of whether the work was empty or ref-solution.
- The current storage/02 post-ref-solution baseline includes fast-ssd, which is wrong. The current daemonset post-ref-solution baseline is empty (correct — setup creates only the namespace).
- The kubectl stub `.fixtures.json` is keyed on canonicalized argv (`get daemonset q05-node-agent -n cka-sim-workloads-scheduling-05 -o jsonpath={.status.numberReady}`). The stub reads the manifest, looks up the key, returns stdout + rc. Missing keys fall through to legacy single-fixture path; if no fixture matches, stub returns empty stdout — matches what the grader sees.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite storage__02-storageclass-dynamic post-ref-solution baseline.json</name>
  <read_first>
    - cka-sim/tests/fixtures/grading-honesty/storage__02-storageclass-dynamic/post-setup/baseline.json (the correct baseline shape)
    - cka-sim/tests/fixtures/grading-honesty/storage__02-storageclass-dynamic/post-ref-solution/baseline.json (the broken file to overwrite)
  </read_first>
  <files>cka-sim/tests/fixtures/grading-honesty/storage__02-storageclass-dynamic/post-ref-solution/baseline.json</files>
  <action>
    Replace the file contents with the byte-identical copy of post-setup/baseline.json:

    ```json
    {
      "captured_at": 1747000000,
      "lab_namespace": "cka-sim-storage-02",
      "resources": [
        {
          "kind": "PersistentVolumeClaim",
          "name": "app-cache",
          "namespace": "cka-sim-storage-02",
          "generation": null,
          "resourceVersion": "200",
          "labels": {}
        }
      ],
      "resource_list": [
        "persistentvolumeclaim/app-cache"
      ]
    }
    ```

    LF line endings, trailing newline.
  </action>
  <verify>
    <automated>diff cka-sim/tests/fixtures/grading-honesty/storage__02-storageclass-dynamic/post-setup/baseline.json cka-sim/tests/fixtures/grading-honesty/storage__02-storageclass-dynamic/post-ref-solution/baseline.json</automated>
  </verify>
  <acceptance_criteria>
    - The two baseline.json files are byte-identical: `diff cka-sim/tests/fixtures/grading-honesty/storage__02-storageclass-dynamic/post-setup/baseline.json cka-sim/tests/fixtures/grading-honesty/storage__02-storageclass-dynamic/post-ref-solution/baseline.json` produces no output and exits 0.
    - JSON parses: `python3 -c 'import json,sys; json.load(open(sys.argv[1]))' cka-sim/tests/fixtures/grading-honesty/storage__02-storageclass-dynamic/post-ref-solution/baseline.json` exits 0.
    - resource_list contains exactly one entry (app-cache, no fast-ssd): `jq '.resource_list | length' cka-sim/tests/fixtures/grading-honesty/storage__02-storageclass-dynamic/post-ref-solution/baseline.json` returns 1.
    - storageclass/fast-ssd does NOT appear in the file: `grep -c 'fast-ssd' cka-sim/tests/fixtures/grading-honesty/storage__02-storageclass-dynamic/post-ref-solution/baseline.json` returns 0.
    - Running `bash cka-sim/tests/run.sh` shows `case passed: storage__02-storageclass-dynamic` (was `case failed (rc=1)` before this task).
  </acceptance_criteria>
  <done>storage/02 post-ref-solution baseline now matches post-setup byte-for-byte; assert_resource_candidate_authored sees fast-ssd as candidate-authored; ref-solution scores 1/1.</done>
</task>

<task type="auto">
  <name>Task 2: Add numberReady fixture entry to workloads-scheduling__05-daemonset .fixtures.json</name>
  <read_first>
    - cka-sim/tests/fixtures/grading-honesty/workloads-scheduling__05-daemonset/post-ref-solution/.fixtures.json (current — missing numberReady)
    - cka-sim/packs/workloads-scheduling/05-daemonset/grade.sh (lines 25-28: reads numberReady)
  </read_first>
  <files>cka-sim/tests/fixtures/grading-honesty/workloads-scheduling__05-daemonset/post-ref-solution/.fixtures.json</files>
  <action>
    Add a new key right after the `desiredNumberScheduled` entry. The added line:

    ```json
      "get daemonset q05-node-agent -n cka-sim-workloads-scheduling-05 -o jsonpath={.status.numberReady}": {"stdout": "1", "rc": 0},
    ```

    Insert it between the existing `desiredNumberScheduled` entry (line 5) and the existing `tolerations[?(@.key==...)].operator` entry (line 6). Maintain the JSON file's existing formatting (one entry per line, two-space indent, trailing-comma-free terminal entry).

    The value `"1"` matches the existing `desiredNumberScheduled` value — assertion 2 requires `numberReady == desiredNumberScheduled` and `desired > 0`.

    LF line endings, trailing newline.
  </action>
  <verify>
    <automated>jq '.["get daemonset q05-node-agent -n cka-sim-workloads-scheduling-05 -o jsonpath={.status.numberReady}"]' cka-sim/tests/fixtures/grading-honesty/workloads-scheduling__05-daemonset/post-ref-solution/.fixtures.json</automated>
  </verify>
  <acceptance_criteria>
    - JSON parses: `python3 -c 'import json,sys; json.load(open(sys.argv[1]))' cka-sim/tests/fixtures/grading-honesty/workloads-scheduling__05-daemonset/post-ref-solution/.fixtures.json` exits 0.
    - numberReady entry present: `jq '.["get daemonset q05-node-agent -n cka-sim-workloads-scheduling-05 -o jsonpath={.status.numberReady}"].stdout' cka-sim/tests/fixtures/grading-honesty/workloads-scheduling__05-daemonset/post-ref-solution/.fixtures.json` returns `"1"`.
    - desiredNumberScheduled entry preserved: `jq '.["get daemonset q05-node-agent -n cka-sim-workloads-scheduling-05 -o jsonpath={.status.desiredNumberScheduled}"].stdout' .../.fixtures.json` returns `"1"`.
    - Total fixture entry count grew by exactly 1: was 10, now 11. Verify: `jq 'length' .../.fixtures.json` returns 11.
    - Running `bash cka-sim/tests/run.sh` shows `case passed: workloads-scheduling__05-daemonset` (was `case failed (rc=1)` before this task).
  </acceptance_criteria>
  <done>numberReady fixture entry lands; daemonset assertion 2 reads "1" matching desiredNumberScheduled "1"; ref-solution scores 4/4.</done>
</task>

<task type="auto">
  <name>Task 3: Spot-check sibling fixtures preserve the baseline-equivalence contract</name>
  <read_first>
    - cka-sim/tests/fixtures/grading-honesty/ (3 sibling pairs to spot-check)
  </read_first>
  <files>(verification only — no edits)</files>
  <action>
    Verify the post-setup baseline.json equals post-ref-solution baseline.json for at least 3 OTHER question fixtures (sample beyond the 2 fixed). For each:

    ```bash
    diff cka-sim/tests/fixtures/grading-honesty/<id>/post-setup/baseline.json cka-sim/tests/fixtures/grading-honesty/<id>/post-ref-solution/baseline.json
    ```

    Expect no output (the contract holds for all sibling fixtures except the two BLG-05 ones we just fixed). Pick 3 fixtures that are NOT storage/02 or workloads-scheduling/05.

    Document the verified pairs in the SUMMARY.

    If any sibling pair diverges, that is a NEW finding — log it as a deferred item under `### Pending Todos` in STATE.md (do NOT fix it in this plan; out of BLG-05 scope).
  </action>
  <verify>
    <automated>set -e; for d in $(ls cka-sim/tests/fixtures/grading-honesty/ 2>/dev/null | grep -vE '^(storage__02|workloads-scheduling__05)' | head -3); do diff -q "cka-sim/tests/fixtures/grading-honesty/$d/post-setup/baseline.json" "cka-sim/tests/fixtures/grading-honesty/$d/post-ref-solution/baseline.json" || echo "DIVERGED: $d"; done</automated>
  </verify>
  <acceptance_criteria>
    - 3 sibling pairs diff-clean (no output).
    - If any diverged pair found, recorded in SUMMARY as a deferred follow-up; DOES NOT block this plan from landing.
  </acceptance_criteria>
  <done>Contract holds across spot-checked siblings; storage/02 fix restores the contract.</done>
</task>

</tasks>

<verification>
1. `diff cka-sim/tests/fixtures/grading-honesty/storage__02-storageclass-dynamic/{post-setup,post-ref-solution}/baseline.json` produces no output.
2. `jq '.["get daemonset q05-node-agent -n cka-sim-workloads-scheduling-05 -o jsonpath={.status.numberReady}"]' .../.fixtures.json` returns the new entry.
3. `bash cka-sim/scripts/test.sh` exits 0 with zero red cases (was 2 reds before).
4. Total run.sh case count unchanged (no new test cases added; fixtures are not cases).
</verification>

<success_criteria>
- Both BLG-05 reds close.
- The Phase 07.1 baseline-equivalence contract is restored for storage/02.
- The kubectl stub fixture for daemonset has the missing numberReady entry.
- No regression to other test cases.
</success_criteria>

<output>
After completion, create `.planning/phases/17-v1-0-2-backlog-cleanup/17-04-SUMMARY.md` documenting:
- Storage/02 root cause: post-ref-solution baseline incorrectly captured ref-solution-modified state instead of pre-work state.
- Daemonset/05 root cause: kubectl stub fixture missed the numberReady jsonpath query that the grader's assertion 2 issues.
- Pre/post test suite red count: 2 → 0.
- Spot-checked sibling fixtures (3 pairs) preserve the contract.
</output>
