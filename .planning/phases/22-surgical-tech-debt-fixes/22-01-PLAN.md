---
phase: 22-surgical-tech-debt-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cka-sim/lib/cmd/drill.sh
  - cka-sim/tests/cases/drill_namespace_render.sh
autonomous: true
requirements:
  - DRILL-NS-01

must_haves:
  truths:
    - "A candidate running `cka-sim drill <pack> <n>` sees the resolved namespace string (e.g. `cka-sim-storage-01`) inline in the rendered prompt for any question that uses ${CKA_SIM_LAB_NS}."
    - "No literal `${CKA_SIM_LAB_NS}` appears in candidate-visible output of `cka-sim drill` for any of the 20+ affected questions."
    - "Only `${CKA_SIM_LAB_NS}` is substituted; other `${VAR}` shapes that may exist in question.md prose are NOT expanded (no shell expansion, no envsubst)."
  artifacts:
    - path: cka-sim/lib/cmd/drill.sh
      provides: "Drill-mode question.md rendering with single-token namespace substitution mirroring exam.sh:191-196."
      contains: "question_content//\\$\\{CKA_SIM_LAB_NS\\}/$CKA_SIM_LAB_NS"
    - path: cka-sim/tests/cases/drill_namespace_render.sh
      provides: "Unit test that locks the substitution shape — asserts rendered output contains the resolved namespace and no literal ${CKA_SIM_LAB_NS} placeholder."
      contains: "drill_namespace_render"
  key_links:
    - from: cka-sim/lib/cmd/drill.sh
      to: cka-sim/lib/cmd/exam.sh
      via: "shared pure-bash parameter-expansion shape"
      pattern: "question_content=\\(<"
---

<objective>
Close DRILL-NS-01: drill-mode renders question.md with the per-question lab namespace substituted in place of every `${CKA_SIM_LAB_NS}` literal, mirroring the pure-bash parameter-expansion pattern exam-mode already ships at `cka-sim/lib/cmd/exam.sh:191-196` (quick task `260517-hvo`).

Purpose: Candidates currently see literal `${CKA_SIM_LAB_NS}` strings in 20+ question.md prompts during drill mode — a UX bug that exam mode already fixed. The renderer is the right fix point; question.md authors keep the convention.

Output: One single-point change in `cka-sim/lib/cmd/drill.sh` at line 321 plus one new unit test case under `cka-sim/tests/cases/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-surgical-tech-debt-fixes/22-CONTEXT.md

@cka-sim/lib/cmd/drill.sh
@cka-sim/lib/cmd/exam.sh
@cka-sim/tests/cases/drill_namespace_construction.sh

<interfaces>
<!-- Reference shape from cka-sim/lib/cmd/exam.sh:189-197 — the pattern DRILL-NS-01 must mirror. -->
<!-- Excerpt below is canonical; the executor should not reinvent the substitution syntax. -->

From cka-sim/lib/cmd/exam.sh (lines 189-197 — exam-mode reference):
- Reads `$qdir/question.md` into a local var via `question_content=$(<"$qdir/question.md")`.
- Emits with `printf '%s\n' "${question_content//\$\{CKA_SIM_LAB_NS\}/$CKA_SIM_LAB_NS}"`.
- Pure-bash parameter expansion; the `\$\{...\}` form replaces ONLY the literal `${CKA_SIM_LAB_NS}` token, not other `$VAR` shapes.

From cka-sim/lib/cmd/drill.sh (lines 320-323 — current bug site):
- Line 320 emits `info "step 3/4: prompt"`.
- Line 321 currently runs `cat "$CKA_SIM_QUESTION_DIR/question.md"` — this is the bug; literal placeholder is shown.
- Line 322 emits `info "Lab ns: $CKA_SIM_LAB_NS"`.

From cka-sim/tests/cases/drill_namespace_construction.sh (test scaffold pattern):
- Sources `$CKA_SIM_ROOT/tests/lib/assert.sh` for `expect_eq`.
- Uses `set -uo pipefail`, `case_failed=0`, exits with `$case_failed`.
- Pure-bash; no kubectl required.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing test for namespace render shape</name>
  <files>cka-sim/tests/cases/drill_namespace_render.sh</files>
  <read_first>
    - cka-sim/tests/cases/drill_namespace_construction.sh (mirror this scaffold — set -uo pipefail, source tests/lib/assert.sh, case_failed flag, exit case_failed)
    - cka-sim/lib/cmd/exam.sh lines 189-197 (the substitution shape under test)
    - cka-sim/lib/cmd/drill.sh lines 318-323 (current bug site at line 321)
    - cka-sim/tests/lib/assert.sh (for expect_eq, expect_contains, expect_not_contains helpers — use whichever exist; otherwise pure-bash `[[ ... ]]` with case_failed=1 on miss)
  </read_first>
  <behavior>
    - Test 1: Given a temp question.md containing the literal four-byte sequence `${CKA_SIM_LAB_NS}` (single occurrence) and CKA_SIM_LAB_NS=cka-sim-test-01, the bash parameter-expansion shape `${question_content//\$\{CKA_SIM_LAB_NS\}/$CKA_SIM_LAB_NS}` produces output containing the literal string `cka-sim-test-01` and NOT containing the literal `${CKA_SIM_LAB_NS}` token.
    - Test 2: Given a temp question.md containing both `${CKA_SIM_LAB_NS}` AND a different shaped token like `$OTHER_VAR` and `${UNRELATED}`, the same expansion replaces ONLY `${CKA_SIM_LAB_NS}` — `$OTHER_VAR` and `${UNRELATED}` survive verbatim.
    - Test 3: The drill.sh source file at line 321 contains the parameter-expansion idiom (a `grep -F` for the literal substring `question_content//\$\{CKA_SIM_LAB_NS\}/$CKA_SIM_LAB_NS` in cka-sim/lib/cmd/drill.sh returns a non-empty hit). Use `grep -F` or `grep -q` with the literal pattern; do NOT use `grep -c | -eq 0` because shell comments confound counts. This test will FAIL until Task 2 lands and is the gate that locks the GREEN state.
  </behavior>
  <action>
    Create `cka-sim/tests/cases/drill_namespace_render.sh` mirroring the `drill_namespace_construction.sh` scaffold (`set -uo pipefail`, source `$CKA_SIM_ROOT/tests/lib/assert.sh`, `case_failed=0`, three test sections, `exit "$case_failed"`).

    Test 1 (substitution behavior): Create a tmp file under `$(mktemp -d)`, write the literal four-byte sequence `${CKA_SIM_LAB_NS}` as a single line (use a heredoc with `'EOF'` quoted-here-doc to suppress shell expansion). Set `CKA_SIM_LAB_NS=cka-sim-test-01`. Read the tmp file with `qc=$(<"$tmp/question.md")`. Apply `out="${qc//\$\{CKA_SIM_LAB_NS\}/$CKA_SIM_LAB_NS}"`. Assert (a) `[[ "$out" == *"cka-sim-test-01"* ]]` and (b) `[[ "$out" != *'${CKA_SIM_LAB_NS}'* ]]`. On miss, set `case_failed=1` and emit a `✗` line to stderr.

    Test 2 (selectivity): Same scaffold; tmp file contains `intro ${CKA_SIM_LAB_NS} middle $OTHER_VAR end ${UNRELATED}` on one line. Apply expansion. Assert `[[ "$out" == *"cka-sim-test-01"* ]]`, `[[ "$out" == *'$OTHER_VAR'* ]]` (literal `$OTHER_VAR` survives — note the single-quotes in the bash glob pattern), and `[[ "$out" == *'${UNRELATED}'* ]]` (literal `${UNRELATED}` survives).

    Test 3 (drill.sh source carries the idiom): Run `grep -F 'question_content//${CKA_SIM_LAB_NS}/' "$CKA_SIM_ROOT/lib/cmd/drill.sh"` (use `-q` for a boolean check). On miss → `case_failed=1`. This is the lock that catches accidental regression to `cat`-only render.

    Clean up tmp dir on EXIT via `trap 'rm -rf "$tmp"' EXIT`. The test must pass under `set -uo pipefail` and exit `$case_failed`.

    Per D-DRILL-NS-01.4 (deferred): do NOT sweep question.md files; the test only validates the renderer, not the prose corpus.
  </action>
  <verify>
    <automated>cd C:\Users\thien\IdeaProjects\CKA-Certified-Kubernetes-Administrator && bash cka-sim/tests/cases/drill_namespace_render.sh; echo "rc=$?"</automated>
  </verify>
  <acceptance_criteria>
    - File `cka-sim/tests/cases/drill_namespace_render.sh` exists and is sourced cleanly under `bash -c 'set -uo pipefail; source cka-sim/tests/cases/drill_namespace_render.sh'` (it is sourced — not exec'd — by `cka-sim/tests/run.sh`).
    - Running the test BEFORE Task 2 lands: Test 3 (drill.sh source grep) FAILS with `case_failed=1`, exit non-zero (RED state — the renderer fix has not landed).
    - Tests 1 and 2 (the parameter-expansion behavior, which uses bash directly without invoking drill.sh) pass independently because they validate the bash language feature itself.
    - The test file does not require `kubectl`, `python3`, or any live cluster — pure bash + filesystem.
  </acceptance_criteria>
  <done>
    Test file committed; `bash cka-sim/tests/run.sh` shows `drill_namespace_render` case running and failing with the Test 3 assertion (drill.sh:321 still uses `cat`, no parameter-expansion idiom present yet).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply parameter-expansion render at drill.sh:321 (mirror exam.sh:191-196)</name>
  <files>cka-sim/lib/cmd/drill.sh</files>
  <read_first>
    - cka-sim/lib/cmd/drill.sh — read the function body containing line 321 to identify the enclosing function (likely `cka_sim::drill::run` or similar). Confirm `local` declarations are in scope so `local question_content` is valid.
    - cka-sim/lib/cmd/exam.sh lines 189-197 (the canonical pattern to mirror exactly).
  </read_first>
  <behavior>
    - After fix: at drill.sh:321 the renderer reads question.md into a local variable, then prints with the parameter-expansion substitution. The exact form is `local question_content; question_content=$(<"$CKA_SIM_QUESTION_DIR/question.md"); printf '%s\n' "${question_content//\$\{CKA_SIM_LAB_NS\}/$CKA_SIM_LAB_NS}"`.
    - The fix replaces ONLY the existing `cat "$CKA_SIM_QUESTION_DIR/question.md"` line; the surrounding `info "step 3/4: prompt"` (line 320) and `info "Lab ns: $CKA_SIM_LAB_NS"` (line 322) are NOT touched.
    - Re-running `cka-sim/tests/cases/drill_namespace_render.sh` (created in Task 1) now PASSES — Test 3 finds the parameter-expansion idiom in drill.sh.
  </behavior>
  <action>
    Open `cka-sim/lib/cmd/drill.sh`, locate line 321 (`cat "$CKA_SIM_QUESTION_DIR/question.md"`). Replace ONLY that single line with three lines mirroring exam.sh:194-196:

    1. A `local question_content` declaration (matching exam.sh:194 — single name, no `=` in declaration).
    2. A `question_content=$(<"$CKA_SIM_QUESTION_DIR/question.md")` assignment that reads the file into the variable (matching exam.sh:195 but with drill's `$CKA_SIM_QUESTION_DIR` instead of exam's `$qdir`).
    3. A `printf '%s\n' "${question_content//\$\{CKA_SIM_LAB_NS\}/$CKA_SIM_LAB_NS}"` emit (matching exam.sh:196 verbatim — same parameter-expansion shape).

    Do NOT use `envsubst`. Do NOT use `sed`/`awk`. Do NOT touch any question.md prose under `cka-sim/packs/**` (per D-DRILL-NS-01.4: question.md authoring convention is preserved; renderer is the fix point).

    Do NOT modify lines 320 or 322 (`info "step 3/4: prompt"` and `info "Lab ns: $CKA_SIM_LAB_NS"`). Confirm by reading the surrounding region after the edit that lines 320/322 are byte-identical to pre-edit.

    Confirm the enclosing function declares `local` is valid (drill.sh:321 is inside a function — `local question_content` is fine; if the function already declares other `local` vars, the new one is consistent with that style).
  </action>
  <verify>
    <automated>cd C:\Users\thien\IdeaProjects\CKA-Certified-Kubernetes-Administrator && bash cka-sim/tests/cases/drill_namespace_render.sh; echo "drill_namespace_render rc=$?"; bash -n cka-sim/lib/cmd/drill.sh; echo "drill.sh syntax rc=$?"; grep -F 'question_content//${CKA_SIM_LAB_NS}/' cka-sim/lib/cmd/drill.sh; echo "idiom-grep rc=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `bash cka-sim/tests/cases/drill_namespace_render.sh` exits 0 (Test 1, Test 2, Test 3 all pass — the source grep in Test 3 now finds the idiom in drill.sh).
    - `bash -n cka-sim/lib/cmd/drill.sh` exits 0 (no bash syntax errors introduced).
    - `grep -F 'question_content//${CKA_SIM_LAB_NS}/' cka-sim/lib/cmd/drill.sh` returns exactly 1 hit (the new line at the former site of `cat ...`).
    - Diff against HEAD shows ONLY 3 lines changed near line 321 of drill.sh: one line removed (`cat ...`), three lines added (`local question_content` / `question_content=$(<...)` / `printf '%s\n' "..."`). No other code changed.
    - `bash cka-sim/scripts/test.sh` exits 0 with no new red cases (the existing 88-case suite + the new drill_namespace_render case all pass on the dev box; pre-existing reds from `step1-results.txt` are out of scope for this plan and remain in their prior state).
  </acceptance_criteria>
  <done>
    drill.sh:321 emits substituted prompts; new test case is GREEN; `bash cka-sim/scripts/test.sh` shows zero new reds attributable to this change. DRILL-NS-01 is closed in code; lab smoke test (running `cka-sim drill <pack> <n>` against the lab cluster) is deferred to Phase 24 UAT batch.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| question.md author → drill renderer | question.md prose is repository content authored by us; not user input. Trust = high. |
| drill renderer → candidate terminal | Renderer output is shown to the candidate verbatim; the candidate is the user. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01-01 | Tampering | parameter expansion in drill.sh:321 | mitigate | Use `${var//\$\{CKA_SIM_LAB_NS\}/$repl}` (replaces only the exact `${CKA_SIM_LAB_NS}` literal). Does NOT use `envsubst` (which would expand any `$VAR`), does NOT use `eval` (which would execute code). Only the one token we own is substituted. |
| T-22-01-02 | Information Disclosure | namespace name in candidate-visible prompt | accept | Namespace name is `cka-sim-<pack>-NN` — non-secret, RFC-1123 deterministic, already shown via `info "Lab ns: $CKA_SIM_LAB_NS"` at drill.sh:322. No new disclosure. |
</threat_model>

<verification>
- `bash cka-sim/tests/cases/drill_namespace_render.sh` exits 0 (new test GREEN).
- `bash -n cka-sim/lib/cmd/drill.sh` exits 0 (syntax clean).
- `grep -F 'question_content//${CKA_SIM_LAB_NS}/' cka-sim/lib/cmd/drill.sh` finds exactly 1 hit.
- `bash cka-sim/scripts/test.sh` produces no NEW red cases compared to the pre-fix baseline (pre-existing reds documented in `cka-sim/current-tests/step1-results.txt` are out of scope and unchanged).
- Lab-cluster smoke (`cka-sim drill storage 1` against the v1.0.1 lab cluster, visual verify resolved namespace appears in prompt) is deferred to Phase 24 UAT — not a Phase 22 plan task.
</verification>

<success_criteria>
- DRILL-NS-01 fix lands at `cka-sim/lib/cmd/drill.sh:321` mirroring `cka-sim/lib/cmd/exam.sh:191-196` shape exactly.
- New test case `cka-sim/tests/cases/drill_namespace_render.sh` exists, exits 0 post-fix, and is exercised by `cka-sim/tests/run.sh`.
- Zero changes to question.md prose under `cka-sim/packs/**` (per D-DRILL-NS-01.4).
- Plan SUMMARY.md records the before/after diff and the test-case addition.
</success_criteria>

<output>
After completion, create `.planning/phases/22-surgical-tech-debt-fixes/22-01-SUMMARY.md`.
</output>
