---
phase: 10-high-single-question-edits
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cka-sim/packs/storage/01-pvc-binding/setup.sh
  - cka-sim/packs/storage/01-pvc-binding/question.md
  - cka-sim/packs/storage/01-pvc-binding/grade.sh
  - cka-sim/packs/storage/01-pvc-binding/ref-solution.sh
  - cka-sim/packs/storage/01-pvc-binding/reset.sh
autonomous: true
requirements: [BUG-H01]
must_haves:
  truths:
    - "After setup, PVC app-data is Bound but Pod q01-app-consumer is stuck in Pending state with a FailedScheduling event citing PV node-affinity (matching question.md's symptom claim verbatim)."
    - "ref-solution.sh patches PV q01-app-pv to add nodeAffinity, then waits for q01-app-consumer to reach Running, and grade.sh exit rc==0 with SCORE 3/3 and 0 traps recorded."
    - "Empty submission scores 0/3 (Pod stays Pending; nodeAffinity assertion fails; trap detect_hostpath_pv_without_nodeaffinity records once)."
    - "reset.sh deletes the consumer Pod, the PVC namespace, the PV q01-app-pv, and /tmp/cka-sim/01-pvc-binding/ — second run is a no-op."
    - "question.md no longer claims the PVC is Pending; instead it says the consumer Pod fails to schedule onto a worker."
  artifacts:
    - path: cka-sim/packs/storage/01-pvc-binding/setup.sh
      provides: "Seeds PV (no nodeAffinity, the trap) + PVC + a busybox consumer Pod that mounts the PVC; Pod stays Pending."
      contains: "kind: Pod, name: q01-app-consumer, image busybox, volumeMounts referencing app-data"
    - path: cka-sim/packs/storage/01-pvc-binding/question.md
      provides: "Symptom claim is 'Pod q01-app-consumer fails to schedule' (matches actual setup state)."
      contains: "q01-app-consumer"
    - path: cka-sim/packs/storage/01-pvc-binding/grade.sh
      provides: "PVC-bound precondition (weight=0); Pod-running precondition (weight=0); 3 scoring assertions on PV nodeAffinity shape (weight=1 each); preserved trap detector."
      contains: "assert_pvc_bound, kubectl wait --for=condition=Ready pod, assert_field_eq pv q01-app-pv, detect_hostpath_pv_without_nodeaffinity"
    - path: cka-sim/packs/storage/01-pvc-binding/ref-solution.sh
      provides: "Patches PV with nodeAffinity, waits for consumer Pod to become Running."
      contains: "kubectl patch pv q01-app-pv, kubectl wait --for=condition=Ready pod/q01-app-consumer"
    - path: cka-sim/packs/storage/01-pvc-binding/reset.sh
      provides: "Idempotent cleanup of Pod, namespace, PV, and per-question tmp dir."
      contains: "kubectl delete pod q01-app-consumer, kubectl delete pv q01-app-pv"
  key_links:
    - from: cka-sim/packs/storage/01-pvc-binding/grade.sh
      to: cka-sim/lib/grade.sh
      via: source $CKA_SIM_ROOT/lib/grade.sh — uses assert_pvc_bound + assert_field_eq
      pattern: 'cka_sim::grade::assert_field_eq pv q01-app-pv'
    - from: cka-sim/packs/storage/01-pvc-binding/grade.sh
      to: cka-sim/lib/traps.sh
      via: detect_hostpath_pv_without_nodeaffinity (preserved)
      pattern: 'cka_sim::trap::detect_hostpath_pv_without_nodeaffinity'
---

<objective>
Fix BUG-H01 (storage/01-pvc-binding) — the question's claimed symptom ("PVC stuck Pending") does not match what setup.sh produces (PVC actually binds because the apiserver does not enforce PV nodeAffinity at bind time; only at Pod scheduling). Rewrite the question around its real, observable symptom: a consumer Pod that mounts the PVC fails to schedule. Setup adds the consumer Pod; question.md, grade.sh, ref-solution.sh, and reset.sh follow.

Purpose: Forensic report finding (HIGH-severity detail #1, .planning/forensics/report-20260517-091657-full-audit.md) — candidate sees `STATUS: Bound` for the PVC and concludes the question is mislabelled, scoring 0/3 even when the trap diagnosis is correct.

Output: A self-consistent question where the symptom is real, the trap pedagogy is preserved (still hostPath-PV-without-nodeAffinity), and ref-solution scores 3/3 against an actual scheduling failure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-high-single-question-edits/10-CONTEXT.md
@.planning/forensics/report-20260517-091657-full-audit.md
@.planning/REQUIREMENTS.md
@cka-sim/lib/setup.sh
@cka-sim/lib/grade.sh
@cka-sim/lib/traps.sh
@cka-sim/packs/storage/01-pvc-binding/setup.sh
@cka-sim/packs/storage/01-pvc-binding/question.md
@cka-sim/packs/storage/01-pvc-binding/grade.sh
@cka-sim/packs/storage/01-pvc-binding/ref-solution.sh
@cka-sim/packs/storage/01-pvc-binding/reset.sh
@cka-sim/packs/storage/01-pvc-binding/metadata.yaml

<interfaces>
From cka-sim/lib/setup.sh:
- `cka_sim::setup::ensure_lab_ns <ns> <pack> <question-id>` — idempotent ns create
- `cka_sim::setup::wait_for_ns_active <ns> <pack> <question-id> [timeout]` — 120s wait

From cka-sim/lib/grade.sh:
- `cka_sim::grade::assert_pvc_bound <ns> <pvc-name> [<weight>]` — weight=0 for informational
- `cka_sim::grade::assert_field_eq <kind> <name> <jsonpath> <expected> [-n <ns>] [<weight>]` — weight=1 default
- `cka_sim::grade::record_trap <trap-id>` — dedups by id
- `cka_sim::grade::emit_result` — final SCORE + Trap N: lines

From cka-sim/lib/traps.sh:
- `cka_sim::trap::detect_hostpath_pv_without_nodeaffinity <pv-name>` — stdout = trap-id on hit, EMPTY on miss

Existing scoring shape (from current grade.sh, lines 22-28):
- One scoring assertion: `assert_field_eq pv q01-app-pv {.spec.nodeAffinity.required.nodeSelectorTerms[0].matchExpressions[0].key} kubernetes.io/hostname` (1 point).
- One trap detector: `detect_hostpath_pv_without_nodeaffinity q01-app-pv` (records 1 trap on miss).
- ROADMAP success criterion is "ref-solution scores 3/3" — current grade is 1 point + 1 trap = max 1 + traps. Need to expand scoring to 3 to match ROADMAP. Use 3 assertions on the PV nodeAffinity shape (key, operator, presence) so the candidate's full nodeAffinity edit is graded, not just the key string.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add consumer Pod to setup.sh</name>
  <read_first>
    - cka-sim/packs/storage/01-pvc-binding/setup.sh (current 52-line file — preserve PV+PVC apply, add Pod after)
    - cka-sim/lib/setup.sh (helper signatures — no new helper needed; inline `kubectl apply -f -` heredoc matches existing pattern at lines 17 and 38)
  </read_first>
  <files>cka-sim/packs/storage/01-pvc-binding/setup.sh</files>
  <action>
    Append a new Step 4 after the PVC apply (after line 51). Apply a minimal busybox Pod named `q01-app-consumer` in `${CKA_SIM_LAB_NS}` that mounts the `app-data` PVC. The Pod MUST stay Pending — do NOT add tolerations or nodeSelectors that would let it schedule despite the PV's missing nodeAffinity.

    Use the same `kubectl apply -f - <<EOF` heredoc pattern already in this file. Pod shape:

    ```yaml
    apiVersion: v1
    kind: Pod
    metadata:
      name: q01-app-consumer
      namespace: ${CKA_SIM_LAB_NS}
      labels:
        cka-sim/pack: storage
        cka-sim/question-id: storage-pvc-binding
    spec:
      restartPolicy: Never
      containers:
        - name: app
          image: busybox:1.36
          command: ["sh", "-c", "sleep 3600"]
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: app-data
    EOF
    ```

    Add a header comment above the Pod block: `# 4. Apply consumer Pod that mounts app-data — Pod stays Pending until candidate fixes the PV's nodeAffinity (real symptom of the trap).`

    Do NOT add a `kubectl wait` for the Pod — it is supposed to stay Pending. Setup must exit 0 immediately after the apply. The existing `set -euo pipefail` is preserved.

    Idempotency: a re-run of setup.sh hits `kubectl apply` for the Pod, which is idempotent. Do NOT add `--force` or `kubectl delete pod` — apply alone is enough.

    LF line endings.
  </action>
  <verify>
    <automated>bash -n cka-sim/packs/storage/01-pvc-binding/setup.sh && grep -c 'q01-app-consumer' cka-sim/packs/storage/01-pvc-binding/setup.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n cka-sim/packs/storage/01-pvc-binding/setup.sh` exits 0.
    - `grep -c '^kind: Pod$' cka-sim/packs/storage/01-pvc-binding/setup.sh` returns 1 (exactly one Pod manifest).
    - `grep -c 'name: q01-app-consumer' cka-sim/packs/storage/01-pvc-binding/setup.sh` returns 1.
    - `grep -c 'claimName: app-data' cka-sim/packs/storage/01-pvc-binding/setup.sh` returns 1.
    - `grep -c 'image: busybox' cka-sim/packs/storage/01-pvc-binding/setup.sh` returns 1.
    - File contains no `kubectl wait` lines after the Pod apply: `grep -A30 'q01-app-consumer' cka-sim/packs/storage/01-pvc-binding/setup.sh | grep -c 'kubectl wait'` returns 0.
    - The PV apply (line ~17) and PVC apply (line ~38) blocks are preserved verbatim.
  </acceptance_criteria>
  <done>setup.sh seeds PV (no nodeAffinity), PVC, and consumer Pod. Pod stays Pending — matches the rewritten question.md symptom claim.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite question.md to match real symptom</name>
  <read_first>
    - cka-sim/packs/storage/01-pvc-binding/question.md (current text says "PVC is stuck Pending" — this is what is being changed)
    - .planning/phases/10-high-single-question-edits/10-CONTEXT.md (BUG-H01 fix path step 2 + 7)
  </read_first>
  <files>cka-sim/packs/storage/01-pvc-binding/question.md</files>
  <action>
    Rewrite cka-sim/packs/storage/01-pvc-binding/question.md to describe the real symptom (Pod fails to schedule) instead of the false symptom (PVC stuck Pending). Keep the file's heading style, "Tasks", "Constraints", "Verify yourself" sections.

    Concrete content changes:
    1. Title stays `# storage/01-pvc-binding`.
    2. Domain/time line stays `**Domain:** Storage  |  **Estimated time:** 8 minutes`.
    3. Replace the opening paragraph (current line 5) with: `A 'PersistentVolumeClaim' named 'app-data' exists in your lab namespace and references a 'PersistentVolume' named 'q01-app-pv'. A consumer Pod named 'q01-app-consumer' has been deployed in your lab namespace to mount that PVC, but the Pod fails to schedule onto a worker node.` (Use single quotes on the resource names for consistency with the original markdown style. The PVC actually binds — that is correct K8s behaviour and is NOT the bug.)
    4. Tasks section — replace task 1 to inspect the Pod and PVC instead of the PVC alone:
       - "Inspect the Pod 'q01-app-consumer' and the PVC 'app-data' in '${CKA_SIM_LAB_NS}', and the PV 'q01-app-pv' (cluster-scoped)."
       - "Diagnose why the Pod cannot schedule. Read the PV spec and Pod events carefully."
       - "Modify the PV in place so the Pod can schedule successfully."
    5. Constraints (lines 13-17) — keep verbatim. The "Do NOT delete or recreate the PV" / "Do NOT modify the PVC" / "1 control-plane + 2 worker nodes" wording is still accurate.
    6. Add one constraint line: `- Do NOT modify the consumer Pod 'q01-app-consumer'.`
    7. Verify yourself section — replace the kubectl commands with:
       ```
       kubectl get pod q01-app-consumer -n ${CKA_SIM_LAB_NS}    # STATUS should be Running
       kubectl get pvc app-data -n ${CKA_SIM_LAB_NS}            # STATUS should be Bound
       kubectl get pv q01-app-pv                                # STATUS should be Bound
       ```

    Do NOT introduce new sections (e.g. "Hints"). Do NOT mention `nodeAffinity` directly — diagnosing it is the question.

    LF line endings.
  </action>
  <verify>
    <automated>grep -c 'q01-app-consumer' cka-sim/packs/storage/01-pvc-binding/question.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains `q01-app-consumer` at least 3 times (paragraph + Tasks + Verify): `grep -c 'q01-app-consumer' cka-sim/packs/storage/01-pvc-binding/question.md` returns >= 3.
    - File no longer claims the PVC is Pending: `grep -c 'PVC is stuck' cka-sim/packs/storage/01-pvc-binding/question.md` returns 0; `grep -ci 'pvc.*pending' cka-sim/packs/storage/01-pvc-binding/question.md` returns 0.
    - Tasks 1-3 still numbered: `grep -cE '^[0-9]\.' cka-sim/packs/storage/01-pvc-binding/question.md` returns 3.
    - Constraints section preserved: `grep -c '^## Constraints$' cka-sim/packs/storage/01-pvc-binding/question.md` returns 1.
    - Verify section uses pod check: `grep -c 'kubectl get pod q01-app-consumer' cka-sim/packs/storage/01-pvc-binding/question.md` returns 1.
    - Verify section retains pvc and pv checks: `grep -c 'kubectl get pvc app-data' cka-sim/packs/storage/01-pvc-binding/question.md` returns 1; `grep -c 'kubectl get pv q01-app-pv' cka-sim/packs/storage/01-pvc-binding/question.md` returns 1.
    - Question text does not leak the answer ("nodeAffinity"): `grep -ci 'nodeaffinity' cka-sim/packs/storage/01-pvc-binding/question.md` returns 0.
  </acceptance_criteria>
  <done>question.md describes the actual scheduling-failure symptom; constraint to leave the consumer Pod untouched is explicit; verify section confirms Pod Running.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite grade.sh to score Pod scheduling + 3 nodeAffinity field assertions</name>
  <read_first>
    - cka-sim/packs/storage/01-pvc-binding/grade.sh (current 31-line file with 1 scoring assertion + trap detector)
    - cka-sim/lib/grade.sh (assert_pvc_bound and assert_field_eq signatures + weight semantics)
    - cka-sim/lib/traps.sh (detect_hostpath_pv_without_nodeaffinity contract)
    - .planning/phases/10-high-single-question-edits/10-CONTEXT.md (BUG-H01 fix step 4) and .planning/ROADMAP.md success criterion #1 ("ref-solution scores 3/3")
  </read_first>
  <files>cka-sim/packs/storage/01-pvc-binding/grade.sh</files>
  <action>
    Rewrite cka-sim/packs/storage/01-pvc-binding/grade.sh while keeping the existing header (shebang, set -uo pipefail, env guards, source statements). Replace the assertion block with the following structure (preserving the bash idiom from cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh — use a `kubectl wait --for=condition=Ready pod ... --timeout=10s >/dev/null 2>&1 || true` line to force the api-server to surface the latest scheduling state before the assertion).

    1. **Precondition (weight=0): PVC is Bound.** Keep `cka_sim::grade::assert_pvc_bound "$CKA_SIM_LAB_NS" "app-data" 0`. PV+PVC binding succeeds at create time regardless of nodeAffinity; this is informational state.

    2. **Precondition (weight=0): consumer Pod is Running.** Add a non-fatal `kubectl wait --for=condition=Ready pod/q01-app-consumer -n "$CKA_SIM_LAB_NS" --timeout=10s >/dev/null 2>&1 || true` (mirrors the pattern in `cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh:19-20`), then call `cka_sim::grade::assert_pod_ready "$CKA_SIM_LAB_NS" "q01-app-consumer" 0`. On empty submission this fails (Pod stays Pending); on ref-solution this passes. Weight 0 keeps this informational — the *scoring* points all live on the PV nodeAffinity shape.

    3. **Scoring assertion 1 (weight=1): PV has the matchExpressions key.** `cka_sim::grade::assert_field_eq pv q01-app-pv '{.spec.nodeAffinity.required.nodeSelectorTerms[0].matchExpressions[0].key}' 'kubernetes.io/hostname'` (this is the existing line at grade.sh:22-24, kept verbatim).

    4. **Scoring assertion 2 (weight=1): PV has the matchExpressions operator.** `cka_sim::grade::assert_field_eq pv q01-app-pv '{.spec.nodeAffinity.required.nodeSelectorTerms[0].matchExpressions[0].operator}' 'Exists'` — matches the operator that ref-solution.sh patches in (ref-solution uses `operator: Exists`). This grades that the candidate authored a *complete, valid* matchExpression, not just a stray key.

    5. **Scoring assertion 3 (weight=1): nodeAffinity expression has at least one term.** Use `cka_sim::grade::assert_field_eq pv q01-app-pv '{.spec.nodeAffinity.required.nodeSelectorTerms[0].matchExpressions[0].key}' 'kubernetes.io/hostname'` is already (3); to give a third independent point use the *presence* of the required block: `cka_sim::grade::assert_field_eq pv q01-app-pv '{.spec.nodeAffinity.required.nodeSelectorTerms[*].matchExpressions[*].key}' 'kubernetes.io/hostname'` — DO NOT use this; it is the same shape. Instead use a presence check on the `required` block itself:

       Use a manual numeric assertion (matching the inline pattern at cka-sim/packs/services-networking/05-kube-proxy-mode/grade.sh:22-31). Increment `CKA_SIM_GRADE_TOTAL`; query `kubectl get pv q01-app-pv -o jsonpath='{.spec.nodeAffinity.required.nodeSelectorTerms[*].matchExpressions[*].key}' 2>/dev/null`; if the result is non-empty, increment `CKA_SIM_GRADE_PASSED`, append to `CKA_SIM_GRADE_PASSES` and call `ok`; else append to `CKA_SIM_GRADE_FAILS` and call `err`. Pass message: `"PV q01-app-pv has nodeAffinity.required.nodeSelectorTerms"`. Fail message: `"PV q01-app-pv has no nodeAffinity.required.nodeSelectorTerms"`. This catches a candidate who wrote `preferred` instead of `required` (still satisfies key/operator on a different jsonpath but is not actually a hard requirement — won't fix the scheduling failure).

    6. **Trap detector (preserved):** Keep the existing `cka_sim::trap::detect_hostpath_pv_without_nodeaffinity q01-app-pv` block (grade.sh:27-28) verbatim.

    7. **Finalizer:** keep `cka_sim::grade::emit_result` as the last non-blank line.

    Add a header comment above the rewritten assertion block: `# Phase 10 BUG-H01 — score the actual symptom (Pod scheduling) and the full nodeAffinity shape.`

    LF line endings.
  </action>
  <verify>
    <automated>bash -n cka-sim/packs/storage/01-pvc-binding/grade.sh && grep -c 'assert_field_eq pv q01-app-pv' cka-sim/packs/storage/01-pvc-binding/grade.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n cka-sim/packs/storage/01-pvc-binding/grade.sh` exits 0.
    - `assert_pvc_bound "$CKA_SIM_LAB_NS" "app-data" 0` present (weight=0): `grep -c 'assert_pvc_bound "$CKA_SIM_LAB_NS" "app-data" 0' cka-sim/packs/storage/01-pvc-binding/grade.sh` returns 1.
    - `assert_pod_ready "$CKA_SIM_LAB_NS" "q01-app-consumer" 0` present (weight=0): `grep -c 'assert_pod_ready "\$CKA_SIM_LAB_NS" "q01-app-consumer" 0' cka-sim/packs/storage/01-pvc-binding/grade.sh` returns 1.
    - `kubectl wait --for=condition=Ready pod/q01-app-consumer` present: `grep -c 'kubectl wait --for=condition=Ready pod/q01-app-consumer' cka-sim/packs/storage/01-pvc-binding/grade.sh` returns 1.
    - Two assert_field_eq scoring assertions on PV (weight defaults to 1): `grep -c 'cka_sim::grade::assert_field_eq pv q01-app-pv' cka-sim/packs/storage/01-pvc-binding/grade.sh` returns 2 (key + operator).
    - Inline manual assertion incrementing `CKA_SIM_GRADE_TOTAL` for nodeSelectorTerms presence: `grep -c 'CKA_SIM_GRADE_TOTAL=$(( CKA_SIM_GRADE_TOTAL + 1 ))' cka-sim/packs/storage/01-pvc-binding/grade.sh` returns 1.
    - Trap detector preserved: `grep -c 'detect_hostpath_pv_without_nodeaffinity q01-app-pv' cka-sim/packs/storage/01-pvc-binding/grade.sh` returns 1.
    - `emit_result` is the last non-blank line: `tail -5 cka-sim/packs/storage/01-pvc-binding/grade.sh | grep -c 'cka_sim::grade::emit_result'` returns 1.
    - Total scoring weight is 3: count of (`assert_field_eq pv q01-app-pv` lines without an explicit weight 0) + (manual TOTAL+1 increments) == 3.
  </acceptance_criteria>
  <done>grade.sh awards 3 points for candidate-authored nodeAffinity shape; preconditions (PVC Bound, Pod Ready) are weight=0 informational; trap detector preserved.</done>
</task>

<task type="auto">
  <name>Task 4: Update ref-solution.sh to wait for Pod Running</name>
  <read_first>
    - cka-sim/packs/storage/01-pvc-binding/ref-solution.sh (current 21-line file: patches PV nodeAffinity)
    - .planning/phases/10-high-single-question-edits/10-CONTEXT.md (BUG-H01 fix step 3)
  </read_first>
  <files>cka-sim/packs/storage/01-pvc-binding/ref-solution.sh</files>
  <action>
    Append a single `kubectl wait` line to cka-sim/packs/storage/01-pvc-binding/ref-solution.sh, after the existing `kubectl patch pv q01-app-pv` block (after line 20). This forces ref-solution to demonstrate the *actual* end-state (Pod Running) and gives grade.sh's Pod-Ready precondition a deterministic state to read.

    Add (preserving the trailing newline):
    ```bash

    # Wait for the consumer Pod to schedule onto a worker now that the PV has nodeAffinity.
    # Timeout is generous because the kubelet has to attach the hostPath volume, which
    # involves the DirectoryOrCreate hostPath check on the chosen node.
    kubectl wait --for=condition=Ready pod/q01-app-consumer \
      -n "$CKA_SIM_LAB_NS" --timeout=60s
    ```

    Do NOT remove `set -euo pipefail`. The wait MUST not be guarded by `|| true` — if the Pod doesn't schedule after the patch, ref-solution should fail loudly so the GRADE round-trip surfaces a regression.

    Do NOT touch the existing `kubectl patch` block.

    LF line endings.
  </action>
  <verify>
    <automated>bash -n cka-sim/packs/storage/01-pvc-binding/ref-solution.sh && grep -c 'kubectl wait --for=condition=Ready pod/q01-app-consumer' cka-sim/packs/storage/01-pvc-binding/ref-solution.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n cka-sim/packs/storage/01-pvc-binding/ref-solution.sh` exits 0.
    - File contains `kubectl wait --for=condition=Ready pod/q01-app-consumer` exactly once: `grep -c 'kubectl wait --for=condition=Ready pod/q01-app-consumer' cka-sim/packs/storage/01-pvc-binding/ref-solution.sh` returns 1.
    - File still contains the original PV patch: `grep -c 'kubectl patch pv q01-app-pv' cka-sim/packs/storage/01-pvc-binding/ref-solution.sh` returns 1.
    - The wait line is NOT followed by `|| true`: `grep -A1 'kubectl wait --for=condition=Ready pod/q01-app-consumer' cka-sim/packs/storage/01-pvc-binding/ref-solution.sh | grep -c '|| true'` returns 0.
    - `set -euo pipefail` preserved: `grep -c 'set -euo pipefail' cka-sim/packs/storage/01-pvc-binding/ref-solution.sh` returns 1.
  </acceptance_criteria>
  <done>ref-solution.sh patches PV and waits for Pod to reach Running, matching the rewritten question's verify-yourself instructions.</done>
</task>

<task type="auto">
  <name>Task 5: Update reset.sh to clean the consumer Pod</name>
  <read_first>
    - cka-sim/packs/storage/01-pvc-binding/reset.sh (current 16-line file: deletes ns + PV + tmp)
    - .planning/phases/10-high-single-question-edits/10-CONTEXT.md (BUG-H01 fix step 6)
  </read_first>
  <files>cka-sim/packs/storage/01-pvc-binding/reset.sh</files>
  <action>
    Insert one new line in cka-sim/packs/storage/01-pvc-binding/reset.sh BEFORE the existing namespace-delete (line 7: `kubectl delete namespace ...`). The Pod is namespaced, so the namespace delete *would* eventually evict it; explicit deletion is faster and idempotent and matches the GRADING-HONESTY contract that "reset must clean everything setup creates".

    New line (with `--ignore-not-found` so re-runs don't fail):
    ```bash
    # Phase 10 BUG-H01 — explicit Pod cleanup (Pod is in $CKA_SIM_LAB_NS but is named so we
    # delete it directly to make reset.sh self-documenting about the resources setup creates).
    kubectl delete pod q01-app-consumer -n "$CKA_SIM_LAB_NS" --ignore-not-found --wait=false
    ```

    Place this line between the existing comment `# 1. Async ns delete` block and the `kubectl delete namespace` line — i.e. add a new step labelled `# 1a. Delete consumer Pod` and renumber comments only if you want to (renumbering is optional; keeping `# 1a.` is acceptable).

    LF line endings.
  </action>
  <verify>
    <automated>bash -n cka-sim/packs/storage/01-pvc-binding/reset.sh && grep -c 'kubectl delete pod q01-app-consumer' cka-sim/packs/storage/01-pvc-binding/reset.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n cka-sim/packs/storage/01-pvc-binding/reset.sh` exits 0.
    - File contains `kubectl delete pod q01-app-consumer` exactly once: `grep -c 'kubectl delete pod q01-app-consumer' cka-sim/packs/storage/01-pvc-binding/reset.sh` returns 1.
    - The pod-delete uses `--ignore-not-found`: `grep 'kubectl delete pod q01-app-consumer' cka-sim/packs/storage/01-pvc-binding/reset.sh | grep -c -- '--ignore-not-found'` returns 1.
    - Existing namespace, PV, and tmp cleanup preserved: `grep -c 'kubectl delete namespace' cka-sim/packs/storage/01-pvc-binding/reset.sh` returns 1; `grep -c 'kubectl delete pv q01-app-pv' cka-sim/packs/storage/01-pvc-binding/reset.sh` returns 1; `grep -c 'rm -rf /tmp/cka-sim/01-pvc-binding/' cka-sim/packs/storage/01-pvc-binding/reset.sh` returns 1.
    - Pod-delete line appears BEFORE the namespace-delete line: `grep -n 'kubectl delete' cka-sim/packs/storage/01-pvc-binding/reset.sh` shows pod line number < namespace line number.
  </acceptance_criteria>
  <done>reset.sh now explicitly cleans the consumer Pod, the namespace, the PV, and the per-question tmp dir — second run is a no-op.</done>
</task>

</tasks>

<verification>
After all 5 tasks complete:

1. `bash -n cka-sim/packs/storage/01-pvc-binding/{setup,grade,ref-solution,reset}.sh` — all four scripts syntactically valid.
2. `bash scripts/lint-packs.sh storage/01-pvc-binding` (if the linter exists in this milestone — verify by `test -x scripts/lint-packs.sh`) — no new errors.
3. Live-cluster GRADE round-trip will be performed in execute-phase verification (out of scope here): `cka-sim drill storage 01-pvc-binding` then run ref-solution.sh → expect SCORE 3/3, 0 traps. Empty submission → expect SCORE 0/3, 1 trap (hostpath-pv-without-nodeaffinity).
4. `metadata.yaml` — no change required. Trap list `hostpath-pv-without-nodeaffinity` is still the only trap that the rewritten grader can record.
</verification>

<success_criteria>
- ref-solution scores 3/3 on a live cluster (ROADMAP success criterion #1).
- Empty submission scores 0/3.
- question.md's symptom claim ("Pod fails to schedule") matches what setup.sh produces.
- Trap pedagogy preserved: hostpath-pv-without-nodeaffinity still fires on empty submission.
- No library changes; no metadata.yaml changes.
</success_criteria>

<output>
After completion, create `.planning/phases/10-high-single-question-edits/10-01-SUMMARY.md` documenting:
- Files modified: setup.sh, question.md, grade.sh, ref-solution.sh, reset.sh
- Symptom rewrite: PVC-Pending → Pod-fails-to-schedule
- Scoring shape: 2 weight=0 preconditions + 3 weight=1 scoring assertions on PV nodeAffinity shape (key, operator, terms presence) + preserved trap detector
- Verification: ref-solution 3/3, empty submission 0/3 + 1 trap (to be confirmed by execute-phase live drill)
</output>
