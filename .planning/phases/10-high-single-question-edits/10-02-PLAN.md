---
phase: 10-high-single-question-edits
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cka-sim/packs/services-networking/05-kube-proxy-mode/setup.sh
autonomous: true
requirements: [BUG-H02]
must_haves:
  truths:
    - "setup.sh:17 seeds the sandbox file with a non-enum placeholder ('placeholder') instead of the live-mode collision token 'ipvs'."
    - "Empty submission scores 0/3 (Assertion 0 'candidate overwrote sandbox file' fails because reported still equals seeded; gated Assertions 2 and 3 also fail because candidate_wrote=0)."
    - "ref-solution.sh scores 3/3 on a cluster running kube-proxy in any of {iptables, ipvs, nftables} mode — including the previously-broken 'ipvs' case."
    - "The .setup-seeded-mode sentinel file content matches the new SEED_MODE value verbatim."
  artifacts:
    - path: cka-sim/packs/services-networking/05-kube-proxy-mode/setup.sh
      provides: "Seeds /tmp/q05-kube-proxy/reported-mode.txt with 'placeholder' (out-of-enum) so candidate-wrote check works on every cluster mode."
      contains: "SEED_MODE='placeholder'"
  key_links:
    - from: cka-sim/packs/services-networking/05-kube-proxy-mode/setup.sh
      to: cka-sim/packs/services-networking/05-kube-proxy-mode/grade.sh
      via: ".setup-seeded-mode sentinel — grade.sh:18 reads it to detect candidate overwrite"
      pattern: 'echo "\$SEED_MODE" > /tmp/q05-kube-proxy/.setup-seeded-mode'
---

<objective>
Fix BUG-H02 (services-networking/05-kube-proxy-mode) — setup.sh:17 hardcodes `SEED_MODE='ipvs'`. On clusters where kube-proxy actually runs in `ipvs`, ref-solution writes `ipvs` to the sandbox file, which equals the seeded value, so grade.sh's Assertion 0 ("candidate overwrote sandbox file") fails. Assertions 2 and 3 are gated on `candidate_wrote=1`, so they also fail. Result: ref-solution scores 0/3 on an ipvs cluster despite producing the correct answer.

Purpose: Forensic report finding (HIGH-severity detail #2). Single-line setup edit; no grade.sh, question.md, ref-solution.sh, or metadata.yaml change required.

Output: SEED_MODE is a non-enum placeholder (`'placeholder'`) so `reported != seeded` is true for every valid kube-proxy mode the candidate could write.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-high-single-question-edits/10-CONTEXT.md
@.planning/forensics/report-20260517-091657-full-audit.md
@cka-sim/packs/services-networking/05-kube-proxy-mode/setup.sh
@cka-sim/packs/services-networking/05-kube-proxy-mode/grade.sh
@cka-sim/packs/services-networking/05-kube-proxy-mode/question.md
@cka-sim/packs/services-networking/05-kube-proxy-mode/ref-solution.sh

<interfaces>
grade.sh contract (verbatim from current file):

- Line 18: `seeded=$(cat /tmp/q05-kube-proxy/.setup-seeded-mode 2>/dev/null | tr -d '[:space:]')`
- Line 19: `reported=$(cat /tmp/q05-kube-proxy/reported-mode.txt 2>/dev/null | tr -d '[:space:]')`
- Line 23: `if [[ -n "$reported" && -n "$seeded" && "$reported" != "$seeded" ]]; then`

The candidate-wrote check is "reported value differs from seeded value, and both are non-empty". Setting SEED_MODE to any string outside {iptables, ipvs, nftables} guarantees `reported != seeded` for all valid candidate writes. `placeholder` is preferable to `unknown` because the question.md mentions `iptables | ipvs | nftables` as the valid enum and "placeholder" is unambiguously not a kube-proxy mode token.

Line 53 valid-enum regex: `[[ "$reported" =~ ^(iptables|ipvs|nftables)$ ]]` — gated on `candidate_wrote == 1`. With seeded='placeholder', empty submission has reported='placeholder' (which is not a valid enum) AND candidate_wrote=0 (because reported == seeded), so both gating conditions fail → Assertion 3 fails honestly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change SEED_MODE from 'ipvs' to 'placeholder'</name>
  <read_first>
    - cka-sim/packs/services-networking/05-kube-proxy-mode/setup.sh (current line 17 hardcodes 'ipvs')
    - cka-sim/packs/services-networking/05-kube-proxy-mode/grade.sh (line 23 reads .setup-seeded-mode for candidate-wrote check; line 53 valid-enum regex)
    - cka-sim/packs/services-networking/05-kube-proxy-mode/question.md (verifies the question text already says "draft value" — no rewrite needed)
    - cka-sim/packs/services-networking/05-kube-proxy-mode/ref-solution.sh (verifies ref-solution writes the live mode unconditionally)
  </read_first>
  <files>cka-sim/packs/services-networking/05-kube-proxy-mode/setup.sh</files>
  <action>
    In cka-sim/packs/services-networking/05-kube-proxy-mode/setup.sh line 17, change the assignment exactly as follows:

    BEFORE:
    ```bash
    # Seed a WRONG initial mode so candidate must overwrite with the actual value
    SEED_MODE='ipvs'
    ```

    AFTER:
    ```bash
    # Phase 10 BUG-H02: seed an out-of-enum placeholder. The previous value 'ipvs'
    # collided with the live mode on ipvs clusters, breaking the candidate-wrote
    # check (grade.sh:23) and gating Assertions 2 and 3 to fail. 'placeholder' is
    # outside {iptables, ipvs, nftables}, so reported != seeded for every valid
    # candidate write on every cluster.
    SEED_MODE='placeholder'
    ```

    DO NOT touch the rest of the file. The `.setup-seeded-mode` sentinel write at line 23 (`echo "$SEED_MODE" > /tmp/q05-kube-proxy/.setup-seeded-mode`) reuses the variable, so it picks up the new value automatically.

    DO NOT change `chmod 0644` / `chmod 0444` lines. DO NOT change the sandbox path. DO NOT change the existing pre-commit comment (line 16: `# Seed a WRONG initial mode...`) — replace it with the BUG-H02 comment block above so future readers see the rationale.

    LF line endings. No CRLF.
  </action>
  <verify>
    <automated>bash -n cka-sim/packs/services-networking/05-kube-proxy-mode/setup.sh && grep -c "SEED_MODE='placeholder'" cka-sim/packs/services-networking/05-kube-proxy-mode/setup.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n cka-sim/packs/services-networking/05-kube-proxy-mode/setup.sh` exits 0.
    - The new value is present exactly once: `grep -c "SEED_MODE='placeholder'" cka-sim/packs/services-networking/05-kube-proxy-mode/setup.sh` returns 1.
    - The old hardcoded value is gone: `grep -c "SEED_MODE='ipvs'" cka-sim/packs/services-networking/05-kube-proxy-mode/setup.sh` returns 0.
    - SEED_MODE is referenced exactly twice in the file (assignment + sentinel write): `grep -c '\bSEED_MODE\b' cka-sim/packs/services-networking/05-kube-proxy-mode/setup.sh` returns 2.
    - Sentinel write line still uses the variable (not a literal): `grep -c 'echo "\$SEED_MODE" > /tmp/q05-kube-proxy/.setup-seeded-mode' cka-sim/packs/services-networking/05-kube-proxy-mode/setup.sh` returns 1.
    - Sandbox file write line still uses the variable: `grep -c 'echo "\$SEED_MODE" > /tmp/q05-kube-proxy/reported-mode.txt' cka-sim/packs/services-networking/05-kube-proxy-mode/setup.sh` returns 1.
    - chmod lines preserved: `grep -c 'chmod 0644 /tmp/q05-kube-proxy/reported-mode.txt' cka-sim/packs/services-networking/05-kube-proxy-mode/setup.sh` returns 1; `grep -c 'chmod 0444 /tmp/q05-kube-proxy/.setup-seeded-mode' cka-sim/packs/services-networking/05-kube-proxy-mode/setup.sh` returns 1.
    - BUG-H02 comment present: `grep -c 'BUG-H02' cka-sim/packs/services-networking/05-kube-proxy-mode/setup.sh` returns 1.
    - The live cluster's actual modes (iptables, ipvs, nftables) do NOT appear in the seed value: `grep -cE "SEED_MODE='(iptables|ipvs|nftables)'" cka-sim/packs/services-networking/05-kube-proxy-mode/setup.sh` returns 0.
  </acceptance_criteria>
  <done>SEED_MODE is 'placeholder', the .setup-seeded-mode sentinel matches, and the candidate-wrote check works for every valid candidate write on every cluster's kube-proxy mode.</done>
</task>

<task type="auto">
  <name>Task 2: Quick scan — verify no other file references the old seed value</name>
  <read_first>
    - cka-sim/packs/services-networking/05-kube-proxy-mode/grade.sh
    - cka-sim/packs/services-networking/05-kube-proxy-mode/ref-solution.sh
    - cka-sim/packs/services-networking/05-kube-proxy-mode/reset.sh
    - cka-sim/packs/services-networking/05-kube-proxy-mode/question.md
    - cka-sim/packs/services-networking/05-kube-proxy-mode/metadata.yaml
  </read_first>
  <files>(read-only verification — produces no edits unless a hidden coupling is found)</files>
  <action>
    Run a series of greps to confirm that no peer file in `cka-sim/packs/services-networking/05-kube-proxy-mode/` greps the seeded value or hard-codes `ipvs` as the seed expectation:

    1. `grep -rn 'ipvs' cka-sim/packs/services-networking/05-kube-proxy-mode/` — expected hits are ONLY:
       - `grade.sh:53` valid-enum regex `^(iptables|ipvs|nftables)$` (correct — this is the candidate's allowed answer set)
       - `grade.sh:48`, `grade.sh:60` `record_trap kube-proxy-mode-mismatch-ipvs-iptables` (trap-id literal, unrelated to the seed value)
       - `metadata.yaml:6` trap-id literal `kube-proxy-mode-mismatch-ipvs-iptables`
       - `question.md:10` valid-enum hint `(one of iptables, ipvs, or nftables)`
    2. `grep -rn '\.setup-seeded-mode' cka-sim/packs/services-networking/05-kube-proxy-mode/` — expected hits ONLY in setup.sh (write) and grade.sh:18 (read).
    3. `grep -rn 'reported-mode\.txt' cka-sim/packs/services-networking/05-kube-proxy-mode/` — confirm path consistency.

    If any unexpected hit is found (e.g. ref-solution.sh hard-codes `'ipvs'` as the answer, or reset.sh greps for it), STOP and surface the finding to the user — this would change the scope of the fix. Otherwise no file edits are needed; this task is a sanity check that the BUG-H02 diagnosis (single-line edit suffices) holds.

    Report the grep output in the SUMMARY.md.
  </action>
  <verify>
    <automated>grep -rn 'SEED_MODE' cka-sim/packs/services-networking/05-kube-proxy-mode/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - SEED_MODE is referenced ONLY in setup.sh: `grep -rln 'SEED_MODE' cka-sim/packs/services-networking/05-kube-proxy-mode/` returns exactly `cka-sim/packs/services-networking/05-kube-proxy-mode/setup.sh`.
    - The valid-enum regex in grade.sh:53 is unchanged: `grep -c '\^(iptables|ipvs|nftables)\$' cka-sim/packs/services-networking/05-kube-proxy-mode/grade.sh` returns 1.
    - ref-solution.sh does NOT hard-code 'ipvs' as a candidate answer: `grep -E "echo +['\"]ipvs" cka-sim/packs/services-networking/05-kube-proxy-mode/ref-solution.sh` returns nothing (it should write `$live_mode`, not a literal).
    - reset.sh does NOT depend on the seed value: `grep -c 'placeholder\|ipvs' cka-sim/packs/services-networking/05-kube-proxy-mode/reset.sh` returns 0.
    - metadata.yaml is unchanged from the working tree's pre-edit state: the only hits for `ipvs` in that file are inside the trap-id `kube-proxy-mode-mismatch-ipvs-iptables`.
    - question.md is unchanged.
  </acceptance_criteria>
  <done>Verified that BUG-H02 is genuinely a single-line setup edit; no peer file requires modification.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `bash -n cka-sim/packs/services-networking/05-kube-proxy-mode/setup.sh` — syntactically valid.
2. Live-cluster GRADE round-trip will be performed in execute-phase verification (out of scope here):
   - On an iptables cluster: ref-solution writes `iptables` → reported (`iptables`) != seeded (`placeholder`) → candidate_wrote=1 → Assertion 0 passes; live_mode=iptables → Assertion 2 passes; `iptables` matches enum → Assertion 3 passes → 3/3.
   - On an ipvs cluster: ref-solution writes `ipvs` → reported (`ipvs`) != seeded (`placeholder`) → candidate_wrote=1 → 3/3.
   - On a nftables cluster: 3/3 (same logic).
   - Empty submission: reported (`placeholder`) == seeded (`placeholder`) → candidate_wrote=0 → all three Assertions fail → 0/3.
</verification>

<success_criteria>
- ref-solution scores 3/3 on iptables, ipvs, AND nftables clusters (ROADMAP success criterion #2).
- Empty submission scores 0/3 on every cluster.
- Single-file change: only setup.sh modified.
- No grade.sh, question.md, ref-solution.sh, reset.sh, or metadata.yaml change.
</success_criteria>

<output>
After completion, create `.planning/phases/10-high-single-question-edits/10-02-SUMMARY.md` documenting:
- File modified: setup.sh (line 17 SEED_MODE assignment)
- Why 'placeholder' over 'unknown': non-collision with any kube-proxy enum value
- Verification: empty + 3 cluster-mode round-trips predicted in execute-phase
- Other files surveyed and confirmed unchanged: grade.sh, ref-solution.sh, reset.sh, question.md, metadata.yaml (Task 2 grep results)
</output>
