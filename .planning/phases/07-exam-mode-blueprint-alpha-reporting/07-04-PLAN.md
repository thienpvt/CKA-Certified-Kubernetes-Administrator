---
phase: 07-exam-mode-blueprint-alpha-reporting
plan: 04
type: execute
wave: 4
depends_on: [07-02, 07-03]
files_modified:
  - cka-sim/lib/cmd/score.sh
  - cka-sim/lib/cmd/list.sh
  - cka-sim/tests/exam/score_command.sh
  - cka-sim/tests/exam/list_history.sh
autonomous: true
requirements: [REPORT-02]
tags: [bash, score, list, history]
validation:
  sampling_rate: per-task-commit
  quick_command: "bash cka-sim/tests/run.sh"
  full_command: "bash cka-sim/scripts/test.sh"
must_haves:
  truths:
    - "lib/cmd/score.sh replaces Phase 1 stub"
    - "cka-sim score (no args) prints most recent session report"
    - "cka-sim score <ts> prints that session's report"
    - "If <ts>.md missing but <ts>.json exists, regenerates report via cka_sim::report::render then prints (D-26)"
    - "lib/cmd/list.sh extended with history subcommand"
    - "cka-sim list history walks ~/.cka-sim/sessions/*.json, prints 5-column table (Started, Blueprint, Score, Pass/Fail, Status) sorted started_at desc (D-27)"
    - "In-progress sessions marked with (in-progress) in Status column"
    - "bash cka-sim/scripts/test.sh remains green"
  artifacts:
    - path: "cka-sim/lib/cmd/score.sh"
      provides: "Score report viewer with on-demand regeneration"
      contains: "cka_sim::score::main"
    - path: "cka-sim/lib/cmd/list.sh"
      provides: "Extended list command with history subcommand"
      contains: "cka_sim::list::history"
---

<objective>
Replace the Phase 1 `score.sh` stub and extend `list.sh` with a `history` subcommand. `cka-sim score [<ts>]` re-displays or regenerates exam reports. `cka-sim list history` shows all completed and in-progress sessions in a table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-exam-mode-blueprint-alpha-reporting/07-CONTEXT.md
@.planning/phases/07-exam-mode-blueprint-alpha-reporting/07-RESEARCH.md
@cka-sim/lib/cmd/drill.sh
@cka-sim/lib/exam-state.sh
@cka-sim/lib/exam-report.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace score.sh + extend list.sh</name>
  <files>cka-sim/lib/cmd/score.sh, cka-sim/lib/cmd/list.sh</files>
  <read_first>
    - cka-sim/lib/cmd/score.sh (current Phase 1 stub — replace entirely)
    - cka-sim/lib/cmd/list.sh (current Phase 1 stub — extend, keep existing pack/blueprint listing)
    - cka-sim/lib/cmd/drill.sh (lines 1-25 for module header shape; source block)
    - cka-sim/lib/exam-state.sh (cka_sim::state::{session_path,report_path,list_sessions})
    - cka-sim/lib/exam-report.sh (cka_sim::report::render for on-demand regeneration)
    - .planning/phases/07-exam-mode-blueprint-alpha-reporting/07-CONTEXT.md (D-26, D-27)
  </read_first>
  <action>
    **score.sh** (~120 lines):
    - Header: `#!/bin/bash`, `set -euo pipefail`, `: "${CKA_SIM_ROOT:?}"`, source colors.sh, log.sh, exam-state.sh, exam-report.sh
    - `cka_sim::score::usage` — help text
    - `cka_sim::score::main`:
      - Parse args: no arg → most recent; `<ts>` → specific session; `-h` → usage
      - Most recent: `ls -t ~/.cka-sim/sessions/*.json 2>/dev/null | head -1`, extract ts from filename
      - Check report exists: `report_path=$(cka_sim::state::report_path "$ts")`
      - If .md exists: `cat "$report_path"`
      - If .md missing but .json exists: `cka_sim::report::render "$json_path" "$report_path"` then `cat "$report_path"`
      - If neither exists: `die "No session found for timestamp: $ts"`
    - Source-vs-execute guard at bottom

    **list.sh** (~100 lines):
    - Keep existing header + source block
    - Replace the stub body with a `case "${1:-}" in` dispatcher:
      - `history` → `cka_sim::list::history`
      - `packs` → list available packs (existing behavior, just print pack dirs)
      - `blueprints` → list available blueprints under exams/
      - `-h|--help|""` → usage showing all subcommands
    - `cka_sim::list::history`:
      - Walk `~/.cka-sim/sessions/*.json` via `cka_sim::state::list_sessions`
      - For each: extract blueprint.id, started_at, total score (from final_report_path or compute), status (completed if final_report_path set, else in-progress)
      - Print 5-column table: Started | Blueprint | Score | Pass/Fail | Status
      - Sort by started_at desc
      - If no sessions: "No exam history yet. Run `cka-sim exam <blueprint>` to start."
    - Source exam-state.sh for list_sessions helper
    - Source-vs-execute guard at bottom
  </action>
  <acceptance_criteria>
    - `bash -n cka-sim/lib/cmd/score.sh` exits 0
    - `bash -n cka-sim/lib/cmd/list.sh` exits 0
    - `! grep -q 'not implemented yet' cka-sim/lib/cmd/score.sh` (stub replaced)
    - `grep -q 'cka_sim::score::main' cka-sim/lib/cmd/score.sh`
    - `grep -q 'cka_sim::list::history' cka-sim/lib/cmd/list.sh`
    - `grep -q 'report::render' cka-sim/lib/cmd/score.sh` (on-demand regeneration)
    - `grep -q 'in-progress' cka-sim/lib/cmd/list.sh` (status marker)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Tests for score and list history</name>
  <files>cka-sim/tests/exam/score_command.sh, cka-sim/tests/exam/list_history.sh</files>
  <read_first>
    - cka-sim/lib/cmd/score.sh (Task 1 output)
    - cka-sim/lib/cmd/list.sh (Task 1 output)
    - cka-sim/tests/cases/drill_load_pack.sh (test case shape)
    - cka-sim/tests/fixtures/exam/session-fixture.json (from plan 07-02)
  </read_first>
  <action>
    **score_command.sh**:
    - Set up: create temp ~/.cka-sim/sessions/ dir, copy session-fixture.json as `20260510T090000Z.json`
    - Test 1: `cka-sim score 20260510T090000Z` — assert it regenerates .md (since .md doesn't exist yet) and prints report to stdout containing "## Per-Domain Breakdown"
    - Test 2: Run again — assert it reads existing .md (no regeneration, just cat)
    - Test 3: `cka-sim score nonexistent` — assert non-zero exit with error message
    - Test 4: `cka-sim score` (no arg) — assert it picks the most recent session
    - Clean up temp dir

    **list_history.sh**:
    - Set up: create temp ~/.cka-sim/sessions/ with 2 fixture sessions (one complete, one in-progress)
    - Test 1: `cka-sim list history` — assert output contains both session timestamps
    - Test 2: Assert in-progress session shows "(in-progress)" in output
    - Test 3: Assert output is sorted (most recent first)
    - Test 4: Empty sessions dir — assert "No exam history" message
    - Clean up

    Both: `chmod +x`, standard test shape.
  </action>
  <acceptance_criteria>
    - `test -x cka-sim/tests/exam/score_command.sh && test -x cka-sim/tests/exam/list_history.sh`
    - `bash -n cka-sim/tests/exam/score_command.sh` exits 0
    - `bash -n cka-sim/tests/exam/list_history.sh` exits 0
    - `bash cka-sim/tests/run.sh` exits 0
    - `bash cka-sim/scripts/test.sh` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `bash cka-sim/scripts/test.sh` exits 0
- score and list history tests pass
</verification>

<success_criteria>
- Candidate can view past exam reports via `cka-sim score`
- Candidate can see exam history via `cka-sim list history`
- On-demand report regeneration works when .md is missing
</success_criteria>

<output>
Create `.planning/phases/07-exam-mode-blueprint-alpha-reporting/07-04-SUMMARY.md`
</output>
