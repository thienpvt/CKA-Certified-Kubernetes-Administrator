---
phase: 07-exam-mode-blueprint-alpha-reporting
plan: 06
type: execute
wave: 5
depends_on: [07-05]
files_modified:
  - cka-sim/scripts/lint-packs.sh
  - cka-sim/tests/exam/lint_blueprint.sh
  - cka-sim/tests/fixtures/exam/lint-blueprint/good/manifest.yaml
  - cka-sim/tests/fixtures/exam/lint-blueprint/good/README.md
  - cka-sim/tests/fixtures/exam/lint-blueprint/bad-count/manifest.yaml
  - cka-sim/tests/fixtures/exam/lint-blueprint/bad-weights/manifest.yaml
  - cka-sim/tests/fixtures/exam/lint-blueprint/bad-dupes/manifest.yaml
  - cka-sim/tests/fixtures/exam/lint-blueprint/bad-sum/manifest.yaml
  - cka-sim/tests/fixtures/exam/lint-blueprint/bad-disclaimer/manifest.yaml
autonomous: true
requirements: [MOCK-01, MOCK-03]
tags: [bash-lint, blueprint, ci]
validation:
  sampling_rate: per-task-commit
  quick_command: "bash cka-sim/scripts/lint-packs.sh"
  full_command: "bash cka-sim/scripts/test.sh"
must_haves:
  truths:
    - "lint-packs.sh gains a new 'pass H: blueprint manifest lint' after existing passes"
    - "Pass H discovers exams/ at repo root via EXAMS_DIR=${CKA_SIM_LINT_EXAMS_DIR:-$REPO_ROOT/exams}"
    - "Pass H validates: count==17, weighting fields present with values 10/15/20/25/30, every (pack,slug) resolves to existing question dir, no duplicate (pack,slug), sum estimatedMinutes in [120,130], exam.disclaimer present with MOCK-03 literal, README.md contains MOCK-03 literal"
    - "Pass H uses same error accumulator pattern as passes A-G"
    - "lint_blueprint.sh test exercises good fixture (pass) + 5 bad fixtures (each fails for a specific reason)"
    - "bash cka-sim/scripts/test.sh remains green"
    - "exams/blueprint-alpha/manifest.yaml passes pass H"
  artifacts:
    - path: "cka-sim/scripts/lint-packs.sh"
      provides: "New pass H between existing passes and final summary"
      contains: "pass H: blueprint manifest"
    - path: "cka-sim/tests/exam/lint_blueprint.sh"
      provides: "Positive + negative fixture test for pass H"
      contains: "lint-blueprint"
---

<objective>
Extend `cka-sim/scripts/lint-packs.sh` with pass H — blueprint manifest validation. This pass enforces MOCK-01 constraints (17 questions, correct weighting, valid references, time budget) and MOCK-03 (disclaimer presence) at CI time. Author a test case with 1 good + 5 bad fixtures proving each validation rule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-exam-mode-blueprint-alpha-reporting/07-CONTEXT.md
@.planning/phases/07-exam-mode-blueprint-alpha-reporting/07-RESEARCH.md
@cka-sim/scripts/lint-packs.sh
@cka-sim/tests/cases/lint_packs_metadata.sh
@exams/blueprint-alpha/manifest.yaml
@exams/blueprint-alpha/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pass H to lint-packs.sh</name>
  <files>cka-sim/scripts/lint-packs.sh</files>
  <read_first>
    - cka-sim/scripts/lint-packs.sh (full file — understand existing pass structure, final summary block location, error accumulator pattern, _strip_quotes helper, _in_array helper)
    - cka-sim/lib/exam-blueprint.sh (cka_sim::blueprint:: functions — can source this for validate() or reimplement inline for lint independence)
    - exams/blueprint-alpha/manifest.yaml (the real manifest pass H must validate)
    - .planning/phases/07-exam-mode-blueprint-alpha-reporting/07-CONTEXT.md (D-15..D-20 for validation rules)
    - .planning/phases/07-exam-mode-blueprint-alpha-reporting/07-RESEARCH.md (§"CI Integration" for pass H design)
  </read_first>
  <action>
    Append pass H to `cka-sim/scripts/lint-packs.sh` BEFORE the final summary block. Use the same pattern as existing passes (info header, iteration, error accumulation).

    Pass H block:
    1. `info "pass H: blueprint manifest lint"`
    2. Resolve exams dir: `EXAMS_DIR="${CKA_SIM_LINT_EXAMS_DIR:-$REPO_ROOT/exams}"`
    3. Guard: `if [[ -d "$EXAMS_DIR" ]]; then ... fi` (skip if no exams/ dir — e.g., during early-phase fixture tests)
    4. Find manifests: `find "$EXAMS_DIR" -mindepth 2 -maxdepth 2 -name manifest.yaml`
    5. For each manifest, validate:
       a. **Count:** Parse questions (grep -c 'slug:' or use the pure-bash walker). Assert == 17.
       b. **Weighting:** Assert manifest contains `storage: 10`, `workloads-scheduling: 15`, `services-networking: 20`, `cluster-architecture: 25`, `troubleshooting: 30` (grep for each).
       c. **No duplicates:** Extract all (pack, slug) pairs, sort, check for adjacent duplicates via `sort | uniq -d`.
       d. **Resolution:** For each (pack, slug) pair, assert `$CKA_SIM_ROOT/packs/$pack/$slug/` exists and contains 6 required files. Use `CKA_SIM_ROOT` (already set at top of lint-packs.sh).
       e. **Sum estimatedMinutes:** For each resolved question dir, read `estimatedMinutes` from metadata.yaml. Sum all. Assert in [120, 130].
       f. **Disclaimer in manifest:** Assert manifest contains literal "Not real CKA exam content; independently authored" (grep -qF).
       g. **README disclaimer:** Assert `$(dirname "$manifest")/README.md` exists and contains the same literal string.
    6. Each failed check: `err "BLUEPRINT: $manifest: <specific failure message>"` + `errors=$(( errors + 1 ))`
    7. `checked=$(( checked + 1 ))` per manifest

    Implementation note: Do NOT source exam-blueprint.sh from lint-packs.sh (lint should be self-contained with minimal dependencies). Reimplement the checks inline using grep/awk — simpler and avoids circular sourcing issues.
  </action>
  <acceptance_criteria>
    - `bash -n cka-sim/scripts/lint-packs.sh` exits 0
    - `grep -q 'pass H: blueprint manifest' cka-sim/scripts/lint-packs.sh`
    - `grep -q 'CKA_SIM_LINT_EXAMS_DIR' cka-sim/scripts/lint-packs.sh` (test-mode override)
    - `grep -q 'BLUEPRINT:' cka-sim/scripts/lint-packs.sh` (error prefix)
    - `bash cka-sim/scripts/lint-packs.sh` exits 0 (real blueprint-alpha passes)
    - `bash cka-sim/scripts/test.sh` exits 0 (no regression)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: lint_blueprint.sh test + fixtures</name>
  <files>cka-sim/tests/exam/lint_blueprint.sh, cka-sim/tests/fixtures/exam/lint-blueprint/**</files>
  <read_first>
    - cka-sim/tests/cases/lint_packs_metadata.sh (test case shape — subprocess capture, assert rc + output)
    - cka-sim/tests/cases/lint_packs_forbidden_command.sh (fixture copy + CKA_SIM_LINT_PACKS_DIR override pattern)
    - cka-sim/scripts/lint-packs.sh (Task 1 output — pass H with CKA_SIM_LINT_EXAMS_DIR override)
    - exams/blueprint-alpha/manifest.yaml (good fixture reference)
  </read_first>
  <action>
    Create fixture directory `cka-sim/tests/fixtures/exam/lint-blueprint/` with:

    **good/** — valid manifest + README (copy from real blueprint-alpha, or create minimal valid version referencing mock-pack-alpha questions if real packs aren't available in test context). Include README.md with disclaimer.

    **bad-count/** — manifest with 16 questions (one removed). README with disclaimer.
    **bad-weights/** — manifest with `storage: 15` (wrong weight). README with disclaimer.
    **bad-dupes/** — manifest with a duplicate (pack, slug) pair. README with disclaimer.
    **bad-sum/** — manifest where estimatedMinutes sum to 140 (outside [120,130]). README with disclaimer.
    **bad-disclaimer/** — manifest WITHOUT the disclaimer string. README also missing it.

    Note: For resolution checks to work in test context, the test must either:
    - Point CKA_SIM_ROOT at a dir containing the real packs (use the actual repo root), OR
    - Skip resolution checks in the bad-* fixtures by making them fail on an earlier check (count/weights/dupes/sum fail before resolution is reached)

    Simplest approach: bad-* fixtures fail on their specific rule BEFORE resolution is attempted (lint checks in order: count → weights → dupes → resolution → sum → disclaimer). So bad-count fails at count check, bad-weights at weights, etc.

    Create `cka-sim/tests/exam/lint_blueprint.sh`:
    - Shape: `#!/bin/bash`, `set -uo pipefail`, source assert.sh, case_failed accumulator
    - For the **good** fixture: set `CKA_SIM_LINT_EXAMS_DIR` to a temp dir containing `good/manifest.yaml` + `good/README.md` (structured as `exams/test-blueprint/manifest.yaml`). Assert lint exits 0 and output does NOT contain "BLUEPRINT:".
    - For each **bad-*** fixture: same pattern, assert lint exits non-zero and output contains "BLUEPRINT:" with the expected error substring.
    - `chmod +x`

    Note on good fixture: since pass H checks that (pack, slug) resolves to real dirs, the good fixture test must run with CKA_SIM_ROOT pointing at the real repo root (so `cka-sim/packs/storage/01-pvc-binding/` exists). This is fine — tests already run from the repo root.
  </action>
  <acceptance_criteria>
    - `test -x cka-sim/tests/exam/lint_blueprint.sh`
    - `bash -n cka-sim/tests/exam/lint_blueprint.sh` exits 0
    - `test -d cka-sim/tests/fixtures/exam/lint-blueprint/good`
    - `test -d cka-sim/tests/fixtures/exam/lint-blueprint/bad-count`
    - `test -d cka-sim/tests/fixtures/exam/lint-blueprint/bad-weights`
    - `test -d cka-sim/tests/fixtures/exam/lint-blueprint/bad-dupes`
    - `test -d cka-sim/tests/fixtures/exam/lint-blueprint/bad-sum`
    - `test -d cka-sim/tests/fixtures/exam/lint-blueprint/bad-disclaimer`
    - `bash cka-sim/tests/run.sh` exits 0 (lint_blueprint test passes)
    - `bash cka-sim/scripts/test.sh` exits 0 (full suite green)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
### T-7-08: Pass H false-negative allows invalid blueprint to merge
- **Threat:** A typo in pass H's grep pattern causes it to miss a validation rule, allowing a broken manifest through CI.
- **Impact:** high — exam crashes at runtime with cryptic error.
- **Mitigation:** 5 negative fixtures each target a specific rule. If any rule's grep is wrong, the corresponding bad-* fixture test fails (lint exits 0 when it should exit non-zero).
- **Verification:** `tests/exam/lint_blueprint.sh` exercises all 5 failure modes + 1 positive case.

### T-7-09: Pass H breaks existing lint-packs.sh behavior
- **Threat:** Pass H insertion corrupts the final summary block or interferes with existing passes.
- **Impact:** medium — all PRs fail CI.
- **Mitigation:** Pass H is appended BEFORE the summary block (same insertion pattern as passes A-G). `bash cka-sim/scripts/test.sh` runs the full existing test suite which exercises passes A-G.
- **Verification:** Full suite green after Task 1.
</threat_model>

<verification>
- `bash cka-sim/scripts/lint-packs.sh` exits 0 (real blueprint-alpha passes pass H)
- `bash cka-sim/scripts/test.sh` exits 0 (full suite including lint_blueprint test)
- No regression to existing passes A-G
</verification>

<success_criteria>
- Blueprint manifests are validated at CI time — invalid manifests cannot merge
- Every MOCK-01 constraint (count, weighting, resolution, time budget, no dupes) is enforced
- MOCK-03 disclaimer is enforced in both manifest and README
</success_criteria>

<output>
Create `.planning/phases/07-exam-mode-blueprint-alpha-reporting/07-06-SUMMARY.md`
</output>
