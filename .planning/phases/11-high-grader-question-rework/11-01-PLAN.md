---
phase: 11-high-grader-question-rework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cka-sim/packs/troubleshooting/04-debug-node/question.md
  - cka-sim/packs/troubleshooting/04-debug-node/grade.sh
  - cka-sim/packs/troubleshooting/04-debug-node/ref-solution.sh
  - cka-sim/packs/troubleshooting/04-debug-node/metadata.yaml
autonomous: true
requirements: [BUG-H05]
must_haves:
  truths:
    - "question.md explicitly tells the candidate that ANY Kubernetes-native node-introspection technique (kubectl debug node, hand-rolled privileged Pod with hostPID/hostNetwork, ephemeral debug container) is acceptable, and that the grader scores answer.txt only."
    - "grade.sh has ONE scoring assertion (weight=1): answer.txt content matches kubectl get node $worker -o jsonpath='{.status.nodeInfo.kernelVersion}'. The forgeable kubectl.kubernetes.io/debug-source label gate is gone."
    - "grade.sh keeps the worker-sentinel weight=0 informational check, all three trap detectors (debug-pod-leaked-not-cleaned, debug-ephemeral-vs-node-confusion, debug-node-missing-chroot-host) as advisory, and emit_result as the last call."
    - "ref-solution.sh keeps the hand-rolled debug Pod approach (works on K8s 1.30+ where kubectl debug node auto-deletes), drops the kubectl.kubernetes.io/debug-source label, writes the kernelVersion to answer.txt, and ends with SCORE 1/1 + 0 traps."
    - "Empty submission scores 0/1 (answer.txt empty → mismatch → fail). Trap detectors may still fire as advisory but never gate scoring."
  artifacts:
    - path: cka-sim/packs/troubleshooting/04-debug-node/question.md
      provides: "Loosened framing — any K8s-native node-introspection technique is acceptable; grader scores answer.txt only; do-not-SSH and do-not-modify-worker constraints preserved."
      contains: "any Kubernetes-native"
    - path: cka-sim/packs/troubleshooting/04-debug-node/grade.sh
      provides: "Single weight=1 scoring assertion on answer.txt vs node kernelVersion; preserved worker-sentinel weight=0 check and three trap detectors as advisory; preserved emit_result."
      contains: "answer.txt matches node kernelVersion"
    - path: cka-sim/packs/troubleshooting/04-debug-node/ref-solution.sh
      provides: "Hand-rolled debug Pod (hostPID/hostNetwork/privileged), no debug-source label, writes kernelVersion to answer.txt; comment that this is one valid approach."
      contains: "ref-solution shows one valid approach"
    - path: cka-sim/packs/troubleshooting/04-debug-node/metadata.yaml
      provides: "Reference note tweaked from 'kubectl debug node/<name> semantics and chroot /host' to acknowledge it as one valid technique. Trap list unchanged."
      contains: "one valid technique"
  key_links:
    - from: cka-sim/packs/troubleshooting/04-debug-node/grade.sh
      to: cka-sim/lib/grade.sh
      via: source $CKA_SIM_ROOT/lib/grade.sh — manual CKA_SIM_GRADE_TOTAL increment + answer.txt vs jsonpath compare
      pattern: 'CKA_SIM_GRADE_TOTAL=\$\(\( CKA_SIM_GRADE_TOTAL \+ 1 \)\)'
    - from: cka-sim/packs/troubleshooting/04-debug-node/grade.sh
      to: cka-sim/lib/traps.sh
      via: cka_sim::grade::record_trap calls for debug-pod-leaked-not-cleaned / debug-ephemeral-vs-node-confusion / debug-node-missing-chroot-host (advisory only)
      pattern: 'cka_sim::grade::record_trap debug-(pod-leaked-not-cleaned|ephemeral-vs-node-confusion|node-missing-chroot-host)'
---

<objective>
Fix BUG-H05 (troubleshooting/04-debug-node) — the grader's `kubectl.kubernetes.io/debug-source=<worker>` label evidence gate (grade.sh:35-38, 42, 49) is candidate-forgeable and the ref-solution itself hand-rolls a privileged Pod with that label rather than running `kubectl debug node` (which auto-deletes pods on session close in K8s 1.30+, making it grader-unfriendly). The grader cannot honestly assert "candidate ran kubectl debug node" — it can only assert "the answer is correct".

Per CONTEXT.md locked decision: loosen the question to allow ANY Kubernetes-native node-introspection technique, drop the debug_evidence gate from the grader, score `answer.txt` only, drop the no-longer-load-bearing label from ref-solution, and acknowledge in metadata.yaml that the linked k8s-doc shows one of several valid techniques. Trap detectors stay as advisory diagnostics so a wrong-answer candidate still gets a hint about what they did wrong.

Output: A self-consistent triangle where the question (any K8s-native technique), grader (scores answer.txt only), and ref-solution (one valid hand-rolled Pod) agree. Empty submission still scores 0; ref-solution scores 1/1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-high-grader-question-rework/11-CONTEXT.md
@.planning/forensics/report-20260517-091657-full-audit.md
@.planning/REQUIREMENTS.md
@cka-sim/lib/grade.sh
@cka-sim/lib/traps.sh
@cka-sim/packs/troubleshooting/04-debug-node/question.md
@cka-sim/packs/troubleshooting/04-debug-node/setup.sh
@cka-sim/packs/troubleshooting/04-debug-node/grade.sh
@cka-sim/packs/troubleshooting/04-debug-node/ref-solution.sh
@cka-sim/packs/troubleshooting/04-debug-node/reset.sh
@cka-sim/packs/troubleshooting/04-debug-node/metadata.yaml

<interfaces>
From cka-sim/lib/grade.sh:
- Manual scoring pattern (no helper covers "compare a file to a kubectl jsonpath"): increment `CKA_SIM_GRADE_TOTAL`, on pass increment `CKA_SIM_GRADE_PASSED` + append to `CKA_SIM_GRADE_PASSES` + call `ok`; on fail append to `CKA_SIM_GRADE_FAILS` + call `err`.
- `cka_sim::grade::record_trap <id>` — dedups by id (advisory; does NOT add to score).
- `cka_sim::grade::emit_result` — final SCORE + Trap N: lines.

From cka-sim/lib/traps.sh:
- The three q04 detectors live inline in this grader (not as library detectors) and are invoked via `cka_sim::grade::record_trap <id>` after their inline conditional fires. The catalog ids `debug-pod-leaked-not-cleaned`, `debug-ephemeral-vs-node-confusion`, `debug-node-missing-chroot-host` remain registered.

Current grade.sh scoring shape (lines 35-49):
- Single conditional bundled assertion: pass requires `actual == expected` AND `-n debug_evidence`. We split this — drop the debug_evidence requirement; pass on `actual == expected` alone.

Setup state (setup.sh) — unchanged:
- Writes `/tmp/q04-debug-node/worker.txt` (one line: chosen worker hostname) and an empty `answer.txt`.
- Creates `${CKA_SIM_LAB_NS}` only — no other cluster objects.

Ref-solution.sh current shape (line 14-39): applies a privileged Pod named `node-debugger-${worker}` with the `kubectl.kubernetes.io/debug-source: ${worker}` label, exec's into it to read `/proc/version`, parses to kernel version, writes to answer.txt. The label was load-bearing for the old grader's evidence gate; with the gate gone, the label is decorative and should go.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Loosen question.md framing</name>
  <read_first>
    - cka-sim/packs/troubleshooting/04-debug-node/question.md (current 28-line file — title already says "Inspect a worker node read-only" which is already neutral; body needs the "any K8s-native technique" clarification and an explicit "grader scores answer.txt only" line)
    - .planning/phases/11-high-grader-question-rework/11-CONTEXT.md (BUG-H05 fix path step 1)
  </read_first>
  <files>cka-sim/packs/troubleshooting/04-debug-node/question.md</files>
  <action>
    Rewrite the body of cka-sim/packs/troubleshooting/04-debug-node/question.md while keeping the heading style and the "Sandbox", "Constraints", "Verify yourself" sections. Concrete content changes:

    1. Title stays `# Troubleshooting: Inspect a worker node read-only`.
    2. Domain/time line stays `**Domain:** Troubleshooting  |  **Estimated time:** 9 minutes`.
    3. Replace the opening paragraph (current line 5) with two paragraphs:

       Paragraph 1 (kept symptom claim, unchanged wording):
       `A scheduling decision depends on a kernel property of one specific worker node. Discover the exact kernel version string from that worker's host and record it in the sandbox answer file, without modifying anything on the worker host.`

       Paragraph 2 (NEW — loosens technique constraint):
       `You may use any Kubernetes-native node-introspection technique: 'kubectl debug node/<worker>', a hand-rolled privileged Pod with 'hostPID' or 'hostNetwork' bound to the worker via 'nodeName', an ephemeral debug container, or any other read-only Pod-based approach. The grader scores '/tmp/q04-debug-node/answer.txt' only — it does not inspect which mechanism you used.`

       Use single quotes around resource/path tokens to match the existing markdown style (line 11-12 of the current file).

    4. Sandbox section (current lines 7-12) — keep verbatim.

    5. Constraints section — keep the three existing bullets verbatim:
       - `- Do not modify any file on the worker host.`
       - `- Do not SSH to the worker; use a Kubernetes-native mechanism.`
       - `- Do not add or modify cluster objects outside '/tmp/q04-debug-node/' and transient inspection tooling you use.`

       Do NOT add new constraints. Do NOT remove the no-SSH or no-mutation rules — they are the meaningful guardrails after the technique constraint is loosened.

    6. Verify yourself section (current lines 20-27) — keep verbatim.

    LF line endings. No CRLF.
  </action>
  <verify>
    <automated>grep -c 'any Kubernetes-native' cka-sim/packs/troubleshooting/04-debug-node/question.md</automated>
  </verify>
  <acceptance_criteria>
    - File contains the "any Kubernetes-native" loosening exactly once: `grep -c 'any Kubernetes-native' cka-sim/packs/troubleshooting/04-debug-node/question.md` returns 1.
    - File explicitly states the grader scores answer.txt only: `grep -c "grader scores '/tmp/q04-debug-node/answer.txt' only" cka-sim/packs/troubleshooting/04-debug-node/question.md` returns 1.
    - The three current constraints are preserved verbatim: `grep -c '^- Do not modify any file on the worker host\.$' cka-sim/packs/troubleshooting/04-debug-node/question.md` returns 1; `grep -c '^- Do not SSH to the worker; use a Kubernetes-native mechanism\.$' cka-sim/packs/troubleshooting/04-debug-node/question.md` returns 1; `grep -c "^- Do not add or modify cluster objects outside '/tmp/q04-debug-node/' and transient inspection tooling you use\.$" cka-sim/packs/troubleshooting/04-debug-node/question.md` returns 1.
    - Sandbox and Verify-yourself sections preserved: `grep -c '^## Sandbox$' cka-sim/packs/troubleshooting/04-debug-node/question.md` returns 1; `grep -c '^## Verify yourself$' cka-sim/packs/troubleshooting/04-debug-node/question.md` returns 1.
    - Verify-yourself jsonpath retained: `grep -c "kubectl get node \"\$worker\" -o jsonpath='{.status.nodeInfo.kernelVersion}'" cka-sim/packs/troubleshooting/04-debug-node/question.md` returns 1.
    - Question text mentions at least two valid techniques: `grep -c 'kubectl debug node' cka-sim/packs/troubleshooting/04-debug-node/question.md` returns >= 1; `grep -c 'hostPID\|ephemeral' cka-sim/packs/troubleshooting/04-debug-node/question.md` returns >= 1.
    - Title and domain line unchanged: `grep -c '^# Troubleshooting: Inspect a worker node read-only$' cka-sim/packs/troubleshooting/04-debug-node/question.md` returns 1; `grep -c '^\*\*Domain:\*\* Troubleshooting  |  \*\*Estimated time:\*\* 9 minutes$' cka-sim/packs/troubleshooting/04-debug-node/question.md` returns 1.
  </acceptance_criteria>
  <done>question.md explicitly authorizes any Kubernetes-native node-introspection technique and explicitly states the grader scores answer.txt only; do-not-SSH and do-not-mutate-host constraints preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite grade.sh — drop debug_evidence gate, score answer.txt only</name>
  <read_first>
    - cka-sim/packs/troubleshooting/04-debug-node/grade.sh (current 62-line file: bundled scoring assertion that requires both answer-correct AND debug_evidence non-empty)
    - cka-sim/lib/grade.sh (manual score-counter pattern; assert_field_eq is NOT used here because we are comparing a file to a jsonpath, not querying a resource)
    - cka-sim/lib/traps.sh (record_trap dedup behaviour; the three q04 detector ids must be valid catalog ids)
    - .planning/phases/11-high-grader-question-rework/11-CONTEXT.md (BUG-H05 fix path step 2)
  </read_first>
  <files>cka-sim/packs/troubleshooting/04-debug-node/grade.sh</files>
  <action>
    Rewrite cka-sim/packs/troubleshooting/04-debug-node/grade.sh while preserving the file's header (shebang, comment, `set -uo pipefail`, env guards, `source` statements). Replace the assertion block (lines 20-59) with:

    1. **Header comment block (NEW):** Replace the Phase 07.1 AUDIT-01 comment with a Phase 11 BUG-H05 comment block. Keep ONE sentence summarising the change:

       ```bash
       # troubleshooting/04-debug-node/grade.sh
       # Phase 11 BUG-H05 — score answer.txt only. The previous debug_evidence gate
       # (kubectl.kubernetes.io/debug-source=<worker> label presence) was candidate-
       # forgeable and the ref-solution itself only satisfies it via a hand-rolled
       # privileged Pod, so the gate could not honestly assert "candidate ran kubectl
       # debug node". Trap detectors below remain as advisory diagnostics — they
       # never gate scoring. See 11-CONTEXT.md for the locked decision.
       set -uo pipefail
       ```

    2. **Read sandbox state — unchanged:**

       ```bash
       sandbox="/tmp/q04-debug-node"
       worker=$(cat "$sandbox/worker.txt" 2>/dev/null || echo "")
       ```

    3. **Worker-sentinel weight=0 informational check (kept verbatim from current lines 23-27):**

       ```bash
       if [[ -z "$worker" ]]; then
         CKA_SIM_GRADE_TOTAL=$(( CKA_SIM_GRADE_TOTAL + 1 ))
         CKA_SIM_GRADE_FAILS+=("worker hostname sentinel missing — reset may have run")
         err "worker hostname sentinel missing — reset may have run"
       fi
       ```

       Per the CONTEXT decision "Keep weight=0 informational checks (worker sentinel exists)", but note the current file actually increments TOTAL by 1 here — keep the existing increment behaviour (when the sentinel is missing it consumes 1 point of TOTAL and fails it). This is the existing semantics; do NOT silently change it.

    4. **Compute expected and actual (kept from current lines 29-33, with `debug_evidence` and `ephemeral` lookups removed):**

       ```bash
       expected=""
       if [[ -n "$worker" ]]; then
         expected=$(kubectl get node "$worker" -o jsonpath='{.status.nodeInfo.kernelVersion}' 2>/dev/null || echo "")
       fi
       actual=$(cat "$sandbox/answer.txt" 2>/dev/null || echo "")
       ```

    5. **Single scoring assertion — answer.txt matches expected (REPLACES current bundled assertion at lines 41-55):**

       ```bash
       CKA_SIM_GRADE_TOTAL=$(( CKA_SIM_GRADE_TOTAL + 1 ))
       if [[ -n "$actual" && -n "$expected" && "$actual" == "$expected" ]]; then
         CKA_SIM_GRADE_PASSED=$(( CKA_SIM_GRADE_PASSED + 1 ))
         CKA_SIM_GRADE_PASSES+=("answer.txt matches node kernelVersion")
         ok "answer.txt matches node kernelVersion"
       else
         CKA_SIM_GRADE_FAILS+=("answer.txt must match node kernelVersion")
         err "answer.txt must match node kernelVersion"
       fi
       ```

       Pass message MUST drop the parenthetical "(discovered via kubectl debug node)" — it lies about what is graded.

    6. **Advisory trap detectors (RESHAPED — they no longer gate the score; they only emit hints):**

       a. Re-introduce the same three lookups the current grader does (lines 35-39) — `debug_pods_running`, `debug_pods_succeeded`, `debug_pods_failed`, `ephemeral` — because the inline detectors below need them. Wrap them in a comment: `# Advisory probes — used only by the trap detectors below; not part of scoring.`

       b. `debug-ephemeral-vs-node-confusion`: fires when the answer is correct AND the candidate left no debug-source labelled Pod evidence (i.e. they probably ran an ephemeral debug container against a Pod, not a node — and got the answer right by accident or by reading the node object directly, but the trap remains a useful diagnostic flag). Inline:

       ```bash
       if [[ -n "$actual" && -n "$expected" && "$actual" == "$expected" \
             && -z "${debug_pods_running}${debug_pods_succeeded}${debug_pods_failed}" \
             && -n "$ephemeral" ]]; then
         cka_sim::grade::record_trap debug-ephemeral-vs-node-confusion
       fi
       ```

       Tightened from the current detector (line 50): require `-n "$ephemeral"` so we ONLY hit when the candidate actually used an ephemeral debug container — not just when no debug-source pod exists.

       c. `debug-node-missing-chroot-host`: fires when the answer is wrong AND an ephemeral debug container is present (candidate forgot `chroot /host`). Keep the current condition shape (line 52):

       ```bash
       if [[ "$actual" != "$expected" && -n "$ephemeral" \
             && -z "${debug_pods_running}${debug_pods_succeeded}${debug_pods_failed}" ]]; then
         cka_sim::grade::record_trap debug-node-missing-chroot-host
       fi
       ```

       d. `debug-pod-leaked-not-cleaned`: fires when a Running debug-source Pod is still in the cluster at grade time. Keep verbatim from current lines 57-59:

       ```bash
       if [[ -n "$debug_pods_running" ]]; then
         cka_sim::grade::record_trap debug-pod-leaked-not-cleaned
       fi
       ```

    7. **Finalizer:** keep `cka_sim::grade::emit_result` as the last non-blank line.

    DO NOT remove any source statement. DO NOT change `set -uo pipefail` to `set -e`. DO NOT introduce assert_field_eq — there is no resource/jsonpath to query against the candidate's authored value; the comparison is `file content == expected string`, which is the pattern used by other inline graders.

    LF line endings.
  </action>
  <verify>
    <automated>bash -n cka-sim/packs/troubleshooting/04-debug-node/grade.sh && grep -c 'answer.txt matches node kernelVersion' cka-sim/packs/troubleshooting/04-debug-node/grade.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n cka-sim/packs/troubleshooting/04-debug-node/grade.sh` exits 0.
    - The forgeable label gate is gone: `grep -c 'kubectl.kubernetes.io/debug-source=' cka-sim/packs/troubleshooting/04-debug-node/grade.sh` returns 0; `grep -c 'debug_evidence' cka-sim/packs/troubleshooting/04-debug-node/grade.sh` returns 0; `grep -c 'kubectl debug node evidence must exist' cka-sim/packs/troubleshooting/04-debug-node/grade.sh` returns 0.
    - The Phase 11 BUG-H05 comment block is present: `grep -c 'Phase 11 BUG-H05' cka-sim/packs/troubleshooting/04-debug-node/grade.sh` returns 1.
    - There is exactly one scoring `CKA_SIM_GRADE_TOTAL=$(( CKA_SIM_GRADE_TOTAL + 1 ))` increment in a positive code path (the missing-sentinel branch also has one, so total count is 2): `grep -c 'CKA_SIM_GRADE_TOTAL=$(( CKA_SIM_GRADE_TOTAL + 1 ))' cka-sim/packs/troubleshooting/04-debug-node/grade.sh` returns 2.
    - The kernelVersion jsonpath compare is present exactly once: `grep -c 'jsonpath=.{.status.nodeInfo.kernelVersion}.' cka-sim/packs/troubleshooting/04-debug-node/grade.sh` returns 1.
    - The pass message is honest (no "discovered via kubectl debug node" lie): `grep -c 'discovered via kubectl debug node' cka-sim/packs/troubleshooting/04-debug-node/grade.sh` returns 0; `grep -c '"answer.txt matches node kernelVersion"' cka-sim/packs/troubleshooting/04-debug-node/grade.sh` returns 2 (the ok message + the PASSES append).
    - All three trap detectors are preserved as advisory: `grep -c 'cka_sim::grade::record_trap debug-ephemeral-vs-node-confusion' cka-sim/packs/troubleshooting/04-debug-node/grade.sh` returns 1; `grep -c 'cka_sim::grade::record_trap debug-node-missing-chroot-host' cka-sim/packs/troubleshooting/04-debug-node/grade.sh` returns 1; `grep -c 'cka_sim::grade::record_trap debug-pod-leaked-not-cleaned' cka-sim/packs/troubleshooting/04-debug-node/grade.sh` returns 1.
    - `cka_sim::grade::emit_result` is the last non-blank line: `tail -5 cka-sim/packs/troubleshooting/04-debug-node/grade.sh | grep -c 'cka_sim::grade::emit_result'` returns 1.
    - Source statements preserved: `grep -c 'source "\$CKA_SIM_ROOT/lib/grade.sh"' cka-sim/packs/troubleshooting/04-debug-node/grade.sh` returns 1; `grep -c 'source "\$CKA_SIM_ROOT/lib/traps.sh"' cka-sim/packs/troubleshooting/04-debug-node/grade.sh` returns 1.
    - `set -uo pipefail` preserved (NOT `set -e`): `grep -c '^set -uo pipefail$' cka-sim/packs/troubleshooting/04-debug-node/grade.sh` returns 1.
  </acceptance_criteria>
  <done>grade.sh awards 1 point for answer.txt vs node kernelVersion; debug_evidence gate is gone; three trap detectors remain advisory; worker-sentinel and emit_result preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Update ref-solution.sh — drop debug-source label, add comment</name>
  <read_first>
    - cka-sim/packs/troubleshooting/04-debug-node/ref-solution.sh (current 54-line file: applies a Pod with kubectl.kubernetes.io/debug-source label, exec's into it, writes kernelVersion to answer.txt)
    - .planning/phases/11-high-grader-question-rework/11-CONTEXT.md (BUG-H05 fix path step 3)
  </read_first>
  <files>cka-sim/packs/troubleshooting/04-debug-node/ref-solution.sh</files>
  <action>
    Edit cka-sim/packs/troubleshooting/04-debug-node/ref-solution.sh in place. Two changes only — do NOT rewrite the whole file.

    1. **Replace the file-leading comment block (current lines 8-13)** with a Phase 11 comment:

       BEFORE (lines 8-13):
       ```bash
       echo "# Canonical: kubectl debug node/$worker --image=busybox:1.36 -- chroot /host cat /proc/version" >&2

       # Create a node-debug pod manually with the same label that kubectl debug node
       # would set. This ensures the pod persists for the grader's evidence gate.
       # kubectl debug node auto-deletes the pod on session close in k8s 1.30+, making
       # it unreliable for automated grading.
       ```

       AFTER:
       ```bash
       # Phase 11 BUG-H05 — ref-solution shows ONE valid approach. The question
       # explicitly accepts any Kubernetes-native node-introspection technique
       # (kubectl debug node, hand-rolled privileged Pod, ephemeral debug container).
       # We hand-roll a privileged Pod here because kubectl debug node auto-deletes
       # the pod on session close in K8s 1.30+, which is grader-unfriendly. The grader
       # scores only $sandbox/answer.txt, NOT the technique used to obtain it.
       echo "# Canonical reference: kubectl debug node/$worker --image=busybox:1.36 -- chroot /host cat /proc/version" >&2
       ```

    2. **Drop the `kubectl.kubernetes.io/debug-source: ${worker}` label from the Pod manifest (current lines 19-20):**

       BEFORE (lines 17-20 of the heredoc):
       ```yaml
       metadata:
         name: node-debugger-${worker}
         labels:
           kubectl.kubernetes.io/debug-source: "${worker}"
       ```

       AFTER:
       ```yaml
       metadata:
         name: node-debugger-${worker}
       ```

       Drop the entire `labels:` map and the single label line under it. The Pod no longer needs a label for the grader (the grader does not look at labels anymore). Keep the rest of the manifest verbatim — `spec.nodeName`, `hostPID`, `hostNetwork`, `restartPolicy`, the privileged container with `chroot /host cat /proc/version`, the hostPath volume — all kept exactly as today.

    Do NOT change the kubectl wait, the exec, the parsing, or the answer.txt write. Do NOT change `set -euo pipefail`. The reset.sh selector that cleans up debug-source-labelled pods (`-l 'kubectl.kubernetes.io/debug-source'`) remains valid for legacy candidate runs that DO use kubectl debug node — see Task 5 for why we keep reset.sh untouched.

    LF line endings.
  </action>
  <verify>
    <automated>bash -n cka-sim/packs/troubleshooting/04-debug-node/ref-solution.sh && grep -c 'kubectl.kubernetes.io/debug-source' cka-sim/packs/troubleshooting/04-debug-node/ref-solution.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n cka-sim/packs/troubleshooting/04-debug-node/ref-solution.sh` exits 0.
    - The debug-source label is gone from ref-solution: `grep -c 'kubectl.kubernetes.io/debug-source' cka-sim/packs/troubleshooting/04-debug-node/ref-solution.sh` returns 0.
    - The Pod still gets created with the same name: `grep -c 'name: node-debugger-\${worker}' cka-sim/packs/troubleshooting/04-debug-node/ref-solution.sh` returns 1.
    - hostPID/hostNetwork/privileged/hostPath path-/ are preserved: `grep -c 'hostPID: true' cka-sim/packs/troubleshooting/04-debug-node/ref-solution.sh` returns 1; `grep -c 'hostNetwork: true' cka-sim/packs/troubleshooting/04-debug-node/ref-solution.sh` returns 1; `grep -c 'privileged: true' cka-sim/packs/troubleshooting/04-debug-node/ref-solution.sh` returns 1; `grep -c 'path: /' cka-sim/packs/troubleshooting/04-debug-node/ref-solution.sh` returns 1.
    - The "one valid approach" comment block is present: `grep -c 'ref-solution shows ONE valid approach' cka-sim/packs/troubleshooting/04-debug-node/ref-solution.sh` returns 1.
    - The exec + parse + answer.txt write are preserved: `grep -c 'kubectl exec "node-debugger-\${worker}"' cka-sim/packs/troubleshooting/04-debug-node/ref-solution.sh` returns 1; `grep -c 'awk .*Linux version' cka-sim/packs/troubleshooting/04-debug-node/ref-solution.sh` returns 1; `grep -c "printf '%s\\\\n' .* > \"\$sandbox/answer.txt\"" cka-sim/packs/troubleshooting/04-debug-node/ref-solution.sh` returns 2.
    - `set -euo pipefail` preserved: `grep -c 'set -euo pipefail' cka-sim/packs/troubleshooting/04-debug-node/ref-solution.sh` returns 1.
  </acceptance_criteria>
  <done>ref-solution.sh hand-rolls the privileged debug Pod without the now-decorative debug-source label; comment is honest about it being one valid technique among several.</done>
</task>

<task type="auto">
  <name>Task 4: Update metadata.yaml reference note (no constraint enforcement)</name>
  <read_first>
    - cka-sim/packs/troubleshooting/04-debug-node/metadata.yaml (current 16-line file — trap list + two references; the k8s-doc note "kubectl debug node/<name> semantics and chroot /host" is informational but reads as if that is the prescribed technique)
    - .planning/phases/11-high-grader-question-rework/11-CONTEXT.md (BUG-H05 fix path step 4 — "metadata.yaml: update if it documents 'must use kubectl debug node' constraint")
  </read_first>
  <files>cka-sim/packs/troubleshooting/04-debug-node/metadata.yaml</files>
  <action>
    Edit one line of cka-sim/packs/troubleshooting/04-debug-node/metadata.yaml. The file does NOT contain a `description` or `summary` field that mirrors the stale "must use kubectl debug node" framing — but the k8s-doc reference's `note` field reads as prescriptive. Soften it.

    BEFORE (line 15):
    ```yaml
        note: kubectl debug node/<name> semantics and chroot /host
    ```

    AFTER:
    ```yaml
        note: kubectl debug node/<name> — one valid technique; question accepts any Kubernetes-native node-introspection approach
    ```

    DO NOT change:
    - `id: troubleshooting-debug-node`
    - `domain: troubleshooting`
    - `estimatedMinutes: 9`
    - `verified_against: "1.35"`
    - The `traps:` list — all three ids stay (they are still meaningful as advisory diagnostics).
    - The prior-art-exercise reference (etcd-backup-restore) — unchanged.
    - The k8s-doc reference's `kind:` and `target:` fields — only the `note:` is reworded.

    LF line endings. No CRLF.
  </action>
  <verify>
    <automated>grep -c 'one valid technique' cka-sim/packs/troubleshooting/04-debug-node/metadata.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'one valid technique' cka-sim/packs/troubleshooting/04-debug-node/metadata.yaml` returns 1.
    - `grep -c 'any Kubernetes-native node-introspection' cka-sim/packs/troubleshooting/04-debug-node/metadata.yaml` returns 1.
    - The trap list is unchanged: `grep -c '^  - debug-node-missing-chroot-host$' cka-sim/packs/troubleshooting/04-debug-node/metadata.yaml` returns 1; `grep -c '^  - debug-pod-leaked-not-cleaned$' cka-sim/packs/troubleshooting/04-debug-node/metadata.yaml` returns 1; `grep -c '^  - debug-ephemeral-vs-node-confusion$' cka-sim/packs/troubleshooting/04-debug-node/metadata.yaml` returns 1.
    - The id/domain/estimatedMinutes/verified_against fields are unchanged: `grep -c '^id: troubleshooting-debug-node$' cka-sim/packs/troubleshooting/04-debug-node/metadata.yaml` returns 1; `grep -c '^domain: troubleshooting$' cka-sim/packs/troubleshooting/04-debug-node/metadata.yaml` returns 1; `grep -c '^estimatedMinutes: 9$' cka-sim/packs/troubleshooting/04-debug-node/metadata.yaml` returns 1; `grep -c '^verified_against: "1.35"$' cka-sim/packs/troubleshooting/04-debug-node/metadata.yaml` returns 1.
    - The k8s-doc target URL is preserved: `grep -c 'https://kubernetes.io/docs/tasks/debug/debug-cluster/kubectl-node-debug/' cka-sim/packs/troubleshooting/04-debug-node/metadata.yaml` returns 1.
    - The prior-art reference target preserved: `grep -c 'cka-sim/packs/cluster-architecture/02-etcd-backup-restore/' cka-sim/packs/troubleshooting/04-debug-node/metadata.yaml` returns 1.
    - No `description:` or `summary:` field was newly added (we don't fabricate one): `grep -cE '^(description|summary):' cka-sim/packs/troubleshooting/04-debug-node/metadata.yaml` returns 0.
  </acceptance_criteria>
  <done>metadata.yaml reference note no longer reads as "you must use kubectl debug node"; trap list and identity fields preserved.</done>
</task>

<task type="auto">
  <name>Task 5: Quick scan — confirm setup.sh and reset.sh need no changes</name>
  <read_first>
    - cka-sim/packs/troubleshooting/04-debug-node/setup.sh
    - cka-sim/packs/troubleshooting/04-debug-node/reset.sh
  </read_first>
  <files>(read-only verification — produces no edits unless a hidden coupling is found)</files>
  <action>
    Run a series of greps to confirm setup.sh and reset.sh do not rely on the debug-source label OR on the dropped debug_evidence gate. Expected results:

    1. setup.sh — only seeds the namespace, sandbox dir, worker.txt sentinel, and an empty answer.txt. It does NOT create any Pod and does NOT reference any label. Confirm with `grep -cE 'kubectl.kubernetes.io/debug-source|debug_evidence|debug-source' cka-sim/packs/troubleshooting/04-debug-node/setup.sh` returns 0.

    2. reset.sh — line 8 currently does `kubectl get pods --all-namespaces -l 'kubectl.kubernetes.io/debug-source' -o name | xargs -r kubectl delete --ignore-not-found`. KEEP this line — it cleans up legacy debug-source-labelled Pods (e.g. from candidates who used kubectl debug node, which DOES auto-set this label even if the candidate doesn't add it manually). The label being absent in our new ref-solution does not invalidate this selector; the selector is intentionally broad so reset works for any candidate technique. Confirm the line exists: `grep -c "kubectl get pods --all-namespaces -l 'kubectl.kubernetes.io/debug-source' -o name" cka-sim/packs/troubleshooting/04-debug-node/reset.sh` returns 1.

       Note: Because our new ref-solution's Pod has NO debug-source label, reset's label-selector cleanup will NOT delete it. We rely on the namespace cleanup at line 14 (`kubectl delete namespace "$CKA_SIM_LAB_NS" --ignore-not-found --wait=false`) to evict our ref-solution Pod. This is fine — the ref-solution Pod is in `$CKA_SIM_LAB_NS`, so the namespace delete catches it. Confirm: `grep -c 'kubectl delete namespace "\$CKA_SIM_LAB_NS"' cka-sim/packs/troubleshooting/04-debug-node/reset.sh` returns 1.

    If any unexpected hit is found, STOP and surface to the user — it would mean the BUG-H05 fix needs to extend to setup.sh or reset.sh, which CONTEXT.md does NOT authorize.

    Report grep output in the SUMMARY.
  </action>
  <verify>
    <automated>grep -cE 'kubectl.kubernetes.io/debug-source|debug_evidence' cka-sim/packs/troubleshooting/04-debug-node/setup.sh</automated>
  </verify>
  <acceptance_criteria>
    - setup.sh has no debug-source-label or debug_evidence reference: `grep -cE 'kubectl.kubernetes.io/debug-source|debug_evidence|debug-source' cka-sim/packs/troubleshooting/04-debug-node/setup.sh` returns 0.
    - reset.sh's label-based legacy-pod cleanup is preserved: `grep -c "kubectl get pods --all-namespaces -l 'kubectl.kubernetes.io/debug-source' -o name" cka-sim/packs/troubleshooting/04-debug-node/reset.sh` returns 1.
    - reset.sh's namespace delete is preserved (it catches the new ref-solution's unlabelled Pod): `grep -c 'kubectl delete namespace "\$CKA_SIM_LAB_NS"' cka-sim/packs/troubleshooting/04-debug-node/reset.sh` returns 1.
    - reset.sh's per-question tmp cleanup preserved: `grep -c 'rm -rf /tmp/cka-sim/04-debug-node/' cka-sim/packs/troubleshooting/04-debug-node/reset.sh` returns 1.
    - No file outside the four touched in this plan was edited: `git diff --name-only -- cka-sim/packs/troubleshooting/04-debug-node/ | sort` should list ONLY question.md, grade.sh, ref-solution.sh, metadata.yaml.
  </acceptance_criteria>
  <done>Verified that BUG-H05 is genuinely a 4-file edit (question.md + grade.sh + ref-solution.sh + metadata.yaml note); setup.sh and reset.sh need no modification.</done>
</task>

</tasks>

<verification>
After all 5 tasks complete:

1. `bash -n cka-sim/packs/troubleshooting/04-debug-node/{grade,ref-solution}.sh` — both syntactically valid.
2. `bash scripts/lint-packs.sh troubleshooting/04-debug-node` (if the linter exists in this milestone — verify by `test -x scripts/lint-packs.sh`) — no new errors.
3. Live-cluster GRADE round-trip will be performed in execute-phase verification (out of scope here):
   - `cka-sim drill troubleshooting 04-debug-node` then run ref-solution.sh → expect SCORE 1/1, 0 traps recorded (no debug-source label means no leaked-pod trap; the Pod is still in the lab namespace so `debug-pod-leaked-not-cleaned` won't fire because it now requires the label-selector hit).

     Wait — recheck: detector queries `kubectl get pods --all-namespaces -l "kubectl.kubernetes.io/debug-source=$worker"`. With the label gone, no pods match, so `debug_pods_running` is empty → trap doesn't fire on ref-solution. Good.

   - Empty submission → answer.txt is empty → mismatch → fail → SCORE 0/1; no trap fires (ephemeral is empty AND no debug-source pods). Acceptable per CONTEXT "Empty submission must score 0".
4. The trap-coverage lint (Phase 12 LINT-01) will continue to find all three trap ids in the grader; metadata.yaml entries match.
</verification>

<success_criteria>
- ref-solution scores 1/1 on a live cluster (ROADMAP Phase 11 success criterion #1, second branch — "OR question is loosened to allow any privileged-pod approach and grades only answer.txt"; that branch is the one CONTEXT.md selected).
- Empty submission scores 0/1.
- The forgeable kubectl.kubernetes.io/debug-source label gate is gone from grade.sh.
- ref-solution.sh's debug-source label is gone (it was load-bearing only for the dropped gate).
- question.md explicitly tells the candidate any K8s-native node-introspection technique is acceptable and the grader scores answer.txt only.
- Trap detectors remain as advisory diagnostics — no scoring side-effect.
</success_criteria>

<output>
After completion, create `.planning/phases/11-high-grader-question-rework/11-01-SUMMARY.md` documenting:
- Files modified: question.md, grade.sh, ref-solution.sh, metadata.yaml
- Honesty rebuild: dropped forgeable kubectl.kubernetes.io/debug-source label gate (grade.sh) and the load-bearing label on the ref-solution Pod
- Scoring shape: 1 weight=1 scoring assertion (answer.txt vs node kernelVersion) + worker-sentinel weight=1 informational fail-only check + 3 advisory trap detectors
- Verification: ref-solution 1/1, empty submission 0/1 (to be confirmed by execute-phase live drill)
- Files surveyed and confirmed unchanged: setup.sh, reset.sh (Task 5 grep results)
</output>
