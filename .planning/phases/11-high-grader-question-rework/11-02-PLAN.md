---
phase: 11-high-grader-question-rework
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cka-sim/packs/troubleshooting/05-static-pod-manifest/question.md
  - cka-sim/packs/troubleshooting/05-static-pod-manifest/metadata.yaml
autonomous: true
requirements: [BUG-H06]
must_haves:
  truths:
    - "question.md title is 'Repair the static-pod manifest' (no longer 'Static pod never becomes Running'), and the lead paragraph frames the task as repairing a broken static-pod manifest with intentional defects (tab indent + image typo) so it is valid YAML, defines a single Pod, and passes client dry-run."
    - "question.md Tasks list and Constraints list are preserved (already YAML-repair focused); Verify-yourself commands are preserved verbatim."
    - "metadata.yaml has no `description` or `summary` field that mirrors the stale 'static pod never becomes Running' framing; if such a field exists today it is rewritten or omitted; trap entries (static-pod-manifest-bad-yaml, static-pod-image-tag-typo) and the third entry (default-sa-used) stay as is."
    - "grade.sh, setup.sh, ref-solution.sh, reset.sh are NOT modified — they already correctly implement and test YAML repair."
    - "Empty submission still scores 0/3 (the broken setup-seeded manifest fails YAML parse + kind=Pod + dry-run); ref-solution still scores 3/3."
  artifacts:
    - path: cka-sim/packs/troubleshooting/05-static-pod-manifest/question.md
      provides: "Title + lead paragraph reframed as YAML repair; Tasks/Constraints/Verify preserved."
      contains: "Repair the static-pod manifest"
    - path: cka-sim/packs/troubleshooting/05-static-pod-manifest/metadata.yaml
      provides: "Identity/trap fields unchanged; if the file ever had a stale-framing description/summary it is brought into agreement with the new question framing."
      contains: "troubleshooting-static-pod-manifest"
  key_links:
    - from: cka-sim/packs/troubleshooting/05-static-pod-manifest/question.md
      to: cka-sim/packs/troubleshooting/05-static-pod-manifest/grade.sh
      via: question Tasks bullet 3 ("Confirm acceptability via client-side dry-run") matches grade.sh's `kubectl apply --dry-run=client` assertion at line 62
      pattern: 'kubectl apply --dry-run=client -f /tmp/q05-staticpod/manifest.yaml'
    - from: cka-sim/packs/troubleshooting/05-static-pod-manifest/question.md
      to: cka-sim/packs/troubleshooting/05-static-pod-manifest/setup.sh
      via: question lead paragraph mentions "intentional defects (tab indent + image typo)"; setup.sh:35 seeds tab-indent variant + setup.sh:53 seeds image-tag-typo variant; setup.sh:63 cp's the broken variant to manifest.yaml
      pattern: 'manifest-broken.yaml.*manifest-tagtypo.yaml'
---

<objective>
Fix BUG-H06 (troubleshooting/05-static-pod-manifest) — the question's title and lead paragraph frame the task as "Static pod never becomes Running" / "When manifest is placed in node-agent static workload directory, intended Pod q05-cache never appears in kubectl get pods -A or never reaches Running". The body Tasks, the Constraints, and the grader actually test pure YAML repair (parseability, kind=Pod, client dry-run) — there is no mirror-pod check, no kubelet-pickup test, no Running assertion. Skill named ≠ skill graded; candidate is told to make a Pod Run but is graded on YAML hygiene.

Per CONTEXT.md locked decision: rewrite question framing as YAML repair. Title becomes "Repair the static-pod manifest". Lead paragraph reframes the task to match what is actually graded — repair the file in place so it is valid YAML, defines a single Pod, and passes client dry-run. Tasks list, Constraints, and Verify-yourself are already YAML-repair focused and stay verbatim. metadata.yaml is brought into agreement if any stale-framing field is present. grade.sh, setup.sh, ref-solution.sh, and reset.sh are NOT touched — they are already correct.

Output: A self-consistent triangle where the question (YAML repair), grader (YAML parseability + kind=Pod + dry-run), and ref-solution (overwrites the broken manifest with a valid one) agree. The "static-pod" pedagogy is preserved by the manifest's `metadata.namespace: kube-system` and the constraint hint about /etc/kubernetes/manifests/ — but the test surface matches what is graded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-high-grader-question-rework/11-CONTEXT.md
@.planning/forensics/report-20260517-091657-full-audit.md
@.planning/REQUIREMENTS.md
@cka-sim/packs/troubleshooting/05-static-pod-manifest/question.md
@cka-sim/packs/troubleshooting/05-static-pod-manifest/setup.sh
@cka-sim/packs/troubleshooting/05-static-pod-manifest/grade.sh
@cka-sim/packs/troubleshooting/05-static-pod-manifest/ref-solution.sh
@cka-sim/packs/troubleshooting/05-static-pod-manifest/reset.sh
@cka-sim/packs/troubleshooting/05-static-pod-manifest/metadata.yaml

<interfaces>
Current grade.sh contract (verbatim — DO NOT change):
- Line 23: `if [[ -s "$manifest" ]]; then` — manifest existence (weight=0 informational, demoted in Phase 07.1 AUDIT-01).
- Lines 31-44: weight=1 YAML-parseability assertion (python3 yaml.safe_load); records `static-pod-manifest-bad-yaml` trap on fail.
- Lines 46-59: weight=1 kind==Pod assertion (python3 yaml.safe_load + assert kind == "Pod").
- Lines 61-69: weight=1 `kubectl apply --dry-run=client` assertion.
- Lines 71-84: image-tag-typo trap detector (advisory; reads spec.containers[0].image and records `static-pod-image-tag-typo` if it contains "doesnotexistXYZ").
- Line 86: emit_result.

Total scoring: 3 points (parseable + kind=Pod + dry-run). Setup state weight=0.

Current setup.sh seed sequence:
- Lines 19-38: `manifest-broken.yaml` — tab-indent typo at line 35 (`\tlimits:`).
- Lines 42-61: `manifest-tagtypo.yaml` — image: nginx:1.27-alpine-doesnotexistXYZ.
- Line 63: `cp manifest-broken.yaml manifest.yaml` — the live candidate target is the tab-indent variant.

ref-solution.sh (NOT modified) overwrites `/tmp/q05-staticpod/manifest.yaml` with a valid Pod manifest (sets nginx:1.27-alpine + correct indent), then exits.

reset.sh: namespace delete + tmp cleanup; no manifest-specific logic.

metadata.yaml current shape (verbatim):
- id, domain, estimatedMinutes, verified_against
- traps: static-pod-manifest-bad-yaml, static-pod-image-tag-typo, default-sa-used
- references: prior-art-exercise (workloads-scheduling/06-static-pod) + k8s-doc (static-pod concepts)

Note: there is NO `description` or `summary` field in the current metadata.yaml. CONTEXT.md says "adjust description/summary field if it mirrors stale framing" — Task 2 below is therefore a verification-only task that confirms no such field exists; if a future revision of the file ever adds one, the verification will fail and surface that.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite question.md title + lead paragraph as YAML repair</name>
  <read_first>
    - cka-sim/packs/troubleshooting/05-static-pod-manifest/question.md (current 36-line file — title and 2 lead paragraphs are stale; Tasks/Constraints/Verify are correct)
    - cka-sim/packs/troubleshooting/05-static-pod-manifest/grade.sh (lines 31-69 — what is actually scored)
    - cka-sim/packs/troubleshooting/05-static-pod-manifest/setup.sh (lines 19-63 — what is seeded)
    - .planning/phases/11-high-grader-question-rework/11-CONTEXT.md (BUG-H06 fix path step 1)
  </read_first>
  <files>cka-sim/packs/troubleshooting/05-static-pod-manifest/question.md</files>
  <action>
    Rewrite cka-sim/packs/troubleshooting/05-static-pod-manifest/question.md. Two areas change — title and the two lead paragraphs (current lines 1, 5, 7). Everything else stays verbatim.

    1. **Title (line 1):**

       BEFORE:
       ```markdown
       # Troubleshooting: Static pod never becomes Running
       ```

       AFTER:
       ```markdown
       # Troubleshooting: Repair the static-pod manifest
       ```

    2. **Domain/time line (line 3) — keep verbatim:** `**Domain:** Troubleshooting  |  **Estimated time:** 10 minutes`

    3. **Lead paragraphs (current lines 5 + 7):**

       BEFORE:
       ```markdown
       Candidate static-pod manifest lives in sandbox. When manifest is placed in node-agent static workload directory, intended Pod `q05-cache` never appears in `kubectl get pods -A` or never reaches `Running`.

       Repair sandbox manifest so that when the node agent picks it up, Pod reaches `Running`.
       ```

       AFTER:
       ```markdown
       A candidate static-pod manifest lives in the sandbox at `/tmp/q05-staticpod/manifest.yaml`. The file has intentional defects (tab-indented YAML and an image-tag typo) so it cannot be parsed as YAML and would never produce a valid Pod if the kubelet were to pick it up.

       Repair the manifest in place so it is (a) valid YAML, (b) defines a single `Pod` named `q05-cache` in the `kube-system` namespace, and (c) passes `kubectl apply --dry-run=client`. The grader scores the file directly — it does NOT install the manifest into `/etc/kubernetes/manifests/` or wait for a Running Pod.
       ```

       Two paragraphs, separated by exactly one blank line. Single backticks around resource names, paths, and shell commands — match existing markdown style.

    4. **Sandbox section (current lines 9-13)** — keep verbatim. Includes:
       - `## Sandbox`
       - `- Working directory: '/tmp/q05-staticpod/'`
       - `- Candidate manifest: '/tmp/q05-staticpod/manifest.yaml'`
       - `- Reference variants, read-only: '/tmp/q05-staticpod/manifest-broken.yaml' and '/tmp/q05-staticpod/manifest-tagtypo.yaml'`

    5. **Tasks section (current lines 15-19)** — keep verbatim. The three bullets are already YAML-repair focused:
       - `- Inspect '/tmp/q05-staticpod/manifest.yaml' and identify why it cannot produce a Running Pod.`
       - `- Edit '/tmp/q05-staticpod/manifest.yaml' in place so it is valid YAML, defines a single 'Pod', and references an existing image.`
       - `- Confirm acceptability via client-side dry-run.`

       Note: bullet 1 says "cannot produce a Running Pod" — this is acceptable as a hint about WHY the file is broken (the kubelet wouldn't accept it) without claiming the grader checks Running. Keep verbatim.

    6. **Constraints section (current lines 21-26)** — keep verbatim. Four bullets:
       - `- Do NOT place manifest into '/etc/kubernetes/manifests/'.`
       - `- Do NOT restart node services.`
       - `- Do NOT run live-service restart commands.`
       - `'metadata.name' must remain 'q05-cache'.`

    7. **Verify yourself section (current lines 28-35)** — keep verbatim:
       ```markdown
       ```bash
       python3 -c 'import yaml; yaml.safe_load(open("/tmp/q05-staticpod/manifest.yaml"))'
       kubectl apply --dry-run=client -f /tmp/q05-staticpod/manifest.yaml
       ```

       Both commands must exit 0.
       ```

    LF line endings. No CRLF.
  </action>
  <verify>
    <automated>grep -c '^# Troubleshooting: Repair the static-pod manifest$' cka-sim/packs/troubleshooting/05-static-pod-manifest/question.md</automated>
  </verify>
  <acceptance_criteria>
    - The new title is present exactly once: `grep -c '^# Troubleshooting: Repair the static-pod manifest$' cka-sim/packs/troubleshooting/05-static-pod-manifest/question.md` returns 1.
    - The old title is gone: `grep -c 'Static pod never becomes Running' cka-sim/packs/troubleshooting/05-static-pod-manifest/question.md` returns 0.
    - The new lead paragraph mentions intentional defects and the two specific defects: `grep -c 'intentional defects' cka-sim/packs/troubleshooting/05-static-pod-manifest/question.md` returns 1; `grep -c 'tab-indented YAML' cka-sim/packs/troubleshooting/05-static-pod-manifest/question.md` returns 1; `grep -c 'image-tag typo' cka-sim/packs/troubleshooting/05-static-pod-manifest/question.md` returns 1.
    - The honest "grader scores the file directly" disclosure is present: `grep -c 'grader scores the file directly' cka-sim/packs/troubleshooting/05-static-pod-manifest/question.md` returns 1.
    - The "no /etc/kubernetes/manifests installation, no Running wait" disclosure is present: `grep -c 'does NOT install the manifest into' cka-sim/packs/troubleshooting/05-static-pod-manifest/question.md` returns 1.
    - The stale "node agent picks it up" framing is gone: `grep -c 'node agent picks it up' cka-sim/packs/troubleshooting/05-static-pod-manifest/question.md` returns 0; `grep -c 'kubectl get pods -A' cka-sim/packs/troubleshooting/05-static-pod-manifest/question.md` returns 0.
    - Sandbox, Tasks, Constraints, Verify-yourself sections preserved: `grep -c '^## Sandbox$' cka-sim/packs/troubleshooting/05-static-pod-manifest/question.md` returns 1; `grep -c '^## Tasks$' cka-sim/packs/troubleshooting/05-static-pod-manifest/question.md` returns 1; `grep -c '^## Constraints$' cka-sim/packs/troubleshooting/05-static-pod-manifest/question.md` returns 1; `grep -c '^## Verify yourself$' cka-sim/packs/troubleshooting/05-static-pod-manifest/question.md` returns 1.
    - The four constraints preserved verbatim: `grep -c "^- Do NOT place manifest into '/etc/kubernetes/manifests/'\.$" cka-sim/packs/troubleshooting/05-static-pod-manifest/question.md` returns 1; `grep -c '^- Do NOT restart node services\.$' cka-sim/packs/troubleshooting/05-static-pod-manifest/question.md` returns 1; `grep -c '^- Do NOT run live-service restart commands\.$' cka-sim/packs/troubleshooting/05-static-pod-manifest/question.md` returns 1; `grep -c "'metadata.name' must remain 'q05-cache'\." cka-sim/packs/troubleshooting/05-static-pod-manifest/question.md` returns 1.
    - Verify-yourself commands preserved: `grep -c "python3 -c 'import yaml; yaml.safe_load(open(\"/tmp/q05-staticpod/manifest.yaml\"))'" cka-sim/packs/troubleshooting/05-static-pod-manifest/question.md` returns 1; `grep -c 'kubectl apply --dry-run=client -f /tmp/q05-staticpod/manifest.yaml' cka-sim/packs/troubleshooting/05-static-pod-manifest/question.md` returns 1.
    - Domain/time line unchanged: `grep -c '^\*\*Domain:\*\* Troubleshooting  |  \*\*Estimated time:\*\* 10 minutes$' cka-sim/packs/troubleshooting/05-static-pod-manifest/question.md` returns 1.
  </acceptance_criteria>
  <done>question.md title and lead paragraphs match what the grader actually tests (YAML repair + kind=Pod + dry-run); Tasks/Constraints/Verify preserved; static-pod pedagogy preserved via the constraint hint and the manifest's kube-system namespace.</done>
</task>

<task type="auto">
  <name>Task 2: Bring metadata.yaml into agreement (verification-first)</name>
  <read_first>
    - cka-sim/packs/troubleshooting/05-static-pod-manifest/metadata.yaml (current 16-line file — id, domain, estimatedMinutes, verified_against, 3 traps, 2 references; NO description/summary field today)
    - .planning/phases/11-high-grader-question-rework/11-CONTEXT.md (BUG-H06 fix path step 2)
  </read_first>
  <files>cka-sim/packs/troubleshooting/05-static-pod-manifest/metadata.yaml</files>
  <action>
    Verification-first: read the current metadata.yaml. If it does NOT contain a `description:` or `summary:` field that mirrors the stale "static pod never becomes Running" framing (which is the case at HEAD), no edit is needed and this task is a no-op confirmation.

    If a `description:` or `summary:` field IS present and contains stale framing (e.g. references "never becomes Running", "kubelet pickup", "/etc/kubernetes/manifests/" as the test surface), rewrite that field's value to: `Repair a broken static-pod manifest (tab-indented YAML + image-tag typo) so it is valid YAML, defines a single Pod, and passes client dry-run.`

    DO NOT add a new description/summary field if none exists today — the current file is fine without one. The CONTEXT directive says "adjust IF it mirrors stale framing" — adjustment is conditional on presence.

    DO NOT change:
    - `id: troubleshooting-static-pod-manifest`
    - `domain: troubleshooting`
    - `estimatedMinutes: 10`
    - `verified_against: "1.35"`
    - The `traps:` list — all three ids stay (`static-pod-manifest-bad-yaml`, `static-pod-image-tag-typo`, `default-sa-used`).
    - The `references:` list — both entries (prior-art-exercise + k8s-doc) stay verbatim. The k8s-doc note "Static pod concepts and the /etc/kubernetes/manifests/ directory" remains valid background reading even though the question's test surface is just YAML repair.

    LF line endings. No CRLF.
  </action>
  <verify>
    <automated>grep -cE '^(description|summary):' cka-sim/packs/troubleshooting/05-static-pod-manifest/metadata.yaml</automated>
  </verify>
  <acceptance_criteria>
    - At HEAD the file has NO description/summary field; if a future revision adds one, this verification fails and the executor must align it: `grep -cE '^(description|summary):' cka-sim/packs/troubleshooting/05-static-pod-manifest/metadata.yaml` returns 0 OR (if present) the field's value contains "Repair" and "valid YAML" but does NOT contain "never becomes Running".
    - Identity fields preserved: `grep -c '^id: troubleshooting-static-pod-manifest$' cka-sim/packs/troubleshooting/05-static-pod-manifest/metadata.yaml` returns 1; `grep -c '^domain: troubleshooting$' cka-sim/packs/troubleshooting/05-static-pod-manifest/metadata.yaml` returns 1; `grep -c '^estimatedMinutes: 10$' cka-sim/packs/troubleshooting/05-static-pod-manifest/metadata.yaml` returns 1; `grep -c '^verified_against: "1.35"$' cka-sim/packs/troubleshooting/05-static-pod-manifest/metadata.yaml` returns 1.
    - Trap list preserved (all three): `grep -c '^  - static-pod-manifest-bad-yaml$' cka-sim/packs/troubleshooting/05-static-pod-manifest/metadata.yaml` returns 1; `grep -c '^  - static-pod-image-tag-typo$' cka-sim/packs/troubleshooting/05-static-pod-manifest/metadata.yaml` returns 1; `grep -c '^  - default-sa-used$' cka-sim/packs/troubleshooting/05-static-pod-manifest/metadata.yaml` returns 1.
    - Reference targets preserved: `grep -c 'cka-sim/packs/workloads-scheduling/06-static-pod/' cka-sim/packs/troubleshooting/05-static-pod-manifest/metadata.yaml` returns 1; `grep -c 'https://kubernetes.io/docs/tasks/configure-pod-container/static-pod/' cka-sim/packs/troubleshooting/05-static-pod-manifest/metadata.yaml` returns 1.
    - File line count is unchanged from HEAD if no description/summary field was rewritten (no spurious newlines added): the file remains parseable as YAML by `python3 -c 'import yaml; yaml.safe_load(open("cka-sim/packs/troubleshooting/05-static-pod-manifest/metadata.yaml"))'`.
  </acceptance_criteria>
  <done>metadata.yaml identity/trap/reference fields preserved; no stale-framing description or summary present (or, if present, brought into agreement with the new question framing).</done>
</task>

<task type="auto">
  <name>Task 3: Quick scan — confirm grade.sh, setup.sh, ref-solution.sh, reset.sh need no changes</name>
  <read_first>
    - cka-sim/packs/troubleshooting/05-static-pod-manifest/grade.sh (already correctly scores YAML repair: parseable + kind=Pod + dry-run)
    - cka-sim/packs/troubleshooting/05-static-pod-manifest/setup.sh (correctly seeds tab-indent + image-tag-typo variants and cps broken to manifest.yaml)
    - cka-sim/packs/troubleshooting/05-static-pod-manifest/ref-solution.sh (overwrites manifest.yaml with a valid Pod)
    - cka-sim/packs/troubleshooting/05-static-pod-manifest/reset.sh (namespace + tmp cleanup; no manifest-specific logic)
    - .planning/phases/11-high-grader-question-rework/11-CONTEXT.md (BUG-H06 fix path steps 3-5: "grade.sh: NO change", "setup.sh: NO change", "ref-solution.sh: NO change")
  </read_first>
  <files>(read-only verification — produces no edits unless a hidden coupling is found)</files>
  <action>
    Run a series of greps to confirm grade.sh, setup.sh, ref-solution.sh, and reset.sh have no stale "Running" / "kubelet pickup" / "/etc/kubernetes/manifests/" assertion that contradicts the new question framing. Expected results:

    1. **grade.sh** — Three weight=1 assertions: YAML parse, kind=Pod, client dry-run. NO mirror-pod check, NO `kubectl wait --for=condition=Ready`, NO `/etc/kubernetes/manifests/` reference. Confirm:
       - `grep -c 'kubectl wait' cka-sim/packs/troubleshooting/05-static-pod-manifest/grade.sh` returns 0.
       - `grep -c '/etc/kubernetes/manifests/' cka-sim/packs/troubleshooting/05-static-pod-manifest/grade.sh` returns 0.
       - `grep -c 'condition=Ready' cka-sim/packs/troubleshooting/05-static-pod-manifest/grade.sh` returns 0.
       - The three scoring CKA_SIM_GRADE_TOTAL increments are all present: `grep -c 'CKA_SIM_GRADE_TOTAL=$(( CKA_SIM_GRADE_TOTAL + 1 ))' cka-sim/packs/troubleshooting/05-static-pod-manifest/grade.sh` returns 3.
       - The dry-run assertion is present: `grep -c 'kubectl apply --dry-run=client -f "\$manifest"' cka-sim/packs/troubleshooting/05-static-pod-manifest/grade.sh` returns 1.

    2. **setup.sh** — Seeds the tab-indent + image-tag-typo variants, cps broken to live manifest.yaml. NO changes needed because the question now explicitly names both defect types ("tab-indented YAML and an image-tag typo"). Confirm:
       - `grep -c "grep -P '\\\\t' \"\$sandbox/manifest-broken.yaml\"" cka-sim/packs/troubleshooting/05-static-pod-manifest/setup.sh` returns 1 (the post-write tab assertion is preserved — sentinel that the heredoc didn't strip the tab).
       - `grep -c 'doesnotexistXYZ' cka-sim/packs/troubleshooting/05-static-pod-manifest/setup.sh` returns 1 (image-tag typo preserved).
       - `grep -c "cp \"\$sandbox/manifest-broken.yaml\" \"\$sandbox/manifest.yaml\"" cka-sim/packs/troubleshooting/05-static-pod-manifest/setup.sh` returns 1.

    3. **ref-solution.sh** — Overwrites manifest.yaml with a valid Pod (untyped tab + corrected image). NO `kubectl apply` to /etc/kubernetes/manifests/, NO mirror-pod wait. Confirm:
       - `grep -c '/etc/kubernetes/manifests/' cka-sim/packs/troubleshooting/05-static-pod-manifest/ref-solution.sh` returns 0.
       - `grep -c 'kubectl wait' cka-sim/packs/troubleshooting/05-static-pod-manifest/ref-solution.sh` returns 0.

    4. **reset.sh** — Namespace + tmp cleanup only. No manifest-specific logic. Confirm:
       - `grep -c 'kubectl delete namespace "\$CKA_SIM_LAB_NS"' cka-sim/packs/troubleshooting/05-static-pod-manifest/reset.sh` returns 1.
       - `grep -c 'rm -rf /tmp/cka-sim/05-static-pod-manifest/' cka-sim/packs/troubleshooting/05-static-pod-manifest/reset.sh` returns 1.

    If any unexpected grep result is found (e.g. grade.sh DOES contain a kubelet/wait/Ready assertion), STOP and surface to the user — it would mean the BUG-H06 scope estimate in CONTEXT.md is wrong and the fix needs to extend.

    Report grep output in the SUMMARY.
  </action>
  <verify>
    <automated>grep -c 'kubectl wait\|/etc/kubernetes/manifests/' cka-sim/packs/troubleshooting/05-static-pod-manifest/grade.sh</automated>
  </verify>
  <acceptance_criteria>
    - grade.sh has no Running/kubelet-pickup contradictions: `grep -c 'kubectl wait' cka-sim/packs/troubleshooting/05-static-pod-manifest/grade.sh` returns 0; `grep -c '/etc/kubernetes/manifests/' cka-sim/packs/troubleshooting/05-static-pod-manifest/grade.sh` returns 0; `grep -c 'condition=Ready' cka-sim/packs/troubleshooting/05-static-pod-manifest/grade.sh` returns 0.
    - grade.sh's three scoring TOTAL increments are intact: `grep -c 'CKA_SIM_GRADE_TOTAL=$(( CKA_SIM_GRADE_TOTAL + 1 ))' cka-sim/packs/troubleshooting/05-static-pod-manifest/grade.sh` returns 3.
    - grade.sh dry-run assertion intact: `grep -c 'kubectl apply --dry-run=client -f "\$manifest"' cka-sim/packs/troubleshooting/05-static-pod-manifest/grade.sh` returns 1.
    - setup.sh seeds both defect variants AND cps broken to live: `grep -c 'doesnotexistXYZ' cka-sim/packs/troubleshooting/05-static-pod-manifest/setup.sh` returns 1; `grep -c "cp \"\$sandbox/manifest-broken.yaml\" \"\$sandbox/manifest.yaml\"" cka-sim/packs/troubleshooting/05-static-pod-manifest/setup.sh` returns 1.
    - ref-solution.sh has no Running-wait or /etc/kubernetes/manifests/ side effect: `grep -c '/etc/kubernetes/manifests/' cka-sim/packs/troubleshooting/05-static-pod-manifest/ref-solution.sh` returns 0; `grep -c 'kubectl wait' cka-sim/packs/troubleshooting/05-static-pod-manifest/ref-solution.sh` returns 0.
    - reset.sh tmp + ns cleanup intact: `grep -c 'kubectl delete namespace "\$CKA_SIM_LAB_NS"' cka-sim/packs/troubleshooting/05-static-pod-manifest/reset.sh` returns 1; `grep -c 'rm -rf /tmp/cka-sim/05-static-pod-manifest/' cka-sim/packs/troubleshooting/05-static-pod-manifest/reset.sh` returns 1.
    - No file outside the two touched in this plan was edited: `git diff --name-only -- cka-sim/packs/troubleshooting/05-static-pod-manifest/ | sort` should list ONLY question.md (and metadata.yaml ONLY if Task 2 found and rewrote a stale-framing field; at HEAD there is none, so the expected diff is question.md only).
  </acceptance_criteria>
  <done>Verified that BUG-H06 is genuinely a question-framing-only fix; grade.sh, setup.sh, ref-solution.sh, reset.sh need no modification; trap pedagogy preserved.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:

1. `bash -n` — no scripts modified; this gate is informational only. The two files touched are question.md and (conditionally) metadata.yaml; both are non-executable.
2. `python3 -c 'import yaml; yaml.safe_load(open("cka-sim/packs/troubleshooting/05-static-pod-manifest/metadata.yaml"))'` exits 0 — metadata.yaml is parseable.
3. Live-cluster GRADE round-trip will be performed in execute-phase verification (out of scope here):
   - `cka-sim drill troubleshooting 05-static-pod-manifest` then run ref-solution.sh → expect SCORE 3/3, 0 traps recorded.
   - Empty submission (candidate touches nothing — manifest.yaml is the broken tab-indent variant) → fails YAML parse → `static-pod-manifest-bad-yaml` trap fires; kind=Pod parses (tab is the only defect at YAML-load level) — wait, recheck: tab indentation under `requests:` (line 35: `\tlimits:`) makes YAML parse FAIL because Python's safe_load rejects mixed tab/space indentation in mapping keys. So both YAML parse AND kind=Pod assertions fail (kind=Pod cannot be evaluated when parse fails — the python helper exits non-zero, scoring 0 for that assertion too); dry-run also fails (kubectl rejects unparseable YAML). → SCORE 0/3, 1 trap (`static-pod-manifest-bad-yaml`). Correct.

   Actually re-checking the image-tag-typo path: the live manifest.yaml is the tab-indent variant, NOT the tag-typo variant. The image-tag-typo trap fires only if the candidate happened to swap to manifest-tagtypo.yaml AND fix the indent. Empty submission stays on the tab variant, so only the bad-yaml trap fires.
4. Trap-coverage lint (Phase 12) will continue to find all three trap ids declared in metadata.yaml; grade.sh records two of them (`static-pod-manifest-bad-yaml` at lines 43+83, `static-pod-image-tag-typo` at line 79). The third trap (`default-sa-used`) is currently an orphan — that is BUG-M03's territory, NOT this phase's concern. Phase 12 LINT-01 will flag it later.
</verification>

<success_criteria>
- Question title and lead paragraph match what the grader actually scores (YAML parse + kind=Pod + dry-run). ROADMAP Phase 11 success criterion #2, first branch ("question rewrites as 'YAML repair + dry-run' exercise matching the existing kubectl apply --dry-run=client grader") — that is the branch CONTEXT.md selected.
- ref-solution still scores 3/3; empty submission still scores 0/3.
- No grade.sh, setup.sh, ref-solution.sh, or reset.sh change.
- Trap entries (static-pod-manifest-bad-yaml, static-pod-image-tag-typo, default-sa-used) preserved in metadata.yaml.
</success_criteria>

<output>
After completion, create `.planning/phases/11-high-grader-question-rework/11-02-SUMMARY.md` documenting:
- Files modified: question.md (and metadata.yaml ONLY if a stale description/summary was found and rewritten — at HEAD, none was found, so this is question.md only)
- Reframing: title "Static pod never becomes Running" → "Repair the static-pod manifest"; lead paragraph rewritten to match the actual graded surface (YAML repair + kind=Pod + dry-run); Tasks/Constraints/Verify preserved verbatim
- Verification: ref-solution 3/3, empty submission 0/3 + 1 trap (static-pod-manifest-bad-yaml) (to be confirmed by execute-phase live drill)
- Files surveyed and confirmed unchanged: grade.sh, setup.sh, ref-solution.sh, reset.sh (Task 3 grep results)
- Pedagogy preservation: static-pod context retained via the manifest's `metadata.namespace: kube-system` and the constraint hint about `/etc/kubernetes/manifests/`
</output>
