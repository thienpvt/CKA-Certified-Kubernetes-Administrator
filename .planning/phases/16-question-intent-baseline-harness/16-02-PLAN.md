---
phase: 16-question-intent-baseline-harness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/AUTHORING.md
  - cka-sim/README.md
  - cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md
  - .planning/PROJECT.md
autonomous: true
requirements: [BASELINE-04, DOC-01]
must_haves:
  truths:
    - "docs/AUTHORING.md ships at the repo root under a new docs/ directory and walks a question author through the test-artifact triplet (expected-symptom.yaml / lib/baseline.sh / grade.sh) plus the cka-sim audit invocation reference. A new author can read the doc and ship a passing expected-symptom.yaml without further guidance."
    - "AUTHORING.md sections are: (1) Why three test artifacts; (2) Triplet at a glance — table mapping artifact → role → who reads it; (3) Authoring expected-symptom.yaml from question.md prose — worked example using storage/01-pvc-binding showing prose lines side-by-side with YAML keys; (4) Setup-state baseline (lib/baseline.sh) — what it does, why it's runner-managed, link to GRADING-HONESTY.md; (5) Grade.sh authoring — link out, no duplication; (6) cka-sim audit invocation reference — three scopes, --report flag, exit codes 0/1/2; (7) Cross-links."
    - "AUTHORING.md cross-links cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md (schema reference; not duplicated) and cka-sim/lib/GRADING-HONESTY.md (candidate-state baseline). Schema doc gets a reciprocal link back to docs/AUTHORING.md so readers can navigate either direction."
    - "cka-sim/README.md gets a Documentation section (or a new line in an existing one) that surfaces docs/AUTHORING.md alongside the existing schema doc reference. The link is relative (../docs/AUTHORING.md from cka-sim/README.md)."
    - ".planning/PROJECT.md question count is corrected from 38 to 34 (D-09): the 'What This Is' paragraph and the Domain Packs table both reflect 6+8+6+8+6=34. The README.md's pack table already lists 34 — no edit needed there. The fix is bundled here per CONTEXT.md D-09."
  artifacts:
    - path: docs/AUTHORING.md
      provides: "Workflow guide for question authors. ~150-300 lines covering the test-artifact triplet, prose-to-YAML derivation worked example, audit invocation reference, and cross-links to schema + grading-honesty docs."
      contains: "# Authoring CKA Exam Simulator Questions"
    - path: cka-sim/README.md
      provides: "Adds a docs link surfacing AUTHORING.md from the existing Architecture or Development section."
      contains: "../docs/AUTHORING.md"
    - path: cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md
      provides: "Adds a reciprocal link to docs/AUTHORING.md at the top (after the title) so navigation is bidirectional."
      contains: "docs/AUTHORING.md"
    - path: .planning/PROJECT.md
      provides: "Question-count corrections per CONTEXT.md D-09: 38 → 34 in the 'What This Is' paragraph (line ~5) and in the v1.0 Validated requirement bullet (line ~31). Domain Packs table values already correct."
      contains: "34 questions"
  key_links:
    - from: docs/AUTHORING.md
      to: cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md
      via: "Schema reference cross-link in section 3 (expected-symptom.yaml authoring) — points readers at the canonical schema instead of duplicating the resource-kind allow-list etc."
      pattern: "../cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md"
    - from: docs/AUTHORING.md
      to: cka-sim/lib/GRADING-HONESTY.md
      via: "Candidate-state baseline cross-link in section 4."
      pattern: "../cka-sim/lib/GRADING-HONESTY.md"
    - from: cka-sim/README.md
      to: docs/AUTHORING.md
      via: "Documentation discovery — README is the entry point; AUTHORING.md is the next-step doc for authors."
      pattern: "../docs/AUTHORING.md"
    - from: cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md
      to: docs/AUTHORING.md
      via: "Reciprocal navigation link near the top so a reader of the schema doc can find the workflow guide."
      pattern: "docs/AUTHORING.md"
---

<objective>
Ship `docs/AUTHORING.md` (BASELINE-04 / DOC-01 — DOC-01 is folded into BASELINE-04 per REQUIREMENTS.md), surface it from `cka-sim/README.md`, add a reciprocal link from `cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md`, and bundle the PROJECT.md 38→34 question-count correction (CONTEXT.md D-09) as a side-effect.

After this plan lands:
- A new question author can clone the repo, read `docs/AUTHORING.md`, and ship a passing `expected-symptom.yaml` for a hypothetical new question without reading any planning artifact.
- `docs/AUTHORING.md` is discoverable from `cka-sim/README.md` AND from `cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md`.
- `.planning/PROJECT.md` no longer claims 38 domain questions (the actual count is 34).
- This plan does NOT modify the audit tool itself (Plan 16-01 owns that) and does NOT regenerate any expected-symptom.yaml (out of scope per CONTEXT.md domain section).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-question-intent-baseline-harness/16-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@cka-sim/README.md
@cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md
@cka-sim/lib/GRADING-HONESTY.md
@cka-sim/packs/storage/01-pvc-binding/question.md
@cka-sim/packs/storage/01-pvc-binding/expected-symptom.yaml

<interfaces>
- The `docs/` directory does not yet exist at the repo root. This plan creates it (`docs/AUTHORING.md` is the first file in it).
- `cka-sim/README.md` already has a Quickstart, Architecture, Domain Packs, Mock Exams, Development, and Disclaimer sections. Insert a Documentation section (or extend Development) — pick the spot that least disrupts the existing structure.
- `cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md` already exists from Phase 15 and is the canonical schema reference. AUTHORING.md cross-links to it; SCHEMA.md gets a reciprocal link.
- `cka-sim/lib/GRADING-HONESTY.md` is the Phase 07.1 candidate-state baseline doc; AUTHORING.md cross-links to it as the third leg of the triplet.
- `.planning/PROJECT.md` line 5 ("What This Is" paragraph) and line ~31 ("Five domain packs ... 38 questions total") both reference 38 questions. The Domain Packs table inside cka-sim/README.md already reads 6+8+6+8+6=34 (no edit needed there).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/AUTHORING.md</name>
  <read_first>
    - .planning/phases/16-question-intent-baseline-harness/16-CONTEXT.md (D-10 locks the section list)
    - cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md (full — used as schema reference cross-link, NOT duplicated)
    - cka-sim/lib/GRADING-HONESTY.md (candidate-state baseline reference)
    - cka-sim/packs/storage/01-pvc-binding/question.md (worked example source — prose lines 1-28)
    - cka-sim/packs/storage/01-pvc-binding/expected-symptom.yaml (worked example target — YAML keys aligned to prose lines)
  </read_first>
  <files>docs/AUTHORING.md</files>
  <action>
    Create `docs/AUTHORING.md` (LF line endings, mode 0644). Aim for ~200-350 lines.

    **Section 1 — Title + intro (1 paragraph)**:
    ```
    # Authoring CKA Exam Simulator Questions

    This guide walks you through authoring a new question end-to-end. The
    simulator's runtime ships three test artifacts per question and one
    forensic tool. Once you understand the triplet and the audit workflow,
    you can author a new question that grades honestly, fails for the right
    reasons, and survives the question-vs-setup drift checks.
    ```

    **Section 2 — The test-artifact triplet at a glance**:

    Render this table:

    | Artifact | Role | Read by | Authored by you |
    |---|---|---|---|
    | `expected-symptom.yaml` | Encodes what `question.md` prose claims about the post-`setup.sh` cluster. Audit-mode and lint-mode diff actual cluster state against this. | `cka-sim audit`, `cka-sim/scripts/lint-question-symptom.sh` | Yes — derive from `question.md` prose |
    | `lib/baseline.sh` (snapshot, runner-managed) | Captures the cluster state immediately after `setup.sh` and before the candidate types `done`. The grader uses this to distinguish setup-seeded resources from candidate-authored work. | `cka-sim drill` runner | No — runner manages it |
    | `grade.sh` | Per-question pass/fail logic. Calls `cka-sim/lib/grade.sh` helpers and emits `Trap N:` diagnostics on failure. | `cka-sim drill`/`exam` | Yes |

    Add a one-paragraph note: "The audit-mode tool (`cka-sim audit`) is forensic — it runs `setup.sh` and diffs actual cluster state against `expected-symptom.yaml`. It is invoked one-shot during forensic phases, not on every commit. Schema for `expected-symptom.yaml` lives at [`../cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md`](../cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md)."

    **Section 3 — Authoring expected-symptom.yaml from question.md prose (the heart of the doc)**:

    Open with: "Derive `expected-symptom.yaml` from your `question.md` prose, NOT from running `setup.sh` and recording what you see. The point of the audit is to catch question-vs-setup drift; if you encode setup output, you encode the drift instead of catching it."

    Then a worked example. Show two side-by-side blocks:

    Prose block (excerpt from `cka-sim/packs/storage/01-pvc-binding/question.md`, lines 5-7):
    ```
    A `PersistentVolumeClaim` named `app-data` exists in your lab namespace
    and references a `PersistentVolume` named `q01-app-pv`. A consumer Pod
    named `q01-app-consumer` has been deployed in your lab namespace to mount
    that PVC, but the Pod fails to schedule onto a worker node.
    ```

    Walk the reader through the derivation:
    - "PVC named `app-data` exists" → resource entry, kind: pvc, name: app-data, namespace: ${CKA_SIM_LAB_NS}.
    - "Pod fails to schedule" → the candidate's claim is that the Pod is not Running. We do not need to enumerate this in `expect:` because the open-world semantic means we only check fields we list. Instead, the PVC is the upstream symptom — claim PVC `status.phase: Pending`.
    - "PV `q01-app-pv` exists" → resource entry, kind: pv, name: q01-app-pv (cluster-scoped, no namespace).
    - "Consumer Pod cannot schedule because the PV does not advertise nodeAffinity for any worker" → the trap the candidate must fix is the missing `spec.nodeAffinity`. We do NOT enumerate `spec.nodeAffinity` under `expect:` — open-world won't fail on a missing field, and `null` is unsupported. The right move is to NOT enumerate it.

    Show the resulting YAML (as a code block):
    ```yaml
    question: storage-pvc-binding
    namespace: ${CKA_SIM_LAB_NS}
    resources:
      - kind: pvc
        name: app-data
        namespace: ${CKA_SIM_LAB_NS}
        expect:
          status.phase: Pending
          spec.storageClassName: manual
      - kind: pv
        name: q01-app-pv
        expect:
          status.phase: Available
          spec.storageClassName: manual
          spec.persistentVolumeReclaimPolicy: Retain
    ```

    Conclude this section with a 1-paragraph "Common pitfalls" callout listing:
    - Don't list every field. Only encode what the prose claims.
    - Don't claim `null` for missing fields. Open-world handles them silently.
    - Use `${CKA_SIM_LAB_NS}` for namespaced resources; cluster-scoped resources (pv, namespace, storageclass, ...) omit `namespace:`.
    - Schema reference: see [`../cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md`](../cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md) for the resource-kind allow-list and jsonpath translator details.

    **Section 4 — Setup-state baseline (`lib/baseline.sh`)**:

    Short — explain that the runner snapshots the cluster after `setup.sh` runs and before the candidate types `done`. The grader uses this snapshot to distinguish setup-seeded resources from candidate-authored ones (Phase 07.1's grading-honesty contract). Question authors do NOT touch `lib/baseline.sh` — it is runner-managed. For details on the snapshot format and how graders consume it, see [`../cka-sim/lib/GRADING-HONESTY.md`](../cka-sim/lib/GRADING-HONESTY.md).

    **Section 5 — Grade.sh authoring**:

    Two-paragraph stub: link out to `cka-sim/lib/grade.sh` for assertion helpers (`assert_resource_exists`, `assert_field_equals`, `assert_changed_since_setup`, `record_trap`). Note that `grade.sh` should fail-soft (accumulate failed assertions, not `die`), each assertion is 1 point, and trap diagnostics use `record_trap "<id>"` — the catalog at `cka-sim/traps/catalog.yaml` is the canonical id list. Don't duplicate that doc here.

    **Section 6 — `cka-sim audit` invocation reference**:

    Render this:
    ```
    Three scopes:

      cka-sim audit                       — all expected-symptom.yaml under packs/
      cka-sim audit <pack>                — one pack
      cka-sim audit <pack>/<question>     — single question

    Optional flag:

      --report path/to.md                 — persist same content as a markdown report

    Exit codes:

      0  all PASS
      1  at least one FAIL
      2  preflight error (no live cluster, missing jq/python3/yaml)
    ```

    Add a usage note: "Audit requires a live kind+Calico (or kubeadm) cluster reachable via kubectl. It is intentionally NOT wired to GHA `validate.yml` — the lint variant (`cka-sim/scripts/lint-question-symptom.sh`) is the CI gate; audit is the forensic tool."

    **Section 7 — Cross-links**:
    - Schema reference — [`../cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md`](../cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md)
    - Candidate-state baseline doc — [`../cka-sim/lib/GRADING-HONESTY.md`](../cka-sim/lib/GRADING-HONESTY.md)
    - cka-sim runtime overview — [`../cka-sim/README.md`](../cka-sim/README.md)
    - Trap catalog (canonical id list) — [`../cka-sim/traps/catalog.yaml`](../cka-sim/traps/catalog.yaml)
    - Phase 16 design context (locked decisions) — [`../.planning/phases/16-question-intent-baseline-harness/16-CONTEXT.md`](../.planning/phases/16-question-intent-baseline-harness/16-CONTEXT.md)

    LF line endings. Markdown lints clean (no trailing whitespace, ATX headings, fenced code blocks).
  </action>
  <verify>
    <automated>test -f docs/AUTHORING.md && grep -c '^# Authoring CKA Exam Simulator Questions' docs/AUTHORING.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f docs/AUTHORING.md`.
    - Title heading present exactly once: `grep -c '^# Authoring CKA Exam Simulator Questions' docs/AUTHORING.md` returns 1.
    - All 7 sections present: `grep -cE '^## ' docs/AUTHORING.md` returns >= 6 (intro is the title; 6 H2 sections).
    - Triplet table present: `grep -c 'expected-symptom.yaml' docs/AUTHORING.md` returns >= 3 (table + worked example + cross-link); `grep -c 'lib/baseline.sh' docs/AUTHORING.md` returns >= 2; `grep -c 'grade.sh' docs/AUTHORING.md` returns >= 2.
    - Worked example uses storage/01-pvc-binding: `grep -c 'storage-pvc-binding\|storage/01-pvc-binding' docs/AUTHORING.md` returns >= 2.
    - Audit invocation reference complete: `grep -c 'cka-sim audit' docs/AUTHORING.md` returns >= 4 (3 scopes + intro mention); `grep -c -- '--report' docs/AUTHORING.md` returns >= 1; `grep -cE '^\s*0\s+all PASS\b|^\s*1\s+at least one FAIL\b|^\s*2\s+preflight error\b' docs/AUTHORING.md` returns 3.
    - Cross-links to schema + grading-honesty docs: `grep -c '../cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md' docs/AUTHORING.md` returns >= 2; `grep -c '../cka-sim/lib/GRADING-HONESTY.md' docs/AUTHORING.md` returns >= 2.
    - "Don't generate from setup output" guidance present: `grep -c 'NOT from running setup.sh\|encode the drift instead' docs/AUTHORING.md` returns >= 1.
    - Audit-not-in-CI note present: `grep -c 'NOT wired to GHA' docs/AUTHORING.md` returns 1.
    - File length reasonable: `wc -l docs/AUTHORING.md` returns between 100 and 500 lines.
  </acceptance_criteria>
  <done>docs/AUTHORING.md ships with the locked 7-section structure, a worked storage/01-pvc-binding prose-to-YAML example, the cka-sim audit invocation reference, and cross-links to the schema and grading-honesty docs.</done>
</task>

<task type="auto">
  <name>Task 2: Surface AUTHORING.md from cka-sim/README.md</name>
  <read_first>
    - cka-sim/README.md (full — find the right insertion point)
    - docs/AUTHORING.md (Task 1 — confirm path is `../docs/AUTHORING.md` from cka-sim/README.md)
  </read_first>
  <files>cka-sim/README.md</files>
  <action>
    Insert a new "Documentation" section between the existing "Mock Exams" and "Development" sections, OR extend the existing "Development" section — pick the lighter touch.

    Recommended (lighter touch): add a new section right after "Mock Exams":

    ```markdown
    ## Documentation

    - [Authoring guide](../docs/AUTHORING.md) — how to author a new question end-to-end (test-artifact triplet, prose-to-YAML derivation, audit invocation).
    - [expected-symptom.yaml schema](packs/EXPECTED-SYMPTOM-SCHEMA.md) — schema reference for the post-setup symptom file.
    - [Grading honesty](lib/GRADING-HONESTY.md) — Phase 07.1 candidate-state baseline contract.
    ```

    Place this AFTER the "Mock Exams" section (the section that currently ends with the blueprint description) and BEFORE the "Development" section. Do NOT touch any other section.

    LF line endings.
  </action>
  <verify>
    <automated>grep -c '^## Documentation$' cka-sim/README.md && grep -c '../docs/AUTHORING.md' cka-sim/README.md</automated>
  </verify>
  <acceptance_criteria>
    - Documentation section heading present: `grep -c '^## Documentation$' cka-sim/README.md` returns 1.
    - AUTHORING.md link present and relative: `grep -c '../docs/AUTHORING.md' cka-sim/README.md` returns 1.
    - Schema doc link present: `grep -c 'packs/EXPECTED-SYMPTOM-SCHEMA.md' cka-sim/README.md` returns 1.
    - GRADING-HONESTY link present: `grep -c 'lib/GRADING-HONESTY.md' cka-sim/README.md` returns 1.
    - Section ordering preserved: the line number of `^## Documentation$` is between the line of `^## Mock Exams$` and the line of `^## Development$`. Verify with `awk '/^## Mock Exams$/{m=NR} /^## Documentation$/{d=NR} /^## Development$/{p=NR} END{exit !(m<d && d<p)}' cka-sim/README.md`.
    - No other section headings touched: `grep -cE '^## (Setup|SSH to Worker Nodes|Quickstart|Architecture|Domain Packs|Mock Exams|Development|Disclaimer)$' cka-sim/README.md` returns 8 (all original sections still present).
  </acceptance_criteria>
  <done>cka-sim/README.md surfaces docs/AUTHORING.md (and the existing schema + grading-honesty docs) in a new Documentation section. Existing sections untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Reciprocal link from EXPECTED-SYMPTOM-SCHEMA.md to AUTHORING.md</name>
  <read_first>
    - cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md (full)
    - docs/AUTHORING.md (Task 1)
  </read_first>
  <files>cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md</files>
  <action>
    Add a navigation note immediately after the title `# expected-symptom.yaml schema` and before the existing first paragraph. Insert exactly:

    ```markdown
    > **For authors:** start with [`../../docs/AUTHORING.md`](../../docs/AUTHORING.md) — this file is the schema reference; AUTHORING.md walks through the prose-to-YAML derivation workflow.
    ```

    Do NOT change any other content. The "Worked example: storage/01-pvc-binding" section already in this doc remains the canonical schema-side example; AUTHORING.md's example is the workflow-side example. Both are intentional.

    LF line endings.
  </action>
  <verify>
    <automated>grep -c 'docs/AUTHORING.md' cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md</automated>
  </verify>
  <acceptance_criteria>
    - AUTHORING.md link present and relative: `grep -c '../../docs/AUTHORING.md' cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md` returns 1.
    - Reciprocal link is in the navigation header (early in the file): `awk '/^# expected-symptom.yaml schema$/{t=NR} /docs\/AUTHORING.md/{l=NR} END{exit !(l > t && l - t < 5)}' cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md`.
    - Existing schema content untouched: `grep -c '^## Shape$' cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md` returns 1; `grep -c '^## Worked example: storage/01-pvc-binding$' cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md` returns 1; `grep -c 'Authoring guidance' cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md` returns 1.
  </acceptance_criteria>
  <done>Schema doc has a top-of-file navigation note pointing readers at AUTHORING.md. Existing content untouched.</done>
</task>

<task type="auto">
  <name>Task 4: Bundle PROJECT.md 38→34 question-count fix (CONTEXT.md D-09)</name>
  <read_first>
    - .planning/PROJECT.md (full)
    - .planning/phases/16-question-intent-baseline-harness/16-CONTEXT.md (D-09 lock)
  </read_first>
  <files>.planning/PROJECT.md</files>
  <action>
    Edit `.planning/PROJECT.md` to replace the two occurrences of "38 questions" / "38 total questions" with "34 questions" / "34 total questions".

    Concrete edits:

    1. Line 5 (the "What This Is" paragraph) — replace `38 total questions` with `34 total questions`.

    2. Line ~16 (the v1.0 shipped state bullet) — replace `38 questions across 5 domain packs` with `34 questions across 5 domain packs`.

    3. Line ~31 (the v1.0 Validated requirement bullet) — replace `38 questions total` with `34 questions total`.

    4. Line ~63 (the v1.0.2 milestone goal section if present) — review for any 38 reference and update if found.

    Use the Edit tool with `replace_all: false` for each occurrence so we don't accidentally match an unrelated 38 (e.g. line numbers, port numbers). For each replacement, include enough surrounding text to make the match unique.

    Verify the file no longer contains "38 questions" or "38 total":
    `grep -cE '38 (questions|total)' .planning/PROJECT.md` should return 0.

    LF line endings preserved.
  </action>
  <verify>
    <automated>! grep -qE '38 (questions|total)' .planning/PROJECT.md && grep -cE '34 questions|34 total' .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - No "38 questions" references remain: `grep -cE '38 questions|38 total questions|38 total' .planning/PROJECT.md` returns 0.
    - 34-count corrections present: `grep -cE '34 questions|34 total questions' .planning/PROJECT.md` returns >= 2.
    - Domain Packs table values still correct (6+8+6+8+6=34): `grep -cE '\| 6 \||\| 8 \|' .planning/PROJECT.md` returns >= 5 (6 appears 3x, 8 appears 2x in the table).
    - Other PROJECT.md content untouched (regression check): `grep -c '^# CKA Exam Simulator$' .planning/PROJECT.md` returns 1; `grep -c '^## Current Milestone' .planning/PROJECT.md` returns 1.
  </acceptance_criteria>
  <done>PROJECT.md question count corrected from 38 to 34 in all narrative references; Domain Packs table already correct; other content untouched.</done>
</task>

</tasks>

<verification>
After all 4 tasks complete:

1. `test -f docs/AUTHORING.md` — file exists.
2. `grep -c '^## ' docs/AUTHORING.md` returns >= 6 — section structure correct.
3. `grep -c '../docs/AUTHORING.md' cka-sim/README.md` returns 1 — README surfaces the doc.
4. `grep -c '../../docs/AUTHORING.md' cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md` returns 1 — schema reciprocates.
5. `! grep -qE '38 (questions|total)' .planning/PROJECT.md` — count corrected.
6. Markdown link integrity (manual): paths resolve relative to their containing files. From `docs/AUTHORING.md`, `../cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md` resolves correctly. From `cka-sim/README.md`, `../docs/AUTHORING.md` resolves correctly. From `cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md`, `../../docs/AUTHORING.md` resolves correctly.
</verification>

<success_criteria>
- docs/AUTHORING.md ships at the repo root under a new docs/ directory with the locked 7-section structure.
- AUTHORING.md is discoverable from cka-sim/README.md AND from cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md.
- PROJECT.md no longer claims 38 domain questions.
- This plan does NOT modify the audit tool itself, does NOT regenerate any expected-symptom.yaml, and does NOT touch GHA workflows. It is documentation + count-fix only.
</success_criteria>

<output>
After completion, create `.planning/phases/16-question-intent-baseline-harness/16-02-SUMMARY.md` documenting:
- Files created (1): `docs/AUTHORING.md` (new docs/ dir).
- Files modified (3): `cka-sim/README.md` (Documentation section), `cka-sim/packs/EXPECTED-SYMPTOM-SCHEMA.md` (reciprocal link), `.planning/PROJECT.md` (38 → 34 fix).
- Section structure of AUTHORING.md: 7 sections per CONTEXT.md D-10 (intro, triplet table, prose-to-YAML worked example, baseline.sh stub, grade.sh stub, audit invocation reference, cross-links).
- Worked example chosen: storage/01-pvc-binding (already cited in EXPECTED-SYMPTOM-SCHEMA.md so the two examples reinforce each other).
- D-09 PROJECT.md correction landed (38 → 34).
</output>
</content>
</invoke>