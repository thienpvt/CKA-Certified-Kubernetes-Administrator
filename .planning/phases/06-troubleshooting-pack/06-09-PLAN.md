---
phase: 06-troubleshooting-pack
plan: 09
type: execute
wave: 3
depends_on: [06-01, 06-02, 06-03, 06-04, 06-05, 06-06, 06-07, 06-08]
files_modified:
  - cka-sim/packs/troubleshooting/manifest.yaml
  - cka-sim/packs/troubleshooting/coverage.yaml
  - cka-sim/packs/troubleshooting/README.md
  - .planning/phases/06-troubleshooting-pack/06-VERIFICATION.md
  - .planning/STATE.md
autonomous: false
requirements: [PACK-05, PACK-06, PACK-07]
tags: [pack-finalization, coverage-matrix, verification, phase-06]
validation:
  sampling_rate: per-wave-merge
  quick_command: "bash cka-sim/scripts/lint-packs.sh && bash cka-sim/scripts/lint-coverage.sh"
  full_command: "bash cka-sim/scripts/test.sh"
  validation_md_rows:
    - 06-09-XX
# autonomous_justification: Task 1 (pack-root authoring) and Task 2 (VERIFICATION.md authoring) execute autonomously. Task 3 (live 6-drill human checkpoint) requires the 1+2 cluster and is the only non-autonomous task. The plan's autonomous:false flag applies to the plan as a gate (at least one task blocks for human), not to all tasks. An executor proceeds through Tasks 1-2 without pausing; Task 3 blocks until candidate types "approved".
must_haves:
  truths:
    - "packs/troubleshooting/manifest.yaml enumerates all 6 questions (Q01 existing + Q02..Q06 new) with correct estimatedMinutes"
    - "packs/troubleshooting/coverage.yaml exists and maps every v1.35 Troubleshooting Tracker checkbox to >=1 question; service-endpoints + imagepullbackoff-diagnosis both reference Q01; static-pod-logs + pending-pods + crashloop-diagnosis all reference Q05"
    - "packs/troubleshooting/README.md reflects the final 6-question table with slugs, tracker slugs, and time budgets"
    - "lint-coverage.sh troubleshooting prints coverage schema OK (>=9 tracker slugs, >=6 questions)"
    - "lint-coverage.sh (all packs) reports every pack schema OK AND 5 packs each with >=1 question per Tracker checkbox — closes PACK-07 100% coverage"
    - "lint-packs.sh exits 0 across every pack including troubleshooting (pass G forbidden-command guard green on all 6 questions' scripts)"
    - "Every troubleshooting metadata.yaml has at least one references[] entry whose target begins with cka-sim/packs/ (D-05 cross-pack guarantee)"
    - "06-VERIFICATION.md authored with 8 must-haves, requirement traceability for PACK-05/PACK-06/PACK-07, Human Verification section enumerating 6 live drills"
    - "STATE.md Phase 6 status flips from READY to verifying/human_needed once candidate confirms live drills"
    - "Autonomous gating: Task 1 and Task 2 execute without prompting; Task 3 is the sole human-verify gate (autonomous:false applies to the plan envelope, not every task — see autonomous_justification frontmatter comment)"
    - "Decision coverage: D-01 (manifest enumerates the 6-question pack), D-02 (Q01 is service/endpoints mismatch retrofit — carried in manifest), D-03 (manifest preserves the progressive ramp order 01..06)."
  artifacts:
    - path: "cka-sim/packs/troubleshooting/manifest.yaml"
      provides: "Final 6-question manifest for the Troubleshooting pack"
      contains: "troubleshooting-netpol-dns-egress"
      contains_extra: "troubleshooting-broken-kubelet"
    - path: "cka-sim/packs/troubleshooting/coverage.yaml"
      provides: "PACK-07 Tracker mapping — 9 tracker slugs collectively mapped to 6 questions"
      contains: "troubleshoot-coredns"
      contains_extra: "debug-kubectl-node"
    - path: "cka-sim/packs/troubleshooting/README.md"
      provides: "Candidate-facing 6-question table"
      contains: "netpol-dns-egress"
    - path: ".planning/phases/06-troubleshooting-pack/06-VERIFICATION.md"
      provides: "Phase 6 verification report (7 automated + 1 manual live drill must-have)"
      contains: "Must-Haves Verification"
  key_links:
    - from: "cka-sim/packs/troubleshooting/coverage.yaml"
      to: "cka-sim/packs/troubleshooting/manifest.yaml"
      via: "coverage.questions[] entries must appear in manifest.questions[].id"
      pattern: "troubleshooting-"
    - from: ".planning/phases/06-troubleshooting-pack/06-VERIFICATION.md"
      to: ".planning/STATE.md"
      via: "Phase 6 status update on sign-off"
      pattern: "Phase 6"
---

<objective>
Wave 3 — finalize the Troubleshooting pack and author Phase 6 VERIFICATION. This plan runs AFTER P01-P08 complete. Output:

1. Replace `cka-sim/packs/troubleshooting/manifest.yaml` placeholder (Phase 3 shape — Q01 only) with the final 6-question shape.
2. Create `cka-sim/packs/troubleshooting/coverage.yaml` mapping every v1.35 Troubleshooting Tracker checkbox to >=1 question (closes PACK-07).
3. Replace `cka-sim/packs/troubleshooting/README.md` placeholder table with the final 6-row shape.
4. Verify `lint-coverage.sh troubleshooting` reports 100% coverage AND full-tree `lint-coverage.sh` reports all 5 packs 100%.
5. Author `.planning/phases/06-troubleshooting-pack/06-VERIFICATION.md` with 8 must-haves, requirement traceability, and a Human Verification section listing the 6 live drills.
6. Checkpoint blocks until candidate confirms the 6 live drills round-trip green.

**Autonomous gating (see autonomous_justification frontmatter comment):** `autonomous: false` in this plan is a plan-level gate marker because at least one task (Task 3) requires human interaction. Task 1 and Task 2 execute fully autonomously — the executor MUST NOT pause between them. The only human-verify pause is Task 3.

This is the phase closure. After sign-off, Phase 6 status flips in STATE.md and the roadmap advances toward Phase 7 (Exam Mode).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-troubleshooting-pack/06-CONTEXT.md
@.planning/phases/06-troubleshooting-pack/06-RESEARCH.md
@.planning/phases/06-troubleshooting-pack/06-PATTERNS.md
@.planning/phases/06-troubleshooting-pack/06-VALIDATION.md
@.planning/phases/05-services-networking-cluster-architecture-packs/05-16-PLAN.md
@cka-sim/packs/troubleshooting/manifest.yaml
@cka-sim/packs/troubleshooting/README.md
@cka-sim/packs/services-networking/manifest.yaml
@cka-sim/packs/services-networking/coverage.yaml
@cka-sim/packs/services-networking/README.md
@cka-sim/packs/workloads-scheduling/coverage.yaml
@cka-sim/scripts/lint-coverage.sh
</context>

<interfaces>
**Pack manifest — final question roster** (mirror `cka-sim/packs/services-networking/manifest.yaml` for byte-exact shape):

| Order | id | path | estimatedMinutes |
|---|---|---|---|
| 1 | troubleshooting-deploy-svc-mismatch | 01-deploy-svc-mismatch | 7 |
| 2 | troubleshooting-netpol-dns-egress | 02-netpol-dns-egress | 8 |
| 3 | troubleshooting-coredns-resolution | 03-coredns-resolution | 8 |
| 4 | troubleshooting-debug-node | 04-debug-node | 9 |
| 5 | troubleshooting-static-pod-manifest | 05-static-pod-manifest | 10 |
| 6 | troubleshooting-broken-kubelet | 06-broken-kubelet | 11 |

Total estimatedMinutes: 53. Pack root: `id: troubleshooting`, `domain: troubleshooting`, `weight: 30`, description line names PACK-05 and "full v1.35 Tracker coverage".

**Coverage schema** (mirror `cka-sim/packs/services-networking/coverage.yaml`): `domain: troubleshooting`; `tracker:` block with 9 slugs; each slug has `label:` (human-readable) and `questions:` (list of manifest question IDs).

v1.35 Troubleshooting Tracker slug → question mapping (per RESEARCH §2):

| Tracker slug | Tracker label | Questions |
|---|---|---|
| kubelet-journalctl | Read kubelet logs with journalctl | troubleshooting-broken-kubelet |
| control-plane-pod-logs | Check control plane pod logs | troubleshooting-static-pod-manifest |
| pending-pods | Debug Pending pods | troubleshooting-static-pod-manifest |
| crashloop-diagnosis | Debug CrashLoopBackOff pods | troubleshooting-static-pod-manifest |
| imagepullbackoff-diagnosis | Debug ImagePullBackOff | troubleshooting-deploy-svc-mismatch |
| service-endpoints | Troubleshoot Service endpoints | troubleshooting-deploy-svc-mismatch |
| troubleshoot-coredns | Troubleshoot CoreDNS | troubleshooting-coredns-resolution |
| troubleshoot-netpol | Troubleshoot NetworkPolicy | troubleshooting-netpol-dns-egress |
| debug-kubectl-node | Use kubectl debug (ephemeral + node) | troubleshooting-debug-node |

9 tracker slugs → 6 questions (N:1 mapping allowed; lint-coverage accepts it).

**README shape** (mirror `cka-sim/packs/services-networking/README.md`): H1 title + domain banner with PACK-05 reference; narrative sentence; `## Questions` section with a 6-row markdown table (columns: `#`, `Slug` as markdown link, `Tracker slug`, `Time`); pack-total summary line; `## Host-safety contract` section (4 bullets: sandboxing, no-/var/lib/kubelet-writes, no-/etc/kubernetes-writes, no-systemctl, no-kube-system-mutation); `## Authoring` pointer to `cka-sim/AUTHORING.md`; `## Running` with `cka-sim drill troubleshooting` examples; blockquote disclaimer "Not real CKA exam content; independently authored."

**VERIFICATION.md shape** (mirror `.planning/phases/05-services-networking-cluster-architecture-packs/05-16-PLAN.md` Task 1 output). Required frontmatter keys: `phase: 06`, `verified: <ISO date>`, `status: human_needed`, `must_haves_passed: 7`, `must_haves_total: 8`, `human_verification_count: 1`, `score: "7/8 must-haves verified programmatically"`, `gaps: []`, `re_verification: { previous_status: null }`, `requirements_coverage: { PACK-05: satisfied, PACK-06: satisfied (Troubleshooting subset), PACK-07: satisfied (all 5 packs at 100% Tracker coverage) }`, `deferred_items` list (see Task 2 action for the 5 entries to carry forward).

8 Must-Haves for Phase 6 (derived from 06-CONTEXT §Boundary + ROADMAP Phase 6 success criteria + CONTEXT D-05/D-09/D-11/D-12):

- MH-1: Troubleshooting pack has >=1 question each for CoreDNS, kubectl debug node, NetworkPolicy, broken kubelet, static pod.
- MH-2: Coverage-matrix lint reports 100% for troubleshooting AND for every pack collectively (closes PACK-07).
- MH-3: Every troubleshooting metadata.yaml has >=1 references[] entry whose target begins with `cka-sim/packs/` (D-05 cross-pack guarantee).
- MH-4: `cka-sim drill troubleshooting` can run every question without error (manual live check — human-gated).
- MH-5: Every trap ID referenced is registered in `cka-sim/traps/catalog.yaml` (catalog + lint-packs pass E).
- MH-6: lint-packs.sh pass G (forbidden-command guard, added in P01) exits 0 against the full troubleshooting pack — NO `systemctl`, NO `kubectl edit configmap coredns -n kube-system`, NO `/etc/kubernetes/` writes, NO `/var/lib/kubelet/` writes.
- MH-7: All 4 previously-existing lints exit 0 on the post-P08 tree (lint-traps, lint-packs, lint-coverage, lint-deprecated-strings).
- MH-8: `bash cka-sim/scripts/test.sh` exits 0 (round-trip fixtures for all 6 questions).

Requirements traceability: PACK-05, PACK-06 (Troubleshooting subset), PACK-07 (final 100% coverage).

Pass G forbidden-command guard lives in lint-packs.sh from P01 and applies to every troubleshooting `.sh` file. Re-assertion here: run `bash cka-sim/scripts/lint-packs.sh` against the final tree and grep the output for pass G evidence.

**Lint-coverage.sh behaviour** (from `cka-sim/scripts/lint-coverage.sh`): walks every `packs/*/` directory, requires `manifest.yaml` to exist, accepts missing `coverage.yaml` with a warning during scaffold. For Phase 6: coverage.yaml MUST exist. Emits `<pack>: coverage schema OK` on pass. Collective-tree invocation with no arg lints all 5 packs.

**CRITICAL ordering constraint**: Tasks 1+2 must complete BEFORE Task 3 (human checkpoint). Tasks 1 and 2 are independent of each other in content but Task 2 references coverage.yaml + manifest.yaml created by Task 1, so Task 2 runs after Task 1 completes.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write troubleshooting/manifest.yaml + coverage.yaml + README.md</name>
  <files>cka-sim/packs/troubleshooting/manifest.yaml, cka-sim/packs/troubleshooting/coverage.yaml, cka-sim/packs/troubleshooting/README.md</files>
  <read_first>
    - cka-sim/packs/troubleshooting/manifest.yaml (current Phase 3 Q01-only shape to replace)
    - cka-sim/packs/troubleshooting/README.md (current Phase 3 placeholder to replace)
    - cka-sim/packs/services-networking/manifest.yaml (canonical 6-question shape with sentinel markers)
    - cka-sim/packs/services-networking/coverage.yaml (canonical coverage shape)
    - cka-sim/packs/services-networking/README.md (canonical 6-row table)
    - cka-sim/packs/workloads-scheduling/coverage.yaml (multi-tracker N:1 mapping example: taints-tolerations + nodeselector-node-affinity both point at workloads-nodeselector-affinity-taints)
    - .planning/phases/06-troubleshooting-pack/06-RESEARCH.md (§2 coverage table)
    - .planning/phases/06-troubleshooting-pack/06-PATTERNS.md (§Excerpt E manifest + coverage shapes)
    - cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/metadata.yaml (verify id + estimatedMinutes after P03 retrofit)
    - cka-sim/packs/troubleshooting/02-netpol-dns-egress/metadata.yaml
    - cka-sim/packs/troubleshooting/03-coredns-resolution/metadata.yaml
    - cka-sim/packs/troubleshooting/04-debug-node/metadata.yaml
    - cka-sim/packs/troubleshooting/05-static-pod-manifest/metadata.yaml
    - cka-sim/packs/troubleshooting/06-broken-kubelet/metadata.yaml
  </read_first>
  <action>
    Produce 3 pack-root files matching the shapes described in Interfaces. Use services-networking/ as the byte-exact structural analog (see read_first).

    **manifest.yaml — required shape:**
    - Top-level `pack:` map with fields: `id: troubleshooting`, `domain: troubleshooting`, `weight: 30`, `description` line naming PACK-05 and the v1.35 Tracker coverage claim.
    - Top-level `questions:` list with 6 entries in the Interfaces-table order. Each entry: `id`, `path` (relative slug), `estimatedMinutes` (integer). Include sentinel comments `# BEGIN phase-06 new questions` / `# END phase-06 new questions` bracketing entries 2-6 (idempotency — any future /gsd-plan-phase re-run can grep for the sentinel and skip).
    - Preserve the existing Q01 entry byte-for-byte; only append entries 2-6 between the sentinels.
    - Pack total estimatedMinutes: 53.

    **coverage.yaml — required shape (NEW file):**
    - Header comment (2 lines): PACK-07 coverage matrix for the Troubleshooting domain (v1.35 Study Progress Tracker); note that `tracker.<slug>.questions` lists manifest question-ids for that Tracker checkbox; multi-question mapping permitted per RESEARCH §2.
    - `domain: troubleshooting`
    - `tracker:` map with the 9 slugs in Interfaces-table order. Each slug key maps to a sub-map with `label:` (human-readable quoted string) and `questions:` (list of 1+ manifest question IDs).
    - Slug order: kubelet-journalctl → control-plane-pod-logs → pending-pods → crashloop-diagnosis → imagepullbackoff-diagnosis → service-endpoints → troubleshoot-coredns → troubleshoot-netpol → debug-kubectl-node.
    - Q01 (`troubleshooting-deploy-svc-mismatch`) appears in TWO slugs: `imagepullbackoff-diagnosis` AND `service-endpoints`.
    - Q05 (`troubleshooting-static-pod-manifest`) appears in THREE slugs: `control-plane-pod-logs`, `pending-pods`, `crashloop-diagnosis`.
    - Q02, Q03, Q04, Q06 each appear in exactly one slug.

    **README.md — required shape:**
    - H1 title: `# Troubleshooting Pack`.
    - Domain/weight banner line: `**Domain:** Troubleshooting (30% of CKA blueprint v1.35) — PACK-05.`
    - Narrative sentence: progressive difficulty ramp from Service/Deployment label diagnosis to sandboxed kubelet-flag-file repair.
    - `## Questions` section: 6-row markdown table with columns `#`, `Slug`, `Tracker slug`, `Time`. Slug cell is a markdown link to the question subdirectory (e.g. `[deploy-svc-mismatch](01-deploy-svc-mismatch/)`). Tracker slug cell lists the coverage.yaml slug(s) — for Q01 use `service-endpoints + imagepullbackoff-diagnosis`; for Q05 use `control-plane-pod-logs + pending-pods + crashloop-diagnosis`; others use the single slug.
    - Pack-total line: `Pack total: 6 questions, ~53 min.`
    - `## Host-safety contract` section with 4 bullets: per-question sandbox at `/tmp/qNN-*/`; no script writes into `/var/lib/kubelet/`, `/etc/kubernetes/`, or `/etc/kubernetes/manifests/`; no script invokes `systemctl`; no script mutates any object in `kube-system` (lab-ns CoreDNS rather than cluster-scoped).
    - `## Authoring` section: pointer to `cka-sim/AUTHORING.md`.
    - `## Running` section: two example command lines (`cka-sim drill troubleshooting` random + `cka-sim drill troubleshooting 1` indexed).
    - Closing blockquote: `> Not real CKA exam content; independently authored.`
  </action>
  <verify>
    <automated>bash cka-sim/scripts/lint-packs.sh && bash cka-sim/scripts/lint-coverage.sh troubleshooting && bash cka-sim/scripts/lint-coverage.sh && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - manifest.yaml lists all 6 questions in order: `for id in troubleshooting-deploy-svc-mismatch troubleshooting-netpol-dns-egress troubleshooting-coredns-resolution troubleshooting-debug-node troubleshooting-static-pod-manifest troubleshooting-broken-kubelet; do grep -qE "^  - id: $id$" cka-sim/packs/troubleshooting/manifest.yaml || { echo "missing: $id"; exit 1; }; done`
    - manifest totals to 53 estimatedMinutes: `awk '/estimatedMinutes:/{sum+=$2} END{print sum}' cka-sim/packs/troubleshooting/manifest.yaml | grep -qx 53`
    - coverage.yaml exists with domain: troubleshooting: `test -f cka-sim/packs/troubleshooting/coverage.yaml && grep -qE '^domain: troubleshooting$' cka-sim/packs/troubleshooting/coverage.yaml`
    - coverage.yaml lists 9 tracker slugs: `grep -cE '^  [a-z][a-z0-9-]+:$' cka-sim/packs/troubleshooting/coverage.yaml | grep -qx 9`
    - All 9 tracker slugs present with correct labels: `for slug in kubelet-journalctl control-plane-pod-logs pending-pods crashloop-diagnosis imagepullbackoff-diagnosis service-endpoints troubleshoot-coredns troubleshoot-netpol debug-kubectl-node; do grep -qE "^  $slug:$" cka-sim/packs/troubleshooting/coverage.yaml || { echo "missing slug: $slug"; exit 1; }; done`
    - Every coverage question reference maps to an existing manifest id: `bash cka-sim/scripts/lint-coverage.sh troubleshooting >/dev/null`
    - Specifically imagepullbackoff-diagnosis and service-endpoints both map to Q01: `awk '/^  imagepullbackoff-diagnosis:/{found=1; next} found && /^      - /{print; exit}' cka-sim/packs/troubleshooting/coverage.yaml | grep -q 'troubleshooting-deploy-svc-mismatch' && awk '/^  service-endpoints:/{found=1; next} found && /^      - /{print; exit}' cka-sim/packs/troubleshooting/coverage.yaml | grep -q 'troubleshooting-deploy-svc-mismatch'`
    - Q05 maps to 3 slugs: `grep -c 'troubleshooting-static-pod-manifest' cka-sim/packs/troubleshooting/coverage.yaml | awk '$1 >= 3 {exit 0} {exit 1}'`
    - README has 6-row table: `grep -cE '^\| [0-9]{2} \|' cka-sim/packs/troubleshooting/README.md | grep -qx 6`
    - README retains the disclaimer: `grep -q 'Not real CKA exam content' cka-sim/packs/troubleshooting/README.md`
    - lint-coverage.sh troubleshooting green: `bash cka-sim/scripts/lint-coverage.sh troubleshooting 2>&1 | grep -qE 'coverage schema OK'`
    - lint-coverage.sh (all 5 packs) green: `bash cka-sim/scripts/lint-coverage.sh 2>&1 | grep -qE 'coverage lint passed \(5 pack\(s\)'`
    - Full suite green: `bash cka-sim/scripts/test.sh; test $? -eq 0`
  </acceptance_criteria>
  <done>manifest.yaml + coverage.yaml + README.md land; lint-coverage reports 100% across 5 packs.</done>
</task>

<task type="auto">
  <name>Task 2: Author Phase 6 VERIFICATION.md with 7 automated must-haves + 1 manual human-verify block</name>
  <files>.planning/phases/06-troubleshooting-pack/06-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/05-services-networking-cluster-architecture-packs/05-16-PLAN.md (Task 1 what-built + action + resume-signal — the canonical VERIFICATION.md shape to mirror)
    - .planning/phases/04-storage-workloads-scheduling-packs/04-VERIFICATION.md (Phase 4 exemplar — section order and frontmatter keys)
    - .planning/phases/06-troubleshooting-pack/06-CONTEXT.md (§Boundary — the 5 required topics)
    - .planning/phases/06-troubleshooting-pack/06-RESEARCH.md (§8 VERIFICATION criteria; §10 Validation Architecture)
    - .planning/phases/06-troubleshooting-pack/06-VALIDATION.md (§Manual-Only Verifications — the 5 live-drill manual checks)
    - .planning/STATE.md (Phase 6 current status; WR-01 + DF-08 deferred items to carry forward)
    - cka-sim/packs/troubleshooting/coverage.yaml (post-Task-1 state)
    - cka-sim/traps/catalog.yaml (post-P02 state)
  </read_first>
  <action>
    Author `.planning/phases/06-troubleshooting-pack/06-VERIFICATION.md` mirroring the 05-16-PLAN.md `what-built` outline. Structural requirements:

    **Frontmatter (YAML) — required keys:**
    - `phase: 06`
    - `verified:` set to the ISO date at author time.
    - `status: human_needed`
    - `must_haves_passed: 7`
    - `must_haves_total: 8`
    - `human_verification_count: 1`
    - `score: "7/8 must-haves verified programmatically"`
    - `gaps: []`
    - `re_verification: { previous_status: null }`
    - `requirements_coverage:` map with `PACK-05: satisfied`, `PACK-06: "satisfied (Troubleshooting subset)"`, `PACK-07: "satisfied (all 5 packs at 100% Tracker coverage)"`.
    - `deferred_items:` list with 5 entries:
      1. `ref: WR-01 (Phase 4)` — note: full vendoring of CSI + metrics-server manifests under cka-sim/vendor/ with recorded SHA256.
      2. `ref: IN-04 (Phase 4)` — note: cka_sim::grade::assert_custom helper + 6-grader retrofit.
      3. `ref: DF-08` — note: Hint reveal (drill mode only).
      4. `ref: Phase 1 live UAT` — note: tracked in 01-HUMAN-UAT.md; reopen via `/gsd-verify-work 1`.
      5. `ref: Phase 5 live UAT` — note: tracked in 05-VERIFICATION.md; reopen via `/gsd-verify-work 5`.

    **Body sections (in this order):**

    1. **Summary** — 1 paragraph: Phase 6 Troubleshooting pack completes PACK-05 with 6 questions at a progressive difficulty ramp (53 min total); closes PACK-07 100% coverage across all 5 packs; introduces 11 new trap catalog entries + 1 forbidden-command lint guard. Host-safety contract (D-09/D-11/D-12) encoded twice: per-question sandboxing + lint-packs pass G.

    2. **Observable Truths table** — 8 rows, one per must-have, with MH-4 marked [HUMAN].

    3. **Required Artifacts table** — every file created or modified across P01-P09: lint-packs.sh (pass G), catalog.yaml (11 new entries), 6 question dirs × 6 files each, 6 fixture dirs × 3 files, manifest.yaml, coverage.yaml, README.md, VERIFICATION.md. Annotate each row with expected `contains` substrings.

    4. **Key Link Verification table** — 5 rows:
       - setup.sh → lib/setup.sh (source + helper calls).
       - metadata.traps → catalog.yaml (lint-packs pass E).
       - coverage.yaml.tracker.*.questions → manifest.yaml.questions[].id (lint-coverage).
       - metadata.references[].target beginning `cka-sim/packs/` → existing pack paths (D-05).
       - troubleshooting/*.sh → pass G forbidden-command guard (no mutation of live kube-system / /var/lib/kubelet/ / /etc/kubernetes/).

    5. **Automated Checks table** — 5 commands + expected evidence:
       - `bash cka-sim/scripts/lint-traps.sh` → schema OK on every entry.
       - `bash cka-sim/scripts/lint-packs.sh` → `pack lint passed` including passes A..G.
       - `bash cka-sim/scripts/lint-coverage.sh` → `coverage lint passed` across all 5 packs.
       - `bash cka-sim/scripts/lint-deprecated-strings.sh` → exits 0.
       - `bash cka-sim/scripts/test.sh` → `all N case(s) passed`.

    6. **8 Must-Haves Verification subsections** — one subsection per MH. Each subsection: copies the MH truth verbatim from the Interfaces 8-must-haves list, pastes the automated command output as evidence, marks MH-4 as `**HUMAN — see Human Verification Required section below**`.

    7. **Requirements Traceability table** — columns: REQ-ID, description, source plan IDs. Rows: PACK-05 / PACK-06 (Troubleshooting subset) / PACK-07, mapping to plan IDs 06-01 through 06-09.

    8. **Anti-Patterns Scan** — reference lint-packs passes A–G. Pass G (NEW in Phase 6) enumerated explicitly: `grep -q 'pass G' cka-sim/scripts/lint-packs.sh` confirms the block is present; the negative-fixture test case `lint_packs_forbidden_command.sh` is in the green suite.

    9. **Behavioral Spot-Checks table** — one row per question with FAIL/PASS fixture scores:
       - Q01: 2/3 → 3/3 (endpoints oracle + 2 resource existence; +trap on FAIL).
       - Q02: 4/6 → 6/6 (4 structural + 2 probes).
       - Q03: 5/6 → 6/6 (5 structural + 1 DNS probe).
       - Q04: 0/1 → 1/1 (D-10 gated oracle; answer + debug-source evidence both required for PASS).
       - Q05: 1/4 → 4/4 (file exists + parse + kind + dry-run).
       - Q06: 1/3 → 3/3 (exists + source-parseable + endpoint correct).

    10. **Human Verification Required section** — MH-4 live checklist. Structural requirements:
        - Intro paragraph explaining the 6-drill loop and the 4 host-safety post-checks.
        - A shell block with the 6-drill for-loop (`for i in 01 02 03 04 05 06; do cka-sim drill troubleshooting --question "$i" --grade-broken; --ref-solution; --grade; --reset; done`) plus the TRIP-02 idempotency double-call.
        - Per-question attention notes (6 bullets):
          - Q01: web-canary reaches ImagePullBackOff within 30s; grader records the trap; ref-solution deletes the canary.
          - Q02: two-stage fix (label-drift + DNS-allow); nslookup + /dev/tcp probes both exit 0 post-fix.
          - Q03: kube-system/coredns ConfigMap UNCHANGED after drill (baseline diff per VALIDATION.md).
          - Q04: NO debug pods survive reset (`kubectl get pods -A -l kubectl.kubernetes.io/debug-source` returns empty).
          - Q05: `/etc/kubernetes/manifests/` listing UNCHANGED before/after drill.
          - Q06: `/var/lib/kubelet/kubeadm-flags.env` sha256 UNCHANGED before/after drill.
        - Pre-drill baseline capture block: sha256sum of `/var/lib/kubelet/kubeadm-flags.env`, ls listing of `/etc/kubernetes/manifests/`, `kubectl -n kube-system get cm coredns -o yaml`.
        - Post-drill check block: diff against each baseline; all must return empty/matching.

    11. **Deferred Items section** — enumerate the 5 entries from frontmatter (WR-01, IN-04, DF-08, Phase 1 UAT, Phase 5 UAT) with one-line notes.

    12. **Gaps Summary** — `No blocking gaps. Phase 6 completes all 3 PACK requirements (PACK-05 + PACK-06 Troubleshooting subset + PACK-07 final 100% coverage). MH-4 is human-gated per standard CONTEXT contract.`
  </action>
  <verify>
    <automated>test -f .planning/phases/06-troubleshooting-pack/06-VERIFICATION.md && for n in 1 2 3 4 5 6 7 8; do grep -qE "MH-$n" .planning/phases/06-troubleshooting-pack/06-VERIFICATION.md || exit 1; done && grep -qE 'status: human_needed' .planning/phases/06-troubleshooting-pack/06-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/06-troubleshooting-pack/06-VERIFICATION.md`
    - Frontmatter has required keys: `grep -qE '^phase: 06$' .planning/phases/06-troubleshooting-pack/06-VERIFICATION.md && grep -qE '^status: human_needed$' .planning/phases/06-troubleshooting-pack/06-VERIFICATION.md && grep -qE '^must_haves_total: 8$' .planning/phases/06-troubleshooting-pack/06-VERIFICATION.md && grep -qE '^must_haves_passed: 7$' .planning/phases/06-troubleshooting-pack/06-VERIFICATION.md`
    - All 8 must-have subsections present: `for n in 1 2 3 4 5 6 7 8; do grep -qE "MH-$n" .planning/phases/06-troubleshooting-pack/06-VERIFICATION.md || exit 1; done`
    - Requirements traceability lists all 3 Phase 6 REQ-IDs: `for r in PACK-05 PACK-06 PACK-07; do grep -q "$r" .planning/phases/06-troubleshooting-pack/06-VERIFICATION.md || exit 1; done`
    - Human Verification section enumerates 6 drills: `grep -c 'cka-sim drill troubleshooting' .planning/phases/06-troubleshooting-pack/06-VERIFICATION.md | awk '$1 >= 3 {exit 0} {exit 1}'`
    - Automated checks reference all 5 lint+test commands: `for s in lint-traps.sh lint-packs.sh lint-coverage.sh lint-deprecated-strings.sh test.sh; do grep -q "$s" .planning/phases/06-troubleshooting-pack/06-VERIFICATION.md || exit 1; done`
    - Anti-Patterns Scan references pass G (new in Phase 6): `grep -q 'pass G' .planning/phases/06-troubleshooting-pack/06-VERIFICATION.md`
    - Behavioral spot-checks list all 6 questions with FAIL→PASS scores: `for q in Q01 Q02 Q03 Q04 Q05 Q06; do grep -q "$q" .planning/phases/06-troubleshooting-pack/06-VERIFICATION.md || exit 1; done`
    - Deferred items carries WR-01 + DF-08 forward: `grep -q 'WR-01' .planning/phases/06-troubleshooting-pack/06-VERIFICATION.md && grep -q 'DF-08' .planning/phases/06-troubleshooting-pack/06-VERIFICATION.md`
    - Gaps Summary declares no-blockers-except-human: `grep -qE '(No blocking gaps|human-gated|human_needed)' .planning/phases/06-troubleshooting-pack/06-VERIFICATION.md`
    - 5 automated lints still exit 0 after VERIFICATION.md authoring: `bash cka-sim/scripts/lint-traps.sh >/dev/null && bash cka-sim/scripts/lint-packs.sh >/dev/null && bash cka-sim/scripts/lint-coverage.sh >/dev/null && bash cka-sim/scripts/lint-deprecated-strings.sh >/dev/null && bash cka-sim/scripts/test.sh >/dev/null`
  </acceptance_criteria>
  <done>06-VERIFICATION.md lands; 7 automated must-haves pre-verified; MH-4 routed to human.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human live-drill checklist + STATE.md advance</name>
  <what-built>
    Seven Phase 6 must-haves are verified programmatically by Tasks 1-2. MH-4 requires the candidate to run 6 live drills against the 1+2 kubeadm cluster. Also covered by this checkpoint: VALIDATION.md's 5 manual-only rows (Q3 kube-system CoreDNS untouched, Q4 debug-pod cleanup, Q5 /etc/kubernetes/manifests/ untouched, Q6 /var/lib/kubelet/ untouched, per-question drill round-trip).
  </what-built>
  <how-to-verify>
    On the control-plane node of the 1+2 kubeadm cluster (Ubuntu 22.04, v1.35):

    Pre-drill baselines (host-safety manual checks per VALIDATION.md):
    ```
    sha256sum /var/lib/kubelet/kubeadm-flags.env > /tmp/q06-baseline.sha
    ls -la /etc/kubernetes/manifests/ > /tmp/q05-manifests-baseline.txt
    kubectl -n kube-system get cm coredns -o yaml > /tmp/q03-coredns-baseline.yaml
    ```

    6-drill loop (TRIP-02 idempotency + per-question round-trip):
    ```
    for i in 01 02 03 04 05 06; do
      echo "=== Drill troubleshooting $i ==="
      cka-sim drill troubleshooting --question "$i" --grade-broken
      cka-sim drill troubleshooting --question "$i" --ref-solution
      cka-sim drill troubleshooting --question "$i" --grade
      cka-sim drill troubleshooting --question "$i" --reset
    done
    cka-sim drill troubleshooting    # random pick
    cka-sim drill troubleshooting    # re-run; confirm idempotent
    ```

    Post-drill checks (host-safety invariants per VALIDATION.md):
    ```
    # Q3 kube-system/coredns unchanged
    diff /tmp/q03-coredns-baseline.yaml <(kubectl -n kube-system get cm coredns -o yaml)
    # Q4 debug pods drained
    kubectl get pods --all-namespaces -l 'kubectl.kubernetes.io/debug-source'
    # Q5 static-pod manifests dir unchanged
    diff /tmp/q05-manifests-baseline.txt <(ls -la /etc/kubernetes/manifests/)
    # Q6 kubelet flags file unchanged
    sha256sum -c /tmp/q06-baseline.sha
    ```

    Expected outcomes for each of the 6 drills:
    - `--grade-broken` (before ref-solution) → exit code 1 + SCORE < MAX + >=1 `Trap:` line
    - `--ref-solution` + `--grade` → exit code 0 + SCORE = MAX + zero or informational `Trap:` lines (Q04 ref-solution's debug-pod is deliberately created; reset.sh sweeps it)
    - `--reset` → exit code 0; lab ns removed (kubectl get ns cka-sim-troubleshooting-0N returns NotFound)

    Any drill failure OR any post-drill host-safety check failure = GAP. Capture the drill invocation, exit code, grader stdout, and feed into `/gsd-plan-phase 06 --gaps`.

    STATE.md advance (after all 6 drills + 4 host-safety checks pass):
    - progress.completed_phases: 5 -> 6
    - Current Position: Phase 06 -> COMPLETE (verified)
    - Accumulated Context > Decisions: append entry summarizing 6-question Troubleshooting pack + 11 new traps + pass G forbidden-command guard
    - Pending Todos: replace "Start Phase 06" with "Start Phase 07: /gsd-discuss-phase 7"
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 drills round-trip green AND all 4 host-safety post-drill checks pass. Otherwise, list the drill question IDs that failed with their grader output; flag any host-safety check that reported a diff.</resume-signal>
  <acceptance_criteria>
    - Candidate confirms 6/6 drills round-trip: grade-broken fails with traps, grade-after-ref-solution passes
    - Post-drill kube-system/coredns ConfigMap diff returns empty (Q3 D-11 host-safety)
    - Post-drill `kubectl get pods -A -l kubectl.kubernetes.io/debug-source` returns empty (Q4 reset sweep)
    - Post-drill `/etc/kubernetes/manifests/` listing diff returns empty (Q5 D-09)
    - Post-drill `/var/lib/kubelet/kubeadm-flags.env` sha256 matches baseline (Q6 D-09)
    - Cluster-wide DNS still works after all drills (smoke: `kubectl run dns-smoke --image=busybox:1.37 --rm -it --restart=Never -- nslookup kubernetes.default.svc.cluster.local` returns Address line)
    - Idempotency confirmed: `cka-sim drill troubleshooting` run twice consecutively completes without `AlreadyExists` errors (TRIP-02)
    - STATE.md updated with Phase 6 verified status AND a new decision-log entry AND Phase 7 pending-todo
  </acceptance_criteria>
  <done>Phase 6 closed: all 6 drills green, no live-host regression, STATE.md advanced.</done>
</task>

</tasks>

<threat_model>
### T-6-23: Coverage regression in other packs while finalizing Troubleshooting
- **Threat:** Editing manifest.yaml or adding coverage.yaml for troubleshooting accidentally breaks `lint-coverage.sh` for the 4 other packs (e.g., misplaced top-level `domain:` key, duplicate slug entry).
- **Impact:** medium — blocks Phase 6 close; creates rework for other packs.
- **Asset:** the 4 Phase 4+5 packs' already-verified coverage.
- **Mitigation:** coverage.yaml is a NEW file (no edit of existing packs). manifest.yaml edit is append-only between sentinel comments (same idiom as services-networking). Acceptance criterion runs `lint-coverage.sh` (no arg = all 5 packs) and verifies all 5 schemas OK.
- **Verification:** `bash cka-sim/scripts/lint-coverage.sh 2>&1 | grep -qE 'coverage lint passed \(5 pack'`.

### T-6-24: VERIFICATION.md falsely reports green while a drill is actually broken
- **Threat:** VERIFICATION.md authoring runs BEFORE the live drills (Task 2 before Task 3). If one of P04-P08's fixture round-trips has a latent bug that only surfaces on live cluster, VERIFICATION.md records 7/8 green prematurely — candidate may gloss over the human checklist.
- **Impact:** medium — contract-breach on claim-to-ship.
- **Asset:** shipped-state accuracy.
- **Mitigation:** MH-4 is explicitly human-gated in the frontmatter (`status: human_needed`, `must_haves_passed: 7` not 8). Checkpoint Task 3 blocks until candidate types `approved`. VALIDATION.md's 5 manual-only rows are baked into the Human Verification section with pre-drill baseline captures + post-drill diff checks.
- **Verification:** frontmatter status is `human_needed`; Task 3 is `type=checkpoint:human-verify gate=blocking`.

### T-6-25: STATE.md advance applied without candidate sign-off
- **Threat:** Automation accidentally flips Phase 6 to complete before the checkpoint resolves.
- **Impact:** low — STATE.md drift; easy to roll back.
- **Asset:** roadmap integrity.
- **Mitigation:** STATE.md update lives INSIDE the checkpoint's `how-to-verify` and acceptance_criteria — it only happens after candidate types `approved`. Executor MUST NOT pre-apply STATE changes before the checkpoint's resume-signal. Tasks 1 and 2 execute autonomously (see autonomous_justification); the gate is specifically Task 3.
- **Verification:** checkpoint gate=blocking; executor reads resume-signal before continuing.
</threat_model>

<verification>
- After Task 1: lint-coverage.sh reports 5 packs schema OK; troubleshooting coverage.yaml maps all 9 tracker slugs
- After Task 2: 06-VERIFICATION.md lands with 7/8 must-haves green + human-gated MH-4
- After Task 3: 6-drill + 4-host-safety-check live verification complete; STATE.md advances; roadmap points at Phase 7
</verification>

<success_criteria>
- PACK-05 satisfied (6 questions covering CoreDNS + kubectl debug node + NetworkPolicy + broken kubelet + static pod + service-endpoints)
- PACK-06 Troubleshooting subset satisfied (all 6 questions meet 8-field metadata schema)
- PACK-07 final 100% coverage satisfied across all 5 packs (storage + workloads-scheduling + services-networking + cluster-architecture + troubleshooting)
- Catalog gains 11 new entries with zero collisions (per P02 delta invariant)
- Pass G forbidden-command guard active on every troubleshooting script
- Every troubleshooting metadata.yaml has >=1 `cka-sim/packs/...` reference (D-05)
- 0 live-host regressions (kube-system/coredns, /etc/kubernetes/manifests/, /var/lib/kubelet/, debug pods all verified clean post-drill)
</success_criteria>

<output>
After candidate signs off on the 6-drill checklist, create `.planning/phases/06-troubleshooting-pack/06-09-SUMMARY.md` covering:
- 8 must-haves verdict table (7 programmatic + 1 human)
- Live-drill per-question pass/fail matrix (populated post-run by candidate)
- Post-drill host-safety invariant results (kube-system diff, debug-pod sweep, manifests listing, kubelet flags sha256)
- Phase 6 closing note: roadmap advances to Phase 7 (Exam Mode + Blueprint Alpha + Reporting)
- Any gaps recorded → feed into `/gsd-plan-phase 06 --gaps` rather than blocking close
- Carried-forward deferred items (WR-01, IN-04, DF-08, Phase 1 UAT, Phase 5 UAT)
</output>
