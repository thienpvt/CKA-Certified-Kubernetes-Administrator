---
phase: 14-question-framing-library-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cka-sim/packs/troubleshooting/02-netpol-dns-egress/question.md
autonomous: true
requirements: [BUG-M07]
must_haves:
  truths:
    - "question.md gains a 'Conventions' section after Tasks/Constraints and before 'Verify yourself' that names the two well-known label selectors the ref-solution depends on: kube-system namespace label kubernetes.io/metadata.name=kube-system and CoreDNS pod label k8s-app=kube-dns."
    - "Tasks, Constraints, opening paragraph, and Verify-yourself sections are preserved verbatim — only the new Conventions block is added."
    - "setup.sh, grade.sh, ref-solution.sh, reset.sh, metadata.yaml are NOT touched in this plan."
  artifacts:
    - path: cka-sim/packs/troubleshooting/02-netpol-dns-egress/question.md
      provides: "Conventions block discloses the kube-system namespace label and the CoreDNS pod label so the candidate has the selector keys/values needed to author the DNS-allow egress rule."
      contains: "kubernetes.io/metadata.name=kube-system"
  key_links:
    - from: cka-sim/packs/troubleshooting/02-netpol-dns-egress/question.md
      to: cka-sim/packs/troubleshooting/02-netpol-dns-egress/ref-solution.sh
      via: ref-solution authors a NetworkPolicy whose DNS egress rule selects namespace by 'kubernetes.io/metadata.name=kube-system' and pods by 'k8s-app=kube-dns'; question.md must surface those exact label keys/values so the candidate can independently author an equivalent rule.
      pattern: 'kubernetes\.io/metadata\.name: kube-system'
---

<objective>
Fix BUG-M07 (troubleshooting/02-netpol-dns-egress) — `ref-solution.sh` constructs a DNS-allow egress rule that selects `kube-system` by the well-known `kubernetes.io/metadata.name=kube-system` namespace label (auto-applied by the `NamespaceDefaultLabelName` admission plugin) and selects CoreDNS pods by the standard `k8s-app=kube-dns` label. `question.md` reveals neither convention, so a candidate following the question literally cannot author the kube-system DNS-allow rule because they do not know which selector keys or values to use.

Per CONTEXT.md locked decision: add a small "Conventions" section to `question.md` that names both label selectors. Place the section AFTER Tasks/Constraints, BEFORE Verify (so it reads as a hint, not a primary instruction). Keep all other sections, constraints, and the opening paragraph verbatim. setup/grade/ref-solution/reset/metadata are unchanged.

Output: a `question.md` that is pedagogically honest — the candidate has the same well-known label conventions in front of them that real CKA exam candidates are expected to either know or look up.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-question-framing-library-fixes/14-CONTEXT.md
@.planning/forensics/report-20260517-091657-full-audit.md
@.planning/REQUIREMENTS.md
@cka-sim/packs/troubleshooting/02-netpol-dns-egress/question.md
@cka-sim/packs/troubleshooting/02-netpol-dns-egress/ref-solution.sh
@cka-sim/packs/troubleshooting/02-netpol-dns-egress/grade.sh
@cka-sim/packs/troubleshooting/02-netpol-dns-egress/metadata.yaml

<interfaces>
From cka-sim/packs/troubleshooting/02-netpol-dns-egress/ref-solution.sh:
- NetworkPolicy `allow-web-to-api` egress includes a DNS rule with `namespaceSelector.matchLabels: { kubernetes.io/metadata.name: kube-system }` and `podSelector.matchLabels: { k8s-app: kube-dns }` — these are the two label selectors the candidate must independently author. The question must surface them.

From cka-sim/packs/troubleshooting/02-netpol-dns-egress/question.md (current 30-line file):
- Section anatomy: title (line 1), domain/time (line 3), opening paragraph (line 5), `## Tasks` (lines 7-11), `## Constraints` (lines 13-17), `## Verify yourself` (lines 19-29). Pattern: H1 title, single H2 sections, single-line bullets prefixed with `- `, Markdown code fences for verify commands.
- Existing constraints already use the lab namespace via `${CKA_SIM_LAB_NS}`; they do not name the kube-system label or the CoreDNS pod label.

The fix is purely additive to question.md. No other file is touched.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Conventions section to question.md after Constraints, before Verify</name>
  <read_first>
    - cka-sim/packs/troubleshooting/02-netpol-dns-egress/question.md (current 30-line file: Tasks at 7-11, Constraints at 13-17, Verify yourself at 19-29)
    - cka-sim/packs/troubleshooting/02-netpol-dns-egress/ref-solution.sh (confirms the two label selectors that the Conventions block must surface)
    - .planning/phases/14-question-framing-library-fixes/14-CONTEXT.md (BUG-M07 fix path)
  </read_first>
  <files>cka-sim/packs/troubleshooting/02-netpol-dns-egress/question.md</files>
  <action>
    Insert a new `## Conventions` section between the existing `## Constraints` block (current line 17 closing constraint) and the `## Verify yourself` heading (current line 19). Do NOT modify any other section.

    EXACT INSERT (between current line 17 and current line 19, replacing the single blank line at line 18 with the new section):

    ```
    ## Conventions

    Cluster labels you can rely on when authoring NetworkPolicy selectors:

    - The `kube-system` namespace carries the well-known label `kubernetes.io/metadata.name=kube-system` (auto-applied by the `NamespaceDefaultLabelName` admission plugin).
    - Cluster DNS pods (CoreDNS) carry the standard label `k8s-app=kube-dns`.
    ```

    Result: the file goes from 30 lines to 38 lines. The new block is bounded by a leading blank line (after the last Constraints bullet) and a trailing blank line (before `## Verify yourself`).

    DO NOT change:
    - Title (`# Troubleshooting: Pod cannot resolve or reach a backend`)
    - Domain/time line
    - Opening paragraph (line 5)
    - `## Tasks` section and its three bullets
    - `## Constraints` section and its three bullets (no new constraint added)
    - `## Verify yourself` section and its two code fences
    - The closing line "Both commands must exit 0."

    LF line endings only. No trailing whitespace on any new line.
  </action>
  <verify>
    <automated>grep -c '^## Conventions$' cka-sim/packs/troubleshooting/02-netpol-dns-egress/question.md</automated>
  </verify>
  <acceptance_criteria>
    - The Conventions heading is present exactly once: `grep -c '^## Conventions$' cka-sim/packs/troubleshooting/02-netpol-dns-egress/question.md` returns 1.
    - The kube-system namespace label is documented: `grep -c 'kubernetes.io/metadata.name=kube-system' cka-sim/packs/troubleshooting/02-netpol-dns-egress/question.md` returns 1.
    - The CoreDNS pod label is documented: `grep -c 'k8s-app=kube-dns' cka-sim/packs/troubleshooting/02-netpol-dns-egress/question.md` returns 1.
    - The NamespaceDefaultLabelName admission plugin is named: `grep -c 'NamespaceDefaultLabelName' cka-sim/packs/troubleshooting/02-netpol-dns-egress/question.md` returns 1.
    - Conventions appears AFTER Constraints and BEFORE Verify yourself (line-order check):
      `awk '/^## /{print NR": "$0}' cka-sim/packs/troubleshooting/02-netpol-dns-egress/question.md` lists the three sections in the order Tasks, Constraints, Conventions, Verify yourself.
    - Title and domain/time line preserved verbatim:
      `grep -c '^# Troubleshooting: Pod cannot resolve or reach a backend$' cka-sim/packs/troubleshooting/02-netpol-dns-egress/question.md` returns 1;
      `grep -c '^\*\*Domain:\*\* Troubleshooting  |  \*\*Estimated time:\*\* 8 minutes$' cka-sim/packs/troubleshooting/02-netpol-dns-egress/question.md` returns 1.
    - Opening paragraph preserved verbatim:
      `grep -c '^A Pod in `\${CKA_SIM_LAB_NS}` from `Deployment web` cannot resolve `kubernetes.default.svc.cluster.local` and cannot reach `Service api-svc` on TCP/8080. Pods are Running and Ready. A `Deployment api` exists behind the Service.$' cka-sim/packs/troubleshooting/02-netpol-dns-egress/question.md` returns 1.
    - Tasks section preserved (3 bullets, exact text):
      `grep -c '^- Inspect namespaced workloads and Policy objects\.$' cka-sim/packs/troubleshooting/02-netpol-dns-egress/question.md` returns 1;
      `grep -c '^- From inside the `web` Pod, reproduce both probe failures\.$' cka-sim/packs/troubleshooting/02-netpol-dns-egress/question.md` returns 1;
      `grep -c '^- Restore both flows\.$' cka-sim/packs/troubleshooting/02-netpol-dns-egress/question.md` returns 1.
    - Constraints section preserved (3 bullets, exact text):
      `grep -c '^- Do not modify Deployments, Pods, or Service\.$' cka-sim/packs/troubleshooting/02-netpol-dns-egress/question.md` returns 1;
      `grep -c '^- Do not delete any Policy\.$' cka-sim/packs/troubleshooting/02-netpol-dns-egress/question.md` returns 1;
      `grep -c '^- Make only lab-ns-scoped changes; do not mutate cluster system namespaces\.$' cka-sim/packs/troubleshooting/02-netpol-dns-egress/question.md` returns 1.
    - Verify-yourself jsonpath/exec lines preserved:
      `grep -c 'nslookup kubernetes.default.svc.cluster.local' cka-sim/packs/troubleshooting/02-netpol-dns-egress/question.md` returns 1;
      `grep -c "echo > /dev/tcp/api-svc/8080" cka-sim/packs/troubleshooting/02-netpol-dns-egress/question.md` returns 1;
      `grep -c '^Both commands must exit 0\.$' cka-sim/packs/troubleshooting/02-netpol-dns-egress/question.md` returns 1.
    - Only question.md was edited under the pack:
      `git diff --name-only -- cka-sim/packs/troubleshooting/02-netpol-dns-egress/ | sort` lists ONLY `cka-sim/packs/troubleshooting/02-netpol-dns-egress/question.md`.
    - Line endings are LF: `file cka-sim/packs/troubleshooting/02-netpol-dns-egress/question.md` does not report CRLF.
  </acceptance_criteria>
  <done>question.md contains a Conventions block disclosing the kube-system namespace label and the CoreDNS pod label; all other sections preserved verbatim; no other file in the pack edited.</done>
</task>

</tasks>

<verification>
After the task completes:

1. `grep -c '^## ' cka-sim/packs/troubleshooting/02-netpol-dns-egress/question.md` returns 4 (Tasks, Constraints, Conventions, Verify yourself).
2. `bash cka-sim/scripts/lint-packs.sh` runs clean — question.md is not validated by the linter for content but the pack as a whole must still pass schema/structural checks.
3. Live-cluster GRADE round-trip is unchanged because grade.sh and ref-solution.sh are untouched. The candidate-facing change is purely informational.
</verification>

<success_criteria>
- A candidate reading question.md alone has the two label selectors required to author the kube-system DNS-allow egress rule equivalent to the ref-solution.
- All non-Conventions sections of question.md are byte-for-byte unchanged.
- No other file in the troubleshooting/02-netpol-dns-egress pack is modified.
</success_criteria>

<output>
After completion, create `.planning/phases/14-question-framing-library-fixes/14-01-SUMMARY.md` documenting:
- File modified: cka-sim/packs/troubleshooting/02-netpol-dns-egress/question.md
- Inserted Conventions section between Constraints and Verify yourself
- Two label conventions surfaced: kubernetes.io/metadata.name=kube-system (kube-system namespace) and k8s-app=kube-dns (CoreDNS pods)
- Confirmed no other pack file edited (git diff scope check)
</output>
