---
phase: 14-question-framing-library-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cka-sim/packs/troubleshooting/03-coredns-resolution/question.md
  - cka-sim/packs/troubleshooting/03-coredns-resolution/metadata.yaml
autonomous: true
requirements: [BUG-M08]
must_haves:
  truths:
    - "question.md acknowledges the unhealthy lab CoreDNS Deployment up front: the candidate is told the Deployment is present but failing to start, and that they must stabilise it AND fix the upstream forwarder before DNS resolution will work for both internal and external names."
    - "Tasks list reflects the two-trap shape: (1) stabilise the lab CoreDNS Deployment, (2) fix its upstream forwarder, (3) verify both internal and external resolution from the q03-dnsclient Pod."
    - "Constraints, Verify-yourself code fences, and the closing 'Both lookups must resolve.' line are preserved verbatim."
    - "metadata.yaml is updated only if its current text mirrors the stale 'other infra running' framing — current file has no description field, so a forensic re-read confirms no metadata edit is required."
    - "setup.sh, grade.sh, ref-solution.sh, reset.sh are NOT touched."
  artifacts:
    - path: cka-sim/packs/troubleshooting/03-coredns-resolution/question.md
      provides: "Lead paragraph names q03-coredns Deployment as failing to start; tasks split into stabilise → fix upstream → verify; matches what setup.sh actually produces (subPath case mismatch + bad upstream forward)."
      contains: "failing to start"
    - path: cka-sim/packs/troubleshooting/03-coredns-resolution/metadata.yaml
      provides: "Verified clean — no description field that mirrors the stale 'other infra running' framing exists, so no edit applied. SUMMARY records the verification."
      contains: "troubleshooting-coredns-resolution"
  key_links:
    - from: cka-sim/packs/troubleshooting/03-coredns-resolution/question.md
      to: cka-sim/packs/troubleshooting/03-coredns-resolution/setup.sh
      via: question.md must acknowledge that setup.sh creates a q03-coredns Deployment with subPath case mismatch (Pod CrashLoopBackOff) AND bad upstream forwarder; the original 'other lab namespace infrastructure is running' line lied about the first trap.
      pattern: 'q03-coredns'
    - from: cka-sim/packs/troubleshooting/03-coredns-resolution/question.md
      to: cka-sim/packs/troubleshooting/03-coredns-resolution/grade.sh
      via: grade.sh validates DNS resolution end-to-end (both internal and external); question.md's Verify-yourself nslookup commands must remain identical to the ones the grader effectively reproduces.
      pattern: 'nslookup (kubernetes\.default\.svc\.cluster\.local|www\.example\.com)'
---

<objective>
Fix BUG-M08 (troubleshooting/03-coredns-resolution) — `setup.sh` creates a `q03-coredns` Deployment whose Pod CrashLoopBackOff is one of two intentional traps (the other being a bad upstream forward in the Corefile). The Pod fails because the volumeMount uses `subPath: corefile` (lowercase 'c') against a ConfigMap key `Corefile` (uppercase 'C'). The current `question.md` lead paragraph says "Other lab namespace infrastructure is running" — implying CoreDNS is healthy and the only issue is bad upstream forward. A candidate reading the question literally is steered away from the first trap.

Per CONTEXT.md locked decision: rewrite the lead paragraph to acknowledge the unhealthy lab CoreDNS Deployment, split the Tasks list into the two-trap shape (stabilise → fix upstream → verify), keep Constraints and Verify-yourself code fences verbatim. Re-read `metadata.yaml`; current snapshot has no description that mirrors the stale framing, so no metadata edit is required (record the verification in the SUMMARY).

Output: a `question.md` whose lead matches what `setup.sh` actually produces, so the candidate is clued in to BOTH traps (Pod-not-Running AND DNS-fails) instead of just one (DNS-fails).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-question-framing-library-fixes/14-CONTEXT.md
@.planning/forensics/report-20260517-091657-full-audit.md
@.planning/REQUIREMENTS.md
@cka-sim/packs/troubleshooting/03-coredns-resolution/question.md
@cka-sim/packs/troubleshooting/03-coredns-resolution/setup.sh
@cka-sim/packs/troubleshooting/03-coredns-resolution/grade.sh
@cka-sim/packs/troubleshooting/03-coredns-resolution/ref-solution.sh
@cka-sim/packs/troubleshooting/03-coredns-resolution/metadata.yaml

<interfaces>
From cka-sim/packs/troubleshooting/03-coredns-resolution/setup.sh:
- Creates a Deployment named `q03-coredns` in `${CKA_SIM_LAB_NS}` whose Pod fails due to subPath case mismatch — Pod is in CrashLoopBackOff. Also creates a ConfigMap whose Corefile has a bad upstream `forward` directive. Both traps must be repaired by the candidate.
- Creates a debug Pod named `q03-dnsclient` in the same namespace, configured to use the lab CoreDNS Service for resolution.

From cka-sim/packs/troubleshooting/03-coredns-resolution/grade.sh:
- Validates DNS resolution end-to-end via nslookup of `kubernetes.default.svc.cluster.local` AND `www.example.com` from the q03-dnsclient Pod. Both must succeed for full credit.

From cka-sim/packs/troubleshooting/03-coredns-resolution/question.md (current 28-line file):
- Title (line 1), domain/time (line 3), opening paragraph (line 5), `## Tasks` (lines 7-10) with 3 numbered items, `## Constraints` (lines 12-15) with 3 bullets, `## Verify yourself` (lines 17-27) with 2 code fences.
- Opening paragraph contains the misleading sentence "Other lab namespace infrastructure is running."
- The Tasks list is currently 3-numbered but its items are framed around a single-trap mental model.

From cka-sim/packs/troubleshooting/03-coredns-resolution/metadata.yaml (current 16-line file):
- Has `id`, `domain`, `estimatedMinutes`, `verified_against`, `traps:` (3 ids), and `references:` (2). No `description` or `summary` field. No edit is required because there is nothing to soften.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite question.md lead paragraph and Tasks list</name>
  <read_first>
    - cka-sim/packs/troubleshooting/03-coredns-resolution/question.md (current 28-line file — opening paragraph at line 5; 3-item Tasks at lines 7-10; Constraints at 12-15; Verify yourself at 17-27)
    - cka-sim/packs/troubleshooting/03-coredns-resolution/setup.sh (confirms two traps: subPath case mismatch on q03-coredns Deployment + bad upstream forward in Corefile)
    - .planning/phases/14-question-framing-library-fixes/14-CONTEXT.md (BUG-M08 fix path)
  </read_first>
  <files>cka-sim/packs/troubleshooting/03-coredns-resolution/question.md</files>
  <action>
    Two surgical edits to cka-sim/packs/troubleshooting/03-coredns-resolution/question.md. Do NOT rewrite the whole file.

    **Edit 1: Replace the opening paragraph (current line 5) with two-sentence framing that acknowledges both traps.**

    BEFORE (line 5):
    ```
    A debug Pod named `q03-dnsclient` in `${CKA_SIM_LAB_NS}` cannot resolve external names such as `www.example.com`. Cluster-internal names such as `kubernetes.default.svc.cluster.local` also fail. Other lab namespace infrastructure is running.
    ```

    AFTER (single block at line 5):
    ```
    A debug Pod named `q03-dnsclient` in `${CKA_SIM_LAB_NS}` cannot resolve external names such as `www.example.com`. Cluster-internal names such as `kubernetes.default.svc.cluster.local` also fail. A lab CoreDNS Deployment named `q03-coredns` is present in the same namespace but is failing to start; once you stabilise it, you must also fix its upstream forwarder so that DNS resolution works for both cluster-internal and external names.
    ```

    The "Other lab namespace infrastructure is running" sentence is REMOVED entirely. The new framing replaces it with concrete acknowledgement of (a) the failing q03-coredns Deployment and (b) the upstream forwarder fix.

    **Edit 2: Rewrite the Tasks list to map onto the two-trap shape.**

    BEFORE (lines 7-10):
    ```
    ## Tasks
    1. Inspect namespaced workloads and supporting resources in `${CKA_SIM_LAB_NS}`.
    2. Reproduce the DNS failure from inside `q03-dnsclient`.
    3. Restore name resolution for both external and cluster-internal names.
    ```

    AFTER:
    ```
    ## Tasks
    1. Stabilise the `q03-coredns` Deployment so its Pod reaches Ready.
    2. Fix the lab CoreDNS upstream forwarder so cluster-internal and external names resolve.
    3. Verify both `kubernetes.default.svc.cluster.local` and `www.example.com` resolve from inside `q03-dnsclient`.
    ```

    DO NOT change:
    - Title (`# Troubleshooting: Pods cannot resolve DNS names`)
    - Domain/time line (`**Domain:** Troubleshooting  |  **Estimated time:** 8 minutes`)
    - The blank lines around `## Tasks`
    - The `## Constraints` section and its three bullets
    - The `## Verify yourself` heading and its two code fences
    - The closing line `Both lookups must resolve.`

    LF line endings only. No trailing whitespace.
  </action>
  <verify>
    <automated>grep -c 'failing to start' cka-sim/packs/troubleshooting/03-coredns-resolution/question.md</automated>
  </verify>
  <acceptance_criteria>
    - The misleading sentence is gone: `grep -c 'Other lab namespace infrastructure is running' cka-sim/packs/troubleshooting/03-coredns-resolution/question.md` returns 0.
    - The new framing is present exactly once: `grep -c 'failing to start' cka-sim/packs/troubleshooting/03-coredns-resolution/question.md` returns 1.
    - The q03-coredns Deployment is named in the lead paragraph: `grep -c 'q03-coredns' cka-sim/packs/troubleshooting/03-coredns-resolution/question.md` returns >= 2 (lead paragraph + Tasks #1).
    - "stabilise it" framing for the upstream forwarder is present: `grep -c 'upstream forwarder' cka-sim/packs/troubleshooting/03-coredns-resolution/question.md` returns >= 1.
    - The Tasks list maps to the two-trap shape:
      `grep -c '^1\. Stabilise the `q03-coredns` Deployment so its Pod reaches Ready\.$' cka-sim/packs/troubleshooting/03-coredns-resolution/question.md` returns 1;
      `grep -c '^2\. Fix the lab CoreDNS upstream forwarder so cluster-internal and external names resolve\.$' cka-sim/packs/troubleshooting/03-coredns-resolution/question.md` returns 1;
      `grep -c '^3\. Verify both `kubernetes\.default\.svc\.cluster\.local` and `www\.example\.com` resolve from inside `q03-dnsclient`\.$' cka-sim/packs/troubleshooting/03-coredns-resolution/question.md` returns 1.
    - Title and domain/time line preserved verbatim:
      `grep -c '^# Troubleshooting: Pods cannot resolve DNS names$' cka-sim/packs/troubleshooting/03-coredns-resolution/question.md` returns 1;
      `grep -c '^\*\*Domain:\*\* Troubleshooting  |  \*\*Estimated time:\*\* 8 minutes$' cka-sim/packs/troubleshooting/03-coredns-resolution/question.md` returns 1.
    - Constraints section preserved verbatim (3 bullets):
      `grep -c '^- Make lab-namespace-scoped changes only\. Do not mutate shared cluster DNS resources\.$' cka-sim/packs/troubleshooting/03-coredns-resolution/question.md` returns 1;
      `grep -c "^- Do NOT modify the `q03-dnsclient` Pod's DNS settings\.$" cka-sim/packs/troubleshooting/03-coredns-resolution/question.md` returns 1;
      `grep -c '^- Adjust supporting resources instead\.$' cka-sim/packs/troubleshooting/03-coredns-resolution/question.md` returns 1.
    - Verify-yourself section preserved (heading + 2 nslookup fences + closing line):
      `grep -c '^## Verify yourself$' cka-sim/packs/troubleshooting/03-coredns-resolution/question.md` returns 1;
      `grep -c 'nslookup kubernetes.default.svc.cluster.local' cka-sim/packs/troubleshooting/03-coredns-resolution/question.md` returns 1;
      `grep -c 'nslookup www.example.com' cka-sim/packs/troubleshooting/03-coredns-resolution/question.md` returns 1;
      `grep -c '^Both lookups must resolve\.$' cka-sim/packs/troubleshooting/03-coredns-resolution/question.md` returns 1.
    - Section ordering check: Tasks → Constraints → Verify yourself.
      `awk '/^## /{print NR": "$0}' cka-sim/packs/troubleshooting/03-coredns-resolution/question.md` lists exactly those three headings in that order.
  </acceptance_criteria>
  <done>question.md lead paragraph and Tasks list map onto the two traps that setup.sh actually plants; Constraints and Verify-yourself sections preserved verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Verify metadata.yaml has no stale 'other infra running' framing — no edit required</name>
  <read_first>
    - cka-sim/packs/troubleshooting/03-coredns-resolution/metadata.yaml (current 16-line file: id, domain, estimatedMinutes, verified_against, traps list of 3, references with 2 entries; no description/summary field)
    - .planning/phases/14-question-framing-library-fixes/14-CONTEXT.md (BUG-M08 fix path step 6: "metadata.yaml adjust description if it mirrors stale framing")
  </read_first>
  <files>(read-only verification — produces no edits unless stale framing is found)</files>
  <action>
    Re-read cka-sim/packs/troubleshooting/03-coredns-resolution/metadata.yaml. Confirm the file has no `description:` or `summary:` field whose text mirrors "Other lab namespace infrastructure is running" or any equivalent stale framing.

    Expected greps (all return 0):

    1. No description/summary field exists: `grep -cE '^(description|summary):' cka-sim/packs/troubleshooting/03-coredns-resolution/metadata.yaml` returns 0.
    2. No stale framing in any note field: `grep -c 'Other lab namespace' cka-sim/packs/troubleshooting/03-coredns-resolution/metadata.yaml` returns 0.
    3. No stale framing about "infrastructure is running": `grep -c 'infrastructure is running' cka-sim/packs/troubleshooting/03-coredns-resolution/metadata.yaml` returns 0.

    If all three return 0 → no edit required. Record the verification result in the SUMMARY.

    If any return non-zero → STOP and surface to the user; the metadata edit is then in scope and the planner needs to extend the plan with a soften-the-note edit.

    No file is modified by this task in the expected case.
  </action>
  <verify>
    <automated>grep -cE '^(description|summary):' cka-sim/packs/troubleshooting/03-coredns-resolution/metadata.yaml</automated>
  </verify>
  <acceptance_criteria>
    - No description/summary field: `grep -cE '^(description|summary):' cka-sim/packs/troubleshooting/03-coredns-resolution/metadata.yaml` returns 0.
    - No "Other lab namespace" string: `grep -c 'Other lab namespace' cka-sim/packs/troubleshooting/03-coredns-resolution/metadata.yaml` returns 0.
    - No "infrastructure is running" string: `grep -c 'infrastructure is running' cka-sim/packs/troubleshooting/03-coredns-resolution/metadata.yaml` returns 0.
    - The trap list preserved (3 ids): `grep -c '^  - coredns-forward-to-invalid-upstream$' cka-sim/packs/troubleshooting/03-coredns-resolution/metadata.yaml` returns 1; `grep -c '^  - coredns-sandbox-configmap-mount$' cka-sim/packs/troubleshooting/03-coredns-resolution/metadata.yaml` returns 1; `grep -c '^  - missing-dns-egress$' cka-sim/packs/troubleshooting/03-coredns-resolution/metadata.yaml` returns 1.
    - The id/domain/estimatedMinutes/verified_against fields preserved:
      `grep -c '^id: troubleshooting-coredns-resolution$' cka-sim/packs/troubleshooting/03-coredns-resolution/metadata.yaml` returns 1;
      `grep -c '^domain: troubleshooting$' cka-sim/packs/troubleshooting/03-coredns-resolution/metadata.yaml` returns 1;
      `grep -c '^estimatedMinutes: 8$' cka-sim/packs/troubleshooting/03-coredns-resolution/metadata.yaml` returns 1;
      `grep -c '^verified_against: "1.35"$' cka-sim/packs/troubleshooting/03-coredns-resolution/metadata.yaml` returns 1.
    - metadata.yaml is unchanged after this task: `git diff --name-only -- cka-sim/packs/troubleshooting/03-coredns-resolution/metadata.yaml` is empty.
  </acceptance_criteria>
  <done>Confirmed metadata.yaml has no description/summary mirroring the stale framing; no edit applied; verification recorded for the SUMMARY.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `grep -c '^## ' cka-sim/packs/troubleshooting/03-coredns-resolution/question.md` returns 3 (Tasks, Constraints, Verify yourself).
2. Only question.md is staged for change in this pack: `git diff --name-only -- cka-sim/packs/troubleshooting/03-coredns-resolution/ | sort` lists ONLY `cka-sim/packs/troubleshooting/03-coredns-resolution/question.md`.
3. `bash cka-sim/scripts/lint-packs.sh` runs clean.
4. Live-cluster GRADE round-trip is unchanged because grade.sh, setup.sh, ref-solution.sh, reset.sh are untouched. Both traps are still planted; the question text now matches.
</verification>

<success_criteria>
- A candidate reading question.md alone is told the q03-coredns Deployment is failing to start AND that the upstream forwarder must be fixed — both traps are surfaced.
- Tasks list explicitly enumerates "stabilise → fix upstream → verify".
- Title, domain/time, Constraints, and Verify-yourself sections are byte-for-byte unchanged.
- metadata.yaml is unchanged (verified, no edit required).
- No setup.sh, grade.sh, ref-solution.sh, or reset.sh changes.
</success_criteria>

<output>
After completion, create `.planning/phases/14-question-framing-library-fixes/14-02-SUMMARY.md` documenting:
- File modified: cka-sim/packs/troubleshooting/03-coredns-resolution/question.md (lead paragraph + Tasks list)
- File verified-only: cka-sim/packs/troubleshooting/03-coredns-resolution/metadata.yaml (no description/summary mirroring stale framing; no edit applied)
- Two-trap acknowledgement: lead names q03-coredns Deployment failing to start; Tasks split stabilise → fix upstream → verify
- Confirmed setup/grade/ref-solution/reset untouched (git diff scope check)
</output>
