---
phase: 14-question-framing-library-fixes
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [LIB-01]
must_haves:
  truths:
    - "cka-sim/lib/setup.sh:218 is verified to contain the forward-slash form 'kubernetes.io/metadata.name: kube-system' — no edit applied if verification holds."
    - "Repo-wide grep for backslash-form 'kubernetes.io\\\\metadata.name' returns zero hits — no other latent occurrence exists."
    - "shellcheck on cka-sim/lib/setup.sh runs clean (no new findings) AND bash cka-sim/scripts/lint-packs.sh runs clean."
    - "If somehow the backslash variant is found at line 218 (defensive path), apply the trivial 1-char fix and re-verify; otherwise close LIB-01 with a verification-only SUMMARY note."
  artifacts:
    - path: cka-sim/lib/setup.sh
      provides: "Verified: the seed_netpol_skeleton helper at line 218 already uses forward-slash kubernetes.io/metadata.name as required; LIB-01 closes verification-only."
      contains: "kubernetes.io/metadata.name: kube-system"
  key_links:
    - from: cka-sim/lib/setup.sh
      to: cka-sim/packs/**/setup.sh
      via: many pack setups depend on cka_sim::setup::seed_netpol_skeleton — a typo in line 218 would break the auto-DNS-egress allow rule that prevents the missing-dns-egress trap; verification confirms the helper still emits a valid namespaceSelector matchLabels expression.
      pattern: 'cka_sim::setup::seed_netpol_skeleton'
---

<objective>
Close LIB-01 (cka-sim/lib/setup.sh:218) — the forensic report dated 09:16Z cited a backslash typo `kubernetes.io\metadata.name` inside the `seed_netpol_skeleton` helper. The current snapshot (09:51Z and later) shows the forward-slash form `kubernetes.io/metadata.name: kube-system`, and a repo-wide grep for backslash variants returns zero. Per CONTEXT.md locked decision: LIB-01 is verification-only — re-read line 218, run `shellcheck cka-sim/lib/setup.sh` and `bash cka-sim/scripts/lint-packs.sh`, and if both pass close the requirement with a SUMMARY note. If somehow the backslash form is found, apply the 1-char fix and re-verify (defensive path).

Output: a verification record that confirms LIB-01 is already-fixed and locks down the closure with the exact greps and tool runs that prove it. No code change in the expected case.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-question-framing-library-fixes/14-CONTEXT.md
@.planning/forensics/report-20260517-091657-full-audit.md
@.planning/REQUIREMENTS.md
@cka-sim/lib/setup.sh
@cka-sim/scripts/lint-packs.sh

<interfaces>
From cka-sim/lib/setup.sh (current snapshot — line 218):
- Inside `cka_sim::setup::seed_netpol_skeleton`, the heredoc'd NetworkPolicy template emits a namespaceSelector matchLabels block. Line 218 reads `              kubernetes.io/metadata.name: kube-system` (forward-slash form).
- Many pack setup.sh scripts call this helper to seed a default-deny + DNS-allow NetworkPolicy. A backslash typo at line 218 would render the namespaceSelector inert, and the missing-dns-egress trap would fire even when the candidate did the right thing — but the current snapshot has the correct form.

From cka-sim/scripts/lint-packs.sh:
- The repo's pack linter; mentioned in CONTEXT.md as one of the two verification gates for this requirement. It exists (`test -x` returns true).

shellcheck availability:
- shellcheck may or may not be installed. Verify with `command -v shellcheck` before running. If unavailable, record "shellcheck not in environment" in the SUMMARY but do not fail the plan — the lint-packs.sh run is the authoritative gate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-read cka-sim/lib/setup.sh:218 and confirm forward-slash form</name>
  <read_first>
    - cka-sim/lib/setup.sh (lines 200-225 — the seed_netpol_skeleton helper body, including the heredoc that emits the namespaceSelector block)
    - .planning/phases/14-question-framing-library-fixes/14-CONTEXT.md (LIB-01 fix path)
  </read_first>
  <files>(read-only verification — produces no edits unless backslash form is found)</files>
  <action>
    Read cka-sim/lib/setup.sh line 218. Confirm it matches the literal forward-slash form `              kubernetes.io/metadata.name: kube-system` (12 leading spaces, then the key-value).

    Run two greps and capture exact counts:

    1. Forward-slash present at line 218: `awk 'NR==218{print}' cka-sim/lib/setup.sh` outputs the expected line (used as evidence in the SUMMARY).
    2. Backslash variant absent repo-wide: `grep -rn 'kubernetes\\.io\\\\metadata\\.name' cka-sim/` returns zero matches.

    Expected outcome: forward-slash confirmed, zero backslash hits. Record both grep outputs verbatim in the SUMMARY artifact.

    DEFENSIVE PATH (only if backslash form is found):
    - If the forward-slash check fails OR the backslash grep returns ≥1 hit at cka-sim/lib/setup.sh:218 — apply the 1-character fix replacing the backslash with a forward slash. Use the Edit tool with the exact line as old_string. Re-run both greps to confirm the fix.
    - Document the defensive edit in the SUMMARY and add `cka-sim/lib/setup.sh` to the files_modified list at the top of the plan (planner-time correction).

    NORMAL PATH (forward-slash already correct): no file edit. Proceed to Task 2.
  </action>
  <verify>
    <automated>awk 'NR==218 && /kubernetes.io\/metadata.name: kube-system/{print "OK"}' cka-sim/lib/setup.sh | grep -c '^OK$'</automated>
  </verify>
  <acceptance_criteria>
    - Line 218 contains the forward-slash form: `awk 'NR==218 && /kubernetes.io\/metadata.name: kube-system/{print "OK"}' cka-sim/lib/setup.sh | grep -c '^OK$'` returns 1.
    - Backslash variant absent repo-wide: `grep -rn 'kubernetes\\.io\\\\metadata\\.name' cka-sim/` returns 0 matches (no output, exit 1 from grep is fine; Task should treat empty as success).
    - The seed_netpol_skeleton function is intact and contains the line: `grep -c 'cka_sim::setup::seed_netpol_skeleton' cka-sim/lib/setup.sh` returns ≥ 2 (definition + usage references; definition is at the function declaration line, usages may exist in pack setup.sh files but the helper file is just one definition).

      Note: relax the count to ≥ 1 if the function name appears only once in the library file itself; the library only DEFINES the function, callers live in pack setup.sh files. Use `grep -c '^cka_sim::setup::seed_netpol_skeleton()' cka-sim/lib/setup.sh` returns 1 instead.
    - cka-sim/lib/setup.sh is unmodified by this task in the normal path: `git diff --name-only -- cka-sim/lib/setup.sh` is empty.
    - If defensive path triggered: edit is exactly one character (single `\` → `/`); the line `awk 'NR==218 && /kubernetes.io\/metadata.name: kube-system/{print "OK"}' cka-sim/lib/setup.sh | grep -c '^OK$'` returns 1 after the fix.
  </acceptance_criteria>
  <done>Verified line 218 holds the forward-slash form OR applied the trivial 1-char defensive fix; backslash form is absent repo-wide.</done>
</task>

<task type="auto">
  <name>Task 2: Run shellcheck (if available) and lint-packs.sh — confirm both clean</name>
  <read_first>
    - cka-sim/lib/setup.sh (read-only; the file under check)
    - cka-sim/scripts/lint-packs.sh (the linter entry point)
  </read_first>
  <files>(read-only — runs verification tools, produces no edits)</files>
  <action>
    Run two commands in sequence:

    1. shellcheck on the library file: `command -v shellcheck >/dev/null 2>&1 && shellcheck cka-sim/lib/setup.sh; echo "shellcheck-exit=$?"`. Capture exit code and output.
       - Exit 0 → clean. Record in SUMMARY.
       - Non-zero with `command -v` failing → shellcheck not installed; record "shellcheck not in environment" and proceed.
       - Non-zero with shellcheck-reported findings → STOP, surface findings to the user; LIB-01 cannot close until resolved.

    2. Pack linter: `bash cka-sim/scripts/lint-packs.sh; echo "lint-packs-exit=$?"`. Capture exit code and output.
       - Exit 0 → clean. Record in SUMMARY.
       - Non-zero → STOP and surface findings to the user; LIB-01 cannot close until resolved.

    The two runs are gates; neither modifies files. The SUMMARY captures the exit codes verbatim as evidence.

    DO NOT silence stderr or `|| true` past either gate. Both must pass cleanly for closure.
  </action>
  <verify>
    <automated>bash cka-sim/scripts/lint-packs.sh > /tmp/lint-packs.out 2>&1; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `bash cka-sim/scripts/lint-packs.sh` exits 0 (clean lint).
    - `shellcheck cka-sim/lib/setup.sh` exits 0 if shellcheck is on PATH; otherwise the SUMMARY records "shellcheck not in environment" and the failure is informational rather than blocking.
    - No file under `cka-sim/` is modified by Task 2: `git diff --name-only -- cka-sim/` lists nothing new beyond what Task 1 produced (which in the normal path is empty).
    - Both tool outputs (exit codes + relevant first-failure lines if any) are captured for the SUMMARY artifact.
  </acceptance_criteria>
  <done>Both verification gates run clean; LIB-01 can close.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. cka-sim/lib/setup.sh:218 confirmed forward-slash form (or defensively repaired).
2. Repo-wide grep for backslash variant returns zero.
3. `shellcheck cka-sim/lib/setup.sh` clean (when available).
4. `bash cka-sim/scripts/lint-packs.sh` clean.
5. The Phase 14 commit footprint contains no LIB-01 code change in the normal path; LIB-01 closes with a verification-only SUMMARY note. The audit trail in `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` should mark LIB-01 closed verified-already-fixed.
</verification>

<success_criteria>
- LIB-01 closes with documented evidence: line 218 forward-slash, zero backslash hits, lint-packs clean (and shellcheck clean if available).
- No code edit in the expected case; if defensive edit applied it is exactly one character.
- No collateral edits in cka-sim/lib/setup.sh or cka-sim/scripts/lint-packs.sh outside the (unlikely) defensive path.
</success_criteria>

<output>
After completion, create `.planning/phases/14-question-framing-library-fixes/14-04-SUMMARY.md` documenting:
- LIB-01 verification result: forward-slash form confirmed at cka-sim/lib/setup.sh:218
- Greps captured: line 218 awk output, repo-wide backslash-variant grep (zero hits)
- Tool gates: shellcheck exit code (or "not in environment"), lint-packs.sh exit code
- Whether normal path (no edit) or defensive path (1-char fix) was taken
- Closure recommendation for LIB-01: "verified pre-existing fix; line 218 correct, repo-wide grep clean, lint gates clean"
</output>
