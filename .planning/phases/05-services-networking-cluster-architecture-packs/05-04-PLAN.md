---
phase: 05-services-networking-cluster-architecture-packs
plan: 04
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - cka-sim/packs/services-networking/03-coredns-resolution/setup.sh
  - cka-sim/packs/services-networking/03-coredns-resolution/grade.sh
  - cka-sim/packs/services-networking/03-coredns-resolution/reset.sh
  - cka-sim/packs/services-networking/03-coredns-resolution/ref-solution.sh
  - cka-sim/packs/services-networking/03-coredns-resolution/metadata.yaml
  - cka-sim/packs/services-networking/03-coredns-resolution/question.md
  - cka-sim/packs/services-networking/manifest.yaml
  - cka-sim/packs/services-networking/coverage.yaml
  - cka-sim/packs/services-networking/README.md
  - cka-sim/tests/fixtures/phase-05/services-networking/03-coredns-resolution/stub-responses.json
  - cka-sim/tests/fixtures/phase-05/services-networking/03-coredns-resolution/expected-fail-score.txt
  - cka-sim/tests/fixtures/phase-05/services-networking/03-coredns-resolution/expected-pass-score.txt
autonomous: true
requirements: [PACK-03, PACK-06, PACK-07]
tags: [services-networking, coredns, dns]
must_haves:
  truths:
    - "setup seeds Pod q03-dnsclient in lab ns with dnsPolicy: None + dnsConfig.nameservers: [1.1.1.1] (intentionally wrong — not cluster DNS)"
    - "setup NEVER mutates kube-system/coredns ConfigMap (read-only per 05-RESEARCH §12)"
    - "grade asserts kubectl exec q03-dnsclient -- nslookup kubernetes.default.svc.cluster.local exits 0"
    - "grade records trap coredns-forward-to-invalid-upstream when nslookup fails OR dnsConfig.nameservers[0] is not the cluster DNS service IP"
    - "ref-solution kubectl deletes + recreates the Pod with dnsConfig.nameservers pointing at the cluster DNS service IP (read from kube-system/kube-dns Service)"
    - "metadata.yaml traps = [coredns-forward-to-invalid-upstream, missing-dns-egress, default-sa-used]"
  artifacts:
    - path: "cka-sim/packs/services-networking/03-coredns-resolution/grade.sh"
      provides: "nslookup behavioural probe + trap recording"
      contains: "kubectl exec"
      contains_extra: "coredns-forward-to-invalid-upstream"
  key_links:
    - from: "cka-sim/packs/services-networking/03-coredns-resolution/setup.sh"
      to: "cka-sim/lib/setup.sh"
      via: "source + ensure_lab_ns + wait_for_ns_active"
      pattern: "cka_sim::setup::wait_for_ns_active"
---

<objective>
Services & Networking Q03 — CoreDNS resolution via Pod-level dnsConfig override (RESEARCH §12). 7 minutes. Trap: `coredns-forward-to-invalid-upstream`.

Per RESEARCH §12 and 05-CONTEXT.md line 24: Phase 5 Q03 does NOT edit live kube-system/coredns. Instead, the drill uses a Pod-level dnsConfig override — seed Pod `q03-dnsclient` with `dnsPolicy: None` and `dnsConfig.nameservers: [1.1.1.1]`. Candidate discovers the cluster DNS Service IP and corrects the Pod's dnsConfig.

Output: 6 question files + 3 fixtures + 3 sentinel appends.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-services-networking-cluster-architecture-packs/05-RESEARCH.md
@cka-sim/lib/setup.sh
@cka-sim/lib/grade.sh
@cka-sim/traps/catalog.yaml
</context>

<interfaces>
Lab ns: `cka-sim-services-networking-03`. Resource prefix: `q03-`. Question id: `services-coredns-resolution`.

Cluster DNS Service: `kubectl get svc kube-dns -n kube-system -o jsonpath='{.spec.clusterIP}'` — this is the correct nameserver the candidate must use.

RESEARCH §14 landmine: `dnsPolicy: None` requires `dnsConfig.nameservers` non-empty. An empty list fails admission. Setup seeds non-empty but wrong nameserver.

Pod image: `busybox:1.37` (RESEARCH §12 line 427) — nslookup available without TLS baggage.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 6 question files + 3 fixtures + sentinel appends</name>
  <files>cka-sim/packs/services-networking/03-coredns-resolution/metadata.yaml, cka-sim/packs/services-networking/03-coredns-resolution/question.md, cka-sim/packs/services-networking/03-coredns-resolution/setup.sh, cka-sim/packs/services-networking/03-coredns-resolution/grade.sh, cka-sim/packs/services-networking/03-coredns-resolution/reset.sh, cka-sim/packs/services-networking/03-coredns-resolution/ref-solution.sh, cka-sim/tests/fixtures/phase-05/services-networking/03-coredns-resolution/stub-responses.json, cka-sim/tests/fixtures/phase-05/services-networking/03-coredns-resolution/expected-fail-score.txt, cka-sim/tests/fixtures/phase-05/services-networking/03-coredns-resolution/expected-pass-score.txt, cka-sim/packs/services-networking/manifest.yaml, cka-sim/packs/services-networking/coverage.yaml, cka-sim/packs/services-networking/README.md</files>
  <read_first>
    - .planning/phases/05-services-networking-cluster-architecture-packs/05-RESEARCH.md (§12 lines 387-438 — dnsConfig Pod YAML + grader probe)
    - cka-sim/lib/setup.sh (ensure_lab_ns + wait_for_ns_active)
    - cka-sim/lib/grade.sh (assert_resource_exists + record_trap + emit_result)
    - cka-sim/traps/catalog.yaml (new entry coredns-forward-to-invalid-upstream added in P01)
  </read_first>
  <action>
    **setup.sh**: source lib/setup.sh. ensure_lab_ns + wait_for_ns_active. Apply Pod `q03-dnsclient` verbatim from RESEARCH §12 lines 416-430: `dnsPolicy: None`, `dnsConfig.nameservers: [1.1.1.1]`, container `busybox:1.37` with `command: ["sleep", "3600"]`. Wait up to 60s for Pod Ready (ignore-not-found).

    **grade.sh**: `set -uo pipefail`; source lib/grade.sh + lib/traps.sh. Assertions:
    - `cka_sim::grade::assert_resource_exists pod q03-dnsclient -n $ns`
    - `cka_sim::grade::assert_field_eq pod q03-dnsclient '{.spec.dnsPolicy}' 'None' -n $ns`
    - Behavioural: capture `kubectl exec -n $ns q03-dnsclient -- nslookup kubernetes.default.svc.cluster.local 2>&1`; if exit=0 AND output contains `Address`, increment PASSED + TOTAL; else increment TOTAL only AND `cka_sim::grade::record_trap coredns-forward-to-invalid-upstream`.
    - `cka_sim::grade::emit_result`

    **ref-solution.sh**: `set -euo pipefail`. Discover cluster DNS via `dns_ip=$(kubectl get svc kube-dns -n kube-system -o jsonpath='{.spec.clusterIP}')`. Die if empty. Delete existing pod `kubectl delete pod q03-dnsclient -n $ns --wait=true --ignore-not-found`. Recreate with `kubectl apply -f -` heredoc: same Pod but `dnsConfig.nameservers: ["$dns_ip"]`. Wait `kubectl wait --for=condition=Ready pod/q03-dnsclient --timeout=60s`.

    **reset.sh**: delete namespace, wait=false.

    **metadata.yaml**: `id: services-coredns-resolution`, `domain: services-networking`, `estimatedMinutes: 7`, `verified_against: "1.35"`, traps: `[coredns-forward-to-invalid-upstream, missing-dns-egress, default-sa-used]`, references: k8s-doc `https://kubernetes.io/docs/concepts/services-networking/dns-pod-service/` + k8s-doc `https://kubernetes.io/docs/tasks/administer-cluster/dns-custom-nameservers/`.

    **question.md**: Pod `q03-dnsclient` is Running but `nslookup kubernetes.default.svc.cluster.local` times out. Task: fix the Pod's DNS so nslookup resolves the cluster kubernetes.default service. Constraints: do NOT edit kube-system/coredns ConfigMap (cluster-scoped); do NOT change dnsPolicy from None; only the Pod's dnsConfig.nameservers may be edited. Hint: kube-system has a Service named kube-dns — read its ClusterIP.

    **Fixtures**:
    - `stub-responses.json`: `{"kind":"Pod","metadata":{"name":"q03-dnsclient"},"spec":{"dnsPolicy":"None","dnsConfig":{"nameservers":["10.96.0.10"]}}}` and kube-dns service `{"spec":{"clusterIP":"10.96.0.10"}}`.
    - `expected-fail-score.txt`: `SCORE: 1/3`
    - `expected-pass-score.txt`: `SCORE: 3/3`

    **Sentinel appends** (idempotent — grep-guard before sed):
    - manifest: `  - id: services-coredns-resolution\n    path: 03-coredns-resolution\n    estimatedMinutes: 7`
    - coverage: `  coredns-resolution:\n    label: "CoreDNS resolution + Pod dnsConfig"\n    questions:\n      - services-coredns-resolution`
    - README: `| 03 | [coredns-resolution](03-coredns-resolution/) | coredns-resolution | 7 |`

    `chmod +x` + git update-index --chmod=+x.
  </action>
  <verify>
    <automated>chmod +x cka-sim/packs/services-networking/03-coredns-resolution/*.sh && for f in cka-sim/packs/services-networking/03-coredns-resolution/*.sh; do bash -n "$f" || exit 1; done && bash cka-sim/scripts/lint-packs.sh && bash cka-sim/scripts/lint-coverage.sh services-networking && bash cka-sim/scripts/lint-deprecated-strings.sh && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - 6 files exist + executable
    - setup NEVER edits kube-system: `! grep -qE 'kubectl[[:space:]]+(apply|patch|edit|replace)[[:space:]]+.*kube-system' cka-sim/packs/services-networking/03-coredns-resolution/setup.sh`
    - setup uses dnsPolicy None + dnsConfig: `grep -q 'dnsPolicy: None' cka-sim/packs/services-networking/03-coredns-resolution/setup.sh && grep -q '1.1.1.1' cka-sim/packs/services-networking/03-coredns-resolution/setup.sh`
    - grade uses exec probe (not mutating): `grep -q 'kubectl exec.*q03-dnsclient' cka-sim/packs/services-networking/03-coredns-resolution/grade.sh && ! grep -qE 'kubectl[[:space:]]+(delete|create|apply|patch|edit|replace)([[:space:]]|$)' cka-sim/packs/services-networking/03-coredns-resolution/grade.sh`
    - grade records correct trap: `grep -q 'record_trap coredns-forward-to-invalid-upstream' cka-sim/packs/services-networking/03-coredns-resolution/grade.sh`
    - ref-solution reads cluster DNS: `grep -q 'kube-dns' cka-sim/packs/services-networking/03-coredns-resolution/ref-solution.sh && grep -q 'clusterIP' cka-sim/packs/services-networking/03-coredns-resolution/ref-solution.sh`
    - manifest row appended exactly once: `grep -c 'id: services-coredns-resolution' cka-sim/packs/services-networking/manifest.yaml | grep -qx 1`
    - coverage slug appended: `grep -c '^  coredns-resolution:' cka-sim/packs/services-networking/coverage.yaml | grep -qx 1`
    - README row appended: `grep -q '\[coredns-resolution\](03-coredns-resolution/)' cka-sim/packs/services-networking/README.md`
    - BUG-3 pre-empt: `! grep -qE '\bnode-0[12]\b' cka-sim/packs/services-networking/03-coredns-resolution/*.sh`
    - git exec bit on 4 .sh: `for f in setup.sh grade.sh reset.sh ref-solution.sh; do git ls-files -s cka-sim/packs/services-networking/03-coredns-resolution/$f | grep -q '^100755' || exit 1; done`
    - All lints + test.sh green
  </acceptance_criteria>
  <done>Q03 CoreDNS resolution ships.</done>
</task>

</tasks>

<verification>
- Q03 round-trips: FAIL path (nameservers=1.1.1.1) fires trap; PASS path (nameservers=cluster DNS IP) resolves
- kube-system/coredns ConfigMap is never mutated by setup or ref-solution
</verification>

<success_criteria>
- All lints + test.sh green
- Coverage now has 3 slugs (netpol-egress + service-core + coredns-resolution)
</success_criteria>

<output>
`.planning/phases/05-services-networking-cluster-architecture-packs/05-04-SUMMARY.md` — Pod-level dnsConfig strategy + trap fire path.
</output>
