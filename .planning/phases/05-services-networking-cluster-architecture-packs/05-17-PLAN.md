---
phase: 05-services-networking-cluster-architecture-packs
plan: 17
type: execute
wave: 1
depends_on: [05-15]
files_modified:
  - cka-sim/packs/cluster-architecture/08-priorityclass/setup.sh
  - cka-sim/packs/cluster-architecture/08-priorityclass/grade.sh
  - cka-sim/packs/cluster-architecture/08-priorityclass/ref-solution.sh
  - cka-sim/packs/cluster-architecture/08-priorityclass/question.md
autonomous: true
gap_closure: true
requirements: [PACK-04, PACK-06, PACK-07]
tags: [cluster-architecture, priorityclass, gap-closure, lint-packs]
updated: 2026-05-13
must_haves:
  truths:
    - "lint-packs.sh pass A (GRADE-02) passes: no `kubectl get ... | grep` idiom in cluster-architecture/08-priorityclass/grade.sh"
    - "Broken state is REACHABLE on a live 1+2 cluster: exactly ZERO PriorityClasses among {q08-critical, q08-batch} have globalDefault=true after setup (admission no longer trips the 2-globalDefault rule)"
    - "grade on broken state fails with rc!=0 and records priorityclass-globaldefault-conflict trap when count != 1"
    - "ref-solution patches exactly one q08 PriorityClass to globalDefault=true, flipping the state to count==1"
    - "grade on post-ref state passes 2/2 rc=0 (both PCs exist AND count of globalDefault==1)"
    - "setup.sh preflight fails with a clear diagnostic if the cluster already owns a globalDefault PC outside q08-* (prevents scoring noise from system-cluster-critical or similar)"
    - "question.md prompt describes the reachable 0-globalDefault broken state and retains the `do not delete` constraint"
  artifacts:
    - path: "cka-sim/packs/cluster-architecture/08-priorityclass/grade.sh"
      provides: "canonical jsonpath + wc -w count of globalDefault=true PriorityClasses"
      contains: "wc -w"
    - path: "cka-sim/packs/cluster-architecture/08-priorityclass/setup.sh"
      provides: "0-globalDefault reachable broken state + preflight for cluster-owned globalDefault"
      contains: "globalDefault: false"
    - path: "cka-sim/packs/cluster-architecture/08-priorityclass/ref-solution.sh"
      provides: "kubectl patch priorityclass q08-critical to globalDefault=true"
      contains: "globalDefault"
  key_links:
    - from: "cka-sim/packs/cluster-architecture/08-priorityclass/grade.sh"
      to: "cka-sim/scripts/lint-packs.sh pass A (GRADE-02)"
      via: "grade.sh uses jsonpath + wc -w instead of banned `kubectl get | grep`"
      pattern: "wc -w"
    - from: "cka-sim/packs/cluster-architecture/08-priorityclass/setup.sh"
      to: "cka-sim/packs/cluster-architecture/08-priorityclass/grade.sh"
      via: "setup seeds count=0, grade requires count=1, ref flips one PC"
      pattern: "globalDefault"
---

<objective>
Close UAT gaps 1 and 15 by redesigning Cluster-Architecture Q08 PriorityClass end-to-end.

Problem: the intended broken state (two PriorityClasses both `globalDefault: true`) is unreachable because the K8s admission plugin rejects the second `create` with `Forbidden: only one globalDefault PriorityClass can exist`. The fallback at setup.sh:43-54 then creates `q08-batch` with `globalDefault: false`, landing the cluster directly in the grader's pass state (count == 1). Both broken-grade and pass-grade return 2/2 rc=0; the trap `priorityclass-globaldefault-conflict` never fires. Additionally, grade.sh:17 uses the banned `kubectl get ... | grep -v '^$' | wc -l` idiom, failing `scripts/lint-packs.sh` pass A (GRADE-02) and aborting `scripts/test.sh` at step 2.

Fix: invert the scenario to a reachable 0-globalDefault broken state. setup seeds both PCs with `globalDefault: false`; grade still requires `count == 1`; ref patches `q08-critical` to `globalDefault: true`. The single grader assertion remains "exactly one PriorityClass is globalDefault" — which now correctly fails on broken (count=0) and passes after ref (count=1). Rewrite the count computation with the storage/03 canonical idiom (jsonpath space-stream + `wc -w`) so lint-packs pass A is green. Update `question.md` so the prompt matches the reachable broken state and retains the "do not delete" constraint.

This closes Gap 1 (lint-packs pass A GRADE-02) and Gap 15 (broken state reachability + trap firing) in a single coherent redesign. `reset.sh` already cleans both cluster-scoped PriorityClasses and is not touched.

Purpose: unblock Phase 5 automated verification (`test.sh` + 5 lints all exit 0) and make Q08 score correctly on a live 1+2 cluster.

Output: 4 modified question files; round-trip verifiable on the live cluster.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-services-networking-cluster-architecture-packs/05-UAT.md
@.planning/phases/05-services-networking-cluster-architecture-packs/05-15-SUMMARY.md
@.planning/phases/05-services-networking-cluster-architecture-packs/05-01-SUMMARY.md
@cka-sim/packs/cluster-architecture/08-priorityclass/setup.sh
@cka-sim/packs/cluster-architecture/08-priorityclass/grade.sh
@cka-sim/packs/cluster-architecture/08-priorityclass/ref-solution.sh
@cka-sim/packs/cluster-architecture/08-priorityclass/question.md
@cka-sim/packs/cluster-architecture/08-priorityclass/reset.sh
@cka-sim/packs/storage/03-access-modes-reclaim/grade.sh
@cka-sim/scripts/lint-packs.sh
@cka-sim/lib/grade.sh
@cka-sim/lib/traps.sh
</context>

<interfaces>
Reference pattern from `cka-sim/packs/storage/03-access-modes-reclaim/grade.sh:37-40` (the canonical jsonpath + `wc -w` idiom):

```
rwx_names=$(kubectl get pv -l cka-sim/question-id=storage-access-modes-reclaim \
  -o jsonpath='{.items[?(@.spec.accessModes[0]=="ReadWriteMany")].metadata.name}' 2>/dev/null || echo "")
rwx_count=$(printf '%s' "$rwx_names" | wc -w | tr -d ' ')
```

lint-packs pass A regex (at `cka-sim/scripts/lint-packs.sh:43`):
```
kubectl[[:space:]]+get[[:space:]].*\|[[:space:]]*grep
```
Any `kubectl get ... | grep` (irrespective of flags) fails. The fix MUST use jsonpath + `wc -w` and MUST NOT pipe to `grep` after `kubectl get`.

Trap to record when count != 1 (already registered in `cka-sim/traps/catalog.yaml`):
- `priorityclass-globaldefault-conflict` (severity: error)

Grader helpers (from `cka-sim/lib/grade.sh`, sourced at top of grade.sh):
- `cka_sim::grade::record_trap <id>` — records trap once (dedup by id)
- `cka_sim::grade::emit_result` — prints SCORE + Trap N: block
- `ok <msg>` / `err <msg>` — live tick marks to stderr
- Accumulators: `CKA_SIM_GRADE_TOTAL`, `CKA_SIM_GRADE_PASSED`, `CKA_SIM_GRADE_FAILS[]`

Setup helpers (from `cka-sim/lib/setup.sh`):
- `cka_sim::setup::ensure_lab_ns <ns> <pack> <question-id>`
- `cka_sim::setup::wait_for_ns_active <ns> <pack> <question-id>`
- `die <msg>` — from `lib/log.sh`, fatal exit

Sandbox convention: `/tmp/q08-priorityclass/` prefix, sentinel-guarded reset cleanup. Do not change.

Cluster-scoped resources touched: `priorityclass/q08-critical`, `priorityclass/q08-batch`. Both labelled `cka-sim/pack: cluster-architecture`, `cka-sim/question-id: cluster-architecture-priorityclass` (already the case in setup).

Lab namespace: `cka-sim-cluster-architecture-08` (set by drill runner via `$CKA_SIM_LAB_NS`).

UAT gap detail: `.planning/phases/05-services-networking-cluster-architecture-packs/05-UAT.md` gaps 1 and 15 (root_cause + artifacts + missing sections already populated).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Redesign setup.sh to seed a reachable 0-globalDefault broken state with preflight</name>
  <files>cka-sim/packs/cluster-architecture/08-priorityclass/setup.sh</files>
  <read_first>
    - cka-sim/packs/cluster-architecture/08-priorityclass/setup.sh (current implementation — see the admission-rejection+fallback trick at lines 30-54 that lands the cluster in the pass state)
    - .planning/phases/05-services-networking-cluster-architecture-packs/05-UAT.md (gap 15 root_cause + missing items)
    - cka-sim/lib/setup.sh (ensure_lab_ns, wait_for_ns_active)
    - cka-sim/lib/log.sh (die)
  </read_first>
  <action>
    Replace the current setup.sh body (lines 17-54 inclusive — the two kubectl apply heredocs, the pipe-to-admission.log fallback, and the re-apply shim) with a clean 0-globalDefault seed plus a preflight check.

    Keep the preamble unchanged (shebang, set flags, env-var guards, `source "$CKA_SIM_ROOT/lib/setup.sh"`, pack/question ids, sandbox setup, ensure_lab_ns, wait_for_ns_active, `mkdir -p "$sandbox"`, `touch "$sandbox/.cka-sim-sentinel"`).

    Preflight — immediately after the sandbox sentinel, before any PriorityClass apply:
    - Query existing cluster-owned globalDefault PriorityClasses NOT named q08-*:
      ```
      existing=$(kubectl get priorityclass \
        -o jsonpath='{range .items[?(@.globalDefault==true)]}{.metadata.name}{" "}{end}' 2>/dev/null || echo "")
      ```
    - Iterate tokens in `$existing`; if any token does NOT start with `q08-`, call `die "setup: cluster already owns a globalDefault PriorityClass '<token>' outside this pack; refusing to seed the q08 drill because the scoring oracle requires exactly one globalDefault to come from q08-critical or q08-batch"`.
    - This keeps system-cluster-critical / system-node-critical from polluting the oracle when they carry globalDefault. On a stock kubeadm v1.35 cluster neither is globalDefault by default, so the preflight is a no-op; if the operator has customised, we fail fast with a clear message.

    PriorityClass apply (both with `globalDefault: false`):

    ```
    kubectl apply -f - <<'EOF'
    apiVersion: scheduling.k8s.io/v1
    kind: PriorityClass
    metadata:
      name: q08-critical
      labels:
        cka-sim/pack: cluster-architecture
        cka-sim/question-id: cluster-architecture-priorityclass
    value: 2000000
    globalDefault: false
    description: "High-priority for critical workloads"
    EOF

    kubectl apply -f - <<'EOF'
    apiVersion: scheduling.k8s.io/v1
    kind: PriorityClass
    metadata:
      name: q08-batch
      labels:
        cka-sim/pack: cluster-architecture
        cka-sim/question-id: cluster-architecture-priorityclass
    value: 100
    globalDefault: false
    description: "Batch workloads"
    EOF
    ```

    No admission-log capture; no fallback; no `|| true`. Both applies must succeed (`set -euo pipefail` aborts on error — the only apply failure mode here is RBAC, which is a legitimate hard stop).

    Post-seed assertion (belt and braces — makes the broken state auditable):

    ```
    after=$(kubectl get priorityclass \
      -o jsonpath='{range .items[?(@.globalDefault==true)]}{.metadata.name}{" "}{end}' 2>/dev/null || echo "")
    after_count=$(printf '%s' "$after" | wc -w | tr -d ' ')
    if [[ "$after_count" != "0" ]]; then
      die "setup: expected 0 q08 PriorityClasses with globalDefault=true after seed; got $after_count ('$after'). Broken state not reachable; aborting."
    fi
    ```

    The `wc -w` / jsonpath-space-stream idiom is required here too (consistency with grade.sh + storage/03 convention; setup.sh is not under the GRADE-02 pass-A ban but using the same idiom keeps the corpus uniform).

    Remove the now-unused `$sandbox/admission.log` reference (setup no longer captures one). Do not touch `$sandbox/.cka-sim-sentinel` or `mkdir -p "$sandbox"`.

    Leave `chmod +x` status alone (file already 100755 per git).
  </action>
  <verify>
    <automated>bash -n cka-sim/packs/cluster-architecture/08-priorityclass/setup.sh && grep -q "globalDefault: false" cka-sim/packs/cluster-architecture/08-priorityclass/setup.sh && ! grep -q "globalDefault: true" cka-sim/packs/cluster-architecture/08-priorityclass/setup.sh && ! grep -qE "kubectl[[:space:]]+get[[:space:]].*\|[[:space:]]*grep" cka-sim/packs/cluster-architecture/08-priorityclass/setup.sh && grep -q "die " cka-sim/packs/cluster-architecture/08-priorityclass/setup.sh</automated>
  </verify>
  <done>
    - Both q08 PriorityClasses applied with `globalDefault: false` — no `globalDefault: true` anywhere in setup.sh
    - Preflight die-check on non-q08 cluster-owned globalDefault PCs present
    - Post-seed assertion dies if count != 0
    - No banned `kubectl get ... | grep` idiom
    - `bash -n` clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite grade.sh count idiom to jsonpath + wc -w; keep both assertions</name>
  <files>cka-sim/packs/cluster-architecture/08-priorityclass/grade.sh</files>
  <read_first>
    - cka-sim/packs/cluster-architecture/08-priorityclass/grade.sh (current — line 17 is the banned idiom)
    - cka-sim/packs/storage/03-access-modes-reclaim/grade.sh:37-40 (canonical pattern — jsonpath + `wc -w`)
    - cka-sim/scripts/lint-packs.sh:40-51 (pass A regex — confirms the ban shape)
    - cka-sim/lib/grade.sh (record_trap, emit_result, ok, err accumulator globals)
  </read_first>
  <action>
    Replace grade.sh in place. Preserve the preamble (shebang, `set -uo pipefail`, env-var guard, `source "$CKA_SIM_ROOT/lib/grade.sh"`).

    Assertion 1 — both q08 PriorityClasses still exist (no-cheat-by-delete):
    ```
    CKA_SIM_GRADE_TOTAL=$(( CKA_SIM_GRADE_TOTAL + 1 ))
    if kubectl get priorityclass q08-critical q08-batch -o name >/dev/null 2>&1; then
      CKA_SIM_GRADE_PASSED=$(( CKA_SIM_GRADE_PASSED + 1 ))
      ok "both q08 PriorityClasses exist"
    else
      CKA_SIM_GRADE_FAILS+=("both q08 PriorityClasses must exist")
      err "both q08 PriorityClasses must exist"
      cka_sim::grade::record_trap priorityclass-globaldefault-conflict
    fi
    ```

    Assertion 2 — exactly one PriorityClass is globalDefault, using jsonpath + `wc -w`:
    ```
    names=$(kubectl get priorityclass \
      -o jsonpath='{.items[?(@.globalDefault==true)].metadata.name}' 2>/dev/null || echo "")
    count=$(printf '%s' "$names" | wc -w | tr -d ' ')
    CKA_SIM_GRADE_TOTAL=$(( CKA_SIM_GRADE_TOTAL + 1 ))
    if [[ "$count" == "1" ]]; then
      CKA_SIM_GRADE_PASSED=$(( CKA_SIM_GRADE_PASSED + 1 ))
      ok "exactly one PriorityClass is globalDefault"
    else
      CKA_SIM_GRADE_FAILS+=("expected exactly one globalDefault PriorityClass, got $count")
      err "expected exactly one globalDefault PriorityClass, got $count"
      cka_sim::grade::record_trap priorityclass-globaldefault-conflict
    fi

    cka_sim::grade::emit_result
    ```

    Notes:
    - MUST use `{.items[?(@.globalDefault==true)].metadata.name}` (space-stream), NOT `{range ...}{.metadata.name}{"\n"}{end}` (newline-stream counted by `wc -l`). The space-stream + `wc -w` pattern is what storage/03 ships and what lint-packs accepts.
    - MUST NOT pipe `kubectl get` to `grep` anywhere in this file. `grep -v '^$'` is the exact idiom lint-packs pass A rejects; so is `grep -c`.
    - Do NOT add mutating verbs (`delete|create|apply|patch|edit|replace`) — grade.sh is read-only per lint-packs pass B.
    - Leave `cka_sim::grade::emit_result` at the bottom.
  </action>
  <verify>
    <automated>bash -n cka-sim/packs/cluster-architecture/08-priorityclass/grade.sh && ! grep -nE '^[[:space:]]*[^#]*kubectl[[:space:]]+get[[:space:]].*\|[[:space:]]*grep' cka-sim/packs/cluster-architecture/08-priorityclass/grade.sh && ! grep -nE '^[[:space:]]*[^#]*kubectl[[:space:]]+(delete|create|apply|patch|edit|replace)([[:space:]]|$)' cka-sim/packs/cluster-architecture/08-priorityclass/grade.sh && grep -q "wc -w" cka-sim/packs/cluster-architecture/08-priorityclass/grade.sh && grep -q "record_trap priorityclass-globaldefault-conflict" cka-sim/packs/cluster-architecture/08-priorityclass/grade.sh && grep -q "cka_sim::grade::emit_result" cka-sim/packs/cluster-architecture/08-priorityclass/grade.sh</automated>
  </verify>
  <done>
    - grade.sh uses jsonpath + `wc -w` space-stream pattern
    - No banned `kubectl get | grep` idiom
    - No mutating verbs (grader remains read-only)
    - Both assertions preserved (both-exist + exactly-one-globalDefault)
    - Trap `priorityclass-globaldefault-conflict` recorded on either failure
    - `bash -n` clean
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite ref-solution.sh to patch q08-critical to globalDefault=true</name>
  <files>cka-sim/packs/cluster-architecture/08-priorityclass/ref-solution.sh</files>
  <read_first>
    - cka-sim/packs/cluster-architecture/08-priorityclass/ref-solution.sh (current — patches q08-batch to false, a no-op on the new seed)
    - .planning/phases/05-services-networking-cluster-architecture-packs/05-UAT.md (gap 15 missing item 5)
  </read_first>
  <action>
    Replace ref-solution.sh body. Preserve shebang + `set -euo pipefail`.

    ```
    kubectl patch priorityclass q08-critical --type=merge -p '{"globalDefault":true}'
    sleep 5
    ```

    Rationale: the new broken state has 0 globalDefault; the fix flips exactly one PC to true. `q08-critical` (higher value=2000000) is the stable choice — the candidate could reasonably pick either, but the reference answer commits to critical. The 5-second settle matches the existing style. Do NOT patch q08-batch. Do NOT delete either PC. Do NOT add extra kubectl calls.
  </action>
  <verify>
    <automated>bash -n cka-sim/packs/cluster-architecture/08-priorityclass/ref-solution.sh && grep -qE 'kubectl patch priorityclass q08-critical .*globalDefault.*true' cka-sim/packs/cluster-architecture/08-priorityclass/ref-solution.sh && ! grep -qE 'kubectl delete priorityclass q08-' cka-sim/packs/cluster-architecture/08-priorityclass/ref-solution.sh</automated>
  </verify>
  <done>
    - ref patches q08-critical to globalDefault=true via kubectl patch --type=merge
    - No delete of either q08 PC
    - `bash -n` clean
  </done>
</task>

<task type="auto">
  <name>Task 4: Rewrite question.md to describe the reachable 0-globalDefault broken state</name>
  <files>cka-sim/packs/cluster-architecture/08-priorityclass/question.md</files>
  <read_first>
    - cka-sim/packs/cluster-architecture/08-priorityclass/question.md (current — references an unreachable 2-globalDefault state)
    - .planning/phases/05-services-networking-cluster-architecture-packs/05-UAT.md (gap 15 missing item 6)
  </read_first>
  <action>
    Replace the question prose. Target content:

    ```
    # PriorityClass globalDefault

    Two PriorityClasses, `q08-critical` and `q08-batch`, exist in the cluster. Neither currently has `globalDefault: true`, so Pods without an explicit `priorityClassName` fall through to whatever cluster-wide default the scheduler resolves — which on a vanilla kubeadm cluster means no priority at all.

    Fix the state so exactly one PriorityClass has `globalDefault: true`.

    ## Constraints

    - Do not delete either PriorityClass. Both `q08-critical` and `q08-batch` must still exist after your fix.
    - Exactly one of them must have `globalDefault: true` after your fix. The other must have `globalDefault: false`.
    - Do not create a new PriorityClass.

    ## Verify

    ```bash
    kubectl get priorityclass q08-critical q08-batch \
      -o custom-columns=NAME:.metadata.name,VALUE:.value,DEFAULT:.globalDefault
    ```

    Exactly one row should show `DEFAULT: true`.
    ```

    Keep the title identical to the current file (`# PriorityClass globalDefault`). Do NOT reference the removed 2-globalDefault scenario or any admission-log path.
  </action>
  <verify>
    <automated>grep -q "globalDefault" cka-sim/packs/cluster-architecture/08-priorityclass/question.md && grep -q "Do not delete" cka-sim/packs/cluster-architecture/08-priorityclass/question.md && ! grep -qi "admission" cka-sim/packs/cluster-architecture/08-priorityclass/question.md && grep -qi "neither currently has" cka-sim/packs/cluster-architecture/08-priorityclass/question.md && ! grep -qi "both.*globaldefault:[[:space:]]*true" cka-sim/packs/cluster-architecture/08-priorityclass/question.md</automated>
  </verify>
  <done>
    - Prompt describes the 0-globalDefault broken state
    - `Do not delete` constraint retained
    - No references to admission errors or the unreachable 2-globalDefault scenario
  </done>
</task>

<task type="auto">
  <name>Task 5: Full lint + test suite — close gaps 1 and 15 together</name>
  <files>(verification only — no file edits)</files>
  <read_first>
    - cka-sim/scripts/test.sh (orchestration)
    - cka-sim/scripts/lint-packs.sh (pass A is the gate for gap 1)
  </read_first>
  <action>
    Run the full gate chain from scope. This task has no code changes — it verifies the three edits above line up.

    Execution order (MUST all exit 0 on a Linux host with bash):

    ```bash
    bash cka-sim/scripts/test.sh
    bash cka-sim/scripts/lint-packs.sh
    bash cka-sim/scripts/lint-coverage.sh
    bash cka-sim/scripts/lint-traps.sh
    bash cka-sim/scripts/lint-deprecated-strings.sh
    ```

    `test.sh` already chains the four lints + unit cases; running them individually is defensive but required by the scope constraint. Capture any failure, diagnose against the tasks above, and fix before declaring the plan done.

    If running on Windows where `bash` is unavailable, document that in SUMMARY.md and mark verification as deferred to a bash-capable host; do not declare the plan green without a live bash run.

    Live-cluster round-trip (defer to /gsd-execute-phase or manual UAT re-run, not required by this task):
    1. `cka-sim drill cluster-architecture --question 08 --grade-broken` → expect rc!=0, SCORE 1/2, Trap `priorityclass-globaldefault-conflict` recorded.
    2. `cka-sim drill cluster-architecture --question 08 --ref-solution`
    3. `cka-sim drill cluster-architecture --question 08 --grade` → expect rc=0, SCORE 2/2, no traps.
    4. `cka-sim drill cluster-architecture --question 08 --reset` → both cluster-scoped q08 PCs deleted.
  </action>
  <verify>
    <automated>bash cka-sim/scripts/test.sh &amp;&amp; bash cka-sim/scripts/lint-packs.sh &amp;&amp; bash cka-sim/scripts/lint-coverage.sh &amp;&amp; bash cka-sim/scripts/lint-traps.sh &amp;&amp; bash cka-sim/scripts/lint-deprecated-strings.sh</automated>
  </verify>
  <done>
    - test.sh passes through all 5 steps (lint-traps, lint-packs, lint-coverage, lint-deprecated-strings, unit cases)
    - lint-packs pass A reports zero errors on cluster-architecture/08-priorityclass/grade.sh (gap 1 closed)
    - All 4 standalone lints exit 0
    - No regressions on sibling packs
  </done>
</task>

</tasks>

<verification>
- UAT gap 1 (lint-packs pass A): grade.sh no longer contains `kubectl get | grep`; `bash cka-sim/scripts/lint-packs.sh` exits 0
- UAT gap 15 (priorityclass broken-state reachability): setup seeds count=0 globalDefault, grade on broken returns rc!=0 and records the trap, ref flips count to 1, grade on post-ref returns rc=0
- reset.sh remains untouched and still cleans both cluster-scoped q08 PCs (TRIP-02 idempotency preserved)
- metadata.yaml untouched (trap IDs and estimatedMinutes still valid; grade.sh still records the registered trap)
- manifest.yaml / coverage.yaml / README.md untouched (no pack-shape changes)
</verification>

<success_criteria>
- `bash cka-sim/scripts/test.sh` exits 0
- `bash cka-sim/scripts/lint-packs.sh` exits 0 (closes gap 1)
- `bash cka-sim/scripts/lint-coverage.sh` exits 0
- `bash cka-sim/scripts/lint-traps.sh` exits 0
- `bash cka-sim/scripts/lint-deprecated-strings.sh` exits 0
- On a live 1+2 kubeadm v1.35 cluster: Q08 broken-grade rc!=0 with trap recorded; post-ref grade rc=0; reset restores clean state (closes gap 15)
- STATE.md and ROADMAP.md are not modified by this plan
</success_criteria>

<output>
Create `.planning/phases/05-services-networking-cluster-architecture-packs/05-17-SUMMARY.md` covering:
- Frontmatter: phase, plan: 17, status: complete (or in-progress with blockers), gap_closure: true, gaps_closed: [1, 15], tasks_completed, task_commits
- Redesign rationale (2-globalDefault unreachable → 0-globalDefault reachable; banned grep → jsonpath + wc -w)
- Task-by-task commit map (one atomic commit per task expected)
- Verification log (outputs of the 5 lints + test.sh)
- Any deviations from plan (auto-fixed issues or deferred items)
- Live-cluster round-trip status (pass/fail/deferred with reason)
- Self-check block confirming all 4 files modified exist and parse
</output>
