---
phase: 05-services-networking-cluster-architecture-packs
plan: 06
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - cka-sim/packs/services-networking/05-kube-proxy-mode/setup.sh
  - cka-sim/packs/services-networking/05-kube-proxy-mode/grade.sh
  - cka-sim/packs/services-networking/05-kube-proxy-mode/reset.sh
  - cka-sim/packs/services-networking/05-kube-proxy-mode/ref-solution.sh
  - cka-sim/packs/services-networking/05-kube-proxy-mode/metadata.yaml
  - cka-sim/packs/services-networking/05-kube-proxy-mode/question.md
  - cka-sim/packs/services-networking/manifest.yaml
  - cka-sim/packs/services-networking/coverage.yaml
  - cka-sim/packs/services-networking/README.md
  - cka-sim/tests/fixtures/phase-05/services-networking/05-kube-proxy-mode/stub-responses.json
  - cka-sim/tests/fixtures/phase-05/services-networking/05-kube-proxy-mode/expected-fail-score.txt
  - cka-sim/tests/fixtures/phase-05/services-networking/05-kube-proxy-mode/expected-pass-score.txt
autonomous: true
requirements: [PACK-03, PACK-06, PACK-07]
tags: [services-networking, kube-proxy, cg-15]
must_haves:
  truths:
    - "setup creates sandbox file /tmp/q05-kube-proxy/reported-mode.txt owned by candidate; does NOT mutate kube-system/kube-proxy ConfigMap"
    - "grade reads the live kube-system/kube-proxy ConfigMap (read-only) via jsonpath + awk extraction; compares candidate's /tmp report to live mode"
    - "grade records trap kube-proxy-mode-mismatch-ipvs-iptables when candidate's reported value != live cluster mode"
    - "ref-solution extracts the live mode and writes it into /tmp/q05-kube-proxy/reported-mode.txt"
    - "reset removes only /tmp/q05-kube-proxy/ + the lab ns; kube-system untouched"
    - "metadata.yaml traps = [kube-proxy-mode-mismatch-ipvs-iptables, missing-dns-egress, default-sa-used]"
  artifacts:
    - path: "cka-sim/packs/services-networking/05-kube-proxy-mode/grade.sh"
      provides: "Behavioural live-mode oracle, no shell-history check"
      contains: "config\\.conf"
    - path: "cka-sim/packs/services-networking/05-kube-proxy-mode/reset.sh"
      provides: "Sandbox cleanup with sentinel guard"
      contains: "/tmp/q05-kube-proxy"
  key_links:
    - from: "cka-sim/packs/services-networking/05-kube-proxy-mode/grade.sh"
      to: "live kube-system/kube-proxy ConfigMap (read-only)"
      via: "kubectl -n kube-system get configmap kube-proxy -o jsonpath='{.data.config\\.conf}'"
      pattern: "configmap kube-proxy"
---

<objective>
Services & Networking Q05 — kube-proxy mode inspection (CG-15 / ROADMAP Phase 5 success criterion 1). 8 minutes. Trap: `kube-proxy-mode-mismatch-ipvs-iptables` (added in P01).

Per RESEARCH §3: read-only inspection. Candidate frames as a "cluster auditor" reporting the kube-proxy mode. setup.sh creates a sandbox file `/tmp/q05-kube-proxy/reported-mode.txt` with a wrong placeholder (`ipvs`); candidate must overwrite with the actual live value. Grader reads the live kube-proxy ConfigMap (never mutates kube-system) and compares.

Output: 6 question files + 3 fixtures + 3 sentinel appends.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-services-networking-cluster-architecture-packs/05-RESEARCH.md
@cka-sim/lib/setup.sh
@cka-sim/lib/grade.sh
@cka-sim/traps/catalog.yaml
</context>

<interfaces>
Lab ns: `cka-sim-services-networking-05`. Sandbox: `/tmp/q05-kube-proxy/` (sentinel-guarded per 05-CONTEXT.md § Cluster-scoped impact policy).

Live-mode extraction (RESEARCH §3 line 120): `kubectl -n kube-system get configmap kube-proxy -o jsonpath='{.data.config\.conf}' | awk '/^mode:/{print $2}' | tr -d '"'`.

Landmine from RESEARCH §14 last bullet: some kubeadm distros may have empty `mode:` meaning defaults. Handle empty as `iptables` (the kubeadm default on Linux).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 6 files + 3 fixtures + sentinel appends (sandbox-based)</name>
  <files>cka-sim/packs/services-networking/05-kube-proxy-mode/metadata.yaml, cka-sim/packs/services-networking/05-kube-proxy-mode/question.md, cka-sim/packs/services-networking/05-kube-proxy-mode/setup.sh, cka-sim/packs/services-networking/05-kube-proxy-mode/grade.sh, cka-sim/packs/services-networking/05-kube-proxy-mode/reset.sh, cka-sim/packs/services-networking/05-kube-proxy-mode/ref-solution.sh, cka-sim/tests/fixtures/phase-05/services-networking/05-kube-proxy-mode/stub-responses.json, cka-sim/tests/fixtures/phase-05/services-networking/05-kube-proxy-mode/expected-fail-score.txt, cka-sim/tests/fixtures/phase-05/services-networking/05-kube-proxy-mode/expected-pass-score.txt, cka-sim/packs/services-networking/manifest.yaml, cka-sim/packs/services-networking/coverage.yaml, cka-sim/packs/services-networking/README.md</files>
  <read_first>
    - .planning/phases/05-services-networking-cluster-architecture-packs/05-RESEARCH.md (§3 lines 113-127 + §14 bullet on kube-proxy mode landmine)
    - cka-sim/lib/setup.sh (ensure_lab_ns + wait_for_ns_active)
    - cka-sim/packs/workloads-scheduling/06-static-pod (sandbox-path + sentinel-guarded cleanup pattern from Phase 4)
  </read_first>
  <action>
    **setup.sh**: source lib/setup.sh, ensure_lab_ns, wait_for_ns_active. Create sandbox dir with sentinel: `mkdir -p /tmp/q05-kube-proxy && touch /tmp/q05-kube-proxy/.cka-sim-sentinel`. Write a WRONG initial mode: `echo 'ipvs' > /tmp/q05-kube-proxy/reported-mode.txt`. Do NOT mutate kube-system. `chmod 0644` the file.

    **grade.sh**: `set -uo pipefail`. source lib/grade.sh + lib/traps.sh. Extract live mode:
    ```
    live_mode=$(kubectl -n kube-system get configmap kube-proxy -o jsonpath='{.data.config\.conf}' 2>/dev/null | awk '/^mode:/{print $2}' | tr -d '"')
    [[ -z "$live_mode" ]] && live_mode=iptables
    ```
    Read candidate report: `reported=$(cat /tmp/q05-kube-proxy/reported-mode.txt 2>/dev/null | tr -d '[:space:]')`. Increment TOTAL; if `$reported == $live_mode` PASSED++; else record_trap `kube-proxy-mode-mismatch-ipvs-iptables` + err message naming expected vs got (without revealing the answer in the question.md).

    Second assertion: reported value is one of the valid enum `{iptables, ipvs, nftables}` (else `record_trap kube-proxy-mode-mismatch-ipvs-iptables`).

    `emit_result`.

    **ref-solution.sh**: extract live mode exactly as grade.sh does; write into sandbox file. `chmod 0644`.

    **reset.sh**: sentinel-guarded cleanup: `if [[ -f /tmp/q05-kube-proxy/.cka-sim-sentinel ]]; then rm -rf /tmp/q05-kube-proxy; fi`. Then `kubectl delete namespace $ns --ignore-not-found --wait=false`. NEVER touches kube-system.

    **metadata.yaml**: `id: services-kube-proxy-mode`, `domain: services-networking`, `estimatedMinutes: 8`, `verified_against: "1.35"`, `traps: [kube-proxy-mode-mismatch-ipvs-iptables, missing-dns-egress, default-sa-used]`, references: k8s-doc virtual-ips + concerns-md CG-15.

    **question.md**: scenario — platform ops needs to report the cluster's current kube-proxy mode. A sandbox file exists at `/tmp/q05-kube-proxy/reported-mode.txt` containing a draft value. Task: overwrite the file with the cluster's actual kube-proxy `mode` (one of `iptables`, `ipvs`, `nftables`). Constraint: read-only — do NOT edit kube-system/kube-proxy ConfigMap. Hint: the kube-proxy ConfigMap in kube-system contains a `config.conf` data key with the runtime config.

    **Fixtures**:
    - stub-responses.json: kube-proxy ConfigMap with `data.config.conf: "mode: iptables\n"` (shape that awk extractor parses)
    - expected-fail-score.txt: `SCORE: 0/2`
    - expected-pass-score.txt: `SCORE: 2/2`

    **Sentinel appends**:
    - manifest: `  - id: services-kube-proxy-mode\n    path: 05-kube-proxy-mode\n    estimatedMinutes: 8`
    - coverage: `  kube-proxy-mode:\n    label: "kube-proxy mode inspection (CG-15)"\n    questions:\n      - services-kube-proxy-mode`
    - README: `| 05 | [kube-proxy-mode](05-kube-proxy-mode/) | kube-proxy-mode | 8 |`

    `chmod +x` + git update-index --chmod=+x.
  </action>
  <verify>
    <automated>chmod +x cka-sim/packs/services-networking/05-kube-proxy-mode/*.sh && for f in cka-sim/packs/services-networking/05-kube-proxy-mode/*.sh; do bash -n "$f" || exit 1; done && bash cka-sim/scripts/lint-packs.sh && bash cka-sim/scripts/lint-coverage.sh services-networking && bash cka-sim/scripts/lint-deprecated-strings.sh && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - 6 files present + 4 .sh have git exec bit
    - setup NEVER mutates kube-system: `! grep -qE 'kubectl[[:space:]]+(apply|patch|edit|replace|delete)[[:space:]]+.*kube-system' cka-sim/packs/services-networking/05-kube-proxy-mode/setup.sh`
    - setup creates sandbox with sentinel: `grep -q '/tmp/q05-kube-proxy' cka-sim/packs/services-networking/05-kube-proxy-mode/setup.sh && grep -q 'cka-sim-sentinel' cka-sim/packs/services-networking/05-kube-proxy-mode/setup.sh`
    - grade reads live ConfigMap read-only + is otherwise mutating-verb free: `grep -q 'configmap kube-proxy' cka-sim/packs/services-networking/05-kube-proxy-mode/grade.sh && ! grep -qE 'kubectl[[:space:]]+(delete|create|apply|patch|edit|replace)([[:space:]]|$)' cka-sim/packs/services-networking/05-kube-proxy-mode/grade.sh`
    - grade records correct trap: `grep -q 'record_trap kube-proxy-mode-mismatch-ipvs-iptables' cka-sim/packs/services-networking/05-kube-proxy-mode/grade.sh`
    - reset is sentinel-guarded: `grep -q 'cka-sim-sentinel' cka-sim/packs/services-networking/05-kube-proxy-mode/reset.sh && grep -q 'rm -rf /tmp/q05-kube-proxy' cka-sim/packs/services-networking/05-kube-proxy-mode/reset.sh`
    - ref-solution extracts live mode: `grep -q 'configmap kube-proxy' cka-sim/packs/services-networking/05-kube-proxy-mode/ref-solution.sh`
    - manifest + coverage + README appended exactly once
    - BUG-3 pre-empt passes
    - All lints + test.sh green
  </acceptance_criteria>
  <done>Q05 ships with read-only live-mode oracle.</done>
</task>

</tasks>

<verification>
- Q05 round-trips: FAIL (file contains wrong mode) → trap; PASS after ref-solution writes correct mode
- kube-system/kube-proxy never written to
- /tmp/q05-kube-proxy cleaned by reset
</verification>

<success_criteria>
- All lints + test.sh green
- Coverage now has 5 slugs
</success_criteria>

<output>
`.planning/phases/05-services-networking-cluster-architecture-packs/05-06-SUMMARY.md` — read-only oracle + sandbox sentinel convention.
</output>
