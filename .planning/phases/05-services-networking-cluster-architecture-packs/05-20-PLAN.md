---
phase: 05-services-networking-cluster-architecture-packs
plan: 20
type: execute
wave: 1
depends_on: [05-11]
files_modified:
  - cka-sim/packs/cluster-architecture/04-pss-enforce/setup.sh
  - cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh
  - cka-sim/packs/cluster-architecture/04-pss-enforce/ref-solution.sh
  - cka-sim/packs/cluster-architecture/04-pss-enforce/question.md
autonomous: true
gap_closure: true
requirements: [PACK-04, PACK-06, PACK-07, CI-02]
tags: [cluster-architecture, pss, pod-security, gap-closure, traps]
must_haves:
  truths:
    - "setup captures PSS admission output from a bare Pod (apiVersion: v1, kind: Pod) so apiserver emits the documented 'pods \"...\" is forbidden: violates PodSecurity \"restricted:v1.35\":' wording matching grade.sh's regex"
    - "setup creates $sandbox/candidate-violator.yaml as the candidate-submission path (placeholder broken content seeded by setup; candidate or ref-solution overwrites with a compliant fix)"
    - "setup applies q04-compliant Deployment AND waits for condition=Available with a 120s timeout so grade.sh readyReplicas assertion is no longer racy"
    - "setup's $sandbox/violator-admission.log is created ONCE by setup and NEVER overwritten by ref-solution"
    - "grade.sh routes trap detection through cka_sim::trap::detect_pss_error_string_mismatch and cka_sim::trap::detect_psp_fictional_pod_label_exemption (from lib/traps.sh), consuming $sandbox/candidate-violator.yaml contents"
    - "grade.sh admission-log regex accepts both 'violates PodSecurity' and 'would violate PodSecurity' wording for defence-in-depth"
    - "ref-solution writes a compliant Pod manifest to $sandbox/candidate-violator.yaml; it does NOT touch the admission log, does NOT recreate q04-compliant, does NOT relabel the namespace"
    - "question.md directs the candidate to edit $sandbox/candidate-violator.yaml, which is what grade.sh inspects via the detector calls"
    - "Broken-state grade records at least one trap from the seeded stub and keeps the 5 assertions scoring honestly"
    - "Post-ref-solution grade reaches 5/5 rc=0 with no traps"
  artifacts:
    - path: "cka-sim/packs/cluster-architecture/04-pss-enforce/setup.sh"
      provides: "bare Pod admission capture + q04-compliant wait + candidate-submission seed"
      contains: "kind: Pod"
    - path: "cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh"
      provides: "detector-routed trap firing against candidate-violator.yaml + broadened admission regex"
      contains: "cka_sim::trap::detect_"
    - path: "cka-sim/packs/cluster-architecture/04-pss-enforce/ref-solution.sh"
      provides: "compliant candidate YAML write; admission log preserved"
      contains: "candidate-violator.yaml"
    - path: "cka-sim/packs/cluster-architecture/04-pss-enforce/question.md"
      provides: "candidate-violator.yaml submission path documented"
      contains: "candidate-violator.yaml"
  key_links:
    - from: "cka-sim/packs/cluster-architecture/04-pss-enforce/setup.sh (bare Pod apply --dry-run=server)"
      to: "cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh (admission log regex)"
      via: "Pod admission error text containing 'violates PodSecurity' wording"
      pattern: "violates PodSecurity"
    - from: "cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh"
      to: "cka-sim/lib/traps.sh (detect_pss_error_string_mismatch + detect_psp_fictional_pod_label_exemption)"
      via: "sourced detector calls against candidate file contents"
      pattern: "cka_sim::trap::detect_"
    - from: "cka-sim/packs/cluster-architecture/04-pss-enforce/ref-solution.sh"
      to: "cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh (candidate-violator.yaml grep)"
      via: "ref writes compliant Pod YAML to $sandbox/candidate-violator.yaml"
      pattern: "candidate-violator.yaml"
---

<objective>
Close UAT gap 4 (test 11 — CA Q04 pss-enforce broken grade 3/5 rc!=0 with no trap line; ref-solution stays at 3/5 rc!=0). Four design bugs compound:

1. setup captures admission from a Deployment dry-run — apiserver emits `Warning: would violate PodSecurity`; grade.sh regex expects Pod rejection wording `violates PodSecurity`. Regex never matches.
2. q04-compliant Deployment has no `kubectl wait`; `readyReplicas` is racy at grade time.
3. grade.sh inline greps target wrong data sources — legacy `PodSecurityPolicy` greped against an admission log that apiserver never emits that string into; `pod-security.kubernetes.io/exempt` greped against `$sandbox/*.yaml` files neither setup nor ref ever populates with the trigger. No candidate-submission artifact path exists, so nothing in the question flow can score the declared traps.
4. ref-solution overwrites both `$sandbox/violator.yaml` AND `$sandbox/violator-admission.log` with compliant dry-run output emitting no PSS text — assertion 3 (admission log wording) gets strictly worse after ref-solution runs.

Fix: rebuild the round-trip so every declared trap is reachable.

- Restructure setup to capture admission output from a bare Pod (apiVersion: v1, kind: Pod) so apiserver emits the documented `pods "<name>" is forbidden: violates PodSecurity "restricted:v1.35":` wording.
- Add `kubectl wait --for=condition=Available deployment/q04-compliant --timeout=120s` in setup.
- Introduce a candidate-submission path `$sandbox/candidate-violator.yaml` — setup seeds a broken-but-commented stub; question.md directs the candidate to edit it; grade.sh runs the registered detectors against its contents. Ref-solution overwrites the candidate file with a compliant Pod manifest that contains neither trigger string.
- Make grade.sh the single authority for admission-log evidence: keep the log untouched after setup writes it; broaden the regex to accept both `violates` and `would violate` wording.
- Focus ref-solution on producing a compliant candidate submission; stop it touching the admission log, the reference violator, or the q04-compliant Deployment.

reset.sh, metadata.yaml, manifest.yaml, coverage.yaml, README.md are NOT touched (trap list in metadata.yaml stays valid — both declared traps are now reachable via detector calls in grade.sh).

Purpose: make Q04 reach at least 4/5 with trap evidence on broken state and 5/5 rc=0 after ref-solution, on a live 1+2 kubeadm v1.35 cluster.

Output: 4 modified question files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-services-networking-cluster-architecture-packs/05-UAT.md
@.planning/phases/05-services-networking-cluster-architecture-packs/05-11-SUMMARY.md
@cka-sim/packs/cluster-architecture/04-pss-enforce/setup.sh
@cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh
@cka-sim/packs/cluster-architecture/04-pss-enforce/ref-solution.sh
@cka-sim/packs/cluster-architecture/04-pss-enforce/reset.sh
@cka-sim/packs/cluster-architecture/04-pss-enforce/metadata.yaml
@cka-sim/packs/cluster-architecture/04-pss-enforce/question.md
@cka-sim/lib/setup.sh
@cka-sim/lib/grade.sh
@cka-sim/lib/traps.sh
</context>

<interfaces>
Registered trap detectors in `cka-sim/lib/traps.sh` (already sourced when grade.sh sources lib/grade.sh, which transitively sources lib/traps.sh on record_trap — but grade.sh MUST explicitly source lib/traps.sh to call the detectors by name):

- `cka_sim::trap::detect_pss_error_string_mismatch <text>` — echoes `pss-error-string-mismatch` if `<text>` contains the literal `PodSecurityPolicy` AND does NOT contain `violates PodSecurity "`.
- `cka_sim::trap::detect_psp_fictional_pod_label_exemption <text>` — echoes `psp-fictional-pod-label-exemption` if `<text>` matches `/pod-security\.kubernetes\.io\/exempt[: ]/`.

Both accept a single text argument (candidate YAML contents). No kubectl calls.

Grader helpers (from `cka-sim/lib/grade.sh`):
- `cka_sim::grade::assert_field_eq <kind> <name> <jsonpath> <expected> [-n ns]`
- `cka_sim::grade::assert_resource_exists <kind> <name> [-n ns]`
- `cka_sim::grade::record_trap <id>` — dedup by id
- `cka_sim::grade::emit_result`
- Accumulators: `CKA_SIM_GRADE_TOTAL`, `CKA_SIM_GRADE_PASSED`, `CKA_SIM_GRADE_FAILS[]`

Setup helpers:
- `cka_sim::setup::ensure_lab_ns`, `cka_sim::setup::wait_for_ns_active` (already used in current setup.sh).

Sandbox path: `/tmp/q04-pss-enforce/` with `.cka-sim-sentinel`. Files currently written by setup: `violator.yaml` (Deployment), `violator-admission.log`, and (in ref-solution) `admission-config.yaml`. This plan replaces the reference with `ref-violator-pod.yaml` (setup-owned bare-Pod input) and adds `candidate-violator.yaml` (candidate submission path). Admission log filename (`violator-admission.log`) is kept for backwards compatibility.

Namespace: `${CKA_SIM_LAB_NS}`. Setup labels it with `pod-security.kubernetes.io/enforce=restricted` and `pod-security.kubernetes.io/enforce-version=v1.35` BEFORE any Pod apply — the current setup already does this; preserve it.

Deprecated-string lint (from `scripts/lint-deprecated-strings.sh`): forbids `PodSecurityPolicy` in manifest blocks under `cka-sim/packs/**`. Comments are allowed. The plan MUST NOT embed `PodSecurityPolicy` as a manifest string anywhere. The candidate stub setup writes intentionally contains `PodSecurityPolicy` INSIDE A COMMENT LINE (starting with `#`) — the lint carveout for comments permits this.

The string `PodSecurityPolicy` as a bare shell literal (e.g. `grep PodSecurityPolicy file`) would also be scanned. grade.sh routes through the detector (which lives in lib/traps.sh, excluded from the packs scan) and therefore does NOT need to mention the literal.

grade.sh's current admission-log regex: `grep -qE 'violates PodSecurity "(privileged|baseline|restricted):(v1\.[0-9]+|latest)":'`. Broaden to: `grep -qE '(would violate|violates) PodSecurity "(privileged|baseline|restricted):(v1\.[0-9]+|latest)":'` — accepts both wordings.

UAT gap 4 root_cause + missing items in full: `.planning/phases/05-services-networking-cluster-architecture-packs/05-UAT.md` gaps entry 4.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: setup.sh — bare Pod admission capture, candidate submission seed, Deployment wait</name>
  <files>cka-sim/packs/cluster-architecture/04-pss-enforce/setup.sh</files>
  <read_first>
    - cka-sim/packs/cluster-architecture/04-pss-enforce/setup.sh (current — Deployment-based admission capture; no wait)
    - .planning/phases/05-services-networking-cluster-architecture-packs/05-UAT.md (gap 4 root_cause + missing items 1-3)
    - cka-sim/lib/setup.sh (ensure_lab_ns, wait_for_ns_active)
  </read_first>
  <action>
    Rewrite the body of setup.sh while preserving the preamble (shebang, set flags, env-var guards, `source "$CKA_SIM_ROOT/lib/setup.sh"`, pack/question id exports, sandbox variable, `ensure_lab_ns`, the two `pod-security.kubernetes.io/enforce*` label applies, `wait_for_ns_active`, `mkdir -p "$sandbox"`, `touch "$sandbox/.cka-sim-sentinel"`).

    After that preamble, execute the following three steps in order:

    Step A — Reference violator Pod (setup-owned, for admission capture). Replace the current Deployment-based capture block:

    Write `$sandbox/ref-violator-pod.yaml` with a bare Pod (apiVersion: v1, kind: Pod) named `q04-ref-violator` in `${CKA_SIM_LAB_NS}`, containing one container from image `nginx:1.27-alpine` with `securityContext.privileged: true`. Then run `kubectl apply --dry-run=server -f "$sandbox/ref-violator-pod.yaml" 2>&1 | tee "$sandbox/violator-admission.log" >/dev/null || true`. The `|| true` is required: apiserver rejects a bare privileged Pod under restricted PSS with a non-zero exit, but the evidence is in the log; `set -euo pipefail` would otherwise abort setup. The admission log will contain the documented wording `pods "q04-ref-violator" is forbidden: violates PodSecurity "restricted:v1.35":`.

    Step B — q04-compliant Deployment (unchanged shape) plus a new kubectl wait (UAT gap 4 missing item 2):

    Apply the existing compliant Deployment heredoc (image `nginxinc/nginx-unprivileged:1.27-alpine`, `runAsNonRoot: true`, `seccompProfile.type: RuntimeDefault`, `allowPrivilegeEscalation: false`, `capabilities.drop: ["ALL"]`). Then add `kubectl wait --for=condition=Available deployment/q04-compliant -n "$CKA_SIM_LAB_NS" --timeout=120s >/dev/null 2>&1 || true`. Trailing `|| true` keeps a slow cluster from failing setup — the final readyReplicas check is the grader's responsibility.

    Step C — Candidate submission stub (UAT gap 4 missing item 3). Write `$sandbox/candidate-violator.yaml` as a quoted heredoc containing:

    - A comment line (starts with `#`) that mentions `PodSecurityPolicy` — legacy wording reference the candidate will remove.
    - A comment line warning against using the exempt label.
    - A Pod manifest (apiVersion: v1, kind: Pod) named `q04-candidate` in `${CKA_SIM_LAB_NS}` with a pod-level label `pod-security.kubernetes.io/exempt: "true"` (fictional exemption — trap trigger) and a container with `securityContext.privileged: true`.

    The two trap triggers embedded in the stub:
    - Literal `PodSecurityPolicy` inside a comment (allowed by the deprecated-strings lint's comment carveout; the detector `detect_pss_error_string_mismatch` fires on the literal regardless of whether it is inside a YAML comment — the detector operates on raw text).
    - The `pod-security.kubernetes.io/exempt:` label at metadata.labels level — matches `detect_psp_fictional_pod_label_exemption`'s regex.

    Both traps fire in broken state. Ref-solution overwrites this file with a compliant Pod (no triggers). Traps go silent; grade becomes 5/5.

    Do NOT retain the OLD `$sandbox/violator.yaml` heredoc (the Deployment shape). Do NOT retain the OLD `kubectl apply --dry-run=server -f "$sandbox/violator.yaml"` line. Step A replaces both.

    Final informational check (not fatal): `[[ -s "$sandbox/violator-admission.log" ]] || warn "setup: violator-admission.log is empty — PSS admission may not be enforcing as expected"`.

    Do NOT add any new `kubectl delete/create/apply/patch/edit/replace` outside Steps A and B. Do NOT touch `/etc/kubernetes/` or `/var/lib/kubelet/`.
  </action>
  <verify>
    <automated>bash -n cka-sim/packs/cluster-architecture/04-pss-enforce/setup.sh &amp;&amp; grep -q 'q04-ref-violator' cka-sim/packs/cluster-architecture/04-pss-enforce/setup.sh &amp;&amp; grep -q 'ref-violator-pod.yaml' cka-sim/packs/cluster-architecture/04-pss-enforce/setup.sh &amp;&amp; grep -q 'candidate-violator.yaml' cka-sim/packs/cluster-architecture/04-pss-enforce/setup.sh &amp;&amp; grep -q 'kubectl wait --for=condition=Available deployment/q04-compliant' cka-sim/packs/cluster-architecture/04-pss-enforce/setup.sh &amp;&amp; grep -q 'pod-security.kubernetes.io/exempt' cka-sim/packs/cluster-architecture/04-pss-enforce/setup.sh &amp;&amp; ! grep -q 'violator.yaml$' cka-sim/packs/cluster-architecture/04-pss-enforce/setup.sh</automated>
  </verify>
  <done>
    - setup writes `$sandbox/ref-violator-pod.yaml` (bare Pod) and captures admission via `apply --dry-run=server`
    - `$sandbox/violator.yaml` is no longer referenced (replaced by ref-violator-pod.yaml + candidate-violator.yaml)
    - setup writes `$sandbox/candidate-violator.yaml` with both trap-trigger strings embedded
    - q04-compliant Deployment still applied, now with kubectl wait
    - bash -n clean
  </done>
</task>

<task type="auto">
  <name>Task 2: grade.sh — detector-routed traps, broadened admission regex, readiness wait</name>
  <files>cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh</files>
  <read_first>
    - cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh (current — inline greps on wrong data sources; narrow regex)
    - cka-sim/lib/traps.sh (detector signatures for pss-error-string-mismatch + psp-fictional-pod-label-exemption)
    - cka-sim/lib/grade.sh (assert_* + record_trap + emit_result)
    - .planning/phases/05-services-networking-cluster-architecture-packs/05-UAT.md (gap 4 missing items 1, 4, 5, 6)
  </read_first>
  <action>
    Rewrite grade.sh in place. Preserve the preamble (shebang, `set -uo pipefail`, env-var guards). Keep `source "$CKA_SIM_ROOT/lib/grade.sh"` and ADD `source "$CKA_SIM_ROOT/lib/traps.sh"` immediately after it so detector symbols are available by name. Define `sandbox="/tmp/q04-pss-enforce"` near the top.

    Grade-time defence-in-depth wait (setup already waits, but reset → setup in quick succession may still be mid-roll):
    Run `kubectl wait --for=condition=Available deployment/q04-compliant -n "$CKA_SIM_LAB_NS" --timeout=30s >/dev/null 2>&1 || true` once at the top, before assertions.

    Assertion 1 — namespace has `pod-security.kubernetes.io/enforce=restricted`: call `cka_sim::grade::assert_field_eq namespace "$CKA_SIM_LAB_NS" '{.metadata.labels.pod-security\.kubernetes\.io/enforce}' restricted`.

    Assertion 2 — namespace has `pod-security.kubernetes.io/enforce-version=v1.35`: call `cka_sim::grade::assert_field_eq namespace "$CKA_SIM_LAB_NS" '{.metadata.labels.pod-security\.kubernetes\.io/enforce-version}' v1.35`.

    Assertion 3 — admission log contains PSS violation wording, accepting BOTH `violates` and `would violate`. Use `log_file="$sandbox/violator-admission.log"`, increment `CKA_SIM_GRADE_TOTAL`, test `grep -qE '(would violate|violates) PodSecurity "(privileged|baseline|restricted):(v1\.[0-9]+|latest)":' "$log_file" 2>/dev/null`; on success increment `CKA_SIM_GRADE_PASSED` and `ok`; on failure push into `CKA_SIM_GRADE_FAILS` and `err`. Do NOT record any trap from this assertion — the admission log is setup-owned evidence, not a candidate-submission signal.

    Assertion 4 — q04-compliant Deployment exists: `cka_sim::grade::assert_resource_exists deployment q04-compliant -n "$CKA_SIM_LAB_NS"`.

    Assertion 5 — readyReplicas is a positive integer. Read `ready=$(kubectl get deployment q04-compliant -n "$CKA_SIM_LAB_NS" -o jsonpath='{.status.readyReplicas}' 2>/dev/null || true)`, increment `CKA_SIM_GRADE_TOTAL`, branch on `[[ "${ready:-0}" =~ ^[1-9][0-9]*$ ]]` (pass) vs fallthrough (fail).

    Trap detection — REPLACES the inline greps at lines 23-30 of the existing file. Read candidate submission once and call both detectors:
    - `candidate_file="$sandbox/candidate-violator.yaml"`
    - `cand_content=""; [[ -r "$candidate_file" ]] && cand_content=$(cat "$candidate_file")`
    - `hit=$(cka_sim::trap::detect_pss_error_string_mismatch "$cand_content"); [[ -n "$hit" ]] && cka_sim::grade::record_trap "$hit"`
    - `hit=$(cka_sim::trap::detect_psp_fictional_pod_label_exemption "$cand_content"); [[ -n "$hit" ]] && cka_sim::grade::record_trap "$hit"`

    End with `cka_sim::grade::emit_result`.

    Remove the current inline grep blocks:
    - `legacy_psp="PodSecurity""Policy"` + subsequent `grep -qE "\b${legacy_psp}\b" "$log_file"` + `record_trap pss-error-string-mismatch`
    - `grep -qE 'pod-security\.kubernetes\.io/.*exempt' "$sandbox"/*.yaml` + `record_trap psp-fictional-pod-label-exemption`

    Both are superseded by the detector calls — single source of truth in `lib/traps.sh` per UAT gap 4 missing item 5.

    Hard constraints:
    - grade.sh stays read-only. No `kubectl delete/create/apply/patch/edit/replace`. No writes to `$sandbox/violator-admission.log` or `$sandbox/candidate-violator.yaml`.
    - No banned `kubectl get | grep` idiom anywhere.
    - The literal string `PodSecurityPolicy` MUST NOT appear in grade.sh. The detector lives in `lib/traps.sh` which is excluded from the packs deprecated-strings scan.
    - `kubectl wait` uses `|| true` — never fatal.
  </action>
  <verify>
    <automated>bash -n cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh &amp;&amp; grep -q 'source .*lib/traps.sh' cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh &amp;&amp; grep -q 'cka_sim::trap::detect_pss_error_string_mismatch' cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh &amp;&amp; grep -q 'cka_sim::trap::detect_psp_fictional_pod_label_exemption' cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh &amp;&amp; grep -qE '\(would violate\|violates\) PodSecurity' cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh &amp;&amp; ! grep -qE '^[[:space:]]*[^#]*kubectl[[:space:]]+(delete|create|apply|patch|edit|replace)([[:space:]]|$)' cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh &amp;&amp; ! grep -qE '^[[:space:]]*[^#]*kubectl[[:space:]]+get[[:space:]].*\|[[:space:]]*grep' cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh &amp;&amp; ! grep -q 'PodSecurityPolicy' cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh &amp;&amp; grep -q 'candidate-violator.yaml' cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh</automated>
  </verify>
  <done>
    - grade.sh sources lib/traps.sh and calls both registered detectors
    - Admission regex accepts both `violates` and `would violate`
    - Candidate file is the detector input
    - No inline grep for `PodSecurityPolicy` or exempt-label pattern
    - No mutating kubectl verbs
    - No banned `kubectl get | grep`
    - No literal `PodSecurityPolicy` in grade.sh
    - bash -n clean
  </done>
</task>

<task type="auto">
  <name>Task 3: ref-solution.sh — write compliant candidate YAML, preserve admission log and reference Pod</name>
  <files>cka-sim/packs/cluster-architecture/04-pss-enforce/ref-solution.sh</files>
  <read_first>
    - cka-sim/packs/cluster-architecture/04-pss-enforce/ref-solution.sh (current — overwrites violator.yaml AND violator-admission.log; no candidate submission)
    - .planning/phases/05-services-networking-cluster-architecture-packs/05-UAT.md (gap 4 missing items 3, 6)
  </read_first>
  <action>
    Replace the entire body of ref-solution.sh while preserving the preamble (shebang, `set -euo pipefail`, `CKA_SIM_LAB_NS` guard). Drop the `CKA_SIM_ROOT` env-var guard if present; it is unused in the new body.

    Body content:
    - Set `sandbox="/tmp/q04-pss-enforce"` and `mkdir -p "$sandbox"`.
    - Write `$sandbox/candidate-violator.yaml` with a compliant Pod manifest using image `nginxinc/nginx-unprivileged:1.27-alpine`, `runAsNonRoot: true`, `seccompProfile.type: RuntimeDefault`, `allowPrivilegeEscalation: false`, `capabilities.drop: ["ALL"]`. Pod name `q04-candidate`, namespace `${CKA_SIM_LAB_NS}`, single label `app: q04-candidate`. NO `pod-security.kubernetes.io/exempt` label anywhere. NO `PodSecurityPolicy` wording anywhere (not even in comments).

    Explicit prohibitions:
    - Do NOT touch `$sandbox/violator-admission.log`.
    - Do NOT touch `$sandbox/ref-violator-pod.yaml`.
    - Do NOT re-apply q04-compliant Deployment; setup already did and waited.
    - Do NOT re-label the namespace; setup already did.
    - Do NOT write `$sandbox/admission-config.yaml`. It is never applied and never inspected — removing it simplifies the round-trip in line with UAT gap 4's demand that every evidence artifact be grade-inspected.

    That is the full body. Compliant Pod shape guarantees both detector calls in grade.sh return empty (no traps) and admission against the restricted namespace would be admitted — passing every assertion.
  </action>
  <verify>
    <automated>bash -n cka-sim/packs/cluster-architecture/04-pss-enforce/ref-solution.sh &amp;&amp; grep -q 'candidate-violator.yaml' cka-sim/packs/cluster-architecture/04-pss-enforce/ref-solution.sh &amp;&amp; ! grep -q 'violator-admission.log' cka-sim/packs/cluster-architecture/04-pss-enforce/ref-solution.sh &amp;&amp; ! grep -q 'ref-violator-pod.yaml' cka-sim/packs/cluster-architecture/04-pss-enforce/ref-solution.sh &amp;&amp; ! grep -q 'q04-compliant' cka-sim/packs/cluster-architecture/04-pss-enforce/ref-solution.sh &amp;&amp; ! grep -q 'PodSecurityPolicy' cka-sim/packs/cluster-architecture/04-pss-enforce/ref-solution.sh &amp;&amp; ! grep -q 'pod-security.kubernetes.io/exempt' cka-sim/packs/cluster-architecture/04-pss-enforce/ref-solution.sh &amp;&amp; grep -q 'runAsNonRoot: true' cka-sim/packs/cluster-architecture/04-pss-enforce/ref-solution.sh</automated>
  </verify>
  <done>
    - ref-solution writes ONLY `$sandbox/candidate-violator.yaml` (compliant Pod)
    - admission log and reference Pod untouched
    - No re-apply of q04-compliant or ns relabel
    - No legacy PSP references; no exempt label
    - bash -n clean
  </done>
</task>

<task type="auto">
  <name>Task 4: question.md — point candidate at candidate-violator.yaml</name>
  <files>cka-sim/packs/cluster-architecture/04-pss-enforce/question.md</files>
  <read_first>
    - cka-sim/packs/cluster-architecture/04-pss-enforce/question.md (current — points at /tmp/q04-pss-enforce/violator.yaml, which no longer exists after Task 1)
    - .planning/phases/05-services-networking-cluster-architecture-packs/05-UAT.md (gap 4 artifacts — question.md currently points at setup-owned file with no scoring path)
  </read_first>
  <action>
    Rewrite question.md. Keep the top-level heading `# Pod Security Standards Enforcement` unchanged (tests for regression).

    Required content, in order:
    - One-paragraph statement that the lab namespace is labelled for `restricted` PSS at version `v1.35`, listing both labels (`pod-security.kubernetes.io/enforce=restricted`, `pod-security.kubernetes.io/enforce-version=v1.35`).
    - Instruction to inspect `/tmp/q04-pss-enforce/violator-admission.log` for evidence of what apiserver emits when it rejects a privileged reference Pod.
    - Task statement: edit `/tmp/q04-pss-enforce/candidate-violator.yaml` so it describes a Pod that complies with restricted. Mention that the file is pre-seeded with a known-wrong template and can be fixed or replaced.
    - Bulleted list of the restricted-profile requirements: no privileged containers, `runAsNonRoot: true`, drop all Linux capabilities, `seccompProfile.type: RuntimeDefault`, `allowPrivilegeEscalation: false`.
    - Constraints section (bulleted): do not add `pod-security.kubernetes.io/exempt` label (no such label bypasses PSS — exemptions are cluster-wide AdmissionConfiguration); do not write legacy PSP wording anywhere; candidate does not need to apply to the cluster (grader inspects file contents directly).
    - `## Verify` section with two fenced `bash` commands: `cat /tmp/q04-pss-enforce/candidate-violator.yaml` and `kubectl apply --dry-run=server -f /tmp/q04-pss-enforce/candidate-violator.yaml`.

    Constraints on question.md content itself:
    - Mention `candidate-violator.yaml` at least twice.
    - Mention `violator-admission.log` at least once.
    - Mention `pod-security.kubernetes.io/exempt` (as a do-not-do warning).
    - Do NOT include the string `/tmp/q04-pss-enforce/violator.yaml` — that file is gone.
    - Keep any mention of legacy PSP wording in PROSE only (no fenced yaml/bash/sh/shell block), so the deprecated-strings lint carveout for markdown prose applies. The safest path is to phrase the warning as `do not write the legacy pod-security policy wording in your submission` — no literal `PodSecurityPolicy` token required. Prefer that phrasing.
  </action>
  <verify>
    <automated>[ "$(grep -c 'candidate-violator.yaml' cka-sim/packs/cluster-architecture/04-pss-enforce/question.md)" -ge 2 ] &amp;&amp; grep -q '^# Pod Security Standards Enforcement' cka-sim/packs/cluster-architecture/04-pss-enforce/question.md &amp;&amp; grep -q 'violator-admission.log' cka-sim/packs/cluster-architecture/04-pss-enforce/question.md &amp;&amp; ! grep -q '/tmp/q04-pss-enforce/violator.yaml' cka-sim/packs/cluster-architecture/04-pss-enforce/question.md &amp;&amp; grep -q 'pod-security.kubernetes.io/exempt' cka-sim/packs/cluster-architecture/04-pss-enforce/question.md</automated>
  </verify>
  <done>
    - Heading preserved
    - Candidate-violator.yaml mentioned twice
    - Admission log referenced
    - Exempt-label warning present
    - No reference to the removed violator.yaml path
  </done>
</task>

<task type="auto">
  <name>Task 5: Full lint + test suite — close gap 4</name>
  <files>(verification only — no file edits)</files>
  <read_first>
    - cka-sim/scripts/test.sh
    - cka-sim/scripts/lint-deprecated-strings.sh
  </read_first>
  <action>
    Run the full gate chain on a bash-capable host:

    - `bash cka-sim/scripts/test.sh`
    - `bash cka-sim/scripts/lint-packs.sh`
    - `bash cka-sim/scripts/lint-coverage.sh`
    - `bash cka-sim/scripts/lint-traps.sh`
    - `bash cka-sim/scripts/lint-deprecated-strings.sh`

    Expected: all exit 0. Deprecated-strings lint should accept the `PodSecurityPolicy` literal embedded in the setup.sh candidate-stub comment line (comment carveout) and find no hits in grade.sh, ref-solution.sh, or question.md.

    If running on Windows where bash is unavailable, document in SUMMARY.md and defer verification to a bash-capable host.

    Live-cluster round-trip (deferred to UAT re-run):
    1. `cka-sim drill cluster-architecture --question 04 --grade-broken` → expect rc!=0; assertions 1, 2, 3, 4, 5 mostly pass (admission log was captured against the bare Pod so wording matches; deployment waited so ready passes); both traps `pss-error-string-mismatch` and `psp-fictional-pod-label-exemption` recorded from the seeded candidate stub.
    2. `cka-sim drill cluster-architecture --question 04 --ref-solution` → ref writes a compliant candidate YAML with no triggers.
    3. `cka-sim drill cluster-architecture --question 04 --grade` → expect 5/5 rc=0 with no traps.
    4. `cka-sim drill cluster-architecture --question 04 --reset` → ns deleted, `$sandbox` wiped via sentinel-guarded cleanup (reset.sh unchanged).

    Note on broken-state semantics: the stub is deliberately "broken" from a trap-detection perspective (not from a PSS-admission perspective). A candidate who pastes a compliant fix but leaves the `PodSecurityPolicy` comment intact still fails `pss-error-string-mismatch` — this is intended; the trap exists to catch legacy wording anywhere in the candidate's artifact.
  </action>
  <verify>
    <automated>bash cka-sim/scripts/test.sh &amp;&amp; bash cka-sim/scripts/lint-packs.sh &amp;&amp; bash cka-sim/scripts/lint-coverage.sh &amp;&amp; bash cka-sim/scripts/lint-traps.sh &amp;&amp; bash cka-sim/scripts/lint-deprecated-strings.sh</automated>
  </verify>
  <done>
    - All 5 lints + test.sh green
    - No regressions on sibling packs
    - Q04 reset.sh, metadata.yaml, manifest.yaml, coverage.yaml, README.md unchanged
  </done>
</task>

</tasks>

<verification>
- UAT gap 4 closed on the live 1+2 cluster: broken grade records at least one trap (both declared traps reachable via detector calls); post-ref grade reaches 5/5 rc=0
- Admission log captured via bare Pod matches grade.sh regex
- q04-compliant Deployment waited to Available — no racy readyReplicas
- Candidate-submission path modelled; traps fire on actual candidate input, not on the wrong data sources
- Ref-solution does not clobber setup-owned evidence
- No regressions to deprecated-strings lint (`PodSecurityPolicy` only present inside a comment; grade.sh and ref-solution.sh avoid the literal entirely)
- Metadata trap list remains honest (both declared traps are now detectable by the question flow)
</verification>

<success_criteria>
- `bash cka-sim/scripts/test.sh` exits 0
- `bash cka-sim/scripts/lint-packs.sh` exits 0
- `bash cka-sim/scripts/lint-coverage.sh` exits 0
- `bash cka-sim/scripts/lint-traps.sh` exits 0
- `bash cka-sim/scripts/lint-deprecated-strings.sh` exits 0
- Live 1+2 cluster: Q04 broken grade rc!=0 with at least one trap recorded; post-ref 5/5 rc=0
- STATE.md and ROADMAP.md not modified by this plan
</success_criteria>

<output>
Create `.planning/phases/05-services-networking-cluster-architecture-packs/05-20-SUMMARY.md` covering:
- Frontmatter: phase, plan: 20, status: complete, gap_closure: true, gaps_closed: [4]
- Redesign rationale (bare Pod admission capture; candidate-submission path; detector-routed traps; setup owns admission log)
- Task-by-task commit map (setup.sh, grade.sh, ref-solution.sh, question.md — 4 atomic commits expected)
- Verification log (5 lints + test.sh outputs)
- Live round-trip status (deferred or observed)
- Self-check: all 4 files parse; ref-violator-pod.yaml + candidate-violator.yaml paths present in setup.sh; admission-config.yaml dropped from ref-solution.sh
</output>
