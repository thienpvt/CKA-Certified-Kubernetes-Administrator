---
phase: 05-services-networking-cluster-architecture-packs
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - cka-sim/packs/services-networking/02-service-core/setup.sh
  - cka-sim/packs/services-networking/02-service-core/grade.sh
  - cka-sim/packs/services-networking/02-service-core/reset.sh
  - cka-sim/packs/services-networking/02-service-core/ref-solution.sh
  - cka-sim/packs/services-networking/02-service-core/metadata.yaml
  - cka-sim/packs/services-networking/02-service-core/question.md
  - cka-sim/packs/services-networking/manifest.yaml
  - cka-sim/packs/services-networking/coverage.yaml
  - cka-sim/packs/services-networking/README.md
  - cka-sim/tests/fixtures/phase-05/services-networking/02-service-core/stub-responses.json
  - cka-sim/tests/fixtures/phase-05/services-networking/02-service-core/expected-fail-score.txt
  - cka-sim/tests/fixtures/phase-05/services-networking/02-service-core/expected-pass-score.txt
autonomous: true
requirements: [PACK-03, PACK-06, PACK-07]
tags: [services-networking, service, endpoints]
must_haves:
  truths:
    - "6 files present under packs/services-networking/02-service-core/"
    - "setup seeds Deployment q02-web with labels {app: q02-web, tier: backend} and a Service q02-web whose selector intentionally mismatches (selector: app=q02-web-typo -> empty Endpoints)"
    - "grade asserts Service endpoints non-empty via kubectl get endpoints q02-web -n <ns> -o jsonpath='{.subsets[0].addresses}'; records trap service-selector-empty-endpoints on empty"
    - "ref-solution kubectl patches Service to selector.app=q02-web (or recreates with correct selector)"
    - "metadata.yaml traps = [service-selector-empty-endpoints, default-sa-used, missing-dns-egress]"
    - "manifest.yaml + coverage.yaml + README.md updated via sentinel-block append (idempotent)"
  artifacts:
    - path: "cka-sim/packs/services-networking/02-service-core/metadata.yaml"
      provides: "Question metadata schema-valid"
      contains: "service-selector-empty-endpoints"
    - path: "cka-sim/packs/services-networking/02-service-core/grade.sh"
      provides: "Endpoints-non-empty assertion + trap record"
      contains: "kubectl get endpoints"
  key_links:
    - from: "cka-sim/packs/services-networking/02-service-core/grade.sh"
      to: "cka-sim/traps/catalog.yaml (service-selector-empty-endpoints)"
      via: "cka_sim::grade::record_trap service-selector-empty-endpoints"
      pattern: "record_trap service-selector-empty-endpoints"
---

<objective>
Services & Networking Q02 — Service core semantics (ClusterIP + Endpoints + selector-matching). Trap ID: `service-selector-empty-endpoints` (already in catalog at line 186-196). 7 minutes per 05-CONTEXT.md.

Scenario: Deployment labelled `app: q02-web, tier: backend` is healthy, but Service uses a typo'd selector (`app: q02-web-typo`). kubectl get endpoints shows empty subsets. Candidate fixes by correcting the Service selector.

Output: 6 question files + 3 fixtures + 3 sentinel appends (manifest, coverage, README).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-services-networking-cluster-architecture-packs/05-RESEARCH.md
@cka-sim/lib/setup.sh
@cka-sim/lib/grade.sh
@cka-sim/traps/catalog.yaml
@cka-sim/packs/workloads-scheduling/01-deployment-requests
</context>

<interfaces>
Lab ns: `cka-sim-services-networking-02`. Resource prefix: `q02-`. Pack: `services-networking`. Question id: `services-service-core`.

Helper signatures (lib/setup.sh): `cka_sim::setup::ensure_lab_ns`, `cka_sim::setup::wait_for_ns_active`, `cka_sim::setup::seed_deployment <ns> <name> <image> [--replicas N] [--sa SA] [--cpu X] [--memory Y]`.

Grader helpers (lib/grade.sh): `assert_resource_exists`, `assert_field_eq`, `record_trap <id>`, `emit_result`.

Service oracle: `kubectl get endpoints q02-web -n $CKA_SIM_LAB_NS -o jsonpath='{.subsets[0].addresses[0].ip}'` — empty string means no endpoints (trap).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 6 question files + 3 fixtures + sentinel-block appends</name>
  <files>cka-sim/packs/services-networking/02-service-core/metadata.yaml, cka-sim/packs/services-networking/02-service-core/question.md, cka-sim/packs/services-networking/02-service-core/setup.sh, cka-sim/packs/services-networking/02-service-core/grade.sh, cka-sim/packs/services-networking/02-service-core/reset.sh, cka-sim/packs/services-networking/02-service-core/ref-solution.sh, cka-sim/tests/fixtures/phase-05/services-networking/02-service-core/stub-responses.json, cka-sim/tests/fixtures/phase-05/services-networking/02-service-core/expected-fail-score.txt, cka-sim/tests/fixtures/phase-05/services-networking/02-service-core/expected-pass-score.txt, cka-sim/packs/services-networking/manifest.yaml, cka-sim/packs/services-networking/coverage.yaml, cka-sim/packs/services-networking/README.md</files>
  <read_first>
    - .planning/phases/05-services-networking-cluster-architecture-packs/05-RESEARCH.md (§1 line 23 — service/endpoints feasibility)
    - cka-sim/lib/setup.sh (seed_deployment signature)
    - cka-sim/packs/workloads-scheduling/01-deployment-requests/grade.sh (assert_resource_exists + assert_field_eq pattern)
    - cka-sim/packs/workloads-scheduling/01-deployment-requests/ref-solution.sh (kubectl patch pattern for ref-solution)
    - cka-sim/packs/services-networking/manifest.yaml (BEGIN/END sentinel location)
    - cka-sim/packs/services-networking/coverage.yaml (BEGIN/END sentinel location)
    - cka-sim/packs/services-networking/README.md (BEGIN/END sentinel location)
  </read_first>
  <action>
    `mkdir -p cka-sim/packs/services-networking/02-service-core cka-sim/tests/fixtures/phase-05/services-networking/02-service-core`.

    **metadata.yaml**: `id: services-service-core`, `domain: services-networking`, `estimatedMinutes: 7`, `verified_against: "1.35"`, `traps: [service-selector-empty-endpoints, default-sa-used, missing-dns-egress]`, references: prior-art `exercises/06-services/` + k8s-doc `https://kubernetes.io/docs/concepts/services-networking/service/#defining-a-service`.

    **question.md**: state Deployment `q02-web` in `${CKA_SIM_LAB_NS}` is healthy (3 replicas Running). Service `q02-web` exists. `kubectl get endpoints q02-web` returns empty. Task: restore traffic by making the Service route to the pods. Constraints: do not modify Deployment labels; fix the Service. Verify: `kubectl get endpoints q02-web -n ${CKA_SIM_LAB_NS}` shows ≥1 address.

    **setup.sh**: source lib/setup.sh, ensure_lab_ns, wait_for_ns_active, seed_deployment `q02-web` nginx:1.27 --replicas 3 --cpu 50m --memory 32Mi. Label override: after seed_deployment, `kubectl label deployment q02-web -n $ns tier=backend --overwrite` AND `kubectl patch deployment q02-web -n $ns --type=strategic -p '{"spec":{"template":{"metadata":{"labels":{"tier":"backend"}}}}}'`. Then `kubectl apply -f -` a Service `q02-web` with selector `app: q02-web-typo` (INTENTIONAL TYPO — the trap), `ports: [{port: 80, targetPort: 80}]`, type: ClusterIP. Wait for Deployment Available (60s ignore-not-found).

    **grade.sh**: `set -uo pipefail`; source lib/grade.sh + lib/traps.sh. Assertions:
    - `cka_sim::grade::assert_resource_exists service q02-web -n $ns`
    - `cka_sim::grade::assert_field_eq service q02-web '{.spec.selector.app}' 'q02-web' -n $ns` (candidate fix must have corrected typo)
    - Behavioural: `addr=$(kubectl get endpoints q02-web -n $ns -o jsonpath='{.subsets[0].addresses[0].ip}' 2>/dev/null || echo "")`; if non-empty, increment PASSED + TOTAL; if empty, increment TOTAL only AND `cka_sim::grade::record_trap service-selector-empty-endpoints`.
    - `cka_sim::grade::emit_result`

    **ref-solution.sh**: `kubectl patch service q02-web -n $ns --type=strategic -p '{"spec":{"selector":{"app":"q02-web"}}}'`. Wait 5s for endpoint controller to reconcile.

    **reset.sh**: `kubectl delete namespace $ns --ignore-not-found --wait=false`.

    **Fixtures** (kubectl stub JSON + expected score texts):
    - `stub-responses.json`: `{"kind":"Service","metadata":{"name":"q02-web"},"spec":{"selector":{"app":"q02-web"}}}` + endpoint subset addresses with 1 ip `10.244.0.5`.
    - `expected-fail-score.txt`: `SCORE: 1/3`
    - `expected-pass-score.txt`: `SCORE: 3/3`

    **Sentinel-block appends** (idempotent — use `grep -q` before append so re-run is safe):

    1. manifest.yaml: insert between `# BEGIN phase-05 new questions` and `# END phase-05 new questions`:
       ```
         - id: services-service-core
           path: 02-service-core
           estimatedMinutes: 7
       ```

    2. coverage.yaml: insert between the same sentinels:
       ```
         service-core:
           label: "Service + Endpoints + selector matching"
           questions:
             - services-service-core
       ```

    3. README.md: insert a table row between the HTML-comment sentinels:
       ```
       | 02 | [service-core](02-service-core/) | service-core | 7 |
       ```

    Idempotent recipe: `grep -q 'id: services-service-core' <file> || sed -i '/# END phase-05 new questions/i\...' <file>`.

    `chmod +x` all .sh + `git update-index --chmod=+x` (BUG-1 pre-empt).
  </action>
  <verify>
    <automated>chmod +x cka-sim/packs/services-networking/02-service-core/*.sh && for f in cka-sim/packs/services-networking/02-service-core/*.sh; do bash -n "$f" || exit 1; done && bash cka-sim/scripts/lint-packs.sh && bash cka-sim/scripts/lint-coverage.sh services-networking && bash cka-sim/scripts/lint-deprecated-strings.sh && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - 6 question files exist: `for f in metadata.yaml question.md setup.sh grade.sh reset.sh ref-solution.sh; do [[ -f cka-sim/packs/services-networking/02-service-core/$f ]] || exit 1; done`
    - 4 .sh are executable with git exec bit: `for f in setup.sh grade.sh reset.sh ref-solution.sh; do git ls-files -s cka-sim/packs/services-networking/02-service-core/$f | grep -q '^100755' || exit 1; done`
    - metadata schema + lint-packs pass E green: `bash cka-sim/scripts/lint-packs.sh; test $? -eq 0`
    - Service intentional typo in setup: `grep -q 'q02-web-typo' cka-sim/packs/services-networking/02-service-core/setup.sh`
    - grade emits trap correctly: `grep -q 'record_trap service-selector-empty-endpoints' cka-sim/packs/services-networking/02-service-core/grade.sh`
    - grade has no mutating verbs: `! grep -qE 'kubectl[[:space:]]+(delete|create|apply|patch|edit|replace)([[:space:]]|$)' cka-sim/packs/services-networking/02-service-core/grade.sh`
    - grade has no get|grep: `! grep -qE 'kubectl[[:space:]]+get[[:space:]].*\|[[:space:]]*grep' cka-sim/packs/services-networking/02-service-core/grade.sh`
    - ref-solution patches selector: `grep -qE 'kubectl patch service q02-web' cka-sim/packs/services-networking/02-service-core/ref-solution.sh && grep -q '"app":"q02-web"' cka-sim/packs/services-networking/02-service-core/ref-solution.sh`
    - manifest appended exactly once (idempotent): `grep -c 'id: services-service-core' cka-sim/packs/services-networking/manifest.yaml | grep -qx 1`
    - coverage appended exactly once: `grep -c '^  service-core:' cka-sim/packs/services-networking/coverage.yaml | grep -qx 1`
    - README row appended: `grep -q '\[service-core\](02-service-core/)' cka-sim/packs/services-networking/README.md`
    - BUG-3 pre-empt: `! grep -qE '\bnode-0[12]\b' cka-sim/packs/services-networking/02-service-core/*.sh`
    - test.sh green: `bash cka-sim/scripts/test.sh; test $? -eq 0`
    - coverage lint green with 2 slugs: `bash cka-sim/scripts/lint-coverage.sh services-networking`
  </acceptance_criteria>
  <done>Q02 ships; pack manifest + coverage + README idempotently updated.</done>
</task>

</tasks>

<verification>
- Service-core question round-trips via kubectl stub: FAIL -> trap service-selector-empty-endpoints; PASS after ref-solution patch
- Pack coverage now has 2 tracker slugs (netpol-egress + service-core)
</verification>

<success_criteria>
- lint-packs + lint-coverage + lint-deprecated-strings + test.sh all green
</success_criteria>

<output>
`.planning/phases/05-services-networking-cluster-architecture-packs/05-03-SUMMARY.md` — question shape + trap fire path + append idempotency proof.
</output>
