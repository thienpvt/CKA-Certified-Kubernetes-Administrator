---
phase: 05-services-networking-cluster-architecture-packs
plan: 12
type: execute
wave: 2
depends_on: [05-01, 05-08]
files_modified:
  - cka-sim/packs/cluster-architecture/05-audit-policy/setup.sh
  - cka-sim/packs/cluster-architecture/05-audit-policy/grade.sh
  - cka-sim/packs/cluster-architecture/05-audit-policy/reset.sh
  - cka-sim/packs/cluster-architecture/05-audit-policy/ref-solution.sh
  - cka-sim/packs/cluster-architecture/05-audit-policy/metadata.yaml
  - cka-sim/packs/cluster-architecture/05-audit-policy/question.md
  - cka-sim/packs/cluster-architecture/manifest.yaml
  - cka-sim/packs/cluster-architecture/coverage.yaml
  - cka-sim/packs/cluster-architecture/README.md
  - cka-sim/tests/fixtures/phase-05/cluster-architecture/05-audit-policy/stub-responses.json
  - cka-sim/tests/fixtures/phase-05/cluster-architecture/05-audit-policy/expected-fail-score.txt
  - cka-sim/tests/fixtures/phase-05/cluster-architecture/05-audit-policy/expected-pass-score.txt
autonomous: true
requirements: [PACK-04, PACK-06, PACK-07]
tags: [cluster-architecture, audit-policy, cg-11]
must_haves:
  truths:
    - "setup creates sandbox /tmp/q05-audit-policy/ sentinel-guarded; seeds policy.yaml empty (candidate's job)"
    - "setup NEVER edits /etc/kubernetes/ (apiserver manifest or policy file)"
    - "grade asserts policy.yaml has apiVersion audit.k8s.io/v1 + kind Policy + at least one rule with level in {None,Metadata,Request,RequestResponse}; uses python3 -c yaml.safe_load structural validation (RESEARCH §6)"
    - "grade records trap audit-policy-wrong-stage-verbosity when level missing/invalid or omitStages has unknown stage names"
    - "ref-solution writes canonical /tmp/q05-audit-policy/policy.yaml matching RESEARCH §6 sample"
    - "metadata.yaml traps = [audit-policy-wrong-stage-verbosity, default-sa-used, missing-dns-egress]"
  artifacts:
    - path: "cka-sim/packs/cluster-architecture/05-audit-policy/grade.sh"
      provides: "Structural Policy YAML validation"
      contains: "audit.k8s.io/v1"
  key_links:
    - from: "cka-sim/packs/cluster-architecture/05-audit-policy/reset.sh"
      to: "sandbox /tmp/q05-audit-policy"
      via: "sentinel-guarded rm -rf"
      pattern: "cka-sim-sentinel"
---

<objective>
Cluster Architecture Q05 — audit policy authoring (CG-11 / ROADMAP Phase 5 criterion 2). 9 minutes. Trap: `audit-policy-wrong-stage-verbosity` (added in P01).

Per RESEARCH §6: candidate writes Policy YAML to sandbox `/tmp/q05-audit-policy/policy.yaml`. Grader structurally validates (yaml.safe_load) — never applies to live apiserver.

Output: 6 question files + 3 fixtures + 3 sentinel appends.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-services-networking-cluster-architecture-packs/05-RESEARCH.md
@cka-sim/lib/setup.sh
@cka-sim/traps/catalog.yaml
</context>

<interfaces>
Lab ns: `cka-sim-cluster-architecture-05`. Sandbox: `/tmp/q05-audit-policy/`.

Canonical Policy from RESEARCH §6 lines 208-226. Stages allowed (omitStages): `RequestReceived`, `ResponseStarted`, `ResponseComplete`, `Panic`. Level enum: `None`, `Metadata`, `Request`, `RequestResponse`.

Python structural validator from RESEARCH §6 lines 238-243.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 6 files + 3 fixtures + sentinel appends</name>
  <files>cka-sim/packs/cluster-architecture/05-audit-policy/metadata.yaml, cka-sim/packs/cluster-architecture/05-audit-policy/question.md, cka-sim/packs/cluster-architecture/05-audit-policy/setup.sh, cka-sim/packs/cluster-architecture/05-audit-policy/grade.sh, cka-sim/packs/cluster-architecture/05-audit-policy/reset.sh, cka-sim/packs/cluster-architecture/05-audit-policy/ref-solution.sh, cka-sim/tests/fixtures/phase-05/cluster-architecture/05-audit-policy/stub-responses.json, cka-sim/tests/fixtures/phase-05/cluster-architecture/05-audit-policy/expected-fail-score.txt, cka-sim/tests/fixtures/phase-05/cluster-architecture/05-audit-policy/expected-pass-score.txt, cka-sim/packs/cluster-architecture/manifest.yaml, cka-sim/packs/cluster-architecture/coverage.yaml, cka-sim/packs/cluster-architecture/README.md</files>
  <read_first>
    - .planning/phases/05-services-networking-cluster-architecture-packs/05-RESEARCH.md (§6 lines 202-248 — canonical policy + structural validation + trap)
    - cka-sim/lib/setup.sh (ensure_lab_ns)
  </read_first>
  <action>
    **setup.sh**: ensure_lab_ns, wait_for_ns_active. Create sandbox `/tmp/q05-audit-policy/` + sentinel. Seed `/tmp/q05-audit-policy/policy.yaml` with WRONG initial content (missing `level` in first rule — seeds trap):
    ```
    apiVersion: audit.k8s.io/v1
    kind: Policy
    rules:
      - resources:
          - group: ""
            resources: ["secrets"]
    ```
    (This intentional policy yaml has no `level` key — fails trap.)

    **grade.sh**: `set -uo pipefail`; source lib/grade.sh + lib/traps.sh.
    - File exists: `[[ -s /tmp/q05-audit-policy/policy.yaml ]]`
    - Structural validation (python3 — note: Phase 4 deferred "validate-local-windows-python3" STATE.md line 56 — we're only running in candidate CP node here, Linux):
    ```
    python3 <<'EOF'
    import yaml, sys
    try:
      p = yaml.safe_load(open('/tmp/q05-audit-policy/policy.yaml'))
      assert p.get('apiVersion') == 'audit.k8s.io/v1'
      assert p.get('kind') == 'Policy'
      assert p.get('rules'), 'empty rules'
      for r in p['rules']:
        assert r.get('level') in {'None','Metadata','Request','RequestResponse'}, f"bad level: {r.get('level')}"
      omit = p.get('omitStages', [])
      for s in omit:
        assert s in {'RequestReceived','ResponseStarted','ResponseComplete','Panic'}, f"bad stage: {s}"
      sys.exit(0)
    except AssertionError as e:
      print(f"ASSERT: {e}", file=sys.stderr); sys.exit(1)
    except Exception as e:
      print(f"ERR: {e}", file=sys.stderr); sys.exit(1)
    EOF
    ```
    Capture exit; if non-zero, `record_trap audit-policy-wrong-stage-verbosity`. TOTAL++ per assertion bucket.
    - Policy has at least one rule: `python3 -c "import yaml; p=yaml.safe_load(open('/tmp/q05-audit-policy/policy.yaml')); assert len(p['rules']) >= 1"` (wrap in exit-capture)
    - emit_result

    **ref-solution.sh**: write canonical policy.yaml per RESEARCH §6 lines 208-226 verbatim.

    **reset.sh**: sentinel-guarded rm -rf /tmp/q05-audit-policy; delete ns.

    **metadata.yaml**: `id: cluster-architecture-audit-policy`, `estimatedMinutes: 9`, `traps: [audit-policy-wrong-stage-verbosity, default-sa-used, missing-dns-egress]`, references: k8s-doc audit.

    **question.md**: scenario — platform security requested an audit policy draft. Task: edit `/tmp/q05-audit-policy/policy.yaml` so it is a valid `audit.k8s.io/v1 Policy` with rules covering: Metadata-level for Secrets, Request-level for ConfigMaps, None-level for Events. Add `omitStages: [RequestReceived]`. Constraint: DO NOT apply to the live apiserver; DO NOT edit /etc/kubernetes/. This is a sandbox drill — grader reads `/tmp/q05-audit-policy/policy.yaml` only.

    **Fixtures**: placeholder + expected-fail `SCORE: 1/3` + expected-pass `SCORE: 3/3`.

    **Sentinel appends**:
    - manifest: `  - id: cluster-architecture-audit-policy\n    path: 05-audit-policy\n    estimatedMinutes: 9`
    - coverage: `  audit-policy:\n    label: "Audit Policy YAML authoring (CG-11)"\n    questions:\n      - cluster-architecture-audit-policy`
    - README: `| 05 | [audit-policy](05-audit-policy/) | audit-policy | 9 |`

    `chmod +x` + git update-index --chmod=+x.
  </action>
  <verify>
    <automated>chmod +x cka-sim/packs/cluster-architecture/05-audit-policy/*.sh && for f in cka-sim/packs/cluster-architecture/05-audit-policy/*.sh; do bash -n "$f" || exit 1; done && bash cka-sim/scripts/lint-packs.sh && bash cka-sim/scripts/lint-coverage.sh cluster-architecture && bash cka-sim/scripts/lint-deprecated-strings.sh && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - 6 files + 4 .sh have git exec bit 100755
    - setup NEVER touches /etc/kubernetes: `! grep -q '/etc/kubernetes' cka-sim/packs/cluster-architecture/05-audit-policy/setup.sh`
    - setup sandbox-based: `grep -q '/tmp/q05-audit-policy' cka-sim/packs/cluster-architecture/05-audit-policy/setup.sh && grep -q 'cka-sim-sentinel' cka-sim/packs/cluster-architecture/05-audit-policy/setup.sh`
    - grade uses python3 structural validation: `grep -q 'python3' cka-sim/packs/cluster-architecture/05-audit-policy/grade.sh && grep -q 'yaml.safe_load' cka-sim/packs/cluster-architecture/05-audit-policy/grade.sh`
    - grade records trap: `grep -q 'record_trap audit-policy-wrong-stage-verbosity' cka-sim/packs/cluster-architecture/05-audit-policy/grade.sh`
    - ref-solution writes canonical policy with correct apiVersion + kind: `grep -q 'audit.k8s.io/v1' cka-sim/packs/cluster-architecture/05-audit-policy/ref-solution.sh && grep -q 'kind: Policy' cka-sim/packs/cluster-architecture/05-audit-policy/ref-solution.sh`
    - ref-solution never applies to apiserver: `! grep -qE 'kubectl[[:space:]]+(apply|patch|edit|replace)' cka-sim/packs/cluster-architecture/05-audit-policy/ref-solution.sh && ! grep -q '/etc/kubernetes' cka-sim/packs/cluster-architecture/05-audit-policy/ref-solution.sh`
    - reset sentinel-guarded: `grep -q 'cka-sim-sentinel' cka-sim/packs/cluster-architecture/05-audit-policy/reset.sh`
    - manifest + coverage + README appended once
    - All lints + test.sh green
  </acceptance_criteria>
  <done>Q05 audit policy ships with structural validation; live apiserver untouched.</done>
</task>

</tasks>

<verification>
- Round-trip: FAIL (no level / invalid stage) → trap; PASS after ref writes canonical policy
- /etc/kubernetes/ never read or written
</verification>

<success_criteria>
- All lints + test.sh green
- Coverage at 5 slugs
</success_criteria>

<output>
`.planning/phases/05-services-networking-cluster-architecture-packs/05-12-SUMMARY.md` — structural validation approach + sandbox note.
</output>
