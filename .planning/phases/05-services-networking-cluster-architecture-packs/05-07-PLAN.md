---
phase: 05-services-networking-cluster-architecture-packs
plan: 07
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - cka-sim/packs/services-networking/06-netpol-endport/setup.sh
  - cka-sim/packs/services-networking/06-netpol-endport/grade.sh
  - cka-sim/packs/services-networking/06-netpol-endport/reset.sh
  - cka-sim/packs/services-networking/06-netpol-endport/ref-solution.sh
  - cka-sim/packs/services-networking/06-netpol-endport/metadata.yaml
  - cka-sim/packs/services-networking/06-netpol-endport/question.md
  - cka-sim/packs/services-networking/manifest.yaml
  - cka-sim/packs/services-networking/coverage.yaml
  - cka-sim/packs/services-networking/README.md
  - cka-sim/tests/fixtures/phase-05/services-networking/06-netpol-endport/stub-responses.json
  - cka-sim/tests/fixtures/phase-05/services-networking/06-netpol-endport/expected-fail-score.txt
  - cka-sim/tests/fixtures/phase-05/services-networking/06-netpol-endport/expected-pass-score.txt
autonomous: true
requirements: [PACK-03, PACK-06, PACK-07]
tags: [services-networking, networkpolicy, endport, cg-16]
must_haves:
  truths:
    - "setup seeds server Pod q06-server multi-port (containerPorts 8080-8090) + client Pod q06-client in lab ns"
    - "setup seeds a baseline NetworkPolicy via cka_sim::setup::seed_netpol_skeleton (DNS-allow egress) so DNS is never the failure"
    - "setup does NOT seed the endPort policy (candidate's job)"
    - "grade asserts NetworkPolicy q06-allow-range exists with spec.ingress[0].ports[0].port=8080 + endPort=8090 + protocol=TCP; probes via kubectl exec client→server:8085 (inside range) AND :8095 (outside range)"
    - "grade records trap netpol-endport-missing-protocol when NetworkPolicy has endPort without protocol"
    - "ref-solution applies the complete NetworkPolicy with port+endPort+protocol"
  artifacts:
    - path: "cka-sim/packs/services-networking/06-netpol-endport/setup.sh"
      provides: "Multi-port server + client + DNS-allow baseline"
      contains: "cka_sim::setup::seed_netpol_skeleton"
    - path: "cka-sim/packs/services-networking/06-netpol-endport/grade.sh"
      provides: "In-range/out-of-range exec probe + structural endPort assertion"
      contains: "endPort"
  key_links:
    - from: "cka-sim/packs/services-networking/06-netpol-endport/setup.sh"
      to: "cka-sim/lib/setup.sh seed_netpol_skeleton helper"
      via: "source + call"
      pattern: "seed_netpol_skeleton"
---

<objective>
Services & Networking Q06 — NetworkPolicy `endPort` + CNI support (CG-16 / ROADMAP Phase 5 success criterion 1). 7 minutes. Trap: `netpol-endport-missing-protocol` (added in P01).

Per RESEARCH §4: Pod listens on ports 8080-8090; candidate writes NetworkPolicy with `port: 8080, endPort: 8090, protocol: TCP`. Grader uses `kubectl exec` probe (inside vs outside range).

Output: 6 question files + 3 fixtures + 3 sentinel appends. Consumes helper `cka_sim::setup::seed_netpol_skeleton` added in P01 (validates its contract end-to-end).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-services-networking-cluster-architecture-packs/05-RESEARCH.md
@cka-sim/lib/setup.sh
@cka-sim/lib/grade.sh
@cka-sim/traps/catalog.yaml
</context>

<interfaces>
Lab ns: `cka-sim-services-networking-06`. Prefix: `q06-`. Question id: `services-netpol-endport`.

Exec-probe image: `nicolaka/netshoot:v0.13` for client (has wget + timeout reliable). Server Pod: small python http.server or netcat loop binding 8080-8090; choose netcat one-liner for simplicity. Alternative: bash `while true; do nc -l -p $port; done` multi-loop — documented in question.md as blackbox.

Probe commands:
- Allow (inside range): `kubectl exec -n $ns q06-client -- wget -qO- --timeout=3 q06-server:8085 >/dev/null 2>&1 && inrange_ok=1`
- Deny (outside range): `kubectl exec -n $ns q06-client -- wget -qO- --timeout=3 q06-server:8095 >/dev/null 2>&1 && outrange_allowed=1 || outrange_blocked=1`

Correct NetworkPolicy: `port: 8080, endPort: 8090, protocol: TCP`, `from: podSelector matchLabels app: q06-client`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 6 files + 3 fixtures + sentinel appends</name>
  <files>cka-sim/packs/services-networking/06-netpol-endport/metadata.yaml, cka-sim/packs/services-networking/06-netpol-endport/question.md, cka-sim/packs/services-networking/06-netpol-endport/setup.sh, cka-sim/packs/services-networking/06-netpol-endport/grade.sh, cka-sim/packs/services-networking/06-netpol-endport/reset.sh, cka-sim/packs/services-networking/06-netpol-endport/ref-solution.sh, cka-sim/tests/fixtures/phase-05/services-networking/06-netpol-endport/stub-responses.json, cka-sim/tests/fixtures/phase-05/services-networking/06-netpol-endport/expected-fail-score.txt, cka-sim/tests/fixtures/phase-05/services-networking/06-netpol-endport/expected-pass-score.txt, cka-sim/packs/services-networking/manifest.yaml, cka-sim/packs/services-networking/coverage.yaml, cka-sim/packs/services-networking/README.md</files>
  <read_first>
    - .planning/phases/05-services-networking-cluster-architecture-packs/05-RESEARCH.md (§4 lines 131-161 — endPort syntax + exec probe + trap semantics; §14 bullet on CNI support caveat)
    - cka-sim/lib/setup.sh (seed_netpol_skeleton signature added in P01 — <ns> <name> <selector-key=value>)
    - cka-sim/packs/services-networking/01-networkpolicy-egress (probe Pod image + NetworkPolicy shape reference)
  </read_first>
  <action>
    **setup.sh**: source lib/setup.sh, ensure_lab_ns, wait_for_ns_active. Apply q06-server Pod labelled `app: q06-server`, image `nicolaka/netshoot:v0.13`, command a bash loop launching 11 netcat listeners on ports 8080..8090 (`for p in $(seq 8080 8090); do nc -l -k -p $p &; done; wait`). Then Service `q06-server` selector `app: q06-server`, ports 8080-8090. Apply q06-client Pod labelled `app: q06-client`, image `nicolaka/netshoot:v0.13`, `command: ["sleep", "3600"]`. Call `cka_sim::setup::seed_netpol_skeleton "$CKA_SIM_LAB_NS" q06-baseline app=q06-client` to pre-install DNS-allow egress so DNS never appears as a red herring.

    **grade.sh**: `set -uo pipefail`; source lib/grade.sh + lib/traps.sh.
    - `assert_resource_exists networkpolicy q06-allow-range -n $ns`
    - `assert_field_eq networkpolicy q06-allow-range '{.spec.ingress[0].ports[0].port}' '8080' -n $ns`
    - `assert_field_eq networkpolicy q06-allow-range '{.spec.ingress[0].ports[0].endPort}' '8090' -n $ns`
    - Protocol presence: `proto=$(kubectl get networkpolicy q06-allow-range -n $ns -o jsonpath='{.spec.ingress[0].ports[0].protocol}')`; TOTAL++; if `$proto == TCP` PASSED++; else record_trap `netpol-endport-missing-protocol`.
    - Behavioural in-range probe: `kubectl exec -n $ns q06-client -- wget -qO- --timeout=3 q06-server:8085 >/dev/null 2>&1`; TOTAL++; exit 0 → PASSED++; non-zero → err + record_trap `netpol-endport-missing-protocol`.
    - Behavioural out-of-range probe: `kubectl exec -n $ns q06-client -- wget -qO- --timeout=3 q06-server:8095 >/dev/null 2>&1`; TOTAL++; non-zero (denied) → PASSED++; exit 0 (allowed) → err.
    - `emit_result`

    **ref-solution.sh**: apply NetworkPolicy `q06-allow-range` with:
    ```
    spec:
      podSelector: { matchLabels: { app: q06-server } }
      policyTypes: [Ingress]
      ingress:
        - from:
            - podSelector: { matchLabels: { app: q06-client } }
          ports:
            - protocol: TCP
              port: 8080
              endPort: 8090
    ```
    Wait 5s for CNI to program the rule.

    **reset.sh**: `kubectl delete namespace $ns --ignore-not-found --wait=false`.

    **metadata.yaml**: `id: services-netpol-endport`, `domain: services-networking`, `estimatedMinutes: 7`, `verified_against: "1.35"`, `traps: [netpol-endport-missing-protocol, missing-dns-egress, default-sa-used]`, references: k8s-doc NetworkPolicy + concerns-md CG-16.

    **question.md**: scenario — `q06-server` Pod listens on TCP 8080-8090 (internal multi-service). `q06-client` must reach the ports in 8080-8090 but nothing outside the range. Task: create NetworkPolicy `q06-allow-range` allowing ingress from `app=q06-client` to `app=q06-server` on ports 8080-8090 inclusive. Constraint: use `endPort` (single policy, not 11 rules). Note: your CNI must support endPort (Calico 3.25+, Cilium 1.13+).

    **Fixtures**:
    - stub-responses.json: NetworkPolicy with complete port/endPort/protocol
    - expected-fail-score.txt: `SCORE: 1/6`
    - expected-pass-score.txt: `SCORE: 6/6`

    **Sentinel appends**:
    - manifest: `  - id: services-netpol-endport\n    path: 06-netpol-endport\n    estimatedMinutes: 7`
    - coverage: `  netpol-endport:\n    label: "NetworkPolicy endPort (CG-16)"\n    questions:\n      - services-netpol-endport`
    - README: `| 06 | [netpol-endport](06-netpol-endport/) | netpol-endport | 7 |`

    `chmod +x` + git update-index --chmod=+x.
  </action>
  <verify>
    <automated>chmod +x cka-sim/packs/services-networking/06-netpol-endport/*.sh && for f in cka-sim/packs/services-networking/06-netpol-endport/*.sh; do bash -n "$f" || exit 1; done && bash cka-sim/scripts/lint-packs.sh && bash cka-sim/scripts/lint-coverage.sh services-networking && bash cka-sim/scripts/lint-deprecated-strings.sh && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - 6 files present + git exec bit on 4 .sh
    - setup uses seed_netpol_skeleton: `grep -q 'cka_sim::setup::seed_netpol_skeleton' cka-sim/packs/services-networking/06-netpol-endport/setup.sh`
    - setup seeds multi-port server: `grep -qE 'seq 8080 8090' cka-sim/packs/services-networking/06-netpol-endport/setup.sh`
    - grade asserts endPort + protocol + behavioural probes: `grep -q 'endPort' cka-sim/packs/services-networking/06-netpol-endport/grade.sh && grep -qE 'kubectl exec.*q06-client' cka-sim/packs/services-networking/06-netpol-endport/grade.sh && grep -q ':8085' cka-sim/packs/services-networking/06-netpol-endport/grade.sh && grep -q ':8095' cka-sim/packs/services-networking/06-netpol-endport/grade.sh`
    - grade records trap: `grep -q 'record_trap netpol-endport-missing-protocol' cka-sim/packs/services-networking/06-netpol-endport/grade.sh`
    - grade is read-only + no get|grep: `! grep -qE 'kubectl[[:space:]]+(delete|create|apply|patch|edit|replace)([[:space:]]|$)' cka-sim/packs/services-networking/06-netpol-endport/grade.sh && ! grep -qE 'kubectl[[:space:]]+get[[:space:]].*\|[[:space:]]*grep' cka-sim/packs/services-networking/06-netpol-endport/grade.sh`
    - ref-solution has all three policy fields: `grep -q 'port: 8080' cka-sim/packs/services-networking/06-netpol-endport/ref-solution.sh && grep -q 'endPort: 8090' cka-sim/packs/services-networking/06-netpol-endport/ref-solution.sh && grep -q 'protocol: TCP' cka-sim/packs/services-networking/06-netpol-endport/ref-solution.sh`
    - manifest + coverage + README appended once; all lints + test.sh green
    - BUG-3 pre-empt: `! grep -qE '\bnode-0[12]\b' cka-sim/packs/services-networking/06-netpol-endport/*.sh`
    - deprecated-strings clean
  </acceptance_criteria>
  <done>Q06 endPort ships; S&N pack now has 6 questions (100% Tracker coverage).</done>
</task>

</tasks>

<verification>
- Q06 round-trips: FAIL (no policy) → trap; PASS after ref applies policy with port+endPort+protocol
- seed_netpol_skeleton helper consumed end-to-end (validates P01 helper contract)
- S&N pack complete (6/6 questions)
</verification>

<success_criteria>
- All lints + test.sh green
- Coverage 6/6 slugs (PACK-07 S&N subset satisfied)
</success_criteria>

<output>
`.planning/phases/05-services-networking-cluster-architecture-packs/05-07-SUMMARY.md` — endPort policy shape + CNI caveat.
</output>
