---
phase: 05-services-networking-cluster-architecture-packs
plan: 14
type: execute
wave: 2
depends_on: [01, 08]
files_modified:
  - cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/setup.sh
  - cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/grade.sh
  - cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/reset.sh
  - cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/ref-solution.sh
  - cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/metadata.yaml
  - cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/question.md
  - cka-sim/packs/cluster-architecture/manifest.yaml
  - cka-sim/packs/cluster-architecture/coverage.yaml
  - cka-sim/packs/cluster-architecture/README.md
  - cka-sim/tests/fixtures/phase-05/cluster-architecture/07-cri-dockerd-endpoint/stub-responses.json
  - cka-sim/tests/fixtures/phase-05/cluster-architecture/07-cri-dockerd-endpoint/expected-fail-score.txt
  - cka-sim/tests/fixtures/phase-05/cluster-architecture/07-cri-dockerd-endpoint/expected-pass-score.txt
autonomous: true
requirements: [PACK-04, PACK-06, PACK-07]
tags: [cluster-architecture, cri-dockerd, cg-13, concerns-activated]
must_haves:
  truths:
    - "setup creates sandbox /tmp/q07-kubelet-flags/ sentinel-guarded; copies /var/lib/kubelet/kubeadm-flags.env AND /etc/kubernetes/kubelet.conf into the sandbox (reads only; never writes live)"
    - "setup pre-seeds WRONG initial state: kubeadm-flags.env contains --container-runtime=remote (removed-1.27 flag); kubelet.conf has a fictional endpoint line to bait the kubeconfig-wrong-file trap"
    - "grade asserts /tmp/q07-kubelet-flags/kubeadm-flags.env contains exactly --container-runtime-endpoint=unix:///run/cri-dockerd.sock AND does NOT contain --container-runtime=remote"
    - "grade records trap kubelet-runtime-flag-in-kubeconfig when candidate edited /tmp/q07-kubelet-flags/kubelet.conf with runtime-endpoint content"
    - "grade records trap removed-container-runtime-flag when --container-runtime=remote still present"
    - "grade records trap cri-endpoint-unix-prefix-missing when endpoint value lacks unix:// prefix"
    - "ref-solution edits kubeadm-flags.env (sandbox copy) with correct endpoint; NEVER edits kubelet.conf; NEVER touches live /var/lib/kubelet/"
  artifacts:
    - path: "cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/grade.sh"
      provides: "3-trap detection covering wrong-file + removed-flag + missing-unix-prefix"
      contains: "container-runtime-endpoint=unix:///run/cri-dockerd.sock"
  key_links:
    - from: "cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/setup.sh"
      to: "sandbox copy of live kubelet files"
      via: "cp -p /var/lib/kubelet/kubeadm-flags.env /etc/kubernetes/kubelet.conf (read-only cp into sandbox)"
      pattern: "/tmp/q07-kubelet-flags"
---

<objective>
Cluster Architecture Q07 — CRI-dockerd endpoint editing (CG-13 / ROADMAP Phase 5 criterion 2). 8 minutes. Activates 2 CONCERNS-seeded traps: `kubelet-runtime-flag-in-kubeconfig` + `removed-container-runtime-flag`. Adds new trap: `cri-endpoint-unix-prefix-missing` (added in P01).

Per RESEARCH §2 + §9 + CONCERNS.md: runtime flags go in `/var/lib/kubelet/kubeadm-flags.env`, NEVER `/etc/kubernetes/kubelet.conf` (that's the kubeconfig). The v1.27+ flag is `--container-runtime-endpoint=unix:///run/cri-dockerd.sock`, NOT the removed `--container-runtime=remote`.

Sandbox drill in `/tmp/q07-kubelet-flags/`; live kubelet config never mutated.

Output: 6 question files + 3 fixtures + 3 sentinel appends.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-services-networking-cluster-architecture-packs/05-RESEARCH.md
@.planning/codebase/CONCERNS.md
@cka-sim/lib/setup.sh
@cka-sim/traps/catalog.yaml
</context>

<interfaces>
Sandbox: `/tmp/q07-kubelet-flags/` sentinel-guarded. Files: `kubeadm-flags.env` (runtime args) + `kubelet.conf` (kubeconfig — MUST NOT edit).

Correct terminal state in `/tmp/q07-kubelet-flags/kubeadm-flags.env`:
```
KUBELET_KUBEADM_ARGS="--container-runtime-endpoint=unix:///run/cri-dockerd.sock --pod-infra-container-image=registry.k8s.io/pause:3.10"
```

Trap matrix:
- `removed-container-runtime-flag` fires if `--container-runtime=remote` anywhere in kubeadm-flags.env
- `kubelet-runtime-flag-in-kubeconfig` fires if kubelet.conf was touched/has `container-runtime-endpoint` content
- `cri-endpoint-unix-prefix-missing` fires if endpoint value lacks `unix://` prefix
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 6 files + 3 fixtures + sentinel appends</name>
  <files>cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/metadata.yaml, cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/question.md, cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/setup.sh, cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/grade.sh, cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/reset.sh, cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/ref-solution.sh, cka-sim/tests/fixtures/phase-05/cluster-architecture/07-cri-dockerd-endpoint/stub-responses.json, cka-sim/tests/fixtures/phase-05/cluster-architecture/07-cri-dockerd-endpoint/expected-fail-score.txt, cka-sim/tests/fixtures/phase-05/cluster-architecture/07-cri-dockerd-endpoint/expected-pass-score.txt, cka-sim/packs/cluster-architecture/manifest.yaml, cka-sim/packs/cluster-architecture/coverage.yaml, cka-sim/packs/cluster-architecture/README.md</files>
  <read_first>
    - .planning/phases/05-services-networking-cluster-architecture-packs/05-RESEARCH.md (§2 lines 86-109 — wrong-file vs correct-file + removed-flag; §9 sandbox layout)
    - .planning/codebase/CONCERNS.md (Content Accuracy - CRI-dockerd section)
    - cka-sim/traps/catalog.yaml (lines 45-76 — kubelet-runtime-flag-in-kubeconfig + removed-container-runtime-flag Phase 2 seed)
    - cka-sim/lib/setup.sh (ensure_lab_ns)
  </read_first>
  <action>
    **setup.sh**: ensure_lab_ns, wait_for_ns_active. Create `/tmp/q07-kubelet-flags/` + sentinel. Copy live files read-only into sandbox (only if they exist — guard with `[[ -f ... ]]`):
    ```
    [[ -f /var/lib/kubelet/kubeadm-flags.env ]] && cp -p /var/lib/kubelet/kubeadm-flags.env /tmp/q07-kubelet-flags/kubeadm-flags.env
    [[ -f /etc/kubernetes/kubelet.conf ]]     && cp -p /etc/kubernetes/kubelet.conf /tmp/q07-kubelet-flags/kubelet.conf
    ```
    Then OVERWRITE the sandbox copy of kubeadm-flags.env with the WRONG initial state:
    ```
    KUBELET_KUBEADM_ARGS="--container-runtime=remote --pod-infra-container-image=registry.k8s.io/pause:3.10"
    ```
    (intentionally uses the REMOVED flag — seeds trap `removed-container-runtime-flag`.)

    **grade.sh**: `set -uo pipefail`; source lib/grade.sh + lib/traps.sh.
    - kubeadm-flags.env exists: `[[ -s /tmp/q07-kubelet-flags/kubeadm-flags.env ]]`
    - Correct endpoint present (full form):
      ```
      grep -qE 'KUBELET_KUBEADM_ARGS=.*--container-runtime-endpoint=unix:///run/cri-dockerd\.sock' /tmp/q07-kubelet-flags/kubeadm-flags.env
      ```
      TOTAL++/PASSED++/err
    - NO removed flag: `! grep -q 'container-runtime=remote' /tmp/q07-kubelet-flags/kubeadm-flags.env`; if violates, record_trap `removed-container-runtime-flag`
    - kubelet.conf did NOT get runtime-endpoint content (trap fires if candidate edited the kubeconfig): `! grep -q 'container-runtime-endpoint' /tmp/q07-kubelet-flags/kubelet.conf 2>/dev/null`; if violates, record_trap `kubelet-runtime-flag-in-kubeconfig`
    - unix:// prefix: if kubeadm-flags.env has `container-runtime-endpoint=` but value doesn't start with `unix://`: `grep -qE 'container-runtime-endpoint=(?!unix://)' /tmp/q07-kubelet-flags/kubeadm-flags.env` — Bash ERE doesn't support negative lookahead; use equivalent: `grep -qE 'container-runtime-endpoint=[^u]' /tmp/q07-kubelet-flags/kubeadm-flags.env || grep -qE 'container-runtime-endpoint=u[^n]' /tmp/q07-kubelet-flags/kubeadm-flags.env`; simpler: `awk -F'container-runtime-endpoint=' '/container-runtime-endpoint=/{print $2}' | awk '{print $1}' | head -c 7` and test for `unix://` prefix. If prefix missing, record_trap `cri-endpoint-unix-prefix-missing`.
    - emit_result

    **ref-solution.sh**: rewrite ONLY `/tmp/q07-kubelet-flags/kubeadm-flags.env` with correct content:
    ```
    echo 'KUBELET_KUBEADM_ARGS="--container-runtime-endpoint=unix:///run/cri-dockerd.sock --pod-infra-container-image=registry.k8s.io/pause:3.10"' > /tmp/q07-kubelet-flags/kubeadm-flags.env
    ```
    EXPLICITLY does NOT touch kubelet.conf. EXPLICITLY does NOT touch live /var/lib/kubelet/.

    **reset.sh**: sentinel-guarded; `[[ -f /tmp/q07-kubelet-flags/.cka-sim-sentinel ]] && rm -rf /tmp/q07-kubelet-flags`; delete ns.

    **metadata.yaml**: `id: cluster-architecture-cri-dockerd-endpoint`, `estimatedMinutes: 8`, `traps: [kubelet-runtime-flag-in-kubeconfig, removed-container-runtime-flag, cri-endpoint-unix-prefix-missing]`, references: concerns-md + k8s-doc container-runtimes.

    **question.md**: scenario — cluster migrating to CRI-dockerd. `/tmp/q07-kubelet-flags/kubeadm-flags.env` currently uses the REMOVED flag `--container-runtime=remote`. Task: edit the sandbox `kubeadm-flags.env` so `KUBELET_KUBEADM_ARGS` contains the v1.35 flag `--container-runtime-endpoint=unix:///run/cri-dockerd.sock` and drops any `--container-runtime=remote`. Constraints: (1) DO NOT edit `/tmp/q07-kubelet-flags/kubelet.conf` — that's the kubelet kubeconfig (apiserver auth), NOT the runtime config. (2) DO NOT modify the live `/var/lib/kubelet/` or `/etc/kubernetes/` paths — this is a sandbox drill. (3) The endpoint MUST start with `unix://`.

    question.md MUST NOT use the deprecated string `PodSecurityPolicy` / `dockershim` / `policy/v1beta1` / `gitRepo:` outside a `#`-comment line (lint-deprecated-strings.sh enforces).

    **Fixtures**: placeholder + expected-fail `SCORE: 1/4` + expected-pass `SCORE: 4/4`.

    **Sentinel appends**:
    - manifest: `  - id: cluster-architecture-cri-dockerd-endpoint\n    path: 07-cri-dockerd-endpoint\n    estimatedMinutes: 8`
    - coverage: `  cri-dockerd-endpoint:\n    label: "CRI-dockerd endpoint config (CG-13)"\n    questions:\n      - cluster-architecture-cri-dockerd-endpoint`
    - README: `| 07 | [cri-dockerd-endpoint](07-cri-dockerd-endpoint/) | cri-dockerd-endpoint | 8 |`

    `chmod +x` + git update-index --chmod=+x.
  </action>
  <verify>
    <automated>chmod +x cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/*.sh && for f in cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/*.sh; do bash -n "$f" || exit 1; done && bash cka-sim/scripts/lint-packs.sh && bash cka-sim/scripts/lint-coverage.sh cluster-architecture && bash cka-sim/scripts/lint-deprecated-strings.sh && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - 6 files + 4 .sh have git exec bit 100755
    - setup seeds sandbox with sentinel: `grep -q '/tmp/q07-kubelet-flags' cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/setup.sh && grep -q 'cka-sim-sentinel' cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/setup.sh`
    - setup seeds WRONG initial state (removed flag): `grep -q 'container-runtime=remote' cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/setup.sh`
    - setup copies live files read-only into sandbox, NEVER writes to /var/lib/kubelet or /etc/kubernetes: `! grep -qE '(cp|mv|tee|echo)[^|]*>[[:space:]]*/var/lib/kubelet' cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/setup.sh && ! grep -qE '(cp|mv|tee|echo)[^|]*>[[:space:]]*/etc/kubernetes' cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/setup.sh`
    - grade records all 3 traps: `grep -q 'record_trap kubelet-runtime-flag-in-kubeconfig' cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/grade.sh && grep -q 'record_trap removed-container-runtime-flag' cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/grade.sh && grep -q 'record_trap cri-endpoint-unix-prefix-missing' cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/grade.sh`
    - ref-solution writes correct endpoint (full form): `grep -q 'container-runtime-endpoint=unix:///run/cri-dockerd.sock' cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/ref-solution.sh`
    - ref-solution does NOT edit kubelet.conf: `! grep -qE '>[[:space:]]*/tmp/q07-kubelet-flags/kubelet\.conf' cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/ref-solution.sh`
    - ref-solution does NOT edit live paths: `! grep -qE '(>[[:space:]]*/var/lib/kubelet|>[[:space:]]*/etc/kubernetes)' cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/ref-solution.sh`
    - reset sentinel-guarded + doesn't touch live: `grep -q 'cka-sim-sentinel' cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/reset.sh && ! grep -qE 'rm.*(var/lib/kubelet|etc/kubernetes)' cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/reset.sh`
    - deprecated-strings clean on this question content: `bash cka-sim/scripts/lint-deprecated-strings.sh`
    - manifest + coverage + README appended once
    - BUG-3 pre-empt passes
    - All lints + test.sh green
  </acceptance_criteria>
  <done>Q07 CRI-dockerd ships with 3 traps active; live kubelet config untouched.</done>
</task>

</tasks>

<verification>
- Round-trip: FAIL (kubeadm-flags.env has --container-runtime=remote) → trap removed-container-runtime-flag; PASS after ref writes correct endpoint
- /var/lib/kubelet/ + /etc/kubernetes/ never mutated by any script
- 2 CONCERNS-seeded traps activated
</verification>

<success_criteria>
- All lints + test.sh green
- Coverage at 7 slugs
</success_criteria>

<output>
`.planning/phases/05-services-networking-cluster-architecture-packs/05-14-SUMMARY.md` — wrong-file vs correct-file trap mechanics + sandbox copy pattern.
</output>
