---
phase: 05-services-networking-cluster-architecture-packs
plan: 15
type: execute
wave: 2
depends_on: [01, 08]
files_modified:
  - cka-sim/packs/cluster-architecture/08-priorityclass/setup.sh
  - cka-sim/packs/cluster-architecture/08-priorityclass/grade.sh
  - cka-sim/packs/cluster-architecture/08-priorityclass/reset.sh
  - cka-sim/packs/cluster-architecture/08-priorityclass/ref-solution.sh
  - cka-sim/packs/cluster-architecture/08-priorityclass/metadata.yaml
  - cka-sim/packs/cluster-architecture/08-priorityclass/question.md
  - cka-sim/packs/cluster-architecture/manifest.yaml
  - cka-sim/packs/cluster-architecture/coverage.yaml
  - cka-sim/packs/cluster-architecture/README.md
  - cka-sim/tests/fixtures/phase-05/cluster-architecture/08-priorityclass/stub-responses.json
  - cka-sim/tests/fixtures/phase-05/cluster-architecture/08-priorityclass/expected-fail-score.txt
  - cka-sim/tests/fixtures/phase-05/cluster-architecture/08-priorityclass/expected-pass-score.txt
autonomous: true
requirements: [PACK-04, PACK-06, PACK-07]
tags: [cluster-architecture, priorityclass]
must_haves:
  truths:
    - "setup creates 2 cluster-scoped PriorityClasses prefixed q08-: q08-critical (globalDefault: true, value: 2000000) + q08-batch (globalDefault: true, value: 100) -- the second creation attempt expected to fail at admission or coexist as invalid state"
    - "setup captures any admission error to /tmp/q08-priorityclass/admission.log (sandbox)"
    - "grade asserts exactly ONE PriorityClass has globalDefault=true AND both q08-critical and q08-batch still exist (candidate shouldn't delete one)"
    - "grade records trap priorityclass-globaldefault-conflict when count of globalDefault=true != 1 OR when either q08 PriorityClass was deleted"
    - "ref-solution: kubectl patch priorityclass q08-batch --type=merge -p '{\"globalDefault\":false}' (leaves q08-critical as sole globalDefault)"
    - "reset deletes both cluster-scoped PriorityClasses + the namespace"
  artifacts:
    - path: "cka-sim/packs/cluster-architecture/08-priorityclass/grade.sh"
      provides: "globalDefault count assertion + both-still-exist check"
      contains: "globalDefault"
  key_links:
    - from: "cka-sim/packs/cluster-architecture/08-priorityclass/reset.sh"
      to: "cluster-scoped PriorityClasses q08-critical + q08-batch"
      via: "kubectl delete priorityclass q08-critical q08-batch --ignore-not-found"
      pattern: "delete priorityclass q08"
---

<objective>
Cluster Architecture Q08 — PriorityClass globalDefault conflict. 7 minutes. Trap: `priorityclass-globaldefault-conflict` (added in P01).

Per RESEARCH §10: only ONE PriorityClass can have `globalDefault: true`. Creating two is an invalid state. Candidate fixes by unsetting `globalDefault` on one — NOT by deleting (that cheats).

Output: 6 question files + 3 fixtures + 3 sentinel appends.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-services-networking-cluster-architecture-packs/05-RESEARCH.md
@cka-sim/lib/setup.sh
@cka-sim/traps/catalog.yaml
</context>

<interfaces>
Lab ns: `cka-sim-cluster-architecture-08`. PriorityClasses cluster-scoped, prefixed `q08-` per TRIP-03.

Admission behaviour: apiserver may reject the second globalDefault=true creation OR accept it (behaviour varies by version; v1.35 rejects). Setup attempts creation and captures the error to sandbox.

Oracle from RESEARCH §10 lines 354-357:
```
count=$(kubectl get priorityclass -o jsonpath='{range .items[?(@.globalDefault==true)]}{.metadata.name}{"\n"}{end}' | wc -l)
```
Correct state: count == 1. Incorrect: count != 1 OR either q08 PC deleted.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 6 files + 3 fixtures + sentinel appends</name>
  <files>cka-sim/packs/cluster-architecture/08-priorityclass/metadata.yaml, cka-sim/packs/cluster-architecture/08-priorityclass/question.md, cka-sim/packs/cluster-architecture/08-priorityclass/setup.sh, cka-sim/packs/cluster-architecture/08-priorityclass/grade.sh, cka-sim/packs/cluster-architecture/08-priorityclass/reset.sh, cka-sim/packs/cluster-architecture/08-priorityclass/ref-solution.sh, cka-sim/tests/fixtures/phase-05/cluster-architecture/08-priorityclass/stub-responses.json, cka-sim/tests/fixtures/phase-05/cluster-architecture/08-priorityclass/expected-fail-score.txt, cka-sim/tests/fixtures/phase-05/cluster-architecture/08-priorityclass/expected-pass-score.txt, cka-sim/packs/cluster-architecture/manifest.yaml, cka-sim/packs/cluster-architecture/coverage.yaml, cka-sim/packs/cluster-architecture/README.md</files>
  <read_first>
    - .planning/phases/05-services-networking-cluster-architecture-packs/05-RESEARCH.md (§10 lines 344-363 — globalDefault semantics + grader oracle + trap)
    - cka-sim/lib/setup.sh (ensure_lab_ns)
  </read_first>
  <action>
    **setup.sh**: ensure_lab_ns, wait_for_ns_active. Create sandbox `/tmp/q08-priorityclass/` + sentinel. Apply first PriorityClass `q08-critical`:
    ```
    apiVersion: scheduling.k8s.io/v1
    kind: PriorityClass
    metadata:
      name: q08-critical
      labels:
        cka-sim/pack: cluster-architecture
        cka-sim/question-id: cluster-architecture-priorityclass
    value: 2000000
    globalDefault: true
    description: "High-priority for critical workloads"
    ```
    Wait a moment. Attempt to apply `q08-batch` ALSO with globalDefault: true (capturing admission error):
    ```
    kubectl apply -f - <<EOF 2>&1 | tee /tmp/q08-priorityclass/admission.log || true
    apiVersion: scheduling.k8s.io/v1
    kind: PriorityClass
    metadata:
      name: q08-batch
      labels:
        cka-sim/pack: cluster-architecture
        cka-sim/question-id: cluster-architecture-priorityclass
    value: 100
    globalDefault: true
    description: "Batch workloads"
    EOF
    ```
    If apiserver rejected, re-apply with globalDefault: false (so q08-batch still exists for the fix scenario — candidate must flip BACK to true first, then fix). Actually simpler: if rejected, create q08-batch with globalDefault: true via `--validate=false` or directly via a sed-patch to bypass admission. Since v1.35 may accept the conflict anyway, prefer the simple path: apply both as-is, accept whichever admission behaviour the cluster exhibits, log it.

    Simplest ordering: apply both with globalDefault: true; whichever admission behaviour the cluster exhibits, grade.sh reads the live state and detects conflict.

    **grade.sh**: `set -uo pipefail`; source lib/grade.sh + lib/traps.sh.
    - Both q08 PCs still exist: `kubectl get priorityclass q08-critical q08-batch -o name >/dev/null 2>&1`; TOTAL++/PASSED++ or err + record_trap (candidate deleted one instead of unsetting globalDefault)
    - Exactly one globalDefault: `count=$(kubectl get priorityclass -o jsonpath='{range .items[?(@.globalDefault==true)]}{.metadata.name}{"\n"}{end}' | grep -v '^$' | wc -l)`; if `count != 1` record_trap `priorityclass-globaldefault-conflict`
    - q08-critical is the global default (stable choice): `gd=$(kubectl get priorityclass q08-critical -o jsonpath='{.spec.globalDefault}' 2>/dev/null || true)` — if $gd == true PASSED++; else err (candidate may have flipped the other way — still valid, but acceptance criterion locks one)

    Actually relax: correct = count==1 AND both exist. Which one is default at candidate's discretion.

    - emit_result

    **ref-solution.sh**: kubectl patch q08-batch to globalDefault=false:
    ```
    kubectl patch priorityclass q08-batch --type=merge -p '{"globalDefault":false}'
    ```
    Wait 5s for reconciliation.

    **reset.sh**: sentinel-guarded sandbox cleanup + `kubectl delete priorityclass q08-critical q08-batch --ignore-not-found` + delete ns.

    **metadata.yaml**: `id: cluster-architecture-priorityclass`, `estimatedMinutes: 7`, `traps: [priorityclass-globaldefault-conflict, default-sa-used, missing-dns-egress]`, references: k8s-doc pod-priority-preemption.

    **question.md**: scenario — two PriorityClasses q08-critical and q08-batch both exist, both claim globalDefault: true. Task: fix so exactly ONE has globalDefault=true (your choice which). Constraint: do NOT delete either PriorityClass (both remain required). Verify: `kubectl get priorityclass -o custom-columns=NAME:.metadata.name,DEFAULT:.globalDefault` shows exactly one `true`.

    **Fixtures**: placeholder + expected-fail `SCORE: 1/3` + expected-pass `SCORE: 3/3`.

    **Sentinel appends**:
    - manifest: `  - id: cluster-architecture-priorityclass\n    path: 08-priorityclass\n    estimatedMinutes: 7`
    - coverage: `  priorityclass:\n    label: "PriorityClass globalDefault semantics"\n    questions:\n      - cluster-architecture-priorityclass`
    - README: `| 08 | [priorityclass](08-priorityclass/) | priorityclass | 7 |`

    `chmod +x` + git update-index --chmod=+x.
  </action>
  <verify>
    <automated>chmod +x cka-sim/packs/cluster-architecture/08-priorityclass/*.sh && for f in cka-sim/packs/cluster-architecture/08-priorityclass/*.sh; do bash -n "$f" || exit 1; done && bash cka-sim/scripts/lint-packs.sh && bash cka-sim/scripts/lint-coverage.sh cluster-architecture && bash cka-sim/scripts/lint-deprecated-strings.sh && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - 6 files + 4 .sh have git exec bit 100755
    - setup creates 2 PCs with globalDefault: true (conflict seed): `grep -c 'globalDefault: true' cka-sim/packs/cluster-architecture/08-priorityclass/setup.sh | awk '$1 >= 2 {exit 0} {exit 1}'`
    - PC names prefixed q08-: `grep -q 'name: q08-critical' cka-sim/packs/cluster-architecture/08-priorityclass/setup.sh && grep -q 'name: q08-batch' cka-sim/packs/cluster-architecture/08-priorityclass/setup.sh`
    - grade records trap: `grep -q 'record_trap priorityclass-globaldefault-conflict' cka-sim/packs/cluster-architecture/08-priorityclass/grade.sh`
    - grade is read-only: `! grep -qE 'kubectl[[:space:]]+(delete|create|apply|patch|edit|replace)([[:space:]]|$)' cka-sim/packs/cluster-architecture/08-priorityclass/grade.sh`
    - ref-solution patches (does not delete): `grep -qE 'kubectl patch priorityclass q08-batch' cka-sim/packs/cluster-architecture/08-priorityclass/ref-solution.sh && ! grep -qE 'kubectl delete priorityclass q08-' cka-sim/packs/cluster-architecture/08-priorityclass/ref-solution.sh`
    - reset cleans both cluster-scoped PCs: `grep -q 'kubectl delete priorityclass q08-critical q08-batch' cka-sim/packs/cluster-architecture/08-priorityclass/reset.sh`
    - BUG-3 pre-empt
    - manifest + coverage + README appended once
    - All lints + test.sh green
  </acceptance_criteria>
  <done>Q08 PriorityClass ships; both cluster-scoped PCs cleaned on reset.</done>
</task>

</tasks>

<verification>
- Round-trip: FAIL (both PCs globalDefault=true) → trap; PASS after ref patches q08-batch to globalDefault=false
- Both PCs still exist after fix (no cheating-by-delete)
- reset removes cluster-scoped PCs so TRIP-02 idempotency holds
</verification>

<success_criteria>
- All lints + test.sh green
- Cluster-Arch coverage at 8 slugs (100% Tracker — PACK-04 + PACK-07 Cluster-Arch subset satisfied)
</success_criteria>

<output>
`.planning/phases/05-services-networking-cluster-architecture-packs/05-15-SUMMARY.md` — globalDefault semantics + no-delete constraint.
</output>
