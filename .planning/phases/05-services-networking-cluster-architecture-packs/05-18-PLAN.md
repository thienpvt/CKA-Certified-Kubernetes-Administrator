---
phase: 05-services-networking-cluster-architecture-packs
plan: 18
type: execute
wave: 1
depends_on: [05-07]
files_modified:
  - cka-sim/packs/services-networking/06-netpol-endport/setup.sh
autonomous: true
gap_closure: true
requirements: [PACK-03, PACK-06, PACK-07]
tags: [services-networking, netpol, endport, gap-closure]
must_haves:
  truths:
    - "After setup, q06-client has the DNS-only baseline egress from seed_netpol_skeleton PLUS an explicit egress allowance to pods labelled app=q06-server on TCP ports 8080-8090 (endPort range)"
    - "After ref-solution applies q06-allow-range (ingress endPort 8080-8090), the grader's 8085 in-range probe from q06-client to q06-server:8085 succeeds"
    - "The grader's 8095 out-of-range probe from q06-client to q06-server:8095 still fails after ref-solution (covered by the absence of a matching ingress endPort AND by the client egress rule topping out at 8090)"
    - "Broken-state grade still returns SCORE 1/6 with netpol-endport-missing-protocol recorded — the new client-egress allowance does NOT open 8085 traffic until ref-solution creates the server-side ingress policy q06-allow-range"
    - "Baseline NetworkPolicy from seed_netpol_skeleton is NOT modified in place — the new egress allowance is a SECOND NetworkPolicy (so the DNS-only baseline lint pattern remains greppable by other Phase 5 graders)"
  artifacts:
    - path: "cka-sim/packs/services-networking/06-netpol-endport/setup.sh"
      provides: "supplemental client-egress NetworkPolicy q06-client-egress allowing TCP 8080-8090 to app=q06-server"
      contains: "q06-client-egress"
  key_links:
    - from: "cka-sim/packs/services-networking/06-netpol-endport/setup.sh"
      to: "cka-sim/packs/services-networking/06-netpol-endport/grade.sh (assertion 5: wget 8085 succeeds)"
      via: "supplemental NetworkPolicy on q06-client with egress TCP 8080-8090 podSelector app=q06-server"
      pattern: "q06-client-egress"
---

<objective>
Close UAT gap 2 (test 7 — S&N Q06 netpol-endport ref-solution grades 5/6 instead of 6/6).

Problem: `seed_netpol_skeleton` (from `lib/setup.sh`) emits a baseline NetworkPolicy selecting `app=q06-client` with `policyTypes: [Ingress, Egress]` and only ONE egress rule — UDP/53 + TCP/53 to kube-system. All other egress from q06-client is denied. The ref-solution creates `q06-allow-range` as an Ingress-only policy on the server side; it does NOT relax client egress. Grader assertion 5 `kubectl exec q06-client -- wget q06-server:8085` then times out because q06-client's egress to q06-server is blocked by the baseline. Assertion 6 (8095 must fail) passes coincidentally via the baseline block.

Fix: after `cka_sim::setup::seed_netpol_skeleton` runs in setup.sh, apply a supplemental NetworkPolicy `q06-client-egress` that ADDS a TCP 8080-8090 egress allowance from `app=q06-client` to `app=q06-server` (same namespace). The 8095 probe still fails because endPort tops out at 8090 — assertion 6 survives. Broken-state behaviour unchanged: without `q06-allow-range` on the server side, ingress denies all traffic regardless of the client-side egress, so broken-grade still returns 1/6 with `netpol-endport-missing-protocol` recorded.

The baseline remains untouched (no parameter added to `seed_netpol_skeleton` — that helper is shared with future questions per 05-01-SUMMARY.md and widening its signature here would ripple). A sibling NetworkPolicy is the correct shape: two overlapping egress policies are UNIONed by k8s ingress/egress admission (both allow-listed egresses are granted).

Purpose: make Q06 reference solution reach the documented 6/6 rc=0 pass grade on a live 1+2 cluster with Calico 3.25+ (endPort-capable CNI).

Output: 1 modified setup.sh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-services-networking-cluster-architecture-packs/05-UAT.md
@.planning/phases/05-services-networking-cluster-architecture-packs/05-01-SUMMARY.md
@cka-sim/packs/services-networking/06-netpol-endport/setup.sh
@cka-sim/packs/services-networking/06-netpol-endport/grade.sh
@cka-sim/packs/services-networking/06-netpol-endport/ref-solution.sh
@cka-sim/packs/services-networking/06-netpol-endport/question.md
@cka-sim/packs/services-networking/06-netpol-endport/metadata.yaml
@cka-sim/lib/setup.sh
</context>

<interfaces>
Current setup.sh (post-P07) seeds three resources in the lab ns:
- Pod `q06-server` (listens TCP 8080-8090) with label `app=q06-server`
- Service `q06-server` (ports 8080, 8085, 8090, 8095)
- Pod `q06-client` with label `app=q06-client`

Then calls `cka_sim::setup::seed_netpol_skeleton "$CKA_SIM_LAB_NS" q06-baseline app=q06-client`, which creates a NetworkPolicy:
```
metadata.name: q06-baseline
spec.podSelector.matchLabels.app: q06-client
spec.policyTypes: [Ingress, Egress]
spec.egress:
  - to: [{ namespaceSelector: { matchLabels: { kubernetes.io/metadata.name: kube-system } } }]
    ports: [{ protocol: UDP, port: 53 }, { protocol: TCP, port: 53 }]
```

Grader (post-P07, unchanged by this plan) asserts 6 items:
1-3. `q06-allow-range` NetworkPolicy exists + port=8080 + endPort=8090 (ingress side, candidate's task)
4. NetworkPolicy declares protocol TCP
5. `kubectl exec q06-client -- wget q06-server:8085` succeeds (in-range probe)
6. `kubectl exec q06-client -- wget q06-server:8095` FAILS (out-of-range probe)

Ref-solution creates:
```
kind: NetworkPolicy
metadata.name: q06-allow-range
spec.podSelector.matchLabels.app: q06-server
spec.policyTypes: [Ingress]
spec.ingress:
  - from: [{ podSelector: { matchLabels: { app: q06-client } } }]
    ports: [{ protocol: TCP, port: 8080, endPort: 8090 }]
```

NetworkPolicy union semantics (K8s docs): a pod is affected by the UNION of all policies that select it. For traffic to flow:
- Server-side ingress rule must match (provided by `q06-allow-range` after ref-solution).
- Client-side egress rule must match (currently DNS-only baseline → traffic denied).

Both conditions are required. The fix adds the client-side egress allowance for TCP 8080-8090.

Gap 2 fix shape (from UAT.md missing):
> In setup.sh, after seed_netpol_skeleton, apply an additional NetworkPolicy (or extend baseline) selecting app=q06-client with an egress rule: to podSelector app=q06-server, TCP ports 8080-8090 via endPort.

Using a second NetworkPolicy rather than extending `seed_netpol_skeleton` is the stated preference — the helper is shared with future questions per 05-01-SUMMARY.md's helper signature lock.

Lab namespace: `${CKA_SIM_LAB_NS}` (e.g. `cka-sim-services-networking-06`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add q06-client-egress supplemental NetworkPolicy to setup.sh</name>
  <files>cka-sim/packs/services-networking/06-netpol-endport/setup.sh</files>
  <read_first>
    - cka-sim/packs/services-networking/06-netpol-endport/setup.sh (current — ends with the seed_netpol_skeleton call + Ready waits)
    - cka-sim/lib/setup.sh (seed_netpol_skeleton signature + emitted YAML shape)
    - cka-sim/packs/services-networking/06-netpol-endport/grade.sh (assertions 5/6 semantics)
    - .planning/phases/05-services-networking-cluster-architecture-packs/05-UAT.md (gap 2 root_cause + missing)
  </read_first>
  <action>
    Insert a new `kubectl apply -f - <<EOF ... EOF` block AFTER the `cka_sim::setup::seed_netpol_skeleton` call and BEFORE the two `kubectl wait --for=condition=Ready pod/...` calls. The new manifest:

    ```
    apiVersion: networking.k8s.io/v1
    kind: NetworkPolicy
    metadata:
      name: q06-client-egress
      namespace: ${CKA_SIM_LAB_NS}
    spec:
      podSelector:
        matchLabels:
          app: q06-client
      policyTypes:
        - Egress
      egress:
        - to:
            - podSelector:
                matchLabels:
                  app: q06-server
          ports:
            - protocol: TCP
              port: 8080
              endPort: 8090
    ```

    Notes:
    - `policyTypes: [Egress]` only — ingress not needed on the client, and omitting Ingress here keeps scope tight.
    - Single egress rule: to podSelector app=q06-server, TCP 8080-8090. UNIONs with the baseline's DNS egress → q06-client can resolve names AND talk to q06-server on 8080-8090.
    - DO NOT widen to 8095. The out-of-range probe in assertion 6 relies on 8095 traffic being denied; the client-egress policy only covers up to 8090, and crucially the server-side ingress policy also does not cover 8095. Both sides independently block 8095 — defence in depth.
    - DO NOT touch the existing `cka_sim::setup::seed_netpol_skeleton "$CKA_SIM_LAB_NS" q06-baseline app=q06-client` call. The DNS baseline stays exactly as-is.
    - DO NOT modify `seed_netpol_skeleton` in `lib/setup.sh`. The helper signature is locked per 05-01-SUMMARY.md.
    - Use `${CKA_SIM_LAB_NS}` interpolation (the surrounding setup.sh already does).

    Placement: the `kubectl wait` calls at the end of current setup.sh give both pods ~60s to become Ready. Apply the new NetworkPolicy BEFORE those waits so the Ready check reflects the steady-state networking posture; k8s NetworkPolicy is deny-by-default once policyTypes includes Egress, so applying it while the pod is still initialising has no adverse effect (the pod is not sending traffic yet).

    Keep `set -euo pipefail`, env-var guards, `source "$CKA_SIM_ROOT/lib/setup.sh"`, and all existing applies untouched.

    Do not update metadata.yaml (trap IDs + estimatedMinutes still accurate; the question still exercises endPort + client egress). Do not update question.md (candidate task is unchanged — still authoring the server-side ingress policy).
  </action>
  <verify>
    <automated>bash -n cka-sim/packs/services-networking/06-netpol-endport/setup.sh && grep -q "q06-client-egress" cka-sim/packs/services-networking/06-netpol-endport/setup.sh && grep -qE "port:[[:space:]]+8080" cka-sim/packs/services-networking/06-netpol-endport/setup.sh && grep -qE "endPort:[[:space:]]+8090" cka-sim/packs/services-networking/06-netpol-endport/setup.sh &amp;&amp; grep -q "seed_netpol_skeleton" cka-sim/packs/services-networking/06-netpol-endport/setup.sh</automated>
  </verify>
  <done>
    - setup.sh applies q06-client-egress NetworkPolicy after seed_netpol_skeleton
    - Egress to app=q06-server on TCP 8080 endPort 8090
    - seed_netpol_skeleton call preserved (DNS baseline intact)
    - `bash -n` clean
    - lib/setup.sh NOT modified (helper signature stable)
  </done>
</task>

<task type="auto">
  <name>Task 2: Lint suite — confirm no regressions</name>
  <files>(verification only)</files>
  <read_first>
    - cka-sim/scripts/test.sh
  </read_first>
  <action>
    Run the full lint + test suite on a bash-capable host:

    ```bash
    bash cka-sim/scripts/test.sh
    bash cka-sim/scripts/lint-packs.sh
    bash cka-sim/scripts/lint-coverage.sh
    bash cka-sim/scripts/lint-traps.sh
    bash cka-sim/scripts/lint-deprecated-strings.sh
    ```

    Expected: all exit 0. The new NetworkPolicy inlined in setup.sh has no deprecated strings (no `policy/v1beta1` — it's `networking.k8s.io/v1`). No hardcoded node tokens. No banned `kubectl get | grep` in any grader.

    Live-cluster verification (deferred to UAT re-run):
    1. `cka-sim drill services-networking --question 06 --grade-broken` → expect rc!=0, SCORE 1/6, trap `netpol-endport-missing-protocol` recorded.
    2. `cka-sim drill services-networking --question 06 --ref-solution`
    3. `cka-sim drill services-networking --question 06 --grade` → expect rc=0, SCORE 6/6.
    4. `cka-sim drill services-networking --question 06 --reset` → lab namespace deleted (sibling NetworkPolicy cleaned by cascading ns delete).
  </action>
  <verify>
    <automated>bash cka-sim/scripts/test.sh &amp;&amp; bash cka-sim/scripts/lint-packs.sh &amp;&amp; bash cka-sim/scripts/lint-coverage.sh &amp;&amp; bash cka-sim/scripts/lint-traps.sh &amp;&amp; bash cka-sim/scripts/lint-deprecated-strings.sh</automated>
  </verify>
  <done>
    - All 5 lints + test.sh green on a Linux host with bash
    - No changes to sibling packs
    - No changes to metadata.yaml, question.md, ref-solution.sh, grade.sh, reset.sh
  </done>
</task>

</tasks>

<verification>
- UAT gap 2 closed: ref-solution grade reaches 6/6 rc=0 on a live 1+2 cluster with Calico 3.25+ (both ingress and egress match 8080-8090 TCP)
- Broken-state behaviour unchanged: without q06-allow-range ingress, traffic still denied — grade returns 1/6 with `netpol-endport-missing-protocol` recorded
- Assertion 6 (8095 fails) preserved: client egress tops out at 8090 AND server has no ingress for 8095
- lib/setup.sh unchanged; seed_netpol_skeleton signature preserved
- No regressions on other S&N questions (Q01 retrofit still uses the same baseline helper)
</verification>

<success_criteria>
- `bash cka-sim/scripts/test.sh` exits 0
- `bash cka-sim/scripts/lint-packs.sh` exits 0
- `bash cka-sim/scripts/lint-coverage.sh` exits 0
- `bash cka-sim/scripts/lint-traps.sh` exits 0
- `bash cka-sim/scripts/lint-deprecated-strings.sh` exits 0
- On the live 1+2 cluster: Q06 broken grade 1/6 rc!=0 with trap; post-ref grade 6/6 rc=0
</success_criteria>

<output>
Create `.planning/phases/05-services-networking-cluster-architecture-packs/05-18-SUMMARY.md` covering:
- Frontmatter: phase, plan: 18, status: complete, gap_closure: true, gaps_closed: [2]
- Fix rationale (NetworkPolicy union semantics require both-sides allow; DNS baseline untouched)
- Single-task commit for the setup.sh edit
- Verification log (5 lints + test.sh)
- Live round-trip status (deferred or observed)
- Self-check: setup.sh references q06-client-egress + seed_netpol_skeleton both present
</output>
