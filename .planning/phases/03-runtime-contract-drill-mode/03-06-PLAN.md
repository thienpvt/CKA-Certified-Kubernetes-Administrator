---
phase: 03-runtime-contract-drill-mode
plan: 06
type: execute
wave: 3
depends_on: [01, 03]
files_modified:
  - cka-sim/packs/services-networking/manifest.yaml
  - cka-sim/packs/services-networking/README.md
  - cka-sim/packs/services-networking/01-networkpolicy-egress/metadata.yaml
  - cka-sim/packs/services-networking/01-networkpolicy-egress/question.md
  - cka-sim/packs/services-networking/01-networkpolicy-egress/setup.sh
  - cka-sim/packs/services-networking/01-networkpolicy-egress/grade.sh
  - cka-sim/packs/services-networking/01-networkpolicy-egress/reset.sh
  - cka-sim/packs/services-networking/01-networkpolicy-egress/ref-solution.sh
autonomous: true
requirements: [TRIP-01, TRIP-02, TRIP-03, TRIP-04, TRIP-05, TRIP-06, GRADE-02, GRADE-03, GRADE-04, GRADE-06, RUN-02]
must_haves:
  truths:
    - "services-networking pack has manifest + README + 1 question dir with all 6 files"
    - "setup.sh creates ns + an egress-restricting NetworkPolicy that BLOCKS DNS (the trap) + a probe pod with bash"
    - "grade.sh asserts pod Ready + DNS lookup succeeds in-pod (custom kubectl exec nslookup probe per RESEARCH Pattern 5 footnote); records missing-dns-egress detector"
    - "ref-solution.sh patches the NetworkPolicy to allow UDP/53 egress to kube-dns"
    - "metadata.yaml declares 3 trap-ids registered in catalog"
  artifacts:
    - path: "cka-sim/packs/services-networking/01-networkpolicy-egress/setup.sh"
      provides: "NetworkPolicy egress-restricted (no DNS allowed) + probe pod with bash image"
      contains: "kind: NetworkPolicy"
    - path: "cka-sim/packs/services-networking/01-networkpolicy-egress/grade.sh"
      provides: "Pod Ready + DNS works (custom probe) + missing-dns-egress detector"
      contains: "cka_sim::trap::detect_missing_dns_egress"
  key_links:
    - from: "cka-sim/packs/services-networking/01-networkpolicy-egress/grade.sh"
      to: "cka-sim/lib/traps.sh"
      via: "detect_missing_dns_egress + kubectl exec nslookup probe (custom, since /dev/tcp is TCP-only)"
      pattern: "detect_missing_dns_egress"
---

<objective>
Ship services-networking pack with 1 reference question. Per CONTEXT D-10: trap is `missing-dns-egress` (NetworkPolicy with egress restrictions but no UDP/53 allow rule).

Per RESEARCH Pattern 5 lines 352-364 + Assumption A3: the probe pod MUST use an image with bash (`nicolaka/netshoot:v0.13` or similar — busybox/alpine `sh` lacks /dev/tcp). The grade.sh uses a custom `kubectl exec ... nslookup` probe (not assert_egress_allowed which is TCP-only).

Output: 8 new files (2 pack-level + 6 question).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-runtime-contract-drill-mode/03-CONTEXT.md
@.planning/phases/03-runtime-contract-drill-mode/03-RESEARCH.md
@.planning/phases/03-runtime-contract-drill-mode/03-PATTERNS.md
@cka-sim/lib/grade.sh
@cka-sim/lib/traps.sh
@cka-sim/scripts/lint-packs.sh
@cka-sim/traps/catalog.yaml
</context>

<interfaces>
From cka-sim/lib/grade.sh:
- cka_sim::grade::assert_pod_ready <ns> <pod> [<weight>]
- cka_sim::grade::assert_resource_exists <kind> <name> [-n <ns>]
- cka_sim::grade::record_trap <id>
- cka_sim::grade::emit_result

From cka-sim/lib/traps.sh:
- cka_sim::trap::detect_missing_dns_egress <ns> <netpol-name> — echoes "missing-dns-egress" if NetworkPolicy has Egress in policyTypes, has at least 1 egress rule, and no rule permits UDP/53 (lines 186-208)

NOTE: Phase 2's assert_egress_allowed uses /dev/tcp (TCP only). For DNS UDP probing, grade.sh uses a custom in-pod nslookup wrapped in a manual fail counter — RESEARCH Pattern 5 lines 357-358 footnote.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create services-networking pack scaffold</name>
  <files>cka-sim/packs/services-networking/manifest.yaml, cka-sim/packs/services-networking/README.md, cka-sim/packs/services-networking/01-networkpolicy-egress/metadata.yaml, cka-sim/packs/services-networking/01-networkpolicy-egress/question.md</files>
  <read_first>
    - .planning/phases/03-runtime-contract-drill-mode/03-CONTEXT.md (D-10 services-networking row)
    - .planning/phases/03-runtime-contract-drill-mode/03-PATTERNS.md (lines 284-352, 553-597)
    - cka-sim/traps/catalog.yaml (post-Wave-1: confirm `missing-dns-egress` exists; reuse existing seeded ones for ≥3)
  </read_first>
  <action>
    `mkdir -p cka-sim/packs/services-networking/01-networkpolicy-egress`.

    File 1 — `cka-sim/packs/services-networking/manifest.yaml`:
    ```
    pack:
      id: services-networking
      domain: services-networking
      weight: 20
      description: "Services & Networking 20% pack — Phase 3 reference question only; full pack in Phase 5 (PACK-03)."
    questions:
      - id: services-networkpolicy-egress
        path: 01-networkpolicy-egress
        estimatedMinutes: 9
    ```

    File 2 — `cka-sim/packs/services-networking/README.md`:
    ```
    # Services & Networking Pack

    **Domain:** Services & Networking (20% of CKA blueprint v1.35)

    Phase 3 reference question only — full pack lands in Phase 5 (PACK-03).

    ## Questions

    | # | Slug | Topic | Time |
    |---|------|-------|------|
    | 01 | networkpolicy-egress | NetworkPolicy egress restrictions block DNS resolution | 9 min |

    ## Running

    ```bash
    cka-sim drill services-networking
    cka-sim drill services-networking 1
    ```

    > Not real CKA exam content; independently authored.
    ```

    File 3 — `cka-sim/packs/services-networking/01-networkpolicy-egress/metadata.yaml`:
    ```
    id: services-networkpolicy-egress
    domain: services-networking
    estimatedMinutes: 9
    verified_against: "1.35"
    traps:
      - missing-dns-egress
      - default-sa-used
      - hostpath-pv-without-nodeaffinity
    references:
      - kind: prior-art-exercise
        target: exercises/05-networkpolicy/
        note: Prior NetworkPolicy exercise
      - kind: k8s-doc
        target: https://kubernetes.io/docs/concepts/services-networking/network-policies/
        note: NetworkPolicy concepts
    ```
    NOTE: third trap is filler from existing seeded catalog (≥3 required); only `missing-dns-egress` is actively detected by grade.sh.

    File 4 — `cka-sim/packs/services-networking/01-networkpolicy-egress/question.md`:
    ```
    # services-networking/01-networkpolicy-egress

    **Domain:** Services & Networking  |  **Estimated time:** 9 minutes

    A `Pod` named `probe` is running in `${CKA_SIM_LAB_NS}` and a `NetworkPolicy` named `egress-restrict` is in place. The pod is supposed to be able to resolve and reach the in-cluster DNS service, but DNS resolution from the pod is failing.

    ## Tasks

    1. Inspect the `NetworkPolicy` `egress-restrict` in `${CKA_SIM_LAB_NS}`.
    2. Diagnose why the pod cannot resolve names like `kubernetes.default`. Read the policy's egress rules carefully.
    3. Modify the NetworkPolicy in place so DNS resolution from the pod succeeds.

    ## Constraints

    - Do NOT delete the NetworkPolicy — modify it in place (`kubectl edit netpol egress-restrict -n ${CKA_SIM_LAB_NS}`).
    - You must NOT widen egress beyond what is required for DNS resolution.

    ## Verify yourself

    Before typing `done`, confirm:

    ```
    kubectl exec -n ${CKA_SIM_LAB_NS} probe -- nslookup kubernetes.default 2>&1 | head -5
    ```
    This should resolve, not time out.
    ```
  </action>
  <verify>
    <automated>[[ -f cka-sim/packs/services-networking/manifest.yaml && -f cka-sim/packs/services-networking/01-networkpolicy-egress/metadata.yaml ]] && grep -q 'services-networkpolicy-egress' cka-sim/packs/services-networking/manifest.yaml</automated>
  </verify>
  <acceptance_criteria>
    - 4 files created
    - manifest.yaml weight 20
    - metadata.yaml has 3 traps; verified_against == "1.35"; estimatedMinutes 9 (in [4,12])
    - question.md does NOT spoiler the trap (does NOT contain "UDP" or "port 53"): `! grep -qiE '(UDP|port 53)' cka-sim/packs/services-networking/01-networkpolicy-egress/question.md`
  </acceptance_criteria>
  <done>Pack scaffold complete.</done>
</task>

<task type="auto">
  <name>Task 2: Create setup, grade, reset, ref-solution for services-networking/01-networkpolicy-egress</name>
  <files>cka-sim/packs/services-networking/01-networkpolicy-egress/setup.sh, cka-sim/packs/services-networking/01-networkpolicy-egress/grade.sh, cka-sim/packs/services-networking/01-networkpolicy-egress/reset.sh, cka-sim/packs/services-networking/01-networkpolicy-egress/ref-solution.sh</files>
  <read_first>
    - .planning/phases/03-runtime-contract-drill-mode/03-RESEARCH.md (Pattern 5 lines 352-364 + Assumption A3 about probe pod image)
    - .planning/phases/03-runtime-contract-drill-mode/03-PATTERNS.md (lines 422-509)
    - cka-sim/lib/traps.sh (lines 186-208 detect_missing_dns_egress signature)
    - cka-sim/packs/storage/01-pvc-binding/setup.sh (pattern reference)
  </read_first>
  <action>
    Create 4 scripts, all `chmod +x`. Probe pod image: `nicolaka/netshoot:v0.13` (has bash + nslookup; per RESEARCH Assumption A3 — busybox `sh` lacks `/dev/tcp` and may have `nslookup` but inconsistent behaviour).

    File 1 — `cka-sim/packs/services-networking/01-networkpolicy-egress/setup.sh`:
    ```
    #!/bin/bash
    # services-networking/01-networkpolicy-egress/setup.sh — NetworkPolicy with egress restrictions but no DNS allow.
    set -euo pipefail
    : "${CKA_SIM_LAB_NS:?CKA_SIM_LAB_NS must be set}"

    # 1. ns + Active wait
    kubectl apply -f - <<EOF
    apiVersion: v1
    kind: Namespace
    metadata:
      name: ${CKA_SIM_LAB_NS}
      labels:
        cka-sim/pack: services-networking
        cka-sim/question-id: services-networkpolicy-egress
    EOF
    phase=""
    for i in $(seq 1 10); do
      phase=$(kubectl get ns "$CKA_SIM_LAB_NS" -o jsonpath='{.status.phase}' 2>/dev/null || echo "")
      [[ "$phase" == "Active" ]] && break
      sleep 5
    done
    [[ "$phase" == "Active" ]] || { echo "ns not Active (phase=$phase)" >&2; exit 1; }

    # 2. Probe pod (image MUST have bash + nslookup — netshoot is the canonical choice).
    kubectl apply -f - <<EOF
    apiVersion: v1
    kind: Pod
    metadata:
      name: probe
      namespace: ${CKA_SIM_LAB_NS}
      labels:
        app: probe
    spec:
      containers:
        - name: probe
          image: nicolaka/netshoot:v0.13
          command: ["sleep", "3600"]
    EOF

    # 3. NetworkPolicy: egress-restricted, allows traffic only to a fictional 10.0.0.0/8 CIDR.
    # INTENTIONAL TRAP: no rule permits UDP/53 to kube-dns -> nslookup fails.
    kubectl apply -f - <<EOF
    apiVersion: networking.k8s.io/v1
    kind: NetworkPolicy
    metadata:
      name: egress-restrict
      namespace: ${CKA_SIM_LAB_NS}
    spec:
      podSelector:
        matchLabels:
          app: probe
      policyTypes:
        - Egress
      egress:
        - to:
            - ipBlock:
                cidr: 10.0.0.0/8
          ports:
            - protocol: TCP
              port: 80
    EOF

    # 4. Wait for probe pod to be Ready
    kubectl wait --for=condition=Ready pod/probe -n "$CKA_SIM_LAB_NS" --timeout=60s 2>/dev/null || true
    ```

    File 2 — `cka-sim/packs/services-networking/01-networkpolicy-egress/grade.sh`:
    ```
    #!/bin/bash
    # services-networking/01-networkpolicy-egress/grade.sh
    set -uo pipefail
    : "${CKA_SIM_LAB_NS:?CKA_SIM_LAB_NS must be set}"
    : "${CKA_SIM_ROOT:?CKA_SIM_ROOT must be set}"

    # shellcheck source=../../../lib/grade.sh disable=SC1091
    source "$CKA_SIM_ROOT/lib/grade.sh"
    # shellcheck source=../../../lib/traps.sh disable=SC1091
    source "$CKA_SIM_ROOT/lib/traps.sh"

    # Wait briefly for pod readiness (best-effort; subsequent checks tolerate not-Ready)
    kubectl wait --for=condition=Ready pod/probe -n "$CKA_SIM_LAB_NS" --timeout=30s 2>/dev/null || true

    # Assertion 1: NetworkPolicy exists
    cka_sim::grade::assert_resource_exists networkpolicy egress-restrict -n "$CKA_SIM_LAB_NS"

    # Assertion 2: probe pod Ready
    cka_sim::grade::assert_pod_ready "$CKA_SIM_LAB_NS" "probe"

    # Assertion 3: DNS resolution works in-pod (custom probe — not assert_egress_allowed because /dev/tcp is TCP-only).
    # Manual increment + named pass/fail message to mirror the helper contract.
    CKA_SIM_GRADE_TOTAL=$(( CKA_SIM_GRADE_TOTAL + 1 ))
    if kubectl exec -n "$CKA_SIM_LAB_NS" probe -- nslookup kubernetes.default >/dev/null 2>&1; then
      CKA_SIM_GRADE_PASSED=$(( CKA_SIM_GRADE_PASSED + 1 ))
      CKA_SIM_GRADE_PASSES+=("DNS resolution from pod 'probe' works")
      ok "DNS resolution from pod 'probe' works"
    else
      CKA_SIM_GRADE_FAILS+=("DNS resolution from pod 'probe' failed (nslookup kubernetes.default)")
      err "DNS resolution from pod 'probe' failed (nslookup kubernetes.default)"
    fi

    # Trap detector: NetworkPolicy egress restricted but no DNS allow.
    tid=$(cka_sim::trap::detect_missing_dns_egress "$CKA_SIM_LAB_NS" "egress-restrict")
    [[ -n "$tid" ]] && cka_sim::grade::record_trap "$tid"

    cka_sim::grade::emit_result
    ```

    File 3 — `cka-sim/packs/services-networking/01-networkpolicy-egress/reset.sh`:
    ```
    #!/bin/bash
    set -uo pipefail
    : "${CKA_SIM_LAB_NS:?CKA_SIM_LAB_NS must be set}"
    kubectl delete namespace "$CKA_SIM_LAB_NS" --ignore-not-found --wait=false
    # No cluster-scoped resources for this question
    exit 0
    ```

    File 4 — `cka-sim/packs/services-networking/01-networkpolicy-egress/ref-solution.sh`:
    ```
    #!/bin/bash
    # services-networking/01-networkpolicy-egress/ref-solution.sh — adds DNS egress allow rule.
    set -euo pipefail
    : "${CKA_SIM_LAB_NS:?CKA_SIM_LAB_NS must be set}"

    # Re-apply the NetworkPolicy with an additional egress rule that permits UDP/53 to kube-dns
    # (and the kube-system ns selector — production-correct DNS allow).
    kubectl apply -f - <<EOF
    apiVersion: networking.k8s.io/v1
    kind: NetworkPolicy
    metadata:
      name: egress-restrict
      namespace: ${CKA_SIM_LAB_NS}
    spec:
      podSelector:
        matchLabels:
          app: probe
      policyTypes:
        - Egress
      egress:
        - to:
            - ipBlock:
                cidr: 10.0.0.0/8
          ports:
            - protocol: TCP
              port: 80
        - to:
            - namespaceSelector:
                matchLabels:
                  kubernetes.io/metadata.name: kube-system
              podSelector:
                matchLabels:
                  k8s-app: kube-dns
          ports:
            - protocol: UDP
              port: 53
            - protocol: TCP
              port: 53
    EOF
    ```

    `chmod +x cka-sim/packs/services-networking/01-networkpolicy-egress/*.sh`.
  </action>
  <verify>
    <automated>chmod +x cka-sim/packs/services-networking/01-networkpolicy-egress/*.sh && for f in cka-sim/packs/services-networking/01-networkpolicy-egress/*.sh; do bash -n "$f" || exit 1; done && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - 4 scripts exist + executable
    - Syntax valid (bash -n on all 4)
    - setup.sh uses `nicolaka/netshoot:v0.13` image (Assumption A3): `grep -q 'nicolaka/netshoot:v0.13' cka-sim/packs/services-networking/01-networkpolicy-egress/setup.sh`
    - setup.sh uses NetworkPolicy apiVersion `networking.k8s.io/v1` (CONVENTIONS.md): `grep -q 'networking.k8s.io/v1' cka-sim/packs/services-networking/01-networkpolicy-egress/setup.sh`
    - grade.sh uses custom nslookup probe (NOT assert_egress_allowed which is TCP-only): `grep -q 'kubectl exec.*nslookup' cka-sim/packs/services-networking/01-networkpolicy-egress/grade.sh && ! grep -q 'assert_egress_allowed' cka-sim/packs/services-networking/01-networkpolicy-egress/grade.sh`
    - grade.sh calls detect_missing_dns_egress + record_trap
    - ref-solution.sh adds UDP/53 rule: `grep -q 'protocol: UDP' cka-sim/packs/services-networking/01-networkpolicy-egress/ref-solution.sh && grep -q 'port: 53' cka-sim/packs/services-networking/01-networkpolicy-egress/ref-solution.sh`
    - lint-packs.sh accepts pack: `bash cka-sim/scripts/test.sh` exits 0
  </acceptance_criteria>
  <done>services-networking pack reference question complete.</done>
</task>

</tasks>

<verification>
- 8 files created
- `bash cka-sim/scripts/test.sh` exits 0
- Image pinning followed (nicolaka/netshoot:v0.13, not :latest)
- apiVersion pinning followed (networking.k8s.io/v1)
- DNS probe is the custom-nslookup form (Pattern 5 footnote)
</verification>

<success_criteria>
- services-networking/01-networkpolicy-egress is fully drillable
- Trap mapping: missing-dns-egress (seeded, primary) + 2 fillers from seeded catalog
- Custom nslookup probe avoids the /dev/tcp limitation in assert_egress_allowed
</success_criteria>

<output>
After completion, create `.planning/phases/03-runtime-contract-drill-mode/03-06-SUMMARY.md` covering files, trap mapping, image-pinning rationale (Assumption A3), human-verification procedure.
</output>
