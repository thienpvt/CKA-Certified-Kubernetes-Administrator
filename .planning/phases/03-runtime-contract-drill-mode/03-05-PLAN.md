---
phase: 03-runtime-contract-drill-mode
plan: 05
type: execute
wave: 3
depends_on: [01, 03]
files_modified:
  - cka-sim/packs/workloads-scheduling/manifest.yaml
  - cka-sim/packs/workloads-scheduling/README.md
  - cka-sim/packs/workloads-scheduling/01-deployment-requests/metadata.yaml
  - cka-sim/packs/workloads-scheduling/01-deployment-requests/question.md
  - cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh
  - cka-sim/packs/workloads-scheduling/01-deployment-requests/grade.sh
  - cka-sim/packs/workloads-scheduling/01-deployment-requests/reset.sh
  - cka-sim/packs/workloads-scheduling/01-deployment-requests/ref-solution.sh
autonomous: true
requirements: [TRIP-01, TRIP-02, TRIP-03, TRIP-04, TRIP-05, TRIP-06, GRADE-02, GRADE-03, GRADE-04, GRADE-06, RUN-02]
must_haves:
  truths:
    - "workloads-scheduling pack has manifest + README + 1 question dir"
    - "01-deployment-requests has all 6 required files"
    - "setup.sh creates ns + a Deployment with NO resources.requests + uses default ServiceAccount (the trap)"
    - "grade.sh asserts deployment Available + has resources.requests.cpu + uses dedicated SA; records default-sa-used trap"
    - "ref-solution.sh creates a dedicated ServiceAccount + patches Deployment to use it AND adds resources.requests"
    - "metadata.yaml declares 3 trap-ids registered in catalog"
  artifacts:
    - path: "cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh"
      provides: "Deployment with default SA + no requests (the trap)"
      contains: "kubectl apply -f -"
    - path: "cka-sim/packs/workloads-scheduling/01-deployment-requests/grade.sh"
      provides: "Deployment Available assertion + resources.requests assertion + default-sa-used detector"
      contains: "cka_sim::trap::detect_default_sa_used"
  key_links:
    - from: "cka-sim/packs/workloads-scheduling/01-deployment-requests/grade.sh"
      to: "cka-sim/lib/grade.sh + cka-sim/lib/traps.sh"
      via: "source + assert helpers + detector"
      pattern: "detect_default_sa_used"
---

<objective>
Ship workloads-scheduling pack with 1 reference question. Per CONTEXT D-10: the trap is `default-sa-used` (Deployment uses the default ServiceAccount). Per RESEARCH Pattern 5 lines 337-350 + Assumption A4: include `kubectl wait` for deployment Available BEFORE the detector call (the detector reads from a Pod which must be scheduled first).

Output: 8 new files (2 pack-level + 6 question).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-runtime-contract-drill-mode/03-CONTEXT.md
@.planning/phases/03-runtime-contract-drill-mode/03-RESEARCH.md
@.planning/phases/03-runtime-contract-drill-mode/03-PATTERNS.md
@cka-sim/lib/grade.sh
@cka-sim/lib/traps.sh
@cka-sim/scripts/lint-packs.sh
@cka-sim/traps/catalog.yaml
</context>

<interfaces>
From cka-sim/lib/grade.sh:
- cka_sim::grade::assert_resource_exists <kind> <name> [-n <ns>] — passes iff resource exists
- cka_sim::grade::assert_field_eq <kind> <name> <jsonpath> <expected> [-n <ns>]
- cka_sim::grade::record_trap <id>
- cka_sim::grade::emit_result

From cka-sim/lib/traps.sh:
- cka_sim::trap::detect_default_sa_used <ns> <pod-name> — echoes "default-sa-used" if pod's serviceAccountName is empty or "default" (lines 170-178)

Env-vars set by drill.sh:
- CKA_SIM_LAB_NS = "cka-sim-workloads-scheduling-01"
- CKA_SIM_ROOT
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create workloads-scheduling pack scaffold (manifest, README, metadata, question)</name>
  <files>cka-sim/packs/workloads-scheduling/manifest.yaml, cka-sim/packs/workloads-scheduling/README.md, cka-sim/packs/workloads-scheduling/01-deployment-requests/metadata.yaml, cka-sim/packs/workloads-scheduling/01-deployment-requests/question.md</files>
  <read_first>
    - .planning/phases/03-runtime-contract-drill-mode/03-CONTEXT.md (D-01, D-02, D-10 workloads row)
    - .planning/phases/03-runtime-contract-drill-mode/03-PATTERNS.md (lines 284-352, 553-597)
    - cka-sim/scripts/lint-packs.sh (schema lint reference)
    - cka-sim/traps/catalog.yaml (post-Wave-1: confirm `default-sa-used` and `deployment-missing-requests` exist)
  </read_first>
  <action>
    `mkdir -p cka-sim/packs/workloads-scheduling/01-deployment-requests`.

    File 1 — `cka-sim/packs/workloads-scheduling/manifest.yaml`:
    ```
    pack:
      id: workloads-scheduling
      domain: workloads-scheduling
      weight: 15
      description: "Workloads & Scheduling 15% pack — Phase 3 reference question only; full pack in Phase 4 (PACK-02)."
    questions:
      - id: workloads-deployment-requests
        path: 01-deployment-requests
        estimatedMinutes: 7
    ```

    File 2 — `cka-sim/packs/workloads-scheduling/README.md`:
    ```
    # Workloads & Scheduling Pack

    **Domain:** Workloads & Scheduling (15% of CKA blueprint v1.35)

    Phase 3 reference question only — full pack lands in Phase 4 (PACK-02).

    ## Questions

    | # | Slug | Topic | Time |
    |---|------|-------|------|
    | 01 | deployment-requests | Deployment uses default SA and has no resource requests | 7 min |

    ## Running

    ```bash
    cka-sim drill workloads-scheduling
    cka-sim drill workloads-scheduling 1
    ```

    > Not real CKA exam content; independently authored.
    ```

    File 3 — `cka-sim/packs/workloads-scheduling/01-deployment-requests/metadata.yaml`:
    ```
    id: workloads-deployment-requests
    domain: workloads-scheduling
    estimatedMinutes: 7
    verified_against: "1.35"
    traps:
      - default-sa-used
      - deployment-missing-requests
      - hostpath-pv-without-nodeaffinity
    references:
      - kind: prior-art-exercise
        target: exercises/16-hpa/
        note: HPA exercise mentions resources.requests prereq
      - kind: k8s-doc
        target: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
        note: Container resource requests
    ```
    NOTE: third trap is filler from existing catalog (≥3 required); the question's grade.sh only actively detects the first two. `hostpath-pv-without-nodeaffinity` is included to meet the ≥3 threshold without requiring catalog extension beyond Wave 1; it's not exercised by this question's grade.sh.

    File 4 — `cka-sim/packs/workloads-scheduling/01-deployment-requests/question.md`:
    ```
    # workloads-scheduling/01-deployment-requests

    **Domain:** Workloads & Scheduling  |  **Estimated time:** 7 minutes

    A `Deployment` named `load-app` exists in your lab namespace. The platform team has decided that:
    - Every workload MUST run under its own dedicated `ServiceAccount` (no shared `default` SA).
    - Every container MUST declare CPU and memory `resources.requests` so the scheduler can place it correctly.

    ## Tasks

    1. Inspect the Deployment `load-app` in `${CKA_SIM_LAB_NS}` and the pods it manages.
    2. Create a `ServiceAccount` named `load-app-sa` in `${CKA_SIM_LAB_NS}`.
    3. Update the Deployment so its pods use `load-app-sa` (not the default).
    4. Add `resources.requests.cpu: 50m` and `resources.requests.memory: 64Mi` to the container spec.
    5. Wait for the Deployment to be Available with the new pod template.

    ## Constraints

    - Do NOT delete the Deployment — modify it in place (`kubectl edit` / `kubectl patch` / re-`apply`).
    - The image must remain `nginx:1.27`.

    ## Verify yourself

    Before typing `done`, confirm:

    ```
    kubectl get sa load-app-sa -n ${CKA_SIM_LAB_NS}
    kubectl get deployment load-app -n ${CKA_SIM_LAB_NS} -o jsonpath='{.spec.template.spec.serviceAccountName}'
    kubectl get deployment load-app -n ${CKA_SIM_LAB_NS} -o jsonpath='{.spec.template.spec.containers[0].resources.requests}'
    ```
    ```
  </action>
  <verify>
    <automated>[[ -f cka-sim/packs/workloads-scheduling/manifest.yaml && -f cka-sim/packs/workloads-scheduling/README.md && -f cka-sim/packs/workloads-scheduling/01-deployment-requests/metadata.yaml && -f cka-sim/packs/workloads-scheduling/01-deployment-requests/question.md ]] && grep -q 'workloads-deployment-requests' cka-sim/packs/workloads-scheduling/manifest.yaml</automated>
  </verify>
  <acceptance_criteria>
    - 4 files created
    - manifest.yaml weight is 15
    - metadata.yaml has 3 traps; verified_against == "1.35"; estimatedMinutes 7 (in [4,12])
    - question.md uses `${CKA_SIM_LAB_NS}` placeholders
  </acceptance_criteria>
  <done>Pack scaffold complete.</done>
</task>

<task type="auto">
  <name>Task 2: Create setup, grade, reset, ref-solution for workloads-scheduling/01-deployment-requests</name>
  <files>cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh, cka-sim/packs/workloads-scheduling/01-deployment-requests/grade.sh, cka-sim/packs/workloads-scheduling/01-deployment-requests/reset.sh, cka-sim/packs/workloads-scheduling/01-deployment-requests/ref-solution.sh</files>
  <read_first>
    - .planning/phases/03-runtime-contract-drill-mode/03-RESEARCH.md (Pattern 5 lines 337-350 detector wiring; Assumption A4 about kubectl wait)
    - .planning/phases/03-runtime-contract-drill-mode/03-PATTERNS.md (lines 422-509)
    - cka-sim/lib/traps.sh (lines 170-178 detect_default_sa_used signature: takes ns + pod-name)
    - cka-sim/lib/grade.sh (assert helpers)
    - cka-sim/packs/storage/01-pvc-binding/setup.sh (just-created — pattern reference for the ns wait loop)
  </read_first>
  <action>
    Create 4 scripts, all `chmod +x`.

    File 1 — `cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh`:
    ```
    #!/bin/bash
    # workloads-scheduling/01-deployment-requests/setup.sh — Deployment WITH default SA, NO requests (the traps).
    set -euo pipefail
    : "${CKA_SIM_LAB_NS:?CKA_SIM_LAB_NS must be set by drill runner}"

    # 1. Idempotent ns + Active wait
    kubectl apply -f - <<EOF
    apiVersion: v1
    kind: Namespace
    metadata:
      name: ${CKA_SIM_LAB_NS}
      labels:
        cka-sim/pack: workloads-scheduling
        cka-sim/question-id: workloads-deployment-requests
    EOF
    phase=""
    for i in $(seq 1 10); do
      phase=$(kubectl get ns "$CKA_SIM_LAB_NS" -o jsonpath='{.status.phase}' 2>/dev/null || echo "")
      [[ "$phase" == "Active" ]] && break
      sleep 5
    done
    [[ "$phase" == "Active" ]] || { echo "ns not Active (phase=$phase)" >&2; exit 1; }

    # 2. Deployment with NO resources.requests AND NO serviceAccountName (defaults to "default" SA — the trap).
    kubectl apply -f - <<EOF
    apiVersion: apps/v1
    kind: Deployment
    metadata:
      name: load-app
      namespace: ${CKA_SIM_LAB_NS}
      labels:
        app: load-app
    spec:
      replicas: 1
      selector:
        matchLabels:
          app: load-app
      template:
        metadata:
          labels:
            app: load-app
        spec:
          containers:
            - name: app
              image: nginx:1.27
              ports:
                - containerPort: 80
    EOF
    ```

    File 2 — `cka-sim/packs/workloads-scheduling/01-deployment-requests/grade.sh`:
    ```
    #!/bin/bash
    # workloads-scheduling/01-deployment-requests/grade.sh
    set -uo pipefail
    : "${CKA_SIM_LAB_NS:?CKA_SIM_LAB_NS must be set}"
    : "${CKA_SIM_ROOT:?CKA_SIM_ROOT must be set}"

    # shellcheck source=../../../lib/grade.sh disable=SC1091
    source "$CKA_SIM_ROOT/lib/grade.sh"
    # shellcheck source=../../../lib/traps.sh disable=SC1091
    source "$CKA_SIM_ROOT/lib/traps.sh"

    # Wait up to 60s for the Deployment to be Available before reading pod state (RESEARCH Assumption A4).
    kubectl wait --for=condition=Available deployment/load-app -n "$CKA_SIM_LAB_NS" --timeout=60s 2>/dev/null || true

    # Assertion 1: deployment exists
    cka_sim::grade::assert_resource_exists deployment load-app -n "$CKA_SIM_LAB_NS"

    # Assertion 2: deployment uses dedicated SA "load-app-sa"
    cka_sim::grade::assert_field_eq deployment load-app \
      '{.spec.template.spec.serviceAccountName}' \
      'load-app-sa' \
      -n "$CKA_SIM_LAB_NS"

    # Assertion 3: container has resources.requests.cpu == 50m
    cka_sim::grade::assert_field_eq deployment load-app \
      '{.spec.template.spec.containers[0].resources.requests.cpu}' \
      '50m' \
      -n "$CKA_SIM_LAB_NS"

    # Assertion 4: container has resources.requests.memory == 64Mi
    cka_sim::grade::assert_field_eq deployment load-app \
      '{.spec.template.spec.containers[0].resources.requests.memory}' \
      '64Mi' \
      -n "$CKA_SIM_LAB_NS"

    # Trap detector: probe the deployment's first running pod for default-sa-used.
    pod=$(kubectl get pod -n "$CKA_SIM_LAB_NS" -l app=load-app -o jsonpath='{.items[0].metadata.name}' 2>/dev/null)
    if [[ -n "$pod" ]]; then
      tid=$(cka_sim::trap::detect_default_sa_used "$CKA_SIM_LAB_NS" "$pod")
      [[ -n "$tid" ]] && cka_sim::grade::record_trap "$tid"
    fi

    cka_sim::grade::emit_result
    ```

    File 3 — `cka-sim/packs/workloads-scheduling/01-deployment-requests/reset.sh`:
    ```
    #!/bin/bash
    # workloads-scheduling/01-deployment-requests/reset.sh
    set -uo pipefail
    : "${CKA_SIM_LAB_NS:?CKA_SIM_LAB_NS must be set}"

    # Async ns delete (cleans up Deployment, SA, all namespaced resources)
    kubectl delete namespace "$CKA_SIM_LAB_NS" --ignore-not-found --wait=false

    # No cluster-scoped resources for this question
    exit 0
    ```

    File 4 — `cka-sim/packs/workloads-scheduling/01-deployment-requests/ref-solution.sh`:
    ```
    #!/bin/bash
    # workloads-scheduling/01-deployment-requests/ref-solution.sh — creates SA + patches Deployment.
    set -euo pipefail
    : "${CKA_SIM_LAB_NS:?CKA_SIM_LAB_NS must be set}"

    # 1. Create the dedicated SA
    kubectl apply -f - <<EOF
    apiVersion: v1
    kind: ServiceAccount
    metadata:
      name: load-app-sa
      namespace: ${CKA_SIM_LAB_NS}
    EOF

    # 2. Patch Deployment: set serviceAccountName + add resources.requests
    kubectl patch deployment load-app -n "$CKA_SIM_LAB_NS" --type=strategic -p='{
      "spec": {
        "template": {
          "spec": {
            "serviceAccountName": "load-app-sa",
            "containers": [{
              "name": "app",
              "resources": {
                "requests": {
                  "cpu": "50m",
                  "memory": "64Mi"
                }
              }
            }]
          }
        }
      }
    }'

    # 3. Wait for new pods to roll out
    kubectl rollout status deployment/load-app -n "$CKA_SIM_LAB_NS" --timeout=60s
    ```

    `chmod +x cka-sim/packs/workloads-scheduling/01-deployment-requests/*.sh`.
  </action>
  <verify>
    <automated>chmod +x cka-sim/packs/workloads-scheduling/01-deployment-requests/*.sh && for f in cka-sim/packs/workloads-scheduling/01-deployment-requests/*.sh; do bash -n "$f" || exit 1; done && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - 4 scripts exist + executable
    - All bash -n syntax-clean
    - grade.sh sources lib/grade.sh AND lib/traps.sh
    - grade.sh has `kubectl wait --for=condition=Available` BEFORE detector call (Assumption A4)
    - grade.sh ends with emit_result
    - setup.sh has NO `kubectl create` (apply heredoc only): `! grep -E '^[[:space:]]*kubectl[[:space:]]+create[[:space:]]' cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh`
    - ref-solution.sh creates SA + patches deployment
    - lint-packs.sh accepts pack: `bash cka-sim/scripts/test.sh` exits 0
  </acceptance_criteria>
  <done>Workloads pack reference question complete.</done>
</task>

</tasks>

<verification>
- 8 files created
- `bash cka-sim/scripts/test.sh` exits 0 (lint + tests still pass)
- No `kubectl create`, no `kubectl get | grep`, no mutating verbs in grade.sh, no `kubectl delete ns` in setup.sh
</verification>

<success_criteria>
- workloads-scheduling/01-deployment-requests is fully drillable
- Trap mapping: default-sa-used (seeded, primary) + deployment-missing-requests (new from Wave 1) + hostpath-pv-without-nodeaffinity (filler for ≥3)
- GRADE-06 round-trip is human-verified (per Open Q1)
</success_criteria>

<output>
After completion, create `.planning/phases/03-runtime-contract-drill-mode/03-05-SUMMARY.md` covering files created, trap mapping, lint-packs status, human-verification procedure.
</output>
