---
phase: 03-runtime-contract-drill-mode
plan: 08
type: execute
wave: 3
depends_on: [01, 03]
files_modified:
  - cka-sim/packs/troubleshooting/manifest.yaml
  - cka-sim/packs/troubleshooting/README.md
  - cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/metadata.yaml
  - cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/question.md
  - cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/setup.sh
  - cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/grade.sh
  - cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/reset.sh
  - cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/ref-solution.sh
autonomous: true
requirements: [TRIP-01, TRIP-02, TRIP-03, TRIP-04, TRIP-05, TRIP-06, GRADE-02, GRADE-03, GRADE-04, GRADE-06, RUN-02]
must_haves:
  truths:
    - "troubleshooting pack has manifest + README + 1 question dir with all 6 files"
    - "setup.sh creates ns + Deployment (pod label app=web) + Service (selector app=webserver) — selector MISMATCH (the trap) → Service has no endpoints"
    - "grade.sh asserts Deployment exists, pods Ready, Service exists, AND Service endpoints nonempty (the core check) + records service-label-mismatch"
    - "ref-solution.sh patches the Service selector to match the Deployment's pod labels"
    - "metadata.yaml declares 3 trap-ids (service-label-mismatch + 2 fillers registered in catalog)"
  artifacts:
    - path: "cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/setup.sh"
      provides: "Deployment + Service with intentional label/selector mismatch"
      contains: "kind: Service"
    - path: "cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/grade.sh"
      provides: "assert_endpoints_nonempty + detect_service_label_mismatch"
      contains: "cka_sim::grade::assert_endpoints_nonempty"
    - path: "cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/ref-solution.sh"
      provides: "Patches Service.spec.selector to match Deployment pod labels"
      contains: "kubectl patch service"
  key_links:
    - from: "cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/grade.sh"
      to: "cka-sim/lib/grade.sh + cka-sim/lib/traps.sh"
      via: "source + assert_endpoints_nonempty + detect_service_label_mismatch"
      pattern: "source.*lib/(grade|traps).sh"
---

<objective>
Ship troubleshooting pack with 1 reference question. Per CONTEXT D-10 troubleshooting row: the trap is a Deployment pod label set that does NOT match the Service selector — endpoints remain empty. Detector: `service-label-mismatch` (new catalog entry added by Plan 01).

Trap mechanic: setup.sh creates a Deployment with pod labels `app=web` and a Service with selector `app=webserver`. The Service's Endpoints object is empty. The fix is to patch the Service selector to `app=web` (or alternatively re-label the pods — but the constraint in question.md steers toward Service modification).

Output: 8 new files (2 pack-level + 6 question).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-runtime-contract-drill-mode/03-CONTEXT.md
@.planning/phases/03-runtime-contract-drill-mode/03-RESEARCH.md
@.planning/phases/03-runtime-contract-drill-mode/03-PATTERNS.md
@cka-sim/lib/grade.sh
@cka-sim/lib/traps.sh
@cka-sim/scripts/lint-packs.sh
@cka-sim/traps/catalog.yaml
</context>

<interfaces>
From cka-sim/lib/grade.sh:
- cka_sim::grade::assert_resource_exists <kind> <name> [-n <ns>]
- cka_sim::grade::assert_pod_ready <ns> <pod> [<weight>]
- cka_sim::grade::assert_endpoints_nonempty <ns> <service> [<weight>] — passes iff Endpoints object for the Service has at least one subset with addresses
- cka_sim::grade::record_trap <id>
- cka_sim::grade::emit_result

From cka-sim/lib/traps.sh (post-Plan-01):
- cka_sim::trap::detect_service_label_mismatch <ns> <service-name> — echoes "service-label-mismatch" iff Service.spec.selector does not match ANY pod in the namespace (or Endpoints is empty despite matching pods existing). Implementation per Plan 01.

Env-vars drill.sh sets:
- CKA_SIM_LAB_NS = "cka-sim-troubleshooting-01"
- CKA_SIM_QUESTION_ID = "troubleshooting-deploy-svc-mismatch"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create troubleshooting pack scaffold</name>
  <files>cka-sim/packs/troubleshooting/manifest.yaml, cka-sim/packs/troubleshooting/README.md, cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/metadata.yaml, cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/question.md</files>
  <read_first>
    - .planning/phases/03-runtime-contract-drill-mode/03-CONTEXT.md (D-10 troubleshooting row)
    - .planning/phases/03-runtime-contract-drill-mode/03-PATTERNS.md (lines 284-352 manifest/metadata, 553-597 question.md)
    - cka-sim/traps/catalog.yaml (post-Plan-01: confirm service-label-mismatch exists)
    - cka-sim/scripts/lint-packs.sh (post-Plan-03: schema lint)
  </read_first>
  <action>
    `mkdir -p cka-sim/packs/troubleshooting/01-deploy-svc-mismatch`.

    File 1 — `cka-sim/packs/troubleshooting/manifest.yaml`:
    ```
    pack:
      id: troubleshooting
      domain: troubleshooting
      weight: 30
      description: "Troubleshooting 30% pack — Phase 3 reference question only; full pack in Phase 6 (PACK-05)."
    questions:
      - id: troubleshooting-deploy-svc-mismatch
        path: 01-deploy-svc-mismatch
        estimatedMinutes: 7
    ```

    File 2 — `cka-sim/packs/troubleshooting/README.md`:
    ```
    # Troubleshooting Pack

    **Domain:** Troubleshooting (30% of CKA blueprint v1.35)

    Phase 3 reference question only — full pack lands in Phase 6 (PACK-05).

    ## Questions

    | # | Slug | Topic | Time |
    |---|------|-------|------|
    | 01 | deploy-svc-mismatch | Deployment pod labels don't match Service selector; endpoints empty | 7 min |

    ## Running

    ```bash
    cka-sim drill troubleshooting
    cka-sim drill troubleshooting 1
    ```

    > Not real CKA exam content; independently authored.
    ```

    File 3 — `cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/metadata.yaml`:
    ```
    id: troubleshooting-deploy-svc-mismatch
    domain: troubleshooting
    estimatedMinutes: 7
    verified_against: "1.35"
    traps:
      - service-label-mismatch
      - default-sa-used
      - missing-dns-egress
    references:
      - kind: prior-art-exercise
        target: exercises/11-troubleshoot-cluster/
        note: Prior troubleshoot exercise
      - kind: k8s-doc
        target: https://kubernetes.io/docs/tasks/debug/debug-application/debug-service/
        note: Debug Services (selector/endpoints)
    ```

    File 4 — `cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/question.md`:
    ```
    # troubleshooting/01-deploy-svc-mismatch

    **Domain:** Troubleshooting  |  **Estimated time:** 7 minutes

    A `Deployment` named `web` and a `Service` named `web-svc` exist in your lab namespace. Users report they cannot reach the Service. The pods are Running but the Service is not routing traffic.

    ## Tasks

    1. Inspect the `Deployment` `web`, its Pods, and the `Service` `web-svc` in `${CKA_SIM_LAB_NS}`.
    2. Diagnose why `Service web-svc` has no endpoints despite the Deployment's Pods being Ready.
    3. Modify the `Service` (not the Deployment) so that the Service routes traffic to the Deployment's Pods.

    ## Constraints

    - Do NOT modify the Deployment, its pod template, or any pod labels.
    - Do NOT recreate the Service — patch it in place.
    - The Pods should remain unchanged (same labels, same image, same replica count).

    ## Verify yourself

    Before typing `done`, confirm:

    ```
    kubectl get endpoints web-svc -n ${CKA_SIM_LAB_NS}
    # ENDPOINTS column should list pod IPs, not <none>
    ```
    ```
  </action>
  <verify>
    <automated>[[ -f cka-sim/packs/troubleshooting/manifest.yaml && -f cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/metadata.yaml ]] && grep -q 'service-label-mismatch' cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/metadata.yaml</automated>
  </verify>
  <acceptance_criteria>
    - 4 files created
    - manifest.yaml weight 30
    - metadata.yaml has exactly 3 traps; first is `service-label-mismatch`; verified_against "1.35"; estimatedMinutes 7 (in [4,12])
    - question.md does NOT spoiler the trap (no "label", "selector", "match"): `! grep -qiE '(label|selector|match)' cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/question.md`
    - question.md steers toward Service modification: `grep -q 'Modify the ' cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/question.md`
  </acceptance_criteria>
  <done>Pack scaffold complete.</done>
</task>

<task type="auto">
  <name>Task 2: Create setup, grade, reset, ref-solution for troubleshooting/01-deploy-svc-mismatch</name>
  <files>cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/setup.sh, cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/grade.sh, cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/reset.sh, cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/ref-solution.sh</files>
  <read_first>
    - .planning/phases/03-runtime-contract-drill-mode/03-RESEARCH.md (Pattern 4/5 setup.sh shape; Pitfall 3 bash -n check)
    - .planning/phases/03-runtime-contract-drill-mode/03-PATTERNS.md (lines 422-509 setup/reset/ref-solution; 355-419 grade.sh)
    - cka-sim/lib/grade.sh (assert_endpoints_nonempty signature)
    - cka-sim/lib/traps.sh (post-Plan-01: detect_service_label_mismatch signature)
    - cka-sim/packs/storage/01-pvc-binding/setup.sh (pattern — ns wait loop)
  </read_first>
  <action>
    Create 4 scripts, all `chmod +x`. Pod image: pinned to `nginx:1.27-alpine` (stable, small, readiness-probe-friendly — Assumption A3 pattern).

    File 1 — `cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/setup.sh`:
    ```
    #!/bin/bash
    # troubleshooting/01-deploy-svc-mismatch/setup.sh — Deployment labels != Service selector (the trap).
    set -euo pipefail
    : "${CKA_SIM_LAB_NS:?CKA_SIM_LAB_NS must be set}"

    # 1. ns + Active wait
    kubectl apply -f - <<EOF
    apiVersion: v1
    kind: Namespace
    metadata:
      name: ${CKA_SIM_LAB_NS}
      labels:
        cka-sim/pack: troubleshooting
        cka-sim/question-id: troubleshooting-deploy-svc-mismatch
    EOF
    phase=""
    for i in $(seq 1 10); do
      phase=$(kubectl get ns "$CKA_SIM_LAB_NS" -o jsonpath='{.status.phase}' 2>/dev/null || echo "")
      [[ "$phase" == "Active" ]] && break
      sleep 5
    done
    [[ "$phase" == "Active" ]] || { echo "ns not Active (phase=$phase)" >&2; exit 1; }

    # 2. Deployment 'web' with pod label app=web
    kubectl apply -f - <<EOF
    apiVersion: apps/v1
    kind: Deployment
    metadata:
      name: web
      namespace: ${CKA_SIM_LAB_NS}
    spec:
      replicas: 2
      selector:
        matchLabels:
          app: web
      template:
        metadata:
          labels:
            app: web
        spec:
          containers:
            - name: web
              image: nginx:1.27-alpine
              ports:
                - containerPort: 80
              readinessProbe:
                httpGet:
                  path: /
                  port: 80
                initialDelaySeconds: 2
                periodSeconds: 3
    EOF

    # 3. Service 'web-svc' — INTENTIONAL TRAP: selector app=webserver (does NOT match deployment pod label app=web).
    kubectl apply -f - <<EOF
    apiVersion: v1
    kind: Service
    metadata:
      name: web-svc
      namespace: ${CKA_SIM_LAB_NS}
    spec:
      type: ClusterIP
      selector:
        app: webserver
      ports:
        - protocol: TCP
          port: 80
          targetPort: 80
    EOF

    # 4. Wait briefly for pods (best-effort — grade.sh re-checks)
    kubectl wait --for=condition=Available deployment/web -n "$CKA_SIM_LAB_NS" --timeout=60s 2>/dev/null || true
    ```

    File 2 — `cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/grade.sh`:
    ```
    #!/bin/bash
    # troubleshooting/01-deploy-svc-mismatch/grade.sh
    set -uo pipefail
    : "${CKA_SIM_LAB_NS:?CKA_SIM_LAB_NS must be set}"
    : "${CKA_SIM_ROOT:?CKA_SIM_ROOT must be set}"

    # shellcheck source=../../../lib/grade.sh disable=SC1091
    source "$CKA_SIM_ROOT/lib/grade.sh"
    # shellcheck source=../../../lib/traps.sh disable=SC1091
    source "$CKA_SIM_ROOT/lib/traps.sh"

    kubectl wait --for=condition=Available deployment/web -n "$CKA_SIM_LAB_NS" --timeout=30s 2>/dev/null || true

    cka_sim::grade::assert_resource_exists deployment web -n "$CKA_SIM_LAB_NS"
    cka_sim::grade::assert_resource_exists service web-svc -n "$CKA_SIM_LAB_NS"

    # Core: Service endpoints must be non-empty for the Service to actually route traffic.
    # This is the single assertion that flips from fail (under trap) to pass (under fix).
    cka_sim::grade::assert_endpoints_nonempty "$CKA_SIM_LAB_NS" "web-svc"

    # Trap detector: Service.spec.selector doesn't match any pod (or endpoints empty despite matching pods).
    tid=$(cka_sim::trap::detect_service_label_mismatch "$CKA_SIM_LAB_NS" "web-svc")
    [[ -n "$tid" ]] && cka_sim::grade::record_trap "$tid"

    cka_sim::grade::emit_result
    ```

    File 3 — `cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/reset.sh`:
    ```
    #!/bin/bash
    set -uo pipefail
    : "${CKA_SIM_LAB_NS:?CKA_SIM_LAB_NS must be set}"
    kubectl delete namespace "$CKA_SIM_LAB_NS" --ignore-not-found --wait=false
    exit 0
    ```

    File 4 — `cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/ref-solution.sh`:
    ```
    #!/bin/bash
    # troubleshooting/01-deploy-svc-mismatch/ref-solution.sh — patches Service selector to match the Deployment pod labels.
    set -euo pipefail
    : "${CKA_SIM_LAB_NS:?CKA_SIM_LAB_NS must be set}"

    kubectl patch service web-svc -n "$CKA_SIM_LAB_NS" \
      --type='merge' \
      -p '{"spec":{"selector":{"app":"web"}}}'
    ```

    `chmod +x cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/*.sh`.
  </action>
  <verify>
    <automated>chmod +x cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/*.sh && for f in cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/*.sh; do bash -n "$f" || exit 1; done && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - 4 scripts exist + executable
    - Syntax valid (bash -n on all 4)
    - setup.sh creates Service with selector `app: webserver` (mismatch with deployment label): `grep -qE 'app:[[:space:]]*webserver' cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/setup.sh`
    - setup.sh Deployment uses pinned image: `grep -q 'nginx:1.27-alpine' cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/setup.sh`
    - grade.sh uses assert_endpoints_nonempty for web-svc: `grep -q 'assert_endpoints_nonempty.*web-svc' cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/grade.sh`
    - grade.sh calls detect_service_label_mismatch
    - ref-solution.sh patches Service selector to `app:"web"`: `grep -qE '"selector":\{"app":"web"\}' cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/ref-solution.sh`
    - lint-packs.sh accepts pack: `bash cka-sim/scripts/test.sh` exits 0
    - grade.sh contains NO mutating verbs: `! grep -qE 'kubectl[[:space:]]+(delete|create|apply|patch|edit|replace)' cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/grade.sh`
  </acceptance_criteria>
  <done>troubleshooting pack reference question complete.</done>
</task>

</tasks>

<verification>
- 8 files created
- `bash cka-sim/scripts/test.sh` exits 0
- Trap mechanic: selector mismatch (app=webserver vs pod label app=web)
- ref-solution patches ONLY the Service selector (per question constraint)
</verification>

<success_criteria>
- troubleshooting/01-deploy-svc-mismatch fully drillable
- Exercises the multi-resource diagnostic pattern (Deployment + Service + Endpoints)
- Trap mapping: service-label-mismatch (primary) + 2 filler seeded ids
</success_criteria>

<output>
After completion, create `.planning/phases/03-runtime-contract-drill-mode/03-08-SUMMARY.md` covering files, trap mechanic, pinned image rationale, human-verification procedure.
</output>
