---
phase: 04-storage-workloads-scheduling-packs
plan: 05
type: execute
wave: 2
depends_on: [01]
files_modified:
  - cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh
autonomous: true
requirements: [PACK-02, PACK-06]
must_haves:
  truths:
    - "workloads-scheduling/01-deployment-requests/setup.sh sources cka-sim/lib/setup.sh and calls ensure_lab_ns + wait_for_ns_active"
    - "the inline ns create + 24-iteration for-loop (setup.sh lines 6-35) is removed"
    - "Deployment heredoc (no SA, no requests — the trap) is preserved byte-for-byte"
    - "script stays idempotent (TRIP-02): running twice exits 0 with no AlreadyExists"
    - "lint-packs.sh + test.sh green post-change"
  artifacts:
    - path: "cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh"
      provides: "Retrofitted setup sourcing lib/setup.sh; retains trap-seeding Deployment"
      contains: "source \"$CKA_SIM_ROOT/lib/setup.sh\""
      contains_extra: "cka_sim::setup::wait_for_ns_active"
  key_links:
    - from: "cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh"
      to: "cka-sim/lib/setup.sh"
      via: "source + ensure_lab_ns + wait_for_ns_active helper calls"
      pattern: "cka_sim::setup::"
---

<objective>
Retrofit the Phase 3 Workloads & Scheduling reference question to use `cka-sim/lib/setup.sh`. Mirror the Plan 04 retrofit exactly — swap the inline ns-Active wait for helper calls, leave the Deployment-seeding heredoc (which seeds the `default-sa-used` + `deployment-missing-requests` traps) byte-identical.

This is the second Wave 2 retrofit. Must ship green before Wave 3 authoring begins so Plans 06-15 import a proven-clean helper library.

Purpose: de-duplicate the 120s ns-Active wait; close the drift surface.
Output: 1 modified file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-runtime-contract-drill-mode/03-05-SUMMARY.md
@.planning/phases/04-storage-workloads-scheduling-packs/04-CONTEXT.md
@.planning/phases/04-storage-workloads-scheduling-packs/04-RESEARCH.md
@cka-sim/lib/setup.sh
@cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh
@cka-sim/packs/workloads-scheduling/01-deployment-requests/grade.sh
@cka-sim/packs/workloads-scheduling/01-deployment-requests/ref-solution.sh
@cka-sim/packs/workloads-scheduling/01-deployment-requests/metadata.yaml
</context>

<interfaces>
From cka-sim/lib/setup.sh:
- `cka_sim::setup::ensure_lab_ns <ns> <pack> <question-id>`
- `cka_sim::setup::wait_for_ns_active <ns> <pack> <question-id> [<timeout-seconds>]`

Pack + question identity (fixed, from metadata.yaml):
- pack = `workloads-scheduling`
- question-id = `workloads-deployment-requests`
- CKA_SIM_LAB_NS = `cka-sim-workloads-scheduling-01` (set by drill runner)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Retrofit workloads-scheduling/01-deployment-requests/setup.sh</name>
  <files>cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh</files>
  <read_first>
    - cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh (full file — identify 30-line inline block: lines 6-35)
    - cka-sim/lib/setup.sh (signatures)
    - cka-sim/packs/workloads-scheduling/01-deployment-requests/grade.sh (confirm CKA_SIM_ROOT already required)
    - .planning/phases/04-storage-workloads-scheduling-packs/04-04-PLAN.md (Plan 04 retrofit sets the pattern — match it exactly for the workloads pack)
  </read_first>
  <action>
    Rewrite `cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh` to the following exact contents:

    ```
    #!/bin/bash
    # workloads-scheduling/01-deployment-requests/setup.sh — Deployment WITH default SA, NO requests (the traps).
    # Retrofitted Phase 4 Plan 05: sources shared cka-sim/lib/setup.sh helpers.
    set -euo pipefail
    : "${CKA_SIM_LAB_NS:?CKA_SIM_LAB_NS must be set by drill runner}"
    : "${CKA_SIM_ROOT:?CKA_SIM_ROOT must be set (drill runner exports it)}"

    # shellcheck source=../../../lib/setup.sh disable=SC1091
    source "$CKA_SIM_ROOT/lib/setup.sh"

    # 1. Idempotent ns create + 120s Active wait.
    cka_sim::setup::ensure_lab_ns "$CKA_SIM_LAB_NS" workloads-scheduling workloads-deployment-requests
    cka_sim::setup::wait_for_ns_active "$CKA_SIM_LAB_NS" workloads-scheduling workloads-deployment-requests 120

    # 2. Deployment with NO resources.requests AND NO serviceAccountName (defaults to "default" SA — the trap).
    kubectl apply -f - <<EOF
    apiVersion: apps/v1
    kind: Deployment
    metadata:
      name: load-app
      namespace: ${CKA_SIM_LAB_NS}
      labels:
        app: load-app
    spec:
      replicas: 1
      selector:
        matchLabels:
          app: load-app
      template:
        metadata:
          labels:
            app: load-app
        spec:
          containers:
            - name: app
              image: nginx:1.27
              ports:
                - containerPort: 80
    EOF
    ```

    Constraints:
    - Deployment YAML (`apiVersion: apps/v1` through `containerPort: 80`) MUST be byte-identical to the current file's lines 39-61. No field reordering, no indentation drift.
    - Only additions above the heredoc: `: "${CKA_SIM_ROOT:?...}"` guard + `source` + two helper calls.
    - Do NOT use `seed_deployment` helper here — the deployment is intentionally broken (missing SA + requests) which is the trap; the helper would add them. The inline heredoc is the right tool for a broken-by-design scenario.
    - Do NOT touch grade.sh / reset.sh / ref-solution.sh / metadata.yaml / question.md.

    Set executable bit: `chmod +x cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh`.
  </action>
  <verify>
    <automated>chmod +x cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh && bash -n cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh && grep -q 'source.*lib/setup.sh' cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh && grep -q 'cka_sim::setup::ensure_lab_ns.*workloads-scheduling.*workloads-deployment-requests' cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh && ! grep -qE 'for i in \$\(seq 1' cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - Syntax valid; executable bit set
    - Sources lib/setup.sh: `grep -q 'source.*lib/setup.sh' cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh`
    - ensure_lab_ns call with correct pack + qid: `grep -q 'cka_sim::setup::ensure_lab_ns "\$CKA_SIM_LAB_NS" workloads-scheduling workloads-deployment-requests' cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh`
    - wait_for_ns_active with 120 timeout: `grep -qE 'cka_sim::setup::wait_for_ns_active "\$CKA_SIM_LAB_NS" workloads-scheduling workloads-deployment-requests 120' cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh`
    - Inline wait-loop removed: `! grep -qE 'for i in \$\(seq 1 24\)' cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh`
    - Deployment still has NO serviceAccountName (trap preserved): `! grep -q 'serviceAccountName' cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh`
    - Deployment still has NO resources.requests (trap preserved): `! grep -qE '(resources:|requests:)' cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh`
    - Deployment image unchanged: `grep -q 'image: nginx:1.27' cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh`
    - No mutating verbs other than apply: `! grep -qE 'kubectl[[:space:]]+(create|delete|patch|edit)' cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh`
    - test.sh green: `bash cka-sim/scripts/test.sh` exits 0
    - File shorter: `wc -l cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh | awk '{print $1}'` returns <= 45 (was 62)
  </acceptance_criteria>
  <done>setup.sh retrofitted, test.sh green, traps unchanged.</done>
</task>

</tasks>

<verification>
- `bash cka-sim/scripts/test.sh` exits 0
- Deployment still seeds both `default-sa-used` and `deployment-missing-requests` traps (grade.sh detectors will still fire pre-ref-solution)
- No change to grade.sh / reset.sh / ref-solution.sh / metadata.yaml / question.md
</verification>

<success_criteria>
- Both Phase 3 reference questions (storage + workloads) now source lib/setup.sh
- Wave 3 authoring starts from a proven-clean helper library (both retrofits green)
- Future ns-wait fixes land in one file
</success_criteria>

<output>
After completion, create `.planning/phases/04-storage-workloads-scheduling-packs/04-05-SUMMARY.md` covering:
- 1 file modified; lines dropped from 62 → ~45
- Trap semantics unchanged (Deployment still missing SA + requests)
- Post-change test.sh green
- Both Wave 2 retrofits complete; Wave 3 unblocked
</output>
