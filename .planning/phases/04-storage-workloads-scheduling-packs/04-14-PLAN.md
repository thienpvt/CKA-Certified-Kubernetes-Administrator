---
phase: 04-storage-workloads-scheduling-packs
plan: 14
type: execute
wave: 3
depends_on: [01, 02, 05]
files_modified:
  - cka-sim/packs/workloads-scheduling/05-daemonset/setup.sh
  - cka-sim/packs/workloads-scheduling/05-daemonset/grade.sh
  - cka-sim/packs/workloads-scheduling/05-daemonset/reset.sh
  - cka-sim/packs/workloads-scheduling/05-daemonset/ref-solution.sh
  - cka-sim/packs/workloads-scheduling/05-daemonset/metadata.yaml
  - cka-sim/packs/workloads-scheduling/05-daemonset/question.md
  - cka-sim/tests/fixtures/workloads-05-daemonset/stub-responses.json
  - cka-sim/tests/fixtures/workloads-05-daemonset/expected-fail-score.txt
  - cka-sim/tests/fixtures/workloads-05-daemonset/expected-pass-score.txt
autonomous: true
requirements: [PACK-02, PACK-06]
must_haves:
  truths:
    - "6 files + 3 fixtures"
    - "setup only creates the lab ns (no DaemonSet pre-seed — candidate authors it from scratch)"
    - "grade asserts DaemonSet q05-node-agent exists + status.desiredNumberScheduled equals cluster node count + toleration for node-role.kubernetes.io/control-plane present"
    - "ref-solution creates DaemonSet with control-plane toleration + dedicated SA + resources.requests"
    - "metadata traps=[default-sa-used, deployment-missing-requests, daemonset-missing-control-plane-toleration] — all 3 genuinely apply to the DaemonSet scenario"
  artifacts:
    - path: "cka-sim/packs/workloads-scheduling/05-daemonset/ref-solution.sh"
      provides: "DaemonSet with control-plane toleration"
      contains: "node-role.kubernetes.io/control-plane"
  key_links:
    - from: "cka-sim/packs/workloads-scheduling/05-daemonset/grade.sh"
      to: "cluster node count"
      via: "assert_field_eq desiredNumberScheduled == number of Ready nodes"
      pattern: "desiredNumberScheduled"
---

<objective>
Ship Workloads Q05 — DaemonSet across control-plane + workers (Tracker: `daemonset`). Per RESEARCH §2.2 Q05: candidate authors DaemonSet `q05-node-agent` that tolerates `node-role.kubernetes.io/control-plane:NoSchedule` so it lands on every Ready node (control-plane + both workers).

Output: 6 files + 3 fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-storage-workloads-scheduling-packs/04-RESEARCH.md
@cka-sim/lib/setup.sh
@cka-sim/lib/grade.sh
</context>

<interfaces>
Cluster node count is computed at grade time via `kubectl get nodes --no-headers | wc -l`. RESEARCH §9 risk row flags that the lab CP may have extra taints (NoExecute from upgrades); ref-solution uses `operator: Exists` to match any control-plane taint effect/value.

CKA_SIM_LAB_NS = `cka-sim-workloads-scheduling-05`; prefix `q05-`.

**Trap dependency:** `daemonset-missing-control-plane-toleration` is a NEW catalog entry authored in Plan 04-02 (7th new trap; catalog grows 13 → 20). Wave 1 must commit before this plan runs.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 6 question files + 3 fixtures</name>
  <files>cka-sim/packs/workloads-scheduling/05-daemonset/metadata.yaml, cka-sim/packs/workloads-scheduling/05-daemonset/question.md, cka-sim/packs/workloads-scheduling/05-daemonset/setup.sh, cka-sim/packs/workloads-scheduling/05-daemonset/grade.sh, cka-sim/packs/workloads-scheduling/05-daemonset/reset.sh, cka-sim/packs/workloads-scheduling/05-daemonset/ref-solution.sh, cka-sim/tests/fixtures/workloads-05-daemonset/stub-responses.json, cka-sim/tests/fixtures/workloads-05-daemonset/expected-fail-score.txt, cka-sim/tests/fixtures/workloads-05-daemonset/expected-pass-score.txt</files>
  <read_first>
    - .planning/phases/04-storage-workloads-scheduling-packs/04-RESEARCH.md (§2.2 Q05 lines 121-130 + §9 DaemonSet risk)
  </read_first>
  <action>
    `mkdir -p cka-sim/packs/workloads-scheduling/05-daemonset cka-sim/tests/fixtures/workloads-05-daemonset`.

    **metadata.yaml** — all 3 trap IDs apply to DaemonSet scheduling:
    ```
    id: workloads-daemonset
    domain: workloads-scheduling
    estimatedMinutes: 7
    verified_against: "1.35"
    traps:
      - default-sa-used
      - deployment-missing-requests
      - daemonset-missing-control-plane-toleration
    references:
      - kind: k8s-doc
        target: "https://kubernetes.io/docs/concepts/workloads/controllers/daemonset/"
        note: DaemonSet concepts
      - kind: k8s-doc
        target: "https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/"
        note: Tolerations for control-plane taint
    ```

    **question.md**:
    ```
    # workloads-scheduling/05-daemonset

    **Domain:** Workloads & Scheduling  |  **Estimated time:** 7 minutes

    Create a DaemonSet that runs a lightweight agent pod on **every** Ready node in the cluster — including the control-plane.

    ## Tasks

    1. In `${CKA_SIM_LAB_NS}`, create a DaemonSet named `q05-node-agent`.
    2. Use image `busybox:1.36` with command `["sh", "-c", "sleep 3600"]`.
    3. Ensure the pod tolerates the taints on the control-plane node so it lands there too.
    4. Each container must declare non-zero `resources.requests` for CPU and memory.

    ## Constraints

    - Exactly `status.desiredNumberScheduled` must equal the total number of Ready nodes.
    - Use a dedicated ServiceAccount (not `default`).

    ## Verify yourself

    ```
    kubectl get ds q05-node-agent -n ${CKA_SIM_LAB_NS}
    kubectl get nodes -o name | wc -l      # compare with desiredNumberScheduled
    ```
    ```

    **setup.sh** (chmod +x):
    ```
    #!/bin/bash
    set -euo pipefail
    : "${CKA_SIM_LAB_NS:?}"
    : "${CKA_SIM_ROOT:?}"
    source "$CKA_SIM_ROOT/lib/setup.sh"

    cka_sim::setup::ensure_lab_ns "$CKA_SIM_LAB_NS" workloads-scheduling workloads-daemonset
    cka_sim::setup::wait_for_ns_active "$CKA_SIM_LAB_NS" workloads-scheduling workloads-daemonset 120
    # No DaemonSet pre-seed -- candidate authors it from scratch.
    ```

    **grade.sh** (chmod +x) — single clean version (do NOT emit intermediate drafts):
    ```
    #!/bin/bash
    set -uo pipefail
    : "${CKA_SIM_LAB_NS:?}"
    : "${CKA_SIM_ROOT:?}"
    source "$CKA_SIM_ROOT/lib/grade.sh"
    source "$CKA_SIM_ROOT/lib/traps.sh"

    cka_sim::grade::assert_resource_exists daemonset q05-node-agent -n "$CKA_SIM_LAB_NS"

    node_count=$(kubectl get nodes --no-headers 2>/dev/null | wc -l | tr -d ' ')
    cka_sim::grade::assert_field_eq daemonset q05-node-agent \
      '{.status.desiredNumberScheduled}' "$node_count" -n "$CKA_SIM_LAB_NS"
    cka_sim::grade::assert_field_eq daemonset q05-node-agent \
      '{.spec.template.spec.tolerations[?(@.key=="node-role.kubernetes.io/control-plane")].operator}' \
      'Exists' -n "$CKA_SIM_LAB_NS"

    cpu=$(kubectl get daemonset q05-node-agent -n "$CKA_SIM_LAB_NS" \
      -o jsonpath='{.spec.template.spec.containers[0].resources.requests.cpu}' 2>/dev/null || echo "")
    CKA_SIM_GRADE_TOTAL=$(( CKA_SIM_GRADE_TOTAL + 1 ))
    if [[ -n "$cpu" && "$cpu" != "0" ]]; then
      CKA_SIM_GRADE_PASSED=$(( CKA_SIM_GRADE_PASSED + 1 ))
      ok "cpu request = $cpu"
    else
      err "cpu request empty or zero"
    fi

    # Trap: default SA used
    pod=$(kubectl get pod -n "$CKA_SIM_LAB_NS" -l app=q05-node-agent -o jsonpath='{.items[0].metadata.name}' 2>/dev/null || echo "")
    if [[ -n "$pod" ]]; then
      tid=$(cka_sim::trap::detect_default_sa_used "$CKA_SIM_LAB_NS" "$pod")
      [[ -n "$tid" ]] && cka_sim::grade::record_trap "$tid"
    fi

    # Trap: DaemonSet missing control-plane toleration
    cp_toleration=$(kubectl get daemonset q05-node-agent -n "$CKA_SIM_LAB_NS" \
      -o jsonpath='{.spec.template.spec.tolerations[?(@.key=="node-role.kubernetes.io/control-plane")].key}' 2>/dev/null || echo "")
    if [[ -z "$cp_toleration" ]]; then
      cka_sim::grade::record_trap daemonset-missing-control-plane-toleration
    fi

    cka_sim::grade::emit_result
    ```

    **reset.sh** (chmod +x):
    ```
    #!/bin/bash
    set -uo pipefail
    : "${CKA_SIM_LAB_NS:?}"
    kubectl delete namespace "$CKA_SIM_LAB_NS" --ignore-not-found --wait=false
    exit 0
    ```

    **ref-solution.sh** (chmod +x):
    ```
    #!/bin/bash
    set -euo pipefail
    : "${CKA_SIM_LAB_NS:?}"

    kubectl apply -f - <<EOF
    apiVersion: v1
    kind: ServiceAccount
    metadata:
      name: q05-node-agent-sa
      namespace: ${CKA_SIM_LAB_NS}
    ---
    apiVersion: apps/v1
    kind: DaemonSet
    metadata:
      name: q05-node-agent
      namespace: ${CKA_SIM_LAB_NS}
      labels:
        app: q05-node-agent
    spec:
      selector:
        matchLabels:
          app: q05-node-agent
      template:
        metadata:
          labels:
            app: q05-node-agent
        spec:
          serviceAccountName: q05-node-agent-sa
          tolerations:
            - key: node-role.kubernetes.io/control-plane
              operator: Exists
              effect: NoSchedule
            - key: node-role.kubernetes.io/control-plane
              operator: Exists
              effect: NoExecute
          containers:
            - name: agent
              image: busybox:1.36
              command: ["sh", "-c", "sleep 3600"]
              resources:
                requests:
                  cpu: 25m
                  memory: 32Mi
    EOF

    kubectl rollout status daemonset/q05-node-agent -n "$CKA_SIM_LAB_NS" --timeout=120s
    ```

    **Fixtures**:
    - `stub-responses.json`: `{"kind":"DaemonSet","metadata":{"name":"q05-node-agent"},"status":{"desiredNumberScheduled":3}}`
    - `expected-fail-score.txt`: `SCORE: 0/4`
    - `expected-pass-score.txt`: `SCORE: 4/4`

    chmod +x all .sh.
  </action>
  <verify>
    <automated>chmod +x cka-sim/packs/workloads-scheduling/05-daemonset/*.sh && for f in cka-sim/packs/workloads-scheduling/05-daemonset/*.sh; do bash -n "$f" || exit 1; done && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - 6 files + 3 fixtures
    - ref-solution has 2 tolerations (NoSchedule + NoExecute) with `operator: Exists` for control-plane: `grep -c 'node-role.kubernetes.io/control-plane' cka-sim/packs/workloads-scheduling/05-daemonset/ref-solution.sh | awk '$1 >= 2 {exit 0} {exit 1}'`
    - grade computes node count dynamically: `grep -q 'kubectl get nodes --no-headers' cka-sim/packs/workloads-scheduling/05-daemonset/grade.sh`
    - grade records the new trap when toleration missing: `grep -q 'record_trap daemonset-missing-control-plane-toleration' cka-sim/packs/workloads-scheduling/05-daemonset/grade.sh`
    - metadata references the new trap (all 3 traps genuinely apply to DaemonSet scheduling): `grep -q '- daemonset-missing-control-plane-toleration' cka-sim/packs/workloads-scheduling/05-daemonset/metadata.yaml`
    - Metadata normalized (W2 block):
      - `grep -q '^id:' cka-sim/packs/workloads-scheduling/05-daemonset/metadata.yaml`
      - `grep -qE '^domain: (storage|workloads-scheduling)$' cka-sim/packs/workloads-scheduling/05-daemonset/metadata.yaml`
      - `grep -qE '^estimatedMinutes: [6-9]$' cka-sim/packs/workloads-scheduling/05-daemonset/metadata.yaml`
      - `grep -q 'verified_against: "1.35"' cka-sim/packs/workloads-scheduling/05-daemonset/metadata.yaml`
      - `python3 -c "import yaml; m=yaml.safe_load(open('cka-sim/packs/workloads-scheduling/05-daemonset/metadata.yaml')); assert len(m.get('traps', [])) >= 3"`
      - `grep -qE '^references:' cka-sim/packs/workloads-scheduling/05-daemonset/metadata.yaml && grep -qE '^[[:space:]]+- kind:' cka-sim/packs/workloads-scheduling/05-daemonset/metadata.yaml`
    - test.sh green
  </acceptance_criteria>
  <done>DaemonSet question ships.</done>
</task>

</tasks>

<verification>
- DaemonSet lands on all nodes (dynamic count) + control-plane toleration with operator=Exists covers extra taint effects
- All 3 metadata trap IDs genuinely apply to DaemonSet: default-sa (SA selection), deployment-missing-requests (resources.requests), daemonset-missing-control-plane-toleration (the core scheduling trap)
</verification>

<success_criteria>
- Tracker slug `daemonset` covered
</success_criteria>

<output>
`04-14-SUMMARY.md`: dynamic node-count assertion, dual-toleration for CP, new trap ID `daemonset-missing-control-plane-toleration` detector wired.
</output>
