---
phase: 04-storage-workloads-scheduling-packs
plan: 11
type: execute
wave: 3
depends_on: [01, 02, 05]
files_modified:
  - cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/setup.sh
  - cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/grade.sh
  - cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/reset.sh
  - cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/ref-solution.sh
  - cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/metadata.yaml
  - cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/question.md
  - cka-sim/tests/fixtures/workloads-02-rolling-update-rollback/stub-responses.json
  - cka-sim/tests/fixtures/workloads-02-rolling-update-rollback/expected-fail-score.txt
  - cka-sim/tests/fixtures/workloads-02-rolling-update-rollback/expected-pass-score.txt
autonomous: true
requirements: [PACK-02, PACK-06]
must_haves:
  truths:
    - "question dir has all 6 files chmod +x"
    - "setup.sh seeds Deployment web at nginx:1.25 with strategy rollingUpdate maxUnavailable:0 maxSurge:1; triggers one bad revision via kubectl set image web=nginx:bad-tag then rolls back, then pushes another forward change"
    - "grade.sh asserts kubectl rollout status succeeds + final image=nginx:1.25 + generation >= 2 via jsonpath (no grep)"
    - "ref-solution.sh performs: set image nginx:1.27 -> rollout status -> rollout undo -> final image == nginx:1.25"
    - "metadata traps=[deployment-missing-requests, default-sa-used, service-selector-empty-endpoints]"
    - "lint-packs.sh + test.sh green"
  artifacts:
    - path: "cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/setup.sh"
      provides: "Deployment web with RollingUpdate + multi-revision history"
      contains: "nginx:1.25"
    - path: "cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/grade.sh"
      provides: "rollout status + final image assertion"
      contains: "rollout"
  key_links:
    - from: "cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/grade.sh"
      to: "cka-sim/lib/grade.sh"
      via: "assert_field_eq on deployment template image + kubectl rollout status behavioural check"
      pattern: "rollout status"
---

<objective>
Ship Workloads Q02 — Rolling update + rollback (Tracker: `rolling-update-rollback`). Per RESEARCH §2.2 Q02: setup seeds Deployment `web` at `nginx:1.25` with RollingUpdate strategy + one prior revision; candidate rolls forward to `nginx:1.27`, confirms rollout, then rolls back to `nginx:1.25`. Grader verifies rollout succeeded and final image matches.

Output: 6 question files + 3 fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-storage-workloads-scheduling-packs/04-RESEARCH.md
@cka-sim/lib/setup.sh
@cka-sim/lib/grade.sh
@cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh
@cka-sim/traps/catalog.yaml
</context>

<interfaces>
Lib: `seed_deployment` (produces deployment with or without SA + requests — used for seed). For this question the seeded deployment must have a rollout history, so follow `seed_deployment` with a `kubectl set image` to create revision 2, then another `set image` back to 1.25 to end with history.

CKA_SIM_LAB_NS = `cka-sim-workloads-scheduling-02`; no cluster-scoped resources.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 6 question files + 3 fixtures</name>
  <files>cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/metadata.yaml, cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/question.md, cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/setup.sh, cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/grade.sh, cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/reset.sh, cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/ref-solution.sh, cka-sim/tests/fixtures/workloads-02-rolling-update-rollback/stub-responses.json, cka-sim/tests/fixtures/workloads-02-rolling-update-rollback/expected-fail-score.txt, cka-sim/tests/fixtures/workloads-02-rolling-update-rollback/expected-pass-score.txt</files>
  <read_first>
    - .planning/phases/04-storage-workloads-scheduling-packs/04-RESEARCH.md (§2.2 Q02 lines 84-94)
    - cka-sim/packs/workloads-scheduling/01-deployment-requests/setup.sh (retrofitted pattern)
  </read_first>
  <action>
    `mkdir -p cka-sim/packs/workloads-scheduling/02-rolling-update-rollback cka-sim/tests/fixtures/workloads-02-rolling-update-rollback`.

    **metadata.yaml**:
    ```
    id: workloads-rolling-update-rollback
    domain: workloads-scheduling
    estimatedMinutes: 7
    verified_against: "1.35"
    traps:
      - deployment-missing-requests
      - default-sa-used
      - service-selector-empty-endpoints
    references:
      - kind: k8s-doc
        target: "https://kubernetes.io/docs/concepts/workloads/controllers/deployment/#rolling-back-a-deployment"
        note: Rolling back a Deployment
    ```

    **question.md**:
    ```
    # workloads-scheduling/02-rolling-update-rollback

    **Domain:** Workloads & Scheduling  |  **Estimated time:** 7 minutes

    A Deployment `web` in `${CKA_SIM_LAB_NS}` currently runs `nginx:1.25`. Ops asks you to try `nginx:1.27`, verify it works, and — if anything looks wrong — roll back.

    ## Tasks

    1. Update Deployment `web` so its pods run `nginx:1.27`.
    2. Wait until the rollout finishes successfully.
    3. After verification, roll back one revision so `web` returns to `nginx:1.25`.

    ## Constraints

    - Do NOT delete and recreate the Deployment.
    - Rollout must complete without replicas dropping to zero (RollingUpdate strategy is pre-configured).

    ## Verify yourself

    ```
    kubectl rollout status deployment/web -n ${CKA_SIM_LAB_NS}
    kubectl get deploy web -n ${CKA_SIM_LAB_NS} -o jsonpath='{.spec.template.spec.containers[0].image}'   # nginx:1.25
    kubectl rollout history deployment/web -n ${CKA_SIM_LAB_NS}
    ```
    ```

    **setup.sh** (chmod +x):
    ```
    #!/bin/bash
    set -euo pipefail
    : "${CKA_SIM_LAB_NS:?}"
    : "${CKA_SIM_ROOT:?}"
    source "$CKA_SIM_ROOT/lib/setup.sh"

    cka_sim::setup::ensure_lab_ns "$CKA_SIM_LAB_NS" workloads-scheduling workloads-rolling-update-rollback
    cka_sim::setup::wait_for_ns_active "$CKA_SIM_LAB_NS" workloads-scheduling workloads-rolling-update-rollback 120

    kubectl apply -f - <<EOF
    apiVersion: apps/v1
    kind: Deployment
    metadata:
      name: web
      namespace: ${CKA_SIM_LAB_NS}
      labels:
        app: web
      annotations:
        kubernetes.io/change-cause: "initial nginx:1.25"
    spec:
      replicas: 2
      revisionHistoryLimit: 10
      strategy:
        type: RollingUpdate
        rollingUpdate:
          maxUnavailable: 0
          maxSurge: 1
      selector:
        matchLabels:
          app: web
      template:
        metadata:
          labels:
            app: web
        spec:
          containers:
            - name: app
              image: nginx:1.25
              ports:
                - containerPort: 80
    EOF

    kubectl rollout status deployment/web -n "$CKA_SIM_LAB_NS" --timeout=120s 2>/dev/null || true

    # Seed a second revision so `rollout undo` has something to go back to.
    kubectl annotate deployment web -n "$CKA_SIM_LAB_NS" --overwrite kubernetes.io/change-cause="revision 2"
    kubectl set image deployment/web app=nginx:1.25 -n "$CKA_SIM_LAB_NS" --record=false 2>/dev/null || true
    # Force generation bump by patching a harmless field (adds a Deployment revision).
    kubectl patch deployment web -n "$CKA_SIM_LAB_NS" --type=json -p='[{"op":"add","path":"/spec/template/metadata/annotations","value":{"cka-sim/rev":"2"}}]'
    kubectl rollout status deployment/web -n "$CKA_SIM_LAB_NS" --timeout=60s 2>/dev/null || true
    ```

    **grade.sh** (chmod +x):
    ```
    #!/bin/bash
    set -uo pipefail
    : "${CKA_SIM_LAB_NS:?}"
    : "${CKA_SIM_ROOT:?}"
    source "$CKA_SIM_ROOT/lib/grade.sh"
    source "$CKA_SIM_ROOT/lib/traps.sh"

    # Assertion 1: Deployment exists.
    cka_sim::grade::assert_resource_exists deployment web -n "$CKA_SIM_LAB_NS"

    # Assertion 2: rollout has completed (no pending progression). Behavioural, exits 0 when done.
    CKA_SIM_GRADE_TOTAL=$(( CKA_SIM_GRADE_TOTAL + 1 ))
    if kubectl rollout status deployment/web -n "$CKA_SIM_LAB_NS" --timeout=60s >/dev/null 2>&1; then
      CKA_SIM_GRADE_PASSED=$(( CKA_SIM_GRADE_PASSED + 1 ))
      ok "rollout status succeeded for deployment/web"
    else
      err "rollout status failed for deployment/web"
    fi

    # Assertion 3: final image is nginx:1.25 (proves rollback happened).
    cka_sim::grade::assert_field_eq deployment web \
      '{.spec.template.spec.containers[0].image}' \
      'nginx:1.25' \
      -n "$CKA_SIM_LAB_NS"

    # Assertion 4: deployment generation >= 3 (proves at least rollout-forward + rollback happened on top of setup).
    gen=$(kubectl get deployment web -n "$CKA_SIM_LAB_NS" -o jsonpath='{.metadata.generation}' 2>/dev/null || echo "0")
    CKA_SIM_GRADE_TOTAL=$(( CKA_SIM_GRADE_TOTAL + 1 ))
    if [[ "$gen" =~ ^[0-9]+$ ]] && (( gen >= 3 )); then
      CKA_SIM_GRADE_PASSED=$(( CKA_SIM_GRADE_PASSED + 1 ))
      ok "deployment generation = $gen (>=3 means rollout + rollback occurred)"
    else
      err "deployment generation = '$gen' (expected >=3)"
    fi

    # Trap: default SA used
    pod=$(kubectl get pod -n "$CKA_SIM_LAB_NS" -l app=web -o jsonpath='{.items[0].metadata.name}' 2>/dev/null || echo "")
    if [[ -n "$pod" ]]; then
      tid=$(cka_sim::trap::detect_default_sa_used "$CKA_SIM_LAB_NS" "$pod")
      [[ -n "$tid" ]] && cka_sim::grade::record_trap "$tid"
    fi

    cka_sim::grade::emit_result
    ```

    **reset.sh** (chmod +x):
    ```
    #!/bin/bash
    set -uo pipefail
    : "${CKA_SIM_LAB_NS:?}"
    kubectl delete namespace "$CKA_SIM_LAB_NS" --ignore-not-found --wait=false
    exit 0
    ```

    **ref-solution.sh** (chmod +x):
    ```
    #!/bin/bash
    set -euo pipefail
    : "${CKA_SIM_LAB_NS:?}"

    kubectl set image deployment/web app=nginx:1.27 -n "$CKA_SIM_LAB_NS"
    kubectl rollout status deployment/web -n "$CKA_SIM_LAB_NS" --timeout=120s
    kubectl rollout undo deployment/web -n "$CKA_SIM_LAB_NS"
    kubectl rollout status deployment/web -n "$CKA_SIM_LAB_NS" --timeout=120s
    ```

    **Fixtures**:
    - `stub-responses.json`: `{"kind":"Deployment","metadata":{"name":"web","generation":3},"spec":{"template":{"spec":{"containers":[{"name":"app","image":"nginx:1.25"}]}}}}`
    - `expected-fail-score.txt`: `SCORE: 1/4`
    - `expected-pass-score.txt`: `SCORE: 4/4`

    chmod +x all .sh files.
  </action>
  <verify>
    <automated>chmod +x cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/*.sh && for f in cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/*.sh; do bash -n "$f" || exit 1; done && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - 6 files + 3 fixtures
    - setup.sh seeds RollingUpdate strategy + maxUnavailable:0 + maxSurge:1: `grep -q 'RollingUpdate' cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/setup.sh && grep -q 'maxUnavailable: 0' cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/setup.sh`
    - grade asserts final image = nginx:1.25: `grep -q "'nginx:1.25'" cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/grade.sh`
    - grade uses kubectl rollout status behavioural check: `grep -q 'kubectl rollout status' cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/grade.sh`
    - grade has NO `kubectl get | grep` and NO mutating verbs: `! grep -qE 'kubectl[[:space:]]+get[[:space:]].*\|[[:space:]]*grep' cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/grade.sh && ! grep -qE 'kubectl[[:space:]]+(delete|create|apply|patch|edit|replace)' cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/grade.sh`
    - ref-solution uses set image + rollout undo: `grep -q 'kubectl set image' cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/ref-solution.sh && grep -q 'kubectl rollout undo' cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/ref-solution.sh`
    - test.sh green
  </acceptance_criteria>
  <done>Rolling-update-rollback question ships.</done>
</task>

</tasks>

<verification>
- lint-packs green
- Behavioural rollout status assertion; no grep idioms
</verification>

<success_criteria>
- Tracker slug `rolling-update-rollback` covered
</success_criteria>

<output>
`04-11-SUMMARY.md`: rollout history pattern, generation-count assertion.
</output>
