---
phase: 04-storage-workloads-scheduling-packs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cka-sim/traps/catalog.yaml
  - cka-sim/tests/fixtures/csi-snapshot-wrong-driver/hit.json
  - cka-sim/tests/fixtures/csi-snapshot-wrong-driver/miss.json
  - cka-sim/tests/fixtures/csi-snapshot-wrong-driver/benign.json
  - cka-sim/tests/fixtures/pvc-pending-wffc-unscheduled-consumer/hit.json
  - cka-sim/tests/fixtures/pvc-pending-wffc-unscheduled-consumer/miss.json
  - cka-sim/tests/fixtures/pvc-pending-wffc-unscheduled-consumer/benign.json
  - cka-sim/tests/fixtures/reclaim-policy-delete-data-loss/hit.json
  - cka-sim/tests/fixtures/reclaim-policy-delete-data-loss/miss.json
  - cka-sim/tests/fixtures/reclaim-policy-delete-data-loss/benign.json
  - cka-sim/tests/fixtures/pvc-accessmode-rwx-on-rwo-sc/hit.json
  - cka-sim/tests/fixtures/pvc-accessmode-rwx-on-rwo-sc/miss.json
  - cka-sim/tests/fixtures/pvc-accessmode-rwx-on-rwo-sc/benign.json
  - cka-sim/tests/fixtures/hpa-missing-metrics-server/hit.json
  - cka-sim/tests/fixtures/hpa-missing-metrics-server/miss.json
  - cka-sim/tests/fixtures/hpa-missing-metrics-server/benign.json
  - cka-sim/tests/fixtures/sidecar-not-native-restartpolicy-always/hit.json
  - cka-sim/tests/fixtures/sidecar-not-native-restartpolicy-always/miss.json
  - cka-sim/tests/fixtures/sidecar-not-native-restartpolicy-always/benign.json
autonomous: true
requirements: [PACK-01, PACK-02, PACK-06]
must_haves:
  truths:
    - "traps/catalog.yaml gains exactly 6 new entries, appended after rbac-viewer-role-mismatch"
    - "append order matches CONTEXT §Authoring lock: csi-snapshot-wrong-driver, pvc-pending-wffc-unscheduled-consumer, reclaim-policy-delete-data-loss, pvc-accessmode-rwx-on-rwo-sc, hpa-missing-metrics-server, sidecar-not-native-restartpolicy-always"
    - "all 6 entries pass scripts/lint-traps.sh (8-field schema, RFC 1123 id, enum fields)"
    - "18 fixture files (hit/miss/benign per new id) exist for downstream detector tests"
  artifacts:
    - path: "cka-sim/traps/catalog.yaml"
      provides: "6 new trap-catalog entries"
      contains: "id: csi-snapshot-wrong-driver"
      contains_extra: "id: hpa-missing-metrics-server"
    - path: "cka-sim/tests/fixtures/csi-snapshot-wrong-driver/hit.json"
      provides: "VolumeSnapshot with wrong driver, readyToUse=false — hit fixture"
      contains: '"driver"'
  key_links:
    - from: "cka-sim/traps/catalog.yaml"
      to: "cka-sim/scripts/lint-traps.sh"
      via: "8-field schema + enum validation"
      pattern: "lint-traps.sh"
---

<objective>
Extend the trap catalog with the 6 new trap IDs locked in CONTEXT §Authoring Pattern and Trap Catalog. Append them as a block after the existing 13 entries (last entry today is `rbac-viewer-role-mismatch`). Ship 18 matching fixture files (hit/miss/benign per Phase 2 convention) so the per-question plans in Wave 3 can write detector tests without scaffolding.

This plan does NOT write detector functions in `lib/traps.sh` — new detectors ship with their owning question in Wave 3. This plan only owns catalog + fixtures.

Append order is LOCKED (CONTEXT D-block):
1. csi-snapshot-wrong-driver (storage)
2. pvc-pending-wffc-unscheduled-consumer (storage)
3. reclaim-policy-delete-data-loss (storage)
4. pvc-accessmode-rwx-on-rwo-sc (storage)
5. hpa-missing-metrics-server (workloads-scheduling)
6. sidecar-not-native-restartpolicy-always (workloads-scheduling)

Purpose: unblock lint-packs.sh trap-id registration check for Wave 3 questions.
Output: 1 catalog extension + 18 fixture files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-storage-workloads-scheduling-packs/04-CONTEXT.md
@.planning/phases/04-storage-workloads-scheduling-packs/04-RESEARCH.md
@cka-sim/traps/catalog.yaml
@cka-sim/scripts/lint-traps.sh
@cka-sim/lib/traps.sh
</context>

<interfaces>
Catalog schema (enforced by lint-traps.sh lines 33-55):
- 8 required top-level fields per entry: `id, name, description, remediation_hint, severity, domain, source, references`
- Field order MUST match above (readability lint convention)
- `references` is a list of `{kind, target, note}` objects; 2-space indent for list items; 6-space for dash + kind; 8-space for target/note
- Closed enums:
  - severity ∈ {info, warn, error}
  - domain ∈ {troubleshooting, cluster-architecture, services-networking, workloads-scheduling, storage}
  - source ∈ {cncf-curriculum, concerns-md, community}
  - reference kind ∈ {concerns-md, k8s-doc, prior-art-exercise, exam-objective, blog-post}
- id must match RFC 1123: `^[a-z0-9]([a-z0-9-]*[a-z0-9])?$`, length ≤ 63

Existing entry blank-line separator: one blank line between entries (see catalog lines 25-27 between pss-error-string-mismatch and psp-fictional-pod-label-exemption).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Append 6 new trap entries to catalog.yaml</name>
  <files>cka-sim/traps/catalog.yaml</files>
  <read_first>
    - cka-sim/traps/catalog.yaml (full file — confirm append point is after line 208, rbac-viewer-role-mismatch block; confirm 2/4/6/8-space indent convention)
    - .planning/phases/04-storage-workloads-scheduling-packs/04-RESEARCH.md (§3 — 6 entries as verbatim YAML, lines 174-252)
    - cka-sim/scripts/lint-traps.sh (lines 33-55 — closed enums; lines 68-180 — schema walker)
  </read_first>
  <action>
    Append 6 new YAML entries to `cka-sim/traps/catalog.yaml`. Append after the last existing entry (`rbac-viewer-role-mismatch`, last line today is catalog line 208 with `note: RBAC Role/RoleBinding semantics`). Use Edit tool.

    Ensure:
    - Exactly ONE blank line between the existing final entry and the first new entry (match pattern between every existing pair).
    - Exactly ONE blank line between each new entry.
    - NO trailing blank line after the sixth new entry beyond a single newline (match file's current EOF).
    - 2-space dash indent, 4-space field indent, 6-space dash for reference items, 8-space for target/note (see existing entries for exact shape).

    Entries verbatim from RESEARCH §3 (reproduced here for unambiguous execution — match character-for-character):

    Entry 1 — csi-snapshot-wrong-driver:
    ```
      - id: csi-snapshot-wrong-driver
        name: VolumeSnapshotClass references a driver not installed on the cluster
        description: "VolumeSnapshot stays stuck without readyToUse=true because the VolumeSnapshotClass spec.driver names a CSI driver that is not installed or not serving snapshots on this cluster."
        remediation_hint: "Install the correct CSI driver that matches the VolumeSnapshotClass spec.driver, or edit the VolumeSnapshotClass to point at an installed driver."
        severity: error
        domain: storage
        source: community
        references:
          - kind: k8s-doc
            target: "https://kubernetes.io/docs/concepts/storage/volume-snapshots/"
            note: VolumeSnapshot + VolumeSnapshotClass concepts
    ```

    Entry 2 — pvc-pending-wffc-unscheduled-consumer:
    ```
      - id: pvc-pending-wffc-unscheduled-consumer
        name: PVC with WaitForFirstConsumer stays Pending because no pod consumes it
        description: "StorageClass has volumeBindingMode=WaitForFirstConsumer; the PVC will not bind until a pod that mounts it is scheduled. Candidates often deploy the PVC alone and conclude the cluster is broken."
        remediation_hint: "Create a Pod (or Deployment/StatefulSet) that mounts the PVC via spec.volumes[].persistentVolumeClaim.claimName. Binding resolves as soon as the pod is scheduled."
        severity: warn
        domain: storage
        source: community
        references:
          - kind: k8s-doc
            target: "https://kubernetes.io/docs/concepts/storage/storage-classes/#volume-binding-mode"
            note: WaitForFirstConsumer binding semantics
    ```

    Entry 3 — reclaim-policy-delete-data-loss:
    ```
      - id: reclaim-policy-delete-data-loss
        name: PV reclaim policy set to Delete destroys data when the PVC is deleted
        description: "PersistentVolume reclaim policy is Delete; when the bound PVC is deleted, the underlying volume (and its data) is removed automatically. Retain preserves the volume for manual reclaim."
        remediation_hint: "Set persistentVolumeReclaimPolicy: Retain on any PV whose data must survive PVC deletion. kubectl patch pv <name> -p '{\"spec\":{\"persistentVolumeReclaimPolicy\":\"Retain\"}}'"
        severity: warn
        domain: storage
        source: community
        references:
          - kind: k8s-doc
            target: "https://kubernetes.io/docs/concepts/storage/persistent-volumes/#reclaiming"
            note: Reclaim policy semantics
    ```

    Entry 4 — pvc-accessmode-rwx-on-rwo-sc:
    ```
      - id: pvc-accessmode-rwx-on-rwo-sc
        name: PVC requests ReadWriteMany access from a StorageClass that only provides ReadWriteOnce
        description: "PVC spec.accessModes contains ReadWriteMany but the matching StorageClass (or PV) only supports ReadWriteOnce; the PVC stays Pending forever. hostPath and most local-path provisioners are RWO-only."
        remediation_hint: "Either request ReadWriteOnce on the PVC (matching what the StorageClass provides) or use a StorageClass whose provisioner supports RWX (NFS, CephFS, cloud file shares)."
        severity: warn
        domain: storage
        source: community
        references:
          - kind: k8s-doc
            target: "https://kubernetes.io/docs/concepts/storage/persistent-volumes/#access-modes"
            note: accessMode support matrix per volume plugin
    ```

    Entry 5 — hpa-missing-metrics-server (source: concerns-md; requires both refs — note that lint-traps.sh passes a `concerns-md` kind reference only if `target` resolves under REPO_ROOT):
    ```
      - id: hpa-missing-metrics-server
        name: HPA cannot scale because metrics-server is not installed
        description: "HorizontalPodAutoscaler targets CPU/memory but metrics.k8s.io is unavailable; the HPA stays FailedGetResourceMetric and never scales. kubeadm clusters do not ship metrics-server by default."
        remediation_hint: "Install metrics-server via the upstream manifest (https://github.com/kubernetes-sigs/metrics-server). On lab kubeadm clusters with self-signed kubelet certs, add --kubelet-insecure-tls to the metrics-server Deployment args."
        severity: error
        domain: workloads-scheduling
        source: concerns-md
        references:
          - kind: concerns-md
            target: .planning/codebase/CONCERNS.md
            note: "CG-06 — metrics-server bootstrap prerequisite for HPA"
          - kind: k8s-doc
            target: "https://github.com/kubernetes-sigs/metrics-server"
            note: metrics-server upstream install manifest
    ```

    Entry 6 — sidecar-not-native-restartpolicy-always:
    ```
      - id: sidecar-not-native-restartpolicy-always
        name: Sidecar added as peer container instead of v1.29+ native sidecar (initContainer with restartPolicy Always)
        description: "Candidate adds a sidecar as a regular spec.containers entry instead of using the v1.29+ native sidecar shape: spec.initContainers[].restartPolicy: Always. Legacy peer-container sidecars do not get guaranteed ordering, lifecycle, or graceful shutdown."
        remediation_hint: "Move the sidecar into spec.initContainers and set restartPolicy: Always on that init container. The SidecarContainers feature gate is GA on 1.29+ and enabled by default in 1.35."
        severity: warn
        domain: workloads-scheduling
        source: concerns-md
        references:
          - kind: concerns-md
            target: .planning/codebase/CONCERNS.md
            note: "CG-08 — native sidecar pattern is the 1.35 canonical shape"
          - kind: k8s-doc
            target: "https://kubernetes.io/docs/concepts/workloads/pods/sidecar-containers/"
            note: Native sidecar containers
    ```

    Important: entries 5 and 6 use `source: concerns-md` and reference `.planning/codebase/CONCERNS.md`. lint-traps.sh (lines 106-111) requires that target to exist relative to REPO_ROOT. Verify `.planning/codebase/CONCERNS.md` exists before running the lint (it does — confirmed in Phase 2).
  </action>
  <verify>
    <automated>bash cka-sim/scripts/lint-traps.sh && grep -c '^  - id:' cka-sim/traps/catalog.yaml | awk '$1 == 19 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 19 `- id:` lines now present: `grep -c '^  - id:' cka-sim/traps/catalog.yaml == 19` (was 13)
    - All 6 new ids present and in the locked order:
      ```
      sed -n '/^  - id:/p' cka-sim/traps/catalog.yaml | tail -6 > /tmp/last6 && diff /tmp/last6 <(cat <<EOF
        - id: csi-snapshot-wrong-driver
        - id: pvc-pending-wffc-unscheduled-consumer
        - id: reclaim-policy-delete-data-loss
        - id: pvc-accessmode-rwx-on-rwo-sc
        - id: hpa-missing-metrics-server
        - id: sidecar-not-native-restartpolicy-always
      EOF
      )
      ```
    - `bash cka-sim/scripts/lint-traps.sh` exits 0 (schema + enum + RFC 1123 + reference-path all green)
    - No existing entries modified: `git diff cka-sim/traps/catalog.yaml | grep '^-' | grep -v '^---' | wc -l` returns 0 (append-only)
    - All new entries use `domain: storage` (4) or `domain: workloads-scheduling` (2): `awk '/^  - id: (csi-snapshot|pvc-pending-wffc|reclaim-policy|pvc-accessmode)/,/^$/ {if ($1 == "domain:") print $2}' cka-sim/traps/catalog.yaml | sort -u` matches `storage`; analog for workloads entries
  </acceptance_criteria>
  <done>catalog.yaml has 19 entries, lint green, original 13 untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Create 18 fixture files (hit/miss/benign per new trap id)</name>
  <files>cka-sim/tests/fixtures/csi-snapshot-wrong-driver/hit.json, cka-sim/tests/fixtures/csi-snapshot-wrong-driver/miss.json, cka-sim/tests/fixtures/csi-snapshot-wrong-driver/benign.json, cka-sim/tests/fixtures/pvc-pending-wffc-unscheduled-consumer/hit.json, cka-sim/tests/fixtures/pvc-pending-wffc-unscheduled-consumer/miss.json, cka-sim/tests/fixtures/pvc-pending-wffc-unscheduled-consumer/benign.json, cka-sim/tests/fixtures/reclaim-policy-delete-data-loss/hit.json, cka-sim/tests/fixtures/reclaim-policy-delete-data-loss/miss.json, cka-sim/tests/fixtures/reclaim-policy-delete-data-loss/benign.json, cka-sim/tests/fixtures/pvc-accessmode-rwx-on-rwo-sc/hit.json, cka-sim/tests/fixtures/pvc-accessmode-rwx-on-rwo-sc/miss.json, cka-sim/tests/fixtures/pvc-accessmode-rwx-on-rwo-sc/benign.json, cka-sim/tests/fixtures/hpa-missing-metrics-server/hit.json, cka-sim/tests/fixtures/hpa-missing-metrics-server/miss.json, cka-sim/tests/fixtures/hpa-missing-metrics-server/benign.json, cka-sim/tests/fixtures/sidecar-not-native-restartpolicy-always/hit.json, cka-sim/tests/fixtures/sidecar-not-native-restartpolicy-always/miss.json, cka-sim/tests/fixtures/sidecar-not-native-restartpolicy-always/benign.json</files>
  <read_first>
    - cka-sim/tests/fixtures/hostpath-pv-without-nodeaffinity/hit.json (fixture shape — single Kubernetes JSON object)
    - cka-sim/tests/fixtures/hostpath-pv-without-nodeaffinity/miss.json (fixture shape — same object with the trap absent)
    - cka-sim/tests/fixtures/hostpath-pv-without-nodeaffinity/benign.json (unrelated object proving detector does not false-positive)
    - cka-sim/tests/bin/kubectl (fixture path resolution — CKA_SIM_TEST_CURRENT/<file>.json)
  </read_first>
  <action>
    Create 18 JSON fixture files. Each is a single Kubernetes resource object shaped as the detector's kubectl get would return. Keep them minimal — only fields the detector reads.

    **csi-snapshot-wrong-driver/**:
    - `hit.json`: VolumeSnapshot object with `spec.source.persistentVolumeClaimName: "app-data"`, `spec.volumeSnapshotClassName: "csi-hostpath-snapshotclass"`, `status.readyToUse: false`, `status.error.message: "Failed to create snapshot: driver not found"`.
      ```
      {"apiVersion":"snapshot.storage.k8s.io/v1","kind":"VolumeSnapshot","metadata":{"name":"q04-app-snapshot","namespace":"cka-sim-storage-04"},"spec":{"source":{"persistentVolumeClaimName":"app-data"},"volumeSnapshotClassName":"csi-hostpath-snapshotclass"},"status":{"readyToUse":false,"error":{"message":"Failed to create snapshot: driver not found"}}}
      ```
    - `miss.json`: Same shape, `readyToUse: true`, no error.
      ```
      {"apiVersion":"snapshot.storage.k8s.io/v1","kind":"VolumeSnapshot","metadata":{"name":"q04-app-snapshot","namespace":"cka-sim-storage-04"},"spec":{"source":{"persistentVolumeClaimName":"app-data"},"volumeSnapshotClassName":"csi-hostpath-snapshotclass"},"status":{"readyToUse":true,"boundVolumeSnapshotContentName":"snapcontent-abc"}}
      ```
    - `benign.json`: A PersistentVolumeClaim (wrong kind) — proves detector only fires on VolumeSnapshot.
      ```
      {"apiVersion":"v1","kind":"PersistentVolumeClaim","metadata":{"name":"app-data","namespace":"cka-sim-storage-04"},"spec":{"accessModes":["ReadWriteOnce"]},"status":{"phase":"Bound"}}
      ```

    **pvc-pending-wffc-unscheduled-consumer/**:
    - `hit.json`: PVC with `spec.storageClassName: "q05-wffc"`, `status.phase: "Pending"`, plus a StorageClass-embedded hint (detectors typically read the SC separately; fixture represents the combined object for a multi-resource probe. Keep the detector free to read either kind via CKA_SIM_TEST_CURRENT).
      ```
      {"apiVersion":"v1","kind":"PersistentVolumeClaim","metadata":{"name":"q05-claim","namespace":"cka-sim-storage-05","annotations":{"volume.kubernetes.io/selected-node":""}},"spec":{"accessModes":["ReadWriteOnce"],"storageClassName":"q05-wffc"},"status":{"phase":"Pending"}}
      ```
    - `miss.json`: PVC Bound (consumer pod has been created and scheduled).
      ```
      {"apiVersion":"v1","kind":"PersistentVolumeClaim","metadata":{"name":"q05-claim","namespace":"cka-sim-storage-05"},"spec":{"accessModes":["ReadWriteOnce"],"storageClassName":"q05-wffc","volumeName":"pvc-xyz"},"status":{"phase":"Bound"}}
      ```
    - `benign.json`: PVC Pending but with `storageClassName: "manual"` (not a WFFC SC — different root cause).
      ```
      {"apiVersion":"v1","kind":"PersistentVolumeClaim","metadata":{"name":"app-data","namespace":"cka-sim-storage-01"},"spec":{"accessModes":["ReadWriteOnce"],"storageClassName":"manual"},"status":{"phase":"Pending"}}
      ```

    **reclaim-policy-delete-data-loss/**:
    - `hit.json`: PV with `spec.persistentVolumeReclaimPolicy: "Delete"` and a bound claimRef (data at risk).
      ```
      {"apiVersion":"v1","kind":"PersistentVolume","metadata":{"name":"q03-delete-pv"},"spec":{"capacity":{"storage":"1Gi"},"accessModes":["ReadWriteOnce"],"persistentVolumeReclaimPolicy":"Delete","storageClassName":"manual","claimRef":{"namespace":"cka-sim-storage-03","name":"q03-rwo-pvc"}}}
      ```
    - `miss.json`: Same PV with `persistentVolumeReclaimPolicy: "Retain"`.
      ```
      {"apiVersion":"v1","kind":"PersistentVolume","metadata":{"name":"q03-retain-pv"},"spec":{"capacity":{"storage":"1Gi"},"accessModes":["ReadWriteOnce"],"persistentVolumeReclaimPolicy":"Retain","storageClassName":"manual"}}
      ```
    - `benign.json`: PV with Delete policy but no claimRef (no data at risk yet).
      ```
      {"apiVersion":"v1","kind":"PersistentVolume","metadata":{"name":"empty-pv"},"spec":{"capacity":{"storage":"1Gi"},"accessModes":["ReadWriteOnce"],"persistentVolumeReclaimPolicy":"Delete","storageClassName":"manual"}}
      ```

    **pvc-accessmode-rwx-on-rwo-sc/**:
    - `hit.json`: PVC with `spec.accessModes: ["ReadWriteMany"]` and `spec.storageClassName: "manual"` (hostPath-backed RWO-only class), phase Pending.
      ```
      {"apiVersion":"v1","kind":"PersistentVolumeClaim","metadata":{"name":"q03-rwx-pvc","namespace":"cka-sim-storage-03"},"spec":{"accessModes":["ReadWriteMany"],"storageClassName":"manual"},"status":{"phase":"Pending"}}
      ```
    - `miss.json`: PVC with `accessModes: ["ReadWriteOnce"]` on same class — bound.
      ```
      {"apiVersion":"v1","kind":"PersistentVolumeClaim","metadata":{"name":"q03-rwo-pvc","namespace":"cka-sim-storage-03"},"spec":{"accessModes":["ReadWriteOnce"],"storageClassName":"manual"},"status":{"phase":"Bound"}}
      ```
    - `benign.json`: PVC with RWX on a StorageClass that supports RWX (simulated via annotation) — different code path.
      ```
      {"apiVersion":"v1","kind":"PersistentVolumeClaim","metadata":{"name":"nfs-claim","namespace":"cka-sim-storage-03"},"spec":{"accessModes":["ReadWriteMany"],"storageClassName":"nfs-csi"},"status":{"phase":"Bound"}}
      ```

    **hpa-missing-metrics-server/**:
    - `hit.json`: HPA object with `status.conditions[0].type: "AbleToScale"`, `status.conditions[0].status: "False"`, `status.conditions[0].reason: "FailedGetResourceMetric"`, `status.conditions[0].message: "unable to fetch metrics from resource metrics API: ..."`.
      ```
      {"apiVersion":"autoscaling/v2","kind":"HorizontalPodAutoscaler","metadata":{"name":"q04-load","namespace":"cka-sim-workloads-scheduling-04"},"spec":{"scaleTargetRef":{"kind":"Deployment","name":"q04-load"},"minReplicas":1,"maxReplicas":5,"metrics":[{"type":"Resource","resource":{"name":"cpu","target":{"type":"Utilization","averageUtilization":50}}}]},"status":{"conditions":[{"type":"AbleToScale","status":"False","reason":"FailedGetResourceMetric","message":"unable to fetch metrics from resource metrics API: the server could not find the requested resource (get pods.metrics.k8s.io)"}]}}
      ```
    - `miss.json`: HPA with healthy conditions (AbleToScale=True).
      ```
      {"apiVersion":"autoscaling/v2","kind":"HorizontalPodAutoscaler","metadata":{"name":"q04-load","namespace":"cka-sim-workloads-scheduling-04"},"spec":{"scaleTargetRef":{"kind":"Deployment","name":"q04-load"},"minReplicas":1,"maxReplicas":5},"status":{"conditions":[{"type":"AbleToScale","status":"True","reason":"ReadyForNewScale"},{"type":"ScalingActive","status":"True","reason":"ValidMetricFound"}]}}
      ```
    - `benign.json`: A Deployment — wrong kind.
      ```
      {"apiVersion":"apps/v1","kind":"Deployment","metadata":{"name":"q04-load","namespace":"cka-sim-workloads-scheduling-04"},"spec":{"replicas":1}}
      ```

    **sidecar-not-native-restartpolicy-always/**:
    - `hit.json`: Deployment with sidecar smuggled into `spec.containers` (not `initContainers`).
      ```
      {"apiVersion":"apps/v1","kind":"Deployment","metadata":{"name":"q07-app","namespace":"cka-sim-workloads-scheduling-07"},"spec":{"template":{"spec":{"containers":[{"name":"app","image":"nginx:1.27"},{"name":"log-tailer","image":"busybox:1.36"}]}}}}
      ```
    - `miss.json`: Deployment with native sidecar (initContainer with restartPolicy=Always).
      ```
      {"apiVersion":"apps/v1","kind":"Deployment","metadata":{"name":"q07-app","namespace":"cka-sim-workloads-scheduling-07"},"spec":{"template":{"spec":{"initContainers":[{"name":"log-tailer","image":"busybox:1.36","restartPolicy":"Always"}],"containers":[{"name":"app","image":"nginx:1.27"}]}}}}
      ```
    - `benign.json`: Deployment with a single container, no sidecar at all.
      ```
      {"apiVersion":"apps/v1","kind":"Deployment","metadata":{"name":"single-container-app","namespace":"cka-sim-workloads-scheduling-07"},"spec":{"template":{"spec":{"containers":[{"name":"app","image":"nginx:1.27"}]}}}}
      ```

    All 18 files are newline-terminated JSON (no trailing garbage). Validate each with `python3 -c "import json; json.load(open('$f'))"` post-creation.
  </action>
  <verify>
    <automated>for f in cka-sim/tests/fixtures/{csi-snapshot-wrong-driver,pvc-pending-wffc-unscheduled-consumer,reclaim-policy-delete-data-loss,pvc-accessmode-rwx-on-rwo-sc,hpa-missing-metrics-server,sidecar-not-native-restartpolicy-always}/{hit,miss,benign}.json; do python3 -c "import json,sys; json.load(open(sys.argv[1]))" "$f" || exit 1; done && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - All 18 files exist: `ls cka-sim/tests/fixtures/{csi-snapshot-wrong-driver,pvc-pending-wffc-unscheduled-consumer,reclaim-policy-delete-data-loss,pvc-accessmode-rwx-on-rwo-sc,hpa-missing-metrics-server,sidecar-not-native-restartpolicy-always}/{hit,miss,benign}.json | wc -l` returns 18
    - All 18 are valid JSON: the Python json.load loop in verify returns 0
    - Each `hit.json` contains a resource object with the trap condition (structural greps):
      - csi-snapshot-wrong-driver/hit.json: `grep -q '"readyToUse":false' ... && grep -q '"error"' ...`
      - reclaim-policy-delete-data-loss/hit.json: `grep -q '"persistentVolumeReclaimPolicy":"Delete"' ... && grep -q '"claimRef"' ...`
      - pvc-accessmode-rwx-on-rwo-sc/hit.json: `grep -q '"ReadWriteMany"' ... && grep -q '"manual"' ...`
      - hpa-missing-metrics-server/hit.json: `grep -q 'FailedGetResourceMetric' ...`
      - sidecar-not-native-restartpolicy-always/hit.json: `grep -q '"log-tailer"' ... && ! grep -q '"initContainers"' ...`
    - `bash cka-sim/scripts/test.sh` still green (no regression)
  </acceptance_criteria>
  <done>18 fixtures exist, parse as JSON, Phase 2/3 test suite unchanged.</done>
</task>

</tasks>

<verification>
- 19 entries total in catalog.yaml (13 existing + 6 new)
- `bash cka-sim/scripts/lint-traps.sh` exits 0
- `bash cka-sim/scripts/test.sh` exits 0 (no case regressions)
- 18 fixture JSON files parse cleanly
- No detector functions added to lib/traps.sh in this plan
</verification>

<success_criteria>
- Wave 3 plans (04-15) can list new trap ids in `metadata.yaml.traps[]` and pass lint-packs.sh id-registration check
- Wave 3 plans can use these fixtures directly for detector unit tests (if they add detectors)
</success_criteria>

<output>
After completion, create `.planning/phases/04-storage-workloads-scheduling-packs/04-02-SUMMARY.md` covering:
- 6 new trap ids + their domain split (4 storage / 2 workloads)
- Append-only delta — no existing entries modified
- Fixture catalog (6 dirs × 3 files = 18 JSON objects)
- lint-traps.sh + test.sh both green post-change
</output>
