---
phase: 04-storage-workloads-scheduling-packs
plan: 16
type: execute
wave: 4
depends_on: [03, 04, 05, 06, 07, 08, 09, 10, 11, 12, 13, 14, 15]
files_modified:
  - cka-sim/packs/storage/manifest.yaml
  - cka-sim/packs/workloads-scheduling/manifest.yaml
  - cka-sim/scripts/test.sh
  - scripts/validate-local.sh
  - .github/workflows/validate.yml
autonomous: true
requirements: [PACK-01, PACK-02, PACK-06, PACK-07]
must_haves:
  truths:
    - "storage manifest.yaml lists all 6 Storage questions"
    - "workloads-scheduling manifest.yaml lists all 8 Workloads questions"
    - "cka-sim/scripts/test.sh invokes scripts/lint-coverage.sh after lint-packs.sh"
    - "scripts/validate-local.sh walks cka-sim/**/*.yaml with Python yaml + (optional) yamllint, and invokes cka-sim/scripts/lint-coverage.sh"
    - ".github/workflows/validate.yml bash-tests job runs `Coverage lint` step that invokes cka-sim/scripts/lint-coverage.sh before the existing test.sh step (belt-and-braces: test.sh also calls it, and CI surfaces a named check)"
    - "full chain green: lint-traps + lint-packs + lint-coverage + unit cases all pass"
    - "7 phase VERIFICATION criteria met for the static subset (coverage.yaml asserts Tracker completeness; trap registration green; schema green)"
  artifacts:
    - path: "cka-sim/packs/storage/manifest.yaml"
      provides: "Final 6-question Storage pack manifest"
      contains: "storage-csi-volumesnapshot"
    - path: "cka-sim/packs/workloads-scheduling/manifest.yaml"
      provides: "Final 8-question Workloads pack manifest"
      contains: "workloads-hpa-metrics-server"
    - path: "cka-sim/scripts/test.sh"
      provides: "Test chain extended with lint-coverage between lint-packs and tests/run.sh"
      contains: "lint-coverage.sh"
    - path: "scripts/validate-local.sh"
      provides: "Local pre-push validator extended to cka-sim/**/*.yaml + coverage lint"
      contains: "lint-coverage.sh"
    - path: ".github/workflows/validate.yml"
      provides: "CI bash-tests job extended with explicit Coverage lint step"
      contains: "Coverage lint"
  key_links:
    - from: "scripts/validate-local.sh"
      to: "cka-sim/scripts/lint-coverage.sh"
      via: "direct invocation after the yaml syntax pass"
      pattern: "lint-coverage"
    - from: ".github/workflows/validate.yml"
      to: "cka-sim/scripts/lint-coverage.sh"
      via: "Coverage lint step in bash-tests job"
      pattern: "lint-coverage.sh"
---

<objective>
Final Wave 4 integration. All 12 new questions, 2 retrofits, 7 trap-catalog entries, 2 coverage.yaml files, and lint-coverage.sh now ship green as unit artifacts. This plan wires them into the CI + local-lint chain and finalises pack manifests so `cka-sim drill <pack>` can enumerate every question and lint-coverage.sh resolves every reference.

Purpose: PACK-07 coverage-matrix enforcement becomes active; PACK-01/02 question enumeration finalised; CI + local scripts cover the phase's invariants.
Output: 2 manifest updates + 1 test.sh extension + 1 validate-local.sh extension + 1 validate.yml CI step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-storage-workloads-scheduling-packs/04-CONTEXT.md
@cka-sim/packs/storage/manifest.yaml
@cka-sim/packs/workloads-scheduling/manifest.yaml
@cka-sim/scripts/test.sh
@cka-sim/scripts/lint-coverage.sh
@scripts/validate-local.sh
@.github/workflows/validate.yml
</context>

<interfaces>
Manifest schema (unchanged): top-level `pack:` map + `questions:` list of `{id, path, estimatedMinutes}`.

`cka-sim/scripts/test.sh` (Phase 2 shape): header → lint-traps → lint-packs → tests/run.sh. This plan inserts lint-coverage between lint-packs and tests/run.sh.

`.github/workflows/validate.yml` triggers on `cka-sim/**` and `**.sh` paths (confirmed line 6-12, 14-21). The `bash-tests` job currently runs a single step: `bash cka-sim/scripts/test.sh` (confirmed line 61-62). This plan adds an explicit `Coverage lint` step BEFORE that final step so CI surfaces the named check on failure rather than hiding it inside test.sh output.

`scripts/validate-local.sh` (repo root) currently walks skeletons/ + exercises/ with Python yaml.safe_load + optional yamllint; does not touch cka-sim/. Extend the for-dir loop to include cka-sim, add a call to cka-sim/scripts/lint-coverage.sh at the end.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite both pack manifest.yaml files with complete question lists</name>
  <files>cka-sim/packs/storage/manifest.yaml, cka-sim/packs/workloads-scheduling/manifest.yaml</files>
  <read_first>
    - cka-sim/packs/storage/manifest.yaml (current 1-question file — preserve the pack: block)
    - cka-sim/packs/workloads-scheduling/manifest.yaml (current 1-question file)
    - cka-sim/packs/storage/02-storageclass-dynamic/metadata.yaml (verify id + path + estimatedMinutes match authored values)
    - cka-sim/packs/storage/03-access-modes-reclaim/metadata.yaml
    - cka-sim/packs/storage/04-csi-volumesnapshot/metadata.yaml
    - cka-sim/packs/storage/05-wait-for-first-consumer/metadata.yaml
    - cka-sim/packs/storage/06-pvc-mount-pod/metadata.yaml
    - cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/metadata.yaml
    - cka-sim/packs/workloads-scheduling/03-configmap-secret-env-volume/metadata.yaml
    - cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/metadata.yaml
    - cka-sim/packs/workloads-scheduling/05-daemonset/metadata.yaml
    - cka-sim/packs/workloads-scheduling/06-static-pod/metadata.yaml
    - cka-sim/packs/workloads-scheduling/07-native-sidecar/metadata.yaml
    - cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/metadata.yaml
  </read_first>
  <action>
    Overwrite both manifests. Update the `description` text to reflect Phase 4 completion; append every authored question to the `questions:` list.

    **cka-sim/packs/storage/manifest.yaml** — exact contents:
    ```
    pack:
      id: storage
      domain: storage
      weight: 10
      description: "Storage 10% domain pack (PACK-01) -- full v1.35 Tracker coverage."
    questions:
      - id: storage-pvc-binding
        path: 01-pvc-binding
        estimatedMinutes: 8
      - id: storage-storageclass-dynamic
        path: 02-storageclass-dynamic
        estimatedMinutes: 7
      - id: storage-access-modes-reclaim
        path: 03-access-modes-reclaim
        estimatedMinutes: 9
      - id: storage-csi-volumesnapshot
        path: 04-csi-volumesnapshot
        estimatedMinutes: 9
      - id: storage-wait-for-first-consumer
        path: 05-wait-for-first-consumer
        estimatedMinutes: 7
      - id: storage-pvc-mount-pod
        path: 06-pvc-mount-pod
        estimatedMinutes: 7
    ```

    **cka-sim/packs/workloads-scheduling/manifest.yaml** — exact contents:
    ```
    pack:
      id: workloads-scheduling
      domain: workloads-scheduling
      weight: 15
      description: "Workloads & Scheduling 15% domain pack (PACK-02) -- full v1.35 Tracker coverage including CG-06 + CG-08."
    questions:
      - id: workloads-deployment-requests
        path: 01-deployment-requests
        estimatedMinutes: 7
      - id: workloads-rolling-update-rollback
        path: 02-rolling-update-rollback
        estimatedMinutes: 7
      - id: workloads-configmap-secret-env-volume
        path: 03-configmap-secret-env-volume
        estimatedMinutes: 8
      - id: workloads-hpa-metrics-server
        path: 04-hpa-metrics-server
        estimatedMinutes: 9
      - id: workloads-daemonset
        path: 05-daemonset
        estimatedMinutes: 7
      - id: workloads-static-pod
        path: 06-static-pod
        estimatedMinutes: 8
      - id: workloads-native-sidecar
        path: 07-native-sidecar
        estimatedMinutes: 8
      - id: workloads-nodeselector-affinity-taints
        path: 08-nodeselector-affinity-taints
        estimatedMinutes: 9
    ```

    Every question-id above MUST match the `id:` value authored in the corresponding `metadata.yaml`. Every `path:` must match the directory name relative to the pack. If lint-coverage.sh reports a mismatch (e.g. `question-id 'storage-foo' referenced in coverage.yaml is not in manifest.yaml`), a metadata drift exists — fix the manifest to match metadata (NOT the other way around; coverage.yaml in Plan 03 already committed to specific ids).
  </action>
  <verify>
    <automated>python3 -c "import yaml; s=yaml.safe_load(open('cka-sim/packs/storage/manifest.yaml')); w=yaml.safe_load(open('cka-sim/packs/workloads-scheduling/manifest.yaml')); assert len(s['questions']) == 6, f'storage has {len(s[\"questions\"])} questions, need 6'; assert len(w['questions']) == 8, f'workloads has {len(w[\"questions\"])} questions, need 8'" && bash cka-sim/scripts/lint-packs.sh</automated>
  </verify>
  <acceptance_criteria>
    - Storage manifest has 6 questions: `python3 -c "import yaml; print(len(yaml.safe_load(open('cka-sim/packs/storage/manifest.yaml'))['questions']))"` prints `6`
    - Workloads manifest has 8 questions: same check returns `8`
    - Every question id listed in manifests matches a dir metadata.yaml id: for each `id:` line in `cka-sim/packs/<pack>/manifest.yaml`, `grep -q "^id: <id>" cka-sim/packs/<pack>/<path>/metadata.yaml` returns 0
    - `bash cka-sim/scripts/lint-packs.sh` exits 0
    - `bash cka-sim/scripts/lint-coverage.sh` exits 0 (all Tracker references now resolve to manifest-registered questions)
  </acceptance_criteria>
  <done>Both manifests extended; lint-packs + lint-coverage both green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire lint-coverage.sh into cka-sim/scripts/test.sh, scripts/validate-local.sh, and .github/workflows/validate.yml</name>
  <files>cka-sim/scripts/test.sh, scripts/validate-local.sh, .github/workflows/validate.yml</files>
  <read_first>
    - cka-sim/scripts/test.sh (4-step structure — inject lint-coverage between step 2 lint-packs and step 3 tests/run.sh)
    - scripts/validate-local.sh (understand for-dir loop pattern; note the script uses `set -euo pipefail` and has `REPO_ROOT`)
    - cka-sim/scripts/lint-coverage.sh (confirm exit-code semantics: 0 on green, 1 on error)
    - .github/workflows/validate.yml (bash-tests job at lines 55-62 — insert a `Coverage lint` step before the `Run cka-sim test suite` step)
  </read_first>
  <action>
    **File 1 — `cka-sim/scripts/test.sh`** — insert one block between existing step 2 (`lint-packs`) and step 3 (`tests/run.sh`). Current file ends:
    ```
    info "step 2: lint packs"
    "$CKA_SIM_ROOT/scripts/lint-packs.sh"
    ok "pack lint passed"

    info "step 3: run bash unit cases"
    "$CKA_SIM_ROOT/tests/run.sh"
    ok "all unit cases passed"

    ok "test.sh complete"
    ```

    Rewrite to insert the coverage step and renumber subsequent steps:
    ```
    info "step 2: lint packs"
    "$CKA_SIM_ROOT/scripts/lint-packs.sh"
    ok "pack lint passed"

    info "step 3: lint coverage"
    "$CKA_SIM_ROOT/scripts/lint-coverage.sh"
    ok "coverage lint passed"

    info "step 4: run bash unit cases"
    "$CKA_SIM_ROOT/tests/run.sh"
    ok "all unit cases passed"

    ok "test.sh complete"
    ```

    Preserve the header, CKA_SIM_ROOT export, colors/log sourcing, and step 1 (lint-traps) unchanged.

    **File 2 — `scripts/validate-local.sh`** — extend the existing for-dir loop to include `cka-sim` (yaml.safe_load walks every .yaml), and append a coverage-lint call after the yamllint block.

    Open the current file (61 lines). Two surgical edits:

    (a) In the `for dir in skeletons exercises; do ... done` block (line 21), change to `for dir in skeletons exercises cka-sim; do` so Python yaml.safe_load walks every cka-sim/**/*.yaml too.

    (b) After the yamllint block (ends at line 51 — `fi`), and before the final `if [ $errors -gt 0 ]` check (line 54), insert:
    ```
    # --- cka-sim coverage + trap lints ---
    if [ -x "$REPO_ROOT/cka-sim/scripts/lint-coverage.sh" ]; then
      echo ""
      echo "=== cka-sim coverage lint ==="
      if bash "$REPO_ROOT/cka-sim/scripts/lint-coverage.sh"; then
        echo -e "${GREEN}cka-sim coverage lint passed${NC}"
      else
        echo -e "${RED}cka-sim coverage lint failed${NC}"
        errors=$((errors + 1))
      fi
    fi
    ```

    Use the Edit tool for both changes — do NOT rewrite validate-local.sh wholesale (preserves shebang, Python yaml block, yamllint block, etc.).

    **File 3 — `.github/workflows/validate.yml`** — add an explicit `Coverage lint` step to the bash-tests job BEFORE the existing `Run cka-sim test suite` step. Current bash-tests job (lines 55-62):
    ```
      bash-tests:
        name: Bash unit tests (traps + grade)
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4

          - name: Run cka-sim test suite
            run: bash cka-sim/scripts/test.sh
    ```

    Insert a `Coverage lint` step between `actions/checkout@v4` and `Run cka-sim test suite` via the Edit tool. Target state:
    ```
      bash-tests:
        name: Bash unit tests (traps + grade)
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4

          - name: Coverage lint
            run: bash cka-sim/scripts/lint-coverage.sh

          - name: Run cka-sim test suite
            run: bash cka-sim/scripts/test.sh
    ```

    Why: test.sh already chains lint-coverage internally (File 1), but a named CI step surfaces coverage failures directly in the GitHub Checks tab rather than burying them inside the test-suite output.
  </action>
  <verify>
    <automated>bash -n cka-sim/scripts/test.sh && bash -n scripts/validate-local.sh && grep -q 'lint-coverage.sh' cka-sim/scripts/test.sh && grep -q 'lint-coverage.sh' scripts/validate-local.sh && grep -q 'Coverage lint' .github/workflows/validate.yml && grep -q 'bash cka-sim/scripts/lint-coverage.sh' .github/workflows/validate.yml && bash cka-sim/scripts/test.sh && bash scripts/validate-local.sh</automated>
  </verify>
  <acceptance_criteria>
    - `cka-sim/scripts/test.sh` invokes `lint-coverage.sh`: `grep -q 'lint-coverage.sh' cka-sim/scripts/test.sh`
    - `scripts/validate-local.sh` invokes `lint-coverage.sh`: `grep -q 'lint-coverage.sh' scripts/validate-local.sh`
    - `scripts/validate-local.sh` now walks `cka-sim`: `grep -qE 'for dir in .*cka-sim' scripts/validate-local.sh`
    - `.github/workflows/validate.yml` has `Coverage lint` step: `grep -q '^[[:space:]]*- name: Coverage lint$' .github/workflows/validate.yml`
    - `.github/workflows/validate.yml` coverage step invokes the script: `grep -q 'bash cka-sim/scripts/lint-coverage.sh' .github/workflows/validate.yml`
    - `.github/workflows/validate.yml` was modified: `git diff .github/workflows/validate.yml | wc -l` returns NON-zero (diff is non-empty)
    - `bash cka-sim/scripts/test.sh` exits 0 — the full chain (lint-traps → lint-packs → lint-coverage → unit cases) is green
    - `bash scripts/validate-local.sh` exits 0 on a clean tree
  </acceptance_criteria>
  <done>CI + local chain wired; all lints green; Coverage lint is a named CI step.</done>
</task>

</tasks>

<verification>
Final phase gate. All 7 VERIFICATION must_haves from CONTEXT §Runtime + Verification Contract:

1. Storage pack has >=1 question per Tracker checkbox — `bash cka-sim/scripts/lint-coverage.sh storage` exits 0
2. Workloads pack has >=1 question per Tracker checkbox — `bash cka-sim/scripts/lint-coverage.sh workloads-scheduling` exits 0
3. Every new question's metadata.yaml passes schema lint — `bash cka-sim/scripts/lint-packs.sh` exits 0 (lint covers id, domain, estimatedMinutes in [4,12], verified_against=="1.35", traps>=3)
4. Every trap ID referenced exists in catalog — lint-packs.sh id-registration check enforces (covered by verify above)
5. `cka-sim drill storage` + `cka-sim drill workloads-scheduling` can enumerate + run every question — MANUAL verification on 1+2 cluster (documented in 04-VERIFICATION.md; same model as Phase 3)
6. 7 new trap entries pass lint-traps.sh — `bash cka-sim/scripts/lint-traps.sh` exits 0
7. lint-coverage.sh reports 100% Tracker coverage — `bash cka-sim/scripts/lint-coverage.sh` exits 0

Criteria 1-4, 6, 7 are static/automated — this plan's verify block covers them.
Criterion 5 is the live-cluster handoff per CONTEXT §Runtime + Verification: the orchestrator queues it for user verification after this plan completes.
</verification>

<success_criteria>
- Phase 4 unit tests + lints all green (test.sh, validate-local.sh, CI validate.yml)
- Both domain packs enumerate all questions
- Coverage matrix 100% for Storage + Workloads domains
- Ready for the user to run `cka-sim drill storage` + `cka-sim drill workloads-scheduling` on their 1+2 cluster for criterion-5 verification (documented in 04-VERIFICATION.md in post-phase step)
</success_criteria>

<output>
After completion, create `.planning/phases/04-storage-workloads-scheduling-packs/04-16-SUMMARY.md` covering:
- Final question counts: Storage 6, Workloads 8 (total 14 across both packs)
- Lint chain: lint-traps (20 entries green) → lint-packs (14 question dirs green) → lint-coverage (Tracker coverage green) → tests/run.sh (Phase 2/3 cases still green + Plan 01 setup_helpers cases + Plan 03 lint_coverage cases pass)
- CI integration: new `Coverage lint` step in bash-tests job surfaces coverage failures as a named GitHub check
- Live-cluster manual verification checklist for criterion 5 (per-question round-trip under `cka-sim drill`) — left for user to run on CP node
- Phase closure: all 7 VERIFICATION must_haves satisfied (6 static automated, 1 manual live-cluster)
</output>
