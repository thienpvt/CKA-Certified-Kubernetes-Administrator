---
phase: 06-troubleshooting-pack
plan: 08
type: execute
wave: 2
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - cka-sim/packs/troubleshooting/06-broken-kubelet/setup.sh
  - cka-sim/packs/troubleshooting/06-broken-kubelet/grade.sh
  - cka-sim/packs/troubleshooting/06-broken-kubelet/reset.sh
  - cka-sim/packs/troubleshooting/06-broken-kubelet/ref-solution.sh
  - cka-sim/packs/troubleshooting/06-broken-kubelet/metadata.yaml
  - cka-sim/packs/troubleshooting/06-broken-kubelet/question.md
  - cka-sim/tests/fixtures/phase-06/troubleshooting-06-broken-kubelet/stub-responses.json
  - cka-sim/tests/fixtures/phase-06/troubleshooting-06-broken-kubelet/expected-fail-score.txt
  - cka-sim/tests/fixtures/phase-06/troubleshooting-06-broken-kubelet/expected-pass-score.txt
autonomous: true
requirements: [PACK-05, PACK-06, PACK-07]
tags: [troubleshooting, kubelet, sandbox, phase-06]
validation:
  sampling_rate: per-task-commit
  quick_command: "bash cka-sim/scripts/lint-packs.sh && bash -n cka-sim/packs/troubleshooting/06-broken-kubelet/*.sh"
  full_command: "bash cka-sim/scripts/test.sh"
  validation_md_rows:
    - 06-08-XX
must_haves:
  truths:
    - "sandbox path /tmp/q06-kubelet-flags/ is DISTINCT from Phase 5 Q07's /tmp/q07-kubelet-flags/ (no collision)"
    - "setup seeds /tmp/q06-kubelet-flags/kubeadm-flags.env with a broken KUBELET_KUBEADM_ARGS line combining 4 defects: removed --container-runtime=remote flag, bare --container-runtime-endpoint without unix:// scheme, stray double-quote mid-value (malformed quoting), and a runtime-endpoint reference in the companion kubelet.conf placeholder"
    - "setup seeds a companion /tmp/q06-kubelet-flags/kubelet.conf whose body references a runtime-endpoint flag (defect 4 — runtime flags do not belong in kubeconfig)"
    - "setup NEVER touches /var/lib/kubelet/ or /etc/kubernetes/ (pass G forbidden-command guard enforces)"
    - "setup NEVER calls systemctl (pass G enforces)"
    - "grade asserts kubeadm-flags.env exists, is bash-parseable in a subshell source, and contains --container-runtime-endpoint=unix:///run/cri-dockerd.sock"
    - "grade records removed-container-runtime-flag when file still contains --container-runtime=remote"
    - "grade records cri-endpoint-unix-prefix-missing when --container-runtime-endpoint= lacks the unix:// scheme"
    - "grade records kubelet-runtime-flag-in-kubeconfig when kubelet.conf has any --container-runtime-endpoint= text"
    - "grade records kubelet-flag-file-malformed-quoting when a subshell source of the env file exits non-zero (bash cannot parse)"
    - "reset.sh sentinel-guarded /tmp/q06-kubelet-flags/ removal; NEVER systemctl, NEVER /var/lib/kubelet/"
    - "ref-solution writes canonical KUBELET_KUBEADM_ARGS with unix:///run/cri-dockerd.sock endpoint to sandbox + empties kubelet.conf"
    - "metadata.yaml references[] contains cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/ per D-05 (exact-role analog per PATTERNS)"
    - "question.md symptoms-only (D-04) — candidate-facing prose uses node-agent / node-level runtime config language only; forbidden tokens kubelet, kubeadm, systemctl MUST NOT appear as bare tokens (the literal sandbox path and file name are permitted exceptions and excluded from the check)"
    - "Decision coverage: D-01 (Q6 of 6), D-03 (Q6 is position 6 — broken kubelet — in progressive ramp), D-06 / D-07 / D-08 (references[] metadata-only, pack-path, prior-art only)."
  artifacts:
    - path: "cka-sim/packs/troubleshooting/06-broken-kubelet/setup.sh"
      provides: "Sandbox seeding of broken kubeadm-flags.env + kubelet.conf placeholder"
      contains: "/tmp/q06-kubelet-flags"
      contains_extra: "KUBELET_KUBEADM_ARGS"
    - path: "cka-sim/packs/troubleshooting/06-broken-kubelet/grade.sh"
      provides: "4 trap detectors (removed flag, unix prefix, runtime-in-kubeconfig, malformed quoting) + endpoint correctness assertion"
      contains: "source \"$flags\""
    - path: "cka-sim/packs/troubleshooting/06-broken-kubelet/metadata.yaml"
      provides: "PACK-06 schema + cross-pack ref to cluster-architecture/07-cri-dockerd-endpoint/"
      contains: "cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/"
  key_links:
    - from: "cka-sim/packs/troubleshooting/06-broken-kubelet/grade.sh"
      to: "cka-sim/traps/catalog.yaml (kubelet-flag-file-malformed-quoting added in P02; 3 reused IDs)"
      via: "record_trap 4 ids"
      pattern: "kubelet-flag-file-malformed-quoting"
---

<objective>
Author Q06 `06-broken-kubelet` — a sandbox kubelet-flags repair question per RESEARCH §4 "Q6 broken-kubelet" and PATTERNS §4 P08 (exact-role analog = Phase 5 Cluster-Arch Q07 cri-dockerd-endpoint). Tracker coverage: Read kubelet logs with journalctl (symptoms pointer in question.md).

Broken baseline combines 4 defects in `/tmp/q06-kubelet-flags/kubeadm-flags.env` so the candidate diagnoses each:
1. Deprecated flag `--container-runtime=remote` still present (removed in v1.27+, trap `removed-container-runtime-flag`).
2. `--container-runtime-endpoint=/run/cri-dockerd.sock` missing `unix://` scheme (trap `cri-endpoint-unix-prefix-missing`).
3. Stray `"` mid-value makes bash `source` fail (trap `kubelet-flag-file-malformed-quoting` — NEW in P02).
4. Placeholder `kubelet.conf` contains a runtime-endpoint reference that doesn't belong in kubeconfig (trap `kubelet-runtime-flag-in-kubeconfig`).

Critical host-safety (D-09, D-12): sandbox `/tmp/q06-kubelet-flags/` is distinct from Phase 5's `/tmp/q07-kubelet-flags/`. Grader never touches `/var/lib/kubelet/`; reset never runs `systemctl`.

**D-04 symptom-only invariant (reviewer-enforced):** candidate-facing question.md MUST NOT name `kubelet`, `kubeadm`, or `systemctl` as bare tokens. Candidates must discover the topic via file inspection, not from the prompt. The sandbox path `/tmp/q06-kubelet-flags/` and the file name `kubeadm-flags.env` ARE permitted as literal references the candidate needs to open — the grep check excludes those paths before scanning for forbidden tokens. setup.sh / grade.sh / ref-solution.sh / reset.sh are free to reference kubelet internals; only question.md is candidate-facing.

Output: 6 question files + 3 fixtures. 11 minutes per RESEARCH §1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-troubleshooting-pack/06-CONTEXT.md
@.planning/phases/06-troubleshooting-pack/06-RESEARCH.md
@.planning/phases/06-troubleshooting-pack/06-PATTERNS.md
@.planning/phases/06-troubleshooting-pack/06-VALIDATION.md
@cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/setup.sh
@cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/grade.sh
@cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/reset.sh
@cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/ref-solution.sh
@cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/metadata.yaml
@cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/question.md
@cka-sim/lib/setup.sh
@cka-sim/lib/grade.sh
</context>

<interfaces>
Lab ns: `cka-sim-troubleshooting-06`. Question id: `troubleshooting-broken-kubelet`. Pack: `troubleshooting`. Sandbox: `/tmp/q06-kubelet-flags/`.

Sandbox file contract:
- `.cka-sim-sentinel` — reset gate.
- `kubeadm-flags.env` — seeded broken with 4 defects encoded in one line.
- `kubelet.conf` — seeded with a runtime-endpoint reference that does not belong in kubeconfig (defect 4).

**Broken `kubeadm-flags.env` required byte content (equivalent to):**

`KUBELET_KUBEADM_ARGS="--container-runtime=remote --container-runtime-endpoint=/run/cri-dockerd.sock --pod-"infra-container-image=registry.k8s.io/pause:3.10"`

Four encoded defects:
- `--container-runtime=remote` (defect 1 — deprecated in v1.27+).
- `--container-runtime-endpoint=/run/cri-dockerd.sock` with NO `unix://` prefix (defect 2).
- Stray `"` between `--pod-` and `infra-container-image` (defect 3 — breaks bash `source`).
- Defect 4 lives in the companion `kubelet.conf` (see below).

To emit the `--container-runtime=remote` literal into the file WITHOUT tripping `lint-deprecated-strings.sh` on setup.sh itself, use the string-split trick from Phase 5 Q07 (setup.sh variable `removed_flag="--container-runtime""=remote"`). Bash string-concatenates adjacent quoted segments; the emitted file contains the deprecated literal, but grep on setup.sh source does not match the deprecated-strings pattern.

**Broken `kubelet.conf` required byte content (equivalent to 3 comment lines):**

- Line 1: `# This kubeconfig-shaped placeholder accidentally references runtime flags.`
- Line 2: `# Runtime args belong in /var/lib/kubelet/kubeadm-flags.env, never in kubelet.conf.`
- Line 3: `# --container-runtime-endpoint=unix:///run/cri-dockerd.sock`

The comment-form reference is enough for the grader's `grep -q 'container-runtime-endpoint' kubelet.conf` to fire the trap (comments count — the whole point is that text-inspection flags the config drift regardless of whether the line is commented).

**Canonical fixed `kubeadm-flags.env` (ref-solution writes this):**

`KUBELET_KUBEADM_ARGS="--container-runtime-endpoint=unix:///run/cri-dockerd.sock --pod-infra-container-image=registry.k8s.io/pause:3.10"`

No deprecated `--container-runtime=remote`; `unix://` prefix present; single bash-parseable string; no stray quote.

Grader oracle paths:
1. File exists + non-empty: `[[ -s "$sandbox/kubeadm-flags.env" ]]`.
2. Subshell source parses: `( set +u; source "$sandbox/kubeadm-flags.env" >/dev/null 2>&1; )` — exit 0 = bash parses; else `kubelet-flag-file-malformed-quoting` trap fires. Fallback hardening per T-6-22: if bash on the dev platform silently parses the broken form, swap the check to `bash -n "$sandbox/kubeadm-flags.env"`.
3. Correct endpoint present: `grep -qE 'KUBELET_KUBEADM_ARGS=.*--container-runtime-endpoint=unix:///run/cri-dockerd\.sock' "$sandbox/kubeadm-flags.env"`.
4. Deprecated flag absent: re-use the string-split trick in grade.sh (`removed_flag="container-runtime""=remote"`), then `grep -q "$removed_flag" "$sandbox/kubeadm-flags.env"`. Match → `removed-container-runtime-flag` trap.
5. Runtime in kubeconfig: `grep -q 'container-runtime-endpoint' "$sandbox/kubelet.conf"` → `kubelet-runtime-flag-in-kubeconfig` trap.
6. Unix-prefix missing (lifted verbatim from Phase 5 Q07 detector): awk-extract the `container-runtime-endpoint=` value from the flags file, strip quotes, and if non-empty + NOT `unix://*` → `cri-endpoint-unix-prefix-missing` trap.

Cross-pack reference (D-05): `cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/` — the natural prior-art, Phase 5 shipped.

Pass G forbidden-command guard forbids `/var/lib/kubelet/` writes, `systemctl`, `/etc/kubernetes/` writes. All Q06 scripts stay in `/tmp/q06-kubelet-flags/`.

Fixture path: `cka-sim/tests/fixtures/phase-06/troubleshooting-06-broken-kubelet/`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Question files (6) + fixture tree (3)</name>
  <files>cka-sim/packs/troubleshooting/06-broken-kubelet/setup.sh, cka-sim/packs/troubleshooting/06-broken-kubelet/grade.sh, cka-sim/packs/troubleshooting/06-broken-kubelet/reset.sh, cka-sim/packs/troubleshooting/06-broken-kubelet/ref-solution.sh, cka-sim/packs/troubleshooting/06-broken-kubelet/metadata.yaml, cka-sim/packs/troubleshooting/06-broken-kubelet/question.md, cka-sim/tests/fixtures/phase-06/troubleshooting-06-broken-kubelet/stub-responses.json, cka-sim/tests/fixtures/phase-06/troubleshooting-06-broken-kubelet/expected-fail-score.txt, cka-sim/tests/fixtures/phase-06/troubleshooting-06-broken-kubelet/expected-pass-score.txt</files>
  <read_first>
    - cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/setup.sh (EXACT-ROLE analog — string-split trick for lint-deprecated-strings, sentinel, sandbox path)
    - cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/grade.sh (file-content grader with 3 trap detectors + manual increment idiom — Q06 adds 1 trap + the subshell-source check)
    - cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/reset.sh (sentinel-guarded rm -rf + ns delete)
    - cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/ref-solution.sh (cat > heredoc writing canonical fixed form)
    - cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/metadata.yaml (8-field shape + references list)
    - cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/question.md (sandbox-path prose + constraints — Q06 adapts the structure but scrubs the bare `kubelet`/`kubeadm`/`systemctl` tokens per D-04)
    - cka-sim/lib/setup.sh (ensure_lab_ns + wait_for_ns_active)
    - cka-sim/traps/catalog.yaml (confirm P02 entry kubelet-flag-file-malformed-quoting + 3 reused IDs are live)
    - .planning/phases/06-troubleshooting-pack/06-RESEARCH.md (§4 Q6 broken line example; §9 no-/var/lib/kubelet + no-systemctl landmines)
  </read_first>
  <action>
    Produce 6 question files and 3 fixtures. Use Phase 5 cluster-architecture/07-cri-dockerd-endpoint/ as the EXACT-ROLE analog for sandbox seeding + trap detectors + reset.

    **setup.sh — required shape:**
    - Shebang + `set -euo pipefail` + env guards (`CKA_SIM_LAB_NS`, `CKA_SIM_ROOT`).
    - `source "$CKA_SIM_ROOT/lib/setup.sh"` with shellcheck directive.
    - Constants: `CKA_SIM_PACK="troubleshooting"`, `CKA_SIM_QUESTION_ID="troubleshooting-broken-kubelet"`, `sandbox="/tmp/q06-kubelet-flags"`, `removed_flag="--container-runtime""=remote"` (string-split — bash concatenates the two adjacent quoted segments at runtime; lint-deprecated-strings does not pattern-match this setup.sh).
    - `cka_sim::setup::ensure_lab_ns` + `cka_sim::setup::wait_for_ns_active` (ns needed for reset's ns-delete path + pass D 6-files check).
    - `mkdir -p "$sandbox" && touch "$sandbox/.cka-sim-sentinel"`.
    - Do NOT `cp` from `/var/lib/kubelet/` (Phase 5 Q07 does this; Q06 intentionally skips — setup is self-contained, generates broken content from scratch).
    - Emit broken kubeadm-flags.env via a single `printf` that expands `$removed_flag` inline. The emitted file MUST contain the exact line `KUBELET_KUBEADM_ARGS="--container-runtime=remote --container-runtime-endpoint=/run/cri-dockerd.sock --pod-"infra-container-image=registry.k8s.io/pause:3.10"`. The stray `"` between `--pod-` and `infra-container-image` is the malformed-quoting defect.
    - Emit broken kubelet.conf via `cat > "$sandbox/kubelet.conf" <<'EOF' ... EOF` with the 3-comment-line body from Interfaces. The grader's grep hits `container-runtime-endpoint` regardless of `#` comment prefix — that is the point.
    - Forbidden in setup.sh: any write into `/var/lib/kubelet/`; any write into `/etc/kubernetes/`; any `systemctl` token.

    **grade.sh — required shape:**
    - Shebang + `set -uo pipefail` + env guards.
    - `source "$CKA_SIM_ROOT/lib/grade.sh"` + `source "$CKA_SIM_ROOT/lib/traps.sh"`.
    - Locals: `sandbox="/tmp/q06-kubelet-flags"`, `flags="$sandbox/kubeadm-flags.env"`, `kubeconfig="$sandbox/kubelet.conf"`, `removed_flag="container-runtime""=remote"` (string-split so this grader file stays lint-deprecated-strings-clean).
    - Assertion 1 — file exists non-empty. Manual increment: `TOTAL++`; `[[ -s "$flags" ]]` pass/fail.
    - Assertion 2 — subshell source parses (malformed-quoting detector). Manual increment: `TOTAL++`. Use `( set +u; source "$flags" >/dev/null 2>&1; )` — the subshell + `set +u` combination prevents the grader's own shell state from being polluted by the sourced file or crashing on unbound vars. On rc 0: PASSED++, PASSES+="kubeadm-flags.env is bash-parseable", `ok`. Else: FAILS+, `err`, `cka_sim::grade::record_trap kubelet-flag-file-malformed-quoting`. Fallback hardening per T-6-22: if author observes permissive bash parsing on the dev platform, swap to `bash -n "$flags"` (syntax check) — `bash -n` reliably catches unmatched quotes.
    - Assertion 3 — correct CRI endpoint present. Manual increment: `TOTAL++`. `grep -qE 'KUBELET_KUBEADM_ARGS=.*--container-runtime-endpoint=unix:///run/cri-dockerd\.sock' "$flags"` → pass, else fail.
    - Trap detector — `removed-container-runtime-flag`: `grep -q "$removed_flag" "$flags" 2>/dev/null` → `cka_sim::grade::record_trap removed-container-runtime-flag`.
    - Trap detector — `kubelet-runtime-flag-in-kubeconfig`: `grep -q 'container-runtime-endpoint' "$kubeconfig" 2>/dev/null` → `cka_sim::grade::record_trap kubelet-runtime-flag-in-kubeconfig`.
    - Trap detector — `cri-endpoint-unix-prefix-missing` (lifted verbatim from Phase 5 Q07): awk-extract the `container-runtime-endpoint=` value from `$flags` (split on the literal, take field 2, split on whitespace, take first token, strip quotes, head -1). If non-empty AND NOT `unix://*` → `cka_sim::grade::record_trap cri-endpoint-unix-prefix-missing`.
    - Final: `cka_sim::grade::emit_result`.
    - Forbidden in grade.sh: any mutating kubectl verb; any `systemctl`; any `/var/lib/kubelet/` or `/etc/kubernetes/` reference.

    **ref-solution.sh — required shape:**
    - Shebang + `set -euo pipefail`.
    - `sandbox="/tmp/q06-kubelet-flags"`. `mkdir -p "$sandbox"` (idempotent; sentinel preserved by setup).
    - `cat > "$sandbox/kubeadm-flags.env" <<'EOF' ... EOF` writing the canonical fixed line (no `$removed_flag` — this is the clean form; see Interfaces for exact bytes).
    - `: > "$sandbox/kubelet.conf"` (empty out the kubeconfig placeholder; the `container-runtime-endpoint` comment reference disappears).

    **reset.sh — required shape (adapt Phase 5 Q07 verbatim):**
    - Shebang + `set -euo pipefail` + `CKA_SIM_LAB_NS` guard.
    - `sandbox="/tmp/q06-kubelet-flags"`. Sentinel-guarded `rm -rf "$sandbox"`.
    - `kubectl delete namespace "$CKA_SIM_LAB_NS" --ignore-not-found --wait=false`.
    - Forbidden: any `systemctl`; any `/var/lib/kubelet/` touch.

    **metadata.yaml — required fields:**
    - `id: troubleshooting-broken-kubelet`
    - `domain: troubleshooting`
    - `estimatedMinutes: 11`
    - `verified_against: "1.35"`
    - `traps` (4): `removed-container-runtime-flag`, `cri-endpoint-unix-prefix-missing`, `kubelet-runtime-flag-in-kubeconfig`, `kubelet-flag-file-malformed-quoting`.
    - `references` (3):
      1. `kind: prior-art-exercise`, `target: cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/`, `note: "Related prior-art: CRI-dockerd endpoint flag file semantics"`.
      2. `kind: k8s-doc`, `target: https://kubernetes.io/docs/reference/command-line-tools-reference/kubelet/`, `note: kubelet CLI reference and KUBELET_KUBEADM_ARGS shape`.
      3. `kind: concerns-md`, `target: .planning/codebase/CONCERNS.md`, `note: Correct node-agent runtime flag file path and endpoint scheme`.
    - Four traps exceed GRADE-04 floor. The 4th is the new P02 entry.

    **question.md — required shape (D-04 symptoms-only; reviewer-enforced invariant):**
    - Title: `# Troubleshooting: a node-agent runtime configuration file is rejected at parse time`.
    - Banner: `**Domain:** Troubleshooting  |  **Estimated time:** 11 minutes`.
    - Narrative: the sandbox file `/tmp/q06-kubelet-flags/kubeadm-flags.env` is the node-level runtime configuration source for a worker in the lab cluster. As currently written, the file is rejected at parse time AND specifies a runtime endpoint that the current node-agent no longer supports. A companion placeholder `/tmp/q06-kubelet-flags/kubelet.conf` exists in the same sandbox and also contains a drifted reference. Repair BOTH sandbox files so the `KUBELET_KUBEADM_ARGS` line is a bash-parseable single line, uses the `unix://` scheme for the container runtime endpoint, and the `kubelet.conf` placeholder contains no runtime-endpoint reference.
    - `## Sandbox` section: working directory `/tmp/q06-kubelet-flags/`; candidate files `kubeadm-flags.env` + `kubelet.conf`.
    - `## Tasks` (3 bullets): inspect both sandbox files and identify every reason the node-agent would refuse to parse or reject this configuration; edit both files in place so `KUBELET_KUBEADM_ARGS` parses cleanly, the container runtime endpoint uses `unix://`, and no runtime flags leak into the kubeconfig placeholder; confirm `bash -c 'source /tmp/q06-kubelet-flags/kubeadm-flags.env'` exits 0.
    - `## Constraints`: do NOT modify any file outside `/tmp/q06-kubelet-flags/`; do NOT run any live-service-restart commands; the endpoint must include the `unix://` scheme.
    - `## Verify yourself`: `bash -c 'source /tmp/q06-kubelet-flags/kubeadm-flags.env'` and `grep 'unix:///run/cri-dockerd.sock' /tmp/q06-kubelet-flags/kubeadm-flags.env` — both must exit 0.
    - **D-04 forbidden tokens (MUST NOT appear as bare tokens in question.md prose):** `kubelet`, `kubeadm`, `systemctl`. Exclusion: the literal sandbox path `/tmp/q06-kubelet-flags/` and the literal file name `kubeadm-flags.env` are permitted since they are the exact strings the candidate must cat/edit. The acceptance grep strips those paths before scanning for forbidden tokens. Use `node-agent`, `node-level runtime configuration`, `runtime endpoint`, `kubeconfig placeholder` in the prose.

    **Fixtures — required shape:**
    - `stub-responses.json`: minimal content-grader fixture (kubectl is barely invoked — only the ns sanity check).
    - `expected-fail-score.txt`: single line `SCORE: 1/3` (pre-fix: file-exists passes, subshell-source fails + malformed-quoting trap, endpoint-correctness fails + removed-flag trap + unix-prefix trap + runtime-in-kubeconfig trap).
    - `expected-pass-score.txt`: single line `SCORE: 3/3` (post-fix: all 3 pass; zero traps).

    `chmod +x` on 4 .sh files.
  </action>
  <verify>
    <automated>chmod +x cka-sim/packs/troubleshooting/06-broken-kubelet/*.sh && for f in setup grade reset ref-solution; do bash -n "cka-sim/packs/troubleshooting/06-broken-kubelet/$f.sh" || exit 1; done && bash cka-sim/scripts/lint-packs.sh && bash cka-sim/scripts/lint-deprecated-strings.sh && bash cka-sim/scripts/lint-traps.sh && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - 6 files + 4 exec bits: `for f in setup.sh grade.sh reset.sh ref-solution.sh; do test -x cka-sim/packs/troubleshooting/06-broken-kubelet/$f || exit 1; done && test -f cka-sim/packs/troubleshooting/06-broken-kubelet/metadata.yaml && test -f cka-sim/packs/troubleshooting/06-broken-kubelet/question.md`
    - Fixture tree: `test -f cka-sim/tests/fixtures/phase-06/troubleshooting-06-broken-kubelet/stub-responses.json && test -f cka-sim/tests/fixtures/phase-06/troubleshooting-06-broken-kubelet/expected-fail-score.txt && test -f cka-sim/tests/fixtures/phase-06/troubleshooting-06-broken-kubelet/expected-pass-score.txt`
    - **stub-responses.json valid JSON** (deep-work acceptance): `python3 -c "import json,sys; json.load(open(sys.argv[1]))" cka-sim/tests/fixtures/phase-06/troubleshooting-06-broken-kubelet/stub-responses.json`
    - Sandbox path distinct from Phase 5 Q07: `grep -q '/tmp/q06-kubelet-flags' cka-sim/packs/troubleshooting/06-broken-kubelet/setup.sh && ! grep -q '/tmp/q07-kubelet-flags' cka-sim/packs/troubleshooting/06-broken-kubelet/setup.sh`
    - Setup uses string-split trick for deprecated-string: `grep -qE 'removed_flag=.*"--container-runtime""=remote"' cka-sim/packs/troubleshooting/06-broken-kubelet/setup.sh`
    - Setup NEVER touches /var/lib/kubelet/: `! grep -qE '/var/lib/kubelet/' cka-sim/packs/troubleshooting/06-broken-kubelet/setup.sh`
    - Setup NEVER touches /etc/kubernetes/: `! grep -qE '/etc/kubernetes/' cka-sim/packs/troubleshooting/06-broken-kubelet/setup.sh`
    - Setup NEVER calls systemctl: `! grep -qE '\bsystemctl\b' cka-sim/packs/troubleshooting/06-broken-kubelet/setup.sh`
    - Grade uses subshell source for malformed-quoting detection: `grep -qE '\( set \+u; source "\$flags"' cka-sim/packs/troubleshooting/06-broken-kubelet/grade.sh || grep -qE '\( source "\$flags"' cka-sim/packs/troubleshooting/06-broken-kubelet/grade.sh || grep -qE 'bash -n "\$flags"' cka-sim/packs/troubleshooting/06-broken-kubelet/grade.sh`
    - Grade records all 4 Q6 traps: `for t in removed-container-runtime-flag cri-endpoint-unix-prefix-missing kubelet-runtime-flag-in-kubeconfig kubelet-flag-file-malformed-quoting; do grep -q "record_trap $t" cka-sim/packs/troubleshooting/06-broken-kubelet/grade.sh || { echo "missing: $t"; exit 1; }; done`
    - Grade is read-only: `! grep -qE 'kubectl[[:space:]]+(delete|create|apply|patch|edit|replace)([[:space:]]|$)' cka-sim/packs/troubleshooting/06-broken-kubelet/grade.sh`
    - Grade NEVER calls systemctl or writes to forbidden paths: `! grep -qE '\bsystemctl\b|/var/lib/kubelet/|/etc/kubernetes/' cka-sim/packs/troubleshooting/06-broken-kubelet/grade.sh`
    - ref-solution writes canonical unix:// endpoint: `grep -q 'unix:///run/cri-dockerd.sock' cka-sim/packs/troubleshooting/06-broken-kubelet/ref-solution.sh && ! grep -qE 'container-runtime=remote' cka-sim/packs/troubleshooting/06-broken-kubelet/ref-solution.sh`
    - ref-solution writes only to sandbox: `grep -q '/tmp/q06-kubelet-flags' cka-sim/packs/troubleshooting/06-broken-kubelet/ref-solution.sh && ! grep -qE '>[[:space:]]*/(var|etc)/' cka-sim/packs/troubleshooting/06-broken-kubelet/ref-solution.sh`
    - Reset sentinel-guarded + no systemctl + no /var/lib/kubelet/: `grep -q '.cka-sim-sentinel' cka-sim/packs/troubleshooting/06-broken-kubelet/reset.sh && ! grep -qE '\bsystemctl\b|/var/lib/kubelet/' cka-sim/packs/troubleshooting/06-broken-kubelet/reset.sh`
    - Metadata declares 4 traps + cross-pack ref: `grep -qE '^    target: cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/' cka-sim/packs/troubleshooting/06-broken-kubelet/metadata.yaml`
    - **question.md D-04 symptom-only invariant (reviewer-enforced):** sandbox path present AND forbidden tokens absent from prose (excluding the literal sandbox path and file name). Use a two-step check — first strip the sandbox path and file-name literals, then grep the remaining prose:
      - `grep -q '/tmp/q06-kubelet-flags' cka-sim/packs/troubleshooting/06-broken-kubelet/question.md` (sandbox path referenced)
      - `if sed -e 's|/tmp/q06-kubelet-flags[^[:space:]`]*||g' -e 's|kubeadm-flags\.env||g' cka-sim/packs/troubleshooting/06-broken-kubelet/question.md | grep -qiE '\b(kubelet|kubeadm|systemctl)\b'; then echo "D-04 violation: forbidden bare token in question.md prose"; exit 1; fi`
      - Belt-and-braces: `! grep -qiE 'stray.quote|malformed.quot|deprecated.flag' cka-sim/packs/troubleshooting/06-broken-kubelet/question.md` (no root-cause naming)
    - Pass G forbidden-command guard passes: `bash cka-sim/scripts/lint-packs.sh >/dev/null`
    - Pass F no hardcoded worker names: `! grep -qE '\bnode-0[12]\b' cka-sim/packs/troubleshooting/06-broken-kubelet/*.sh`
    - lint-traps green: `bash cka-sim/scripts/lint-traps.sh >/dev/null`
    - lint-deprecated-strings green: `bash cka-sim/scripts/lint-deprecated-strings.sh >/dev/null`
    - Expected-score files single-line SCORE strings: `grep -cE '^SCORE: [0-9]+/[0-9]+$' cka-sim/tests/fixtures/phase-06/troubleshooting-06-broken-kubelet/expected-fail-score.txt | grep -qx 1 && grep -cE '^SCORE: [0-9]+/[0-9]+$' cka-sim/tests/fixtures/phase-06/troubleshooting-06-broken-kubelet/expected-pass-score.txt | grep -qx 1`
    - Full suite green: `bash cka-sim/scripts/test.sh 2>&1 | grep -qE 'all [0-9]+ case\(s\) passed'`
  </acceptance_criteria>
  <done>Q06 broken-kubelet question ships; sandbox-only; D-04 symptom-only invariant enforced in question.md; lints + test.sh green.</done>
</task>

</tasks>

<threat_model>
### T-6-19: Setup writes to /var/lib/kubelet/kubeadm-flags.env (bricks worker)
- **Threat:** Author copies Phase 5 Q07's `cp -p /var/lib/kubelet/kubeadm-flags.env "$sandbox/..."` line, or inverts it and writes back to `/var/lib/kubelet/`. kubelet refuses to start on next restart. Live worker is bricked; unreversible without host-level SSH access.
- **Impact:** critical — unreversible worker damage.
- **Asset:** live 1+2 worker kubelet.
- **Mitigation:** Pass G forbidden-command guard rejects `> /var/lib/kubelet/` writes. Plan action explicitly REMOVES the `cp` from `/var/lib/kubelet/` that exists in Phase 5 Q07 — Q06's setup is fully self-contained (generates broken content from scratch). Acceptance criterion greps for `/var/lib/kubelet/` absence in setup.sh.
- **Verification:** `bash cka-sim/scripts/lint-packs.sh` passes G. P09 VERIFICATION.md manual: `sha256sum /var/lib/kubelet/kubeadm-flags.env` identical before/after drill.

### T-6-20: systemctl invocation in grade/reset/ref-solution
- **Threat:** Author writes `systemctl restart kubelet` expecting to "apply the fix" — bricks the live worker's kubelet or triggers cascading restart of every static-pod (kube-apiserver, etcd).
- **Impact:** critical — cluster-wide outage.
- **Asset:** live cluster control plane.
- **Mitigation:** Pass G forbidden-command guard rejects `\bsystemctl\b` anywhere in `packs/troubleshooting/**/*.sh`. Acceptance criterion greps for `\bsystemctl\b` absence in all 4 Q06 scripts.
- **Verification:** `bash cka-sim/scripts/lint-packs.sh` passes G; grep miss across all scripts.

### T-6-21: Sandbox path collision with Phase 5 Q07
- **Threat:** Phase 5 Q07 uses `/tmp/q07-kubelet-flags/`. If Q06 reuses the same path, running both drills in the same session pollutes one with the other's state.
- **Impact:** medium — fixture flakiness + bug reports.
- **Asset:** drill idempotency.
- **Mitigation:** Q06 uses `/tmp/q06-kubelet-flags/` (distinct). Acceptance criterion asserts `/tmp/q06-kubelet-flags` presence AND `/tmp/q07-kubelet-flags` absence in setup.sh.
- **Verification:** `grep -q '/tmp/q06-' setup.sh && ! grep -q '/tmp/q07-' setup.sh`.

### T-6-22: Subshell source silently parses a broken env file on some bash versions
- **Threat:** Grader's `( source "$flags" )` subshell successfully parses a broken file under some locale/bash conditions, so `kubelet-flag-file-malformed-quoting` trap never fires.
- **Impact:** medium — trap detector silently misses the defect.
- **Asset:** Trap fidelity.
- **Mitigation:** Subshell uses `set +u` to prevent unbound-var crashes; explicit `>/dev/null 2>&1` discards stray output. Fallback: `bash -n "$flags"` (syntax check) reliably catches unmatched quotes. Author-time testing: run grader against setup-only output and observe the trap fires.
- **Verification:** Run grader against setup-only output locally; observe trap kubelet-flag-file-malformed-quoting in FAIL round-trip. If miss, switch to `bash -n "$flags"`.

### T-6-22b: D-04 grep check false-negative via sandbox-path token overlap
- **Threat:** The question.md D-04 check strips `/tmp/q06-kubelet-flags` and `kubeadm-flags.env` before scanning — but if an author writes `run kubeadm init` in the prose, the sed strip does not cover that, and the bare `kubeadm` token survives. The grep correctly flags it (desired). Conversely, if an author writes `kubelet-flags` as a word (not as part of the path), the strip removes nothing and the grep flags `kubelet` — also desired.
- **Impact:** low — the check is strict by design; any prose mention of bare `kubelet`/`kubeadm`/`systemctl` fails.
- **Asset:** D-04 compliance fidelity.
- **Mitigation:** The two-step sed-then-grep check in acceptance strips ONLY the two exact literal paths. Anything else containing those tokens is flagged.
- **Verification:** acceptance runs the exact sed+grep sequence.
</threat_model>

<verification>
- Round-trip (fixture): setup only → SCORE 1/3 + 4 traps (removed flag, unix prefix missing, runtime-in-kubeconfig, malformed-quoting); setup+ref-solution → SCORE 3/3 + zero traps
- Pass G + pass F + lint-deprecated-strings all green
- Sandbox /tmp/q06-kubelet-flags/ distinct from Phase 5 Q07
- Cross-pack ref `cka-sim/packs/cluster-architecture/07-cri-dockerd-endpoint/` exists
- question.md D-04 symptom-only invariant holds (no bare `kubelet`/`kubeadm`/`systemctl` tokens in prose; sandbox path and file name permitted)
</verification>

<success_criteria>
- Q06 covers the remaining kubelet-log + control-plane-file Tracker ergonomics without any live-host mutation
- The 4th new trap kubelet-flag-file-malformed-quoting is consumed here (P02's catalog extension justified)
- No /var/lib/kubelet/, no /etc/kubernetes/, no systemctl anywhere
- Candidate prompt is true symptom-only: names "node-agent runtime configuration file" symptoms, not the technology by name
</success_criteria>

<output>
Create `.planning/phases/06-troubleshooting-pack/06-08-SUMMARY.md` documenting:
- 4 encoded defects and the 4 traps they trigger
- Subshell-source technique for malformed-quoting detection (+ bash -n fallback per T-6-22)
- Sandbox path distinctness from Phase 5 Q07
- D-04 prose-language choice (node-agent / node-level runtime configuration)
- Fixture scores 1/3 → 3/3
</output>
