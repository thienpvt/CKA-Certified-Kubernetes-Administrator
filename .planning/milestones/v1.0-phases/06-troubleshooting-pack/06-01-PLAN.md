---
phase: 06-troubleshooting-pack
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cka-sim/scripts/lint-packs.sh
  - cka-sim/tests/fixtures/lint-packs/bad-forbidden-systemctl/setup.sh
  - cka-sim/tests/fixtures/lint-packs/bad-forbidden-systemctl/grade.sh
  - cka-sim/tests/fixtures/lint-packs/bad-forbidden-systemctl/reset.sh
  - cka-sim/tests/fixtures/lint-packs/bad-forbidden-systemctl/ref-solution.sh
  - cka-sim/tests/fixtures/lint-packs/bad-forbidden-systemctl/metadata.yaml
  - cka-sim/tests/fixtures/lint-packs/bad-forbidden-systemctl/question.md
  - cka-sim/tests/fixtures/lint-packs/bad-forbidden-coredns-edit/setup.sh
  - cka-sim/tests/fixtures/lint-packs/bad-forbidden-coredns-edit/grade.sh
  - cka-sim/tests/fixtures/lint-packs/bad-forbidden-coredns-edit/reset.sh
  - cka-sim/tests/fixtures/lint-packs/bad-forbidden-coredns-edit/ref-solution.sh
  - cka-sim/tests/fixtures/lint-packs/bad-forbidden-coredns-edit/metadata.yaml
  - cka-sim/tests/fixtures/lint-packs/bad-forbidden-coredns-edit/question.md
  - cka-sim/tests/fixtures/lint-packs/bad-forbidden-varlibkubelet-write/setup.sh
  - cka-sim/tests/fixtures/lint-packs/bad-forbidden-varlibkubelet-write/grade.sh
  - cka-sim/tests/fixtures/lint-packs/bad-forbidden-varlibkubelet-write/reset.sh
  - cka-sim/tests/fixtures/lint-packs/bad-forbidden-varlibkubelet-write/ref-solution.sh
  - cka-sim/tests/fixtures/lint-packs/bad-forbidden-varlibkubelet-write/metadata.yaml
  - cka-sim/tests/fixtures/lint-packs/bad-forbidden-varlibkubelet-write/question.md
  - cka-sim/tests/cases/lint_packs_forbidden_command.sh
autonomous: true
requirements: [PACK-05, PACK-06]
tags: [bash-lint, host-safety, phase-06]
validation:
  sampling_rate: per-task-commit
  quick_command: "bash -n cka-sim/scripts/lint-packs.sh && bash cka-sim/scripts/lint-packs.sh"
  full_command: "bash cka-sim/scripts/test.sh"
  validation_md_rows:
    - 06-01-XX
# scope_justification: This plan is the single cohesive lint foundation (pass G script + fixtures + test case) — splitting would create chicken-and-egg between the guard and its own tests. The 20+ files_modified count is intentional and breaks down as 1 lint extension + 3 negative fixture trees (6 files each) + 1 test case. No other plan consumes these fixtures; no earlier plan can produce them without pass G itself.
must_haves:
  truths:
    - "lint-packs.sh gains a new 'pass G: FORBIDDEN-COMMAND guard' scoped to cka-sim/packs/troubleshooting/**/*.sh"
    - "Pass G flags systemctl usage, live kube-system CoreDNS ConfigMap mutation, kubectl delete ns kube-system, worker cordon/drain, writes to /etc/kubernetes/ (including /etc/kubernetes/manifests/), and writes to /var/lib/kubelet/"
    - "Pass G ignores comments (first non-whitespace character is '#') — same idiom as passes A-F"
    - "Pass G is scoped to troubleshooting only; other packs are untouched (no false positive on existing cri-dockerd-endpoint/setup.sh which writes to a /tmp sandbox)"
    - "New test case lint_packs_forbidden_command.sh exercises each of the 3 seeded fixture families + verifies a clean-input case does not trip the guard"
    - "bash cka-sim/scripts/test.sh remains green"
    - "Scope justification (per scope_justification frontmatter comment): 20+ files_modified is intentional — single cohesive foundation; splitting would break the guard-vs-its-own-tests ordering."
    - "Decision coverage: D-09 (sandbox-only host impact), D-11 (no live kube-system CoreDNS mutation), D-12 (no systemctl / no system-service restart) — pass G enforces all three at lint time."
  artifacts:
    - path: "cka-sim/scripts/lint-packs.sh"
      provides: "New pass G between existing pass F (line 152-179) and the final summary block (line 181)"
      contains: "pass G: FORBIDDEN-COMMAND"
    - path: "cka-sim/tests/cases/lint_packs_forbidden_command.sh"
      provides: "3-fixture-family negative regression suite + 1 positive (clean-input)"
      contains: "FORBIDDEN-COMMAND"
  key_links:
    - from: "cka-sim/scripts/lint-packs.sh"
      to: "cka-sim/packs/troubleshooting/"
      via: "find $PACKS_DIR/troubleshooting -type f -name '*.sh'"
      pattern: "\\$PACKS_DIR/troubleshooting"
---

<objective>
Extend `cka-sim/scripts/lint-packs.sh` with a new `pass G: FORBIDDEN-COMMAND` guard scoped to `cka-sim/packs/troubleshooting/**/*.sh`. This pass fails the build if any troubleshooting script contains an operation that could mutate the live 1+2 cluster's host or kube-system — the host-safety contract locked by CONTEXT D-09, D-11, and D-12 and the landmines enumerated in RESEARCH §9.

Pass G is the enforcement mechanism behind P03-P08: later plans write scripts that, if they regress, MUST fail lint before merging. P09's VERIFICATION.md asserts pass G fires.

Scope is narrow by design: troubleshooting-only. Phase 5 Cluster-Architecture Q07 `cri-dockerd-endpoint/setup.sh` already writes into `/tmp/q07-kubelet-flags/` and must NOT be caught. Existing passes A-F keep their global scope.

Output: 1 lint extension + 3 negative fixture trees + 1 new test case.

Scope note (see scope_justification in frontmatter): this plan's 20+ files_modified are one cohesive unit. Splitting into "lint extension" vs "fixtures" vs "test case" plans would put pass G's test harness in a wave that runs before the guard itself — the test case cannot pass until both the guard AND all 3 fixture families land together. Single-plan is intentional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-troubleshooting-pack/06-CONTEXT.md
@.planning/phases/06-troubleshooting-pack/06-RESEARCH.md
@.planning/phases/06-troubleshooting-pack/06-PATTERNS.md
@.planning/phases/06-troubleshooting-pack/06-VALIDATION.md
@cka-sim/scripts/lint-packs.sh
@cka-sim/tests/cases/lint_packs_setup_guard.sh
@cka-sim/tests/cases/lint_packs_mutating_verb.sh
@cka-sim/tests/fixtures/lint-packs/bad-deletens/setup.sh
@cka-sim/tests/fixtures/lint-packs/good/setup.sh
</context>

<interfaces>
Existing `lint-packs.sh` structure (lines 40-187): passes A (grade-02 grep ban) → B (mutating-verb in grade.sh) → C (delete-ns in setup.sh) → D (6-files + exec bits) → E (metadata schema + trap registration) → F (BUG-3 node-0[12] literal). Every pass uses the comment-aware idiom `grep -nE '^[[:space:]]*[^#]*<pattern>'` with per-line iteration and `errors=$(( errors + 1 ))` accumulation. Final summary block (lines 181-187) stays the exit gate.

Pass G insertion point: after pass F closing `done < <(find ...)` on line 179 and BEFORE the `printf '\n' >&2` on line 181. Pass G MUST use the same `info "pass G: ..."`, same `grep -nE '^[[:space:]]*[^#]*<pattern>'` idiom, same `err "FORBIDDEN-COMMAND: $sh_file:$line_num ..."` emit, same `errors=$(( errors + 1 ))` accumulation. Use the `checked=$(( checked + 1 ))` counter update once per iterated script.

Scope filter: iterate only `find "$PACKS_DIR/troubleshooting" -type f -name '*.sh' -not -path '*/tests/*'`. Guard the find with `[[ -d "$PACKS_DIR/troubleshooting" ]]` so the lint does not fail when troubleshooting pack is absent during test-mode fixture runs.

Test case shape: mirror `cka-sim/tests/cases/lint_packs_setup_guard.sh` exactly. For each bad-forbidden-* fixture family, the test copies the fixture into a temp `$root/troubleshooting/01-test/` tree (not `$root/storage/01-test/` — pass G only fires under troubleshooting/), then runs `CKA_SIM_LINT_PACKS_DIR="$root" bash lint-packs.sh 2>&1`, asserts non-zero exit AND output contains `FORBIDDEN-COMMAND`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Append pass G to lint-packs.sh + seed 3 negative fixtures</name>
  <files>cka-sim/scripts/lint-packs.sh, cka-sim/tests/fixtures/lint-packs/bad-forbidden-systemctl/setup.sh, cka-sim/tests/fixtures/lint-packs/bad-forbidden-systemctl/grade.sh, cka-sim/tests/fixtures/lint-packs/bad-forbidden-systemctl/reset.sh, cka-sim/tests/fixtures/lint-packs/bad-forbidden-systemctl/ref-solution.sh, cka-sim/tests/fixtures/lint-packs/bad-forbidden-systemctl/metadata.yaml, cka-sim/tests/fixtures/lint-packs/bad-forbidden-systemctl/question.md, cka-sim/tests/fixtures/lint-packs/bad-forbidden-coredns-edit/setup.sh, cka-sim/tests/fixtures/lint-packs/bad-forbidden-coredns-edit/grade.sh, cka-sim/tests/fixtures/lint-packs/bad-forbidden-coredns-edit/reset.sh, cka-sim/tests/fixtures/lint-packs/bad-forbidden-coredns-edit/ref-solution.sh, cka-sim/tests/fixtures/lint-packs/bad-forbidden-coredns-edit/metadata.yaml, cka-sim/tests/fixtures/lint-packs/bad-forbidden-coredns-edit/question.md, cka-sim/tests/fixtures/lint-packs/bad-forbidden-varlibkubelet-write/setup.sh, cka-sim/tests/fixtures/lint-packs/bad-forbidden-varlibkubelet-write/grade.sh, cka-sim/tests/fixtures/lint-packs/bad-forbidden-varlibkubelet-write/reset.sh, cka-sim/tests/fixtures/lint-packs/bad-forbidden-varlibkubelet-write/ref-solution.sh, cka-sim/tests/fixtures/lint-packs/bad-forbidden-varlibkubelet-write/metadata.yaml, cka-sim/tests/fixtures/lint-packs/bad-forbidden-varlibkubelet-write/question.md</files>
  <read_first>
    - cka-sim/scripts/lint-packs.sh (existing 6 passes, comment-aware `grep -nE '^[[:space:]]*[^#]*<pattern>'` idiom, final summary at lines 181-187)
    - cka-sim/tests/fixtures/lint-packs/bad-deletens/setup.sh (the canonical 1-file bad fixture shape)
    - cka-sim/tests/fixtures/lint-packs/good/setup.sh + good/grade.sh + good/metadata.yaml (clean-input shapes used to round out temp pack dirs so passes A-F do not trip during fixture test runs)
    - .planning/phases/06-troubleshooting-pack/06-RESEARCH.md (§6 table — 7 forbidden patterns + rationale)
    - .planning/phases/06-troubleshooting-pack/06-CONTEXT.md (D-09 sandbox-only, D-11 no live coredns, D-12 no systemctl)
    - .planning/phases/06-troubleshooting-pack/06-VALIDATION.md (row 06-01-XX)
  </read_first>
  <action>
    APPEND pass G to `cka-sim/scripts/lint-packs.sh` between the closing `done < <(find ...)` of pass F (line 179) and the `printf '\n' >&2` line (line 181). Do NOT modify any other pass. Preserve the final summary block verbatim.

    Pass G block (per RESEARCH §6, CONTEXT D-09/D-11/D-12; comment-aware, same shape as pass F):

    1. `info "pass G: FORBIDDEN-COMMAND guard — troubleshooting pack host-safety (D-09/D-11/D-12)"` header line.
    2. Scope guard: `if [[ -d "$PACKS_DIR/troubleshooting" ]]; then ... fi` around the pass G body so test-mode fixture trees without a troubleshooting subdir skip the pass.
    3. Inside the scope guard, iterate `while IFS= read -r sh_file; do ... done < <(find "$PACKS_DIR/troubleshooting" -type f -name '*.sh' -not -path '*/tests/*')`.
    4. For each script, increment `checked=$(( checked + 1 ))` once at top of loop body.
    5. Define the 7 forbidden patterns (array of pattern/label pairs — each pattern is a POSIX extended regex). Labels are used verbatim in the err line. Patterns:
       - pattern: `\bsystemctl\b` — label: `systemctl`
       - pattern: `kubectl[[:space:]]+edit[[:space:]]+configmap[[:space:]]+coredns[[:space:]]+-n[[:space:]]+kube-system` — label: `kubectl edit cm coredns (kube-system)`
       - pattern: `kubectl[[:space:]]+delete[[:space:]]+(namespace|ns)[[:space:]]+kube-system` — label: `kubectl delete ns kube-system`
       - pattern: `kubectl[[:space:]]+(cordon|drain)[[:space:]]` — label: `kubectl cordon/drain`
       - pattern: `>[[:space:]]*/etc/kubernetes/` — label: `write into /etc/kubernetes/ (covers /etc/kubernetes/manifests/ via prefix)`
       - pattern: `>[[:space:]]*/var/lib/kubelet/` — label: `write into /var/lib/kubelet/`
       - pattern: `cp[[:space:]]+([^#][^[:space:]]*[[:space:]]+)+/etc/kubernetes/manifests/` — label: `copy into /etc/kubernetes/manifests/`
    6. For each pattern, use the comment-aware form: `grep -nE '^[[:space:]]*[^#]*<pattern>' "$sh_file"` piped into a `while read -r hit_line` loop. Inner per-line check re-strips leading whitespace from the matched line content (same `stripped="${rest#"${rest%%[![:space:]]*}"}"` idiom as pass F) and explicitly skips comment lines (`[[ "${stripped:0:1}" == "#" ]] && continue`). On a real hit: `err "FORBIDDEN-COMMAND: $sh_file:$line_num forbidden pattern '<label>' (D-09/D-11/D-12 host-safety)"` and `errors=$(( errors + 1 ))`.
    7. Do NOT use pass F's `# cka-sim-lint: allow-node-literal` sentinel — pass G has no legitimate opt-out in Phase 6 (every forbidden pattern names a live-cluster mutation; sandbox equivalents live under `/tmp/qNN-*`).

    Seed 3 negative fixture families under `cka-sim/tests/fixtures/lint-packs/`. Each family is a full 6-file question dir so that when the test case copies it into a `troubleshooting/01-test/` layout passes A-E do not trip (only pass G should fire). Each dir contains:

    - `setup.sh` — carries the forbidden pattern (see below). Other 5 files copy the `good/` fixture shapes (or minimal stubs). setup.sh minimum content: 3 lines — shebang `#!/bin/bash`, `set -euo pipefail`, then the trigger line (below).

    Family 1 — `bad-forbidden-systemctl/`:
      - `setup.sh` trigger line (line 3): `systemctl restart kubelet`
      - Expected match: pattern 1 (`\bsystemctl\b`).

    Family 2 — `bad-forbidden-coredns-edit/`:
      - `setup.sh` trigger line (line 3): `kubectl edit configmap coredns -n kube-system`
      - Expected match: pattern 2.

    Family 3 — `bad-forbidden-varlibkubelet-write/`:
      - `setup.sh` trigger line (line 3): `echo KUBELET_EXTRA_ARGS=--fake > /var/lib/kubelet/kubeadm-flags.env`
      - Expected match: pattern 6.

    For each family, the 5 supporting files (`grade.sh`, `reset.sh`, `ref-solution.sh`, `metadata.yaml`, `question.md`) must be clean — copy the shapes from `cka-sim/tests/fixtures/lint-packs/good/` where those files exist; if `good/` is missing a given file, create a minimal valid stub:
      - `grade.sh`: `#!/bin/bash` + `exit 0`
      - `reset.sh`: `#!/bin/bash` + `exit 0`
      - `ref-solution.sh`: `#!/bin/bash` + `exit 0`
      - `metadata.yaml`: 8 lines matching the schema (id: test-troubleshoot-fixture, domain: troubleshooting, estimatedMinutes: 5, verified_against: "1.35", traps: 3 valid IDs from existing catalog e.g. [default-sa-used, missing-dns-egress, service-selector-empty-endpoints], references: 1 k8s-doc entry).
      - `question.md`: empty or single-line placeholder.
    `chmod +x` on all 4 `*.sh` files per fixture (required by pass D).
  </action>
  <verify>
    <automated>bash -n cka-sim/scripts/lint-packs.sh && bash cka-sim/scripts/lint-packs.sh && chmod +x cka-sim/tests/fixtures/lint-packs/bad-forbidden-systemctl/*.sh cka-sim/tests/fixtures/lint-packs/bad-forbidden-coredns-edit/*.sh cka-sim/tests/fixtures/lint-packs/bad-forbidden-varlibkubelet-write/*.sh && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - lint-packs.sh syntax clean: `bash -n cka-sim/scripts/lint-packs.sh` exits 0
    - Pass G header present: `grep -qE '^info "pass G: FORBIDDEN-COMMAND' cka-sim/scripts/lint-packs.sh`
    - Pass G scoped to troubleshooting only: `grep -q '"\$PACKS_DIR/troubleshooting"' cka-sim/scripts/lint-packs.sh` (inside pass G body)
    - All 7 forbidden patterns present: `for p in systemctl "kubectl.*edit.*configmap.*coredns" "kubectl.*delete.*(namespace\|ns).*kube-system" "kubectl.*(cordon\|drain)" "/etc/kubernetes/" "/var/lib/kubelet/" "/etc/kubernetes/manifests/"; do grep -qE "$p" cka-sim/scripts/lint-packs.sh || { echo "missing: $p"; exit 1; }; done`
    - Comment-exclusion idiom reused in pass G: `grep -c '\[\[ "\${stripped:0:1}" == "#" \]\] && continue' cka-sim/scripts/lint-packs.sh` returns a count >=2 (pass F + pass G)
    - lint-packs.sh exits 0 on current tree (troubleshooting pack is still Phase 3 reference shape — no forbidden commands yet): `bash cka-sim/scripts/lint-packs.sh; test $? -eq 0`
    - 3 bad-forbidden-* fixture trees exist with all 6 files each: `for dir in bad-forbidden-systemctl bad-forbidden-coredns-edit bad-forbidden-varlibkubelet-write; do for f in setup.sh grade.sh reset.sh ref-solution.sh metadata.yaml question.md; do test -f "cka-sim/tests/fixtures/lint-packs/$dir/$f" || { echo "missing: $dir/$f"; exit 1; }; done; done`
    - Each bad-forbidden-* setup.sh carries the expected forbidden pattern: `grep -qE '\bsystemctl\b' cka-sim/tests/fixtures/lint-packs/bad-forbidden-systemctl/setup.sh && grep -q 'kubectl edit configmap coredns -n kube-system' cka-sim/tests/fixtures/lint-packs/bad-forbidden-coredns-edit/setup.sh && grep -q '/var/lib/kubelet/' cka-sim/tests/fixtures/lint-packs/bad-forbidden-varlibkubelet-write/setup.sh`
    - All fixture `*.sh` files executable: `for dir in bad-forbidden-systemctl bad-forbidden-coredns-edit bad-forbidden-varlibkubelet-write; do for f in setup.sh grade.sh reset.sh ref-solution.sh; do test -x "cka-sim/tests/fixtures/lint-packs/$dir/$f" || { echo "not +x: $dir/$f"; exit 1; }; done; done`
    - No regression to existing passes: `bash cka-sim/scripts/lint-packs.sh 2>&1 | grep -qE 'pack lint passed'`
  </acceptance_criteria>
  <done>Pass G and 3 bad-forbidden fixture families land; lint-packs still green on the current packs tree.</done>
</task>

<task type="auto">
  <name>Task 2: lint_packs_forbidden_command.sh test case</name>
  <files>cka-sim/tests/cases/lint_packs_forbidden_command.sh</files>
  <read_first>
    - cka-sim/tests/cases/lint_packs_setup_guard.sh (canonical pattern — fixture copy into `$root/storage/01-test/`, invoke lint-packs via `CKA_SIM_LINT_PACKS_DIR`, assert non-zero exit + signature string in output)
    - cka-sim/tests/cases/lint_packs_mutating_verb.sh (same shape; second exemplar)
    - cka-sim/tests/lib/assert.sh (ok/err/expect_* helpers the case sources)
    - Task 1 fixture paths: cka-sim/tests/fixtures/lint-packs/bad-forbidden-{systemctl,coredns-edit,varlibkubelet-write}/
  </read_first>
  <action>
    Create `cka-sim/tests/cases/lint_packs_forbidden_command.sh` using the `lint_packs_setup_guard.sh` shape exactly. Key delta vs existing cases: pack dir is `troubleshooting/01-test/` (not `storage/01-test/`) because pass G is scoped to troubleshooting only.

    Shape:
    1. Shebang `#!/bin/bash`, `set -uo pipefail` (NOT `-e` — accumulate per-family failures into a single `case_failed` integer).
    2. `: "${CKA_SIM_ROOT:?must be set by run.sh}"` + `: "${CKA_SIM_TEST_FIXTURES_DIR:?must be set by run.sh}"`.
    3. `source "$CKA_SIM_ROOT/tests/lib/assert.sh"`.
    4. `case_failed=0`; `fixtures="$CKA_SIM_TEST_FIXTURES_DIR/lint-packs"`.
    5. For EACH of the 3 bad-forbidden-* fixture families, run a sub-block:
       a. `root=$(mktemp -d -t lint-packs-forbidden-XXXXXX)`
       b. `mkdir -p "$root/troubleshooting/01-test"`
       c. Copy the fixture family's 6 files into `$root/troubleshooting/01-test/` (setup.sh, grade.sh, reset.sh, ref-solution.sh, metadata.yaml, question.md).
       d. `chmod +x "$root/troubleshooting/01-test"/*.sh`
       e. `out=$(CKA_SIM_LINT_PACKS_DIR="$root" bash "$CKA_SIM_ROOT/scripts/lint-packs.sh" 2>&1; printf '\nRC:%d' $?)`; `rc="${out##*RC:}"`
       f. Assert `rc != 0`; on zero, `err "<family> fixture: lint-packs exited 0 unexpectedly"; case_failed=1`.
       g. Assert `out` contains `FORBIDDEN-COMMAND`; on miss, `err "<family>: expected 'FORBIDDEN-COMMAND' in output"; case_failed=1`.
       h. `rm -rf "$root"`.
    6. Positive (clean-input) sub-block: fourth block copies the `good/` fixture family into `$root/troubleshooting/01-test/`. Assert `rc == 0`; assert `out` does NOT contain `FORBIDDEN-COMMAND`. This proves pass G does not false-positive on clean troubleshooting scripts.
    7. `exit "$case_failed"`.

    `chmod +x` the test case.
  </action>
  <verify>
    <automated>chmod +x cka-sim/tests/cases/lint_packs_forbidden_command.sh && bash -n cka-sim/tests/cases/lint_packs_forbidden_command.sh && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - Test case exists + executable + syntax clean: `[[ -x cka-sim/tests/cases/lint_packs_forbidden_command.sh ]] && bash -n cka-sim/tests/cases/lint_packs_forbidden_command.sh`
    - Git exec bit tracked: `git ls-files -s cka-sim/tests/cases/lint_packs_forbidden_command.sh | grep -q '^100755'`
    - Exercises all 3 families: `for f in bad-forbidden-systemctl bad-forbidden-coredns-edit bad-forbidden-varlibkubelet-write; do grep -q "$f" cka-sim/tests/cases/lint_packs_forbidden_command.sh || { echo "missing: $f"; exit 1; }; done`
    - Positive (clean-input) sub-block present: `grep -q 'fixtures/lint-packs/good' cka-sim/tests/cases/lint_packs_forbidden_command.sh`
    - Case dir in `troubleshooting/01-test/` not storage: `grep -q 'troubleshooting/01-test' cka-sim/tests/cases/lint_packs_forbidden_command.sh && ! grep -q 'storage/01-test' cka-sim/tests/cases/lint_packs_forbidden_command.sh`
    - Full suite green: `bash cka-sim/scripts/test.sh 2>&1 | tee /tmp/test-out; grep -qE 'all [0-9]+ case\(s\) passed' /tmp/test-out`
    - Case runs + passes by name: `bash cka-sim/scripts/test.sh 2>&1 | grep -qE 'case passed: lint_packs_forbidden_command'`
  </acceptance_criteria>
  <done>lint_packs_forbidden_command.sh case green; all 4 sub-blocks (3 negative + 1 positive) exit 0.</done>
</task>

</tasks>

<threat_model>
### T-6-01: Forbidden-command guard fails to fire (meta-threat for P01)
- **Threat:** A troubleshooting script introduces a live-host mutation (`systemctl`, kube-system CoreDNS edit, `/var/lib/kubelet/` write, etc.) but pass G's regex misses it or has a comment-exclusion bug, so the PR merges and the next drill bricks the cluster.
- **Impact:** high — any single false-negative here defeats the entire Phase 6 host-safety contract; CONTEXT D-09/D-11/D-12 depend on this pass.
- **Asset:** live 1+2 kubeadm cluster (worker kubelet, kube-system/coredns, /etc/kubernetes/).
- **Mitigation:** 3 negative fixture families exercise the highest-blast-radius patterns (`systemctl`, `kubectl edit cm coredns -n kube-system`, `/var/lib/kubelet/` write). Positive (clean-input) sub-block proves no false-positives on `good/` fixtures. Every per-task commit re-runs `bash cka-sim/scripts/test.sh`, which runs the suite in < 60s. P09 VERIFICATION.md re-asserts pass G fires on the final pack tree.
- **Verification:** `bash cka-sim/scripts/test.sh 2>&1 | grep -q 'case passed: lint_packs_forbidden_command'` AND (manual) after P03-P08 land, `bash cka-sim/scripts/lint-packs.sh` exits 0 on the real tree.

### T-6-02: Pass G false-positive on non-troubleshooting packs
- **Threat:** Pass G scope leaks outside `packs/troubleshooting/` and rejects Cluster-Arch Q07 `cri-dockerd-endpoint/setup.sh` which legitimately writes `/tmp/q07-kubelet-flags/` (NOT `/var/lib/kubelet/`), breaking Phase 5 output.
- **Impact:** medium — a broken lint blocks the entire repo; Phase 5 is shipped and must stay green.
- **Asset:** Phase 5 Cluster-Architecture pack.
- **Mitigation:** pass G iterator is `find "$PACKS_DIR/troubleshooting" -type f -name '*.sh'` — hard-scoped; test case uses `troubleshooting/01-test/`, never `cluster-architecture/*`. Positive (clean-input) sub-block confirms clean scripts do not trip the guard.
- **Verification:** `bash cka-sim/scripts/lint-packs.sh` on the current tree (before P03-P08 touch anything) exits 0 AND the existing 29+ test-case suite remains green.
</threat_model>

<verification>
- Every P02-P08 setup.sh/grade.sh/reset.sh/ref-solution.sh is subject to pass G as soon as it lands under `packs/troubleshooting/`
- Pre-existing Phase 5 pack scripts are unaffected (scope is `packs/troubleshooting/` only)
- `bash cka-sim/scripts/test.sh` exits 0 end-to-end after Task 2
</verification>

<success_criteria>
- P03-P08 implementers can detect a forbidden-pattern regression at commit time without needing to inspect lint-packs.sh source
- P09 VERIFICATION.md criterion 3 (`bash cka-sim/scripts/lint-packs.sh cka-sim/packs/troubleshooting` exits 0) is mechanically enforceable
- A future PR that accidentally adds `systemctl restart kubelet` in any troubleshooting script fails CI before human review
</success_criteria>

<output>
Create `.planning/phases/06-troubleshooting-pack/06-01-SUMMARY.md` documenting:
- Pass G placement (line numbers before/after insertion) + the 7 forbidden patterns exactly as inserted
- 3 negative fixture families + 1 positive (clean-input) sub-block design
- How P03-P08 authors verify their scripts pass G locally (`bash cka-sim/scripts/lint-packs.sh`)
- Confirmation that existing Phase 5 pack scripts remain green (scope is troubleshooting only)
</output>
