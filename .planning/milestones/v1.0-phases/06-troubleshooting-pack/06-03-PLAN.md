---
phase: 06-troubleshooting-pack
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/setup.sh
  - cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/grade.sh
  - cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/ref-solution.sh
  - cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/metadata.yaml
  - cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/question.md
  - cka-sim/tests/fixtures/phase-06/troubleshooting-01-deploy-svc-mismatch/stub-responses.json
  - cka-sim/tests/fixtures/phase-06/troubleshooting-01-deploy-svc-mismatch/expected-fail-score.txt
  - cka-sim/tests/fixtures/phase-06/troubleshooting-01-deploy-svc-mismatch/expected-pass-score.txt
autonomous: true
requirements: [PACK-05, PACK-06, PACK-07]
tags: [troubleshooting, retrofit, imagepullbackoff, phase-06]
validation:
  sampling_rate: per-task-commit
  quick_command: "bash cka-sim/scripts/lint-packs.sh && bash -n cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/*.sh"
  full_command: "bash cka-sim/scripts/test.sh"
  validation_md_rows:
    - 06-03-XX
must_haves:
  truths:
    - "setup.sh sources $CKA_SIM_ROOT/lib/setup.sh and calls ensure_lab_ns + wait_for_ns_active (Phase 5 retrofit shape — replaces Phase 3's inline ns loop)"
    - "setup.sh creates a second Deployment 'web-canary' with image tag that does NOT exist (ImagePullBackOff trap seeded)"
    - "setup.sh keeps the original Service selector 'app: webserver' trap (the endpoint-mismatch root cause)"
    - "grade.sh adds ImagePullBackOff detector that records the new trap imagepullbackoff-wrong-tag"
    - "grade.sh keeps the service-selector-empty-endpoints detector and assert_endpoints_nonempty oracle unchanged"
    - "ref-solution.sh patches the Service selector AND deletes the web-canary Deployment"
    - "metadata.yaml traps[] has 4 entries: service-selector-empty-endpoints, default-sa-used, missing-dns-egress, imagepullbackoff-wrong-tag"
    - "metadata.yaml references[] has exactly one D-05 cross-pack target beginning with 'cka-sim/packs/'"
    - "question.md gains one symptom-only sentence (D-04) about a secondary failure state without naming ImagePullBackOff"
    - "reset.sh unchanged — ns-scoped delete already covers the additional Deployment"
    - "lint-packs.sh exits 0 (pass G forbidden-command guard already in effect from P01) and lint-deprecated-strings.sh stays green"
    - "Decision coverage: D-01 (Q1 of 6-question pack), D-02 (Q1 is service/endpoints mismatch retrofit of Phase 3's 01-deploy-svc-mismatch), D-03 (Q1 is position 1 in the progressive ramp), D-06 (references[] metadata-only, not shown before grading), D-07 (reference targets use cka-sim/packs/... paths), D-08 (references are 'related prior-art', not prerequisites or remediation UI)."
  artifacts:
    - path: "cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/setup.sh"
      provides: "Retrofit setup using lib/setup.sh helpers + second Deployment with bad image tag"
      contains: "source \"$CKA_SIM_ROOT/lib/setup.sh\""
      contains_extra: "web-canary"
    - path: "cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/grade.sh"
      provides: "Endpoints oracle + ImagePullBackOff detector"
      contains: "record_trap imagepullbackoff-wrong-tag"
    - path: "cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/metadata.yaml"
      provides: "PACK-06 schema with 4 traps + cross-pack ref"
      contains: "imagepullbackoff-wrong-tag"
      contains_extra: "cka-sim/packs/services-networking/02-service-core/"
  key_links:
    - from: "cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/setup.sh"
      to: "cka-sim/lib/setup.sh"
      via: "source + ensure_lab_ns + wait_for_ns_active"
      pattern: "cka_sim::setup::wait_for_ns_active"
    - from: "cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/grade.sh"
      to: "cka-sim/traps/catalog.yaml (entry added in P02)"
      via: "record_trap call resolves catalog id"
      pattern: "imagepullbackoff-wrong-tag"
---

<objective>
Retrofit the existing Phase 3 reference question `cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/` to the Phase 5 shape and extend it with an ImagePullBackOff trap so it cleanly covers two Tracker checkboxes (Service endpoints + ImagePullBackOff diagnosis) per RESEARCH §2.

Retrofit plan per RESEARCH §4.7 ("Q1 retrofit delta"):
1. setup.sh: source `lib/setup.sh`; replace inline ns loop with helpers.
2. setup.sh: add a second Deployment `web-canary` pinned to `nginx:1.27-alpine-typoXYZ` (non-existent tag) so its pods loop in ImagePullBackOff.
3. grade.sh: add jsonpath detector for any container waiting.reason `ImagePullBackOff` or `ErrImagePull` → record `imagepullbackoff-wrong-tag`.
4. metadata.yaml: bump traps[] from 3 to 4; keep the prior-art-exercise ref; add cross-pack ref to `cka-sim/packs/services-networking/02-service-core/` (prior-art per D-05/D-07).
5. ref-solution.sh: also delete the `web-canary` Deployment.
6. question.md: append one symptom-only sentence noting a secondary failure state.
7. Fixtures: new Phase 6 fixture tree under `cka-sim/tests/fixtures/phase-06/troubleshooting-01-deploy-svc-mismatch/`.

Output: retrofit 5 files + add 3 fixtures. reset.sh unchanged. This plan is Wave 1 because Wave 2 questions (P04-P08) reuse the same retrofit shape and expect the new trap ID to be live in the catalog.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-troubleshooting-pack/06-CONTEXT.md
@.planning/phases/06-troubleshooting-pack/06-RESEARCH.md
@.planning/phases/06-troubleshooting-pack/06-PATTERNS.md
@.planning/phases/06-troubleshooting-pack/06-VALIDATION.md
@cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/setup.sh
@cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/grade.sh
@cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/reset.sh
@cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/ref-solution.sh
@cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/metadata.yaml
@cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/question.md
@cka-sim/lib/setup.sh
@cka-sim/packs/services-networking/01-networkpolicy-egress/setup.sh
@cka-sim/packs/services-networking/01-networkpolicy-egress/grade.sh
</context>

<interfaces>
Lab ns (existing, unchanged): `cka-sim-troubleshooting-01`. Question id (existing, unchanged): `troubleshooting-deploy-svc-mismatch`. Resource prefix for new objects: `web-canary` (a second Deployment alongside the existing `web`).

Library helpers available (from `cka-sim/lib/setup.sh`): `cka_sim::setup::ensure_lab_ns <ns> <pack> <qid>`, `cka_sim::setup::wait_for_ns_active <ns> <pack> <qid> [timeout=120]`. Pack identifier: `troubleshooting`. qid: `troubleshooting-deploy-svc-mismatch`.

Grader library helpers (from `cka-sim/lib/grade.sh` + `cka-sim/lib/traps.sh`): `cka_sim::grade::assert_resource_exists`, `cka_sim::grade::assert_endpoints_nonempty`, `cka_sim::grade::record_trap <id>`, `cka_sim::grade::emit_result`, `cka_sim::trap::detect_service_label_mismatch` (already used). Manual increment pattern for custom checks uses `CKA_SIM_GRADE_TOTAL`/`PASSED`/`FAILS` (see services-networking/01-networkpolicy-egress/grade.sh:25-33 for the DNS probe exemplar).

ImagePullBackOff detection path: `kubectl get pods -n "$CKA_SIM_LAB_NS" -l app=web-canary -o jsonpath='{.items[*].status.containerStatuses[*].state.waiting.reason}'` — if any token in the output equals `ImagePullBackOff` or `ErrImagePull`, record the trap. Do NOT use `grep -A` / `grep -B`; use jsonpath per GRADE-02.

Image tag (deterministic non-existent): `nginx:1.27-alpine-typoXYZ`. Do NOT use the canonical image typo pattern from CONCERNS.md (those reference real tags). `typoXYZ` suffix guarantees the pull fails.

New fixture path convention (per Phase 5 plan 05-04): `cka-sim/tests/fixtures/phase-06/troubleshooting-01-deploy-svc-mismatch/{stub-responses.json,expected-fail-score.txt,expected-pass-score.txt}`. Phase 4 uses flat `cka-sim/tests/fixtures/<slug>/`; Phase 5+ uses the `phase-NN/` prefix (see existing `cka-sim/tests/fixtures/phase-05/` tree). Continue the `phase-06/` prefix for every Phase 6 fixture.

Pass G forbidden-command guard (from P01) is live and will reject any `systemctl` / `kubectl edit cm coredns -n kube-system` / `/var/lib/kubelet/` write if accidentally introduced. None of those are needed for this question, so this plan is inherently pass-G-clean.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Retrofit setup.sh + grade.sh + ref-solution.sh (lib sourcing + web-canary Deployment + ImagePullBackOff detector)</name>
  <files>cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/setup.sh, cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/grade.sh, cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/ref-solution.sh</files>
  <read_first>
    - cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/setup.sh (current inline-ns-loop shape to replace)
    - cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/grade.sh (current assertion ordering to preserve)
    - cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/ref-solution.sh (current single-patch shape to extend)
    - cka-sim/lib/setup.sh (ensure_lab_ns + wait_for_ns_active contracts)
    - cka-sim/packs/services-networking/01-networkpolicy-egress/setup.sh:1-14 (canonical Phase 5 retrofit header — source lib + ensure + wait)
    - cka-sim/packs/services-networking/01-networkpolicy-egress/grade.sh:25-37 (manual increment + record_trap idiom for custom probes)
    - .planning/phases/06-troubleshooting-pack/06-RESEARCH.md (§4.7 Q1 retrofit delta)
    - .planning/phases/06-troubleshooting-pack/06-PATTERNS.md (§Excerpt B — grade.sh shape)
  </read_first>
  <action>
    Rewrite the 3 files to the retrofit shape. Keep the file shebangs and `set` modes unchanged (setup.sh `set -euo pipefail`; grade.sh `set -uo pipefail`; ref-solution.sh `set -euo pipefail`).

    **setup.sh** full rewrite (keep same file, overwrite contents):
    1. Shebang + comment: `# troubleshooting/01-deploy-svc-mismatch/setup.sh — Phase 6 retrofit: lib sourcing + web-canary ImagePullBackOff trap.`
    2. `set -euo pipefail`
    3. Env guards: `: "${CKA_SIM_LAB_NS:?CKA_SIM_LAB_NS must be set}"` and `: "${CKA_SIM_ROOT:?CKA_SIM_ROOT must be set}"`
    4. `# shellcheck source=../../../lib/setup.sh disable=SC1091` then `source "$CKA_SIM_ROOT/lib/setup.sh"`
    5. Constants: `CKA_SIM_PACK="troubleshooting"` and `CKA_SIM_QUESTION_ID="troubleshooting-deploy-svc-mismatch"`
    6. Call `cka_sim::setup::ensure_lab_ns "$CKA_SIM_LAB_NS" "$CKA_SIM_PACK" "$CKA_SIM_QUESTION_ID"` then `cka_sim::setup::wait_for_ns_active "$CKA_SIM_LAB_NS" "$CKA_SIM_PACK" "$CKA_SIM_QUESTION_ID" 120`
    7. Apply Deployment `web` with pod label `app: web`, replicas: 2, image `nginx:1.27-alpine`, readinessProbe httpGet:/ port 80 initialDelaySeconds:2 periodSeconds:3 (preserve Phase 3 content verbatim — reuse the existing heredoc).
    8. Apply Service `web-svc` with selector `app: webserver` (INTENTIONAL — keep the Phase 3 trap).
    9. Apply Deployment `web-canary` with pod label `app: web-canary`, replicas: 1, image `nginx:1.27-alpine-typoXYZ` (the intentional bad tag). No readinessProbe (pod never gets past pull). Add a comment `# INTENTIONAL TRAP: image tag does not exist -> ImagePullBackOff`.
    10. `kubectl wait --for=condition=Available deployment/web -n "$CKA_SIM_LAB_NS" --timeout=60s 2>/dev/null || true` (best-effort; web only — the canary will never become Available).

    **grade.sh** full rewrite:
    1. Keep shebang + `# troubleshooting/01-deploy-svc-mismatch/grade.sh` + `set -uo pipefail`.
    2. Env guards (unchanged).
    3. `source "$CKA_SIM_ROOT/lib/grade.sh"` + `source "$CKA_SIM_ROOT/lib/traps.sh"` (unchanged).
    4. `kubectl wait --for=condition=Available deployment/web -n "$CKA_SIM_LAB_NS" --timeout=30s 2>/dev/null || true` (unchanged).
    5. Preserve existing assertions: assert_resource_exists deployment web, assert_resource_exists service web-svc, assert_endpoints_nonempty $ns web-svc. Preserve the `cka_sim::trap::detect_service_label_mismatch "$CKA_SIM_LAB_NS" "web-svc"` detector call.
    6. NEW block — ImagePullBackOff detector for the web-canary deployment. Manual-increment custom check (not a generic helper). Use jsonpath:
       `canary_reasons=$(kubectl get pods -n "$CKA_SIM_LAB_NS" -l app=web-canary -o jsonpath='{.items[*].status.containerStatuses[*].state.waiting.reason}' 2>/dev/null || echo "")`
       Then: if `canary_reasons` contains `ImagePullBackOff` OR `ErrImagePull`, call `cka_sim::grade::record_trap imagepullbackoff-wrong-tag`. Do NOT count this trap in PASSED/TOTAL — trap recording is a side-effect, not an assertion. (Compare with services-networking/01-networkpolicy-egress/grade.sh:36-37 trap-only pattern.)
    7. `cka_sim::grade::emit_result` as the final line (unchanged).

    **ref-solution.sh** full rewrite:
    1. Shebang + comment block (keep existing explanation lines).
    2. `set -euo pipefail` + env guards (unchanged).
    3. `kubectl patch service web-svc -n "$CKA_SIM_LAB_NS" --type='merge' -p '{"spec":{"selector":{"app":"web"}}}'` (unchanged).
    4. NEW line: `kubectl delete deployment web-canary -n "$CKA_SIM_LAB_NS" --ignore-not-found --wait=false` — removes the canary so its ImagePullBackOff trap no longer fires under the pass round-trip.

    Preserve file exec bits. If git loses the exec bit on Windows, re-apply: `git update-index --chmod=+x cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/{setup,grade,ref-solution}.sh`.
  </action>
  <verify>
    <automated>chmod +x cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/*.sh && for f in setup grade reset ref-solution; do bash -n "cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/$f.sh" || exit 1; done && bash cka-sim/scripts/lint-packs.sh && bash cka-sim/scripts/lint-deprecated-strings.sh</automated>
  </verify>
  <acceptance_criteria>
    - setup.sh sources lib/setup.sh: `grep -q 'source "$CKA_SIM_ROOT/lib/setup.sh"' cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/setup.sh`
    - setup.sh calls both helpers: `grep -q 'cka_sim::setup::ensure_lab_ns' cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/setup.sh && grep -q 'cka_sim::setup::wait_for_ns_active' cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/setup.sh`
    - setup.sh no longer has the inline 24-iteration ns loop: `! grep -qE 'for i in \$\(seq 1 24\)' cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/setup.sh`
    - setup.sh seeds web-canary with the bad tag: `grep -q 'web-canary' cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/setup.sh && grep -q 'nginx:1.27-alpine-typoXYZ' cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/setup.sh`
    - setup.sh keeps the Service selector trap: `grep -q 'app: webserver' cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/setup.sh`
    - grade.sh records the ImagePullBackOff trap: `grep -q 'record_trap imagepullbackoff-wrong-tag' cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/grade.sh`
    - grade.sh preserves existing oracle: `grep -q 'assert_endpoints_nonempty' cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/grade.sh && grep -q 'detect_service_label_mismatch' cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/grade.sh`
    - grade.sh uses jsonpath (GRADE-02): `grep -q 'jsonpath' cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/grade.sh && ! grep -qE 'kubectl[[:space:]]+get[[:space:]].*\|[[:space:]]*grep' cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/grade.sh`
    - ref-solution.sh deletes the canary: `grep -q 'kubectl delete deployment web-canary' cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/ref-solution.sh`
    - All 3 .sh files syntax-valid + executable: `for f in setup grade ref-solution; do bash -n "cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/$f.sh" && test -x "cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/$f.sh" || exit 1; done`
    - Pass G forbidden-command guard passes: `bash cka-sim/scripts/lint-packs.sh >/dev/null`
    - No deprecated strings: `bash cka-sim/scripts/lint-deprecated-strings.sh >/dev/null`
  </acceptance_criteria>
  <done>Retrofit setup/grade/ref-solution land; all lints green.</done>
</task>

<task type="auto">
  <name>Task 2: Update metadata.yaml + question.md + seed Phase 6 fixture tree</name>
  <files>cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/metadata.yaml, cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/question.md, cka-sim/tests/fixtures/phase-06/troubleshooting-01-deploy-svc-mismatch/stub-responses.json, cka-sim/tests/fixtures/phase-06/troubleshooting-01-deploy-svc-mismatch/expected-fail-score.txt, cka-sim/tests/fixtures/phase-06/troubleshooting-01-deploy-svc-mismatch/expected-pass-score.txt</files>
  <read_first>
    - cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/metadata.yaml (existing 3-trap + 2-ref shape to extend)
    - cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/question.md (existing Phase 3 prose to append to — D-04 symptom-only rule)
    - cka-sim/packs/services-networking/06-netpol-endport/metadata.yaml (PATTERNS §D exemplar for mixed-kind references list)
    - cka-sim/tests/fixtures/storage-06-pvc-mount-pod/ (canonical 3-file fixture shape per PATTERNS §G)
    - cka-sim/tests/fixtures/phase-05/services-networking/03-coredns-resolution/ (Phase 5 fixture directory layout under phase-NN/ prefix)
    - cka-sim/traps/catalog.yaml (verify imagepullbackoff-wrong-tag is registered from P02)
  </read_first>
  <action>
    **metadata.yaml** (full rewrite, keep file):
    ```
    id: troubleshooting-deploy-svc-mismatch
    domain: troubleshooting
    estimatedMinutes: 7
    verified_against: "1.35"
    traps:
      - service-selector-empty-endpoints
      - default-sa-used
      - missing-dns-egress
      - imagepullbackoff-wrong-tag
    references:
      - kind: prior-art-exercise
        target: cka-sim/packs/services-networking/02-service-core/
        note: "Related prior-art: Service + Endpoints selector matching"
      - kind: prior-art-exercise
        target: exercises/11-troubleshoot-cluster/
        note: Prior troubleshoot exercise (pre-simulator)
      - kind: k8s-doc
        target: https://kubernetes.io/docs/tasks/debug/debug-application/debug-service/
        note: Debug Services (selector/endpoints)
    ```
    4 traps satisfy GRADE-04 (≥3). First references[] entry is the D-05 cross-pack target whose `target` begins with `cka-sim/packs/` — lint-packs pass E enforces the enum `prior-art-exercise` for kind. The old `exercises/11-troubleshoot-cluster/` ref stays as secondary prior-art.

    **question.md** — APPEND (do not rewrite) one new sentence to the "Tasks" section. Do NOT touch the title, the "Constraints" section, or the "Verify yourself" block. Insert as a new bullet (Task 4) AFTER the existing three bullets:

    ```
    4. One replica of a sibling workload is also failing with a different symptom; identify and resolve that failure as part of the fix.
    ```

    D-04 rule: the sentence names the symptom ("one replica of a sibling workload is also failing") but not the root cause ("ImagePullBackOff", "bad image tag", or "wrong tag"). Do NOT add any reference to the catalog entry name.

    Update the Verify step `kubectl get endpoints web-svc -n ${CKA_SIM_LAB_NS}` — leave it unchanged. No new verify step for the canary (the candidate discovers it by inspecting `kubectl get pods -n ns`).

    **Fixture tree** at `cka-sim/tests/fixtures/phase-06/troubleshooting-01-deploy-svc-mismatch/`:

    1. `stub-responses.json` — a single JSON object the PATH-shadowed `kubectl` stub can replay. Shape: the Deployment + Service + Pod objects the grader queries. Minimum content so the FAIL path records ≥1 trap and the PASS path passes all assertions:
       - `kind: Deployment` for `web` (Available=true, ready replicas 2)
       - `kind: Deployment` for `web-canary` (replicas 1, ready 0)
       - `kind: Service` for `web-svc`
       - For FAIL: Endpoints subset of `web-svc` is empty + pod list for `app=web-canary` carries `state.waiting.reason: ImagePullBackOff`
       - For PASS: Endpoints subset is non-empty + no `web-canary` pods present (ref-solution deleted the canary)
       The single file is consumed for both round-trips via the kubectl stub's lookup convention (see `cka-sim/tests/fixtures/storage-06-pvc-mount-pod/stub-responses.json:1-32` as the reference shape). If the harness requires TWO separate stub files for fail vs pass (check by running `bash cka-sim/scripts/test.sh` locally during author time), split as `stub-responses-fail.json` and `stub-responses-pass.json` and reflect filenames in both the `<files>` list and the expected-*.txt contents — do NOT change the stub runner. (Current Phase 4 fixtures use a single JSON; this plan follows that convention.)

    2. `expected-fail-score.txt` — single line matching the grader's `emit_result` output on the pre-fix state. Given the 3 assertions (resource_exists deploy, resource_exists svc, endpoints_nonempty) + 0 trap-credit: `SCORE: 2/3`. (TOTAL=3, PASSED=2 for exist checks, FAIL for endpoints.)

    3. `expected-pass-score.txt` — single line matching the post-fix state: `SCORE: 3/3`.

    Verify the fixture values against `cka_sim::grade::emit_result`'s actual output format. If `emit_result` prints with different whitespace or order, align the expected-*.txt files to match exactly. (The existing storage-06 fixture uses `SCORE: 4/4` / `SCORE: 0/4`; follow the same format.)

    No chmod needed on `.json`/`.txt` fixture files. Metadata and question.md do NOT need exec bits either.
  </action>
  <verify>
    <automated>bash cka-sim/scripts/lint-packs.sh && bash cka-sim/scripts/lint-traps.sh && bash cka-sim/scripts/lint-coverage.sh troubleshooting 2>&1 | tee /tmp/cov-out; bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - metadata.yaml has 4 traps: `awk '/^traps:/{in=1;next} in && /^[a-z]/ {in=0} in && /^  - /{n++} END{print n}' cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/metadata.yaml | grep -qx 4`
    - All 4 trap IDs present: `for id in service-selector-empty-endpoints default-sa-used missing-dns-egress imagepullbackoff-wrong-tag; do grep -qE "^  - $id$" cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/metadata.yaml || exit 1; done`
    - D-05 cross-pack reference present: `grep -qE '^    target: cka-sim/packs/services-networking/02-service-core/' cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/metadata.yaml`
    - question.md D-04 compliance: sentence added but no root-cause language. Assert NEW task line present: `grep -q 'sibling workload is also failing' cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/question.md`. Assert forbidden root-cause tokens absent from the new sentence: `! grep -qiE 'ImagePullBackOff|bad.tag|image.tag|wrong.tag|typo' cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/question.md`
    - Fixture tree exists: `test -f cka-sim/tests/fixtures/phase-06/troubleshooting-01-deploy-svc-mismatch/stub-responses.json && test -f cka-sim/tests/fixtures/phase-06/troubleshooting-01-deploy-svc-mismatch/expected-fail-score.txt && test -f cka-sim/tests/fixtures/phase-06/troubleshooting-01-deploy-svc-mismatch/expected-pass-score.txt`
    - Expected-score files are single-line SCORE strings: `grep -cE '^SCORE: [0-9]+/[0-9]+$' cka-sim/tests/fixtures/phase-06/troubleshooting-01-deploy-svc-mismatch/expected-fail-score.txt | grep -qx 1 && grep -cE '^SCORE: [0-9]+/[0-9]+$' cka-sim/tests/fixtures/phase-06/troubleshooting-01-deploy-svc-mismatch/expected-pass-score.txt | grep -qx 1`
    - stub-responses.json is valid JSON: `python3 -c 'import json,sys; json.load(open(sys.argv[1]))' cka-sim/tests/fixtures/phase-06/troubleshooting-01-deploy-svc-mismatch/stub-responses.json`
    - lint-packs pass E resolves all 4 traps against catalog: `bash cka-sim/scripts/lint-packs.sh >/dev/null`
    - lint-coverage troubleshooting still green (this plan does not touch coverage.yaml — that comes in P09; but the existing manifest.yaml entry is still valid): `bash cka-sim/scripts/lint-coverage.sh troubleshooting 2>&1 | grep -qvE 'error'` (a `no coverage.yaml yet — skipping` warning is acceptable pre-P09)
    - Full suite still green: `bash cka-sim/scripts/test.sh 2>&1 | grep -qE 'all [0-9]+ case\(s\) passed'`
    - reset.sh unchanged (sanity — no edits this task): `git diff --name-only cka-sim/packs/troubleshooting/01-deploy-svc-mismatch/reset.sh 2>/dev/null | wc -l | grep -qx 0` (skip this check if not run under git)
  </acceptance_criteria>
  <done>Metadata + prompt + fixtures land; all lints + test.sh green.</done>
</task>

</tasks>

<threat_model>
### T-6-05: Retrofit breaks existing round-trip behaviour
- **Threat:** The retrofit changes setup.sh in a way that breaks the endpoint-mismatch fail → pass cycle (e.g., ref-solution patches the wrong field, setup's second deployment competes for ports).
- **Impact:** medium — would regress Phase 3's verified green state.
- **Asset:** Phase 3 live verification (completed 2026-05-10).
- **Mitigation:** 6-file shape preserved (reset.sh untouched). Port contention avoided by selector labels (`app: web` vs `app: web-canary`). Manual increment trap recording does not change PASSED/TOTAL counts for the existing oracle. Fixture FAIL/PASS scores written to match the grader's actual emit_result.
- **Verification:** `bash cka-sim/scripts/test.sh` green including the Phase 6 fixture round-trip (when the fixture tree is exercised by the stub harness in P09 VERIFICATION).

### T-6-06: ImagePullBackOff trap false-negative under race
- **Threat:** The web-canary pod has not yet entered ImagePullBackOff when grade.sh runs (initial pull attempt still in progress), so `state.waiting.reason` is empty and the trap is not recorded.
- **Impact:** low — trap records are informational; the core oracle (endpoints_nonempty) still flips fail/pass correctly.
- **Asset:** Grader trap-emission fidelity.
- **Mitigation:** grade.sh already waits 30s for `web` deployment availability before asserting; the 30s wall gives kubelet ample time to fail the canary's image pull (typical ImagePullBackOff latency is 2-5s). If CI fixtures show flakiness, the fix is to extend the wait OR use a longer backoff threshold, not to rewire the fixture.
- **Verification:** Phase 6 fixture stub-responses.json encodes `ImagePullBackOff` directly (no timing dependency in CI). Live 1+2 verification covers real-cluster timing (P09 VERIFICATION.md manual checklist).
</threat_model>

<verification>
- Round-trip self-check: `setup.sh` alone (no ref-solution) → grade records `service-selector-empty-endpoints` AND `imagepullbackoff-wrong-tag`, endpoints check fails
- Round-trip self-check: `setup.sh && ref-solution.sh` → grade records zero traps, endpoints check passes
- 4 traps declared in metadata.yaml; all resolve against catalog via lint-packs pass E
- Cross-pack reference `cka-sim/packs/services-networking/02-service-core/` exists (created in Phase 5 Plan 03); lint-packs pass E enforces `kind` enum
- question.md is D-04 compliant (symptoms only, no root-cause naming in the new Task 4 line)
</verification>

<success_criteria>
- Q01 now covers two Tracker checkboxes (Service endpoints + ImagePullBackOff) per RESEARCH §2
- P04-P08 use the same retrofit shape (lib/setup.sh sourcing, manual-increment trap detectors) with confidence
- All lints + test.sh green
</success_criteria>

<output>
Create `.planning/phases/06-troubleshooting-pack/06-03-SUMMARY.md` documenting:
- Retrofit delta (lib sourcing, web-canary deployment, trap detector, metadata extension)
- Fixture tree under `cka-sim/tests/fixtures/phase-06/troubleshooting-01-deploy-svc-mismatch/` (single stub + FAIL 2/3 + PASS 3/3)
- Two Tracker checkboxes now covered by Q01
- D-04 compliance note on the appended question.md sentence
</output>
