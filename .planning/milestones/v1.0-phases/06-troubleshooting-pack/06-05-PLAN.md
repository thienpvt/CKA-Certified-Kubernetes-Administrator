---
phase: 06-troubleshooting-pack
plan: 05
type: execute
wave: 2
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - cka-sim/packs/troubleshooting/03-coredns-resolution/setup.sh
  - cka-sim/packs/troubleshooting/03-coredns-resolution/grade.sh
  - cka-sim/packs/troubleshooting/03-coredns-resolution/reset.sh
  - cka-sim/packs/troubleshooting/03-coredns-resolution/ref-solution.sh
  - cka-sim/packs/troubleshooting/03-coredns-resolution/metadata.yaml
  - cka-sim/packs/troubleshooting/03-coredns-resolution/question.md
  - cka-sim/tests/fixtures/phase-06/troubleshooting-03-coredns-resolution/stub-responses.json
  - cka-sim/tests/fixtures/phase-06/troubleshooting-03-coredns-resolution/expected-fail-score.txt
  - cka-sim/tests/fixtures/phase-06/troubleshooting-03-coredns-resolution/expected-pass-score.txt
autonomous: true
requirements: [PACK-05, PACK-06, PACK-07]
tags: [troubleshooting, coredns, dns, phase-06]
validation:
  sampling_rate: per-task-commit
  quick_command: "bash cka-sim/scripts/lint-packs.sh && bash -n cka-sim/packs/troubleshooting/03-coredns-resolution/*.sh"
  full_command: "bash cka-sim/scripts/test.sh"
  validation_md_rows:
    - 06-05-XX
must_haves:
  truths:
    - "setup seeds a lab-namespace-scoped CoreDNS Deployment + ConfigMap (key 'Corefile' — capital C per v1.35 currency) + Service, distinct from kube-system/coredns"
    - "Corefile forward clause is seeded to a deliberately unreachable upstream '203.0.113.53:53' (TEST-NET-3, RFC 5737) to trigger coredns-forward-to-invalid-upstream"
    - "setup ALSO drifts the Deployment's volume mount to reference ConfigMap key 'corefile' (lowercase — wrong) while the ConfigMap key is 'Corefile' — triggers coredns-sandbox-configmap-mount trap"
    - "setup NEVER edits kube-system/coredns ConfigMap or Deployment (D-11 host-safety; pass G forbidden-command guard would reject it)"
    - "setup seeds a probe Pod (busybox:1.37) with dnsPolicy None and dnsConfig.nameservers pointing at the lab CoreDNS Service ClusterIP"
    - "grade probes nslookup via kubectl exec; exit 0 + Address-bearing stdout oracle"
    - "grade records coredns-forward-to-invalid-upstream and coredns-sandbox-configmap-mount on the pre-fix state"
    - "ref-solution re-applies the ConfigMap with 'forward . /etc/resolv.conf' AND re-applies the Deployment with the correct 'Corefile' volume mount key"
    - "metadata.yaml references[] contains cka-sim/packs/services-networking/03-coredns-resolution/ per D-05"
    - "question.md symptoms-only (D-04): 'Pods cannot resolve external names' + 'cluster-internal names also fail' — does not mention CoreDNS, Corefile, or ConfigMap"
    - "Decision coverage: D-01 (Q3 of 6), D-03 (Q3 is position 3 — CoreDNS — in progressive ramp), D-06 / D-07 / D-08 (references[] metadata-only, pack-path, prior-art only)."
  artifacts:
    - path: "cka-sim/packs/troubleshooting/03-coredns-resolution/setup.sh"
      provides: "Lab-scoped CoreDNS Deployment + ConfigMap(key:Corefile) + Service + probe Pod (dnsConfig.nameservers -> lab CoreDNS)"
      contains: "coredns/coredns:"
      contains_extra: "203.0.113.53:53"
    - path: "cka-sim/packs/troubleshooting/03-coredns-resolution/grade.sh"
      provides: "nslookup probe + 2 CoreDNS trap detectors"
      contains: "record_trap coredns-forward-to-invalid-upstream"
    - path: "cka-sim/packs/troubleshooting/03-coredns-resolution/metadata.yaml"
      provides: "PACK-06 schema + cross-pack ref"
      contains: "cka-sim/packs/services-networking/03-coredns-resolution/"
  key_links:
    - from: "cka-sim/packs/troubleshooting/03-coredns-resolution/setup.sh"
      to: "cka-sim/lib/setup.sh"
      via: "source + ensure_lab_ns + wait_for_ns_active"
      pattern: "cka_sim::setup::wait_for_ns_active"
    - from: "cka-sim/packs/troubleshooting/03-coredns-resolution/grade.sh"
      to: "cka-sim/traps/catalog.yaml (entry added in P02: coredns-sandbox-configmap-mount)"
      via: "record_trap resolves catalog id via pass E"
      pattern: "coredns-sandbox-configmap-mount"
---

<objective>
Author Q03 `03-coredns-resolution` — a CoreDNS troubleshooting question with two defects per RESEARCH §4 "Q3 CoreDNS — per-question lab CoreDNS vs per-pod dnsConfig":
1. Corefile `forward` clause points at an unreachable upstream IP (203.0.113.53 — TEST-NET-3, never routable).
2. The CoreDNS Deployment's volume mount references ConfigMap key `corefile` (lowercase) while the ConfigMap ships key `Corefile` (capital C). Result: the ConfigMap mount is empty → CoreDNS starts with no Corefile → cluster-internal names also fail.

Critical host-safety contract (D-11): the question NEVER mutates `kube-system/coredns`. Everything lives in the lab ns — a separate CoreDNS Deployment + ConfigMap + Service mirror the kube-system shape. The probe Pod uses `dnsPolicy: None` + `dnsConfig.nameservers: [<lab CoreDNS Service ClusterIP>]`.

Output: 6 question files + 3 fixtures. 8 minutes per RESEARCH §1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-troubleshooting-pack/06-CONTEXT.md
@.planning/phases/06-troubleshooting-pack/06-RESEARCH.md
@.planning/phases/06-troubleshooting-pack/06-PATTERNS.md
@.planning/phases/06-troubleshooting-pack/06-VALIDATION.md
@cka-sim/packs/services-networking/03-coredns-resolution/setup.sh
@cka-sim/packs/services-networking/03-coredns-resolution/grade.sh
@cka-sim/packs/services-networking/03-coredns-resolution/ref-solution.sh
@cka-sim/packs/services-networking/03-coredns-resolution/metadata.yaml
@cka-sim/packs/cluster-architecture/05-audit-policy/setup.sh
@cka-sim/lib/setup.sh
@cka-sim/lib/grade.sh
@cka-sim/lib/traps.sh
</context>

<interfaces>
Lab ns: `cka-sim-troubleshooting-03`. Question id: `troubleshooting-coredns-resolution`. Pack: `troubleshooting`.

Lab-ns object set (all RFC-1123):
- ConfigMap `q03-coredns-corefile` — data key `Corefile` (capital C per v1.35 currency §11). Corefile content: zone `.:53` block containing `errors`, `health`, `ready`, `forward . 203.0.113.53:53`, `cache 30`, `loop`, `reload`, `loadbalance` — NO `kubernetes` plugin (T-6-11 rationale). 203.0.113.53:53 is TEST-NET-3 (RFC 5737) — guaranteed unroutable; seeds `coredns-forward-to-invalid-upstream`.
- Deployment `q03-coredns` — image `coredns/coredns:1.11.1`, args `["-conf", "/etc/coredns/Corefile"]`, replicas 1, pod label `app.kubernetes.io/name: q03-coredns`. Volume `config` references configMap `q03-coredns-corefile` with items mapping key `Corefile` → path `Corefile`. volumeMounts[0]: `name: config`, `mountPath: /etc/coredns/Corefile`, `subPath: corefile` (INTENTIONAL lowercase — trap; case-sensitive mismatch with ConfigMap key `Corefile`). Container ports 53/UDP + 53/TCP.
- Service `q03-coredns` — ClusterIP, selector `app.kubernetes.io/name: q03-coredns`, ports UDP/53 + TCP/53 → targetPort 53.
- Pod `q03-dnsclient` — image `busybox:1.37`, `dnsPolicy: None`, `dnsConfig.nameservers: [<lab-coredns-service-clusterip>]` (substituted at apply time). command `["sleep", "3600"]`.

Service ClusterIP is allocated dynamically, so setup.sh applies the Pod AFTER the Service exists: discover via `kubectl get svc q03-coredns -n "$ns" -o jsonpath='{.spec.clusterIP}'` with poll-until-non-empty (max 30s), then substitute the IP into the Pod heredoc.

RBAC: lab CoreDNS Corefile OMITS the `kubernetes` plugin (T-6-11). Only `forward` + cache + health + ready + loop + reload + loadbalance. No ServiceAccount with `system:coredns` needed; default SA is fine; no ClusterRoleBinding creation; no cross-namespace permissions. D-11 safety envelope preserved.

Grader oracle:
- Probe: `kubectl exec -n "$ns" q03-dnsclient -- nslookup kubernetes.default.svc.cluster.local` → rc 0 AND stdout contains `Address` = pass.
- Also probe: `kubectl exec -n "$ns" q03-dnsclient -- nslookup www.example.com` (exercises the forward-upstream fix).
- Detector `coredns-forward-to-invalid-upstream`: read Corefile via `kubectl get configmap q03-coredns-corefile -n "$ns" -o jsonpath='{.data.Corefile}'`. If it contains `203.0.113.` OR `192.0.2.` OR `198.51.100.` (RFC 5737 documentation ranges), record the trap.
- Detector `coredns-sandbox-configmap-mount`: read subPaths via `kubectl get deploy q03-coredns -n "$ns" -o jsonpath='{.spec.template.spec.containers[0].volumeMounts[*].subPath}'`. If the joined string contains `corefile` (lowercase) and NOT `Corefile` (capital), record the trap. Case-sensitive bash match.

Fixture path: `cka-sim/tests/fixtures/phase-06/troubleshooting-03-coredns-resolution/`.

Pass G forbidden-command guard specifically forbids `kubectl edit configmap coredns -n kube-system`. setup/grade/ref-solution/reset MUST use the lab-ns ConfigMap `q03-coredns-corefile` (NOT `coredns`); MUST NOT reference `kube-system` anywhere.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Question files (6) + fixture tree (3)</name>
  <files>cka-sim/packs/troubleshooting/03-coredns-resolution/setup.sh, cka-sim/packs/troubleshooting/03-coredns-resolution/grade.sh, cka-sim/packs/troubleshooting/03-coredns-resolution/reset.sh, cka-sim/packs/troubleshooting/03-coredns-resolution/ref-solution.sh, cka-sim/packs/troubleshooting/03-coredns-resolution/metadata.yaml, cka-sim/packs/troubleshooting/03-coredns-resolution/question.md, cka-sim/tests/fixtures/phase-06/troubleshooting-03-coredns-resolution/stub-responses.json, cka-sim/tests/fixtures/phase-06/troubleshooting-03-coredns-resolution/expected-fail-score.txt, cka-sim/tests/fixtures/phase-06/troubleshooting-03-coredns-resolution/expected-pass-score.txt</files>
  <read_first>
    - cka-sim/packs/services-networking/03-coredns-resolution/setup.sh (Phase 5 analog — dnsConfig-None Pod shape; Q03 uses lab CoreDNS instead of per-Pod dnsConfig hack)
    - cka-sim/packs/services-networking/03-coredns-resolution/grade.sh (kubectl exec nslookup oracle + record_trap idiom)
    - cka-sim/packs/services-networking/03-coredns-resolution/ref-solution.sh (kubectl delete + re-apply Pod idiom; adapt to ConfigMap re-apply)
    - cka-sim/packs/cluster-architecture/05-audit-policy/setup.sh (sandbox ConfigMap seeding heredoc pattern for Deployment+ConfigMap inline)
    - cka-sim/lib/setup.sh (ensure_lab_ns + wait_for_ns_active contracts)
    - cka-sim/lib/grade.sh (assert_resource_exists signature + manual increment protocol)
    - cka-sim/traps/catalog.yaml (confirm P02 entries coredns-forward-to-invalid-upstream AND coredns-sandbox-configmap-mount are live)
    - .planning/phases/06-troubleshooting-pack/06-RESEARCH.md (§4 Q3 section; §11 CoreDNS ConfigMap field name currency)
    - .planning/phases/06-troubleshooting-pack/06-PATTERNS.md (§4 P05 row; §Excerpt A sandbox setup; §Excerpt B grade.sh)
  </read_first>
  <action>
    Produce 6 question files and 3 fixtures. Use services-networking/03-coredns-resolution/ as the structural analog for the nslookup-probe grader and cluster-architecture/05-audit-policy/setup.sh for the ConfigMap+Deployment heredoc pattern.

    **setup.sh — required shape:**
    - Shebang `#!/bin/bash` + `set -euo pipefail` + env guards (`CKA_SIM_LAB_NS`, `CKA_SIM_ROOT`).
    - `source "$CKA_SIM_ROOT/lib/setup.sh"` with shellcheck directive.
    - Constants: `CKA_SIM_PACK="troubleshooting"`, `CKA_SIM_QUESTION_ID="troubleshooting-coredns-resolution"`, `ns="$CKA_SIM_LAB_NS"`.
    - Call `cka_sim::setup::ensure_lab_ns` + `cka_sim::setup::wait_for_ns_active` (timeout 120).
    - Apply ConfigMap `q03-coredns-corefile` via `kubectl apply -f -` heredoc. `data.Corefile:` is a literal block scalar containing the zone (see Interfaces for the exact plugin set). Include inline comment `# INTENTIONAL TRAP: forward upstream 203.0.113.53:53 is TEST-NET-3 (unroutable)`.
    - Apply Deployment `q03-coredns` via heredoc. volumeMount subPath is `corefile` (lowercase); add inline comment `# INTENTIONAL TRAP: subPath 'corefile' does not match ConfigMap key 'Corefile' (case-sensitive)`. Volume items must explicitly map key `Corefile` → path `Corefile` (the ConfigMap key stays capital; the trap lives in volumeMount subPath).
    - Apply Service `q03-coredns` (ClusterIP, selector `app.kubernetes.io/name: q03-coredns`, ports UDP/53 + TCP/53, targetPort 53).
    - `kubectl wait --for=condition=Available deployment/q03-coredns -n "$ns" --timeout=60s 2>/dev/null || true`.
    - Poll up to 30s for `clusterip=$(kubectl get svc q03-coredns -n "$ns" -o jsonpath='{.spec.clusterIP}')`; `die` if still empty after 30s.
    - Apply Pod `q03-dnsclient` via heredoc with the discovered ClusterIP substituted into `dnsConfig.nameservers`.
    - Best-effort Ready wait: `kubectl wait --for=condition=Ready pod/q03-dnsclient -n "$ns" --timeout=60s 2>/dev/null || true`.
    - Forbidden in setup.sh: any mention of `kube-system`; `kubectl edit`/`kubectl patch`/`kubectl delete` against kube-system/coredns; `systemctl`; `/etc/kubernetes/`; `/var/lib/kubelet/`.

    **grade.sh — required shape:**
    - Shebang + `set -uo pipefail` + env guards.
    - `source "$CKA_SIM_ROOT/lib/grade.sh"` + `source "$CKA_SIM_ROOT/lib/traps.sh"`.
    - `ns="$CKA_SIM_LAB_NS"`. Best-effort Ready wait on `pod/q03-dnsclient` (30s).
    - Structural assertions via helpers:
      - `cka_sim::grade::assert_resource_exists configmap q03-coredns-corefile -n "$ns"`
      - `cka_sim::grade::assert_resource_exists deployment q03-coredns -n "$ns"`
      - `cka_sim::grade::assert_resource_exists service q03-coredns -n "$ns"`
      - `cka_sim::grade::assert_resource_exists pod q03-dnsclient -n "$ns"`
      - `cka_sim::grade::assert_field_eq pod q03-dnsclient '{.spec.dnsPolicy}' 'None' -n "$ns"`
    - Custom probe (DNS) — manual increment: `TOTAL++`; capture `out` and `rc` from `kubectl exec -n "$ns" q03-dnsclient -- nslookup kubernetes.default.svc.cluster.local 2>&1`; on `rc==0 && out contains "Address"` → PASSED++, `ok`; else FAILS+, `err`.
    - Trap detector `coredns-forward-to-invalid-upstream`: read Corefile via jsonpath `{.data.Corefile}`; if it contains `203.0.113.` OR `192.0.2.` OR `198.51.100.`, `cka_sim::grade::record_trap coredns-forward-to-invalid-upstream`.
    - Trap detector `coredns-sandbox-configmap-mount`: read subPaths via jsonpath `{.spec.template.spec.containers[0].volumeMounts[*].subPath}`; if the joined string contains lowercase `corefile` AND does NOT contain capital `Corefile`, `cka_sim::grade::record_trap coredns-sandbox-configmap-mount`.
    - Final line: `cka_sim::grade::emit_result`.
    - Forbidden in grade.sh: any mutating `kubectl` verb; any `kubectl get ... | grep` pipe; any `kubectl get -A`.

    **ref-solution.sh — required shape:**
    - Shebang + `set -euo pipefail` + env guards.
    - Re-apply ConfigMap `q03-coredns-corefile` via heredoc with the same `Corefile:` key but the forward clause is `forward . /etc/resolv.conf` (production-correct delegate to node's resolv.conf).
    - Re-apply Deployment `q03-coredns` via heredoc identical to setup.sh EXCEPT `volumeMounts[0].subPath: Corefile` (capital C).
    - `kubectl rollout restart deployment/q03-coredns -n "$CKA_SIM_LAB_NS"` then best-effort `kubectl rollout status ... --timeout=60s`.
    - Do NOT recreate `q03-dnsclient` — Service ClusterIP is stable across ConfigMap/Deployment edits.

    **reset.sh — required shape:**
    - Shebang + `set -uo pipefail` + `CKA_SIM_LAB_NS` guard.
    - `kubectl delete namespace "$CKA_SIM_LAB_NS" --ignore-not-found --wait=false`.
    - `exit 0`.

    **metadata.yaml — required fields:**
    - `id: troubleshooting-coredns-resolution`
    - `domain: troubleshooting`
    - `estimatedMinutes: 8`
    - `verified_against: "1.35"`
    - `traps` (3): `coredns-forward-to-invalid-upstream`, `coredns-sandbox-configmap-mount`, `missing-dns-egress`. Third trap is declared to satisfy GRADE-04 (>=3 traps); grade.sh does not record it in this flow (no NetworkPolicy), which is acceptable (lint-packs pass E counts declared traps, not recorded).
    - `references` (2):
      1. `kind: prior-art-exercise`, `target: cka-sim/packs/services-networking/03-coredns-resolution/`, `note: "Related prior-art: CoreDNS Pod dnsConfig + nslookup oracle"`.
      2. `kind: k8s-doc`, `target: https://kubernetes.io/docs/tasks/administer-cluster/coredns/`, `note: CoreDNS Corefile + ConfigMap administration`.
    - Field ORDER per schema (see services-networking/06-netpol-endport/metadata.yaml for byte-exact order).

    **question.md — required shape (D-04 symptoms-only):**
    - Title: `# Troubleshooting: Pods cannot resolve DNS names`.
    - Banner: `**Domain:** Troubleshooting  |  **Estimated time:** 8 minutes`.
    - Narrative: a debug Pod `q03-dnsclient` in `${CKA_SIM_LAB_NS}` cannot resolve external names (e.g. `www.example.com`) or cluster-internal names (e.g. `kubernetes.default.svc.cluster.local`); other lab-ns infrastructure is running.
    - `## Tasks` (3 bullets): inspect namespaced workloads + supporting resources; reproduce the DNS failure from inside `q03-dnsclient`; restore name resolution for both external and internal names.
    - `## Constraints`: lab-namespace-scoped changes only (no `kube-system` mutation); do NOT modify `q03-dnsclient` Pod's DNS settings — adjust supporting resources.
    - `## Verify yourself`: two `kubectl exec q03-dnsclient -- nslookup ...` snippets (cluster-internal + external); both must resolve.
    - D-04 forbidden tokens (MUST NOT appear): `CoreDNS`, `coredns`, `Corefile`, `corefile`, `ConfigMap` (when referring to q03-coredns-corefile), `configmap`, `forward`, `subPath`, `upstream`.

    **Fixtures — required shape:**
    - `stub-responses.json`: 4 object shapes (ConfigMap + Deployment + Service + Pod) in both pre-fix and post-fix variants plus exec result maps for the nslookup probe.
    - `expected-fail-score.txt`: single line `SCORE: 5/6` (5 structural assertions pass + 1 DNS probe fails).
    - `expected-pass-score.txt`: single line `SCORE: 6/6`.
    - Verify against real grader output during author time.

    `chmod +x` on the 4 .sh files.
  </action>
  <verify>
    <automated>chmod +x cka-sim/packs/troubleshooting/03-coredns-resolution/*.sh && for f in setup grade reset ref-solution; do bash -n "cka-sim/packs/troubleshooting/03-coredns-resolution/$f.sh" || exit 1; done && bash cka-sim/scripts/lint-packs.sh && bash cka-sim/scripts/lint-deprecated-strings.sh && bash cka-sim/scripts/lint-traps.sh && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - 6 files + 4 exec bits: `for f in setup.sh grade.sh reset.sh ref-solution.sh; do test -x cka-sim/packs/troubleshooting/03-coredns-resolution/$f || exit 1; done && test -f cka-sim/packs/troubleshooting/03-coredns-resolution/metadata.yaml && test -f cka-sim/packs/troubleshooting/03-coredns-resolution/question.md`
    - Fixture tree: `test -f cka-sim/tests/fixtures/phase-06/troubleshooting-03-coredns-resolution/stub-responses.json && test -f cka-sim/tests/fixtures/phase-06/troubleshooting-03-coredns-resolution/expected-fail-score.txt && test -f cka-sim/tests/fixtures/phase-06/troubleshooting-03-coredns-resolution/expected-pass-score.txt`
    - stub-responses.json valid JSON: `python3 -c "import json,sys; json.load(open(sys.argv[1]))" cka-sim/tests/fixtures/phase-06/troubleshooting-03-coredns-resolution/stub-responses.json`
    - Lab-ns CoreDNS only — NO kube-system mutation: `! grep -qE 'kubectl[[:space:]]+(edit|patch|apply|delete)[[:space:]]+(configmap[[:space:]]+)?coredns[[:space:]]+-n[[:space:]]+kube-system' cka-sim/packs/troubleshooting/03-coredns-resolution/*.sh && ! grep -qE 'kube-system' cka-sim/packs/troubleshooting/03-coredns-resolution/setup.sh`
    - setup uses Corefile capital C in ConfigMap key: `grep -qE '^[[:space:]]+Corefile:' cka-sim/packs/troubleshooting/03-coredns-resolution/setup.sh`
    - setup forward clause uses 203.0.113.53 (TEST-NET-3): `grep -q '203.0.113.53' cka-sim/packs/troubleshooting/03-coredns-resolution/setup.sh`
    - setup seeds lowercase subPath trap: `grep -q 'subPath: corefile' cka-sim/packs/troubleshooting/03-coredns-resolution/setup.sh`
    - grade records both Q3-specific traps: `grep -q 'record_trap coredns-forward-to-invalid-upstream' cka-sim/packs/troubleshooting/03-coredns-resolution/grade.sh && grep -q 'record_trap coredns-sandbox-configmap-mount' cka-sim/packs/troubleshooting/03-coredns-resolution/grade.sh`
    - grade uses jsonpath (GRADE-02): `grep -q 'jsonpath' cka-sim/packs/troubleshooting/03-coredns-resolution/grade.sh && ! grep -qE 'kubectl[[:space:]]+get[[:space:]].*\|[[:space:]]*grep' cka-sim/packs/troubleshooting/03-coredns-resolution/grade.sh && ! grep -qE 'kubectl[[:space:]]+get[[:space:]]+-A' cka-sim/packs/troubleshooting/03-coredns-resolution/grade.sh`
    - grade is read-only: `! grep -qE 'kubectl[[:space:]]+(delete|create|apply|patch|edit|replace)([[:space:]]|$)' cka-sim/packs/troubleshooting/03-coredns-resolution/grade.sh`
    - ref-solution restores Corefile forward + subPath: `grep -q 'forward \. /etc/resolv.conf' cka-sim/packs/troubleshooting/03-coredns-resolution/ref-solution.sh && grep -q 'subPath: Corefile' cka-sim/packs/troubleshooting/03-coredns-resolution/ref-solution.sh`
    - metadata declares 3 traps + cross-pack ref: `grep -qE '^    target: cka-sim/packs/services-networking/03-coredns-resolution/' cka-sim/packs/troubleshooting/03-coredns-resolution/metadata.yaml`
    - question.md D-04 compliant: `! grep -qiE 'coredns|corefile|configmap|forward|subPath|upstream' cka-sim/packs/troubleshooting/03-coredns-resolution/question.md`
    - Pass G forbidden-command guard passes: `bash cka-sim/scripts/lint-packs.sh >/dev/null`
    - Pass F no hardcoded worker names: `! grep -qE '\bnode-0[12]\b' cka-sim/packs/troubleshooting/03-coredns-resolution/*.sh`
    - lint-traps green: `bash cka-sim/scripts/lint-traps.sh >/dev/null`
    - lint-deprecated-strings green: `bash cka-sim/scripts/lint-deprecated-strings.sh >/dev/null`
    - Expected-score files single-line SCORE strings: `grep -cE '^SCORE: [0-9]+/[0-9]+$' cka-sim/tests/fixtures/phase-06/troubleshooting-03-coredns-resolution/expected-fail-score.txt | grep -qx 1 && grep -cE '^SCORE: [0-9]+/[0-9]+$' cka-sim/tests/fixtures/phase-06/troubleshooting-03-coredns-resolution/expected-pass-score.txt | grep -qx 1`
    - Full suite green: `bash cka-sim/scripts/test.sh 2>&1 | grep -qE 'all [0-9]+ case\(s\) passed'`
  </acceptance_criteria>
  <done>Q03 lab-scoped CoreDNS question ships; lints + test.sh green; kube-system untouched.</done>
</task>

</tasks>

<threat_model>
### T-6-10: Accidental mutation of kube-system/coredns ConfigMap
- **Threat:** Author reaches for `kubectl edit configmap coredns -n kube-system` or similar, breaking every other workload's DNS resolution on the live 1+2 cluster.
- **Impact:** critical — bricks cluster-wide DNS; affects every other pack's live drills.
- **Asset:** live 1+2 kubeadm cluster's kube-system/coredns.
- **Mitigation:** Pass G forbidden-command guard from P01 fails the build on `kubectl edit configmap coredns -n kube-system`. All CoreDNS state lives in lab-ns objects `q03-coredns` / `q03-coredns-corefile`. Acceptance criterion greps for `kube-system` in the scripts and rejects any match.
- **Verification:** `bash cka-sim/scripts/lint-packs.sh` exits 0 AND `! grep -q 'kube-system' cka-sim/packs/troubleshooting/03-coredns-resolution/setup.sh`. P09 VERIFICATION.md manual check: `kubectl -n kube-system get cm coredns -o yaml` before/after drill returns identical YAML.

### T-6-11: Corefile `kubernetes` plugin requires RBAC that cross-ns-escalates
- **Threat:** Author adds `kubernetes cluster.local in-addr.arpa ip6.arpa {...}` to the Corefile, requiring the lab CoreDNS Pod to read `services` and `endpoints` cluster-wide. That requires a ClusterRoleBinding — a cross-namespace escalation beyond the lab ns.
- **Impact:** medium — breaks D-11 lab-ns-only contract; introduces RBAC that persists across drill sessions.
- **Asset:** cluster-scoped RBAC purity.
- **Mitigation:** Corefile explicitly OMITS the `kubernetes` plugin. Cluster-internal DNS resolution goes upstream via forward to node resolv.conf → works because node resolv.conf points at kube-system kube-dns which DOES have the `kubernetes` plugin. Plan action and acceptance enforce Corefile shape.
- **Verification:** `grep -q 'kubernetes cluster.local' cka-sim/packs/troubleshooting/03-coredns-resolution/setup.sh` returns non-zero (miss = pass).

### T-6-12: Image pin coredns/coredns:1.11.1 unavailable offline
- **Threat:** On an air-gapped lab, the public `coredns/coredns:1.11.1` image is not pullable, and setup.sh hangs.
- **Impact:** low — only affects offline lab bootstrap; v1 scope assumes online.
- **Asset:** live-drill reliability.
- **Mitigation:** 1.11.1 is the default image bundled with kubeadm v1.35; nodes pre-pull CoreDNS during `kubeadm init`. Fallback: tag-pin to whatever `kubectl -n kube-system get deploy coredns -o jsonpath='{.spec.template.spec.containers[0].image}'` returns on the candidate's cluster — defer to post-live-drill fix.
- **Verification:** P09 VERIFICATION.md live-drill checklist for Q03.
</threat_model>

<verification>
- Round-trip (fixture): setup only → grade records coredns-forward-to-invalid-upstream + coredns-sandbox-configmap-mount + DNS probe FAIL; setup+ref-solution → grade zero traps + DNS probe PASS
- No kube-system mutation anywhere in the 4 .sh files
- Cross-pack ref `cka-sim/packs/services-networking/03-coredns-resolution/` exists (Phase 5 shipped)
- Pass G guard active; all lints green
</verification>

<success_criteria>
- Q03 covers Troubleshoot CoreDNS Tracker checkbox
- Candidate must fix BOTH the Corefile forward upstream AND the ConfigMap subPath to reach pass
- kube-system/coredns is never mutated (host-safety D-11)
</success_criteria>

<output>
Create `.planning/phases/06-troubleshooting-pack/06-05-SUMMARY.md` documenting:
- Lab-ns CoreDNS architecture (why not per-pod dnsConfig, why not kube-system edit)
- Two-stage fix: forward upstream + subPath key-case fix
- Image pin and Corefile plugin choice (no `kubernetes` plugin to avoid cluster-scoped RBAC)
- Fixture scores 5/6 → 6/6
</output>
