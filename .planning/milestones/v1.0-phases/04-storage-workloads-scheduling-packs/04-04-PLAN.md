---
phase: 04-storage-workloads-scheduling-packs
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - cka-sim/packs/storage/01-pvc-binding/setup.sh
autonomous: true
requirements: [PACK-01, PACK-06]
must_haves:
  truths:
    - "storage/01-pvc-binding/setup.sh sources cka-sim/lib/setup.sh and calls ensure_lab_ns + wait_for_ns_active helpers"
    - "the inline ns create + 24-iteration for-loop (setup.sh lines 6-38) is removed"
    - "PV + PVC heredoc seeding logic is preserved byte-for-byte (trap semantics unchanged)"
    - "script stays idempotent (TRIP-02): running twice exits 0 with no AlreadyExists"
    - "lint-packs.sh + test.sh green post-change (GRADE-06 round-trip unchanged for this question)"
  artifacts:
    - path: "cka-sim/packs/storage/01-pvc-binding/setup.sh"
      provides: "Retrofitted setup sourcing lib/setup.sh; retains trap-seeding PV + PVC"
      contains: "source \"$CKA_SIM_ROOT/lib/setup.sh\""
      contains_extra: "cka_sim::setup::wait_for_ns_active"
  key_links:
    - from: "cka-sim/packs/storage/01-pvc-binding/setup.sh"
      to: "cka-sim/lib/setup.sh"
      via: "source + ensure_lab_ns + wait_for_ns_active helper calls"
      pattern: "cka_sim::setup::"
---

<objective>
Retrofit the Phase 3 Storage reference question to use the shared `cka-sim/lib/setup.sh` helpers from Plan 01. Replace the inline ns-create + 24-iteration wait-loop (setup.sh lines 6-38) with two helper calls. Preserve the PV-seeding-without-nodeAffinity trap and the PVC seeding logic exactly as they are today (byte-for-byte).

This is the first of two Wave 2 retrofits. It proves the helper library does not regress the GRADE-06 round-trip green that Phase 3 verified on 2026-05-10 (commit `a69fe8a`).

Purpose: de-duplicate the 120s ns-Active wait so the bug fix from commit `5c421c1` propagates automatically to every future question.
Output: 1 modified file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-runtime-contract-drill-mode/03-04-SUMMARY.md
@.planning/phases/04-storage-workloads-scheduling-packs/04-CONTEXT.md
@.planning/phases/04-storage-workloads-scheduling-packs/04-RESEARCH.md
@cka-sim/lib/setup.sh
@cka-sim/packs/storage/01-pvc-binding/setup.sh
@cka-sim/packs/storage/01-pvc-binding/grade.sh
@cka-sim/packs/storage/01-pvc-binding/ref-solution.sh
@cka-sim/packs/storage/01-pvc-binding/reset.sh
@cka-sim/packs/storage/01-pvc-binding/metadata.yaml
</context>

<interfaces>
From cka-sim/lib/setup.sh (shipped by Plan 01):
- `cka_sim::setup::ensure_lab_ns <ns> <pack> <question-id>` — idempotent Namespace apply with cka-sim labels
- `cka_sim::setup::wait_for_ns_active <ns> <pack> <question-id> [<timeout-seconds>]` — default timeout 120s; re-applies ns if it disappears mid-wait

Pack + question identity (fixed, from metadata.yaml):
- pack = `storage`
- question-id = `storage-pvc-binding`
- CKA_SIM_LAB_NS = `cka-sim-storage-01` (set by drill runner — never hard-code in setup.sh)

Env-var contract (unchanged):
- `CKA_SIM_LAB_NS` must be set by drill runner — guard stays
- `CKA_SIM_ROOT` must be set so `source "$CKA_SIM_ROOT/lib/setup.sh"` resolves — add guard (Phase 3 setup.sh did NOT require CKA_SIM_ROOT; the retrofit needs it, same contract as grade.sh)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Retrofit storage/01-pvc-binding/setup.sh to source lib/setup.sh</name>
  <files>cka-sim/packs/storage/01-pvc-binding/setup.sh</files>
  <read_first>
    - cka-sim/packs/storage/01-pvc-binding/setup.sh (full file — identify the exact 32-line block to replace: lines 6-38)
    - cka-sim/lib/setup.sh (confirm function signatures + required CKA_SIM_ROOT guard)
    - cka-sim/packs/storage/01-pvc-binding/grade.sh (confirms CKA_SIM_ROOT contract already used by grade.sh)
    - .planning/phases/03-runtime-contract-drill-mode/03-04-SUMMARY.md (trap semantics to preserve)
  </read_first>
  <action>
    Rewrite `cka-sim/packs/storage/01-pvc-binding/setup.sh` to the following exact contents. This replaces lines 6-38 (inline ns create + 24-iteration wait loop) with two helper calls and adds the CKA_SIM_ROOT guard. PV + PVC heredoc blocks (lines 40-76 today) are preserved verbatim.

    ```
    #!/bin/bash
    # storage/01-pvc-binding/setup.sh — seeds hostPath PV WITHOUT nodeAffinity (trap) + PVC.
    # Retrofitted Phase 4 Plan 04: sources shared cka-sim/lib/setup.sh helpers.
    set -euo pipefail
    : "${CKA_SIM_LAB_NS:?CKA_SIM_LAB_NS must be set by drill runner}"
    : "${CKA_SIM_ROOT:?CKA_SIM_ROOT must be set (drill runner exports it)}"

    # shellcheck source=../../../lib/setup.sh disable=SC1091
    source "$CKA_SIM_ROOT/lib/setup.sh"

    # 1. Idempotent ns create + 120s Active wait (helper absorbs the --wait=false race).
    cka_sim::setup::ensure_lab_ns "$CKA_SIM_LAB_NS" storage storage-pvc-binding
    cka_sim::setup::wait_for_ns_active "$CKA_SIM_LAB_NS" storage storage-pvc-binding 120

    # 2. Apply hostPath PV — INTENTIONALLY MISSING nodeAffinity (the trap).
    # Cluster-scoped -> q01- prefix per TRIP-03.
    kubectl apply -f - <<EOF
    apiVersion: v1
    kind: PersistentVolume
    metadata:
      name: q01-app-pv
      labels:
        cka-sim/pack: storage
        cka-sim/question-id: storage-pvc-binding
    spec:
      capacity:
        storage: 1Gi
      accessModes:
        - ReadWriteOnce
      persistentVolumeReclaimPolicy: Retain
      storageClassName: manual
      hostPath:
        path: /tmp/q01-app-pv
        type: DirectoryOrCreate
    EOF

    # 3. Apply PVC (will stay Pending until candidate fixes the PV's nodeAffinity).
    kubectl apply -f - <<EOF
    apiVersion: v1
    kind: PersistentVolumeClaim
    metadata:
      name: app-data
      namespace: ${CKA_SIM_LAB_NS}
    spec:
      accessModes:
        - ReadWriteOnce
      resources:
        requests:
          storage: 500Mi
      storageClassName: manual
    EOF
    ```

    Constraints:
    - The PV YAML (apiVersion through type: DirectoryOrCreate) MUST be byte-identical to the current file's lines 42-60. Do not reformat indentation or reorder fields.
    - The PVC YAML (apiVersion through storageClassName: manual) MUST be byte-identical to the current file's lines 63-76.
    - The only additions above the first heredoc: `: "${CKA_SIM_ROOT:?...}"` guard + `source "$CKA_SIM_ROOT/lib/setup.sh"` + two helper calls.
    - Do NOT change the shebang or `set -euo pipefail`.
    - Do NOT touch grade.sh / reset.sh / ref-solution.sh / metadata.yaml / question.md — this plan owns ONLY setup.sh.

    After writing, ensure executable bit is set: `chmod +x cka-sim/packs/storage/01-pvc-binding/setup.sh`.
  </action>
  <verify>
    <automated>chmod +x cka-sim/packs/storage/01-pvc-binding/setup.sh && bash -n cka-sim/packs/storage/01-pvc-binding/setup.sh && grep -q 'source.*lib/setup.sh' cka-sim/packs/storage/01-pvc-binding/setup.sh && grep -q 'cka_sim::setup::ensure_lab_ns' cka-sim/packs/storage/01-pvc-binding/setup.sh && grep -q 'cka_sim::setup::wait_for_ns_active' cka-sim/packs/storage/01-pvc-binding/setup.sh && ! grep -qE 'for i in \$\(seq 1' cka-sim/packs/storage/01-pvc-binding/setup.sh && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - Syntax valid: `bash -n cka-sim/packs/storage/01-pvc-binding/setup.sh` exits 0
    - Executable: `[[ -x cka-sim/packs/storage/01-pvc-binding/setup.sh ]]`
    - Sources lib/setup.sh: `grep -q 'source.*lib/setup.sh' cka-sim/packs/storage/01-pvc-binding/setup.sh`
    - Uses ensure_lab_ns helper: `grep -q 'cka_sim::setup::ensure_lab_ns.*storage.*storage-pvc-binding' cka-sim/packs/storage/01-pvc-binding/setup.sh`
    - Uses wait_for_ns_active helper with 120 timeout: `grep -qE 'cka_sim::setup::wait_for_ns_active.*storage.*storage-pvc-binding.*120' cka-sim/packs/storage/01-pvc-binding/setup.sh`
    - Inline wait-loop removed: `! grep -qE 'for i in \$\(seq 1 24\)' cka-sim/packs/storage/01-pvc-binding/setup.sh`
    - Inline phase=empty fallback removed: `! grep -qE 'if \[\[ -z "\$phase" \]\]' cka-sim/packs/storage/01-pvc-binding/setup.sh`
    - PV name preserved: `grep -q 'name: q01-app-pv' cka-sim/packs/storage/01-pvc-binding/setup.sh`
    - PV is still hostPath WITHOUT nodeAffinity (trap preserved): `grep -q 'path: /tmp/q01-app-pv' cka-sim/packs/storage/01-pvc-binding/setup.sh && ! grep -q 'nodeAffinity' cka-sim/packs/storage/01-pvc-binding/setup.sh`
    - PVC name preserved: `grep -q 'name: app-data' cka-sim/packs/storage/01-pvc-binding/setup.sh`
    - No `kubectl create` (TRIP-02): `! grep -qE 'kubectl[[:space:]]+create' cka-sim/packs/storage/01-pvc-binding/setup.sh`
    - No `kubectl delete ns` in setup (D-09): `! grep -qE 'kubectl[[:space:]]+delete[[:space:]]+(namespace|ns)' cka-sim/packs/storage/01-pvc-binding/setup.sh`
    - Full test suite green: `bash cka-sim/scripts/test.sh` exits 0 (lint-packs accepts retrofitted file)
    - File is shorter (inline wait removed): `wc -l cka-sim/packs/storage/01-pvc-binding/setup.sh | awk '{print $1}'` returns <= 55 (was 76)
  </acceptance_criteria>
  <done>setup.sh retrofitted, test.sh green, trap semantics unchanged.</done>
</task>

</tasks>

<verification>
- `bash cka-sim/scripts/test.sh` exits 0
- No change to grade.sh / reset.sh / ref-solution.sh / metadata.yaml / question.md (Plan 04 owns only setup.sh)
- Retrofitted setup.sh uses both helpers
- Trap-seeding logic (hostPath PV without nodeAffinity) is byte-identical to Phase 3
- Static lint-only verification here; live 1+2 cluster GRADE-06 round-trip happens in phase-end VERIFICATION.md (per Phase 3 model)
</verification>

<success_criteria>
- Future bug fixes to the ns-Active wait land in one place (lib/setup.sh), not N copies
- Wave 3 plans confidently source lib/setup.sh knowing one of the two references has already passed retrofit lint
- No regression to test.sh suite
</success_criteria>

<output>
After completion, create `.planning/phases/04-storage-workloads-scheduling-packs/04-04-SUMMARY.md` covering:
- 1 file modified; lines dropped from 76 → ~55
- Helper calls replace 32-line inline block
- Trap semantics confirmed unchanged (hostPath PV still missing nodeAffinity)
- Post-change test.sh suite green
- Deferred: live-cluster round-trip verification belongs in phase VERIFICATION.md (Plan 16 triggers it)
</output>
