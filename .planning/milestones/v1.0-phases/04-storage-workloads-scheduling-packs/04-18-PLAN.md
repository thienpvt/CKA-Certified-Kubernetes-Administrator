---
phase: 04-storage-workloads-scheduling-packs
plan: 18
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/setup.sh
  - cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/reset.sh
  - cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/ref-solution.sh
  - cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/grade.sh
  - cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/question.md
autonomous: true
requirements: [PACK-02, PACK-06]
must_haves:
  truths:
    - "workloads/08 setup.sh no longer hardcodes `node-02`; taints the first non-control-plane worker discovered via kubectl selector"
    - "workloads/08 reset.sh mirrors the same discovery and cleans whichever worker setup used"
    - "workloads/08 ref-solution.sh labels the same discovered worker (not `node-02`) so the patched Deployment's nodeAffinity resolves on the live 1+2 cluster"
    - "workloads/08 grade.sh discovers the same worker and asserts replicas land there + that worker carries label gpu=true"
    - "workloads/08 question.md instructs the candidate to target `the non-control-plane worker node` (with the discovery command shown), not literal `node-02`"
    - "`cka-sim drill workloads-scheduling --question 08` no longer fails with `Error from server (NotFound): nodes \"node-02\" not found` (BUG-3 evidence: cka-sim/results.txt line 493)"
  artifacts:
    - path: "cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/setup.sh"
      provides: "setup that taints a dynamically-discovered worker"
      contains: "kubectl get nodes -l '!node-role.kubernetes.io/control-plane'"
    - path: "cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/reset.sh"
      provides: "reset that untaints + unlabels the same discovered worker"
      contains: "kubectl get nodes -l '!node-role.kubernetes.io/control-plane'"
    - path: "cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/ref-solution.sh"
      provides: "ref that labels the discovered worker + patches Deployment"
      contains: "kubectl get nodes -l '!node-role.kubernetes.io/control-plane'"
    - path: "cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/grade.sh"
      provides: "grader that discovers the worker and asserts placement"
      contains: "kubectl get nodes -l '!node-role.kubernetes.io/control-plane'"
    - path: "cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/question.md"
      provides: "Candidate prompt that names the target node dynamically"
      contains: "!node-role.kubernetes.io/control-plane"
  key_links:
    - from: "setup.sh"
      to: "reset.sh + ref-solution.sh + grade.sh"
      via: "identical worker-discovery kubectl command (same selector, same jsonpath)"
      pattern: "!node-role.kubernetes.io/control-plane.*\\{\\.items\\[0\\]\\.metadata\\.name\\}"
    - from: "question.md"
      to: "candidate workflow"
      via: "documented discovery command in the 'Find your target node' section"
      pattern: "kubectl get nodes -l"
---

<objective>
Close BUG-3 (04-VERIFICATION.md gaps section, severity critical, must_have MH-5): workloads-scheduling question 08 hardcodes the literal K8s node name `node-02` in 5 files. On the user's 1+2 kubeadm cluster the K8s node names visible to `kubectl get nodes` do not match the SSH aliases `node-01`/`node-02` from Phase 1 BOOT-03, so setup.sh fails with `Error from server (NotFound): nodes "node-02" not found` (cka-sim/results.txt line 493).

The VERIFICATION.md `fix:` field names only setup.sh + reset.sh + ref-solution.sh, but `grade.sh` (lines 27, 30, 32, 33, 35, 36, 39, 40, 44, 45, 47, 48) and `question.md` (lines 5, 9, 11, 13, 17, 24, 28) also hardcode `node-02`. Without updating those two the grader will always fail its placement + label assertions and the candidate prompt still points at a nonexistent node. This plan fixes all 5 files consistently with the same discovery idiom.

**Discovery idiom (single source of truth):**
```
target_node=$(kubectl get nodes \
  -l '!node-role.kubernetes.io/control-plane' \
  --no-headers -o jsonpath='{.items[0].metadata.name}')
```

Exactly one line, exactly this form, in every script. Identical selector (`!node-role.kubernetes.io/control-plane`) and identical jsonpath (`{.items[0].metadata.name}`) means all four scripts pick the same worker on a given cluster as long as the node set is stable between drill phases (setup → grade → ref → reset all run against the same API server within a single drill invocation).

Purpose: Unblock MH-5 live round-trip for workloads question 08 (PACK-02 + PACK-06). Closes the second of two gaps in `re_verification.gaps_remaining`.
Output: 5 surgical file edits (setup/reset/ref-solution/grade/question.md) replacing literal `node-02` with the dynamically-discovered worker. Zero new helpers, zero schema changes, zero dependencies on 04-17.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-storage-workloads-scheduling-packs/04-VERIFICATION.md
@.planning/phases/04-storage-workloads-scheduling-packs/04-CONTEXT.md
@cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/setup.sh
@cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/reset.sh
@cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/ref-solution.sh
@cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/grade.sh
@cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/question.md
</context>

<interfaces>
Existing conventions (keep):
- `set -euo pipefail` (setup/ref-solution) or `set -uo pipefail` (reset/grade — allow non-zero to continue cleanup/grading)
- `: "${CKA_SIM_LAB_NS:?...}"` guard at top of every script
- `lib/setup.sh` + `lib/grade.sh` + `lib/traps.sh` sourcing patterns unchanged
- Deployment name `q08-gpu-sim`, lab ns `$CKA_SIM_LAB_NS`, label key `gpu`, value `true`, taint effect `NoSchedule`
- Grader structure: CKA_SIM_GRADE_TOTAL / CKA_SIM_GRADE_PASSED accumulators + CKA_SIM_GRADE_PASSES / CKA_SIM_GRADE_FAILS lists + `ok`/`err` emitters + `cka_sim::grade::emit_result` at tail

New invariant introduced by this plan:
- Every q08 script that needs the target node name MUST compute it via:
  ```
  target_node=$(kubectl get nodes -l '!node-role.kubernetes.io/control-plane' --no-headers -o jsonpath='{.items[0].metadata.name}')
  ```
  then guard with:
  ```
  [[ -n "${target_node}" ]] || { echo "ERROR: no non-control-plane worker node found" >&2; exit 1; }
  ```
  (graders use the soft form — append to CKA_SIM_GRADE_FAILS rather than exit 1)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite setup.sh and reset.sh to discover the worker dynamically</name>
  <files>
    cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/setup.sh,
    cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/reset.sh
  </files>
  <read_first>
    - cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/setup.sh (full file — 52 lines; the heredoc Deployment spec stays byte-identical, only the taint line + comments change)
    - cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/reset.sh (full file — 19 lines; both cluster-scoped kubectl calls change)
    - cka-sim/results.txt line 493 (confirm evidence: `Error from server (NotFound): nodes "node-02" not found`)
    - cka-sim/tests/fixtures/workloads-* (list the dir; if any fixture contains literal `node-02` in stubbed kubectl output relevant to q08, add a side-edit to the test.sh verify step — most likely the q08 fixture uses a stub name like `worker-1`, so the discovery idiom resolves to the stub node; verify after editing)
  </read_first>
  <action>
    **File 1 — `setup.sh`** — surgical edit. Target post-state (overwrite the whole file is safe here since it is only 52 lines):
    ```
    #!/bin/bash
    # workloads-scheduling/08-nodeselector-affinity-taints/setup.sh
    # Seeds BROKEN Deployment q08-gpu-sim (no toleration, no nodeAffinity) and
    # adds the gpu=true:NoSchedule taint to the first non-control-plane worker
    # node (discovered dynamically so this works on clusters where the K8s node
    # names differ from the Phase 1 BOOT-03 SSH aliases node-01/node-02).
    # Candidate labels the same worker with gpu=true + patches Deployment with
    # toleration + required nodeAffinity.
    #
    # External dep: reset.sh MUST remove the gpu label + taint from the same
    # worker (discovery idiom is identical across setup/reset/ref/grade).
    set -euo pipefail
    : "${CKA_SIM_LAB_NS:?CKA_SIM_LAB_NS must be set by drill runner}"
    : "${CKA_SIM_ROOT:?CKA_SIM_ROOT must be set (drill runner exports it)}"

    # shellcheck source=../../../lib/setup.sh disable=SC1091
    source "$CKA_SIM_ROOT/lib/setup.sh"

    cka_sim::setup::ensure_lab_ns "$CKA_SIM_LAB_NS" workloads-scheduling workloads-nodeselector-affinity-taints
    cka_sim::setup::wait_for_ns_active "$CKA_SIM_LAB_NS" workloads-scheduling workloads-nodeselector-affinity-taints 120

    # Discover the target worker. Selector `!node-role.kubernetes.io/control-plane`
    # excludes control-plane nodes; jsonpath picks the first worker deterministically.
    target_node=$(kubectl get nodes -l '!node-role.kubernetes.io/control-plane' \
      --no-headers -o jsonpath='{.items[0].metadata.name}')
    [[ -n "${target_node}" ]] || { echo "ERROR: no non-control-plane worker node found (q08 requires >=1 worker)" >&2; exit 1; }

    # Add gpu=true:NoSchedule taint to the discovered worker (overwrite so re-runs are idempotent).
    kubectl taint nodes "${target_node}" gpu=true:NoSchedule --overwrite

    # Intentionally DO NOT label the worker with gpu=true -- that is the candidate's job.

    # Broken Deployment: no tolerations, no nodeAffinity -> replicas stay Pending
    # because they cannot tolerate the worker's taint and have no affinity pinning
    # them to the target worker. Requests included so the scheduler has real
    # placement feedback.
    kubectl apply -f - <<EOF
    apiVersion: apps/v1
    kind: Deployment
    metadata:
      name: q08-gpu-sim
      namespace: ${CKA_SIM_LAB_NS}
      labels:
        app: q08-gpu-sim
    spec:
      replicas: 2
      selector:
        matchLabels:
          app: q08-gpu-sim
      template:
        metadata:
          labels:
            app: q08-gpu-sim
        spec:
          containers:
            - name: app
              image: busybox:1.36
              command: ["sh","-c","sleep 3600"]
              resources:
                requests:
                  cpu: 25m
                  memory: 32Mi
    EOF
    ```

    **File 2 — `reset.sh`** — target post-state (19 lines, safe to overwrite):
    ```
    #!/bin/bash
    # workloads-scheduling/08-nodeselector-affinity-taints/reset.sh
    # Cluster-scoped cleanup: removes both the gpu=true label AND the
    # gpu=true:NoSchedule taint from the worker that setup.sh selected, so
    # later questions do not see unexpected scheduler behaviour. Discovery
    # idiom is identical to setup.sh (same selector, same jsonpath) so the
    # same worker is selected within one drill invocation.
    set -uo pipefail
    : "${CKA_SIM_LAB_NS:?CKA_SIM_LAB_NS must be set}"

    kubectl delete namespace "$CKA_SIM_LAB_NS" --ignore-not-found --wait=false

    target_node=$(kubectl get nodes -l '!node-role.kubernetes.io/control-plane' \
      --no-headers -o jsonpath='{.items[0].metadata.name}' 2>/dev/null || true)

    if [[ -n "${target_node}" ]]; then
      # Untaint the worker (trailing dash on key = remove).
      kubectl taint nodes "${target_node}" gpu- 2>/dev/null || true
      # Unlabel the worker (trailing dash on key = remove).
      # IN-03 (04-REVIEW.md): --overwrite is only meaningful when SETTING a label;
      # for 'key-' removal syntax the flag is a no-op and newer kubectl warns on it.
      kubectl label nodes "${target_node}" gpu- 2>/dev/null || true
    fi

    exit 0
    ```

    Use the Write tool on both files (not Edit) — each file is small enough that full rewrite is lower-risk than sed-patching individual lines, and a full rewrite eliminates any residual `node-02` literal.

    **Test fixture check:** After writing both files, run `grep -rln 'node-02' cka-sim/tests/fixtures/` — if any fixture mentions `node-02` in the context of q08, update that fixture to use the fixture's own stub node name (most Phase 2/3 kubectl stubs use names like `worker-1` / `worker1`). If lint-packs or test.sh goes red after the edit, track the stub mismatch as a side-effect of this plan and fix the fixture in the same commit.
  </action>
  <verify>
    <automated>bash -c 'set -e; ! grep -n "node-02" cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/setup.sh && ! grep -n "node-02" cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/reset.sh; grep -q "kubectl get nodes -l .!node-role.kubernetes.io/control-plane." cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/setup.sh; grep -q "kubectl get nodes -l .!node-role.kubernetes.io/control-plane." cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/reset.sh; bash -n cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/setup.sh; bash -n cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/reset.sh; bash cka-sim/scripts/lint-packs.sh; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - Zero `node-02` literals in setup.sh: `grep -c 'node-02' cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/setup.sh` prints `0`
    - Zero `node-02` literals in reset.sh: same check returns `0`
    - setup.sh contains the discovery command: `grep -qE "kubectl get nodes -l '!node-role\\.kubernetes\\.io/control-plane'" cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/setup.sh` exits 0
    - reset.sh contains the discovery command: same regex, exits 0
    - Both files parse: `bash -n setup.sh && bash -n reset.sh` exits 0
    - Both files still `100755` (no accidental mode regression): `git ls-files -s cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/{setup,reset}.sh | awk '{print $1}'` prints `100755` twice
    - Heredoc YAML Deployment body in setup.sh is byte-identical to pre-state (apart from indentation preserved by the rewrite): `grep -q 'image: busybox:1.36' setup.sh && grep -q 'cpu: 25m' setup.sh && grep -q 'memory: 32Mi' setup.sh`
    - `bash cka-sim/scripts/lint-packs.sh` exits 0 (no GRADE-02 / D-09 / mutating-verb regressions)
  </acceptance_criteria>
  <done>setup.sh + reset.sh use dynamic worker discovery; no `node-02` literal remains; lint-packs green.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite ref-solution.sh and grade.sh with matching worker discovery</name>
  <files>
    cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/ref-solution.sh,
    cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/grade.sh
  </files>
  <read_first>
    - cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/ref-solution.sh (33 lines; single `kubectl label nodes node-02` line + JSON-patch body that stays byte-identical)
    - cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/grade.sh (52 lines; Assertions 3 + 4 compare against literal `node-02`)
    - cka-sim/lib/grade.sh (confirm `cka_sim::grade::assert_field_eq` signature — Assertions 1 + 2 don't touch the node name so they stay unchanged)
  </read_first>
  <action>
    **File 1 — `ref-solution.sh`** — target post-state (safe to overwrite):
    ```
    #!/bin/bash
    # workloads-scheduling/08-nodeselector-affinity-taints/ref-solution.sh
    # Labels the dynamically-discovered worker with gpu=true and patches
    # q08-gpu-sim with a toleration for the NoSchedule taint + required
    # nodeAffinity pinning replicas to nodes with label gpu=true.
    #
    # Discovery idiom is identical to setup.sh / reset.sh / grade.sh so this
    # targets the same worker setup.sh tainted.
    set -euo pipefail
    : "${CKA_SIM_LAB_NS:?CKA_SIM_LAB_NS must be set}"

    target_node=$(kubectl get nodes -l '!node-role.kubernetes.io/control-plane' \
      --no-headers -o jsonpath='{.items[0].metadata.name}')
    [[ -n "${target_node}" ]] || { echo "ERROR: no non-control-plane worker node found" >&2; exit 1; }

    kubectl label nodes "${target_node}" gpu=true --overwrite

    kubectl patch deployment q08-gpu-sim -n "$CKA_SIM_LAB_NS" --type=strategic -p='{
      "spec": {
        "template": {
          "spec": {
            "tolerations": [
              {"key":"gpu","operator":"Equal","value":"true","effect":"NoSchedule"}
            ],
            "affinity": {
              "nodeAffinity": {
                "requiredDuringSchedulingIgnoredDuringExecution": {
                  "nodeSelectorTerms": [
                    {"matchExpressions": [{"key":"gpu","operator":"In","values":["true"]}]}
                  ]
                }
              }
            }
          }
        }
      }
    }'

    kubectl rollout status deployment/q08-gpu-sim -n "$CKA_SIM_LAB_NS" --timeout=120s
    ```

    **File 2 — `grade.sh`** — target post-state (safe to overwrite):
    ```
    #!/bin/bash
    # workloads-scheduling/08-nodeselector-affinity-taints/grade.sh
    set -uo pipefail
    : "${CKA_SIM_LAB_NS:?CKA_SIM_LAB_NS must be set}"
    : "${CKA_SIM_ROOT:?CKA_SIM_ROOT must be set}"

    # shellcheck source=../../../lib/grade.sh disable=SC1091
    source "$CKA_SIM_ROOT/lib/grade.sh"
    # shellcheck source=../../../lib/traps.sh disable=SC1091
    source "$CKA_SIM_ROOT/lib/traps.sh"

    # Wait up to 90s for Available (scheduler needs time after candidate patch rolls out).
    kubectl wait --for=condition=Available deployment/q08-gpu-sim -n "$CKA_SIM_LAB_NS" --timeout=90s 2>/dev/null || true

    # Discover target worker. Identical idiom to setup.sh / reset.sh / ref-solution.sh.
    # On failure the discovery-dependent assertions auto-FAIL (soft fail -- do not exit).
    target_node=$(kubectl get nodes -l '!node-role.kubernetes.io/control-plane' \
      --no-headers -o jsonpath='{.items[0].metadata.name}' 2>/dev/null || echo "")

    # Assertion 1: Deployment has a toleration for key=gpu with effect=NoSchedule.
    cka_sim::grade::assert_field_eq deployment q08-gpu-sim \
      '{.spec.template.spec.tolerations[?(@.key=="gpu")].effect}' \
      'NoSchedule' -n "$CKA_SIM_LAB_NS"

    # Assertion 2: required nodeAffinity with matchExpression key=gpu, operator=In.
    cka_sim::grade::assert_field_eq deployment q08-gpu-sim \
      '{.spec.template.spec.affinity.nodeAffinity.requiredDuringSchedulingIgnoredDuringExecution.nodeSelectorTerms[0].matchExpressions[?(@.key=="gpu")].operator}' \
      'In' -n "$CKA_SIM_LAB_NS"

    # Assertion 3: every replica lands on the target worker (no other node names).
    # jsonpath emits a space-separated stream of nodeName values.
    nodes=$(kubectl get pod -n "$CKA_SIM_LAB_NS" -l app=q08-gpu-sim -o jsonpath='{.items[*].spec.nodeName}' 2>/dev/null || echo "")
    unique_nodes=$(echo "$nodes" | tr ' ' '\n' | sort -u | tr '\n' ',' | sed 's/,$//')
    CKA_SIM_GRADE_TOTAL=$(( CKA_SIM_GRADE_TOTAL + 1 ))
    if [[ -n "$nodes" ]] && [[ -n "$target_node" ]] && [[ "$unique_nodes" == "$target_node" ]]; then
      CKA_SIM_GRADE_PASSED=$(( CKA_SIM_GRADE_PASSED + 1 ))
      CKA_SIM_GRADE_PASSES+=("all replicas land on ${target_node}")
      ok "all replicas land on ${target_node}"
    else
      CKA_SIM_GRADE_FAILS+=("replicas are on nodes '${nodes:-<none>}' (expected only '${target_node:-<discovery-failed>}')")
      err "replicas are on nodes '${nodes:-<none>}' (expected only '${target_node:-<discovery-failed>}')"
    fi

    # Assertion 4: target worker has label gpu=true.
    CKA_SIM_GRADE_TOTAL=$(( CKA_SIM_GRADE_TOTAL + 1 ))
    if [[ -n "$target_node" ]]; then
      label=$(kubectl get node "$target_node" -o jsonpath='{.metadata.labels.gpu}' 2>/dev/null || echo "")
      if [[ "$label" == "true" ]]; then
        CKA_SIM_GRADE_PASSED=$(( CKA_SIM_GRADE_PASSED + 1 ))
        CKA_SIM_GRADE_PASSES+=("${target_node} has label gpu=true")
        ok "${target_node} has label gpu=true"
      else
        CKA_SIM_GRADE_FAILS+=("${target_node} label gpu='${label:-<unset>}' (expected 'true')")
        err "${target_node} label gpu='${label:-<unset>}' (expected 'true')"
      fi
    else
      CKA_SIM_GRADE_FAILS+=("target worker discovery failed (no non-control-plane node visible)")
      err "target worker discovery failed (no non-control-plane node visible)"
    fi

    cka_sim::grade::emit_result
    ```

    Implementation notes:
    - Use the Write tool to overwrite each file (the whole-file rewrite is smaller than the delta for a precise Edit).
    - Assertions 1 and 2 in grade.sh are unchanged (they check Deployment spec fields, not node names) — only Assertions 3 and 4 touch the node name.
    - Grader discovery uses the soft form (`|| echo ""` + per-assertion empty-string guard) instead of `exit 1`, because grading must continue to emit SCORE: x/M even when discovery fails — that is the Phase 3 grade.sh contract (assertions accumulate, never `die`).
    - Do NOT modify `lib/grade.sh`. The grade.sh accumulator pattern here inlines the `CKA_SIM_GRADE_TOTAL++ / PASSES+=/FAILS+=` idiom, identical to pre-state for the same two assertions — only the target-node value changes.
    - Do NOT introduce any new helper in `lib/setup.sh`. The discovery idiom is a one-off for q08.
  </action>
  <verify>
    <automated>bash -c 'set -e; ! grep -n "node-02" cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/ref-solution.sh && ! grep -n "node-02" cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/grade.sh; grep -q "kubectl get nodes -l .!node-role.kubernetes.io/control-plane." cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/ref-solution.sh; grep -q "kubectl get nodes -l .!node-role.kubernetes.io/control-plane." cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/grade.sh; bash -n cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/ref-solution.sh; bash -n cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/grade.sh; bash cka-sim/scripts/lint-packs.sh; bash cka-sim/scripts/test.sh; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - Zero `node-02` literals in ref-solution.sh: `grep -c 'node-02' ref-solution.sh` prints `0`
    - Zero `node-02` literals in grade.sh: same check returns `0`
    - Both files source the discovery idiom: `grep -qE "kubectl get nodes -l '!node-role\\.kubernetes\\.io/control-plane'" {ref-solution,grade}.sh` exits 0 for each
    - Both files parse: `bash -n ref-solution.sh && bash -n grade.sh` exits 0
    - Both files still `100755`: `git ls-files -s .../{ref-solution,grade}.sh | awk '{print $1}'` prints `100755` twice
    - JSON-patch body in ref-solution.sh byte-identical: `grep -q '"requiredDuringSchedulingIgnoredDuringExecution"' ref-solution.sh && grep -q '"operator":"Equal"' ref-solution.sh`
    - grade.sh still has 4 assertions: `grep -c '^CKA_SIM_GRADE_TOTAL=(( CKA_SIM_GRADE_TOTAL + 1 ))$\|cka_sim::grade::assert_field_eq' grade.sh` reports >= 4 (2 assert_field_eq + 2 inline accumulator hits)
    - grade.sh GRADE-02 / mutating-verb lint still green — `bash cka-sim/scripts/lint-packs.sh` exits 0 (passes A + B must not regress: no `kubectl get | grep`, no `kubectl {delete,create,apply,patch,edit,replace}` in grade.sh)
    - `bash cka-sim/scripts/test.sh` exits 0 — full chain green (lint-traps + lint-packs + lint-coverage + unit cases)
  </acceptance_criteria>
  <done>ref-solution.sh + grade.sh use matching worker discovery; assertions 3+4 compare against the discovered worker; lint-packs + test.sh green.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite question.md so the candidate prompt no longer names node-02</name>
  <files>
    cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/question.md
  </files>
  <read_first>
    - cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/question.md (30 lines; 5 literal `node-02` references at lines 5, 9, 11, 13, 17, 24, 28)
    - cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/metadata.yaml (confirm `estimatedMinutes: 9` stays — prose edit only)
  </read_first>
  <action>
    Use the Write tool to overwrite the whole file (prose-only edit, no metadata or structure changes). Target post-state:

    ```
    # workloads-scheduling/08-nodeselector-affinity-taints

    **Domain:** Workloads & Scheduling  |  **Estimated time:** 9 minutes

    Deployment `q08-gpu-sim` in `${CKA_SIM_LAB_NS}` must run exclusively on the cluster's GPU worker node. The lab has tainted the first non-control-plane worker with `gpu=true:NoSchedule`. That worker does NOT yet carry the label `gpu=true`.

    ## Find your target node

    ```
    kubectl get nodes -l '!node-role.kubernetes.io/control-plane' \
      --no-headers -o jsonpath='{.items[0].metadata.name}'
    ```

    The node printed is the one the lab tainted and the one your replicas must land on. Use this node name wherever `<target-node>` appears below.

    ## Tasks

    1. Label `<target-node>` with `gpu=true`.
    2. Modify Deployment `q08-gpu-sim` so every replica:
       - tolerates the `gpu=true:NoSchedule` taint on `<target-node>`.
       - has a required `nodeAffinity` matching the `gpu=true` label (operator `In`).
    3. Confirm all replicas land on `<target-node>`.

    ## Constraints

    - Do NOT remove the taint from `<target-node>`.
    - Do NOT use `nodeSelector` — use `nodeAffinity` with `requiredDuringSchedulingIgnoredDuringExecution`.
    - Keep the image (`busybox:1.36`) and existing `resources.requests`.

    ## Verify yourself

    ```
    TARGET=$(kubectl get nodes -l '!node-role.kubernetes.io/control-plane' \
      --no-headers -o jsonpath='{.items[0].metadata.name}')

    kubectl get node "$TARGET" -o jsonpath='{.metadata.labels.gpu}'                                # true
    kubectl get deploy q08-gpu-sim -n ${CKA_SIM_LAB_NS} \
      -o jsonpath='{.spec.template.spec.tolerations[?(@.key=="gpu")].effect}'                      # NoSchedule
    kubectl get pod -n ${CKA_SIM_LAB_NS} -l app=q08-gpu-sim \
      -o jsonpath='{.items[*].spec.nodeName}'                                                      # all $TARGET
    ```
    ```

    Notes:
    - `question.md` stays `100644` (data file — do NOT chmod +x).
    - No metadata.yaml edits: estimatedMinutes, traps, references all stay exact.
    - Zero `node-02` literals remain.
    - The "Find your target node" section is a deliberate add — candidate must know the discovery command, because otherwise they have no way to know which node to label. This is aligned with CKA exam style where candidates routinely run `kubectl get nodes` to inspect cluster topology before scheduling work.
  </action>
  <verify>
    <automated>bash -c 'set -e; ! grep -n "node-02" cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/question.md; grep -q "!node-role.kubernetes.io/control-plane" cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/question.md; grep -q "Find your target node" cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/question.md; test "$(git ls-files -s cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/question.md | awk "{print \$1}")" = "100644"; grep -c "node-02" cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/*.sh cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/question.md | tee /tmp/q08-residue.txt; ! grep -v ":0$" /tmp/q08-residue.txt; bash cka-sim/scripts/test.sh; bash cka-sim/scripts/lint-packs.sh; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - Zero `node-02` literals in question.md: `grep -c 'node-02' question.md` prints `0`
    - question.md references the discovery command: `grep -q '!node-role.kubernetes.io/control-plane' question.md` exits 0
    - question.md has the new "Find your target node" section: `grep -q '^## Find your target node$' question.md` exits 0
    - question.md mode unchanged (100644, data file): `git ls-files -s question.md | awk '{print $1}'` prints `100644`
    - metadata.yaml unchanged: `git diff cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/metadata.yaml` empty
    - Repository-wide q08 `node-02` sweep clean: `grep -rn 'node-02' cka-sim/packs/workloads-scheduling/08-nodeselector-affinity-taints/` prints nothing
    - Full chain green: `bash cka-sim/scripts/test.sh` exits 0 AND `bash cka-sim/scripts/lint-packs.sh` exits 0
    - `bash cka-sim/scripts/lint-coverage.sh` exits 0 (coverage.yaml references workloads-nodeselector-affinity-taints by id, which is unchanged in metadata.yaml)
  </acceptance_criteria>
  <done>question.md prose + verify commands use the discovery idiom; no `node-02` residue anywhere in q08; full lint + test chain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary                             | Description                                                                                                                  |
|--------------------------------------|------------------------------------------------------------------------------------------------------------------------------|
| drill runner -> kubeapi (target node lookup) | Untrusted output surface: `kubectl get nodes` returns node names the scripts splice into `kubectl taint/label` commands. |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                     | Disposition | Mitigation Plan                                                                                                                                                                                                              |
|-------------|------------------------|-----------------------------------------------|-------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| T-04-18-01  | Tampering              | `target_node` splicing into kubectl commands  | mitigate    | Discovery command produces a single metadata.name field from `-o jsonpath='{.items[0].metadata.name}'`. Node names are RFC 1123 labels (validated by the API server), so the value cannot contain shell metacharacters. Scripts quote the variable (`"${target_node}"`) at every use site so even if the API ever relaxed the validation the shell parse is safe. |
| T-04-18-02  | Denial of Service      | Empty discovery result on single-node cluster | mitigate    | Every script guards with `[[ -n "${target_node}" ]]` (setup + ref: hard-fail with explicit error; reset + grade: soft-guard so cleanup / grading continues). A single-node (control-plane-only) cluster legitimately cannot run q08 — the error message makes that explicit instead of silently mis-targeting the control plane. |
| T-04-18-03  | Elevation of Privilege | Accidentally tainting/labelling control-plane | mitigate    | Selector `!node-role.kubernetes.io/control-plane` excludes control-plane nodes at the API layer. Even if the first-worker heuristic picks a surprising node, it is guaranteed not to be control-plane, so the taint cannot accidentally evict system pods from control-plane. |
| T-04-18-04  | Information Disclosure | Leaking node name in failure messages         | accept      | Node names are non-sensitive cluster-topology metadata; exposing them in grader error messages mirrors pre-state (which exposed `node-02` the same way). No new disclosure surface introduced. |
</threat_model>

<verification>
Static automated verification (grep + bash -n + lint-packs + test.sh) fully covers this gap:
- BUG-3 evidence (`results.txt` line 493 `nodes "node-02" not found`) is the symptom of the hardcode; once the hardcode is gone, setup runs against a real worker and the drill can proceed.
- Assertions 3 + 4 in grade.sh previously compared `unique_nodes == "node-02"` — an always-false match on any cluster where the worker is not literally named `node-02`. Post-fix, they compare against `$target_node`, which tracks whatever setup tainted.
- question.md previously instructed candidates to label a nonexistent node; post-fix it shows the discovery command so candidates know which worker to target on any cluster topology.

Live handoff: MH-5 live round-trip for workloads q08 becomes runnable on the user's 1+2 cluster. Verified together with 04-17 (BUG-1) during the per-question round-trip checklist in 04-VERIFICATION.md `human_verification` section 2.
</verification>

<success_criteria>
- Zero `node-02` literals in any of the 5 workloads/08 files (setup/reset/ref-solution/grade/question.md)
- All 4 scripts use the identical discovery command so they target the same worker within a drill
- `bash cka-sim/scripts/test.sh` exits 0
- `bash cka-sim/scripts/lint-packs.sh` exits 0 (all 5 passes including GRADE-02 read-only grader guard)
- `bash cka-sim/scripts/lint-coverage.sh` exits 0
- BUG-3 row in 04-VERIFICATION.md `gaps_remaining` can be closed by re-verification
</success_criteria>

<output>
After completion, create `.planning/phases/04-storage-workloads-scheduling-packs/04-18-SUMMARY.md` covering:
- BUG-3 closed: 5 files (setup/reset/ref-solution/grade/question.md) migrated from literal `node-02` to the shared dynamic-discovery idiom
- Discovery idiom: `kubectl get nodes -l '!node-role.kubernetes.io/control-plane' --no-headers -o jsonpath='{.items[0].metadata.name}'` — identical across all four scripts, so a single drill invocation targets one consistent worker
- Commit SHA; `grep -rn 'node-02' cka-sim/packs/workloads-scheduling/08-*` prints nothing
- Note: VERIFICATION.md fix field under-specified (named 3 files; 2 more — grade.sh + question.md — also needed the migration or the grader would always fail its placement+label assertions)
- `bash cka-sim/scripts/test.sh` exit 0 at HEAD
- Live-drill re-verification next step (paired with 04-17) — run `cka-sim drill workloads-scheduling --question 08` on the 1+2 cluster to close MH-5
</output>
