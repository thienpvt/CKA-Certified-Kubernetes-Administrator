---
phase: 04-storage-workloads-scheduling-packs
plan: 13
type: execute
wave: 3
depends_on: [01, 02, 05]
files_modified:
  - cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/setup.sh
  - cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/grade.sh
  - cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/reset.sh
  - cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/ref-solution.sh
  - cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/metadata.yaml
  - cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/question.md
  - cka-sim/tests/fixtures/workloads-04-hpa-metrics-server/stub-responses.json
  - cka-sim/tests/fixtures/workloads-04-hpa-metrics-server/expected-fail-score.txt
  - cka-sim/tests/fixtures/workloads-04-hpa-metrics-server/expected-pass-score.txt
autonomous: true
requirements: [PACK-02, PACK-06]
must_haves:
  truths:
    - "6 files chmod +x + 3 fixtures"
    - "setup seeds Deployment q04-load with CPU + memory requests + SA q04-load-sa; does NOT install metrics-server (that is the candidate's job)"
    - "grade asserts HPA q04-load exists + minReplicas=1 + maxReplicas=5 + resource.name=cpu + kubectl top pod exits 0 (metrics-server alive)"
    - "ref-solution.sh installs metrics-server pinned v0.7.2 with --kubelet-insecure-tls patch + creates HPA q04-load"
    - "reset.sh deletes the question's ns + HPA only; DOES NOT uninstall metrics-server (cluster-wide shared tool)"
    - "metadata traps=[hpa-missing-metrics-server, deployment-missing-requests, default-sa-used]"
  artifacts:
    - path: "cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/setup.sh"
      provides: "Seeds Deployment with requests; does not pre-install metrics-server"
      contains: "q04-load"
    - path: "cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/ref-solution.sh"
      provides: "Installs metrics-server v0.7.2 + HPA v2"
      contains: "v0.7.2"
      contains_extra: "--kubelet-insecure-tls"
  key_links:
    - from: "cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/ref-solution.sh"
      to: "kubernetes-sigs/metrics-server v0.7.2 components.yaml"
      via: "kubectl apply + kubectl patch --kubelet-insecure-tls"
      pattern: "metrics-server/releases/download/v0.7.2"
---

<objective>
Ship Workloads Q04 — HPA autoscaling/v2 + CG-06 metrics-server bootstrap (Tracker: `hpa-autoscaling-v2`). Per RESEARCH §2.2 Q04 + §6.2: setup seeds a Deployment with CPU + memory requests but NO metrics-server. The candidate installs metrics-server (with `--kubelet-insecure-tls` for kubeadm clusters) and creates an HPA v2 that scales 1→5 replicas at 50% CPU.

Reset does NOT uninstall metrics-server — it is a cluster-wide tool other questions benefit from (RESEARCH §6.2 lines 570-573).

Output: 6 question files + 3 fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-storage-workloads-scheduling-packs/04-RESEARCH.md
@cka-sim/lib/setup.sh
@cka-sim/lib/grade.sh
@cka-sim/traps/catalog.yaml
</context>

<interfaces>
Pinned upstream URL (RESEARCH §6.2): `https://github.com/kubernetes-sigs/metrics-server/releases/download/v0.7.2/components.yaml`.
kubeadm TLS workaround: `kubectl patch deployment metrics-server -n kube-system --type=json -p='[{"op":"add","path":"/spec/template/spec/containers/0/args/-","value":"--kubelet-insecure-tls"}]'`.

CKA_SIM_LAB_NS = `cka-sim-workloads-scheduling-04`; prefix `q04-`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 6 question files + 3 fixtures</name>
  <files>cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/metadata.yaml, cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/question.md, cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/setup.sh, cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/grade.sh, cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/reset.sh, cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/ref-solution.sh, cka-sim/tests/fixtures/workloads-04-hpa-metrics-server/stub-responses.json, cka-sim/tests/fixtures/workloads-04-hpa-metrics-server/expected-fail-score.txt, cka-sim/tests/fixtures/workloads-04-hpa-metrics-server/expected-pass-score.txt</files>
  <read_first>
    - .planning/phases/04-storage-workloads-scheduling-packs/04-RESEARCH.md (§2.2 Q04 lines 107-119 + §6.2 lines 549-573)
    - cka-sim/packs/workloads-scheduling/01-deployment-requests/ref-solution.sh (SA + requests pattern)
    - cka-sim/lib/setup.sh (seed_deployment helper supports --cpu --memory --sa)
  </read_first>
  <action>
    `mkdir -p cka-sim/packs/workloads-scheduling/04-hpa-metrics-server cka-sim/tests/fixtures/workloads-04-hpa-metrics-server`.

    **metadata.yaml**:
    ```
    id: workloads-hpa-metrics-server
    domain: workloads-scheduling
    estimatedMinutes: 9
    verified_against: "1.35"
    traps:
      - hpa-missing-metrics-server
      - deployment-missing-requests
      - default-sa-used
    references:
      - kind: concerns-md
        target: .planning/codebase/CONCERNS.md
        note: "CG-06 metrics-server bootstrap prerequisite"
      - kind: k8s-doc
        target: "https://github.com/kubernetes-sigs/metrics-server"
        note: metrics-server upstream manifest (v0.7.2 pinned)
      - kind: k8s-doc
        target: "https://kubernetes.io/docs/tasks/run-application/horizontal-pod-autoscale/"
        note: HPA v2 concepts
    ```

    **question.md** (no `--kubelet-insecure-tls` spoiler — that's the whole point):
    ```
    # workloads-scheduling/04-hpa-metrics-server

    **Domain:** Workloads & Scheduling  |  **Estimated time:** 9 minutes

    A Deployment `q04-load` exists in `${CKA_SIM_LAB_NS}` with CPU and memory requests already set. Platform ops wants it autoscaled. `kubectl top pod` currently fails on this cluster.

    ## Tasks

    1. Make `kubectl top pod -n ${CKA_SIM_LAB_NS} -l app=q04-load` return readings.
    2. Create a HorizontalPodAutoscaler `q04-load` targeting Deployment `q04-load`:
       - `minReplicas: 1`
       - `maxReplicas: 5`
       - CPU target: `averageUtilization: 50`
       - `apiVersion: autoscaling/v2`

    ## Constraints

    - Do NOT modify `q04-load` Deployment's resources.requests.
    - Installing metrics-server on a kubeadm cluster with self-signed kubelet certs requires extra flags — consult its upstream README.

    ## Verify yourself

    ```
    kubectl top pod -n ${CKA_SIM_LAB_NS} -l app=q04-load
    kubectl get hpa q04-load -n ${CKA_SIM_LAB_NS}
    ```
    ```

    **setup.sh** (chmod +x):
    ```
    #!/bin/bash
    set -euo pipefail
    : "${CKA_SIM_LAB_NS:?}"
    : "${CKA_SIM_ROOT:?}"
    source "$CKA_SIM_ROOT/lib/setup.sh"

    cka_sim::setup::ensure_lab_ns "$CKA_SIM_LAB_NS" workloads-scheduling workloads-hpa-metrics-server
    cka_sim::setup::wait_for_ns_active "$CKA_SIM_LAB_NS" workloads-scheduling workloads-hpa-metrics-server 120

    kubectl apply -f - <<EOF
    apiVersion: v1
    kind: ServiceAccount
    metadata:
      name: q04-load-sa
      namespace: ${CKA_SIM_LAB_NS}
    EOF

    # Deployment with CPU + memory requests (NOT missing; the trap here is metrics-server).
    cka_sim::setup::seed_deployment "$CKA_SIM_LAB_NS" q04-load nginx:1.27 --replicas 1 --sa q04-load-sa --cpu 100m --memory 64Mi

    kubectl wait --for=condition=Available deployment/q04-load -n "$CKA_SIM_LAB_NS" --timeout=60s 2>/dev/null || true
    ```

    **grade.sh** (chmod +x):
    ```
    #!/bin/bash
    set -uo pipefail
    : "${CKA_SIM_LAB_NS:?}"
    : "${CKA_SIM_ROOT:?}"
    source "$CKA_SIM_ROOT/lib/grade.sh"
    source "$CKA_SIM_ROOT/lib/traps.sh"

    cka_sim::grade::assert_resource_exists hpa q04-load -n "$CKA_SIM_LAB_NS"
    cka_sim::grade::assert_field_eq hpa q04-load '{.spec.minReplicas}' '1' -n "$CKA_SIM_LAB_NS"
    cka_sim::grade::assert_field_eq hpa q04-load '{.spec.maxReplicas}' '5' -n "$CKA_SIM_LAB_NS"
    cka_sim::grade::assert_field_eq hpa q04-load \
      '{.spec.metrics[?(@.type=="Resource")].resource.name}' 'cpu' -n "$CKA_SIM_LAB_NS"

    # Behavioural: kubectl top pod must succeed (metrics-server serving).
    CKA_SIM_GRADE_TOTAL=$(( CKA_SIM_GRADE_TOTAL + 1 ))
    if kubectl top pod -n "$CKA_SIM_LAB_NS" -l app=q04-load >/dev/null 2>&1; then
      CKA_SIM_GRADE_PASSED=$(( CKA_SIM_GRADE_PASSED + 1 ))
      ok "kubectl top pod returns readings (metrics-server alive)"
    else
      err "kubectl top pod failed (metrics-server unreachable or not installed)"
    fi

    # Trap: HPA exists but AbleToScale=False with FailedGetResourceMetric.
    reason=$(kubectl get hpa q04-load -n "$CKA_SIM_LAB_NS" \
      -o jsonpath='{.status.conditions[?(@.type=="AbleToScale")].reason}' 2>/dev/null || echo "")
    if [[ "$reason" == "FailedGetResourceMetric" ]]; then
      cka_sim::grade::record_trap hpa-missing-metrics-server
    fi

    cka_sim::grade::emit_result
    ```

    **reset.sh** (chmod +x) — **does NOT uninstall metrics-server** per RESEARCH §6.2:
    ```
    #!/bin/bash
    set -uo pipefail
    : "${CKA_SIM_LAB_NS:?}"
    kubectl delete namespace "$CKA_SIM_LAB_NS" --ignore-not-found --wait=false
    # Intentionally keep metrics-server installed -- other questions (and the candidate) benefit from it.
    exit 0
    ```

    **ref-solution.sh** (chmod +x):
    ```
    #!/bin/bash
    set -euo pipefail
    : "${CKA_SIM_LAB_NS:?}"

    if ! kubectl get deployment metrics-server -n kube-system >/dev/null 2>&1; then
      kubectl apply -f https://github.com/kubernetes-sigs/metrics-server/releases/download/v0.7.2/components.yaml
      # kubeadm self-signed kubelet cert workaround.
      kubectl patch deployment metrics-server -n kube-system --type=json -p='[
        {"op": "add", "path": "/spec/template/spec/containers/0/args/-", "value": "--kubelet-insecure-tls"}
      ]'
    fi
    kubectl wait --for=condition=Available deployment/metrics-server -n kube-system --timeout=180s

    # Give metrics-server a moment to scrape at least one cycle.
    sleep 15

    kubectl apply -f - <<EOF
    apiVersion: autoscaling/v2
    kind: HorizontalPodAutoscaler
    metadata:
      name: q04-load
      namespace: ${CKA_SIM_LAB_NS}
    spec:
      scaleTargetRef:
        apiVersion: apps/v1
        kind: Deployment
        name: q04-load
      minReplicas: 1
      maxReplicas: 5
      metrics:
        - type: Resource
          resource:
            name: cpu
            target:
              type: Utilization
              averageUtilization: 50
    EOF
    ```

    **Fixtures**:
    - `stub-responses.json`: `{"kind":"HorizontalPodAutoscaler","metadata":{"name":"q04-load"},"spec":{"minReplicas":1,"maxReplicas":5,"metrics":[{"type":"Resource","resource":{"name":"cpu"}}]}}`
    - `expected-fail-score.txt`: `SCORE: 0/5`
    - `expected-pass-score.txt`: `SCORE: 5/5`

    chmod +x all .sh.
  </action>
  <verify>
    <automated>chmod +x cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/*.sh && for f in cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/*.sh; do bash -n "$f" || exit 1; done && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - 6 files present
    - ref-solution pins metrics-server v0.7.2: `grep -q 'metrics-server/releases/download/v0.7.2' cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/ref-solution.sh`
    - ref-solution patches --kubelet-insecure-tls: `grep -q 'kubelet-insecure-tls' cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/ref-solution.sh`
    - ref-solution creates HPA v2: `grep -q 'apiVersion: autoscaling/v2' cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/ref-solution.sh`
    - reset does NOT uninstall metrics-server: `! grep -q 'metrics-server' cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/reset.sh`
    - grade uses kubectl top behavioural probe + no get|grep: `grep -q 'kubectl top pod' cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/grade.sh && ! grep -qE 'kubectl[[:space:]]+get[[:space:]].*\|[[:space:]]*grep' cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/grade.sh`
    - No mutating verbs in grade
    - test.sh green
    - Metadata normalized (W2 block):
      - `grep -q '^id:' cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/metadata.yaml`
      - `grep -qE '^domain: (storage|workloads-scheduling)$' cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/metadata.yaml`
      - `grep -qE '^estimatedMinutes: [4-9]$|^estimatedMinutes: 1[0-2]$' cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/metadata.yaml`
      - `grep -q 'verified_against: "1.35"' cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/metadata.yaml`
      - `python3 -c "import yaml; m=yaml.safe_load(open('cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/metadata.yaml')); assert len(m.get('traps', [])) >= 3"`
      - `grep -qE '^references:' cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/metadata.yaml && grep -qE '^[[:space:]]+- kind:' cka-sim/packs/workloads-scheduling/04-hpa-metrics-server/metadata.yaml`
  </acceptance_criteria>
  <done>HPA + metrics-server question ships.</done>
</task>

</tasks>

<verification>
- metrics-server install is idempotent; --kubelet-insecure-tls patch present; HPA v2 only
- reset keeps metrics-server installed (RESEARCH §6.2 policy)
</verification>

<success_criteria>
- Tracker slug `hpa-autoscaling-v2` covered; CG-06 metrics-server bootstrap closed
</success_criteria>

<output>
`04-13-SUMMARY.md`: install gating + reset-scope note.
</output>
