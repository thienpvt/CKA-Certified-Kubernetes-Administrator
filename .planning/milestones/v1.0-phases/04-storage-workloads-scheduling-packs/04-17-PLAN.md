---
phase: 04-storage-workloads-scheduling-packs
plan: 17
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - cka-sim/packs/storage/04-csi-volumesnapshot/setup.sh
  - cka-sim/packs/storage/04-csi-volumesnapshot/grade.sh
  - cka-sim/packs/storage/04-csi-volumesnapshot/reset.sh
  - cka-sim/packs/storage/04-csi-volumesnapshot/ref-solution.sh
autonomous: true
requirements: [PACK-01]
must_haves:
  truths:
    - "all four scripts in storage/04-csi-volumesnapshot are committed as 100755 in the git index (matches every other pack dir)"
    - "`cka-sim drill storage --question 04` no longer emits `setup.sh not executable` (fixing MH-5 live-drill BUG-1 from 04-VERIFICATION.md)"
  artifacts:
    - path: "cka-sim/packs/storage/04-csi-volumesnapshot/setup.sh"
      provides: "Executable setup script after chmod+x"
      mode: "100755"
    - path: "cka-sim/packs/storage/04-csi-volumesnapshot/grade.sh"
      provides: "Executable grade script after chmod+x"
      mode: "100755"
    - path: "cka-sim/packs/storage/04-csi-volumesnapshot/reset.sh"
      provides: "Executable reset script after chmod+x"
      mode: "100755"
    - path: "cka-sim/packs/storage/04-csi-volumesnapshot/ref-solution.sh"
      provides: "Executable ref-solution after chmod+x"
      mode: "100755"
  key_links:
    - from: "git index"
      to: "script exec bit"
      via: "git update-index --chmod=+x"
      pattern: "100755"
---

<objective>
Close BUG-1 (04-VERIFICATION.md gaps section, severity critical, must_have MH-5): all four .sh files under `cka-sim/packs/storage/04-csi-volumesnapshot/` are committed as `100644` in the git index while every other pack script is `100755`. The live drill on 2026-05-11 failed with `setup.sh not executable` (cka-sim/results.txt line 157). Windows git dropped the exec bit during the octopus merges even though Plan 04-08 originally committed the file as 100755.

Purpose: Unblock MH-5 live round-trip for storage question 04 (CG-01 CSI/VolumeSnapshot), closing one of the two gaps in `re_verification.gaps_remaining`.
Output: All four scripts land as `100755` in the git index; chmod-only commit with zero content drift.

Confirmed pre-state (verified via `git ls-files -s cka-sim/packs/storage/04-csi-volumesnapshot/`):
  100644 ... grade.sh
  100644 ... metadata.yaml  (not modified by this plan - yaml files stay 100644)
  100644 ... question.md    (not modified by this plan)
  100644 ... ref-solution.sh
  100644 ... reset.sh
  100644 ... setup.sh
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-storage-workloads-scheduling-packs/04-VERIFICATION.md
@.planning/phases/04-storage-workloads-scheduling-packs/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set exec bit on all four storage/04-csi-volumesnapshot scripts via git index</name>
  <files>
    cka-sim/packs/storage/04-csi-volumesnapshot/setup.sh,
    cka-sim/packs/storage/04-csi-volumesnapshot/grade.sh,
    cka-sim/packs/storage/04-csi-volumesnapshot/reset.sh,
    cka-sim/packs/storage/04-csi-volumesnapshot/ref-solution.sh
  </files>
  <read_first>
    - Pre-state: run `git ls-files -s cka-sim/packs/storage/04-csi-volumesnapshot/` — confirm all four .sh entries show mode `100644` (the bug). The metadata.yaml + question.md files stay `100644` and MUST NOT be touched.
    - Reference mode: run `git ls-files -s cka-sim/packs/storage/03-access-modes-reclaim/` — confirm all four .sh entries show `100755` (target mode for the fix).
  </read_first>
  <action>
    Run exactly these four `git update-index --chmod=+x` commands (order does not matter; they operate on the git index, not the filesystem):

    ```
    git update-index --chmod=+x cka-sim/packs/storage/04-csi-volumesnapshot/setup.sh
    git update-index --chmod=+x cka-sim/packs/storage/04-csi-volumesnapshot/grade.sh
    git update-index --chmod=+x cka-sim/packs/storage/04-csi-volumesnapshot/reset.sh
    git update-index --chmod=+x cka-sim/packs/storage/04-csi-volumesnapshot/ref-solution.sh
    ```

    Implementation notes:
    - Use `git update-index --chmod=+x` (NOT `chmod +x` on disk) because Windows git (user's dev environment) typically cannot set the exec bit on NTFS; the index bit is the source of truth for the checked-in mode.
    - No content change. `git status` after running will show `modified:` on all four files; `git diff --cached` will show `old mode 100644 / new mode 100755` hunks ONLY, zero textual diff.
    - Do NOT add `--add`, `--assume-unchanged`, `--skip-worktree`, or any other flag. Do NOT run filesystem `chmod` (no-op on NTFS, confuses the diff on Linux checkouts).
    - Do NOT touch metadata.yaml or question.md — those stay `100644` (correct mode for data files).
    - Commit with this exact message via the repo's one-shot identity shim (per `.claude/projects/.../MEMORY.md`):
      ```
      git -c user.email=pvtcwd@gmail.com -c user.name=thienpv commit -m "fix(04-gap-bug1): restore exec bit on storage/04-csi-volumesnapshot scripts

      BUG-1 from 04-VERIFICATION.md: Windows git dropped the exec bit on all
      four .sh files during the Phase 4 octopus merges. Every other pack dir
      (storage/01..03,05,06 + workloads-scheduling/01..08) has its .sh files
      as 100755 in the index; only storage/04 regressed to 100644.

      Fix: git update-index --chmod=+x on setup/grade/reset/ref-solution.
      Zero content change; chmod-only commit.

      Evidence: cka-sim/results.txt line 157 (live-drill 2026-05-11T11:40Z):
        ✗ /root/CKA-Certified-Kubernetes-Administrator/cka-sim/packs/storage/04-csi-volumesnapshot/setup.sh not executable

      Closes MH-5 BUG-1."
      ```
  </action>
  <verify>
    <automated>bash -c 'modes=$(git ls-files -s cka-sim/packs/storage/04-csi-volumesnapshot/setup.sh cka-sim/packs/storage/04-csi-volumesnapshot/grade.sh cka-sim/packs/storage/04-csi-volumesnapshot/reset.sh cka-sim/packs/storage/04-csi-volumesnapshot/ref-solution.sh | awk "{print \$1}"); count=$(echo "$modes" | grep -c "^100755"); test "$count" = "4" || { echo "FAIL: expected 4x 100755 entries, got: $modes"; exit 1; }; echo "OK: all four scripts are 100755 in the index"' && bash cka-sim/scripts/lint-packs.sh && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files -s cka-sim/packs/storage/04-csi-volumesnapshot/setup.sh | awk '{print $1}'` prints exactly `100755`
    - Same for `grade.sh`, `reset.sh`, `ref-solution.sh` — all four emit `100755`
    - No content drift: `git show HEAD --stat` lists the four files with `0 insertions(+), 0 deletions(-)` and only a mode change hunk
    - `git ls-files -s cka-sim/packs/storage/04-csi-volumesnapshot/metadata.yaml` still prints `100644` (data file NOT accidentally chmodded)
    - `git ls-files -s cka-sim/packs/storage/04-csi-volumesnapshot/question.md` still prints `100644`
    - `bash cka-sim/scripts/lint-packs.sh` exits `0` (pass D exec-bit lint stays green for all packs)
    - `bash cka-sim/scripts/test.sh` exits `0` (full chain: lint-traps + lint-packs + lint-coverage + unit cases + setup_helpers cases)
  </acceptance_criteria>
  <done>Four scripts committed as 100755; metadata + question stay 100644; full automated chain green; BUG-1 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary introduced. Change is an in-repo git index mode adjustment; no network, no external input, no new capability surface.

## STRIDE Threat Register

| Threat ID    | Category               | Component            | Disposition | Mitigation Plan                                                                                                                                                             |
|--------------|------------------------|----------------------|-------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| T-04-17-01   | Tampering              | git index mode       | accept      | Change is under version control; `git diff --cached` surfaces the mode-change hunk explicitly and the commit requires the author's identity. No third-party input crosses a boundary. |
| T-04-17-02   | Elevation of Privilege | storage/04 scripts   | accept      | Scripts already had exec intent in the original Phase 4 author commit (04-08); every sibling pack dir is 100755. Restoring the exec bit re-asserts authored intent, not new capability. |
| T-04-17-03   | Tampering              | sibling data files   | mitigate    | Task explicitly scopes `git update-index --chmod=+x` to the four .sh files; metadata.yaml + question.md stay 100644. Acceptance criteria grep-verify both data files remain 100644. |
</threat_model>

<verification>
Static automated. BUG-1 is an index-mode regression — once the four entries read `100755` and `lint-packs.sh` + `test.sh` stay green, the runtime `setup.sh not executable` check in the drill runner no longer trips.

Post-merge live handoff: user re-runs `cka-sim drill storage --question 04` on the 1+2 cluster as part of MH-5 live round-trip (paired with BUG-3 fix in 04-18). That full-pack round-trip is the user-action row in 04-VERIFICATION.md `human_verification` section 1.
</verification>

<success_criteria>
- All four storage/04-csi-volumesnapshot scripts committed as `100755` in the git index
- `bash cka-sim/scripts/test.sh` exits `0`
- Git log shows a single chmod-only commit (no content drift)
- BUG-1 row in 04-VERIFICATION.md `gaps_remaining` can be closed by re-verification
</success_criteria>

<output>
After completion, create `.planning/phases/04-storage-workloads-scheduling-packs/04-17-SUMMARY.md` covering:
- BUG-1 closed: four storage/04-csi-volumesnapshot scripts now 100755
- Commit SHA (chmod-only, zero content drift) + `git ls-files -s` evidence (4x 100755)
- `bash cka-sim/scripts/test.sh` exit code 0 at HEAD
- metadata.yaml + question.md mode unchanged (100644) — confirmed
- Ready for live-drill re-verification of MH-5 (paired with 04-18 BUG-3 fix)
</output>
