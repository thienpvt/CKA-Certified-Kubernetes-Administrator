---
phase: 03-runtime-contract-drill-mode
plan: 07
type: execute
wave: 3
depends_on: [01, 03]
files_modified:
  - cka-sim/packs/cluster-architecture/manifest.yaml
  - cka-sim/packs/cluster-architecture/README.md
  - cka-sim/packs/cluster-architecture/01-rbac-viewer/metadata.yaml
  - cka-sim/packs/cluster-architecture/01-rbac-viewer/question.md
  - cka-sim/packs/cluster-architecture/01-rbac-viewer/setup.sh
  - cka-sim/packs/cluster-architecture/01-rbac-viewer/grade.sh
  - cka-sim/packs/cluster-architecture/01-rbac-viewer/reset.sh
  - cka-sim/packs/cluster-architecture/01-rbac-viewer/ref-solution.sh
autonomous: true
requirements: [TRIP-01, TRIP-02, TRIP-03, TRIP-04, TRIP-05, TRIP-06, GRADE-02, GRADE-03, GRADE-04, GRADE-06, RUN-02]
must_haves:
  truths:
    - "cluster-architecture pack has manifest + README + 1 question dir with all 6 files"
    - "setup.sh creates ns + Role + RoleBinding that WILL NOT grant the candidate's ServiceAccount the 'get pods' verb (the trap — wrong verbs OR wrong subject binding)"
    - "grade.sh asserts Role exists + RoleBinding exists + `kubectl auth can-i get pods` returns 'yes' for the target SA (the fix) + records rbac-viewer-role-mismatch if detector fires"
    - "ref-solution.sh patches the Role's verbs (or RoleBinding's subjects) so can-i returns yes"
    - "metadata.yaml declares rbac-viewer-role-mismatch (the new catalog entry from Plan 01 — NOT as-flag-format-wrong)"
    - "D-10-revision: trap for cluster-architecture/01 is rbac-viewer-role-mismatch (not the text-based as-flag-format-wrong)"
  artifacts:
    - path: "cka-sim/packs/cluster-architecture/01-rbac-viewer/setup.sh"
      provides: "ns + SA + Role (intentionally wrong verbs) + RoleBinding — trap: role-mismatch"
      contains: "kind: Role"
    - path: "cka-sim/packs/cluster-architecture/01-rbac-viewer/grade.sh"
      provides: "asserts can-i get pods == yes for the target SA; detects rbac-viewer-role-mismatch"
      contains: "cka_sim::grade::assert_can_i"
    - path: "cka-sim/packs/cluster-architecture/01-rbac-viewer/ref-solution.sh"
      provides: "patches the Role to add 'get,list' on pods"
      contains: "kubectl patch role"
  key_links:
    - from: "cka-sim/packs/cluster-architecture/01-rbac-viewer/grade.sh"
      to: "cka-sim/lib/grade.sh + cka-sim/lib/traps.sh"
      via: "source + assert_can_i + detect_rbac_viewer_role_mismatch"
      pattern: "source.*lib/(grade|traps).sh"
---

<objective>
Ship cluster-architecture pack with 1 reference question (01-rbac-viewer). Per user override #1 (D-10-revision): the seeded `as-flag-format-wrong` trap is text-pattern based and cannot fire from cluster state alone; this question uses the new catalog entry `rbac-viewer-role-mismatch` (added in Plan 01) — a state-detectable trap that fires when a Role binds a viewer SA but lacks `get`/`list` verbs on pods.

Trap mechanic: setup.sh creates a Role with `verbs: ["watch"]` only (missing `get`/`list`). Candidate must patch the Role to include `get`/`list`. `kubectl auth can-i get pods --as=system:serviceaccount:${ns}:viewer` returns `no` under the trap, `yes` under the fix.

Output: 8 new files (2 pack-level + 6 question).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-runtime-contract-drill-mode/03-CONTEXT.md
@.planning/phases/03-runtime-contract-drill-mode/03-RESEARCH.md
@.planning/phases/03-runtime-contract-drill-mode/03-PATTERNS.md
@cka-sim/lib/grade.sh
@cka-sim/lib/traps.sh
@cka-sim/scripts/lint-packs.sh
@cka-sim/traps/catalog.yaml
</context>

<interfaces>
From cka-sim/lib/grade.sh:
- cka_sim::grade::assert_can_i <verb> <resource> --as=<subject> [-n <ns>] [<weight>] — passes iff kubectl auth can-i ... returns "yes"
- cka_sim::grade::assert_resource_exists <kind> <name> [-n <ns>]
- cka_sim::grade::record_trap <id>
- cka_sim::grade::emit_result

From cka-sim/lib/traps.sh (post-Plan-01):
- cka_sim::trap::detect_rbac_viewer_role_mismatch <ns> <role-name> — echoes "rbac-viewer-role-mismatch" iff Role has rules for pods but missing get/list verbs. (NEW detector added by Plan 01 alongside the catalog entry.)

Env-vars drill.sh sets:
- CKA_SIM_LAB_NS = "cka-sim-cluster-architecture-01"
- CKA_SIM_ROOT
- CKA_SIM_QUESTION_ID = "cluster-architecture-rbac-viewer"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create cluster-architecture pack scaffold</name>
  <files>cka-sim/packs/cluster-architecture/manifest.yaml, cka-sim/packs/cluster-architecture/README.md, cka-sim/packs/cluster-architecture/01-rbac-viewer/metadata.yaml, cka-sim/packs/cluster-architecture/01-rbac-viewer/question.md</files>
  <read_first>
    - .planning/phases/03-runtime-contract-drill-mode/03-CONTEXT.md (D-10 cluster-architecture row + user override #1 D-10-revision note in 03-01-PLAN.md)
    - .planning/phases/03-runtime-contract-drill-mode/03-PATTERNS.md (lines 284-352 manifest/metadata schemas, 553-597 question.md skeleton)
    - cka-sim/traps/catalog.yaml (post-Plan-01: confirm rbac-viewer-role-mismatch exists)
    - cka-sim/scripts/lint-packs.sh (post-Plan-03: schema lint rules)
  </read_first>
  <action>
    `mkdir -p cka-sim/packs/cluster-architecture/01-rbac-viewer`.

    File 1 — `cka-sim/packs/cluster-architecture/manifest.yaml`:
    ```
    pack:
      id: cluster-architecture
      domain: cluster-architecture
      weight: 25
      description: "Cluster Architecture 25% pack — Phase 3 reference question only; full pack in Phase 5 (PACK-04)."
    questions:
      - id: cluster-architecture-rbac-viewer
        path: 01-rbac-viewer
        estimatedMinutes: 8
    ```

    File 2 — `cka-sim/packs/cluster-architecture/README.md`:
    ```
    # Cluster Architecture Pack

    **Domain:** Cluster Architecture, Installation & Configuration (25% of CKA blueprint v1.35)

    Phase 3 reference question only — full pack lands in Phase 5 (PACK-04).

    ## Questions

    | # | Slug | Topic | Time |
    |---|------|-------|------|
    | 01 | rbac-viewer | Role for a viewer ServiceAccount lacks required pod-read verbs | 8 min |

    ## Running

    ```bash
    cka-sim drill cluster-architecture
    cka-sim drill cluster-architecture 1
    ```

    > Not real CKA exam content; independently authored.
    ```

    File 3 — `cka-sim/packs/cluster-architecture/01-rbac-viewer/metadata.yaml`:
    ```
    id: cluster-architecture-rbac-viewer
    domain: cluster-architecture
    estimatedMinutes: 8
    verified_against: "1.35"
    traps:
      - rbac-viewer-role-mismatch
      - default-sa-used
      - missing-dns-egress
    references:
      - kind: prior-art-exercise
        target: exercises/04-rbac/
        note: Prior RBAC exercise
      - kind: k8s-doc
        target: https://kubernetes.io/docs/reference/access-authn-authz/rbac/
        note: RBAC reference
    ```
    NOTE: filler trap ids (default-sa-used, missing-dns-egress) are seeded — required to hit ≥3. Only rbac-viewer-role-mismatch is actively detected.

    File 4 — `cka-sim/packs/cluster-architecture/01-rbac-viewer/question.md`:
    ```
    # cluster-architecture/01-rbac-viewer

    **Domain:** Cluster Architecture  |  **Estimated time:** 8 minutes

    A `ServiceAccount` named `viewer` exists in your lab namespace, along with a `Role` named `pod-viewer` and a `RoleBinding` named `viewer-binding`. The intent is that `viewer` can read Pods in the namespace, but it cannot.

    ## Tasks

    1. Inspect the Role `pod-viewer`, the RoleBinding `viewer-binding`, and the ServiceAccount `viewer` in `${CKA_SIM_LAB_NS}`.
    2. Diagnose why the `viewer` ServiceAccount cannot read Pods. Check what the Role actually permits.
    3. Modify the resources so that `kubectl auth can-i get pods --as=system:serviceaccount:${CKA_SIM_LAB_NS}:viewer -n ${CKA_SIM_LAB_NS}` returns `yes`.

    ## Constraints

    - Do NOT delete or recreate the Role or RoleBinding — modify in place.
    - Do NOT grant cluster-wide permissions (ClusterRole/ClusterRoleBinding).
    - The Role must still be scoped to Pods only (no wildcards, no other resources).

    ## Verify yourself

    Before typing `done`, confirm:

    ```
    kubectl auth can-i get pods -n ${CKA_SIM_LAB_NS} \
      --as=system:serviceaccount:${CKA_SIM_LAB_NS}:viewer
    # Should print: yes
    ```
    ```
  </action>
  <verify>
    <automated>[[ -f cka-sim/packs/cluster-architecture/manifest.yaml && -f cka-sim/packs/cluster-architecture/01-rbac-viewer/metadata.yaml ]] && grep -q 'rbac-viewer-role-mismatch' cka-sim/packs/cluster-architecture/01-rbac-viewer/metadata.yaml && ! grep -q 'as-flag-format-wrong' cka-sim/packs/cluster-architecture/01-rbac-viewer/metadata.yaml</automated>
  </verify>
  <acceptance_criteria>
    - 4 files created
    - manifest.yaml weight 25
    - metadata.yaml has exactly 3 traps; first trap id is `rbac-viewer-role-mismatch` (D-10-revision)
    - metadata.yaml does NOT reference `as-flag-format-wrong` anywhere: `! grep -q 'as-flag-format-wrong' cka-sim/packs/cluster-architecture/01-rbac-viewer/metadata.yaml`
    - question.md does NOT spoiler the trap (no mention of `get`, `list`, `verbs`): `! grep -qiE '(get.pods|list.pods|verbs:|rules:)' cka-sim/packs/cluster-architecture/01-rbac-viewer/question.md`
    - question.md includes the target can-i command with `--as=system:serviceaccount:` form
  </acceptance_criteria>
  <done>Pack scaffold complete.</done>
</task>

<task type="auto">
  <name>Task 2: Create setup, grade, reset, ref-solution for cluster-architecture/01-rbac-viewer</name>
  <files>cka-sim/packs/cluster-architecture/01-rbac-viewer/setup.sh, cka-sim/packs/cluster-architecture/01-rbac-viewer/grade.sh, cka-sim/packs/cluster-architecture/01-rbac-viewer/reset.sh, cka-sim/packs/cluster-architecture/01-rbac-viewer/ref-solution.sh</files>
  <read_first>
    - .planning/phases/03-runtime-contract-drill-mode/03-RESEARCH.md (Pattern 4 lines 297-311 for setup.sh shape; Pitfall 3 bash-n check)
    - .planning/phases/03-runtime-contract-drill-mode/03-PATTERNS.md (lines 422-509 setup/reset/ref-solution patterns; lines 355-419 grade.sh pattern)
    - cka-sim/lib/grade.sh (assert_can_i signature + accumulator state)
    - cka-sim/lib/traps.sh (post-Plan-01: detect_rbac_viewer_role_mismatch signature)
    - cka-sim/packs/storage/01-pvc-binding/setup.sh (pattern reference — ns wait loop)
  </read_first>
  <action>
    Create 4 scripts, all `chmod +x`.

    File 1 — `cka-sim/packs/cluster-architecture/01-rbac-viewer/setup.sh`:
    ```
    #!/bin/bash
    # cluster-architecture/01-rbac-viewer/setup.sh — Role intentionally missing get/list verbs (the trap).
    set -euo pipefail
    : "${CKA_SIM_LAB_NS:?CKA_SIM_LAB_NS must be set}"

    # 1. ns + Active wait
    kubectl apply -f - <<EOF
    apiVersion: v1
    kind: Namespace
    metadata:
      name: ${CKA_SIM_LAB_NS}
      labels:
        cka-sim/pack: cluster-architecture
        cka-sim/question-id: cluster-architecture-rbac-viewer
    EOF
    phase=""
    for i in $(seq 1 10); do
      phase=$(kubectl get ns "$CKA_SIM_LAB_NS" -o jsonpath='{.status.phase}' 2>/dev/null || echo "")
      [[ "$phase" == "Active" ]] && break
      sleep 5
    done
    [[ "$phase" == "Active" ]] || { echo "ns not Active (phase=$phase)" >&2; exit 1; }

    # 2. ServiceAccount 'viewer'
    kubectl apply -f - <<EOF
    apiVersion: v1
    kind: ServiceAccount
    metadata:
      name: viewer
      namespace: ${CKA_SIM_LAB_NS}
    EOF

    # 3. Role 'pod-viewer' — INTENTIONAL TRAP: only "watch" verb, missing get+list.
    kubectl apply -f - <<EOF
    apiVersion: rbac.authorization.k8s.io/v1
    kind: Role
    metadata:
      name: pod-viewer
      namespace: ${CKA_SIM_LAB_NS}
    rules:
      - apiGroups: [""]
        resources: ["pods"]
        verbs: ["watch"]
    EOF

    # 4. RoleBinding 'viewer-binding' binding SA viewer to Role pod-viewer
    kubectl apply -f - <<EOF
    apiVersion: rbac.authorization.k8s.io/v1
    kind: RoleBinding
    metadata:
      name: viewer-binding
      namespace: ${CKA_SIM_LAB_NS}
    subjects:
      - kind: ServiceAccount
        name: viewer
        namespace: ${CKA_SIM_LAB_NS}
    roleRef:
      kind: Role
      name: pod-viewer
      apiGroup: rbac.authorization.k8s.io
    EOF
    ```

    File 2 — `cka-sim/packs/cluster-architecture/01-rbac-viewer/grade.sh`:
    ```
    #!/bin/bash
    # cluster-architecture/01-rbac-viewer/grade.sh
    set -uo pipefail
    : "${CKA_SIM_LAB_NS:?CKA_SIM_LAB_NS must be set}"
    : "${CKA_SIM_ROOT:?CKA_SIM_ROOT must be set}"

    # shellcheck source=../../../lib/grade.sh disable=SC1091
    source "$CKA_SIM_ROOT/lib/grade.sh"
    # shellcheck source=../../../lib/traps.sh disable=SC1091
    source "$CKA_SIM_ROOT/lib/traps.sh"

    # Assertions
    cka_sim::grade::assert_resource_exists role pod-viewer -n "$CKA_SIM_LAB_NS"
    cka_sim::grade::assert_resource_exists rolebinding viewer-binding -n "$CKA_SIM_LAB_NS"
    cka_sim::grade::assert_resource_exists serviceaccount viewer -n "$CKA_SIM_LAB_NS"

    # Core: can the viewer SA actually get pods? yes == fix applied, no == trap still present.
    cka_sim::grade::assert_can_i get pods \
      --as="system:serviceaccount:${CKA_SIM_LAB_NS}:viewer" \
      -n "$CKA_SIM_LAB_NS"

    # Trap detector: Role binds pods but lacks get/list verbs.
    tid=$(cka_sim::trap::detect_rbac_viewer_role_mismatch "$CKA_SIM_LAB_NS" "pod-viewer")
    [[ -n "$tid" ]] && cka_sim::grade::record_trap "$tid"

    cka_sim::grade::emit_result
    ```

    File 3 — `cka-sim/packs/cluster-architecture/01-rbac-viewer/reset.sh`:
    ```
    #!/bin/bash
    set -uo pipefail
    : "${CKA_SIM_LAB_NS:?CKA_SIM_LAB_NS must be set}"
    kubectl delete namespace "$CKA_SIM_LAB_NS" --ignore-not-found --wait=false
    # No cluster-scoped resources for this question (Role/RoleBinding/SA are all namespace-scoped)
    exit 0
    ```

    File 4 — `cka-sim/packs/cluster-architecture/01-rbac-viewer/ref-solution.sh`:
    ```
    #!/bin/bash
    # cluster-architecture/01-rbac-viewer/ref-solution.sh — patches Role to add get+list verbs.
    set -euo pipefail
    : "${CKA_SIM_LAB_NS:?CKA_SIM_LAB_NS must be set}"

    kubectl apply -f - <<EOF
    apiVersion: rbac.authorization.k8s.io/v1
    kind: Role
    metadata:
      name: pod-viewer
      namespace: ${CKA_SIM_LAB_NS}
    rules:
      - apiGroups: [""]
        resources: ["pods"]
        verbs: ["get", "list", "watch"]
    EOF
    ```

    `chmod +x cka-sim/packs/cluster-architecture/01-rbac-viewer/*.sh`.
  </action>
  <verify>
    <automated>chmod +x cka-sim/packs/cluster-architecture/01-rbac-viewer/*.sh && for f in cka-sim/packs/cluster-architecture/01-rbac-viewer/*.sh; do bash -n "$f" || exit 1; done && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - 4 scripts exist + executable
    - Syntax valid (bash -n on all 4)
    - setup.sh creates Role with `verbs: ["watch"]` only: `grep -qE 'verbs:[[:space:]]*\["?watch"?\]' cka-sim/packs/cluster-architecture/01-rbac-viewer/setup.sh`
    - setup.sh creates all 3 resources (SA + Role + RoleBinding): `grep -c 'kind: ServiceAccount\|kind: Role$\|kind: RoleBinding' cka-sim/packs/cluster-architecture/01-rbac-viewer/setup.sh` returns ≥3
    - grade.sh calls assert_can_i with `get pods` and `--as=system:serviceaccount:`: `grep -q 'assert_can_i get pods' cka-sim/packs/cluster-architecture/01-rbac-viewer/grade.sh && grep -q 'as=system:serviceaccount' cka-sim/packs/cluster-architecture/01-rbac-viewer/grade.sh`
    - grade.sh calls detect_rbac_viewer_role_mismatch (from Plan 01 new detector)
    - ref-solution.sh adds `"get"` and `"list"` to verbs: `grep -qE '"get"[[:space:]]*,[[:space:]]*"list"' cka-sim/packs/cluster-architecture/01-rbac-viewer/ref-solution.sh`
    - lint-packs.sh accepts pack: `bash cka-sim/scripts/test.sh` exits 0
    - grade.sh contains NO mutating verbs (lint-packs Pass B): `! grep -qE 'kubectl[[:space:]]+(delete|create|apply|patch|edit|replace)' cka-sim/packs/cluster-architecture/01-rbac-viewer/grade.sh`
  </acceptance_criteria>
  <done>cluster-architecture pack reference question complete.</done>
</task>

</tasks>

<verification>
- 8 files created (2 pack-level + 6 question)
- `bash cka-sim/scripts/test.sh` exits 0
- D-10-revision honored: trap is `rbac-viewer-role-mismatch`, NOT `as-flag-format-wrong`
- The `watch` verb alone causes `auth can-i get pods` to return `no`; ref-solution adds get+list → `yes`
</verification>

<success_criteria>
- cluster-architecture/01-rbac-viewer fully drillable
- State-detectable trap mapped (fixes the RESEARCH Q3 gap)
- Trap mapping: rbac-viewer-role-mismatch (primary) + 2 filler seeded ids to satisfy ≥3
</success_criteria>

<output>
After completion, create `.planning/phases/03-runtime-contract-drill-mode/03-07-SUMMARY.md` covering files, D-10-revision rationale, trap detection mechanic, human-verification procedure.
</output>
