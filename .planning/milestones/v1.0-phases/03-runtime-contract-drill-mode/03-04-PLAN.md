---
phase: 03-runtime-contract-drill-mode
plan: 04
type: execute
wave: 3
depends_on: [01, 03]
files_modified:
  - cka-sim/packs/storage/manifest.yaml
  - cka-sim/packs/storage/README.md
  - cka-sim/packs/storage/01-pvc-binding/metadata.yaml
  - cka-sim/packs/storage/01-pvc-binding/question.md
  - cka-sim/packs/storage/01-pvc-binding/setup.sh
  - cka-sim/packs/storage/01-pvc-binding/grade.sh
  - cka-sim/packs/storage/01-pvc-binding/reset.sh
  - cka-sim/packs/storage/01-pvc-binding/ref-solution.sh
autonomous: true
requirements: [TRIP-01, TRIP-02, TRIP-03, TRIP-04, TRIP-05, TRIP-06, GRADE-02, GRADE-03, GRADE-04, GRADE-06, RUN-02]
must_haves:
  truths:
    - "storage pack has manifest.yaml + README.md + 1 question dir"
    - "storage/01-pvc-binding has all 6 required files (metadata, question, setup, grade, reset, ref-solution)"
    - "setup.sh creates ns + a hostPath PV WITHOUT nodeAffinity (the seeded trap) + a PVC stuck Pending"
    - "grade.sh asserts PVC Bound + PV nodeAffinity AND records hostpath-pv-without-nodeaffinity if detector fires"
    - "ref-solution.sh patches the PV to add nodeAffinity, making PVC bind"
    - "reset.sh deletes the lab ns async + cluster-scoped PV q01-app-pv"
    - "metadata.yaml declares 3 trap-ids, all registered in catalog"
    - "lint-packs.sh passes for this pack"
  artifacts:
    - path: "cka-sim/packs/storage/manifest.yaml"
      provides: "Pack manifest per D-02"
      contains: "storage-pvc-binding"
    - path: "cka-sim/packs/storage/01-pvc-binding/setup.sh"
      provides: "Idempotent ns + PV (no nodeAffinity, the trap) + PVC seeding"
      contains: "kubectl apply -f -"
    - path: "cka-sim/packs/storage/01-pvc-binding/grade.sh"
      provides: "PVC binding + nodeAffinity assertions + trap detector"
      contains: "cka_sim::grade::assert_pvc_bound"
    - path: "cka-sim/packs/storage/01-pvc-binding/ref-solution.sh"
      provides: "Patches PV to add kubernetes.io/hostname nodeAffinity"
      contains: "kubectl patch pv q01-app-pv"
  key_links:
    - from: "cka-sim/packs/storage/01-pvc-binding/grade.sh"
      to: "cka-sim/lib/grade.sh + cka-sim/lib/traps.sh"
      via: "source + assert_pvc_bound + detect_hostpath_pv_without_nodeaffinity"
      pattern: "source.*lib/(grade|traps).sh"
---

<objective>
Ship the storage domain pack: manifest, README, and 1 reference question (01-pvc-binding) with all 6 required files. The question exercises the seeded trap `hostpath-pv-without-nodeaffinity` (CONTEXT D-10 storage row).

Per RESEARCH Pattern 5 lines 318-335: a hostPath PV is created without `nodeAffinity` (the trap), making PVC binding silently fragile on multi-node clusters. The candidate must add `nodeAffinity` to the PV.

Purpose: TRIP-01..06 (triplet contract), GRADE-02..04+06 (grader contract), RUN-02 (drillable via `cka-sim drill storage`).
Output: 8 new files: 2 pack-level (manifest + README) + 6 question files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-runtime-contract-drill-mode/03-CONTEXT.md
@.planning/phases/03-runtime-contract-drill-mode/03-RESEARCH.md
@.planning/phases/03-runtime-contract-drill-mode/03-PATTERNS.md
@cka-sim/lib/grade.sh
@cka-sim/lib/traps.sh
@cka-sim/scripts/lint-packs.sh
@cka-sim/traps/catalog.yaml
</context>

<interfaces>
From cka-sim/lib/grade.sh — helpers grade.sh consumes:
- cka_sim::grade::assert_pvc_bound <ns> <pvc> [<weight>] — passes iff PVC.status.phase == Bound
- cka_sim::grade::assert_field_eq <kind> <name> <jsonpath> <expected> [-n <ns>] [<weight>] — passes iff jsonpath returns exactly <expected>
- cka_sim::grade::record_trap <id> — registers a trap (validates id, dedups)
- cka_sim::grade::emit_result — finalizer; prints SCORE: + Trap N: lines to stdout

From cka-sim/lib/traps.sh — detector for this question:
- cka_sim::trap::detect_hostpath_pv_without_nodeaffinity <pv-name> — echoes "hostpath-pv-without-nodeaffinity" if PV has hostPath and no nodeAffinity

Env-vars drill.sh sets (consumed by setup/grade/reset/ref-solution):
- CKA_SIM_LAB_NS = "cka-sim-storage-01"
- CKA_SIM_ROOT (for source line in grade.sh)
- CKA_SIM_QUESTION_ID = "storage-pvc-binding"

Per-question metadata schema (D-12(b)):
- id, domain, estimatedMinutes in [4,12], verified_against == "1.35", traps[>=3], references[]
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create storage pack manifest, README, and question scaffold</name>
  <files>cka-sim/packs/storage/manifest.yaml, cka-sim/packs/storage/README.md, cka-sim/packs/storage/01-pvc-binding/metadata.yaml, cka-sim/packs/storage/01-pvc-binding/question.md</files>
  <read_first>
    - .planning/phases/03-runtime-contract-drill-mode/03-CONTEXT.md (D-01, D-02, D-10 storage row, `<specifics>` lines 230-246 metadata sample)
    - .planning/phases/03-runtime-contract-drill-mode/03-PATTERNS.md (lines 284-352 manifest + metadata schemas; lines 553-597 question.md skeleton)
    - .planning/phases/03-runtime-contract-drill-mode/03-RESEARCH.md (lines 791-815 question.md example)
    - cka-sim/scripts/lint-packs.sh (just-created — to know what schema lint validates)
    - cka-sim/traps/catalog.yaml (post-Wave-1 — verify the 3 trap-ids exist)
  </read_first>
  <action>
    Create dir tree: `mkdir -p cka-sim/packs/storage/01-pvc-binding`.

    File 1 — `cka-sim/packs/storage/manifest.yaml`:
    ```
    pack:
      id: storage
      domain: storage
      weight: 10
      description: "Storage 10% domain pack — Phase 3 reference question only; full pack in Phase 4 (PACK-01)."
    questions:
      - id: storage-pvc-binding
        path: 01-pvc-binding
        estimatedMinutes: 8
    ```

    File 2 — `cka-sim/packs/storage/README.md`:
    ```
    # Storage Pack

    **Domain:** Storage (10% of CKA blueprint v1.35)

    Phase 3 reference question only — the full Storage pack covering all v1.35 Tracker checkboxes lands in Phase 4 (PACK-01).

    ## Questions

    | # | Slug | Topic | Time |
    |---|------|-------|------|
    | 01 | pvc-binding | PVC stuck Pending on hostPath PV without nodeAffinity | 8 min |

    ## Authoring

    See `cka-sim/AUTHORING.md` for the question authoring contract.

    ## Running

    ```bash
    cka-sim drill storage          # random question
    cka-sim drill storage 1        # 1-based index into manifest.yaml
    ```

    > Not real CKA exam content; independently authored.
    ```

    File 3 — `cka-sim/packs/storage/01-pvc-binding/metadata.yaml` (verbatim from CONTEXT specifics lines 230-246):
    ```
    id: storage-pvc-binding
    domain: storage
    estimatedMinutes: 8
    verified_against: "1.35"
    traps:
      - hostpath-pv-without-nodeaffinity
      - pvc-wrong-storageclass
      - pv-accessmodes-mismatch
    references:
      - kind: prior-art-exercise
        target: exercises/12-storage-pv-pvc/
        note: Prior prose version
      - kind: k8s-doc
        target: https://kubernetes.io/docs/concepts/storage/persistent-volumes/
        note: PV/PVC concepts
    ```

    File 4 — `cka-sim/packs/storage/01-pvc-binding/question.md` (PSI-style; do NOT spell out the trap):
    ```
    # storage/01-pvc-binding

    **Domain:** Storage  |  **Estimated time:** 8 minutes

    A `PersistentVolumeClaim` named `app-data` exists in your lab namespace and references a `PersistentVolume` named `q01-app-pv`. The PVC is stuck `Pending`.

    ## Tasks

    1. Inspect the PVC `app-data` in `${CKA_SIM_LAB_NS}` and the PV `q01-app-pv` (cluster-scoped).
    2. Diagnose why the PVC is not binding. Read the PV spec and events carefully.
    3. Modify the PV in place so the PVC can bind successfully.

    ## Constraints

    - Do NOT delete or recreate the PV — modify it in place.
    - Do NOT modify the PVC.
    - The lab cluster has 1 control-plane + 2 worker nodes. The PV must remain usable on any worker.

    ## Verify yourself

    Before typing `done`, confirm:

    ```
    kubectl get pvc app-data -n ${CKA_SIM_LAB_NS}    # STATUS should be Bound
    kubectl get pv q01-app-pv                        # STATUS should be Bound
    ```
    ```
  </action>
  <verify>
    <automated>[[ -f cka-sim/packs/storage/manifest.yaml && -f cka-sim/packs/storage/README.md && -f cka-sim/packs/storage/01-pvc-binding/metadata.yaml && -f cka-sim/packs/storage/01-pvc-binding/question.md ]] && grep -q 'storage-pvc-binding' cka-sim/packs/storage/manifest.yaml && grep -q 'storage-pvc-binding' cka-sim/packs/storage/01-pvc-binding/metadata.yaml</automated>
  </verify>
  <acceptance_criteria>
    - 4 files exist (manifest.yaml, README.md, metadata.yaml, question.md)
    - manifest.yaml has `id: storage` + 1 question with id `storage-pvc-binding`, path `01-pvc-binding`, estimatedMinutes 8
    - metadata.yaml has 6 required keys; traps list has exactly 3 entries; verified_against is `"1.35"`
    - question.md does NOT spoiler the trap: `! grep -q nodeAffinity cka-sim/packs/storage/01-pvc-binding/question.md`
    - question.md uses `${CKA_SIM_LAB_NS}` placeholder: `grep -q 'CKA_SIM_LAB_NS' cka-sim/packs/storage/01-pvc-binding/question.md`
  </acceptance_criteria>
  <done>Pack scaffold + question metadata + question prompt complete.</done>
</task>

<task type="auto">
  <name>Task 2: Create setup.sh, grade.sh, reset.sh, ref-solution.sh for storage/01-pvc-binding</name>
  <files>cka-sim/packs/storage/01-pvc-binding/setup.sh, cka-sim/packs/storage/01-pvc-binding/grade.sh, cka-sim/packs/storage/01-pvc-binding/reset.sh, cka-sim/packs/storage/01-pvc-binding/ref-solution.sh</files>
  <read_first>
    - .planning/phases/03-runtime-contract-drill-mode/03-CONTEXT.md (D-07, D-08, D-09, specifics lines 248-294)
    - .planning/phases/03-runtime-contract-drill-mode/03-RESEARCH.md (Pattern 3 lines 229-290; Pattern 4 lines 297-311; Pattern 5 lines 318-335; Pitfall 3 lines 479-498)
    - .planning/phases/03-runtime-contract-drill-mode/03-PATTERNS.md (lines 422-509 setup/reset/ref-solution patterns; lines 355-419 grade.sh pattern)
    - cka-sim/lib/grade.sh (assertion helper signatures lines 42-239)
    - cka-sim/lib/traps.sh (lines 214-225 detect_hostpath_pv_without_nodeaffinity signature)
  </read_first>
  <action>
    Create 4 scripts. ALL must be `chmod +x` after creation (lint-packs.sh enforces).

    File 1 — `cka-sim/packs/storage/01-pvc-binding/setup.sh`:
    ```
    #!/bin/bash
    # storage/01-pvc-binding/setup.sh — seeds hostPath PV WITHOUT nodeAffinity (trap) + PVC.
    set -euo pipefail
    : "${CKA_SIM_LAB_NS:?CKA_SIM_LAB_NS must be set by drill runner}"

    # 1. Idempotent ns create
    kubectl apply -f - <<EOF
    apiVersion: v1
    kind: Namespace
    metadata:
      name: ${CKA_SIM_LAB_NS}
      labels:
        cka-sim/pack: storage
        cka-sim/question-id: storage-pvc-binding
    EOF

    # 2. Wait up to 50s for ns Active (handles prior reset --wait=false leaving Terminating)
    phase=""
    for i in $(seq 1 10); do
      phase=$(kubectl get ns "$CKA_SIM_LAB_NS" -o jsonpath='{.status.phase}' 2>/dev/null || echo "")
      [[ "$phase" == "Active" ]] && break
      sleep 5
    done
    [[ "$phase" == "Active" ]] || { echo "ns $CKA_SIM_LAB_NS not Active after 50s (phase=$phase)" >&2; exit 1; }

    # 3. Apply hostPath PV — INTENTIONALLY MISSING nodeAffinity (the trap).
    # Cluster-scoped -> q01- prefix per TRIP-03.
    kubectl apply -f - <<EOF
    apiVersion: v1
    kind: PersistentVolume
    metadata:
      name: q01-app-pv
      labels:
        cka-sim/pack: storage
        cka-sim/question-id: storage-pvc-binding
    spec:
      capacity:
        storage: 1Gi
      accessModes:
        - ReadWriteOnce
      persistentVolumeReclaimPolicy: Retain
      storageClassName: manual
      hostPath:
        path: /tmp/q01-app-pv
        type: DirectoryOrCreate
    EOF

    # 4. Apply PVC (will stay Pending until candidate fixes the PV's nodeAffinity).
    kubectl apply -f - <<EOF
    apiVersion: v1
    kind: PersistentVolumeClaim
    metadata:
      name: app-data
      namespace: ${CKA_SIM_LAB_NS}
    spec:
      accessModes:
        - ReadWriteOnce
      resources:
        requests:
          storage: 500Mi
      storageClassName: manual
    EOF
    ```

    File 2 — `cka-sim/packs/storage/01-pvc-binding/grade.sh`:
    ```
    #!/bin/bash
    # storage/01-pvc-binding/grade.sh — asserts PVC bound + PV has nodeAffinity; records trap if seeded condition still present.
    set -uo pipefail
    : "${CKA_SIM_LAB_NS:?CKA_SIM_LAB_NS must be set by drill runner}"
    : "${CKA_SIM_ROOT:?CKA_SIM_ROOT must be set}"

    # shellcheck source=../../../lib/grade.sh disable=SC1091
    source "$CKA_SIM_ROOT/lib/grade.sh"
    # shellcheck source=../../../lib/traps.sh disable=SC1091
    source "$CKA_SIM_ROOT/lib/traps.sh"

    # Assertion 1: PVC must be Bound
    cka_sim::grade::assert_pvc_bound "$CKA_SIM_LAB_NS" "app-data"

    # Assertion 2: PV must have nodeAffinity expression key kubernetes.io/hostname
    cka_sim::grade::assert_field_eq pv q01-app-pv \
      '{.spec.nodeAffinity.required.nodeSelectorTerms[0].matchExpressions[0].key}' \
      'kubernetes.io/hostname'

    # Trap detector: if PV still has hostPath but no nodeAffinity, record the seeded trap.
    tid=$(cka_sim::trap::detect_hostpath_pv_without_nodeaffinity q01-app-pv)
    [[ -n "$tid" ]] && cka_sim::grade::record_trap "$tid"

    # Finalize — prints SCORE + Trap N: lines to stdout.
    cka_sim::grade::emit_result
    ```

    File 3 — `cka-sim/packs/storage/01-pvc-binding/reset.sh`:
    ```
    #!/bin/bash
    # storage/01-pvc-binding/reset.sh — async ns delete + cluster-scoped PV cleanup.
    set -uo pipefail
    : "${CKA_SIM_LAB_NS:?CKA_SIM_LAB_NS must be set by drill runner}"

    # 1. Async ns delete
    kubectl delete namespace "$CKA_SIM_LAB_NS" --ignore-not-found --wait=false

    # 2. Cluster-scoped (q<NN>- prefix per TRIP-03)
    kubectl delete pv q01-app-pv --ignore-not-found

    exit 0
    ```

    File 4 — `cka-sim/packs/storage/01-pvc-binding/ref-solution.sh`:
    ```
    #!/bin/bash
    # storage/01-pvc-binding/ref-solution.sh — patches PV q01-app-pv to add nodeAffinity (the trap fix).
    # Invoked by GRADE-06 round-trip: bash setup.sh && bash ref-solution.sh && bash grade.sh -> SCORE = max + 0 traps.
    # NOT exposed to candidates during drills.
    set -euo pipefail
    : "${CKA_SIM_LAB_NS:?CKA_SIM_LAB_NS must be set}"

    # Patch the PV to add a permissive nodeAffinity (operator: Exists matches every node with the label key).
    kubectl patch pv q01-app-pv --type=json -p='[
      {"op": "add", "path": "/spec/nodeAffinity", "value": {
        "required": {
          "nodeSelectorTerms": [{
            "matchExpressions": [{
              "key": "kubernetes.io/hostname",
              "operator": "Exists"
            }]
          }]
        }
      }}
    ]'
    ```

    Make all 4 scripts executable: `chmod +x cka-sim/packs/storage/01-pvc-binding/*.sh`.
  </action>
  <verify>
    <automated>chmod +x cka-sim/packs/storage/01-pvc-binding/*.sh && bash -n cka-sim/packs/storage/01-pvc-binding/setup.sh && bash -n cka-sim/packs/storage/01-pvc-binding/grade.sh && bash -n cka-sim/packs/storage/01-pvc-binding/reset.sh && bash -n cka-sim/packs/storage/01-pvc-binding/ref-solution.sh && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - 4 scripts exist + executable: `for f in setup.sh grade.sh reset.sh ref-solution.sh; do [[ -x cka-sim/packs/storage/01-pvc-binding/$f ]] || exit 1; done` returns 0
    - All 4 syntax-valid: `for f in cka-sim/packs/storage/01-pvc-binding/*.sh; do bash -n "$f" || exit 1; done` returns 0
    - setup.sh uses apply heredoc only (no `kubectl create`): `! grep -E '^[[:space:]]*kubectl[[:space:]]+create[[:space:]]' cka-sim/packs/storage/01-pvc-binding/setup.sh`
    - reset.sh uses --ignore-not-found everywhere: `grep -c '\-\-ignore-not-found' cka-sim/packs/storage/01-pvc-binding/reset.sh` returns >= 2
    - grade.sh uses `set -uo pipefail` (NOT -euo): `grep -q 'set -uo pipefail' cka-sim/packs/storage/01-pvc-binding/grade.sh`
    - grade.sh sources both lib/grade.sh AND lib/traps.sh: `grep -q 'source.*lib/grade.sh' cka-sim/packs/storage/01-pvc-binding/grade.sh && grep -q 'source.*lib/traps.sh' cka-sim/packs/storage/01-pvc-binding/grade.sh`
    - grade.sh ends with emit_result: `grep -q 'cka_sim::grade::emit_result' cka-sim/packs/storage/01-pvc-binding/grade.sh`
    - ref-solution.sh patches the PV: `grep -q 'kubectl patch pv q01-app-pv' cka-sim/packs/storage/01-pvc-binding/ref-solution.sh`
    - lint-packs.sh accepts pack: `bash cka-sim/scripts/test.sh` exits 0
  </acceptance_criteria>
  <done>4 scripts implemented, all chmod +x, lint-packs green.</done>
</task>

</tasks>

<verification>
- All 8 files exist + correct permissions
- `bash cka-sim/scripts/test.sh` exits 0 (lint-packs green for new pack; existing tests still pass)
- No `kubectl create` (TRIP-02 idempotency)
- No `kubectl get | grep` (GRADE-02)
- No `kubectl delete ns` in setup.sh (D-09)
- No mutating verbs in grade.sh (override #3)
- 3 trap-ids in metadata.yaml all registered in catalog (post-Wave-1)
</verification>

<success_criteria>
- storage/01-pvc-binding is fully drillable: `cka-sim drill storage` end-to-end (live verification per Open Q1)
- GRADE-06 round-trip is documented for human verification: setup.sh + grade.sh -> FAIL with hostpath-pv-without-nodeaffinity trap; setup.sh + ref-solution.sh + grade.sh -> PASS
- lint-packs.sh accepts the pack (5 lint passes all green)
</success_criteria>

<output>
After completion, create `.planning/phases/03-runtime-contract-drill-mode/03-04-SUMMARY.md` covering:
- 8 new files (2 pack-level + 6 question)
- Trap mapping: hostpath-pv-without-nodeaffinity (seeded) + pvc-wrong-storageclass + pv-accessmodes-mismatch
- Live-cluster verification procedure (5 minutes): documented for end-of-phase SUMMARY
- Confirm test.sh suite still green
</output>
