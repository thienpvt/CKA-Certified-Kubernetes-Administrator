---
phase: 03-runtime-contract-drill-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cka-sim/traps/catalog.yaml
autonomous: true
requirements: [GRADE-04]
must_haves:
  truths:
    - "5 new trap entries are registered in traps/catalog.yaml so per-question metadata.yaml files can reference them without lint-packs failing"
    - "One of the new entries is a state-detectable RBAC trap suitable for cluster-architecture/01-rbac-viewer (D-10 revision per user override #1)"
    - "lint-traps.sh still passes (8 seeded entries intact + 5 new entries pass schema)"
  artifacts:
    - path: "cka-sim/traps/catalog.yaml"
      provides: "8 seeded + 5 new trap entries (13 total)"
      contains: "rbac-viewer-role-mismatch"
  key_links:
    - from: "cka-sim/traps/catalog.yaml"
      to: "cka-sim/scripts/lint-traps.sh"
      via: "8-field schema lint"
      pattern: "lint-traps.sh exits 0"
---

<objective>
Extend the trap catalog with 5 new entries so the 5 reference questions in Wave 3 can reference them without violating lint-packs rule (c) (every metadata.yaml trap-id must exist in catalog.yaml).

This plan must land BEFORE any pack metadata.yaml that references the new IDs (per user override #4 and RESEARCH Open Q5).

Per user override #1: the seeded `as-flag-format-wrong` trap is text-based and cannot fire from cluster state alone. Replace its mapping for cluster-architecture/01-rbac-viewer with a NEW state-detectable RBAC trap added in this plan.

Purpose: Unblock the per-question metadata.yaml files that declare ≥3 trap-ids (PACK-06 / GRADE-04).
Output: traps/catalog.yaml grows from 8 to 13 entries; lint-traps.sh still passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-runtime-contract-drill-mode/03-CONTEXT.md
@.planning/phases/03-runtime-contract-drill-mode/03-RESEARCH.md
@.planning/phases/03-runtime-contract-drill-mode/03-PATTERNS.md
@cka-sim/traps/catalog.yaml
@cka-sim/scripts/lint-traps.sh
</context>

<interfaces>
<!-- From cka-sim/scripts/lint-traps.sh:34-38 — closed enums every entry must satisfy: -->
- severity ∈ {info, warn, error}
- domain ∈ {troubleshooting, cluster-architecture, services-networking, workloads-scheduling, storage}
- source ∈ {cncf-curriculum, concerns-md, community}
- references[].kind ∈ {concerns-md, k8s-doc, prior-art-exercise, exam-objective, blog-post}

<!-- From cka-sim/scripts/lint-traps.sh:53 — required field order (lint-enforced): -->
- id, name, description, remediation_hint, severity, domain, source, references

<!-- From cka-sim/lib/traps.sh:36-41 — RFC 1123 id constraint: -->
- id matches ^[a-z0-9]([a-z0-9-]*[a-z0-9])?$ AND length ≤ 63

<!-- From cka-sim/scripts/lint-traps.sh:106-110 — references with kind=concerns-md or kind=prior-art-exercise must resolve under repo root -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add 5 new trap entries to traps/catalog.yaml</name>
  <files>cka-sim/traps/catalog.yaml</files>
  <read_first>
    - cka-sim/traps/catalog.yaml (full — to see exact indent and field-order template)
    - cka-sim/scripts/lint-traps.sh (lines 34-53 enums + required fields, lines 124-180 walker — to know what shape lint validates)
    - .planning/phases/03-runtime-contract-drill-mode/03-PATTERNS.md (lines 240-281 — catalog-extension pattern with copy-paste-ready entry block)
    - .planning/phases/03-runtime-contract-drill-mode/03-CONTEXT.md (D-10 trap mapping + Claude's Discretion §"New trap catalog entries")
  </read_first>
  <action>
    Append 5 new entries to `cka-sim/traps/catalog.yaml` AFTER the existing 8 seeded entries (preserving the trailing blank line and any final newline). Use the EXACT 8-field block format from existing entries (e.g., `hostpath-pv-without-nodeaffinity` at catalog.yaml lines ~78-94). Indent: `  - id:` (2-space), `    <field>:` (4-space), `      - kind:` (6-space dash), `        target:` / `        note:` (8-space).

    Field order MUST be: id, name, description, remediation_hint, severity, domain, source, references. Lint enforces this implicitly via the parser's expectation.

    For each entry, set `source: community` (none of these are CONCERNS-derived; per CONTEXT `<code_context>` "Catalog extension"). Skip any `references[].kind == concerns-md` (those targets must resolve under repo root and CONCERNS.md doesn't anchor these new ones).

    Add EXACTLY these 5 entries:

    1. `pvc-wrong-storageclass` (storage, severity: warn) — for storage/01-pvc-binding's per-question additional trap.
       - name: "PVC requests a StorageClass not present on the cluster"
       - description: "PVC stays Pending because spec.storageClassName references a StorageClass that no provisioner serves."
       - remediation_hint: "Update PVC.spec.storageClassName to a class that exists, or create the missing StorageClass."
       - references: 1 item — kind: k8s-doc, target: "https://kubernetes.io/docs/concepts/storage/storage-classes/", note: StorageClass concepts.

    2. `pv-accessmodes-mismatch` (storage, severity: warn) — for storage/01-pvc-binding's per-question additional trap.
       - name: "PV and PVC accessModes do not match"
       - description: "Binder skips a PV whose accessModes don't include every mode the PVC requests."
       - remediation_hint: "Align PV.spec.accessModes with PVC.spec.accessModes (PVC requesting RWO needs PV listing RWO)."
       - references: 1 item — kind: k8s-doc, target: "https://kubernetes.io/docs/concepts/storage/persistent-volumes/#access-modes", note: accessModes matching rules.

    3. `deployment-missing-requests` (workloads-scheduling, severity: warn) — for workloads-scheduling/01-deployment-requests' per-question additional trap.
       - name: "Deployment containers omit resources.requests"
       - description: "Without resources.requests, HPA cannot compute CPU utilisation and the scheduler treats the pod as best-effort QoS."
       - remediation_hint: "Add resources.requests.cpu and resources.requests.memory to every container in the Deployment template."
       - references: 1 item — kind: k8s-doc, target: "https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/", note: container resource requests.

    4. `service-selector-empty-endpoints` (troubleshooting, severity: error) — for troubleshooting/01-deploy-svc-mismatch's seeded trap (the question's PRIMARY trap, since CONTEXT D-10 marks the troubleshooting trap as "TBD" and the user-override notes it should be additional non-seeded).
       - name: "Service selector matches no Ready pods so endpoints are empty"
       - description: "Service.spec.selector labels do not match any Ready pod's labels; Endpoints/EndpointSlices contain no addresses."
       - remediation_hint: "Diff Service.spec.selector against the Deployment template's pod labels and align them (label keys AND values)."
       - references: 1 item — kind: k8s-doc, target: "https://kubernetes.io/docs/concepts/services-networking/service/#defining-a-service", note: selector / endpoints.

    5. `rbac-viewer-role-mismatch` (cluster-architecture, severity: error) — NEW state-detectable RBAC trap per user override #1 (replaces text-based `as-flag-format-wrong` for cluster-architecture/01-rbac-viewer). This trap detects a real cluster-state misconfiguration: the candidate's Role grants a verb/resource set that does NOT include `list pods` (or the RoleBinding subject does not match the expected ServiceAccount).
       - name: "RBAC Role or RoleBinding does not grant the required viewer permissions"
       - description: "Candidate created a Role/RoleBinding pair but the Role's rules omit the required verb-resource (e.g., 'list pods') OR the RoleBinding subject does not name the expected ServiceAccount; kubectl auth can-i returns 'no' for the requested impersonation."
       - remediation_hint: "Ensure the Role grants verbs ['get','list','watch'] on resources ['pods'] in apiGroups ['']; ensure the RoleBinding subject is kind=ServiceAccount, name=<expected-sa>, namespace=<lab-ns>."
       - references: 1 item — kind: k8s-doc, target: "https://kubernetes.io/docs/reference/access-authn-authz/rbac/", note: RBAC Role/RoleBinding semantics.

    Verbatim block for entry 5 (illustrating the exact indent — 2-space dash, 4-space fields, 6-space references dash, 8-space sub-fields):
    ```yaml
      - id: rbac-viewer-role-mismatch
        name: RBAC Role or RoleBinding does not grant the required viewer permissions
        description: "Candidate created a Role/RoleBinding pair but the Role's rules omit the required verb-resource (e.g., 'list pods') OR the RoleBinding subject does not name the expected ServiceAccount; kubectl auth can-i returns 'no' for the requested impersonation."
        remediation_hint: "Ensure the Role grants verbs ['get','list','watch'] on resources ['pods'] in apiGroups ['']; ensure the RoleBinding subject is kind=ServiceAccount, name=<expected-sa>, namespace=<lab-ns>."
        severity: error
        domain: cluster-architecture
        source: community
        references:
          - kind: k8s-doc
            target: "https://kubernetes.io/docs/reference/access-authn-authz/rbac/"
            note: RBAC Role/RoleBinding semantics
    ```

    Use the same shape for entries 1-4. Each entry separated by exactly ONE blank line (matching the existing pattern between seeded entries).
  </action>
  <verify>
    <automated>bash cka-sim/scripts/lint-traps.sh 2>&1 | tee /tmp/lint-traps.out && grep -q "catalog lint passed (13 entr" /tmp/lint-traps.out</automated>
  </verify>
  <acceptance_criteria>
    - `bash cka-sim/scripts/lint-traps.sh` exits 0
    - Output contains literal substring `catalog lint passed (13 entr`
    - `grep -cE '^[[:space:]]{2}-[[:space:]]+id:' cka-sim/traps/catalog.yaml` returns `13`
    - All 5 new IDs appear: `for id in pvc-wrong-storageclass pv-accessmodes-mismatch deployment-missing-requests service-selector-empty-endpoints rbac-viewer-role-mismatch; do grep -qE "^[[:space:]]{2}-[[:space:]]+id:[[:space:]]+${id}\$" cka-sim/traps/catalog.yaml || exit 1; done` returns 0
    - All 8 seeded IDs still present (lint-traps.sh:41-50 seed_completeness check still passes — implied by lint exit 0)
  </acceptance_criteria>
  <done>13 entries, all schema-valid, all 5 new IDs present, lint-traps.sh green.</done>
</task>

</tasks>

<verification>
- `bash cka-sim/scripts/lint-traps.sh` → exit 0, "catalog lint passed (13 entr(ies))"
- `bash cka-sim/scripts/test.sh` → exit 0 (full suite still green; this plan adds catalog entries only, doesn't touch tests)
- New IDs grep cleanly:
  ```
  grep -cE '^[[:space:]]{2}-[[:space:]]+id:' cka-sim/traps/catalog.yaml  # → 13
  ```
</verification>

<success_criteria>
- traps/catalog.yaml has 13 entries (8 seeded + 5 new)
- lint-traps.sh exits 0
- The new `rbac-viewer-role-mismatch` is state-detectable (description says so explicitly), unblocking D-10's revised mapping for cluster-architecture/01-rbac-viewer (Wave 3 plan 03-07)
</success_criteria>

<output>
After completion, create `.planning/phases/03-runtime-contract-drill-mode/03-01-SUMMARY.md` covering:
- 5 new trap IDs added (with one-line description each)
- Confirmation lint-traps.sh still passes (13 entries)
- Note: `rbac-viewer-role-mismatch` is the D-10 revision for cluster-architecture/01 (per user override #1) — Wave 3 plan 03-07 will use this ID instead of `as-flag-format-wrong`
</output>

## Notes

- **D-10-revision (per user override #1):** The seeded `as-flag-format-wrong` trap is text-based and cannot fire from cluster state alone (RESEARCH Open Q3). Plan 03-01 adds a new state-detectable RBAC trap `rbac-viewer-role-mismatch` to the catalog. Plan 03-07 (cluster-architecture/01-rbac-viewer) will declare `rbac-viewer-role-mismatch` (NOT `as-flag-format-wrong`) in its metadata.yaml `traps[]` list and detect it in grade.sh via state inspection of the Role/RoleBinding. CONTEXT.md D-10 should be patched in execute (the cluster-architecture row's "Seeded trap exercised" cell becomes `rbac-viewer-role-mismatch` instead of `as-flag-format-wrong`).
- **Wave dependency (per user override #4):** This plan MUST land before plans 03-04..03-08 (the question packs) so their metadata.yaml `traps[]` entries pass lint-packs.sh rule (c) (every trap-id must be in catalog).
