---
phase: 02-trap-framework-assertion-library
plan: 05
type: execute
wave: 4
depends_on: ["02-04"]
files_modified:
  - .github/workflows/validate.yml
autonomous: true
requirements:
  - GRADE-01
  - GRADE-05
  - TRIP-07
must_haves:
  truths:
    - "GitHub Actions runs cka-sim/scripts/test.sh on every push and pull request that touches cka-sim/** or *.sh files"
    - "The bash-tests job extends paths: filter to include cka-sim/** so it actually triggers on relevant changes"
    - "The existing yamllint job is unchanged (Phase 8 will revisit CI more broadly)"
  artifacts:
    - path: ".github/workflows/validate.yml"
      provides: "CI workflow with extended paths: + new bash-tests job"
      contains: "bash-tests:, bash cka-sim/scripts/test.sh, cka-sim/**"
  key_links:
    - from: ".github/workflows/validate.yml"
      to: "cka-sim/scripts/test.sh"
      via: "bash-tests job invokes the orchestrator"
      pattern: 'bash cka-sim/scripts/test\.sh'
---

<objective>
Extend `.github/workflows/validate.yml` to run the Phase 2 bash test harness in CI: extend the `paths:` filter to include `cka-sim/**` and `**.sh`, and add a new `bash-tests` job that invokes `bash cka-sim/scripts/test.sh` on `ubuntu-latest`.

Purpose: Close the loop. Plans 02-01 through 02-04 deliver the test harness; this plan ensures it runs on every relevant PR. Without this, regressions silently land. After this, the catalog lint + 15 unit cases gate every push that touches the library or fixtures.
Output: One modified YAML file. No new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-trap-framework-assertion-library/02-CONTEXT.md
@.planning/phases/02-trap-framework-assertion-library/02-PATTERNS.md
@.planning/phases/02-trap-framework-assertion-library/02-04-SUMMARY.md
@.github/workflows/validate.yml
@cka-sim/scripts/test.sh

<interfaces>
<!-- The existing workflow has one job (yamllint). We add a second job (bash-tests). -->
<!-- The yamllint job is left intact: Phase 2 only adds; does NOT modify the YAML lint behaviour. -->
<!-- Phase 8 will revisit CI more broadly (shellcheck, deprecated-strings lint) — out of scope here. -->

Existing validate.yml shape (from plan 02-03 read of the file):
```yaml
name: Validate YAML
on:
  push:
    branches: [main]
    paths: [skeletons/**, exercises/**, **.yaml, **.yml]
  pull_request:
    branches: [main]
    paths: [skeletons/**, exercises/**, **.yaml, **.yml]
jobs:
  yamllint:
    name: YAML Lint
    runs-on: ubuntu-latest
    steps: ...
```

Modifications required (from CONTEXT D-11 + PATTERNS.md "validate.yml" section):
1. Extend both `paths:` lists with `cka-sim/**` AND `**.sh`. Apply to BOTH the push: and pull_request: blocks.
2. Add a new top-level job under `jobs:` named `bash-tests` (lower-case-hyphenated; mirrors yamllint job naming).
3. The bash-tests job runs `bash cka-sim/scripts/test.sh` on ubuntu-latest. No extra package installs (jq is pre-installed; python3 not needed; no python-based steps).
4. Do NOT add shellcheck pre-job in this plan (CONTEXT explicitly defers shellcheck to Phase 8 CI-02).
5. Do NOT modify the existing yamllint job's name, runs-on, or steps.
6. Do NOT change the workflow's top-level `name:`. (Phase 8 may rename it.)

GHA YAML conventions to preserve from the existing file:
- 2-space indent.
- `uses: actions/checkout@v4` (already pinned).
- `runs-on: ubuntu-latest`.
- Multi-line `run: |` blocks for orchestrated commands (only needed if doing multi-step; for our case `run: bash cka-sim/scripts/test.sh` is one line).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend .github/workflows/validate.yml — add cka-sim/** + **.sh to paths: filters; add bash-tests job</name>
  <read_first>
    - .github/workflows/validate.yml (current contents — modify in place; do not overwrite)
    - cka-sim/scripts/test.sh (the orchestrator — confirm invocation path is correct)
    - .planning/phases/02-trap-framework-assertion-library/02-CONTEXT.md (D-11 — invocation = scripts/test.sh + new GHA job)
    - .planning/phases/02-trap-framework-assertion-library/02-PATTERNS.md (section ".github/workflows/validate.yml" — exact shape of paths extension and the bash-tests job)
  </read_first>
  <files>.github/workflows/validate.yml</files>
  <action>
    Modify `.github/workflows/validate.yml` IN PLACE. Two changes:

    **Change 1 — extend paths: filter on both push: and pull_request: blocks.**

    Existing block (lines 4-17):
    ```yaml
    on:
      push:
        branches: [main]
        paths:
          - 'skeletons/**'
          - 'exercises/**'
          - '**.yaml'
          - '**.yml'
      pull_request:
        branches: [main]
        paths:
          - 'skeletons/**'
          - 'exercises/**'
          - '**.yaml'
          - '**.yml'
    ```

    Becomes:
    ```yaml
    on:
      push:
        branches: [main]
        paths:
          - 'skeletons/**'
          - 'exercises/**'
          - 'cka-sim/**'
          - '**.yaml'
          - '**.yml'
          - '**.sh'
      pull_request:
        branches: [main]
        paths:
          - 'skeletons/**'
          - 'exercises/**'
          - 'cka-sim/**'
          - '**.yaml'
          - '**.yml'
          - '**.sh'
    ```

    Insertion order: place `cka-sim/**` AFTER `exercises/**` and BEFORE `**.yaml`. Place `**.sh` at the end of each list.

    **Change 2 — add new `bash-tests` job under jobs: (after the existing yamllint job).**

    The existing `jobs:` block ends at line 50 (the `echo "All YAML files valid"` line is the last step of the yamllint job). After that final step, add:

    ```yaml
      bash-tests:
        name: Bash unit tests (traps + grade)
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4

          - name: Run cka-sim test suite
            run: bash cka-sim/scripts/test.sh
    ```

    The new job is at the same indent level as `yamllint:` (2 spaces from `jobs:`). Steps under it use 4-space indent. Mirror the yamllint job's style exactly.

    Do NOT add any of the following (out of scope per D-11 and CONTEXT exclusions):
    - shellcheck install/run (Phase 8 CI-02).
    - Deprecated-strings grep lint (Phase 8 CI-02).
    - jq install (`ubuntu-latest` ships jq pre-installed; verify by running `which jq` if curious; no install needed).
    - python3 install (the bash-tests job is pure bash + jq; lint-traps.sh is pure bash too).
    - Modifying the workflow's top-level `name: Validate YAML` (Phase 8 may rename).

    Preserve the existing `yamllint` job verbatim — every step, every `run: |` block, every command stays identical.

    Use 2-space YAML indent (matches existing). LF line endings.
  </action>
  <verify>
    <automated>python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/validate.yml')); assert 'bash-tests' in d['jobs']; assert 'yamllint' in d['jobs']; assert 'cka-sim/**' in d['on']['push']['paths']; assert '**.sh' in d['on']['pull_request']['paths']; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File still parses as YAML: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/validate.yml'))"` exits 0
    - Both jobs present: `python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/validate.yml')); print(sorted(d['jobs'].keys()))"` outputs `['bash-tests', 'yamllint']`
    - paths: filter on push includes cka-sim/**: `python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/validate.yml')); assert 'cka-sim/**' in d['on']['push']['paths']"`
    - paths: filter on pull_request includes cka-sim/**: `python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/validate.yml')); assert 'cka-sim/**' in d['on']['pull_request']['paths']"`
    - paths: filter includes **.sh on both blocks: `python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/validate.yml')); assert '**.sh' in d['on']['push']['paths']; assert '**.sh' in d['on']['pull_request']['paths']"`
    - bash-tests job runs the orchestrator: `python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/validate.yml')); steps=d['jobs']['bash-tests']['steps']; assert any(s.get('run','').strip() == 'bash cka-sim/scripts/test.sh' for s in steps), steps"`
    - bash-tests job uses ubuntu-latest: `python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/validate.yml')); assert d['jobs']['bash-tests']['runs-on'] == 'ubuntu-latest'"`
    - bash-tests job has the actions/checkout@v4 step: `python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/validate.yml')); steps=d['jobs']['bash-tests']['steps']; assert any(s.get('uses','') == 'actions/checkout@v4' for s in steps)"`
    - existing yamllint job unchanged — has the YAML Lint name + the same step names: `python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/validate.yml')); j=d['jobs']['yamllint']; assert j['name']=='YAML Lint'; assert j['runs-on']=='ubuntu-latest'; names=[s.get('name','') for s in j['steps'] if 'name' in s]; assert 'Install yamllint' in names; assert 'Lint skeleton YAML files' in names"`
    - File ends with a newline; LF endings preserved: `tail -c 1 .github/workflows/validate.yml | od -c | head -1 | grep -q '\\\\n'`
    - No shellcheck install added: `grep -ci 'shellcheck' .github/workflows/validate.yml` returns 0
    - No deprecated-strings step added: `grep -ci 'PodSecurityPolicy\|dockershim' .github/workflows/validate.yml` returns 0
    - End-to-end smoke (only meaningful locally if act / push to fork; we can't validate the actual run from the planner machine — but we CAN dry-run the orchestrator the workflow will invoke): `bash cka-sim/scripts/test.sh && echo OK` returns OK.
  </acceptance_criteria>
  <done>
    `.github/workflows/validate.yml` parses cleanly as YAML, has two top-level jobs (`yamllint` unchanged, `bash-tests` new), and both paths: filters (push and pull_request) include `cka-sim/**` and `**.sh`. The bash-tests job invokes `bash cka-sim/scripts/test.sh` on `ubuntu-latest` with `actions/checkout@v4`. No out-of-scope additions (shellcheck / deprecated-strings) leaked in. `bash cka-sim/scripts/test.sh` continues to pass locally — confirming the workflow has something to run.
  </done>
</task>

</tasks>

<verification>
After the task completes:

1. YAML still parses: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/validate.yml'))" && echo OK`.
2. Both jobs visible: `grep -E '^  (yamllint|bash-tests):$' .github/workflows/validate.yml` returns 2 lines.
3. paths: filter coverage: `grep -E "cka-sim/\*\*" .github/workflows/validate.yml | wc -l` returns 2 (one for push, one for pull_request).
4. The orchestrator (which CI will invoke) still passes locally: `bash cka-sim/scripts/test.sh` exits 0.
5. Plan reverse-check: `grep -c 'shellcheck' .github/workflows/validate.yml` returns 0 (Phase 8 territory).
</verification>

<success_criteria>
- `.github/workflows/validate.yml` has been modified in place: paths: filter extended on both push and pull_request blocks; new `bash-tests` job added.
- Existing yamllint job is bit-for-bit unchanged in name, runs-on, and steps.
- The new job invokes `bash cka-sim/scripts/test.sh` (the orchestrator from plan 02-03) — which lints the catalog and runs all 15 unit cases.
- No out-of-scope CI additions (shellcheck, deprecated-strings lint) leaked in — those land in Phase 8 per CI-01/CI-02/CI-03.
</success_criteria>

<output>
After completion, create `.planning/phases/02-trap-framework-assertion-library/02-05-SUMMARY.md` documenting:
- The exact diff applied to `.github/workflows/validate.yml`.
- Confirmation that `bash cka-sim/scripts/test.sh` runs locally (proxy for CI).
- Phase 2 close-out: all 5 success criteria from ROADMAP.md are now demonstrably true:
  1. lib/traps.sh exports 8 detectors ✓ (plan 02-02)
  2. lib/grade.sh exports 7 helpers + emit_result ✓ (plan 02-01)
  3. catalog.yaml has 8 entries + passes lint ✓ (plan 02-02 + 02-03)
  4. unit tests fire correctly on known-bad fixtures ✓ (plan 02-04)
  5. RFC 1123 compliance ✓ (lint-traps.sh + traps.sh::is_valid_id)
- Open carry-forward to Phase 3: graders will source lib/grade.sh + lib/traps.sh; the 8 seeded traps are now ready for use.
</output>
