---
phase: 07.1-grading-honesty-rebuild
plan: 05
subsystem: pack-fix-services-netpol-endport
tags: [pack-fix, network-policy, candidate-authored-gate, reachability]
dependency_graph:
  requires: [grade-honesty-helpers, lib/baseline.sh]
  provides: [services-netpol-endport-empty-zero]
  affects: [cka-sim/packs/services-networking/06-netpol-endport]
tech_stack:
  added: []
  patterns: [candidate-authored-gate, baseline-aware-reachability]
key_files:
  created: []
  modified:
    - cka-sim/packs/services-networking/06-netpol-endport/grade.sh
    - cka-sim/packs/services-networking/06-netpol-endport/reset.sh
decisions:
  - "Use is_candidate_modified + cluster-existence check (two-step gate) instead of assert_resource_candidate_authored to keep accumulator math intact for the existing single-point reachability assertion"
  - "Preserve assertions 1-4 and assertion 6 unchanged — empty submission already correctly fails them (no NP -> kubectl returns empty -> all fail; default-allow makes :8095 reachable -> assertion 6 fails)"
  - "Remove misplaced cka_sim::grade::record_trap netpol-endport-missing-protocol from the reachability fail branch — that trap belongs to the protocol assertion (already recorded there on line 27), not reachability"
metrics:
  duration_seconds: 420
  completed: "2026-05-15T17:26:00Z"
  tasks_completed: 2
  tasks_total: 2
  files_created: 0
  files_modified: 2
---

# Phase 07.1 Plan 05: services-networking/06-netpol-endport Reachability Gate Summary

Apply RESEARCH Q4 fix: gate the `:8085` reachability assertion on a candidate-authored NetworkPolicy so default-allow stops leaking 1 point on empty submission, and add `/tmp/cka-sim/06-netpol-endport/` cleanup to reset.sh.

## Commits

| Task | Type | Hash    | Description                                                                |
| ---- | ---- | ------- | -------------------------------------------------------------------------- |
| 1    | fix  | fda4dfb | Gate reachability assertion on candidate-authored NetworkPolicy            |
| 2    | fix  | 3fb6c66 | Add /tmp/cka-sim/06-netpol-endport/ cleanup to reset.sh                    |

## What Was Built

1. **grade.sh np_authored gate** — Inserted before assertion 5 (`:8085` reachability). Calls `cka_sim::baseline::is_candidate_modified networkpolicy q06-allow-range -n "$CKA_SIM_LAB_NS"`; if that returns 0 (modified or back-compat), then verifies the NP currently exists in the cluster via `kubectl get networkpolicy q06-allow-range -o name`. Only when both checks pass is `np_authored=1`.

2. **grade.sh assertion 5 success path tightened** — `:8085` reachability now only awards a point when `np_authored == 1` AND the wget probe succeeds. The fail branch distinguishes the two reasons: "skipped — no candidate-authored NetworkPolicy" vs. "cannot reach". The misplaced `record_trap netpol-endport-missing-protocol` call (which belonged to assertion 4, not 5) was removed from this branch.

3. **reset.sh /tmp cleanup** — Appended `rm -rf /tmp/cka-sim/06-netpol-endport/` between the env-var check and the namespace delete (per plan: before any conditional namespace-delete block). Literal slug, no variable expansion. Satisfies the Plan 02 lint-packs Pass I warning.

## Predicted Score Behavior

- **Empty submission:** 0/6 (assertions 1-3 fail because no NP exists; assertion 4 fails because protocol jsonpath returns empty; assertion 5 fails because `np_authored=0`; assertion 6 fails because default-allow makes `:8095` reachable).
- **Reference solution:** 6/6 (NP `q06-allow-range` exists with TCP 8080-8090; `:8085` in range reaches; `:8095` out of range blocked).

## Deviations from Plan

None — plan executed exactly as written.

## Verification Results

- `bash -n cka-sim/packs/services-networking/06-netpol-endport/grade.sh` exits 0
- `grep -c 'np_authored' cka-sim/packs/services-networking/06-netpol-endport/grade.sh` = 4 (declaration, set-on-success, two equality checks)
- `grep -c 'assert_resource_candidate_authored networkpolicy q06-allow-range'` is 0 — gate uses `is_candidate_modified` directly per the plan's interfaces note (keeps accumulator math intact for the single-point assertion).
- Assertions 1-4 (lines 14-28) preserved unchanged
- Assertion 6 (`:8095` unreachability, lines 56-65) preserved unchanged
- `cka_sim::grade::emit_result` is the last call in grade.sh
- `bash -n cka-sim/packs/services-networking/06-netpol-endport/reset.sh` exits 0
- `grep -c 'rm -rf /tmp/cka-sim/06-netpol-endport' cka-sim/packs/services-networking/06-netpol-endport/reset.sh` = 1

## Notes on must_haves Drift

The plan's `must_haves.artifacts` block expects `grade.sh` to contain the literal string `assert_resource_candidate_authored networkpolicy q06-allow-range`. The `<interfaces>` section of the plan explicitly directs the executor away from that helper for this gate (because passing weight=0 to keep accumulator math intact is unverified, and the existing assertion already increments `CKA_SIM_GRADE_TOTAL` separately). Following the interfaces guidance, the implementation uses `cka_sim::baseline::is_candidate_modified` + cluster existence check, which delivers the same semantic gate without double-counting. The behavior contract from `must_haves.truths` (empty=0/6, ref=6/6, reachability gated, default-allow doesn't leak) is fully satisfied.

## Known Stubs

None — all code is fully wired with real logic.

## Self-Check: PASSED

- `cka-sim/packs/services-networking/06-netpol-endport/grade.sh` FOUND
- `cka-sim/packs/services-networking/06-netpol-endport/reset.sh` FOUND
- Commit fda4dfb FOUND in git log
- Commit 3fb6c66 FOUND in git log
