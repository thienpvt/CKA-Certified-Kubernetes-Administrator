---
phase: 07.1-grading-honesty-rebuild
plan: 13
type: execute
wave: 6
depends_on: [07.1-08, 07.1-09, 07.1-10, 07.1-11, 07.1-12]
files_modified:
  - cka-sim/GRADING-HONESTY.md
  - cka-sim/AUTHORING.md
  - cka-sim/README.md
  - cka-sim/scripts/lint-packs.sh
  - .planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-VERIFICATION.md
autonomous: true
requirements: [DOC-05, CI-04]
must_haves:
  truths:
    - "GRADING-HONESTY.md documents the baselining contract: when to use each helper, namespace label-edit caveat, baseline lifecycle"
    - "AUTHORING.md cross-references GRADING-HONESTY.md in the grading section"
    - "README.md scoring-honesty caveat removed ONLY AFTER live UAT confirms 0/100 empty submission"
    - "lint-packs.sh reset-tmp-cleanup pass switched to enforcing mode"
    - "07.1-VERIFICATION.md contains goal-backward analysis + audit-escape inventory from Plans 08-12"
  artifacts:
    - path: cka-sim/GRADING-HONESTY.md
      provides: "Baselining contract documentation for question authors"
      min_lines: 60
    - path: cka-sim/AUTHORING.md
      provides: "Cross-reference to GRADING-HONESTY.md"
      contains: "GRADING-HONESTY.md"
    - path: cka-sim/README.md
      provides: "Scoring-honesty caveat removed"
    - path: .planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-VERIFICATION.md
      provides: "Goal-backward verification + audit-escape inventory"
      min_lines: 40
  key_links:
    - from: cka-sim/AUTHORING.md
      to: cka-sim/GRADING-HONESTY.md
      via: markdown cross-reference link
      pattern: 'GRADING-HONESTY.md'
    - from: 07.1-VERIFICATION.md
      to: Plans 08-12 SUMMARYs
      via: audit-escape inventory aggregation
      pattern: 'audit-escape'
---

<objective>
Ship documentation + verification. Write `cka-sim/GRADING-HONESTY.md` documenting the baselining contract for question authors. Cross-reference from AUTHORING.md. Remove the scoring-honesty caveat from README.md (gated on live UAT confirmation). Switch lint-packs.sh reset-tmp-cleanup pass to enforcing mode. Write 07.1-VERIFICATION.md with goal-backward analysis and audit-escape inventory.

Purpose: D-22 + DOC-05 -- the authoring contract must be documented so future question authors use the new helpers correctly. The README caveat removal signals the rebuild is complete. VERIFICATION.md closes the phase with a goal-backward analysis.

Output: GRADING-HONESTY.md + AUTHORING.md cross-ref + README edit + lint enforcement + VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-CONTEXT.md
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-RESEARCH.md
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-08-SUMMARY.md
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-09-SUMMARY.md
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-10-SUMMARY.md
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-11-SUMMARY.md
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-12-SUMMARY.md
@cka-sim/AUTHORING.md
@cka-sim/README.md
@cka-sim/scripts/lint-packs.sh
@cka-sim/lib/baseline.sh
@cka-sim/lib/grade.sh

<interfaces>
From RESEARCH Q1: namespace label-edit caveat -- `kubectl label namespace` does NOT bump generation; only resourceVersion changes. Document this so authors don't use assert_generation_delta_ge on namespaces.

From RESEARCH Q7: baseline lifecycle -- captured by runner after setup, deleted by reset.sh, mode 0444, /tmp/cka-sim/<slug>/baseline.json path, CKA_SIM_BASELINE_PATH env var.

From Plans 08-12 SUMMARYs: audit-escape inventory -- aggregate all D-22 flagged questions with their rationale (file-baseline gap, kubectl-stub limitation, pedagogy-thin).

From CONTEXT D-22: audit-escape questions get flagged in VERIFICATION.md, not fixed. Ship 07.1 with the other questions honest; audit-escape Qs tracked for v1.x.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write GRADING-HONESTY.md + AUTHORING.md cross-ref + README caveat removal</name>
  <read_first>
    - cka-sim/AUTHORING.md (locate the grading section to add cross-reference)
    - cka-sim/README.md (locate the scoring-honesty caveat to remove -- search for "scoring-honesty" or "caveat" or "grading" or "setup-state")
    - cka-sim/lib/baseline.sh (function signatures for documentation)
    - cka-sim/lib/grade.sh (3 new helper signatures for documentation)
    - .planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-RESEARCH.md Q1 (namespace label-edit caveat) + Q7 (lifecycle)
  </read_first>
  <action>
    Step 1 -- create cka-sim/GRADING-HONESTY.md. Structure:

    ```markdown
    # Grading Honesty Contract

    This document defines the baselining mechanism that ensures graders score
    candidate work, not setup state. All question authors MUST follow this contract.

    ## Baseline Lifecycle

    1. Runner calls `setup.sh` (creates lab namespace + seed resources).
    2. Runner sleeps 1s (absorbs controller status flap).
    3. Runner calls `cka_sim::baseline::capture "$CKA_SIM_LAB_NS"`.
    4. Baseline written to `/tmp/cka-sim/<question-slug>/baseline.json` (mode 0444).
    5. `CKA_SIM_BASELINE_PATH` exported to grader + trap detectors.
    6. Candidate works on cluster.
    7. Runner calls `grade.sh` (reads baseline via env var).
    8. `reset.sh` removes `/tmp/cka-sim/<question-slug>/` on cleanup.

    Authors do NOT call baseline logic from setup.sh -- the runner enforces it.

    ## Baseline Schema

    [lean JSON schema from RESEARCH Q2]

    ## Delta-Aware Assertion Helpers

    ### assert_changed_since_setup <kind> <name> [-n <ns>] [<weight>]
    Passes iff current generation > baseline.generation OR current resourceVersion != baseline.resourceVersion.
    Generation is checked FIRST (preferred signal). resourceVersion is the FALLBACK
    for kinds without generation (ConfigMap, Secret, Namespace labels).
    Use when: candidate EDITS an existing resource that setup created.

    ### assert_generation_delta_ge <kind> <name> <N> [-n <ns>] [<weight>]
    Passes iff (current.generation - baseline.generation) >= N.
    Use when: candidate must perform N spec-changing operations (e.g., rollout + rollback = delta 2).
    NOTE: fails if resource not in baseline (use assert_resource_candidate_authored instead).
    NOTE: fails if baseline.generation is null (kind has no generation -- use assert_changed_since_setup).

    ### assert_resource_candidate_authored <kind> <name> [-n <ns>] [<weight>]
    Passes iff resource exists in cluster AND was NOT present in baseline.
    Use when: candidate must CREATE a new resource.

    ## Caveats

    ### Namespace label edits
    `kubectl label namespace` does NOT bump .metadata.generation -- only resourceVersion changes.
    Do NOT use `assert_generation_delta_ge` on namespaces for label-edit detection.
    Use `assert_changed_since_setup namespace <name>` instead (relies on resourceVersion).

    ### ConfigMap / Secret
    These resources have no .spec -- generation stays at 1 forever.
    `assert_changed_since_setup` relies on resourceVersion only for these kinds.

    ### Back-compatibility
    If CKA_SIM_BASELINE_PATH is unset or the file is missing, all delta helpers FAIL
    (they cannot prove candidate work without a baseline). The `is_candidate_modified`
    helper returns 0 (fire freely) for back-compat with trap detectors.

    ## Reserved Paths

    `/tmp/cka-sim/<question-slug>/` is reserved for the runner. Do not write to this
    directory from setup.sh, grade.sh, or ref-solution.sh. reset.sh MUST `rm -rf` it.

    ## CI Enforcement

    `bash cka-sim/scripts/test.sh` runs per-question grading-honesty tests that assert:
    - Empty submission (post-setup state) scores 0/N.
    - Reference solution scores N/N.

    Test cases live at `cka-sim/tests/grading-honesty/<pack>__<slug>.sh`.
    Fixtures at `cka-sim/tests/fixtures/grading-honesty/<pack>__<slug>/`.
    ```

    Step 2 -- edit cka-sim/AUTHORING.md. Locate the grading section (search for "grade" or "assertion" or "grader"). Add a subsection or paragraph:
    ```
    ### Grading Honesty

    All graders MUST use the delta-aware assertion helpers to avoid scoring setup state.
    See [GRADING-HONESTY.md](GRADING-HONESTY.md) for the full baselining contract,
    helper API reference, and caveats.
    ```

    Step 3 -- edit cka-sim/README.md. README caveat removal is GATED on live UAT confirmation:
    - FIRST: Run `cka-sim exam blueprint-alpha` with immediate submission (or check if Plans 08-12 SUMMARYs confirm all grading-honesty tests pass with 0/N empty scores).
    - If live UAT has been confirmed (check 07.1-VERIFICATION.md or SUMMARY evidence): remove the scoring-honesty caveat paragraph/block entirely.
    - If live UAT has NOT yet been confirmed: replace the caveat text with a softer note: "Grading honesty rebuild (Phase 07.1) has shipped. CI-mocked tests confirm empty submissions score 0. Live-cluster UAT pending." This ensures the caveat is updated but not prematurely removed.
    - Search for "setup-state", "scoring", "honesty", "caveat", "grading" in README.md to locate the text.
  </action>
  <verify>
    <automated>test -f cka-sim/GRADING-HONESTY.md && grep -q "GRADING-HONESTY.md" cka-sim/AUTHORING.md && wc -l < cka-sim/GRADING-HONESTY.md | xargs test 60 -le</automated>
  </verify>
  <acceptance_criteria>
    - cka-sim/GRADING-HONESTY.md exists with >= 60 lines covering lifecycle, schema, 3 helpers, caveats, reserved paths, CI enforcement.
    - GRADING-HONESTY.md documents generation-first/rv-fallback behavior for assert_changed_since_setup.
    - cka-sim/AUTHORING.md contains a cross-reference to GRADING-HONESTY.md.
    - cka-sim/README.md scoring-honesty caveat is either removed (if live UAT confirmed) or softened to reflect Phase 07.1 shipped status (verify: `grep -ci "scoring-honesty\|setup-state.*caveat" cka-sim/README.md` == 0 OR caveat replaced with "Phase 07.1 shipped" note).
    - All 3 files pass basic markdown lint (no broken links within the repo).
  </acceptance_criteria>
  <done>Documentation shipped; README caveat handled (removed if UAT confirmed, softened otherwise).</done>
</task>

<task type="auto">
  <name>Task 2: Enforce lint-packs.sh reset-tmp-cleanup + write 07.1-VERIFICATION.md</name>
  <read_first>
    - cka-sim/scripts/lint-packs.sh (locate the ENFORCE_RESET_TMP_CLEANUP variable from Plan 02)
    - Plans 08-12 SUMMARYs (aggregate audit-escape inventory)
    - .planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-CONTEXT.md (success criteria for goal-backward)
    - .planning/ROADMAP.md Phase 07.1 (4 success criteria)
  </read_first>
  <action>
    Step 1 -- switch lint-packs.sh reset-tmp-cleanup pass to enforcing mode. Locate the `ENFORCE_RESET_TMP_CLEANUP=${ENFORCE_RESET_TMP_CLEANUP:-0}` line (Plan 02 added it) and change default to 1:
    `ENFORCE_RESET_TMP_CLEANUP=${ENFORCE_RESET_TMP_CLEANUP:-1}`

    Run `bash cka-sim/scripts/lint-packs.sh` -- must exit 0 (all reset.sh files now have the cleanup line from Plans 03-12).

    Step 2 -- write .planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-VERIFICATION.md. Structure:

    ```markdown
    # Phase 07.1: Grading Honesty Rebuild -- VERIFICATION

    ## Goal-Backward Analysis

    **Goal:** Empty exam submission of blueprint-alpha scores 0/100 across all 17 questions.

    ### Observable Truths
    1. Empty submission scores 0 raw points for every question in every pack.
    2. Reference-solution round-trip still passes for every question.
    3. CI gate fails the build if any question's empty-submission score is non-zero.
    4. GRADING-HONESTY.md documents the baselining contract.

    ### Verification Status

    | Truth | Status | Evidence |
    |-------|--------|----------|
    | 1. Empty = 0 | PASS (with audit-escape exceptions) | `bash cka-sim/scripts/test.sh` -- all grading-honesty cases green |
    | 2. Ref-solution = full | PASS (with audit-escape exceptions) | Same test suite verifies ref=max/max |
    | 3. CI gate | PASS | tests/grading-honesty/ walked by run.sh; bash-tests GHA job |
    | 4. Documentation | PASS | cka-sim/GRADING-HONESTY.md committed |

    ## ROADMAP Success Criteria

    | Criterion | Status |
    |-----------|--------|
    | Empty exam submission scores 0/100, 0 raw, 0 traps | PASS (live-cluster UAT pending; CI-mocked passes) |
    | CI gate fails on non-zero empty-submission | PASS |
    | Ref-solution round-trip passes everywhere | PASS (CI-mocked; live UAT pending) |
    | GRADING-HONESTY.md documents contract | PASS |

    ## Audit-Escape Inventory (D-22)

    [Aggregate from Plans 08-12 SUMMARYs -- list each audit-escape question with rationale]

    | Pack | Question | Rationale | v1.x Fix |
    |------|----------|-----------|----------|
    | cluster-architecture | 04-pss-enforce | Pedagogy-thin; all 5 original assertions are setup-state; added 1 candidate-authored Pod assertion | Rebuild question flow |
    | cluster-architecture | 02-etcd-backup-restore | File-baseline gap (D-03 tracks K8s API only) | Add file-mtime+sha256 to baseline schema |
    | cluster-architecture | 05-audit-policy | File-baseline gap | Same |
    | cluster-architecture | 07-cri-dockerd-endpoint | File-baseline gap | Same |
    | troubleshooting | 04-debug-node | File-baseline gap (answer.txt) | Same |
    | troubleshooting | 05-static-pod-manifest | File-baseline gap (node manifest) | Same |
    | troubleshooting | 06-broken-kubelet | File-baseline gap + SSH node-action | Same |
    | (others from Plans 08-10 if any) | ... | ... | ... |

    NOTE: Audit-escape questions have weight-0 setup-state assertions (score 0 on empty) but
    their positive-path scoring may be thin or absent. They pass the "empty=0" contract but
    may not fully validate candidate work. Tracked for v1.x scope expansion.

    ## Live-Cluster UAT (pending)

    After this phase ships, run on a live 1+2 cluster:
    ```bash
    cka-sim exam blueprint-alpha  # submit immediately without answering
    # Expected: SCORE: 0/100, 0 raw points, 0 traps
    ```

    This is the definitive success criterion. CI-mocked tests cover the structural contract;
    live UAT covers controller-timing edge cases the mocked tests cannot.

    README caveat removal is gated on this live UAT passing. Do NOT remove the caveat
    until `SCORE: 0/100` is confirmed on a real cluster.

    ## Phase Complete

    Phase 07.1 ships with:
    - 3 new assertion helpers + baseline capture library
    - 6 trap detectors ownership-gated
    - 4 confirmed offender fixes (Plans 03-06)
    - 30 audited questions (Plans 08-12) with audit-escape annotations
    - CI regression harness (Plan 07)
    - Documentation (Plan 13)
    ```

    Adjust the audit-escape table based on actual Plans 08-12 SUMMARY contents (read them during execution).
  </action>
  <verify>
    <automated>bash cka-sim/scripts/lint-packs.sh && test -f .planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - lint-packs.sh reset-tmp-cleanup pass is enforcing (default=1) and exits 0.
    - 07.1-VERIFICATION.md exists with goal-backward analysis, ROADMAP success criteria table, audit-escape inventory, and live-cluster UAT instructions.
    - 07.1-VERIFICATION.md explicitly states README caveat removal is gated on live UAT confirmation.
    - `bash cka-sim/scripts/lint-packs.sh` exits 0.
    - `bash cka-sim/scripts/test.sh` exits 0.
  </acceptance_criteria>
  <done>Lint enforcing; VERIFICATION.md documents phase completion + audit-escape inventory + UAT gate for README caveat.</done>
</task>

</tasks>

<verification>
- GRADING-HONESTY.md documents the full contract.
- AUTHORING.md cross-references it.
- README caveat handled (removed after UAT or softened pending UAT).
- lint-packs.sh enforces reset-tmp-cleanup.
- 07.1-VERIFICATION.md provides goal-backward closure.
</verification>

<success_criteria>
- Phase 07.1 documentation complete.
- All CI checks pass: `bash cka-sim/scripts/test.sh && bash cka-sim/scripts/lint-packs.sh && bash cka-sim/scripts/lint-traps.sh`.
- VERIFICATION.md committed with audit-escape inventory.
- README caveat removal gated on live UAT (not premature).
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-13-SUMMARY.md`.
</output>
