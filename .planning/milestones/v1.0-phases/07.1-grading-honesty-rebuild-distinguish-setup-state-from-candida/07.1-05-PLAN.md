---
phase: 07.1-grading-honesty-rebuild
plan: 05
type: execute
wave: 3
depends_on: [07.1-02]
files_modified:
  - cka-sim/packs/services-networking/06-netpol-endport/grade.sh
  - cka-sim/packs/services-networking/06-netpol-endport/reset.sh
autonomous: true
requirements: [FIX-01]
must_haves:
  truths:
    - "Empty submission of services-networking/06-netpol-endport scores 0/6 raw points"
    - "Reference solution (create NP q06-allow-range) still scores full points"
    - "Assertion 5 (reachability to :8085) only scores when candidate authored a NetworkPolicy"
    - "Default-allow reachability does NOT leak any score"
    - "reset.sh removes /tmp/cka-sim/06-netpol-endport/"
  artifacts:
    - path: cka-sim/packs/services-networking/06-netpol-endport/grade.sh
      provides: "Reachability assertion gated on assert_resource_candidate_authored networkpolicy q06-allow-range"
      contains: "assert_resource_candidate_authored networkpolicy q06-allow-range"
    - path: cka-sim/packs/services-networking/06-netpol-endport/reset.sh
      contains: "rm -rf /tmp/cka-sim"
  key_links:
    - from: grade.sh
      to: lib/grade.sh
      via: assert_resource_candidate_authored gate
      pattern: 'assert_resource_candidate_authored networkpolicy q06-allow-range'
---

<objective>
Apply RESEARCH Q4 fix to services-networking/06-netpol-endport. Assertion 5 (`kubectl exec q06-client -- wget q06-server:8085`) currently passes via default-allow when no NetworkPolicy exists — leaks 1 point on empty submission. Gate the reachability check on a candidate-authored NetworkPolicy.

Purpose: D-15 — UAT Test 12 reports this question scores 1/6 on empty submission (17% leak).

Output: Updated grade.sh with NetworkPolicy authorship gate before assertion 5; reset.sh wipes /tmp/cka-sim/06-netpol-endport/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-CONTEXT.md
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-RESEARCH.md
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-01-SUMMARY.md
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-02-SUMMARY.md
@cka-sim/packs/services-networking/06-netpol-endport/grade.sh
@cka-sim/packs/services-networking/06-netpol-endport/setup.sh
@cka-sim/packs/services-networking/06-netpol-endport/reset.sh
@cka-sim/packs/services-networking/06-netpol-endport/ref-solution.sh
@cka-sim/lib/grade.sh

<interfaces>
From Plan 01: `cka_sim::grade::assert_resource_candidate_authored <kind> <name> [-n <ns>] [<weight>]`. Pass weight=0 to use as a gate without scoring (still increments accumulator if weight>0; with weight=0 the accumulator stays put — VERIFY in Plan 01's helper that weight=0 is honored; if not, use direct call to `cka_sim::baseline::is_candidate_modified` instead and skip accumulator entirely).

Existing assertion 5 (lines 30-39 of grade.sh):
- Increments CKA_SIM_GRADE_TOTAL by 1.
- `kubectl exec q06-client -- wget q06-server:8085` → exit 0 = pass; non-zero = fail.

Setup behavior: creates ns + 2 pods (q06-client, q06-server) but does NOT create a NetworkPolicy. With no NP in the namespace, kubernetes default-allow lets the wget through → assertion 5 passes on empty submission.

Reference-solution behavior: creates NetworkPolicy q06-allow-range with port range 8080-8090 → reachability to :8085 (in range) passes; :8095 (out of range) blocked.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate assertion 5 on candidate-authored NetworkPolicy</name>
  <read_first>
    - cka-sim/packs/services-networking/06-netpol-endport/grade.sh (full file — locate assertion 5 block at lines 30-39)
    - cka-sim/packs/services-networking/06-netpol-endport/setup.sh (confirm NO NetworkPolicy in setup)
    - cka-sim/packs/services-networking/06-netpol-endport/ref-solution.sh (confirm NP q06-allow-range created)
    - cka-sim/lib/baseline.sh (Plan 01 — is_candidate_modified signature)
  </read_first>
  <action>
    Modify cka-sim/packs/services-networking/06-netpol-endport/grade.sh.

    Insert ABOVE assertion 5 (current line 30) a gate variable:
    ```
    # Phase 07.1 D-15 — gate reachability assertion on candidate-authored NetworkPolicy.
    # Without this gate, default-allow lets the wget through with no NP, leaking 1 point.
    np_authored=0
    if cka_sim::baseline::is_candidate_modified networkpolicy q06-allow-range -n "$CKA_SIM_LAB_NS"; then
      # is_candidate_modified returns 0 if NP is candidate-authored OR baseline missing (back-compat).
      # Verify the NP actually exists in the cluster — back-compat fires the gate but cluster may still be empty.
      if kubectl get networkpolicy q06-allow-range -n "$CKA_SIM_LAB_NS" -o name >/dev/null 2>&1; then
        np_authored=1
      fi
    fi
    ```

    Replace assertion 5 block (the `if kubectl exec ... wget ... q06-server:8085 ...` block) so the success path requires `np_authored == 1`. Keep the accumulator increment but the success branch becomes:
    ```
    CKA_SIM_GRADE_TOTAL=$(( CKA_SIM_GRADE_TOTAL + 1 ))
    if (( np_authored == 1 )) \
       && kubectl exec -n "$CKA_SIM_LAB_NS" q06-client -- wget -qO- --timeout=3 q06-server:8085 >/dev/null 2>&1; then
      CKA_SIM_GRADE_PASSED=$(( CKA_SIM_GRADE_PASSED + 1 ))
      CKA_SIM_GRADE_PASSES+=("q06-client can reach q06-server:8085 inside allowed endPort range (gated on candidate-authored NP)")
      ok "q06-client can reach q06-server:8085 inside allowed endPort range"
    else
      if (( np_authored == 0 )); then
        CKA_SIM_GRADE_FAILS+=("reachability check skipped — no candidate-authored NetworkPolicy q06-allow-range")
        err "reachability check skipped — no candidate-authored NetworkPolicy"
      else
        CKA_SIM_GRADE_FAILS+=("q06-client cannot reach q06-server:8085 inside allowed endPort range")
        err "q06-client cannot reach q06-server:8085 inside allowed endPort range"
      fi
    fi
    ```

    DO NOT modify assertion 6 (`q06-server:8095` should be unreachable). Per RESEARCH Q4: with no NP, default-allow makes :8095 reachable, which makes assertion 6 correctly FAIL on empty (the assertion wants the request blocked, gets it through, fails). Empty submission already scores 0 on assertion 6 — no fix needed.

    DO NOT modify assertions 1-4 (NP existence + jsonpath fields + protocol check) — all 4 already correctly fail on empty (no NP → kubectl returns empty → all fail).

    Trap detector block stays unchanged.
  </action>
  <verify>
    <automated>bash -n cka-sim/packs/services-networking/06-netpol-endport/grade.sh && grep -c 'np_authored' cka-sim/packs/services-networking/06-netpol-endport/grade.sh</automated>
  </verify>
  <acceptance_criteria>
    - grade.sh contains a `np_authored` gate variable assigned via `cka_sim::baseline::is_candidate_modified networkpolicy q06-allow-range -n "$CKA_SIM_LAB_NS"`.
    - The :8085 reachability assertion's success branch requires `np_authored == 1`.
    - bash -n passes.
    - assertions 1-4 (NP existence/fields/protocol) preserved unchanged.
    - assertion 6 (:8095 unreachability) preserved unchanged.
    - cka_sim::grade::emit_result is the last call.
  </acceptance_criteria>
  <done>Reachability scoring requires candidate-authored NetworkPolicy.</done>
</task>

<task type="auto">
  <name>Task 2: Add /tmp/cka-sim/<slug>/ cleanup to reset.sh</name>
  <read_first>
    - cka-sim/packs/services-networking/06-netpol-endport/reset.sh
  </read_first>
  <action>
    Append `rm -rf /tmp/cka-sim/06-netpol-endport/` to reset.sh. Literal slug, no variable expansion. Place before any conditional namespace-delete block.
  </action>
  <verify>
    <automated>bash -n cka-sim/packs/services-networking/06-netpol-endport/reset.sh && grep -c 'rm -rf /tmp/cka-sim/06-netpol-endport' cka-sim/packs/services-networking/06-netpol-endport/reset.sh</automated>
  </verify>
  <acceptance_criteria>
    - reset.sh contains `rm -rf /tmp/cka-sim/06-netpol-endport/` exactly once.
    - bash -n passes.
  </acceptance_criteria>
  <done>reset.sh wipes baseline dir.</done>
</task>

</tasks>

<verification>
- Reachability gated on candidate-authored NP.
- reset.sh /tmp cleanup added.
- Wave 4 fixture test will verify empty=0/6.
</verification>

<success_criteria>
- Empty submission predicted score: 0/6.
- Ref-solution predicted score: 6/6.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-05-SUMMARY.md`.
</output>
