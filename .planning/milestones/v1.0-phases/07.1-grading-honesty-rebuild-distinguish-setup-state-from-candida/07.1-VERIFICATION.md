# Phase 07.1: Grading Honesty Rebuild -- VERIFICATION

## Goal-Backward Analysis

**Goal:** Empty exam submission of blueprint-alpha scores 0/100 across all 17 questions.

### Observable Truths

1. Empty submission scores 0 raw points for every question in every pack.
2. Reference-solution round-trip still passes for every question.
3. CI gate fails the build if any question's empty-submission score is non-zero.
4. GRADING-HONESTY.md documents the baselining contract.

### Verification Status

| Truth | Status | Evidence |
|-------|--------|----------|
| 1. Empty = 0 | PASS (with audit-escape exceptions) | `bash cka-sim/scripts/test.sh` -- all grading-honesty cases green |
| 2. Ref-solution = full | PASS (with audit-escape exceptions) | Same test suite verifies ref=max/max |
| 3. CI gate | PASS | tests/grading-honesty/ walked by run.sh; bash-tests GHA job |
| 4. Documentation | PASS | cka-sim/GRADING-HONESTY.md committed |

## ROADMAP Success Criteria

| Criterion | Status |
|-----------|--------|
| Empty exam submission scores 0/100, 0 raw, 0 traps | PASS (live-cluster UAT pending; CI-mocked passes) |
| CI gate fails on non-zero empty-submission | PASS |
| Ref-solution round-trip passes everywhere | PASS (CI-mocked; live UAT pending) |
| GRADING-HONESTY.md documents contract | PASS |

## Audit-Escape Inventory (D-22)

Questions where the grading-honesty fix is non-mechanical due to limitations of
the K8s-API-only baseline mechanism. These pass the "empty=0" contract (setup-state
assertions demoted to weight 0) but their positive-path scoring may be thin or
rely on file-system state that the baseline cannot track.

| Pack | Question | Rationale | v1.x Fix |
|------|----------|-----------|----------|
| cluster-architecture | 04-pss-enforce | Pedagogy-thin; all 5 original assertions are setup-state; added 1 candidate-authored Pod assertion (Plan 06) | Rebuild question flow with file-baseline helper |
| cluster-architecture | 02-etcd-backup-restore | File-baseline gap (D-03 tracks K8s API only; etcd snapshot is a file) | Add file-mtime+sha256 to baseline schema |
| cluster-architecture | 05-audit-policy | File-baseline gap (audit-policy.yaml is a file on the node) | Same |
| cluster-architecture | 07-cri-dockerd-endpoint | File-baseline gap (kubelet flags file on node) | Same |
| troubleshooting | 04-debug-node | File-baseline gap (answer.txt written by candidate) | Same |
| troubleshooting | 05-static-pod-manifest | File-baseline gap (static pod manifest on node filesystem) | Same |
| troubleshooting | 06-broken-kubelet | File-baseline gap + SSH node-action (kubeadm-flags.env) | Same + SSH stub |

NOTE: Audit-escape questions have weight-0 setup-state assertions (score 0 on empty) but
their positive-path scoring may be thin or absent. They pass the "empty=0" contract but
may not fully validate candidate work. Tracked for v1.x scope expansion.

## Live-Cluster UAT (pending)

After this phase ships, run on a live 1+2 cluster:

```bash
cka-sim exam blueprint-alpha  # submit immediately without answering
# Expected: SCORE: 0/100, 0 raw points, 0 traps
```

This is the definitive success criterion. CI-mocked tests cover the structural contract;
live UAT covers controller-timing edge cases the mocked tests cannot.

README caveat removal is gated on this live UAT passing. Do NOT remove any future
scoring-honesty caveat until `SCORE: 0/100` is confirmed on a real cluster.

## Phase Complete

Phase 07.1 ships with:
- 3 new assertion helpers (`assert_changed_since_setup`, `assert_generation_delta_ge`, `assert_resource_candidate_authored`) + baseline capture library (`lib/baseline.sh`)
- 6 trap detectors ownership-gated via `cka_sim::baseline::is_candidate_modified`
- 4 confirmed offender fixes (Plans 03-06: workloads-scheduling/02, storage/02, services-networking/06, cluster-architecture/04)
- 30 audited questions across 5 packs (Plans 08-12) with audit-escape annotations
- CI regression harness (Plan 07): kubectl-stub fixture dispatch + per-question grading-honesty tests
- lint-packs.sh pass I enforcing reset.sh /tmp/cka-sim/ cleanup
- Documentation: GRADING-HONESTY.md + AUTHORING.md cross-reference (Plan 13)
