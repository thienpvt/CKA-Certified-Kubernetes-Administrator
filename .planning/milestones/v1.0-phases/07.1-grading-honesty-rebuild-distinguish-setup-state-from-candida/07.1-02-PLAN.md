---
phase: 07.1-grading-honesty-rebuild
plan: 02
type: tdd
wave: 2
depends_on: [07.1-01]
files_modified:
  - cka-sim/lib/traps.sh
  - cka-sim/lib/cmd/drill.sh
  - cka-sim/lib/cmd/exam.sh
  - cka-sim/scripts/lint-packs.sh
  - cka-sim/tests/cases/traps_ownership_gate.sh
  - cka-sim/tests/cases/baseline_runner_hook.sh
  - cka-sim/tests/fixtures/grading-honesty/ownership-gate/baseline.json
  - cka-sim/tests/fixtures/grading-honesty/ownership-gate/pod-setup-owned.json
  - cka-sim/tests/fixtures/grading-honesty/ownership-gate/pod-candidate-modified.json
autonomous: true
requirements: [TRAP-01, BASELINE-01]
must_haves:
  truths:
    - "Per-resource trap detectors gate on cka_sim::baseline::is_candidate_modified BEFORE firing"
    - "Q3 default-sa-used regression case: detector returns empty when called on setup-owned pod with unchanged baseline"
    - "Text-based detectors (5 setup-allowed) are unchanged -- no ownership gate added"
    - "drill.sh exports CKA_SIM_BASELINE_PATH after setup.sh and calls cka_sim::baseline::capture before prompt"
    - "exam.sh per-question setup_question() exports CKA_SIM_BASELINE_PATH and calls capture after setup.sh"
    - "exam.sh batch_grade loop re-exports per-question CKA_SIM_BASELINE_PATH before each grade.sh invocation"
    - "Every reset.sh removes /tmp/cka-sim/<question-slug>/ -- lint-packs.sh enforces this"
    - "Capture failure in runner (drill.sh or exam.sh) ABORTS the current question with clear error -- does not proceed with no baseline"
  artifacts:
    - path: cka-sim/lib/traps.sh
      provides: "6 per-resource detectors gate on baseline; 5 text-based detectors unchanged"
      contains: "cka_sim::baseline::is_candidate_modified"
    - path: cka-sim/lib/cmd/drill.sh
      provides: "Baseline-capture call between setup.sh and prompt"
      contains: "cka_sim::baseline::capture"
    - path: cka-sim/lib/cmd/exam.sh
      provides: "Baseline-capture call inside setup_question() + per-question re-export in batch_grade()"
    - path: cka-sim/scripts/lint-packs.sh
      provides: "Pass: every reset.sh contains rm -rf /tmp/cka-sim/<slug>/"
  key_links:
    - from: cka-sim/lib/traps.sh
      to: cka-sim/lib/baseline.sh
      via: source statement + is_candidate_modified call
      pattern: 'cka_sim::baseline::is_candidate_modified'
    - from: cka-sim/lib/cmd/drill.sh
      to: cka-sim/lib/baseline.sh
      via: source + capture call after bash setup.sh
      pattern: 'cka_sim::baseline::capture'
    - from: cka-sim/lib/cmd/exam.sh
      to: cka-sim/lib/baseline.sh
      via: source + capture call inside setup_question + re-export in batch_grade
      pattern: 'cka_sim::baseline::capture'
---

<objective>
Wire baseline into the runtime: refactor 6 per-resource trap detectors to gate on baseline ownership, hook baseline-capture into drill.sh and exam.sh after setup.sh returns, and add a lint check that every reset.sh wipes its /tmp/cka-sim/<slug>/ dir.

Purpose: Without this wiring, the new helpers from Plan 01 sit unused. After this plan, every grader run produces a baseline.json the new helpers + refactored detectors consume. Q3 `default-sa-used` regression on workloads-scheduling/02 closes here.

Output: 6 per-resource detector signatures gated on `cka_sim::baseline::is_candidate_modified`; drill.sh + exam.sh capture hooks; exam.sh batch_grade per-question baseline re-export; capture-failure abort logic; new lint check; 2 new test cases proving ownership gate works on hit/miss/back-compat fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-CONTEXT.md
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-RESEARCH.md
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-01-SUMMARY.md
@cka-sim/lib/traps.sh
@cka-sim/lib/cmd/drill.sh
@cka-sim/lib/cmd/exam.sh
@cka-sim/lib/baseline.sh
@cka-sim/scripts/lint-packs.sh

<interfaces>
From Plan 01:
- `cka_sim::baseline::capture <namespace>` writes /tmp/cka-sim/<slug>/baseline.json (caller exports CKA_SIM_BASELINE_PATH first). Exits non-zero on kubectl/jq failure (does NOT write empty baseline).
- `cka_sim::baseline::is_candidate_modified <kind> <name> [-n <ns>]` returns 0 on candidate-modified OR back-compat (no baseline); returns 1 on unchanged setup-state.

drill.sh anchor (lines 290-307):
- Line 290: `trap cka_sim::drill::cleanup EXIT`
- Line 295: `bash "$CKA_SIM_QUESTION_DIR/reset.sh"`
- Line 300: `cka_sim::drill::_wait_ns_gone "$CKA_SIM_LAB_NS" 60`
- Line 303: `bash "$CKA_SIM_QUESTION_DIR/setup.sh"`  <- BASELINE-CAPTURE INSERTS HERE
- Line 305: `info "step 3/4: prompt"`

exam.sh anchor (cka_sim::exam::setup_question lines 223-256):
- Line 244: `bash "$qdir/setup.sh" </dev/null || setup_rc=$?`  <- BASELINE-CAPTURE INSERTS HERE (after setup_rc check, before CKA_SIM_EXAM_SETUP_IDXS+=)
- Line 254: `CKA_SIM_EXAM_SETUP_IDXS+=("$idx")`

exam.sh batch_grade loop (cka_sim::exam::batch_grade lines 400-438):
- Line 409: `for (( i=0; i<CKA_SIM_EXAM_QUESTION_COUNT; i++ )); do`
- Line 420: `info "Grading Q$((i+1))..."`
- Line 421: `cka_sim::exam::export_lab_ns "$i"`  <- PER-QUESTION BASELINE RE-EXPORT INSERTS HERE
- Line 424: `bash "$qdir/grade.sh" </dev/null > "$tmp" 2>&1 || rc=$?`

Per-resource detectors needing ownership gate (RESEARCH Q6):
- detect_default_sa_used <ns> <pod>
- detect_missing_dns_egress <ns> <netpol>
- detect_hostpath_pv_without_nodeaffinity <pv>
- detect_rbac_viewer_role_mismatch <ns> <role>
- detect_service_label_mismatch <ns> <svc>
- detect_pvc_wrong_storageclass -- locate via `grep -n "detect_pvc_wrong_storageclass" cka-sim/lib/traps.sh` (Phase 4 addition); if it takes pvc subject, gate it.

Text-based detectors NOT to gate (5):
- detect_pss_error_string_mismatch <text>
- detect_psp_fictional_pod_label_exemption <text>
- detect_kubelet_runtime_flag_in_kubeconfig <text>
- detect_removed_container_runtime_flag <text>
- detect_as_flag_format_wrong <text>
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED -- write failing tests for ownership gate + runner hook</name>
  <read_first>
    - cka-sim/lib/traps.sh lines 167-345 (current detector signatures)
    - cka-sim/tests/cases/ (existing case file pattern; locate one that tests detect_default_sa_used)
    - cka-sim/tests/fixtures/grading-honesty/baseline-stub/baseline.json (Plan 01 fixture as template)
    - .planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-RESEARCH.md (Q6 ownership refactor pattern, Q7 lifecycle, D-12 Q3 regression)
  </read_first>
  <behavior>
    Test 1 (traps_ownership_gate.sh): cka_sim::trap::detect_default_sa_used "$ns" "$pod"
      - Case A (Q3 REGRESSION FIX): baseline.resource_list contains "pod/web-abc123"; baseline has pod/web-abc123 entry with gen=1 rv="100"; current pod fixture returns gen=1 rv="100" (unchanged) -> detector MUST return EMPTY (no trap-id echoed). This is the load-bearing case D-12 calls out.
      - Case B (candidate-modified): baseline same pod gen=1 rv="100"; current returns gen=1 rv="200" -> detector runs existing logic; if .spec.serviceAccountName == "default" -> echoes "default-sa-used".
      - Case C (candidate-authored): pod NOT in baseline.resource_list; current pod has default SA -> detector echoes "default-sa-used" (back-compat to candidate-authored case).
      - Case D (back-compat): unset CKA_SIM_BASELINE_PATH; detector behaves as before -- fires freely on default SA. This is the critical pitfall RESEARCH Q6 calls out.
    Test 2 (baseline_runner_hook.sh): smoke test that lib/cmd/drill.sh sources lib/baseline.sh and calls capture after setup.sh:
      - Static assertion: `grep -c 'source.*lib/baseline.sh' cka-sim/lib/cmd/drill.sh` == 1.
      - Static assertion: `grep -c 'cka_sim::baseline::capture' cka-sim/lib/cmd/drill.sh` >= 1.
      - Static assertion: `grep -c 'CKA_SIM_BASELINE_PATH' cka-sim/lib/cmd/drill.sh` >= 1 (env export).
      - Same three assertions for cka-sim/lib/cmd/exam.sh.
      - Sequence assertion (drill.sh): line containing `bash "$CKA_SIM_QUESTION_DIR/setup.sh"` MUST come BEFORE line containing `cka_sim::baseline::capture`. Use awk to compare line numbers.
      - Exam batch_grade assertion: `grep -A5 'Grading Q' cka-sim/lib/cmd/exam.sh | grep -c 'CKA_SIM_BASELINE_PATH'` >= 1 (proves per-question re-export exists in the grading loop).
  </behavior>
  <action>
    Create cka-sim/tests/cases/traps_ownership_gate.sh -- sources lib/traps.sh, sets CKA_SIM_BASELINE_PATH, CKA_SIM_TEST_FIXTURES_DIR, CKA_SIM_TEST_CURRENT per case. Uses inline JSON heredoc to write baseline.json into a temp file. Calls detect_default_sa_used and asserts stdout is empty (case A) or "default-sa-used" (cases B/C/D).
    Create cka-sim/tests/cases/baseline_runner_hook.sh -- pure static checks via grep + awk; no kubectl needed. Asserts source statements + sequence ordering in drill.sh and exam.sh. Also asserts that exam.sh batch_grade loop contains per-question CKA_SIM_BASELINE_PATH re-export.
    Create 3 fixture files under cka-sim/tests/fixtures/grading-honesty/ownership-gate/:
    - baseline.json -- minimal: {captured_at: 1747000000, lab_namespace: "test-ns", resources: [{kind: "Pod", name: "web-abc123", namespace: "test-ns", generation: 1, resourceVersion: "100", labels: {}}], resource_list: ["pod/web-abc123"]}
    - pod-setup-owned.json -- kubectl get pod web-abc123 fixture: {.metadata.name: "web-abc123", .metadata.generation: 1, .metadata.resourceVersion: "100", .spec.serviceAccountName: "default"}.
    - pod-candidate-modified.json -- same but resourceVersion: "200" (rv bumped).
    Run `bash cka-sim/scripts/test.sh` -- 2 NEW cases MUST FAIL (refactor not done; runner hooks not in place). RED state confirmed.
  </action>
  <verify>
    <automated>bash cka-sim/scripts/test.sh 2>&1 | grep -E "(traps_ownership_gate|baseline_runner_hook)" | grep -c "case failed" -- expect 2</automated>
  </verify>
  <acceptance_criteria>
    - 2 new test case files under cka-sim/tests/cases/.
    - 3 fixture files under cka-sim/tests/fixtures/grading-honesty/ownership-gate/.
    - `bash cka-sim/scripts/test.sh` exits non-zero with traps_ownership_gate + baseline_runner_hook listed as failed.
    - Existing test cases (including Plan 01's 4 cases) all still pass.
    - baseline_runner_hook.sh includes an assertion that exam.sh batch_grade loop re-exports CKA_SIM_BASELINE_PATH per question.
  </acceptance_criteria>
  <done>RED state -- 2 failing cases proving ownership gate + runner hooks don't exist yet.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN -- refactor 6 detectors + wire drill.sh and exam.sh + reset.sh lint</name>
  <read_first>
    - cka-sim/lib/traps.sh (full file -- locate all 11 detector definitions)
    - cka-sim/lib/cmd/drill.sh lines 280-310 (main() orchestration block)
    - cka-sim/lib/cmd/exam.sh lines 223-256 (setup_question function)
    - cka-sim/lib/cmd/exam.sh lines 400-438 (batch_grade function -- the end-of-exam grading loop)
    - cka-sim/lib/baseline.sh (Plan 01 deliverable -- capture + is_candidate_modified signatures)
    - cka-sim/scripts/lint-packs.sh (existing pass structure -- locate where to add new pass)
  </read_first>
  <action>
    Step 1 -- extend cka-sim/lib/traps.sh:
    Add `source "$CKA_SIM_ROOT/lib/baseline.sh"` after line 12 (after the existing log.sh source). Refactor 6 per-resource detectors to gate on baseline. Pattern (apply to each):
    ```
    cka_sim::trap::detect_default_sa_used() {
      local ns="${1:?detect_default_sa_used: namespace required}"
      local pod="${2:?detect_default_sa_used: pod name required}"
      cka_sim::baseline::is_candidate_modified pod "$pod" -n "$ns" || return 0
      # ...existing logic unchanged below this line...
    }
    ```
    Apply the same one-line gate (`cka_sim::baseline::is_candidate_modified <kind> <name> -n <ns> || return 0` for namespaced; `cka_sim::baseline::is_candidate_modified <kind> <name> || return 0` for cluster-scoped) to:
    - detect_default_sa_used -> kind=pod
    - detect_missing_dns_egress -> kind=networkpolicy
    - detect_hostpath_pv_without_nodeaffinity -> kind=pv (cluster-scoped, no -n flag)
    - detect_rbac_viewer_role_mismatch -> kind=role
    - detect_service_label_mismatch -> kind=service
    - detect_pvc_wrong_storageclass (if it exists with subject pvc; verify via grep first) -> kind=pvc
    DO NOT modify the 5 text-based detectors (pss-error-string-mismatch, psp-fictional-pod-label-exemption, kubelet-runtime-flag-in-kubeconfig, removed-container-runtime-flag, as-flag-format-wrong) -- they take <text> args and have no resource subject.

    Step 2 -- extend cka-sim/lib/cmd/drill.sh:
    After line 23 add `source "$CKA_SIM_ROOT/lib/baseline.sh"`.
    Insert AFTER `bash "$CKA_SIM_QUESTION_DIR/setup.sh"` (current line 303) and BEFORE `info "step 3/4: prompt"` (current line 305):
    ```
    # Baseline capture (Phase 07.1) -- runner-managed per CONTEXT D-01.
    # Setup-script authors do NOT call this; the runner enforces the contract once.
    CKA_SIM_BASELINE_PATH="/tmp/cka-sim/$CKA_SIM_QUESTION_ID/baseline.json"
    export CKA_SIM_BASELINE_PATH
    sleep 1  # absorb final controller status flap (RESEARCH Q1 mitigation)
    if ! cka_sim::baseline::capture "$CKA_SIM_LAB_NS"; then
      err "Baseline capture failed for $CKA_SIM_QUESTION_ID -- aborting question."
      err "Check kubectl connectivity and jq availability."
      die "Cannot proceed without baseline (grading-honesty contract requires it)."
    fi
    ```
    Update cleanup() (line 225-233) to `rm -rf "/tmp/cka-sim/${CKA_SIM_QUESTION_ID:-}/"` after the existing reset.sh run.

    Step 3 -- extend cka-sim/lib/cmd/exam.sh setup_question():
    After line 18-22 source block add `source "$CKA_SIM_ROOT/lib/baseline.sh"` (locate exact source-block area; if absent, add near top imports).
    Inside cka_sim::exam::setup_question (currently lines 223-256), AFTER the `bash "$qdir/setup.sh"` call (line 244) and the setup_rc check (lines 249-253), BEFORE `CKA_SIM_EXAM_SETUP_IDXS+=("$idx")` (line 254):
    ```
    # Baseline capture (Phase 07.1) -- per-question, runner-managed.
    local slug="${CKA_SIM_EXAM_QDIRS[$idx]##*/}"
    CKA_SIM_BASELINE_PATH="/tmp/cka-sim/$slug/baseline.json"
    export CKA_SIM_BASELINE_PATH
    sleep 1
    if ! cka_sim::baseline::capture "$CKA_SIM_LAB_NS"; then
      err "Q$((idx+1)) baseline capture failed -- aborting question setup."
      err "Grading-honesty contract requires a valid baseline. Check cluster connectivity."
      # Mark question as failed-setup so batch_grade skips it gracefully.
      CKA_SIM_EXAM_SETUP_FAILURES+=("$idx")
      return 1
    fi
    ```
    Sleep 1s is acceptable in exam mode per CONTEXT D-01 + RESEARCH Q1; setup_question already has its own kubectl wait calls inside setup.sh.

    Step 4 -- extend cka-sim/lib/cmd/exam.sh batch_grade() loop:
    The batch_grade function (lines 400-438) iterates all questions and calls `bash "$qdir/grade.sh"`. BEFORE each grade.sh invocation (after `cka_sim::exam::export_lab_ns "$i"` on line 421), insert per-question baseline path re-export:
    ```
    # Re-export per-question baseline path for grading (Phase 07.1).
    # Each question has its own baseline at /tmp/cka-sim/<slug>/baseline.json.
    local slug="${CKA_SIM_EXAM_QDIRS[$i]##*/}"
    CKA_SIM_BASELINE_PATH="/tmp/cka-sim/$slug/baseline.json"
    export CKA_SIM_BASELINE_PATH
    ```
    This ensures grade.sh (and the delta-aware helpers it calls) read the CORRECT per-question baseline, not a stale path from the last setup_question call. The path pattern `/tmp/cka-sim/<slug>/baseline.json` matches what setup_question wrote during capture.

    Step 5 -- extend cka-sim/scripts/lint-packs.sh:
    Add a new pass (locate the existing pass structure -- passes A through H from Phases 4-7). Add Pass I "reset.sh tmp cleanup":
    - For every reset.sh under cka-sim/packs/*/[0-9]*-*/, verify it contains `rm -rf /tmp/cka-sim/<slug>/` where <slug> matches the question dir basename.
    - Acceptable patterns: `rm -rf /tmp/cka-sim/$CKA_SIM_QUESTION_ID/`, `rm -rf "/tmp/cka-sim/<literal-slug>/"`, or any line matching `rm -rf .*/tmp/cka-sim/.+/`. Use `grep -lE 'rm[[:space:]]+-rf[[:space:]]+["]?(/tmp/)?[^[:space:]]*tmp/cka-sim/' "$reset_path"` per file.
    - Fail with explicit error message naming any reset.sh missing the cleanup.
    - To unblock initial rollout: this pass is ENFORCING-MODE FALSE for now (warn only). Wave 3 plans add the cleanup line per offender; Wave 5 audit plans add it for the rest. Set ENFORCE_RESET_TMP_CLEANUP=${ENFORCE_RESET_TMP_CLEANUP:-0} so CI can flip it on after Wave 5 lands.

    Run `bash cka-sim/scripts/test.sh` -- 2 RED tests MUST flip GREEN; ALL existing tests still pass.
  </action>
  <verify>
    <automated>bash cka-sim/scripts/test.sh && bash cka-sim/scripts/lint-packs.sh</automated>
  </verify>
  <acceptance_criteria>
    - cka-sim/lib/traps.sh -- 6 per-resource detectors call cka_sim::baseline::is_candidate_modified before existing logic (verify: `grep -B1 'is_candidate_modified' cka-sim/lib/traps.sh | grep -c 'detect_' >= 6`).
    - 5 text-based detectors UNCHANGED: `grep -A20 'detect_pss_error_string_mismatch\|detect_psp_fictional\|detect_kubelet_runtime\|detect_removed_container\|detect_as_flag_format' cka-sim/lib/traps.sh | grep -c 'is_candidate_modified'` == 0.
    - cka-sim/lib/cmd/drill.sh contains `cka_sim::baseline::capture` after `bash "$CKA_SIM_QUESTION_DIR/setup.sh"` (verify ordering via awk line-number compare).
    - cka-sim/lib/cmd/drill.sh aborts (die) on capture failure -- does NOT proceed with warn-only fallback.
    - cka-sim/lib/cmd/exam.sh setup_question contains `cka_sim::baseline::capture` after `bash "$qdir/setup.sh"` AND aborts question on capture failure (returns 1 / marks setup failure).
    - cka-sim/lib/cmd/exam.sh batch_grade loop re-exports CKA_SIM_BASELINE_PATH per question BEFORE `bash "$qdir/grade.sh"` (verify: the slug-derivation + export appears between `export_lab_ns` and `grade.sh` invocation in the for-loop body).
    - cka-sim/scripts/lint-packs.sh contains a pass that walks reset.sh files for /tmp/cka-sim/ cleanup.
    - `bash cka-sim/scripts/test.sh` exits 0 (4 Plan-01 + 2 Plan-02 + N existing all green).
    - Existing 11/12 Phase 7 UAT functionality not regressed: drill.sh + exam.sh sequence preserved.
  </acceptance_criteria>
  <done>GREEN state -- Q3 default-sa-used regression closed, runner hooks live, capture-failure aborts question, exam batch_grade re-exports per-question baseline, lint pass added (warn-only).</done>
</task>

</tasks>

<verification>
- 6 per-resource trap detectors call is_candidate_modified.
- 5 text-based detectors unchanged.
- drill.sh + exam.sh capture baseline after setup.
- drill.sh dies on capture failure; exam.sh marks question as setup-failed on capture failure.
- exam.sh batch_grade re-exports CKA_SIM_BASELINE_PATH per question before each grade.sh call.
- lint-packs.sh has new reset-tmp-cleanup pass.
- All test cases pass; no regression.
</verification>

<success_criteria>
- `bash cka-sim/scripts/test.sh` exits 0.
- `bash cka-sim/scripts/lint-packs.sh` exits 0 (warn mode for reset cleanup).
- Manual check: drill.sh sets CKA_SIM_BASELINE_PATH; reset.sh /tmp/ cleanup will be enforced after Wave 5.
- Capture failure = question abort in both drill and exam mode (not degrade-to-no-baseline).
- Exam batch grading uses correct per-question baseline path for each grade.sh invocation.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-02-SUMMARY.md`.
</output>
