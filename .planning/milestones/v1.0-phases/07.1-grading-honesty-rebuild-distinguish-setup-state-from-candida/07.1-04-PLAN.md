---
phase: 07.1-grading-honesty-rebuild
plan: 04
type: execute
wave: 3
depends_on: [07.1-02]
files_modified:
  - cka-sim/packs/storage/02-storageclass-dynamic/grade.sh
  - cka-sim/packs/storage/02-storageclass-dynamic/reset.sh
autonomous: true
requirements: [FIX-01]
must_haves:
  truths:
    - "Empty submission of storage/02-storageclass-dynamic scores 0/3 raw points"
    - "Reference solution (create SC fast-ssd) still scores 3/3"
    - "Assertion that PVC.spec.storageClassName == fast-ssd is gated on candidate-authored StorageClass"
    - "reset.sh removes /tmp/cka-sim/02-storageclass-dynamic/"
  artifacts:
    - path: cka-sim/packs/storage/02-storageclass-dynamic/grade.sh
      provides: "Honest grading: SC must be candidate-authored before field-equality assertion counts"
      contains: "cka_sim::grade::assert_resource_candidate_authored storageclass fast-ssd"
    - path: cka-sim/packs/storage/02-storageclass-dynamic/reset.sh
      contains: "rm -rf /tmp/cka-sim"
  key_links:
    - from: grade.sh
      to: lib/grade.sh
      via: assert_resource_candidate_authored call
      pattern: 'assert_resource_candidate_authored storageclass fast-ssd'
---

<objective>
Apply RESEARCH Q4 fix to storage/02-storageclass-dynamic. Setup writes `pvc.spec.storageClassName == fast-ssd` verbatim, so the existing assertion 3 leaks 1 point on empty submission. Replace with `assert_resource_candidate_authored storageclass fast-ssd` — candidate must create the StorageClass for any score on this question.

Purpose: D-14 — UAT Test 12 reports this question scores 1/3 on empty submission (33% leak).

Output: Updated grade.sh with candidate-authored SC assertion replacing the field-equality leak; reset.sh wipes /tmp/cka-sim/02-storageclass-dynamic/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-CONTEXT.md
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-RESEARCH.md
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-01-SUMMARY.md
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-02-SUMMARY.md
@cka-sim/packs/storage/02-storageclass-dynamic/grade.sh
@cka-sim/packs/storage/02-storageclass-dynamic/setup.sh
@cka-sim/packs/storage/02-storageclass-dynamic/reset.sh
@cka-sim/packs/storage/02-storageclass-dynamic/ref-solution.sh
@cka-sim/lib/grade.sh

<interfaces>
From Plan 01: `cka_sim::grade::assert_resource_candidate_authored <kind> <name> [-n <ns>] [<weight>]` — passes iff <kind>/<name> exists in current cluster AND NOT in baseline.resource_list.

Setup behavior (storage/02-storageclass-dynamic/setup.sh): creates ns + applies a PVC `app-cache` with `.spec.storageClassName = fast-ssd` but does NOT create the StorageClass. PVC stays Pending until candidate creates SC fast-ssd.

Existing assertions (grade.sh):
- A1: `assert_resource_exists storageclass fast-ssd` — already correctly fails on empty (cluster-scoped, candidate-authored only).
- A2: `assert_pvc_bound app-cache` — already correctly fails on empty (no SC → PVC Pending).
- A3 (LEAKS): `assert_field_eq pvc app-cache '{.spec.storageClassName}' 'fast-ssd'` — setup writes this field, so passes on empty.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace assertion 3 with candidate-authored SC check</name>
  <read_first>
    - cka-sim/packs/storage/02-storageclass-dynamic/grade.sh (current 3 assertions + trap detector)
    - cka-sim/packs/storage/02-storageclass-dynamic/setup.sh (confirm SC NOT created in setup)
    - cka-sim/packs/storage/02-storageclass-dynamic/ref-solution.sh (confirm ref-solution creates SC fast-ssd)
  </read_first>
  <action>
    Replace assertion 3 in cka-sim/packs/storage/02-storageclass-dynamic/grade.sh (currently lines 26-29):

    BEFORE: `cka_sim::grade::assert_field_eq pvc app-cache '{.spec.storageClassName}' 'fast-ssd' -n "$CKA_SIM_LAB_NS"`

    AFTER: `cka_sim::grade::assert_resource_candidate_authored storageclass fast-ssd`

    The new assertion captures the same intent (PVC binds to fast-ssd) plus the missing requirement (candidate created the SC). Per RESEARCH Q4: setup does NOT create the SC, so this assertion correctly fails on empty submission AND passes when ref-solution creates the SC.

    Note: A1 (`assert_resource_exists storageclass fast-ssd`) is now structurally redundant with A3 — both pass iff the SC exists. Per RESEARCH Q4 recommendation keep A1 for now (existence-only, acceptable redundancy with the candidate-authored gate). DO NOT remove A1.

    Add a comment above A3: "Phase 07.1 D-14 — was assert_field_eq on storageClassName; setup wrote that verbatim, leaking 1 point. Now requires candidate-authored StorageClass."

    Trap detector block (`detect_pvc_wrong_storageclass` if present in this file's footer): leave footer unchanged. Plan 02's refactor handles ownership gating at the detector level.

    `cka_sim::grade::emit_result` stays as last call.
  </action>
  <verify>
    <automated>bash -n cka-sim/packs/storage/02-storageclass-dynamic/grade.sh</automated>
  </verify>
  <acceptance_criteria>
    - grade.sh contains `assert_resource_candidate_authored storageclass fast-ssd` exactly once.
    - grade.sh does NOT contain `assert_field_eq pvc app-cache '{.spec.storageClassName}'` (assertion 3 replaced).
    - bash -n grade.sh passes.
    - A1 (assert_resource_exists storageclass fast-ssd) and A2 (assert_pvc_bound) preserved.
    - cka_sim::grade::emit_result is the last non-blank line.
  </acceptance_criteria>
  <done>grade.sh requires candidate-authored StorageClass for the third scoring assertion.</done>
</task>

<task type="auto">
  <name>Task 2: Add /tmp/cka-sim/<slug>/ cleanup to reset.sh</name>
  <read_first>
    - cka-sim/packs/storage/02-storageclass-dynamic/reset.sh
  </read_first>
  <action>
    Append the line `rm -rf /tmp/cka-sim/02-storageclass-dynamic/` to reset.sh (literal slug, NOT a variable). Place before the namespace-delete block to maintain idempotency. Pattern matches Plan 02's lint-packs.sh Pass I expectation.
  </action>
  <verify>
    <automated>bash -n cka-sim/packs/storage/02-storageclass-dynamic/reset.sh && grep -c 'rm -rf /tmp/cka-sim/02-storageclass-dynamic' cka-sim/packs/storage/02-storageclass-dynamic/reset.sh</automated>
  </verify>
  <acceptance_criteria>
    - reset.sh contains `rm -rf /tmp/cka-sim/02-storageclass-dynamic/` exactly once.
    - bash -n passes.
  </acceptance_criteria>
  <done>reset.sh wipes baseline dir on cleanup.</done>
</task>

</tasks>

<verification>
- grade.sh now requires candidate-authored SC.
- reset.sh /tmp cleanup added.
- Wave 4 fixture-based test will verify empty=0, ref-solution=3.
</verification>

<success_criteria>
- Empty submission predicted score: 0/3.
- Ref-solution predicted score: 3/3.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-04-SUMMARY.md`.
</output>
