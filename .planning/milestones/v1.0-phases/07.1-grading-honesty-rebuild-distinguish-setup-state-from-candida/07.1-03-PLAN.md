---
phase: 07.1-grading-honesty-rebuild
plan: 03
type: execute
wave: 3
depends_on: [07.1-02]
files_modified:
  - cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/grade.sh
  - cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/reset.sh
autonomous: true
requirements: [FIX-01]
must_haves:
  truths:
    - "Empty submission of workloads-scheduling/02-rolling-update-rollback scores 0/N raw points (all setup-state assertions either removed or weight=0)"
    - "Reference solution (rollout forward + rollback) still scores N/N — happy path preserved"
    - "Generation-delta assertion fires only when candidate has actually rolled forward AND back (delta >= 2)"
    - "reset.sh removes /tmp/cka-sim/02-rolling-update-rollback/ on every run"
  artifacts:
    - path: cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/grade.sh
      provides: "Honest grading: only delta-assertion (and trap detector) score points"
      contains: "cka_sim::grade::assert_generation_delta_ge"
    - path: cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/reset.sh
      provides: "/tmp/cka-sim/<slug>/ cleanup"
      contains: "rm -rf /tmp/cka-sim"
  key_links:
    - from: grade.sh
      to: lib/grade.sh
      via: assert_generation_delta_ge call
      pattern: 'cka_sim::grade::assert_generation_delta_ge deployment web 2'
---

<objective>
Apply RESEARCH Q4 fix to workloads-scheduling/02-rolling-update-rollback. Replace setup-state assertions (gen >= 3, image == nginx:1.25, rollout status, resource_exists) with a single delta-aware assertion: `assert_generation_delta_ge deployment web 2`. Setup leaves gen at 3; candidate's rollout-forward + rollback must add at least 2 more.

Purpose: D-13 — UAT Test 12 reports this question scores 4/4 on empty submission (100% leak). Most egregious offender; canonical Wave 3 reference pattern.

Output: Updated grade.sh with one delta-aware scoring assertion (plus weight-0 preconditions); reset.sh wipes /tmp/cka-sim/02-rolling-update-rollback/ for baseline cleanup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-CONTEXT.md
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-RESEARCH.md
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-01-SUMMARY.md
@.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-02-SUMMARY.md
@cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/grade.sh
@cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/setup.sh
@cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/reset.sh
@cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/ref-solution.sh
@cka-sim/lib/grade.sh

<interfaces>
From Plan 01: `cka_sim::grade::assert_generation_delta_ge <kind> <name> <N> [-n <ns>] [<weight>]` — passes iff (current.generation - baseline.generation) >= N. If resource not in baseline, FAILS (per D-07).

Setup behavior (lines 20-65 of setup.sh):
- Step 2: `kubectl apply` initial Deployment → generation = 1.
- Step 3: `kubectl annotate ... change-cause=...` → generation stays at 1 (annotation is metadata-only).
- Step 3: `kubectl patch ... -p '{"spec":{"template":{"metadata":{"annotations":{"cka-sim/seeded-at":...}}}}}'` → strategic-merge to .spec.template → generation bumps to 2.
- Setup leaves Deployment at generation = 2 (not 3 — UAT description was approximate; verify post-Plan-02 with live cluster but the threshold logic is the same).

Reference-solution behavior (read ref-solution.sh):
- `kubectl set image deployment/web app=nginx:1.27` → bumps generation to 3.
- `kubectl rollout status` waits for rollout.
- `kubectl rollout undo deployment/web` → bumps generation to 4.
- Final delta vs setup baseline = 4 - 2 = 2.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace 4 setup-state assertions with 1 delta-aware assertion</name>
  <read_first>
    - cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/grade.sh (current 4 assertions)
    - cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/setup.sh (verify generation count after setup)
    - cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/ref-solution.sh (verify candidate path bumps generation)
    - cka-sim/lib/grade.sh (assert_generation_delta_ge signature from Plan 01)
  </read_first>
  <action>
    Rewrite cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/grade.sh keeping the existing header (set -uo pipefail, env guards, source statements). Replace the assertion block (lines 13-44) with this exact structure (per RESEARCH Q4 fix recommendation):

    1. KEEP assertion 1 (`assert_resource_exists deployment web -n "$CKA_SIM_LAB_NS"`) but pass weight=0 — purely a precondition for downstream messages, no points awarded.
    2. KEEP assertion 2 (rollout status check) but explicitly increment ONLY messaging arrays, NOT the accumulator — convert the inline kubectl rollout status block to a precondition that prints ok/err to stderr without touching CKA_SIM_GRADE_TOTAL/PASSED. Pattern: wrap in `if kubectl rollout status ... ; then ok "..."; else err "..."; fi` with NO accumulator math.
    3. REMOVE assertion 3 entirely (image == nginx:1.25 is structurally tautological per RESEARCH Q4-(b) — the rollback returns to nginx:1.25 which == initial state; cannot detect from final state alone).
    4. REPLACE assertion 4 (inline gen >= 3) with: `cka_sim::grade::assert_generation_delta_ge deployment web 2 -n "$CKA_SIM_LAB_NS"`. Threshold 2 = candidate must execute at least one rollout-forward (gen+1) AND one rollout-undo (gen+1) for delta to reach 2. The 2-threshold is robust against setup variance (setup leaves gen at 2; ref-solution moves it to 4).
    5. Trap detector block (lines 46-51 — `detect_default_sa_used` on first running pod) — KEEP unchanged. Plan 02's refactor makes this detector return empty when called on setup-owned unchanged pods (D-12 regression closes here automatically).
    6. KEEP `cka_sim::grade::emit_result` finalizer at the end.

    Add a comment block above the new assertion noting "Phase 07.1 D-13 — score only the candidate-driven generation delta; preconditions are weight-0 informational checks."
  </action>
  <verify>
    <automated>bash -n cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/grade.sh && grep -c 'assert_generation_delta_ge deployment web 2' cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/grade.sh</automated>
  </verify>
  <acceptance_criteria>
    - grade.sh contains `cka_sim::grade::assert_generation_delta_ge deployment web 2 -n "$CKA_SIM_LAB_NS"` exactly once.
    - grade.sh does NOT contain `assert_field_eq deployment web '{.spec.template.spec.containers[0].image}' 'nginx:1.25'` (assertion 3 removed).
    - grade.sh does NOT contain a `(( gen >= 3 ))` numeric threshold (old assertion 4 replaced).
    - bash -n grade.sh passes (syntactically valid).
    - The accumulator-incrementing block from old assertion 2 (lines 17-25) is removed or refactored to NOT touch CKA_SIM_GRADE_TOTAL/PASSED.
    - Trap detector block (detect_default_sa_used) preserved unchanged.
    - `cka_sim::grade::emit_result` is the last non-blank line.
  </acceptance_criteria>
  <done>grade.sh awards points only for candidate-driven generation delta; setup-state assertions demoted or removed.</done>
</task>

<task type="auto">
  <name>Task 2: Add /tmp/cka-sim/<slug>/ cleanup to reset.sh</name>
  <read_first>
    - cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/reset.sh (current cleanup actions)
  </read_first>
  <action>
    Append (or insert before the namespace-delete block, whichever is more idempotent — verify by reading the file) the line:
    ```
    rm -rf /tmp/cka-sim/02-rolling-update-rollback/
    ```
    Use the literal slug `02-rolling-update-rollback` (the question's dir basename) NOT a variable expansion — reset.sh runs in cleanup contexts where CKA_SIM_QUESTION_ID may be unset. Pattern matches Plan 02's lint-packs.sh Pass I expectation.
  </action>
  <verify>
    <automated>bash -n cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/reset.sh && grep -c 'rm -rf /tmp/cka-sim/02-rolling-update-rollback' cka-sim/packs/workloads-scheduling/02-rolling-update-rollback/reset.sh</automated>
  </verify>
  <acceptance_criteria>
    - reset.sh contains `rm -rf /tmp/cka-sim/02-rolling-update-rollback/` exactly once.
    - bash -n passes.
    - reset.sh remains idempotent (no failure on second run; rm -rf is idempotent by nature).
  </acceptance_criteria>
  <done>reset.sh wipes /tmp/cka-sim/<slug>/ on every run, preventing baseline poisoning across drills.</done>
</task>

</tasks>

<verification>
- grade.sh awards 1 point on candidate-authored delta only.
- reset.sh /tmp cleanup added.
- Wave 4 fixture-based test will verify empty submission scores 0; ref-solution scores 1.
</verification>

<success_criteria>
- Empty submission predicted score: 0/1.
- Ref-solution predicted score: 1/1.
- No live-cluster verification in this plan; Wave 4 fixture tests cover automated regression.
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-grading-honesty-rebuild-distinguish-setup-state-from-candida/07.1-03-SUMMARY.md`.
</output>
