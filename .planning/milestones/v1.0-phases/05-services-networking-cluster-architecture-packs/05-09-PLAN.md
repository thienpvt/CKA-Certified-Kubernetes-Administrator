---
phase: 05-services-networking-cluster-architecture-packs
plan: 09
type: execute
wave: 2
depends_on: [05-01, 05-08]
files_modified:
  - cka-sim/packs/cluster-architecture/02-etcd-backup-restore/setup.sh
  - cka-sim/packs/cluster-architecture/02-etcd-backup-restore/grade.sh
  - cka-sim/packs/cluster-architecture/02-etcd-backup-restore/reset.sh
  - cka-sim/packs/cluster-architecture/02-etcd-backup-restore/ref-solution.sh
  - cka-sim/packs/cluster-architecture/02-etcd-backup-restore/metadata.yaml
  - cka-sim/packs/cluster-architecture/02-etcd-backup-restore/question.md
  - cka-sim/packs/cluster-architecture/manifest.yaml
  - cka-sim/packs/cluster-architecture/coverage.yaml
  - cka-sim/packs/cluster-architecture/README.md
  - cka-sim/tests/fixtures/phase-05/cluster-architecture/02-etcd-backup-restore/stub-responses.json
  - cka-sim/tests/fixtures/phase-05/cluster-architecture/02-etcd-backup-restore/expected-fail-score.txt
  - cka-sim/tests/fixtures/phase-05/cluster-architecture/02-etcd-backup-restore/expected-pass-score.txt
autonomous: true
requirements: [PACK-04, PACK-06, PACK-07]
tags: [cluster-architecture, etcd, backup-restore]
must_haves:
  truths:
    - "setup creates sandbox /tmp/q02-etcd-backup/ with sentinel .cka-sim-sentinel; NEVER writes to /var/lib/etcd"
    - "setup seeds a wrong-state target-dir marker /tmp/q02-etcd-backup/restored-data/.pre-existing so fail path is observable"
    - "grade asserts snapshot.db exists non-empty; etcdutl snapshot status exits 0; restored-data/member/wal/ dir exists"
    - "grade records trap etcd-snapshot-without-env-set when ref-solution (candidate's apply-script.sh) lacks ETCDCTL_API=3 export"
    - "grade records trap etcd-restore-wrong-data-dir when restore targets /var/lib/etcd OR --data-dir flag is missing from candidate's script"
    - "ref-solution: ETCDCTL_API=3 etcdctl snapshot save to /tmp/q02-etcd-backup/snapshot.db with kubeadm CA/cert/key paths; etcdutl snapshot restore --data-dir=/tmp/q02-etcd-backup/restored-data"
    - "reset cleans /tmp/q02-etcd-backup sentinel-guarded"
  artifacts:
    - path: "cka-sim/packs/cluster-architecture/02-etcd-backup-restore/ref-solution.sh"
      provides: "Sandbox etcd snapshot + etcdutl restore"
      contains: "ETCDCTL_API=3"
      contains_extra: "etcdutl snapshot restore"
    - path: "cka-sim/packs/cluster-architecture/02-etcd-backup-restore/grade.sh"
      provides: "Snapshot magic + restored data-dir assertions"
      contains: "snapshot.db"
  key_links:
    - from: "cka-sim/packs/cluster-architecture/02-etcd-backup-restore/ref-solution.sh"
      to: "live etcd (read-only via snapshot save)"
      via: "etcdctl snapshot save (never touches /var/lib/etcd)"
      pattern: "snapshot save"
---

<objective>
Cluster Architecture Q02 — etcd snapshot save + etcdutl restore drill. 10 minutes. Traps: `etcd-snapshot-without-env-set` + `etcd-restore-wrong-data-dir` (added in P01).

Per RESEARCH §7: snapshot save is read-only on live etcd; restore writes to a sandbox data-dir. Candidate writes `apply-script.sh` (not executed live — structural grading). Grader checks candidate script content + snapshot artifact integrity.

Output: 6 question files + 3 fixtures + 3 sentinel appends.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-services-networking-cluster-architecture-packs/05-RESEARCH.md
@cka-sim/lib/setup.sh
@cka-sim/traps/catalog.yaml
</context>

<interfaces>
Lab ns: `cka-sim-cluster-architecture-02`. Sandbox: `/tmp/q02-etcd-backup/` sentinel-guarded. kubeadm etcd paths: `/etc/kubernetes/pki/etcd/ca.crt`, `/etc/kubernetes/pki/etcd/server.crt`, `/etc/kubernetes/pki/etcd/server.key`, endpoint `https://127.0.0.1:2379`.

etcdutl split from etcdctl in etcd 3.5+ — RESEARCH §14 bullet: detect binary in setup, warn if missing. v1.35 kubeadm ships etcd 3.5+, so etcdutl is present.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 6 files + 3 fixtures + sentinel appends (sandbox-based)</name>
  <files>cka-sim/packs/cluster-architecture/02-etcd-backup-restore/metadata.yaml, cka-sim/packs/cluster-architecture/02-etcd-backup-restore/question.md, cka-sim/packs/cluster-architecture/02-etcd-backup-restore/setup.sh, cka-sim/packs/cluster-architecture/02-etcd-backup-restore/grade.sh, cka-sim/packs/cluster-architecture/02-etcd-backup-restore/reset.sh, cka-sim/packs/cluster-architecture/02-etcd-backup-restore/ref-solution.sh, cka-sim/tests/fixtures/phase-05/cluster-architecture/02-etcd-backup-restore/stub-responses.json, cka-sim/tests/fixtures/phase-05/cluster-architecture/02-etcd-backup-restore/expected-fail-score.txt, cka-sim/tests/fixtures/phase-05/cluster-architecture/02-etcd-backup-restore/expected-pass-score.txt, cka-sim/packs/cluster-architecture/manifest.yaml, cka-sim/packs/cluster-architecture/coverage.yaml, cka-sim/packs/cluster-architecture/README.md</files>
  <read_first>
    - .planning/phases/05-services-networking-cluster-architecture-packs/05-RESEARCH.md (§7 lines 252-292 — save + restore commands + grader assertions + trap fire conditions; §14 etcdutl binary detection)
    - cka-sim/lib/setup.sh (ensure_lab_ns + wait_for_ns_active)
    - cka-sim/packs/workloads-scheduling/06-static-pod (sandbox sentinel pattern proven in Phase 4)
  </read_first>
  <action>
    **setup.sh**: source lib/setup.sh, ensure_lab_ns, wait_for_ns_active. Create `/tmp/q02-etcd-backup/` + `touch .cka-sim-sentinel`. Pre-detect etcdutl presence: `command -v etcdutl >/dev/null 2>&1 || warn "etcdutl not present — install etcd 3.5+ binaries"`. Pre-seed a stub `apply-script.sh` with placeholder lines containing the WRONG form (candidate overwrites):
    ```
    # TODO: snapshot save (replace with correct command)
    # TODO: restore to sandbox data-dir
    ```

    **ref-solution.sh**: set `ETCDCTL_API=3` (export); run:
    ```
    ETCDCTL_API=3 etcdctl \
      --endpoints=https://127.0.0.1:2379 \
      --cacert=/etc/kubernetes/pki/etcd/ca.crt \
      --cert=/etc/kubernetes/pki/etcd/server.crt \
      --key=/etc/kubernetes/pki/etcd/server.key \
      snapshot save /tmp/q02-etcd-backup/snapshot.db
    etcdutl snapshot restore /tmp/q02-etcd-backup/snapshot.db \
      --data-dir=/tmp/q02-etcd-backup/restored-data
    ```
    Also write the exact commands into `/tmp/q02-etcd-backup/apply-script.sh` (candidate's expected output for grading).

    **grade.sh**: `set -uo pipefail`; source lib/grade.sh + lib/traps.sh.
    - Assert snapshot.db exists non-empty: `[[ -s /tmp/q02-etcd-backup/snapshot.db ]]`; TOTAL++/PASSED++/err
    - Integrity: `etcdutl snapshot status /tmp/q02-etcd-backup/snapshot.db --write-out=table >/dev/null 2>&1`
    - Restored wal dir: `[[ -d /tmp/q02-etcd-backup/restored-data/member/wal ]]`
    - Candidate script contains `ETCDCTL_API=3`: if not, `record_trap etcd-snapshot-without-env-set`
    - Candidate script restore targets `--data-dir=/tmp/q02-etcd-backup/restored-data` (NOT `/var/lib/etcd`): if violates, `record_trap etcd-restore-wrong-data-dir`
    - emit_result

    Grader is read-only relative to cluster — runs etcdutl against sandbox files only.

    **reset.sh**: sentinel-guarded `rm -rf /tmp/q02-etcd-backup` if `.cka-sim-sentinel` present; `kubectl delete namespace $ns --ignore-not-found --wait=false`. NEVER touches /var/lib/etcd.

    **metadata.yaml**: `id: cluster-architecture-etcd-backup-restore`, `domain: cluster-architecture`, `estimatedMinutes: 10`, `verified_against: "1.35"`, `traps: [etcd-snapshot-without-env-set, etcd-restore-wrong-data-dir, default-sa-used]`, references: k8s-doc configure-upgrade-etcd.

    **question.md**: scenario — cluster operator needs a verified etcd backup. Task: take an etcd snapshot of the live control-plane etcd, write it to `/tmp/q02-etcd-backup/snapshot.db`, then restore into `/tmp/q02-etcd-backup/restored-data` (sandbox data-dir). Record your commands in `/tmp/q02-etcd-backup/apply-script.sh`. Constraints: MUST NOT write to `/var/lib/etcd`; MUST use v3 API; kubeadm CA/cert/key paths are under `/etc/kubernetes/pki/etcd/`. Use `etcdutl snapshot restore` (v3.5+ split from etcdctl).

    **Fixtures**:
    - stub-responses.json: placeholder structural JSON (sandbox-only question; fixture exists for harness convention)
    - expected-fail-score.txt: `SCORE: 1/3`
    - expected-pass-score.txt: `SCORE: 3/3`

    **Sentinel appends** (idempotent grep-guard-then-sed):
    - manifest: `  - id: cluster-architecture-etcd-backup-restore\n    path: 02-etcd-backup-restore\n    estimatedMinutes: 10`
    - coverage: `  etcd-backup-restore:\n    label: "etcd snapshot save + etcdutl restore"\n    questions:\n      - cluster-architecture-etcd-backup-restore`
    - README: `| 02 | [etcd-backup-restore](02-etcd-backup-restore/) | etcd-backup-restore | 10 |`

    `chmod +x` + git update-index --chmod=+x.
  </action>
  <verify>
    <automated>chmod +x cka-sim/packs/cluster-architecture/02-etcd-backup-restore/*.sh && for f in cka-sim/packs/cluster-architecture/02-etcd-backup-restore/*.sh; do bash -n "$f" || exit 1; done && bash cka-sim/scripts/lint-packs.sh && bash cka-sim/scripts/lint-coverage.sh cluster-architecture && bash cka-sim/scripts/lint-deprecated-strings.sh && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - 6 files + 4 .sh have git exec bit 100755
    - setup sandbox sentinel: `grep -q '/tmp/q02-etcd-backup' cka-sim/packs/cluster-architecture/02-etcd-backup-restore/setup.sh && grep -q 'cka-sim-sentinel' cka-sim/packs/cluster-architecture/02-etcd-backup-restore/setup.sh`
    - setup NEVER writes /var/lib/etcd: `! grep -q '/var/lib/etcd' cka-sim/packs/cluster-architecture/02-etcd-backup-restore/setup.sh`
    - ref-solution uses v3 API + correct paths: `grep -q 'ETCDCTL_API=3' cka-sim/packs/cluster-architecture/02-etcd-backup-restore/ref-solution.sh && grep -q '/etc/kubernetes/pki/etcd/ca.crt' cka-sim/packs/cluster-architecture/02-etcd-backup-restore/ref-solution.sh && grep -q 'etcdutl snapshot restore' cka-sim/packs/cluster-architecture/02-etcd-backup-restore/ref-solution.sh && grep -q '/tmp/q02-etcd-backup/restored-data' cka-sim/packs/cluster-architecture/02-etcd-backup-restore/ref-solution.sh`
    - ref-solution does NOT restore to /var/lib/etcd: `! grep -qE '\-\-data-dir=/var/lib/etcd' cka-sim/packs/cluster-architecture/02-etcd-backup-restore/ref-solution.sh`
    - grade records both traps: `grep -q 'record_trap etcd-snapshot-without-env-set' cka-sim/packs/cluster-architecture/02-etcd-backup-restore/grade.sh && grep -q 'record_trap etcd-restore-wrong-data-dir' cka-sim/packs/cluster-architecture/02-etcd-backup-restore/grade.sh`
    - grade is kubectl-free mutating-verb wise: `! grep -qE 'kubectl[[:space:]]+(delete|create|apply|patch|edit|replace)([[:space:]]|$)' cka-sim/packs/cluster-architecture/02-etcd-backup-restore/grade.sh`
    - reset sentinel-guarded + doesn't touch /var/lib/etcd: `grep -q 'cka-sim-sentinel' cka-sim/packs/cluster-architecture/02-etcd-backup-restore/reset.sh && grep -q 'rm -rf /tmp/q02-etcd-backup' cka-sim/packs/cluster-architecture/02-etcd-backup-restore/reset.sh && ! grep -q 'rm.*var/lib/etcd' cka-sim/packs/cluster-architecture/02-etcd-backup-restore/reset.sh`
    - manifest + coverage + README appended once
    - BUG-3 pre-empt
    - All lints + test.sh green
  </acceptance_criteria>
  <done>Q02 etcd drill ships with live-etcd-safe sandbox.</done>
</task>

</tasks>

<verification>
- Round-trip: FAIL (no snapshot.db) → missing-file + trap; PASS after ref-solution creates snapshot + restores
- /var/lib/etcd never touched by any of the 4 scripts
- Sandbox cleaned by reset
</verification>

<success_criteria>
- All lints + test.sh green
- Coverage now has 2 slugs (rbac-viewer + etcd-backup-restore)
</success_criteria>

<output>
`.planning/phases/05-services-networking-cluster-architecture-packs/05-09-SUMMARY.md` — sandbox-only etcd drill + v3/etcdutl note.
</output>
