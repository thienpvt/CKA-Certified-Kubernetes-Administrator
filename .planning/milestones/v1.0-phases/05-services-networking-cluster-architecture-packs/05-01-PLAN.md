---
phase: 05-services-networking-cluster-architecture-packs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cka-sim/lib/setup.sh
  - cka-sim/traps/catalog.yaml
  - cka-sim/scripts/lint-packs.sh
  - cka-sim/scripts/lint-deprecated-strings.sh
  - .github/workflows/validate.yml
  - cka-sim/tests/fixtures/setup_helpers/seed_netpol_skeleton/basic.json
  - cka-sim/tests/fixtures/setup_helpers/read_node_worker/worker.json
  - cka-sim/tests/fixtures/setup_helpers/read_node_worker/empty.json
  - cka-sim/tests/cases/setup_helpers_seed_netpol_skeleton.sh
  - cka-sim/tests/cases/setup_helpers_read_node_worker.sh
  - cka-sim/tests/cases/lint_deprecated_strings.sh
  - cka-sim/tests/fixtures/lint_deprecated_strings/hit.md
  - cka-sim/tests/fixtures/lint_deprecated_strings/miss.md
autonomous: true
requirements: [CI-02, PACK-06, PACK-07]
tags: [bash-lint, helper-library, ci]
must_haves:
  truths:
    - "lib/setup.sh defines 2 new helpers cka_sim::setup::seed_netpol_skeleton + cka_sim::setup::read_node_worker"
    - "traps/catalog.yaml gains 10 new entries (4 S&N + 6 Cluster-Arch + 1 priorityclass) matching 05-CONTEXT.md decisions § Authoring Pattern"
    - "scripts/lint-packs.sh gains a grep-lint forbidding literal node-01/node-02 in packs/**/*.sh (BUG-3 pre-empt)"
    - "scripts/lint-deprecated-strings.sh exists, executable, exits 0 on Phase 4 output"
    - ".github/workflows/validate.yml runs lint-deprecated-strings.sh in the bash-tests job"
    - "scripts/test.sh remains green after additions (29 cases -> 32+ cases)"
  artifacts:
    - path: "cka-sim/lib/setup.sh"
      provides: "2 new helpers appended to existing 4"
      contains: "cka_sim::setup::seed_netpol_skeleton"
      contains_extra: "cka_sim::setup::read_node_worker"
    - path: "cka-sim/scripts/lint-deprecated-strings.sh"
      provides: "Forbids 5 deprecated API strings in cka-sim/packs/**"
      contains: "PodSecurityPolicy"
    - path: "cka-sim/traps/catalog.yaml"
      provides: "10 new trap entries"
      contains: "kube-proxy-mode-mismatch-ipvs-iptables"
      contains_extra: "priorityclass-globaldefault-conflict"
  key_links:
    - from: "cka-sim/scripts/lint-deprecated-strings.sh"
      to: ".github/workflows/validate.yml"
      via: "GHA step run: bash cka-sim/scripts/lint-deprecated-strings.sh"
      pattern: "lint-deprecated-strings"
    - from: "cka-sim/tests/cases/setup_helpers_*.sh"
      to: "cka-sim/lib/setup.sh"
      via: "source under kubectl-stub harness"
      pattern: "source.*lib/setup.sh"
---

<objective>
Extend Phase 4's library + CI contract with everything Phase 5's question-authoring plans (P02-P15) depend on:

1. `cka-sim/lib/setup.sh` gains two new helpers: `cka_sim::setup::seed_netpol_skeleton` (DNS-allow egress skeleton, prevents missing-dns-egress trap) and `cka_sim::setup::read_node_worker` (dynamic non-control-plane node discovery — BUG-3 pre-empt promoted to library).
2. `cka-sim/traps/catalog.yaml` gains 10 new entries per 05-CONTEXT.md § Authoring Pattern list.
3. `cka-sim/scripts/lint-packs.sh` gains a new pass (pass F) forbidding literal `node-01`/`node-02` in any `packs/**/*.sh` (BUG-3 from STATE.md line 43 — must never regress).
4. `cka-sim/scripts/lint-deprecated-strings.sh` is NEW per RESEARCH §13, wired into `.github/workflows/validate.yml` as a dedicated named step.
5. Unit coverage: 2 new helper test cases + 1 new lint-deprecated-strings test case.

No pack content is authored in this plan. P02-P15 consume these primitives.

Output: 1 lib extension + 1 catalog extension + 1 lint-packs extension + 1 new lint script + 1 CI wire-up + 3 test cases + 5 fixture files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-services-networking-cluster-architecture-packs/05-CONTEXT.md
@.planning/phases/05-services-networking-cluster-architecture-packs/05-RESEARCH.md
@cka-sim/lib/setup.sh
@cka-sim/traps/catalog.yaml
@cka-sim/scripts/lint-packs.sh
</context>

<interfaces>
From existing `cka-sim/lib/setup.sh` (Phase 4): `ensure_lab_ns`, `wait_for_ns_active`, `seed_pv_hostpath`, `seed_deployment` already exported. CKA_SIM_ROOT guard + `source lib/log.sh` already at top. Append new helpers BELOW `seed_deployment`.

Canonical shapes for the 2 new helpers are in RESEARCH §15 (lines 532-574) — paste verbatim, wrapping the `die` call through the existing `log.sh` symbol.

Existing `lint-packs.sh` has passes A/B/C/D/E. Pass F (new) iterates `find "$PACKS_DIR" -type f -name '*.sh'` and fails on any literal `node-01` or `node-02` match. Mirror the existing pass shape.

Existing `.github/workflows/validate.yml` line 67 runs `bash cka-sim/scripts/test.sh`. The deprecated-strings lint ALSO gets a dedicated named step `Lint deprecated strings` (per RESEARCH §13 and VALIDATION.md Wave 0 requirement) so any future change to `test.sh`'s lint chain does not silently drop CI coverage.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend lib/setup.sh with 2 helpers + 2 unit test cases</name>
  <files>cka-sim/lib/setup.sh, cka-sim/tests/fixtures/setup_helpers/seed_netpol_skeleton/basic.json, cka-sim/tests/fixtures/setup_helpers/read_node_worker/worker.json, cka-sim/tests/fixtures/setup_helpers/read_node_worker/empty.json, cka-sim/tests/cases/setup_helpers_seed_netpol_skeleton.sh, cka-sim/tests/cases/setup_helpers_read_node_worker.sh</files>
  <read_first>
    - cka-sim/lib/setup.sh (existing 4 helpers, lines 1-190 — header + CKA_SIM_ROOT guard + log.sh source pattern to preserve)
    - .planning/phases/05-services-networking-cluster-architecture-packs/05-RESEARCH.md (§15 lines 532-574 — canonical helper shapes to paste verbatim)
    - cka-sim/tests/cases/setup_helpers_ensure_lab_ns.sh (Phase 4 case shape — kubectl-apply capture pattern)
    - cka-sim/tests/cases/setup_helpers_wait_for_ns_active.sh (kubectl get stub + sleep override pattern)
  </read_first>
  <action>
    APPEND (do not rewrite) 2 helpers below `cka_sim::setup::seed_deployment` in `cka-sim/lib/setup.sh`:

    1. `cka_sim::setup::seed_netpol_skeleton <ns> <name> <selector-key=value>` — emits a NetworkPolicy with `policyTypes: [Ingress, Egress]` and a DNS-allow egress rule (UDP/53 + TCP/53 to `kubernetes.io/metadata.name: kube-system`). `podSelector.matchLabels` is parsed from `${selector%%=*}: ${selector##*=}`. Copy the shape from RESEARCH §15 lines 532-560 verbatim. Use `kubectl apply -f -` heredoc (same idiom as existing helpers).

    2. `cka_sim::setup::read_node_worker` — echoes the first non-control-plane node name. Use `kubectl get nodes -l '!node-role.kubernetes.io/control-plane' --no-headers -o jsonpath='{.items[0].metadata.name}'`. Empty result -> `die "read_node_worker: no non-control-plane worker node found"`. Copy RESEARCH §15 lines 563-573 verbatim.

    Preserve existing header + CKA_SIM_ROOT guard + `source "$CKA_SIM_ROOT/lib/log.sh"`. Do NOT touch `ensure_lab_ns`, `wait_for_ns_active`, `seed_pv_hostpath`, `seed_deployment`.

    Test case 1 (`cka-sim/tests/cases/setup_helpers_seed_netpol_skeleton.sh`): stub kubectl to cat stdin on `apply`, source lib/setup.sh, call `cka_sim::setup::seed_netpol_skeleton cka-sim-services-networking-06 dns-allow app=probe`. Assert output contains:
      - `kind: NetworkPolicy`
      - `name: dns-allow`
      - `namespace: cka-sim-services-networking-06`
      - `app: probe` (selector)
      - `policyTypes:` with both `Ingress` and `Egress`
      - `port: 53` (appears twice, for TCP + UDP)
      - `kubernetes.io/metadata.name: kube-system`

    Test case 2 (`cka-sim/tests/cases/setup_helpers_read_node_worker.sh`): stub kubectl to echo `worker-1` for the non-CP selector; call helper; `expect_eq "$out" "worker-1"`. Second sub-case: stub kubectl to echo empty; call helper in a subshell; `expect_match "$(call 2>&1)" "no non-control-plane worker node found"` AND assert exit code non-zero. Use subshell isolation so `die` does not kill the test runner.

    Fixture files: `basic.json` + `worker.json` + `empty.json` are placeholders used by `CKA_SIM_TEST_CURRENT` fixture-path convention (the cases above stub kubectl inline, so fixture content is minimal structural JSON as in Phase 4 Plan 04-01 Task 2).

    `chmod +x` both test cases.
  </action>
  <verify>
    <automated>chmod +x cka-sim/tests/cases/setup_helpers_seed_netpol_skeleton.sh cka-sim/tests/cases/setup_helpers_read_node_worker.sh && bash -n cka-sim/lib/setup.sh && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - lib/setup.sh syntax-valid: `bash -n cka-sim/lib/setup.sh` exits 0
    - 6 helpers total (4 existing + 2 new): `grep -v '^#' cka-sim/lib/setup.sh | grep -cE '^cka_sim::setup::[a-z_]+\(\)' | grep -qx 6`
    - Both new names present: `grep -qE 'cka_sim::setup::seed_netpol_skeleton\(\)' cka-sim/lib/setup.sh && grep -qE 'cka_sim::setup::read_node_worker\(\)' cka-sim/lib/setup.sh`
    - read_node_worker uses the correct selector: `grep -qE "!node-role.kubernetes.io/control-plane" cka-sim/lib/setup.sh`
    - Existing 4 helpers untouched: `grep -qE 'cka_sim::setup::ensure_lab_ns\(\)' cka-sim/lib/setup.sh && grep -qE 'cka_sim::setup::wait_for_ns_active\(\)' cka-sim/lib/setup.sh && grep -qE 'cka_sim::setup::seed_pv_hostpath\(\)' cka-sim/lib/setup.sh && grep -qE 'cka_sim::setup::seed_deployment\(\)' cka-sim/lib/setup.sh`
    - 2 new test cases executable + syntax-valid
    - `bash cka-sim/scripts/test.sh` exits 0 AND reports the 2 new cases: `bash cka-sim/scripts/test.sh 2>&1 | grep -E 'setup_helpers_(seed_netpol_skeleton|read_node_worker)' | wc -l` >= 2
    - No regression in existing 29 cases: `bash cka-sim/scripts/test.sh 2>&1 | grep -qE 'all [0-9]+ case\(s\) passed'`
  </acceptance_criteria>
  <done>2 helpers appended, 2 unit test cases green, no regression.</done>
</task>

<task type="auto">
  <name>Task 2: Extend traps/catalog.yaml with 11 entries + lint-packs.sh pass F (BUG-3 pre-empt)</name>
  <files>cka-sim/traps/catalog.yaml, cka-sim/scripts/lint-packs.sh</files>
  <read_first>
    - cka-sim/traps/catalog.yaml (existing 25 entries, confirmed via `grep -c '^  - id:' cka-sim/traps/catalog.yaml` — schema shape + field order per file header comment)
    - .planning/phases/05-services-networking-cluster-architecture-packs/05-CONTEXT.md (lines 32-41 — 10 trap ID list) and (lines 106-108 — CRI-dockerd trap semantics)
    - .planning/phases/05-services-networking-cluster-architecture-packs/05-RESEARCH.md (§2 + §3 + §4 + §5 + §6 + §7 + §8 + §10 + §11 + §12 — description + remediation hints per trap)
    - cka-sim/scripts/lint-packs.sh (existing passes A-E, lines 40-150 — pass F follows the same shape)
  </read_first>
  <action>
    APPEND 11 entries to `cka-sim/traps/catalog.yaml` matching the 8-field schema (id, name, description, remediation_hint, severity, domain, source, references — with `references` as structured `kind/target/note` list). Order entries identically to 05-CONTEXT.md lines 32-41. For each entry, the exact IDs + domain + severity mapping:

    1. `kube-proxy-mode-mismatch-ipvs-iptables` — severity: warn, domain: services-networking, source: community. description derived from RESEARCH §3. references: k8s-doc `https://kubernetes.io/docs/reference/networking/virtual-ips/`.
    2. `netpol-endport-missing-protocol` — severity: error, domain: services-networking, source: community. description from RESEARCH §4. references: k8s-doc `https://kubernetes.io/docs/concepts/services-networking/network-policies/`.
    3. `coredns-forward-to-invalid-upstream` — severity: error, domain: services-networking, source: community. description from RESEARCH §12. references: k8s-doc `https://kubernetes.io/docs/tasks/administer-cluster/coredns/`.
    4. `ingress-missing-ingressclass` — severity: warn, domain: services-networking, source: community. description from RESEARCH §11. references: k8s-doc `https://kubernetes.io/docs/concepts/services-networking/ingress/`.
    5. `etcd-snapshot-without-env-set` — severity: error, domain: cluster-architecture, source: community. description from RESEARCH §7 line 290. references: k8s-doc `https://kubernetes.io/docs/tasks/administer-cluster/configure-upgrade-etcd/`.
    6. `etcd-restore-wrong-data-dir` — severity: error, domain: cluster-architecture, source: community. description from RESEARCH §7 line 292. references: same as #5.
    7. `kubeadm-upgrade-skip-plan` — severity: warn, domain: cluster-architecture, source: community. description from RESEARCH §8. references: k8s-doc `https://kubernetes.io/docs/tasks/administer-cluster/kubeadm/kubeadm-upgrade/`.
    8. `audit-policy-wrong-stage-verbosity` — severity: warn, domain: cluster-architecture, source: community. description from RESEARCH §6. references: k8s-doc `https://kubernetes.io/docs/tasks/debug/debug-cluster/audit/`.
    9. `crd-missing-scope-field` — severity: error, domain: cluster-architecture, source: community. description from RESEARCH §5. references: k8s-doc `https://kubernetes.io/docs/tasks/extend-kubernetes/custom-resources/custom-resource-definitions/`.
    10. `cri-endpoint-unix-prefix-missing` — severity: error, domain: cluster-architecture, source: community. description from RESEARCH §9. references: k8s-doc `https://kubernetes.io/docs/setup/production-environment/container-runtimes/`.
    11. `priorityclass-globaldefault-conflict` — severity: error, domain: cluster-architecture, source: community. description from RESEARCH §10. references: k8s-doc `https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/`.

    (11 entries total; the list at CONTEXT line 32-41 bundles priorityclass as the 11th — the count "~10" is approximate per CONTEXT wording.)

    Preserve existing 25 entries byte-for-byte. Only append.

    **lint-packs.sh pass F (new — BUG-3 pre-empt)**: after existing pass E block (line 150), insert a new `info "pass F: BUG-3 pre-empt — no hardcoded node-01/node-02 in packs/**/*.sh"` block. Iterate `find "$PACKS_DIR" -type f -name '*.sh' -not -path '*/tests/*'` and fail on any match for `\bnode-0[12]\b` — but ALLOW the match if the line is a comment (first non-whitespace char is `#`). Increment `errors` counter on hit. Preserve the existing final-summary block at the bottom.
  </action>
  <verify>
    <automated>bash -n cka-sim/scripts/lint-packs.sh && bash cka-sim/scripts/lint-traps.sh && bash cka-sim/scripts/lint-packs.sh && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - catalog.yaml has exactly 36 entries (25 existing + 11 new): `grep -c '^  - id:' cka-sim/traps/catalog.yaml | grep -qx 36`
    - All 11 new IDs present: `for id in kube-proxy-mode-mismatch-ipvs-iptables netpol-endport-missing-protocol coredns-forward-to-invalid-upstream ingress-missing-ingressclass etcd-snapshot-without-env-set etcd-restore-wrong-data-dir kubeadm-upgrade-skip-plan audit-policy-wrong-stage-verbosity crd-missing-scope-field cri-endpoint-unix-prefix-missing priorityclass-globaldefault-conflict; do grep -q "id: $id" cka-sim/traps/catalog.yaml || exit 1; done`
    - lint-traps.sh exits 0: `bash cka-sim/scripts/lint-traps.sh >/dev/null`
    - lint-traps reports 36 entries: `bash cka-sim/scripts/lint-traps.sh 2>&1 | grep -qE '36 (catalog )?entr(y|ies) schema OK'`
    - lint-packs pass F exists: `grep -q 'pass F' cka-sim/scripts/lint-packs.sh && grep -q 'node-0' cka-sim/scripts/lint-packs.sh`
    - lint-packs green on current tree (Phase 4 content has no hardcoded node-01/02 post-BUG-3 fix): `bash cka-sim/scripts/lint-packs.sh >/dev/null`
    - Existing 25 entries untouched (spot-check on 3): `grep -q 'id: pss-error-string-mismatch' cka-sim/traps/catalog.yaml && grep -q 'id: default-sa-used' cka-sim/traps/catalog.yaml && grep -q 'id: static-pod-applied-via-kubectl-apply' cka-sim/traps/catalog.yaml`
  </acceptance_criteria>
  <done>11 trap entries appended; lint-packs gains BUG-3 pre-empt pass F; both lints exit 0.</done>
</task>

<task type="auto">
  <name>Task 3: Create lint-deprecated-strings.sh + wire into test.sh and validate.yml + 1 test case</name>
  <files>cka-sim/scripts/lint-deprecated-strings.sh, cka-sim/scripts/test.sh, .github/workflows/validate.yml, cka-sim/tests/cases/lint_deprecated_strings.sh, cka-sim/tests/fixtures/lint_deprecated_strings/hit.md, cka-sim/tests/fixtures/lint_deprecated_strings/miss.md</files>
  <read_first>
    - .planning/phases/05-services-networking-cluster-architecture-packs/05-RESEARCH.md (§13 lines 443-507 — full lint script design with carveouts)
    - cka-sim/scripts/lint-coverage.sh (header + CKA_SIM_ROOT resolution + header/ok/warn/err idioms to mirror)
    - cka-sim/scripts/test.sh (orchestrator — identify where to insert the new lint call between lint-coverage and unit-case runner)
    - .github/workflows/validate.yml (existing bash-tests job — identify step list + anchor for the new `Lint deprecated strings` step)
  </read_first>
  <action>
    Create `cka-sim/scripts/lint-deprecated-strings.sh` per RESEARCH §13 lines 457-491. Key correctness points:

    1. Shebang `#!/bin/bash`; `set -uo pipefail` (NOT `set -e`: we iterate patterns and accumulate failures).
    2. `CKA_SIM_ROOT="$(cd "$(dirname "$0")/.." && pwd)"` + `source lib/colors.sh` + `source lib/log.sh` + `header "deprecated-strings lint"` — mirror lint-coverage.sh header exactly.
    3. Test-mode override: `PACKS_DIR="${CKA_SIM_LINT_PACKS_DIR:-$CKA_SIM_ROOT/packs}"` — same escape hatch unit cases use.
    4. Forbidden patterns array: `"PodSecurityPolicy" "\-\-container-runtime=remote" "policy/v1beta1" "gitRepo:" "dockershim"`. Note the escaped hyphens in pattern 2 (literal `--container-runtime=remote` — a regex double-dash needs escaping to avoid grep long-option confusion).
    5. File scope: `find "$PACKS_DIR" -type f \( -name '*.yaml' -o -name '*.sh' -o -name '*.md' \)`.
    6. Carveouts per RESEARCH §13:
        - YAML/sh line comment: line's first non-whitespace char is `#` AND `#` precedes the forbidden token on the same line.
        - Markdown fenced prose: in `.md` files, lines OUTSIDE fenced blocks labelled `yaml|bash|sh|shell` are prose and ALLOWED. Inside those fenced blocks, hits fail. Implement with awk state-tracker that toggles on `^```(yaml|bash|sh|shell)` and `^```$`.
    7. Print `LINT FAIL: file:line: content` on violations; exit $failures.
    8. `chmod +x` the script.

    **Wire into scripts/test.sh**: insert `bash "$CKA_SIM_ROOT/scripts/lint-deprecated-strings.sh"` AFTER the existing `lint-coverage.sh` call and BEFORE unit-case invocation. Match the pattern used for existing lints (capture exit, fail-fast).

    **Wire into .github/workflows/validate.yml** (per RESEARCH §13 + VALIDATION.md Wave 0 — mandatory dedicated step): add a new step BEFORE the existing `bash cka-sim/scripts/test.sh` step in the bash-tests job. Step name: `Lint deprecated strings`. Step body: `run: bash cka-sim/scripts/lint-deprecated-strings.sh`. Do NOT rely on test.sh's indirect invocation as the only CI coverage — the dedicated step guarantees lint enforcement survives future test.sh refactors.

    **Test case `cka-sim/tests/cases/lint_deprecated_strings.sh`**: create a fixture tree under `$CKA_SIM_TMPDIR/packs/fake/q/` containing:
      - `hit.yaml` with a YAML block `policy/v1beta1` NOT inside a comment (expected: FAIL)
      - `hit.md` with a fenced ```yaml code block containing `gitRepo:` (expected: FAIL)
      - `miss.yaml` with `# policy/v1beta1` in a comment line (expected: skip)
      - `miss.md` with prose line `The old --container-runtime=remote flag is removed.` outside any fenced block (expected: skip)
    Set `CKA_SIM_LINT_PACKS_DIR=$CKA_SIM_TMPDIR/packs` and run the lint. `expect_eq $? 2` (one fail per hit file). Cleanup tmp dir.

    Fixtures `hit.md` + `miss.md` under `cka-sim/tests/fixtures/lint_deprecated_strings/` are reference samples the test case copies into its tmpdir.

    `chmod +x` the test case.
  </action>
  <verify>
    <automated>chmod +x cka-sim/scripts/lint-deprecated-strings.sh cka-sim/tests/cases/lint_deprecated_strings.sh && bash -n cka-sim/scripts/lint-deprecated-strings.sh && bash cka-sim/scripts/lint-deprecated-strings.sh && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - lint-deprecated-strings.sh exists + executable + syntax-valid: `[[ -x cka-sim/scripts/lint-deprecated-strings.sh ]] && bash -n cka-sim/scripts/lint-deprecated-strings.sh`
    - BUG-1 exec-bit guard via git: `git ls-files -s cka-sim/scripts/lint-deprecated-strings.sh | grep -q '^100755'`
    - Script contains all 5 forbidden strings: `for s in PodSecurityPolicy container-runtime=remote policy/v1beta1 gitRepo: dockershim; do grep -q "$s" cka-sim/scripts/lint-deprecated-strings.sh || exit 1; done`
    - Script exits 0 on current tree (Phase 4 output has no violations): `bash cka-sim/scripts/lint-deprecated-strings.sh; test $? -eq 0`
    - test.sh calls the new lint: `grep -q 'lint-deprecated-strings.sh' cka-sim/scripts/test.sh`
    - validate.yml has the dedicated named step running the lint: `grep -qE 'name: *Lint deprecated strings' .github/workflows/validate.yml && grep -q 'bash cka-sim/scripts/lint-deprecated-strings.sh' .github/workflows/validate.yml`
    - Test case executable + green: `[[ -x cka-sim/tests/cases/lint_deprecated_strings.sh ]] && bash cka-sim/scripts/test.sh 2>&1 | grep -qE 'lint_deprecated_strings'`
    - Full test suite green: `bash cka-sim/scripts/test.sh; test $? -eq 0`
    - No regression: case count increased but all pass `bash cka-sim/scripts/test.sh 2>&1 | grep -qE 'all [0-9]+ case\(s\) passed'`
  </acceptance_criteria>
  <done>lint-deprecated-strings.sh lands; wired into test.sh AND as a dedicated named `Lint deprecated strings` step in validate.yml; 1 unit case green; CI pipeline passes.</done>
</task>

</tasks>

<verification>
- `bash cka-sim/scripts/test.sh` exits 0 end-to-end (full lint chain + all cases)
- `bash cka-sim/scripts/lint-traps.sh` reports 36 entries schema OK
- `bash cka-sim/scripts/lint-packs.sh` passes F, no literal node-0[12] in current packs
- `bash cka-sim/scripts/lint-deprecated-strings.sh` exits 0 on current tree (Phase 4 output clean)
- `.github/workflows/validate.yml` contains the dedicated `Lint deprecated strings` step
- No retrofit of existing packs in this plan (P02 + P08 do that)
- No pack content authored in this plan (P03-P07 + P09-P15 do that)
</verification>

<success_criteria>
- P02-P15 can `source $CKA_SIM_ROOT/lib/setup.sh` and call either new helper
- P02-P15 can declare any of the 11 new trap IDs in metadata.yaml and lint-packs pass E will resolve them
- CI pipeline catches any future content containing PodSecurityPolicy/dockershim/etc in a non-comment context
- BUG-3 regression guard active: adding a literal `node-01` in any new setup.sh fails lint-packs pass F
</success_criteria>

<output>
After completion, create `.planning/phases/05-services-networking-cluster-architecture-packs/05-01-SUMMARY.md` covering:
- 2 new helper signatures (copy-paste-ready for P02-P15)
- 11 new trap IDs table (id, domain, severity, which plan activates)
- lint-packs pass F example (what fails, what passes)
- lint-deprecated-strings.sh invocation + carveout behaviour summary
- Test count before/after (baseline 29 -> 32 cases)
- BUG-3 pre-empt confirmation (hardcoded-node grep rejects `node-01`/`node-02` in any future pack setup.sh)
</output>
