---
phase: 05-services-networking-cluster-architecture-packs
plan: 16
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03, 05-04, 05-05, 05-06, 05-07, 05-08, 05-09, 05-10, 05-11, 05-12, 05-13, 05-14, 05-15]
files_modified:
  - .planning/phases/05-services-networking-cluster-architecture-packs/05-VERIFICATION.md
autonomous: false
requirements: [PACK-03, PACK-04, PACK-06, PACK-07, CI-02]
tags: [verification, goal-backward]
must_haves:
  truths:
    - "Services-Networking pack has >=1 question per Tracker checkbox in the S&N domain (lint-coverage.sh asserts 100%)"
    - "Cluster-Architecture pack has >=1 question per Tracker checkbox in the Cluster-Arch domain"
    - "Every new question's metadata.yaml passes schema lint: id, domain, estimatedMinutes in [4,12], verified_against: \"1.35\", traps: [>=3 IDs], references: [...]"
    - "Every trap ID referenced by any question exists in traps/catalog.yaml (catalog lint)"
    - "cka-sim drill services-networking and cka-sim drill cluster-architecture can drill every question in those packs without error (manual 1+2 cluster verification)"
    - "All ~10 new trap entries in traps/catalog.yaml pass scripts/lint-traps.sh (8-field schema, structured references)"
    - "CI deprecated-strings lint fails any content under cka-sim/packs/** containing PodSecurityPolicy, --container-runtime=remote, policy/v1beta1, gitRepo:, or dockershim (comment references allowed)"
    - "Phase 3 retrofits (01-networkpolicy-egress, 01-rbac-viewer) still round-trip green after sourcing lib/setup.sh"
  artifacts:
    - path: ".planning/phases/05-services-networking-cluster-architecture-packs/05-VERIFICATION.md"
      provides: "Phase 5 verification report with 8 must-haves + requirement coverage + human-verification checklist"
      contains: "Must-Haves Verification"
      contains_extra: "Human Verification Required"
  key_links:
    - from: ".planning/phases/05-services-networking-cluster-architecture-packs/05-VERIFICATION.md"
      to: ".planning/phases/05-services-networking-cluster-architecture-packs/05-CONTEXT.md"
      via: "8 must-haves copied verbatim from CONTEXT § Runtime + Verification Contract item 8"
      pattern: "Must-Haves Verification"
    - from: ".planning/phases/05-services-networking-cluster-architecture-packs/05-VERIFICATION.md"
      to: "STATE.md"
      via: "status update on completion"
      pattern: "Phase 5"
---

<objective>
Wave 3 — Verification report for Phase 5. Mirrors Phase 4's 04-VERIFICATION.md shape. Records automated lint + test-harness results plus the manual live-drill checklist the candidate runs on the 1+2 kubeadm cluster.

This plan runs AFTER all 15 authoring plans (P01-P15) are complete. It is the final Phase 5 artifact before the phase is marked for human verification.

Output: 1 VERIFICATION.md with 8 must-haves audited (7 programmatic + 1 manual live-drill).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-services-networking-cluster-architecture-packs/05-CONTEXT.md
@.planning/phases/05-services-networking-cluster-architecture-packs/05-RESEARCH.md
@.planning/phases/05-services-networking-cluster-architecture-packs/05-VALIDATION.md
@.planning/phases/04-storage-workloads-scheduling-packs/04-VERIFICATION.md
@.planning/STATE.md
</context>

<interfaces>
8 must-haves from 05-CONTEXT.md lines 57-65 — MUST be copied verbatim as MH-1..MH-8.

Mapping of automated check commands (from 05-VALIDATION.md):
- MH-1: `bash cka-sim/scripts/lint-coverage.sh services-networking` exits 0 with 100% Tracker coverage
- MH-2: `bash cka-sim/scripts/lint-coverage.sh cluster-architecture` exits 0
- MH-3: `bash cka-sim/scripts/lint-packs.sh` pass E (metadata schema) green across all 14 new questions
- MH-4: `bash cka-sim/scripts/lint-packs.sh` pass E (trap-id registration) green
- MH-5: Manual — 14 live drill invocations on the 1+2 cluster (12 new + 2 retrofits)
- MH-6: `bash cka-sim/scripts/lint-traps.sh` reports 36 entries schema OK
- MH-7: `bash cka-sim/scripts/lint-deprecated-strings.sh` exits 0 AND rejects forbidden strings on a fixture
- MH-8: Included in MH-5 (14 drill count = 12 new + 2 retrofits)

VERIFICATION.md shape mirrors 04-VERIFICATION.md (already read into context): frontmatter, phase summary, Observable Truths table, Required Artifacts table, Key Link Verification, Automated Checks, 8 Must-Haves Verification subsections, Requirements Traceability, Anti-Patterns Scan, Behavioral Spot-Checks, Human Verification Required, Deferred Items, Gaps Summary.
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Author 05-VERIFICATION.md + present for human live-drill sign-off</name>
  <files>.planning/phases/05-services-networking-cluster-architecture-packs/05-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/05-services-networking-cluster-architecture-packs/05-CONTEXT.md (lines 57-65 — 8 must-haves to copy verbatim)
    - .planning/phases/04-storage-workloads-scheduling-packs/04-VERIFICATION.md (shape + section order to mirror)
    - .planning/phases/05-services-networking-cluster-architecture-packs/05-VALIDATION.md (automated command map + manual-only table)
    - cka-sim/packs/services-networking/coverage.yaml (post-P02-P07 state — 6 tracker slugs)
    - cka-sim/packs/cluster-architecture/coverage.yaml (post-P08-P15 state — 8 tracker slugs)
    - cka-sim/traps/catalog.yaml (post-P01 state — 36 entries total)
  </read_first>
  <what-built>
    Phase 5 Verification Report covering:
    - Status frontmatter (`verified`, `status: {human_needed|gaps_found}`, must_haves_passed/total, automated score)
    - Observable Truths table (7 rows — one per goal-backward truth from 05-CONTEXT.md)
    - Required Artifacts table (lib/setup.sh + 14 question dirs + 2 pack manifest+coverage+README + traps/catalog.yaml + 5 scripts + .github/workflows/validate.yml)
    - Key Link Verification table (source→lib/setup.sh; metadata.traps→catalog; coverage→manifest; lint-deprecated-strings→CI)
    - Automated Checks table (5 commands + expected exit 0)
    - 8 Must-Haves Verification subsections (MH-1..MH-8) with evidence
    - Requirements Traceability table (PACK-03, PACK-04, PACK-06, PACK-07, CI-02) → source plan IDs
    - Anti-Patterns Scan (lint-packs passes A/B/C/D/E/F)
    - Behavioral Spot-Checks table
    - Human Verification Required section with exact `cka-sim drill` commands for each of the 14 questions
    - Deferred Items (WR-01 vendoring from Phase 4 still deferred; DF-08 hint-reveal; pack README polish)
    - Gaps Summary
  </what-built>
  <action>
    Author `.planning/phases/05-services-networking-cluster-architecture-packs/05-VERIFICATION.md` with the structure from 04-VERIFICATION.md. Required content highlights:

    **Frontmatter**: `phase: 05`, `verified: <ISO date>`, `status: human_needed` (MH-5 + MH-8 live-drill always manual per CONTEXT), `must_haves_passed: 7`, `must_haves_total: 8`, `human_verification_count: 1` (live drill), `score: 7/8 must-haves verified programmatically`, `gaps: []`, `re_verification: {previous_status: null}`, `requirements_coverage: {PACK-03: satisfied, PACK-04: satisfied, PACK-06: satisfied, PACK-07: satisfied (S&N + Cluster-Arch subset, 100% Tracker coverage), CI-02: satisfied}`.

    **8 Must-Haves Verification subsections** — copy the CONTEXT truth verbatim as the subsection heading, then run the automated command and paste the output. For MH-5 route to Human Verification. Expected outputs:
    - MH-1: `lint-coverage.sh services-networking` prints `services-networking: coverage schema OK (6 slugs, 6 questions)` and exits 0
    - MH-2: `lint-coverage.sh cluster-architecture` prints `cluster-architecture: coverage schema OK (8 slugs, 8 questions)` and exits 0
    - MH-3: `lint-packs.sh` pass E exits 0 across all 14 new metadata.yaml files (6 S&N + 8 Cluster-Arch)
    - MH-4: same pass E cross-references metadata.traps against catalog.yaml → 0 errors
    - MH-5: HUMAN — must run 14 drills live
    - MH-6: `lint-traps.sh` reports `36 entries schema OK` (25 existing + 11 new per P01)
    - MH-7: `lint-deprecated-strings.sh` exits 0 on current tree; write a test fixture demonstrating it rejects `PodSecurityPolicy` (not in a comment) by running against a tempfile
    - MH-8: included in MH-5 count (2 retrofits + 12 new = 14)

    **Human Verification Required section** — enumerate the 14 drills with exact commands (mirror 04-VERIFICATION.md lines 257-303):
    ```
    # Services-Networking pack (6 questions, including 01 retrofit):
    for i in 01 02 03 04 05 06; do
      cka-sim drill services-networking --question "$i" --grade-broken
      cka-sim drill services-networking --question "$i" --ref-solution
      cka-sim drill services-networking --question "$i" --grade
      cka-sim drill services-networking --question "$i" --reset
    done
    cka-sim drill services-networking; cka-sim drill services-networking   # idempotency

    # Cluster-Architecture pack (8 questions, including 01 retrofit):
    for i in 01 02 03 04 05 06 07 08; do
      cka-sim drill cluster-architecture --question "$i" --grade-broken
      cka-sim drill cluster-architecture --question "$i" --ref-solution
      cka-sim drill cluster-architecture --question "$i" --grade
      cka-sim drill cluster-architecture --question "$i" --reset
    done
    cka-sim drill cluster-architecture; cka-sim drill cluster-architecture  # idempotency
    ```

    Per-question special-attention notes:
    - Q02 etcd-backup-restore: requires etcdutl binary (v3.5+) present; read-only on live etcd; restore targets sandbox only
    - Q03 kubeadm-upgrade: pure sandbox; NO real `kubeadm upgrade` invocation on live CP
    - Q04 pss-enforce: namespace must be created BEFORE PSS labels applied; violator admission expected via `--dry-run=server`
    - Q05 audit-policy: python3 yaml.safe_load required on candidate CP
    - Q07 cri-dockerd-endpoint: copies /var/lib/kubelet/kubeadm-flags.env + /etc/kubernetes/kubelet.conf into sandbox READ-ONLY; live files untouched
    - Q08 priorityclass: 2 cluster-scoped PCs; reset must delete both
    - Q06 S&N coredns-resolution: dnsPolicy None requires dnsConfig.nameservers non-empty
    - Q06 S&N netpol-endport: CNI must support endPort (Calico 3.25+)
    - Q04 S&N ingress-path-host: structural validation only; 1+2 cluster may have no ingress controller

    **Gaps Summary**: Expected `No blocking gaps.` — all 7 programmatic MHs pass. MH-5 is first-class human verification per CONTEXT contract. Once candidate confirms 14 drills round-trip green on the 1+2 cluster, Phase 5 status flips to `verified`.

    **Resume-signal**: Human must execute the 14-drill checklist + report PASS/FAIL per question. Checkpoint blocks until candidate types `approved` or describes drill failures.
  </action>
  <how-to-verify>
    1. Read the authored 05-VERIFICATION.md end-to-end; confirm all 8 must-haves have sections + automated command output where applicable + HUMAN marker on MH-5.
    2. Confirm the Human Verification Required section enumerates all 14 drill invocations (6 S&N + 8 Cluster-Arch) with exact commands.
    3. Run the 5 automated lints locally (the same commands pasted into the automated-checks table) and confirm each exits 0:
       ```
       bash cka-sim/scripts/lint-traps.sh
       bash cka-sim/scripts/lint-packs.sh
       bash cka-sim/scripts/lint-coverage.sh
       bash cka-sim/scripts/lint-deprecated-strings.sh
       bash cka-sim/scripts/test.sh
       ```
    4. Execute the live drill loop on the candidate's 1+2 kubeadm cluster (Ubuntu 22.04, v1.35) per the Human Verification Required section. Record pass/fail per question.
    5. Expected outcome: 14/14 drills round-trip green. Any drill failure is a gap — goes back through `/gsd-plan-phase 05 --gaps`.
  </how-to-verify>
  <resume-signal>Type "approved" if all 14 drills round-trip green, or list the question ids that failed with their exit output.</resume-signal>
  <acceptance_criteria>
    - 05-VERIFICATION.md exists at `.planning/phases/05-services-networking-cluster-architecture-packs/05-VERIFICATION.md`
    - Frontmatter contains: `phase: 05`, `status: human_needed`, `must_haves_total: 8`, `must_haves_passed: 7`
    - All 8 must-have subsections present with goal-backward truths copied verbatim from CONTEXT lines 57-65: `for n in 1 2 3 4 5 6 7 8; do grep -qE "MH-$n" .planning/phases/05-services-networking-cluster-architecture-packs/05-VERIFICATION.md || exit 1; done`
    - Requirements traceability table lists all 5 Phase 5 requirements: `for r in PACK-03 PACK-04 PACK-06 PACK-07 CI-02; do grep -q "$r" .planning/phases/05-services-networking-cluster-architecture-packs/05-VERIFICATION.md || exit 1; done`
    - Human Verification section enumerates 14 drill targets (6 S&N + 8 Cluster-Arch): `grep -c 'cka-sim drill' .planning/phases/05-services-networking-cluster-architecture-packs/05-VERIFICATION.md | awk '$1 >= 8 {exit 0} {exit 1}'`
    - Automated checks table lists all 5 lint commands: `for s in lint-traps.sh lint-packs.sh lint-coverage.sh lint-deprecated-strings.sh test.sh; do grep -q "$s" .planning/phases/05-services-networking-cluster-architecture-packs/05-VERIFICATION.md || exit 1; done`
    - Anti-Patterns Scan references all 6 lint-packs passes (A/B/C/D/E + NEW F from P01): `grep -q 'pass F' .planning/phases/05-services-networking-cluster-architecture-packs/05-VERIFICATION.md`
    - Deferred Items section carries forward WR-01 (Phase 4) + DF-08: `grep -q 'WR-01' .planning/phases/05-services-networking-cluster-architecture-packs/05-VERIFICATION.md && grep -q 'DF-08' .planning/phases/05-services-networking-cluster-architecture-packs/05-VERIFICATION.md`
    - Gaps Summary declares human-gated: `grep -qE '(No blocking gaps|human-gated|human_needed)' .planning/phases/05-services-networking-cluster-architecture-packs/05-VERIFICATION.md`
    - Live-drill checklist lands verbatim per-question (at least Q02/Q03/Q04/Q05/Q07/Q08 Cluster-Arch special-attention notes)
    - Running the 5 automated lints on the post-P15 tree: all exit 0
    - Running `bash cka-sim/scripts/lint-traps.sh 2>&1 | grep -qE '36 .*schema OK'` (36 trap entries: 25 existing + 11 added by P01)
    - Running `bash cka-sim/scripts/lint-coverage.sh 2>&1 | grep -qE 'coverage lint passed'` (storage + workloads + S&N + Cluster-Arch packs with 100% Tracker coverage; format relaxed to survive intervening lint-coverage.sh edits)
  </acceptance_criteria>
  <done>05-VERIFICATION.md in place; candidate has the 14-drill checklist to run live.</done>
</task>

</tasks>

<verification>
- 05-VERIFICATION.md lands with 8 must-have sections + 14-drill human checklist
- All 5 automated lints green on the post-Phase-5 tree
- Status: human_needed (blocked on 14 live drills)
- Once the candidate reports 14/14 green, STATE.md flips Phase 5 to complete
</verification>

<success_criteria>
- 7/8 must-haves verified programmatically
- MH-5 + MH-8 routed to human verification
- All 5 Phase 5 REQ-IDs (PACK-03, PACK-04, PACK-06, PACK-07, CI-02) satisfied
- Phase 4 deferred items (WR-01 vendoring + DF-08 hint-reveal + validate-local-windows-python3) carried forward cleanly
- 11 new trap entries + 4 CONCERNS-seeded trap activations accounted for (Q04 PSS x2, Q07 CRI-dockerd x2)
</success_criteria>

<output>
After the candidate signs off on the 14-drill checklist, create `.planning/phases/05-services-networking-cluster-architecture-packs/05-16-SUMMARY.md` covering:
- 8 must-have verdict table
- Live-drill per-question pass/fail matrix (populated post-run)
- Phase 5 closing note: roadmap advances to Phase 6 (Troubleshooting)
- Any gaps recorded → feed into `/gsd-plan-phase 05 --gaps` rather than blocking close
</output>
