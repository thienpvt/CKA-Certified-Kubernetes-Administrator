---
phase: 05-services-networking-cluster-architecture-packs
plan: 11
type: execute
wave: 2
depends_on: [05-01, 05-08]
files_modified:
  - cka-sim/packs/cluster-architecture/04-pss-enforce/setup.sh
  - cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh
  - cka-sim/packs/cluster-architecture/04-pss-enforce/reset.sh
  - cka-sim/packs/cluster-architecture/04-pss-enforce/ref-solution.sh
  - cka-sim/packs/cluster-architecture/04-pss-enforce/metadata.yaml
  - cka-sim/packs/cluster-architecture/04-pss-enforce/question.md
  - cka-sim/packs/cluster-architecture/manifest.yaml
  - cka-sim/packs/cluster-architecture/coverage.yaml
  - cka-sim/packs/cluster-architecture/README.md
  - cka-sim/tests/fixtures/phase-05/cluster-architecture/04-pss-enforce/stub-responses.json
  - cka-sim/tests/fixtures/phase-05/cluster-architecture/04-pss-enforce/expected-fail-score.txt
  - cka-sim/tests/fixtures/phase-05/cluster-architecture/04-pss-enforce/expected-pass-score.txt
autonomous: true
requirements: [PACK-04, PACK-06, PACK-07]
tags: [cluster-architecture, pss, cg-10, concerns-activated]
must_haves:
  truths:
    - "setup applies namespace labels pod-security.kubernetes.io/enforce=restricted + enforce-version=v1.35 on the lab ns BEFORE any pod attempts (RESEARCH §14 ordering)"
    - "setup seeds a Deployment q04-compliant (no privileged, no hostNetwork, no hostPath, runAsNonRoot=true) — admitted under restricted"
    - "setup attempts a Deployment q04-violator with securityContext.privileged=true expecting admission rejection; captured rejection message written to /tmp/q04-pss-enforce/violator-admission.log"
    - "grade asserts namespace has enforce + enforce-version labels; asserts /tmp/q04-pss-enforce/violator-admission.log contains regex 'violates PodSecurity \"(privileged|baseline|restricted):(v1\\.[0-9]+|latest)\":' (RESEARCH §2 v1.25+ wording)"
    - "grade records trap pss-error-string-mismatch when log contains legacy 'PodSecurityPolicy' token; records trap psp-fictional-pod-label-exemption when any manifest under /tmp/q04-pss-enforce/ contains 'pod-security.kubernetes.io/exempt' label/annotation"
    - "ref-solution does NOT edit /etc/kubernetes/; writes compliant pod YAML under /tmp/q04-pss-enforce/ and applies it; writes AdmissionConfiguration sample to sandbox (illustrative only — not applied to apiserver)"
    - "metadata.yaml traps = [pss-error-string-mismatch, psp-fictional-pod-label-exemption, default-sa-used]"
  artifacts:
    - path: "cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh"
      provides: "v1.25+ PSS regex assertion + PSP legacy string trap"
      contains: "violates PodSecurity"
  key_links:
    - from: "cka-sim/packs/cluster-architecture/04-pss-enforce/setup.sh"
      to: "live apiserver PSS admission"
      via: "namespace label pod-security.kubernetes.io/enforce=restricted"
      pattern: "pod-security.kubernetes.io/enforce"
---

<objective>
Cluster Architecture Q04 — PSS enforcement (CG-10 / ROADMAP Phase 5 criterion 2). 9 minutes. Activates 2 CONCERNS.md-seeded traps from Phase 2: `pss-error-string-mismatch` + `psp-fictional-pod-label-exemption`.

Per RESEARCH §2: MUST use v1.25+ wording `violates PodSecurity "<LEVEL>:<VERSION>":`. MUST NOT use pre-1.25 PSP wording anywhere. Exemptions are cluster-scoped only (AdmissionConfiguration); there is NO valid pod-label exemption.

Output: 6 question files + 3 fixtures + 3 sentinel appends.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-services-networking-cluster-architecture-packs/05-RESEARCH.md
@.planning/codebase/CONCERNS.md
@cka-sim/lib/setup.sh
@cka-sim/traps/catalog.yaml
</context>

<interfaces>
Lab ns: `cka-sim-cluster-architecture-04`. Sandbox: `/tmp/q04-pss-enforce/` sentinel-guarded.

Namespace labels from RESEARCH §2 line 67-74:
- `pod-security.kubernetes.io/enforce: restricted`
- `pod-security.kubernetes.io/enforce-version: v1.35`

v1.25+ error regex from RESEARCH §2 line 59: `violates PodSecurity "(privileged|baseline|restricted):(v1\.[0-9]+|latest)":`.

Forbidden strings in any candidate manifest (trap fires): `pod-security.kubernetes.io/exempt` label OR any `pod-security.kubernetes.io/*exempt*` annotation.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 6 files + 3 fixtures + sentinel appends</name>
  <files>cka-sim/packs/cluster-architecture/04-pss-enforce/metadata.yaml, cka-sim/packs/cluster-architecture/04-pss-enforce/question.md, cka-sim/packs/cluster-architecture/04-pss-enforce/setup.sh, cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh, cka-sim/packs/cluster-architecture/04-pss-enforce/reset.sh, cka-sim/packs/cluster-architecture/04-pss-enforce/ref-solution.sh, cka-sim/tests/fixtures/phase-05/cluster-architecture/04-pss-enforce/stub-responses.json, cka-sim/tests/fixtures/phase-05/cluster-architecture/04-pss-enforce/expected-fail-score.txt, cka-sim/tests/fixtures/phase-05/cluster-architecture/04-pss-enforce/expected-pass-score.txt, cka-sim/packs/cluster-architecture/manifest.yaml, cka-sim/packs/cluster-architecture/coverage.yaml, cka-sim/packs/cluster-architecture/README.md</files>
  <read_first>
    - .planning/phases/05-services-networking-cluster-architecture-packs/05-RESEARCH.md (§2 lines 38-85 — v1.25+ wording + pod-exempt trap conditions; §14 bullet on ns-first-then-label ordering)
    - .planning/codebase/CONCERNS.md (Content Accuracy & Version Drift — PSS section)
    - cka-sim/traps/catalog.yaml (lines 9-43 — pss-error-string-mismatch + psp-fictional-pod-label-exemption Phase 2 seed)
    - cka-sim/lib/setup.sh (ensure_lab_ns)
  </read_first>
  <action>
    **setup.sh**: ensure_lab_ns (must run BEFORE applying PSS labels per RESEARCH §14 — existing pods are not re-evaluated). Patch namespace with PSS enforce labels:
    ```
    kubectl label namespace "$CKA_SIM_LAB_NS" \
      pod-security.kubernetes.io/enforce=restricted \
      pod-security.kubernetes.io/enforce-version=v1.35 \
      --overwrite
    ```
    wait_for_ns_active. mkdir sandbox `/tmp/q04-pss-enforce/` + sentinel.

    Apply compliant Deployment `q04-compliant`: image nginx:1.27-alpine-slim-unprivileged (any unprivileged image), securityContext with runAsNonRoot=true + allowPrivilegeEscalation=false + capabilities.drop=[ALL] + seccompProfile.type=RuntimeDefault. Expected to be admitted.

    Attempt violator Deployment `q04-violator`: securityContext.privileged=true (fails restricted). Capture admission rejection to sandbox:
    ```
    kubectl apply --dry-run=server -f /tmp/q04-pss-enforce/violator.yaml 2>&1 | tee /tmp/q04-pss-enforce/violator-admission.log || true
    ```
    (dry-run=server invokes admission without creating the object — cleanest capture for the audit trap string.)

    **grade.sh**: `set -uo pipefail`; source lib/grade.sh + lib/traps.sh.
    - `assert_field_eq namespace $ns '{.metadata.labels.pod-security\.kubernetes\.io/enforce}' 'restricted'` — note the escaped `.` and `/` in jsonpath
    - `assert_field_eq namespace $ns '{.metadata.labels.pod-security\.kubernetes\.io/enforce-version}' 'v1.35'`
    - Log has v1.25+ wording: `grep -qE 'violates PodSecurity "(privileged|baseline|restricted):(v1\.[0-9]+|latest)":' /tmp/q04-pss-enforce/violator-admission.log`; TOTAL++/PASSED++/err
    - Log has NO legacy PSP wording: `! grep -qE '\bPodSecurityPolicy\b' /tmp/q04-pss-enforce/violator-admission.log`; if violates, record_trap `pss-error-string-mismatch`
    - Scan all candidate manifests under `/tmp/q04-pss-enforce/*.yaml` for fictional exempt label/annotation: `grep -qE 'pod-security\.kubernetes\.io/.*exempt' /tmp/q04-pss-enforce/*.yaml 2>/dev/null`; if found, record_trap `psp-fictional-pod-label-exemption`
    - Compliant deployment rolls out: `assert_resource_exists deployment q04-compliant -n $ns` + `.status.readyReplicas >= 1`
    - emit_result

    **ref-solution.sh**: rewrite `/tmp/q04-pss-enforce/violator.yaml` to compliant form (removes privileged:true, adds runAsNonRoot etc). Re-run admission probe capturing a clean admission. Also write illustrative `/tmp/q04-pss-enforce/admission-config.yaml` demonstrating AdmissionConfiguration exemptions (comment: "NOT applied to live apiserver — reference only").

    **reset.sh**: sentinel-guarded sandbox cleanup; `kubectl delete namespace $ns --ignore-not-found --wait=false`.

    **metadata.yaml**: `id: cluster-architecture-pss-enforce`, `domain: cluster-architecture`, `estimatedMinutes: 9`, `verified_against: "1.35"`, `traps: [pss-error-string-mismatch, psp-fictional-pod-label-exemption, default-sa-used]`, references: concerns-md + k8s-doc pod-security-admission.

    **question.md**: scenario — namespace has PSS enforce=restricted (v1.35). A Deployment `q04-violator` attempted to deploy with privileged=true and was REJECTED by admission. Task: (1) inspect `/tmp/q04-pss-enforce/violator-admission.log` to confirm v1.25+ PSS error format, (2) fix the Deployment YAML so it complies with `restricted` profile (no privileged, runAsNonRoot, drop ALL capabilities, seccomp RuntimeDefault), (3) re-verify admission passes. Constraint: do NOT attempt pod-level exemption via any `pod-security.kubernetes.io/exempt` label — no such label exists. Exemptions are cluster-scoped only.

    Must NOT contain the legacy string `PodSecurityPolicy` anywhere in question.md or the manifest blocks (enforced by P01 lint-deprecated-strings.sh).

    **Fixtures**:
    - stub-responses.json: namespace with PSS labels + admission log sample with v1.25+ wording
    - expected-fail-score.txt: `SCORE: 2/6`
    - expected-pass-score.txt: `SCORE: 6/6`

    **Sentinel appends**:
    - manifest: `  - id: cluster-architecture-pss-enforce\n    path: 04-pss-enforce\n    estimatedMinutes: 9`
    - coverage: `  pss-enforce:\n    label: "PodSecurity Standards enforce (CG-10, v1.25+ wording)"\n    questions:\n      - cluster-architecture-pss-enforce`
    - README: `| 04 | [pss-enforce](04-pss-enforce/) | pss-enforce | 9 |`

    `chmod +x` + git update-index --chmod=+x.
  </action>
  <verify>
    <automated>chmod +x cka-sim/packs/cluster-architecture/04-pss-enforce/*.sh && for f in cka-sim/packs/cluster-architecture/04-pss-enforce/*.sh; do bash -n "$f" || exit 1; done && bash cka-sim/scripts/lint-packs.sh && bash cka-sim/scripts/lint-coverage.sh cluster-architecture && bash cka-sim/scripts/lint-deprecated-strings.sh && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - 6 files + 4 .sh have git exec bit 100755
    - Namespace label applied BEFORE pod attempts (ordering): setup.sh has kubectl label line appearing before any pod/deployment apply (verify via line numbers: `lbl=$(grep -n 'pod-security.kubernetes.io/enforce' cka-sim/packs/cluster-architecture/04-pss-enforce/setup.sh | head -1 | cut -d: -f1); pod=$(grep -n 'q04-violator' cka-sim/packs/cluster-architecture/04-pss-enforce/setup.sh | head -1 | cut -d: -f1); [[ "$lbl" -lt "$pod" ]]`)
    - grade uses v1.25+ wording regex: `grep -q 'violates PodSecurity' cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh`
    - grade records both PSS traps: `grep -q 'record_trap pss-error-string-mismatch' cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh && grep -q 'record_trap psp-fictional-pod-label-exemption' cka-sim/packs/cluster-architecture/04-pss-enforce/grade.sh`
    - Nothing under this question dir uses forbidden legacy string (will be caught by lint-deprecated-strings.sh): `! grep -rE '\bPodSecurityPolicy\b' cka-sim/packs/cluster-architecture/04-pss-enforce/*.{yaml,md,sh} | grep -v '^#'` (allowing comments only)
    - No pod-level exempt attempt in ref-solution: `! grep -qE 'pod-security\.kubernetes\.io/.*exempt' cka-sim/packs/cluster-architecture/04-pss-enforce/ref-solution.sh`
    - Ref-solution never edits /etc/kubernetes/: `! grep -q '/etc/kubernetes' cka-sim/packs/cluster-architecture/04-pss-enforce/ref-solution.sh`
    - manifest + coverage + README appended once
    - BUG-3 pre-empt
    - lint-deprecated-strings clean on new content: `bash cka-sim/scripts/lint-deprecated-strings.sh`
    - All lints + test.sh green
  </acceptance_criteria>
  <done>Q04 PSS enforce ships with v1.25+ wording + CONCERNS traps activated.</done>
</task>

</tasks>

<verification>
- Round-trip: FAIL (violator admission log captured) → trap pss-error-string-mismatch if wording wrong; PASS after ref-solution makes violator compliant
- deprecated-strings lint catches any future content drift toward pre-1.25 PSP wording
- /etc/kubernetes/ never touched
</verification>

<success_criteria>
- All lints + test.sh green
- Coverage at 4 slugs
- 2 CONCERNS-seeded traps (pss-error-string-mismatch + psp-fictional-pod-label-exemption) activated on this question
</success_criteria>

<output>
`.planning/phases/05-services-networking-cluster-architecture-packs/05-11-SUMMARY.md` — v1.25+ wording compliance + CONCERNS traps activation.
</output>
