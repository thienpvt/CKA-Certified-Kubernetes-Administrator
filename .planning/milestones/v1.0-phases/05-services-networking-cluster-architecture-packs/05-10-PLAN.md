---
phase: 05-services-networking-cluster-architecture-packs
plan: 10
type: execute
wave: 2
depends_on: [05-01, 05-08]
files_modified:
  - cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/setup.sh
  - cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/grade.sh
  - cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/reset.sh
  - cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/ref-solution.sh
  - cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/metadata.yaml
  - cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/question.md
  - cka-sim/packs/cluster-architecture/manifest.yaml
  - cka-sim/packs/cluster-architecture/coverage.yaml
  - cka-sim/packs/cluster-architecture/README.md
  - cka-sim/tests/fixtures/phase-05/cluster-architecture/03-kubeadm-upgrade/stub-responses.json
  - cka-sim/tests/fixtures/phase-05/cluster-architecture/03-kubeadm-upgrade/expected-fail-score.txt
  - cka-sim/tests/fixtures/phase-05/cluster-architecture/03-kubeadm-upgrade/expected-pass-score.txt
autonomous: true
requirements: [PACK-04, PACK-06, PACK-07]
tags: [cluster-architecture, kubeadm, upgrade]
must_haves:
  truths:
    - "setup creates sandbox /tmp/q03-kubeadm-upgrade/ with 4 files: current-version.txt, planned-upgrade.txt (empty), kubeadm-upgrade-plan.txt (mocked output seeded by setup), apply-script.sh (empty)"
    - "setup NEVER runs kubeadm upgrade against live cluster"
    - "grade asserts planned-upgrade.txt specifies target v1.35; apply-script.sh contains kubeadm upgrade plan then kubeadm upgrade apply v1.35"
    - "grade records trap kubeadm-upgrade-skip-plan when apply-script.sh has apply but no plan beforehand"
    - "ref-solution writes the expected documents into sandbox"
    - "reset sentinel-guarded rm -rf /tmp/q03-kubeadm-upgrade"
  artifacts:
    - path: "cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/grade.sh"
      provides: "Structural assertions on candidate's sandbox files"
      contains: "kubeadm upgrade plan"
  key_links:
    - from: "cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/setup.sh"
      to: "sandbox /tmp/q03-kubeadm-upgrade/"
      via: "file seeding only; never kubeadm binary invocation"
      pattern: "/tmp/q03-kubeadm-upgrade"
---

<objective>
Cluster Architecture Q03 — kubeadm upgrade planning drill. 10 minutes. Trap: `kubeadm-upgrade-skip-plan` (added in P01).

Per RESEARCH §8: live kubeadm upgrade restarts CP components — not safe. Drill is pure file-level in `/tmp/q03-kubeadm-upgrade/`. Candidate writes planned-upgrade.txt + apply-script.sh; grader checks content structurally.

Output: 6 question files + 3 fixtures + 3 sentinel appends.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-services-networking-cluster-architecture-packs/05-RESEARCH.md
@cka-sim/lib/setup.sh
@cka-sim/traps/catalog.yaml
</context>

<interfaces>
Sandbox: `/tmp/q03-kubeadm-upgrade/` sentinel-guarded.

Files seeded by setup.sh (mocked upgrade plan output from RESEARCH §8 lines 301-308):
- `current-version.txt` containing `v1.34.2`
- `kubeadm-upgrade-plan.txt` containing a mocked kubeadm plan output with "target version: v1.35.0", "etcd: 3.5.x", component upgrade rows
- `planned-upgrade.txt` (empty; candidate writes their plan doc)
- `apply-script.sh` (empty; candidate writes the upgrade script)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 6 files + 3 fixtures + sentinel appends</name>
  <files>cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/metadata.yaml, cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/question.md, cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/setup.sh, cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/grade.sh, cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/reset.sh, cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/ref-solution.sh, cka-sim/tests/fixtures/phase-05/cluster-architecture/03-kubeadm-upgrade/stub-responses.json, cka-sim/tests/fixtures/phase-05/cluster-architecture/03-kubeadm-upgrade/expected-fail-score.txt, cka-sim/tests/fixtures/phase-05/cluster-architecture/03-kubeadm-upgrade/expected-pass-score.txt, cka-sim/packs/cluster-architecture/manifest.yaml, cka-sim/packs/cluster-architecture/coverage.yaml, cka-sim/packs/cluster-architecture/README.md</files>
  <read_first>
    - .planning/phases/05-services-networking-cluster-architecture-packs/05-RESEARCH.md (§8 lines 294-325 — sandbox layout + grader assertions + trap)
    - cka-sim/packs/workloads-scheduling/06-static-pod (sandbox sentinel pattern)
  </read_first>
  <action>
    **setup.sh**: source lib/setup.sh, ensure_lab_ns, wait_for_ns_active. Create `/tmp/q03-kubeadm-upgrade/` with sentinel. Seed 4 files per RESEARCH §8:
    - `current-version.txt`: `v1.34.2`
    - `kubeadm-upgrade-plan.txt`: mocked kubeadm plan output containing `target version: v1.35.0` + kubelet/kube-proxy/etcd component rows
    - `planned-upgrade.txt`: empty
    - `apply-script.sh`: empty; chmod 0644

    **grade.sh**: `set -uo pipefail`; source lib/grade.sh + lib/traps.sh.
    - Candidate wrote planned-upgrade.txt: `[[ -s /tmp/q03-kubeadm-upgrade/planned-upgrade.txt ]]` TOTAL++/PASSED++/err
    - Plan mentions v1.35 target: `grep -qE 'target[[:space:]]*version:[[:space:]]*v1\.35' /tmp/q03-kubeadm-upgrade/planned-upgrade.txt`
    - apply-script runs `kubeadm upgrade plan`: `grep -q 'kubeadm upgrade plan' /tmp/q03-kubeadm-upgrade/apply-script.sh` — if missing, `record_trap kubeadm-upgrade-skip-plan`
    - apply-script runs `kubeadm upgrade apply v1.35`: `grep -qE 'kubeadm upgrade apply v1\.35' /tmp/q03-kubeadm-upgrade/apply-script.sh`
    - Plan step before apply step (line ordering): plan_line=$(grep -n 'kubeadm upgrade plan' apply-script.sh | head -1 | cut -d: -f1); apply_line=$(grep -n 'kubeadm upgrade apply' apply-script.sh | head -1 | cut -d: -f1); if plan_line > apply_line OR plan_line empty: record_trap `kubeadm-upgrade-skip-plan`
    - emit_result

    **ref-solution.sh**: write `planned-upgrade.txt` with a markdown-style plan naming target v1.35; write `apply-script.sh` with exactly (in order):
    ```
    #!/bin/bash
    set -euo pipefail
    kubeadm upgrade plan
    kubeadm upgrade apply v1.35.0
    ```
    chmod 0755 the apply-script.sh.

    **reset.sh**: sentinel-guarded; `[[ -f /tmp/q03-kubeadm-upgrade/.cka-sim-sentinel ]] && rm -rf /tmp/q03-kubeadm-upgrade`. Then delete ns.

    **metadata.yaml**: `id: cluster-architecture-kubeadm-upgrade`, `estimatedMinutes: 10`, `traps: [kubeadm-upgrade-skip-plan, default-sa-used, missing-dns-egress]`, references: k8s-doc kubeadm-upgrade.

    **question.md**: scenario — cluster at v1.34.2; need to plan the v1.35 upgrade. Constraints: ONLY sandbox drill — do NOT run kubeadm upgrade against the real cluster (would break it). Produce `/tmp/q03-kubeadm-upgrade/planned-upgrade.txt` (markdown summary naming target version v1.35) AND `/tmp/q03-kubeadm-upgrade/apply-script.sh` (bash script that would be used to apply the upgrade). Must run `kubeadm upgrade plan` BEFORE `kubeadm upgrade apply`.

    **Fixtures**: placeholder JSON; expected-fail `SCORE: 1/5`; expected-pass `SCORE: 5/5`.

    **Sentinel appends**:
    - manifest: `  - id: cluster-architecture-kubeadm-upgrade\n    path: 03-kubeadm-upgrade\n    estimatedMinutes: 10`
    - coverage: `  kubeadm-upgrade:\n    label: "kubeadm upgrade planning + apply sequencing"\n    questions:\n      - cluster-architecture-kubeadm-upgrade`
    - README: `| 03 | [kubeadm-upgrade](03-kubeadm-upgrade/) | kubeadm-upgrade | 10 |`

    `chmod +x` + git update-index --chmod=+x.
  </action>
  <verify>
    <automated>chmod +x cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/*.sh && for f in cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/*.sh; do bash -n "$f" || exit 1; done && bash cka-sim/scripts/lint-packs.sh && bash cka-sim/scripts/lint-coverage.sh cluster-architecture && bash cka-sim/scripts/lint-deprecated-strings.sh && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - 6 files + 4 .sh have git exec bit 100755
    - setup seeds sandbox with 4 required files: `grep -q 'current-version.txt' cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/setup.sh && grep -q 'planned-upgrade.txt' cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/setup.sh && grep -q 'apply-script.sh' cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/setup.sh && grep -q 'kubeadm-upgrade-plan.txt' cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/setup.sh`
    - setup NEVER runs kubeadm binary: `! grep -qE '^[^#]*kubeadm[[:space:]]+(upgrade|init|join|reset)' cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/setup.sh`
    - grade records trap: `grep -q 'record_trap kubeadm-upgrade-skip-plan' cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/grade.sh`
    - ref-solution has plan before apply: ref-solution writes a script where `kubeadm upgrade plan` line number < `kubeadm upgrade apply` line number (verify via awk/grep line numbers): `plan_l=$(grep -n 'kubeadm upgrade plan' cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/ref-solution.sh | head -1 | cut -d: -f1); apply_l=$(grep -n 'kubeadm upgrade apply' cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/ref-solution.sh | head -1 | cut -d: -f1); [[ -n "$plan_l" && -n "$apply_l" && "$plan_l" -lt "$apply_l" ]]`
    - reset sentinel-guarded: `grep -q 'cka-sim-sentinel' cka-sim/packs/cluster-architecture/03-kubeadm-upgrade/reset.sh`
    - manifest + coverage + README appended once
    - BUG-3 pre-empt + all lints + test.sh green
  </acceptance_criteria>
  <done>Q03 kubeadm upgrade drill ships; live cluster untouched.</done>
</task>

</tasks>

<verification>
- Round-trip: FAIL (no planned-upgrade/apply-script) → trap kubeadm-upgrade-skip-plan; PASS after ref writes both with plan-before-apply
- Zero kubeadm-binary invocations in any of the 4 scripts
</verification>

<success_criteria>
- All lints + test.sh green
- Coverage at 3 slugs
</success_criteria>

<output>
`.planning/phases/05-services-networking-cluster-architecture-packs/05-10-SUMMARY.md` — pure-file sandbox drill mechanics.
</output>
