---
phase: 05-services-networking-cluster-architecture-packs
plan: 05
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - cka-sim/packs/services-networking/04-ingress-path-host/setup.sh
  - cka-sim/packs/services-networking/04-ingress-path-host/grade.sh
  - cka-sim/packs/services-networking/04-ingress-path-host/reset.sh
  - cka-sim/packs/services-networking/04-ingress-path-host/ref-solution.sh
  - cka-sim/packs/services-networking/04-ingress-path-host/metadata.yaml
  - cka-sim/packs/services-networking/04-ingress-path-host/question.md
  - cka-sim/packs/services-networking/manifest.yaml
  - cka-sim/packs/services-networking/coverage.yaml
  - cka-sim/packs/services-networking/README.md
  - cka-sim/tests/fixtures/phase-05/services-networking/04-ingress-path-host/stub-responses.json
  - cka-sim/tests/fixtures/phase-05/services-networking/04-ingress-path-host/expected-fail-score.txt
  - cka-sim/tests/fixtures/phase-05/services-networking/04-ingress-path-host/expected-pass-score.txt
autonomous: true
requirements: [PACK-03, PACK-06, PACK-07]
tags: [services-networking, ingress, ingressclass]
must_haves:
  truths:
    - "setup seeds cluster-scoped IngressClass q04-nginx with spec.controller: k8s.io/ingress-placeholder (prefixed q04- per TRIP-03) + Deployment q04-web + Service q04-web in lab ns"
    - "setup does NOT seed an Ingress resource (candidate's job)"
    - "grade asserts Ingress q04-web exists with spec.ingressClassName=q04-nginx AND host rule 'api.example.local' AND a path rule; uses structural kubectl get -o jsonpath (RESEARCH §11: no HTTP probe, 1+2 cluster has no ingress controller)"
    - "grade records trap ingress-missing-ingressclass when spec.ingressClassName is empty AND kubernetes.io/ingress.class annotation is empty"
    - "ref-solution applies an Ingress q04-web with ingressClassName + host api.example.local + path / -> q04-web:80"
    - "reset deletes the IngressClass (cluster-scoped, prefixed q04-) AND the namespace"
  artifacts:
    - path: "cka-sim/packs/services-networking/04-ingress-path-host/grade.sh"
      provides: "Structural ingress assertion, no HTTP probe"
      contains: "ingressClassName"
  key_links:
    - from: "cka-sim/packs/services-networking/04-ingress-path-host/reset.sh"
      to: "cluster-scoped IngressClass q04-nginx"
      via: "kubectl delete ingressclass q04-nginx --ignore-not-found"
      pattern: "delete ingressclass q04-nginx"
---

<objective>
Services & Networking Q04 — Ingress + IngressClass (path + host routing). 8 minutes. Trap: `ingress-missing-ingressclass` (added to catalog in P01).

Per RESEARCH §11: 1+2 kubeadm cluster may have no Ingress controller. Grader uses STRUCTURAL validation (kubectl get -o jsonpath), not HTTP probe. Setup seeds an IngressClass `q04-nginx` with placeholder `spec.controller: k8s.io/ingress-placeholder`; candidate writes the Ingress resource referencing it.

Output: 6 question files + 3 fixtures + 3 sentinel appends.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-services-networking-cluster-architecture-packs/05-RESEARCH.md
@cka-sim/lib/setup.sh
@cka-sim/traps/catalog.yaml
</context>

<interfaces>
Lab ns: `cka-sim-services-networking-04`. Resource prefix: `q04-`. Question id: `services-ingress-path-host`.

IngressClass is cluster-scoped — prefixed `q04-` per TRIP-03 rule (see 05-RESEARCH §1 Q04 + §11). reset.sh must delete it.

Ingress API: `networking.k8s.io/v1`. spec.rules[0].host + spec.rules[0].http.paths[].{path,pathType,backend.service.{name,port.number}}.

Grader assertions from RESEARCH §11 lines 372-383.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 6 files + 3 fixtures + sentinel appends</name>
  <files>cka-sim/packs/services-networking/04-ingress-path-host/metadata.yaml, cka-sim/packs/services-networking/04-ingress-path-host/question.md, cka-sim/packs/services-networking/04-ingress-path-host/setup.sh, cka-sim/packs/services-networking/04-ingress-path-host/grade.sh, cka-sim/packs/services-networking/04-ingress-path-host/reset.sh, cka-sim/packs/services-networking/04-ingress-path-host/ref-solution.sh, cka-sim/tests/fixtures/phase-05/services-networking/04-ingress-path-host/stub-responses.json, cka-sim/tests/fixtures/phase-05/services-networking/04-ingress-path-host/expected-fail-score.txt, cka-sim/tests/fixtures/phase-05/services-networking/04-ingress-path-host/expected-pass-score.txt, cka-sim/packs/services-networking/manifest.yaml, cka-sim/packs/services-networking/coverage.yaml, cka-sim/packs/services-networking/README.md</files>
  <read_first>
    - .planning/phases/05-services-networking-cluster-architecture-packs/05-RESEARCH.md (§11 lines 366-385 — grader structural assertions)
    - cka-sim/lib/setup.sh (seed_deployment for Service backend)
    - cka-sim/traps/catalog.yaml (ingress-missing-ingressclass entry added in P01)
  </read_first>
  <action>
    **setup.sh**: source lib/setup.sh, ensure_lab_ns, wait_for_ns_active. Apply cluster-scoped IngressClass `q04-nginx` via heredoc: `apiVersion: networking.k8s.io/v1`, `kind: IngressClass`, `metadata.name: q04-nginx`, labels `cka-sim/pack: services-networking` + `cka-sim/question-id: services-ingress-path-host`, `spec.controller: k8s.io/ingress-placeholder`. Then seed_deployment `q04-web` nginx:1.27 --replicas 2. Then `kubectl apply -f -` Service `q04-web` ClusterIP port 80 targetPort 80 selector `app: q04-web`. Do NOT seed the Ingress (candidate's job).

    **grade.sh** (structural, read-only):
    - `assert_resource_exists ingress q04-web -n $ns`
    - Extract `ic=$(kubectl get ingress q04-web -n $ns -o jsonpath='{.spec.ingressClassName}' 2>/dev/null)`; TOTAL++; if `ic == q04-nginx` PASSED++; else record_trap `ingress-missing-ingressclass`.
    - `assert_field_eq ingress q04-web '{.spec.rules[0].host}' 'api.example.local' -n $ns`
    - Extract first rule path: `p=$(kubectl get ingress q04-web -n $ns -o jsonpath='{.spec.rules[0].http.paths[0].path}')`; TOTAL++; `[[ -n "$p" ]] && PASSED++` else err.
    - Extract backend service: `assert_field_eq ingress q04-web '{.spec.rules[0].http.paths[0].backend.service.name}' 'q04-web' -n $ns`
    - emit_result

    **ref-solution.sh**: apply Ingress heredoc with `spec.ingressClassName: q04-nginx`, `spec.rules[0].host: api.example.local`, `spec.rules[0].http.paths[0].path: /`, `pathType: Prefix`, `backend.service.name: q04-web`, `backend.service.port.number: 80`.

    **reset.sh**: `kubectl delete namespace $ns --ignore-not-found --wait=false` + `kubectl delete ingressclass q04-nginx --ignore-not-found` (cluster-scoped cleanup per TRIP-03).

    **metadata.yaml**: `id: services-ingress-path-host`, `domain: services-networking`, `estimatedMinutes: 8`, `verified_against: "1.35"`, `traps: [ingress-missing-ingressclass, default-sa-used, missing-dns-egress]`, references: k8s-doc Ingress + IngressClass concepts.

    **question.md**: scenario — Service `q04-web` backs Deployment `q04-web` in `${CKA_SIM_LAB_NS}`. An IngressClass `q04-nginx` exists cluster-wide. Task: write an Ingress `q04-web` routing host `api.example.local` path `/` to Service `q04-web:80`. Constraint: must reference ingressClassName (v1.35 deprecates the legacy `kubernetes.io/ingress.class` annotation path).

    **Fixtures**:
    - stub-responses.json: Ingress with ingressClassName=q04-nginx + rule host api.example.local + path /
    - expected-fail-score.txt: `SCORE: 1/4`
    - expected-pass-score.txt: `SCORE: 4/4`

    **Sentinel appends** (grep-q-then-sed idempotent pattern — identical to P03):
    - manifest: `  - id: services-ingress-path-host\n    path: 04-ingress-path-host\n    estimatedMinutes: 8`
    - coverage: `  ingress-path-host:\n    label: "Ingress + IngressClass + host/path routing"\n    questions:\n      - services-ingress-path-host`
    - README: `| 04 | [ingress-path-host](04-ingress-path-host/) | ingress-path-host | 8 |`

    `chmod +x` + git update-index --chmod=+x all 4 .sh.
  </action>
  <verify>
    <automated>chmod +x cka-sim/packs/services-networking/04-ingress-path-host/*.sh && for f in cka-sim/packs/services-networking/04-ingress-path-host/*.sh; do bash -n "$f" || exit 1; done && bash cka-sim/scripts/lint-packs.sh && bash cka-sim/scripts/lint-coverage.sh services-networking && bash cka-sim/scripts/lint-deprecated-strings.sh && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - 6 files present + 4 .sh have git exec bit 100755
    - setup applies IngressClass with required fields: `grep -q 'kind: IngressClass' cka-sim/packs/services-networking/04-ingress-path-host/setup.sh && grep -q 'name: q04-nginx' cka-sim/packs/services-networking/04-ingress-path-host/setup.sh && grep -q 'controller: k8s.io/ingress-placeholder' cka-sim/packs/services-networking/04-ingress-path-host/setup.sh`
    - setup does NOT pre-seed an Ingress resource: `! grep -qE '^kind:[[:space:]]+Ingress[[:space:]]*$' cka-sim/packs/services-networking/04-ingress-path-host/setup.sh`
    - reset cleans cluster-scoped IngressClass: `grep -q 'kubectl delete ingressclass q04-nginx' cka-sim/packs/services-networking/04-ingress-path-host/reset.sh`
    - grade uses structural assertions (no HTTP probe): `! grep -qE 'curl|wget' cka-sim/packs/services-networking/04-ingress-path-host/grade.sh && grep -q 'ingressClassName' cka-sim/packs/services-networking/04-ingress-path-host/grade.sh && grep -q 'api.example.local' cka-sim/packs/services-networking/04-ingress-path-host/grade.sh`
    - grade records correct trap: `grep -q 'record_trap ingress-missing-ingressclass' cka-sim/packs/services-networking/04-ingress-path-host/grade.sh`
    - grade is read-only: `! grep -qE 'kubectl[[:space:]]+(delete|create|apply|patch|edit|replace)([[:space:]]|$)' cka-sim/packs/services-networking/04-ingress-path-host/grade.sh`
    - ref-solution applies correct Ingress: `grep -q 'ingressClassName: q04-nginx' cka-sim/packs/services-networking/04-ingress-path-host/ref-solution.sh && grep -q 'api.example.local' cka-sim/packs/services-networking/04-ingress-path-host/ref-solution.sh`
    - manifest + coverage + README appended exactly once
    - BUG-3 pre-empt: `! grep -qE '\bnode-0[12]\b' cka-sim/packs/services-networking/04-ingress-path-host/*.sh`
    - All lints + test.sh green
  </acceptance_criteria>
  <done>Q04 ingress ships; cluster-scoped IngressClass labelled + cleaned.</done>
</task>

</tasks>

<verification>
- Round-trip: FAIL (no Ingress) → trap; PASS after ref applies Ingress with correct ingressClassName + host + path
- reset removes cluster-scoped IngressClass so TRIP-02 idempotency holds
</verification>

<success_criteria>
- All lints + test.sh green
- Coverage now has 4 slugs
</success_criteria>

<output>
`.planning/phases/05-services-networking-cluster-architecture-packs/05-05-SUMMARY.md` — structural-validation decision + cluster-scoped-cleanup note.
</output>
