---
phase: 05-services-networking-cluster-architecture-packs
plan: 13
type: execute
wave: 2
depends_on: [05-01, 05-08]
files_modified:
  - cka-sim/packs/cluster-architecture/06-crd-basics/setup.sh
  - cka-sim/packs/cluster-architecture/06-crd-basics/grade.sh
  - cka-sim/packs/cluster-architecture/06-crd-basics/reset.sh
  - cka-sim/packs/cluster-architecture/06-crd-basics/ref-solution.sh
  - cka-sim/packs/cluster-architecture/06-crd-basics/metadata.yaml
  - cka-sim/packs/cluster-architecture/06-crd-basics/question.md
  - cka-sim/packs/cluster-architecture/manifest.yaml
  - cka-sim/packs/cluster-architecture/coverage.yaml
  - cka-sim/packs/cluster-architecture/README.md
  - cka-sim/tests/fixtures/phase-05/cluster-architecture/06-crd-basics/stub-responses.json
  - cka-sim/tests/fixtures/phase-05/cluster-architecture/06-crd-basics/expected-fail-score.txt
  - cka-sim/tests/fixtures/phase-05/cluster-architecture/06-crd-basics/expected-pass-score.txt
autonomous: true
requirements: [PACK-04, PACK-06, PACK-07]
tags: [cluster-architecture, crd, cg-12]
must_haves:
  truths:
    - "setup does NOT pre-install the CRD (candidate's job); seeds an informational README-in-ns about the target group cka-sim.io and plural q06widgets"
    - "grade asserts CRD q06widgets.cka-sim.io exists with spec.scope=Namespaced + spec.group=cka-sim.io"
    - "grade records trap crd-missing-scope-field when spec.scope is empty or set to Cluster"
    - "grade asserts a CR q06widget of kind Q06Widget in the lab ns with spec.size as integer"
    - "ref-solution applies CRD (cluster-scoped, prefixed q06-) + CR in lab ns"
    - "reset deletes CRD q06widgets.cka-sim.io (cascades CRs) + namespace"
  artifacts:
    - path: "cka-sim/packs/cluster-architecture/06-crd-basics/ref-solution.sh"
      provides: "Minimal CRD + CR apply"
      contains: "apiextensions.k8s.io/v1"
      contains_extra: "spec.scope"
  key_links:
    - from: "cka-sim/packs/cluster-architecture/06-crd-basics/reset.sh"
      to: "cluster-scoped CRD q06widgets.cka-sim.io"
      via: "kubectl delete crd q06widgets.cka-sim.io --ignore-not-found"
      pattern: "delete crd q06widgets.cka-sim.io"
---

<objective>
Cluster Architecture Q06 — CRD basics (CG-12 / ROADMAP Phase 5 criterion 2). 6 minutes. Trap: `crd-missing-scope-field` (added in P01).

Per RESEARCH §5: minimal CRD in `apiextensions.k8s.io/v1`, namespace-scoped, with openAPIV3Schema. No external controller. Candidate installs CRD + creates a CR.

Output: 6 question files + 3 fixtures + 3 sentinel appends.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-services-networking-cluster-architecture-packs/05-RESEARCH.md
@cka-sim/lib/setup.sh
@cka-sim/traps/catalog.yaml
</context>

<interfaces>
Lab ns: `cka-sim-cluster-architecture-06`. CRD prefix `q06-` per TRIP-03. CRD name: `q06widgets.cka-sim.io` (plural.group). Group: `cka-sim.io`. Scope: Namespaced.

reset cascade: deleting the CRD automatically removes all CRs of that type — per K8s apiextensions semantics.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 6 files + 3 fixtures + sentinel appends</name>
  <files>cka-sim/packs/cluster-architecture/06-crd-basics/metadata.yaml, cka-sim/packs/cluster-architecture/06-crd-basics/question.md, cka-sim/packs/cluster-architecture/06-crd-basics/setup.sh, cka-sim/packs/cluster-architecture/06-crd-basics/grade.sh, cka-sim/packs/cluster-architecture/06-crd-basics/reset.sh, cka-sim/packs/cluster-architecture/06-crd-basics/ref-solution.sh, cka-sim/tests/fixtures/phase-05/cluster-architecture/06-crd-basics/stub-responses.json, cka-sim/tests/fixtures/phase-05/cluster-architecture/06-crd-basics/expected-fail-score.txt, cka-sim/tests/fixtures/phase-05/cluster-architecture/06-crd-basics/expected-pass-score.txt, cka-sim/packs/cluster-architecture/manifest.yaml, cka-sim/packs/cluster-architecture/coverage.yaml, cka-sim/packs/cluster-architecture/README.md</files>
  <read_first>
    - .planning/phases/05-services-networking-cluster-architecture-packs/05-RESEARCH.md (§5 lines 163-200 — minimal CRD + CR + trap + reset cascade)
    - cka-sim/lib/setup.sh (ensure_lab_ns)
  </read_first>
  <action>
    **setup.sh**: ensure_lab_ns, wait_for_ns_active. Do NOT apply the CRD; candidate installs it. Optionally seed a ConfigMap `q06-readme` in the lab ns with group + plural hints (so candidate reads `kubectl -n $ns describe cm q06-readme`).

    **grade.sh**: `set -uo pipefail`; source lib/grade.sh + lib/traps.sh.
    - CRD exists: `cka_sim::grade::assert_resource_exists crd q06widgets.cka-sim.io`
    - Scope correct: `scope=$(kubectl get crd q06widgets.cka-sim.io -o jsonpath='{.spec.scope}')`; if `[[ "$scope" != "Namespaced" ]]`: record_trap `crd-missing-scope-field`
    - Group: `cka_sim::grade::assert_field_eq crd q06widgets.cka-sim.io '{.spec.group}' 'cka-sim.io'`
    - At least one CR in lab ns: `count=$(kubectl -n $ns get q06widgets.cka-sim.io -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}' 2>/dev/null | wc -l)`; if $count >= 1 PASSED++; TOTAL++
    - CR has spec.size numeric: `size=$(kubectl -n $ns get q06widgets.cka-sim.io -o jsonpath='{.items[0].spec.size}' 2>/dev/null)`; if $size =~ ^[0-9]+$ PASSED++; TOTAL++
    - emit_result

    **ref-solution.sh**: apply CRD heredoc per RESEARCH §5 lines 170-194:
    ```
    apiVersion: apiextensions.k8s.io/v1
    kind: CustomResourceDefinition
    metadata:
      name: q06widgets.cka-sim.io
      labels:
        cka-sim/pack: cluster-architecture
        cka-sim/question-id: cluster-architecture-crd-basics
    spec:
      group: cka-sim.io
      scope: Namespaced
      names:
        plural: q06widgets
        singular: q06widget
        kind: Q06Widget
        shortNames: [q06w]
      versions:
        - name: v1
          served: true
          storage: true
          schema:
            openAPIV3Schema:
              type: object
              properties:
                spec:
                  type: object
                  properties:
                    size: { type: integer }
    ```
    Wait for CRD Established + NamesAccepted: `kubectl wait --for=condition=Established crd/q06widgets.cka-sim.io --timeout=60s`. Then create CR `q06-sample` in lab ns with `spec.size: 3`.

    **reset.sh**: `kubectl delete crd q06widgets.cka-sim.io --ignore-not-found` (cascades CRs); `kubectl delete namespace $ns --ignore-not-found --wait=false`.

    **metadata.yaml**: `id: cluster-architecture-crd-basics`, `estimatedMinutes: 6`, `traps: [crd-missing-scope-field, default-sa-used, missing-dns-egress]`, references: k8s-doc custom-resource-definitions.

    **question.md**: scenario — platform wants a minimal CRD for a custom `Q06Widget` resource, scope `Namespaced`, group `cka-sim.io`. Task: (1) install CRD `q06widgets.cka-sim.io`, (2) create a CR of kind `Q06Widget` in `${CKA_SIM_LAB_NS}` with `spec.size` numeric. Constraint: scope MUST be Namespaced (not Cluster). Verify: `kubectl -n ${CKA_SIM_LAB_NS} get q06widgets.cka-sim.io`.

    **Fixtures**: placeholder + expected-fail `SCORE: 1/5` + expected-pass `SCORE: 5/5`.

    **Sentinel appends**:
    - manifest: `  - id: cluster-architecture-crd-basics\n    path: 06-crd-basics\n    estimatedMinutes: 6`
    - coverage: `  crd-basics:\n    label: "CRD basics: group/scope/CR (CG-12)"\n    questions:\n      - cluster-architecture-crd-basics`
    - README: `| 06 | [crd-basics](06-crd-basics/) | crd-basics | 6 |`

    `chmod +x` + git update-index --chmod=+x.
  </action>
  <verify>
    <automated>chmod +x cka-sim/packs/cluster-architecture/06-crd-basics/*.sh && for f in cka-sim/packs/cluster-architecture/06-crd-basics/*.sh; do bash -n "$f" || exit 1; done && bash cka-sim/scripts/lint-packs.sh && bash cka-sim/scripts/lint-coverage.sh cluster-architecture && bash cka-sim/scripts/lint-deprecated-strings.sh && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - 6 files + 4 .sh have git exec bit 100755
    - setup does NOT pre-install CRD: `! grep -q 'kind: CustomResourceDefinition' cka-sim/packs/cluster-architecture/06-crd-basics/setup.sh`
    - ref-solution applies CRD with correct scope + group: `grep -q 'apiextensions.k8s.io/v1' cka-sim/packs/cluster-architecture/06-crd-basics/ref-solution.sh && grep -q 'scope: Namespaced' cka-sim/packs/cluster-architecture/06-crd-basics/ref-solution.sh && grep -q 'group: cka-sim.io' cka-sim/packs/cluster-architecture/06-crd-basics/ref-solution.sh`
    - ref-solution waits for Established: `grep -q 'condition=Established crd/q06widgets.cka-sim.io' cka-sim/packs/cluster-architecture/06-crd-basics/ref-solution.sh`
    - reset deletes CRD: `grep -q 'kubectl delete crd q06widgets.cka-sim.io' cka-sim/packs/cluster-architecture/06-crd-basics/reset.sh`
    - grade records trap: `grep -q 'record_trap crd-missing-scope-field' cka-sim/packs/cluster-architecture/06-crd-basics/grade.sh`
    - CRD name respects `q06-` TRIP-03 rule (prefix on cluster-scoped resource): grep shows CRD name starts with `q06`
    - manifest + coverage + README appended once
    - All lints + test.sh green
  </acceptance_criteria>
  <done>Q06 CRD ships with clean cluster-scoped cleanup.</done>
</task>

</tasks>

<verification>
- Round-trip: FAIL (no CRD) → assert fail + trap; PASS after ref installs + creates CR with spec.size=3
- reset cascade removes CRs with CRD (no orphans)
</verification>

<success_criteria>
- All lints + test.sh green
- Coverage at 6 slugs
</success_criteria>

<output>
`.planning/phases/05-services-networking-cluster-architecture-packs/05-13-SUMMARY.md` — minimal CRD shape + reset cascade.
</output>
