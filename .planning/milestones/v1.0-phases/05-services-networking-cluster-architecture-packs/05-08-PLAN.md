---
phase: 05-services-networking-cluster-architecture-packs
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - cka-sim/packs/cluster-architecture/01-rbac-viewer/setup.sh
  - cka-sim/packs/cluster-architecture/manifest.yaml
  - cka-sim/packs/cluster-architecture/coverage.yaml
  - cka-sim/packs/cluster-architecture/README.md
autonomous: true
requirements: [PACK-04, PACK-06, PACK-07]
tags: [cluster-architecture, pack-shell, retrofit]
must_haves:
  truths:
    - "packs/cluster-architecture/01-rbac-viewer/setup.sh sources lib/setup.sh and uses ensure_lab_ns + wait_for_ns_active (no inline 24-iteration poll)"
    - "packs/cluster-architecture/manifest.yaml lists 8 questions with sentinel block for P09-P15 to append"
    - "packs/cluster-architecture/coverage.yaml exists with 8 tracker slugs and sentinel block"
    - "packs/cluster-architecture/README.md has 8-question table"
    - "Retrofit preserves existing traps (rbac-viewer-role-mismatch, default-sa-used, missing-dns-egress) and question-id cluster-architecture-rbac-viewer"
  artifacts:
    - path: "cka-sim/packs/cluster-architecture/01-rbac-viewer/setup.sh"
      provides: "Retrofitted setup sourcing lib/setup.sh"
      contains: "source \"$CKA_SIM_ROOT/lib/setup.sh\""
    - path: "cka-sim/packs/cluster-architecture/manifest.yaml"
      provides: "8-question manifest with sentinel block"
      contains: "# BEGIN phase-05 new questions"
    - path: "cka-sim/packs/cluster-architecture/coverage.yaml"
      provides: "8-slug coverage matrix with sentinel block"
      contains: "# BEGIN phase-05 new questions"
  key_links:
    - from: "cka-sim/packs/cluster-architecture/01-rbac-viewer/setup.sh"
      to: "cka-sim/lib/setup.sh"
      via: "source + ensure_lab_ns + wait_for_ns_active"
      pattern: "cka_sim::setup::wait_for_ns_active"
    - from: "cka-sim/packs/cluster-architecture/coverage.yaml"
      to: "cka-sim/packs/cluster-architecture/manifest.yaml"
      via: "tracker.<slug>.questions references manifest question-ids"
      pattern: "cluster-architecture-"
---

<objective>
Stand up the Cluster Architecture pack shell (sibling to P02): retrofit the Phase 3 reference question `01-rbac-viewer` to source `lib/setup.sh`, and create pack-level `manifest.yaml` + `coverage.yaml` + `README.md` with 8 questions declared but only slot 01 filled. P09-P15 append their rows idempotently under a sentinel-bracketed block.

Parallel-safe with P01 and P02 because it only touches `packs/cluster-architecture/`.

Output: 1 retrofitted setup.sh + 3 pack-level files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-services-networking-cluster-architecture-packs/05-CONTEXT.md
@cka-sim/packs/storage/manifest.yaml
@cka-sim/packs/storage/coverage.yaml
@cka-sim/packs/cluster-architecture/01-rbac-viewer/setup.sh
@cka-sim/lib/setup.sh
</context>

<interfaces>
Pack slug: `cluster-architecture`. Pack domain: `cluster-architecture`. Pack weight: 25. Question 01 existing id: `cluster-architecture-rbac-viewer`.

8 questions per 05-CONTEXT.md line 19:
| # | dir | tracker slug | est min |
|---|---|---|---|
| 01 | 01-rbac-viewer | rbac-viewer | 8 (existing) |
| 02 | 02-etcd-backup-restore | etcd-backup-restore | 10 |
| 03 | 03-kubeadm-upgrade | kubeadm-upgrade | 10 |
| 04 | 04-pss-enforce | pss-enforce | 9 |
| 05 | 05-audit-policy | audit-policy | 9 |
| 06 | 06-crd-basics | crd-basics | 6 |
| 07 | 07-cri-dockerd-endpoint | cri-dockerd-endpoint | 8 |
| 08 | 08-priorityclass | priorityclass | 7 |

Same sentinel block pattern as P02. Same helper signatures.
</interfaces>

<parallel_safety>
Wave 2 merge discipline (applies to ALL plans in this phase that append to pack-level files — manifest.yaml, coverage.yaml, README.md):

The append pattern uses sentinel-bracketed blocks (`# BEGIN phase-05 new questions` / `# END phase-05 new questions`) plus `grep -q <question-id> <file> || sed -i ...i...` idempotency guards. This makes each `sed -i` rerun-safe in isolation but does NOT make concurrent commits to the same base branch race-free.

Serialization rule: within a single pack (services-networking OR cluster-architecture), Wave 2 plans merge back to the base branch ONE AT A TIME. The executor runs plans in parallel against separate worktrees, but the final merge-back (or direct push to the base branch) is serialized — next plan rebases on the prior plan's merge before pushing. This avoids the three-way-merge ambiguity that arises when two plans append adjacent lines inside the same sentinel block in the same commit range.

Cross-pack (S&N plans merging while Cluster-Arch plans merge) is safe — different files, no overlap.

Execute-phase honors this by sequencing Wave 2 merge operations per pack. Wave 1 (independent-file work: individual question dirs, fixtures, lib helpers) remains fully parallel.
</parallel_safety>


<tasks>

<task type="auto">
  <name>Task 1: Retrofit 01-rbac-viewer/setup.sh to source lib/setup.sh</name>
  <files>cka-sim/packs/cluster-architecture/01-rbac-viewer/setup.sh</files>
  <read_first>
    - cka-sim/packs/cluster-architecture/01-rbac-viewer/setup.sh (current 75 lines — lines 6-35 are the inline 24-iteration poll for replacement)
    - cka-sim/lib/setup.sh (ensure_lab_ns + wait_for_ns_active signatures)
    - cka-sim/packs/storage/01-pvc-binding/setup.sh (Phase 4 retrofit reference)
  </read_first>
  <action>
    Rewrite `cka-sim/packs/cluster-architecture/01-rbac-viewer/setup.sh` preserving:
    - shebang `#!/bin/bash`
    - `set -euo pipefail`
    - `: "${CKA_SIM_LAB_NS:?CKA_SIM_LAB_NS must be set}"`
    - NEW: `: "${CKA_SIM_ROOT:?CKA_SIM_ROOT must be set}"`
    - NEW: `source "$CKA_SIM_ROOT/lib/setup.sh"`

    Replace lines 6-35 (inline ns apply + poll + re-apply) with:

    ```
    cka_sim::setup::ensure_lab_ns "$CKA_SIM_LAB_NS" cluster-architecture cluster-architecture-rbac-viewer
    cka_sim::setup::wait_for_ns_active "$CKA_SIM_LAB_NS" cluster-architecture cluster-architecture-rbac-viewer 120
    ```

    Preserve the remainder UNCHANGED: ServiceAccount `viewer` apply (lines 37-44), Role `pod-viewer` with intentional `verbs: [watch]` trap (lines 46-57), RoleBinding `viewer-binding` (lines 59-74). The traps being seeded (`rbac-viewer-role-mismatch`, `default-sa-used`, `missing-dns-egress`) and the metadata contract (question-id `cluster-architecture-rbac-viewer`) stay intact — 05-CONTEXT.md lines 20-21 forbid grader/metadata/trap changes.

    Note: 05-CONTEXT.md line 29 says "`as-flag-format-wrong` -> optional reinforcement on the `01-rbac-viewer` retrofit (no behaviour change)" — this means NO change to the trap set in metadata.yaml. Do not add the trap here; the trap is already a candidate-error detection in Phase 3's existing grader logic.

    `chmod +x` + git update-index --chmod=+x (BUG-1 pre-empt).
  </action>
  <verify>
    <automated>chmod +x cka-sim/packs/cluster-architecture/01-rbac-viewer/setup.sh && bash -n cka-sim/packs/cluster-architecture/01-rbac-viewer/setup.sh && bash cka-sim/scripts/lint-packs.sh && bash cka-sim/scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - setup.sh syntax-valid: `bash -n cka-sim/packs/cluster-architecture/01-rbac-viewer/setup.sh`
    - Sources lib/setup.sh: `grep -q 'source "\$CKA_SIM_ROOT/lib/setup.sh"' cka-sim/packs/cluster-architecture/01-rbac-viewer/setup.sh`
    - Uses both helpers: `grep -q 'cka_sim::setup::ensure_lab_ns' cka-sim/packs/cluster-architecture/01-rbac-viewer/setup.sh && grep -q 'cka_sim::setup::wait_for_ns_active' cka-sim/packs/cluster-architecture/01-rbac-viewer/setup.sh`
    - Inline 24-iteration loop removed: `! grep -q 'seq 1 24' cka-sim/packs/cluster-architecture/01-rbac-viewer/setup.sh`
    - Role + RoleBinding blocks preserved: `grep -q 'name: pod-viewer' cka-sim/packs/cluster-architecture/01-rbac-viewer/setup.sh && grep -q 'verbs: \["watch"\]' cka-sim/packs/cluster-architecture/01-rbac-viewer/setup.sh && grep -q 'name: viewer-binding' cka-sim/packs/cluster-architecture/01-rbac-viewer/setup.sh`
    - Executable + git exec bit: `[[ -x cka-sim/packs/cluster-architecture/01-rbac-viewer/setup.sh ]] && git ls-files -s cka-sim/packs/cluster-architecture/01-rbac-viewer/setup.sh | grep -q '^100755'`
    - lint-packs green: `bash cka-sim/scripts/lint-packs.sh >/dev/null`
    - test.sh green: `bash cka-sim/scripts/test.sh; test $? -eq 0`
    - BUG-3 pre-empt passes: `! grep -qE '\bnode-0[12]\b' cka-sim/packs/cluster-architecture/01-rbac-viewer/setup.sh`
  </acceptance_criteria>
  <done>Retrofit in place; inline poll removed; Role+RoleBinding untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Create pack manifest.yaml + coverage.yaml + README.md</name>
  <files>cka-sim/packs/cluster-architecture/manifest.yaml, cka-sim/packs/cluster-architecture/coverage.yaml, cka-sim/packs/cluster-architecture/README.md</files>
  <read_first>
    - cka-sim/packs/storage/manifest.yaml (manifest shape)
    - cka-sim/packs/storage/coverage.yaml (coverage shape)
    - .planning/phases/05-services-networking-cluster-architecture-packs/05-CONTEXT.md (line 19 — 8 question list)
  </read_first>
  <action>
    **manifest.yaml**:

    ```
    pack:
      id: cluster-architecture
      domain: cluster-architecture
      weight: 25
      description: "Cluster Architecture 25% domain pack (PACK-04) -- full v1.35 Tracker coverage + CONCERNS.md content replacements (PSS + CRI-dockerd)."
    questions:
      - id: cluster-architecture-rbac-viewer
        path: 01-rbac-viewer
        estimatedMinutes: 8
      # BEGIN phase-05 new questions (P09-P15 append below this line — one row per plan, idempotent)
      # END phase-05 new questions
    ```

    **coverage.yaml**:

    ```
    # cka-sim/packs/cluster-architecture/coverage.yaml
    # PACK-07 coverage matrix for the Cluster Architecture domain (v1.35 Study Progress Tracker).
    domain: cluster-architecture
    tracker:
      rbac-viewer:
        label: "RBAC Roles + RoleBindings for a viewer SA"
        questions:
          - cluster-architecture-rbac-viewer
      # BEGIN phase-05 new questions (P09-P15 append below this line)
      # END phase-05 new questions
    ```

    **README.md**:

    ```
    # cluster-architecture pack

    PACK-04 — Cluster Architecture 25% domain pack. Includes CONCERNS.md content replacements: PSS (v1.25+ wording), CRI-dockerd (correct endpoint flag + file), audit-policy, CRD basics.

    | # | question | tracker slug | est. minutes |
    | - | -------- | ------------ | ------------ |
    | 01 | [rbac-viewer](01-rbac-viewer/) | rbac-viewer | 8 |

    <!-- BEGIN phase-05 new questions (P09-P15 append one table row each) -->
    <!-- END phase-05 new questions -->

    Pack total: 8 questions, ~68 min.

    Run all questions: `cka-sim drill cluster-architecture`.
    ```
  </action>
  <verify>
    <automated>bash cka-sim/scripts/lint-coverage.sh cluster-architecture && bash cka-sim/scripts/lint-packs.sh</automated>
  </verify>
  <acceptance_criteria>
    - manifest.yaml has pack metadata + 1 question row + sentinel block: `grep -q '^  id: cluster-architecture' cka-sim/packs/cluster-architecture/manifest.yaml && grep -q 'id: cluster-architecture-rbac-viewer' cka-sim/packs/cluster-architecture/manifest.yaml && grep -q '# BEGIN phase-05 new questions' cka-sim/packs/cluster-architecture/manifest.yaml`
    - coverage.yaml has 1 tracker slug + sentinel: `grep -q '^domain: cluster-architecture' cka-sim/packs/cluster-architecture/coverage.yaml && grep -q '^  rbac-viewer:' cka-sim/packs/cluster-architecture/coverage.yaml && grep -q '# BEGIN phase-05 new questions' cka-sim/packs/cluster-architecture/coverage.yaml`
    - README.md has sentinel comment: `grep -q 'BEGIN phase-05 new questions' cka-sim/packs/cluster-architecture/README.md`
    - lint-coverage green: `bash cka-sim/scripts/lint-coverage.sh cluster-architecture; test $? -eq 0`
    - lint-packs green: `bash cka-sim/scripts/lint-packs.sh; test $? -eq 0`
    - No orphan warning: `! bash cka-sim/scripts/lint-coverage.sh cluster-architecture 2>&1 | grep -q 'orphan'`
    - Manifest question count == 1: `awk '/^questions:/,/^[a-zA-Z]/' cka-sim/packs/cluster-architecture/manifest.yaml | grep -cE '^\s+- id:' | grep -qx 1`
    - deprecated-strings lint clean: `bash cka-sim/scripts/lint-deprecated-strings.sh`
  </acceptance_criteria>
  <done>Pack shell ready; P09-P15 can idempotently append rows between BEGIN/END markers.</done>
</task>

</tasks>

<verification>
- `cka-sim drill cluster-architecture` picks up only 01-rbac-viewer pre-P09-P15
- 01-rbac-viewer still round-trips green — retrofit is source-only
- P09-P15 can sed-insert rows in parallel without conflict
</verification>

<success_criteria>
- Pack shell accepts idempotent appends
- Retrofit preserves question-01 FAIL→trap behaviour
</success_criteria>

<output>
After completion, create `.planning/phases/05-services-networking-cluster-architecture-packs/05-08-SUMMARY.md` covering:
- Retrofit diff summary (inline loop → helper call)
- Sentinel block convention + sed recipe P09-P15 use
- 7 downstream plans that will append to this pack (P09-P15)
</output>
