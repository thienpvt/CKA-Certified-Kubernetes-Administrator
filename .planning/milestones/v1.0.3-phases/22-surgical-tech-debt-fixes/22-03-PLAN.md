---
phase: 22-surgical-tech-debt-fixes
plan: 03
type: execute
wave: 2
depends_on:
  - "22-02"
files_modified:
  - cka-sim/packs/workloads-scheduling/06-static-pod/setup.sh
  - cka-sim/packs/workloads-scheduling/06-static-pod/metadata.yaml
  - cka-sim/lib/symptom-diff.sh
  - cka-sim/lib/cmd/audit.sh
  - cka-sim/scripts/lint-question-symptom.sh
  - cka-sim/tests/cases/symptom-diff-unsupported-in-audit.sh
autonomous: true
requirements:
  - AUDIT-W&S06

must_haves:
  truths:
    - "On the v1.0.1 lab cluster, `cka-sim audit workloads-scheduling/06-static-pod` produces a deterministic outcome — either PASS verdict (fix path) OR SKIPPED verdict (skip path). No silent ERROR row remains."
    - "The Phase 22 audit baseline advances from `33/34 PASS, 0 FAIL, 1 errors, 0 skipped` (per `cka-sim/current-tests/step2-results.txt:49`) to either `34/34 PASS` (fix path) OR `33/34 PASS + 1 SKIPPED` (skip path)."
    - "Drill-mode and exam-mode behavior for `workloads-scheduling/06-static-pod` is preserved — `cka-sim drill workloads-scheduling 6` and the exam-mode equivalent still run setup.sh end-to-end and grade against ref-solution unchanged."
    - "The drill-mode regression check is exercised by the unit suite: a test case asserts that the audit-skip flag (if introduced) does NOT affect drill/exam code paths — only audit-mode entry points consult it."
  artifacts:
    - path: cka-sim/packs/workloads-scheduling/06-static-pod/setup.sh
      provides: "Either: (fix path) updated setup.sh that succeeds against an audit-harness sandbox namespace on a 1+2 kubeadm cluster; OR (skip path) unchanged setup.sh — the question keeps current drill/exam behavior."
      contains: "ensure_lab_ns"
    - path: cka-sim/packs/workloads-scheduling/06-static-pod/metadata.yaml
      provides: "Either: (fix path) unchanged; OR (skip path) carries `unsupported-in-audit-mode: true` (new, orthogonal to the existing `unsupported-on-kind: true` per Phase 17 BLG-02 semantics)."
      contains: "id: workloads-static-pod"
    - path: cka-sim/lib/symptom-diff.sh
      provides: "Either: unchanged (fix path); OR (skip path) carries new helper `cka_sim::symptom_diff::is_unsupported_in_audit_mode` mirroring `is_unsupported_on_kind` shape at lines 73-78."
      contains: "is_unsupported_on_kind"
    - path: cka-sim/lib/cmd/audit.sh
      provides: "Either: unchanged (fix path); OR (skip path) audit driver consults the new helper around line 246-251 and emits SKIPPED for the flagged question, mirroring the existing kind-skip branch shape."
      contains: "_AUDIT_SKIPPED"
    - path: cka-sim/tests/cases/symptom-diff-unsupported-in-audit.sh
      provides: "Either: a no-op stub (fix path — file documents that no audit-skip flag was introduced); OR (skip path) unit test mirroring `symptom-diff-unsupported-on-kind.sh` shape that locks the new helper's true/missing/false/no-meta semantics."
      contains: "is_unsupported_in_audit_mode"
  key_links:
    - from: cka-sim/packs/workloads-scheduling/06-static-pod/metadata.yaml
      to: cka-sim/lib/cmd/audit.sh
      via: "audit driver reads metadata.yaml flags via the symptom-diff helpers; the new flag (skip path) flows through the same gate shape Phase 17 established for kind"
      pattern: "is_unsupported_in_audit_mode"
    - from: cka-sim/scripts/lint-question-symptom.sh
      to: cka-sim/lib/cmd/audit.sh
      via: "lint script and audit script both must honor the new flag — keeping skip semantics consistent across both audit-mode entry points"
      pattern: "is_unsupported_in_audit_mode"
---

<objective>
Close AUDIT-W&S06: `workloads-scheduling/06-static-pod` audit-mode outcome on the v1.0.1 lab cluster becomes deterministic — either PASS via a fix to `setup.sh`, or SKIPPED via a new `unsupported-in-audit-mode` flag honored by the audit harness. The 33/34 PASS + 1 ERROR baseline from `cka-sim/current-tests/step2-results.txt:49` advances to either 34/34 PASS or 33/34 PASS + 1 SKIPPED.

Purpose: Phase 18's audit-on-real-cluster re-run (post-BLG-02 flag-strip; STATE.md v1.0.2 Close-Out section) surfaced this question as the only remaining ERROR. The error masks signal — we want the lab-cluster audit to be either fully green or to deterministically skip this question with a recorded reason.

Output: Either a one-line setup.sh tweak (fix path) OR a 4-file change (skip path: metadata.yaml flag + symptom-diff.sh helper + audit.sh driver gate + lint-question-symptom.sh driver gate + new unit test). The investigation in Task 1 picks the path; Task 2 implements it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-surgical-tech-debt-fixes/22-CONTEXT.md

@cka-sim/packs/workloads-scheduling/06-static-pod/setup.sh
@cka-sim/packs/workloads-scheduling/06-static-pod/metadata.yaml
@cka-sim/lib/symptom-diff.sh
@cka-sim/lib/cmd/audit.sh
@cka-sim/scripts/lint-question-symptom.sh
@cka-sim/lib/setup.sh
@cka-sim/tests/cases/symptom-diff-unsupported-on-kind.sh
@cka-sim/current-tests/step2-results.txt

<interfaces>
<!-- Bug evidence and the existing kind-skip pattern that the skip-path mirrors. -->

From cka-sim/current-tests/step2-results.txt (lines 41-44 — the failure):
- `✗ workloads-scheduling/06-static-pod: FAIL (0 expectation(s) failed of 1)`
- Verdict row: `setup.sh failed (ns=cka-sim-audit-workloads-scheduling-06-static-pod)`
- Step 2 audit re-run: lab cluster, 1 control-plane + 2 workers, Calico, enforcing CNI; 33/34 PASS, 1 ERROR.

From cka-sim/packs/workloads-scheduling/06-static-pod/setup.sh (current — 24 lines):
- Sources lib/setup.sh.
- SSH preflight to `node-01` via `ssh -o BatchMode=yes -o ConnectTimeout=5 node-01 true`. Hard fail with shell exit 1 if SSH fails.
- `cka_sim::setup::ensure_lab_ns "$CKA_SIM_LAB_NS" workloads-scheduling workloads-static-pod`.
- `cka_sim::setup::wait_for_ns_active "$CKA_SIM_LAB_NS" workloads-scheduling workloads-static-pod 120`.
- No in-cluster pre-seed; question is "candidate drops manifest into /etc/kubernetes/manifests on node-01."

From cka-sim/packs/workloads-scheduling/06-static-pod/metadata.yaml (current — 16 lines):
- `id: workloads-static-pod` / `domain: workloads-scheduling` / `estimatedMinutes: 8`.
- Already carries `unsupported-on-kind: true` (Phase 17 BLG-02). Step 2 audit (per STATE.md v1.0.2 Close-Out, line 60) "`BLG-02 unsupported-on-kind` flag removed from the 3 affected questions" — so the lab-cluster re-run intentionally bypassed this flag to test against the real cluster.
- Trap declared: `static-pod-applied-via-kubectl-apply`.

From cka-sim/lib/symptom-diff.sh (lines 70-78 — the existing kind-skip pattern to mirror):
- `cka_sim::symptom_diff::is_unsupported_on_kind` greps metadata.yaml for `^unsupported-on-kind:[[:space:]]*true[[:space:]]*(#.*)?$` and returns 0 if matched, 1 otherwise.
- Pure-bash, no yq dependency. RFC-1123 ns builder follows below at lines 80-89.

From cka-sim/lib/cmd/audit.sh (lines 246-251 — the existing kind-skip gate to mirror):
- Inside the per-question loop in `cka_sim::audit::run`:
- `if cka_sim::symptom_diff::is_unsupported_on_kind "$q_dir"; then info "...skipped..."; _AUDIT_SKIPPED=$(( ... + 1 )); _AUDIT_REPORT_BUFFER+="ⓘ ...SKIPPED (unsupported-on-kind)..."; continue; fi`
- Below that gate, line 257 calls `run_one ... "audit" 3>"$tsv_tmp"`.

From cka-sim/scripts/lint-question-symptom.sh (lines 56-59 — existing kind-skip gate):
- `if cka_sim::symptom_diff::is_unsupported_on_kind "$q_dir"; then warn "...skipped..."; continue; fi`

From cka-sim/tests/cases/symptom-diff-unsupported-on-kind.sh (Phase 17 BLG-02 reference test):
- Creates 4 fixture metadata.yaml files (flag=true / missing / flag=false / no-meta-file).
- Asserts `is_unsupported_on_kind` returns 0 only on flag=true case.
- Final sanity: walks real packs/ tree, asserts at least 3 packs are flagged (3 questions had `unsupported-on-kind: true` post-Phase-17).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate root cause and select fix-path vs skip-path</name>
  <files>cka-sim/packs/workloads-scheduling/06-static-pod/setup.sh</files>
  <read_first>
    - cka-sim/packs/workloads-scheduling/06-static-pod/setup.sh (entire file — 24 lines)
    - cka-sim/lib/setup.sh (the helpers `ensure_lab_ns` and `wait_for_ns_active` — understand their failure modes)
    - cka-sim/lib/symptom-diff.sh lines 100-130 (`run_one` setup-invocation block; understand how the audit harness exports `CKA_SIM_LAB_NS` and runs setup.sh in a subshell)
    - cka-sim/lib/cmd/audit.sh lines 240-260 (per-question loop; understand the audit harness execution context)
    - cka-sim/current-tests/step2-results.txt entire file (the failure evidence)
    - .planning/STATE.md v1.0.2 Close-Out section (BLG-02 flag-strip context that explains why metadata.yaml's existing `unsupported-on-kind: true` was bypassed during step 2)
  </read_first>
  <action>
    Determine the failure mode and select fix-path or skip-path. Investigation steps:

    Step A — Static analysis of setup.sh failure surface. setup.sh has exactly three operations that could fail:
    1. `source "$CKA_SIM_ROOT/lib/setup.sh"` — fails only if CKA_SIM_ROOT is unset or path missing; the audit harness exports CKA_SIM_ROOT correctly (per `run_one` lines 121-122), so this is not the failure.
    2. `ssh -o BatchMode=yes -o ConnectTimeout=5 node-01 true` — fails if (a) `node-01` is not resolvable from the audit-harness execution context, (b) the audit harness runs as a non-root user without SSH keys distributed by `cka-sim bootstrap`, or (c) the lab cluster's bootstrap state is incomplete. Per STATE.md, the lab cluster ran bootstrap (Q06-static-pod requires it), so SSH topology IS in place — but the audit harness may run setup.sh in an environment that does not inherit the bootstrap-distributed SSH agent or known_hosts. Per `cka-sim/current-tests/step2-results.txt:3`, the audit was invoked as `root@master:~/CKA-Certified-Kubernetes-Administrator/cka-sim# bash bin/cka-sim audit ...` — same user as bootstrap, so SSH keys ARE present. Verify by reading `cka-sim/lib/symptom-diff.sh:120-124`: `run_one` exports `CKA_SIM_LAB_NS` and `CKA_SIM_ROOT`, runs `bash "$q_dir/setup.sh"` in a subshell with stdout/stderr redirected to `/dev/null`. The subshell inherits the parent shell's environment (including `$HOME` and any agent forwarding). SSH should work.
    3. `cka_sim::setup::ensure_lab_ns` + `cka_sim::setup::wait_for_ns_active` — fails on kubectl errors or 120s timeout. The audit harness's per-question namespace `cka-sim-audit-workloads-scheduling-06-static-pod` is 49 chars — within RFC-1123 63-char limit, so `compute_ns` produces a valid string.

    Step B — Distinguish drill-mode success from audit-mode failure. The question runs successfully under drill mode (per ROADMAP P22 success criterion 2: "Drill-mode and exam-mode behavior MUST be preserved"). Drill-mode setup.sh and audit-mode setup.sh are byte-identical (same file). The DIFFERENCE is the namespace: drill mode uses `cka-sim-workloads-scheduling-06`; audit mode uses `cka-sim-audit-workloads-scheduling-06-static-pod` (audit prefix + question slug, longer name). Both fit RFC-1123. Possible failure: the audit-mode subshell suppresses stdout+stderr (`>/dev/null 2>&1` at run_one line 124), so the actual error is invisible — only the wrapper's `if !` exit-code branch fires.

    Step C — Recognize that this question is fundamentally not audit-harness-compatible. The question's grading criterion is "candidate drops a manifest into /etc/kubernetes/manifests on node-01" — a node-local filesystem mutation that does not produce any in-namespace Kubernetes resource until the kubelet picks up the static pod. The audit harness's symptom-diff model expects in-cluster resources matching `expected-symptom.yaml`. For static-pod questions, there is no in-namespace artifact to diff against until AFTER the candidate's intervention, which the audit harness does not perform (it runs setup.sh + diffs current cluster state; no candidate step). Thus even if setup.sh succeeded, the audit comparison would either be vacuous (zero expectations) or wrong (expecting a static pod the audit harness never created).

    Verify the conclusion by reading `cka-sim/packs/workloads-scheduling/06-static-pod/expected-symptom.yaml` (if present) — if its `expect` block claims a static pod exists in the audit namespace, that's a structural mismatch with what setup.sh produces (nothing). If it does not exist or is empty, the question is correctly modeled but the audit harness still cannot exercise it.

    Selection rule:
    - If reading expected-symptom.yaml shows it claims in-cluster artifacts that setup.sh CANNOT produce without a candidate step → SKIP PATH (the question is structurally incompatible with audit mode).
    - If expected-symptom.yaml is missing, empty, or claims only the namespace itself → SKIP PATH (the audit run yields zero useful signal even on a green setup).
    - If a small, well-scoped tweak to setup.sh (e.g., adding a kubectl apply for a non-static seed pod, changing the SSH preflight to a soft-skip) would yield meaningful symptom-diff signal AND the lab cluster supports it AND it does not change drill/exam semantics → FIX PATH.

    Default expectation per CONTEXT D-AUDIT-W&S06.3: SKIP PATH is correct because static-pod questions live outside any namespace (`/etc/kubernetes/manifests/` is a node-local kubelet manifest dir). The audit harness's namespace-isolated model cannot simulate this.

    Record the decision and rationale in a 5-10 line block at the top of the plan SUMMARY (do NOT modify setup.sh or metadata.yaml in this task; Task 2 carries the change). The decision is "FIX" or "SKIP", followed by 3 evidence bullets and 1 sentence on what Task 2 will do.

    Do NOT invent a new lab-cluster mock. Do NOT change drill-mode setup.sh behavior. Do NOT remove the existing `unsupported-on-kind: true` flag in metadata.yaml — that is Phase 17 BLG-02's kind-specific gate and must be preserved per the deferred CONTEXT decision (`unsupported-on-kind` stays narrow; new flag is orthogonal).
  </action>
  <verify>
    <automated>cd C:\Users\thien\IdeaProjects\CKA-Certified-Kubernetes-Administrator && ls cka-sim/packs/workloads-scheduling/06-static-pod/expected-symptom.yaml 2>&1; ls cka-sim/packs/workloads-scheduling/06-static-pod/setup.sh; bash -n cka-sim/packs/workloads-scheduling/06-static-pod/setup.sh && echo "setup.sh syntax-ok"</automated>
  </verify>
  <acceptance_criteria>
    - Investigation produces a single-word decision: `FIX` or `SKIP`. The decision is recorded in the plan SUMMARY (Task 2 will commit the SUMMARY) — for this task, capture the decision in a temporary note (e.g., comment in `cka-sim/packs/workloads-scheduling/06-static-pod/setup.sh` with the prefix `# AUDIT-W&S06 decision:` followed by `FIX` or `SKIP`, then a 1-line rationale; this comment is removed in Task 2 if FIX path lands, or preserved as documentation if SKIP path lands).
    - Decision rationale references at least 2 of: (a) the actual contents of `expected-symptom.yaml` (or its absence), (b) the audit harness's namespace-sandbox model in `run_one`, (c) the static-pod question's `/etc/kubernetes/manifests/` non-namespaced nature, (d) drill-mode setup.sh contract preservation.
    - Both setup.sh and metadata.yaml are unchanged at the end of this task — investigation only, no fix application yet. The temporary `# AUDIT-W&S06 decision:` comment is the only modification (single-line addition near top of setup.sh).
    - `bash -n cka-sim/packs/workloads-scheduling/06-static-pod/setup.sh` exits 0 (syntax preserved).
  </acceptance_criteria>
  <done>
    Decision (`FIX` or `SKIP`) captured as a single-line comment in setup.sh; Task 2 reads that comment to choose its branch.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply chosen path (FIX or SKIP) and add lock test</name>
  <files>cka-sim/packs/workloads-scheduling/06-static-pod/setup.sh, cka-sim/packs/workloads-scheduling/06-static-pod/metadata.yaml, cka-sim/lib/symptom-diff.sh, cka-sim/lib/cmd/audit.sh, cka-sim/scripts/lint-question-symptom.sh, cka-sim/tests/cases/symptom-diff-unsupported-in-audit.sh</files>
  <read_first>
    - cka-sim/packs/workloads-scheduling/06-static-pod/setup.sh (read the `# AUDIT-W&S06 decision:` comment from Task 1 to determine branch)
    - cka-sim/lib/symptom-diff.sh lines 70-78 (`is_unsupported_on_kind` shape — mirror this for SKIP path)
    - cka-sim/lib/cmd/audit.sh lines 246-251 (existing kind-skip gate — mirror for SKIP path)
    - cka-sim/scripts/lint-question-symptom.sh lines 56-59 (existing kind-skip gate — mirror for SKIP path)
    - cka-sim/tests/cases/symptom-diff-unsupported-on-kind.sh (existing kind-skip test — mirror for SKIP path's new test case)
  </read_first>
  <behavior>
    SKIP path post-fix:
    - `cka_sim::symptom_diff::is_unsupported_in_audit_mode "$q_dir"` returns 0 when `q_dir/metadata.yaml` contains `unsupported-in-audit-mode: true`, returns 1 otherwise (including when metadata.yaml is missing or the flag is false/absent).
    - `cka-sim audit workloads-scheduling/06-static-pod` emits `ⓘ workloads-scheduling/06-static-pod: SKIPPED (unsupported-in-audit-mode)` and increments `_AUDIT_SKIPPED`.
    - `bash cka-sim/scripts/lint-question-symptom.sh` does NOT process this question's expected-symptom.yaml (if any) — same skip behavior as kind-flagged questions.
    - Drill-mode and exam-mode entry points (`cka-sim drill workloads-scheduling 6`, exam-mode pack-traversal) do NOT consult the new flag — the runner code paths in `cka-sim/lib/cmd/drill.sh` and `cka-sim/lib/cmd/exam.sh` are unchanged.
    - New test `cka-sim/tests/cases/symptom-diff-unsupported-in-audit.sh` exits 0 (locks the helper's true/missing/false/no-meta semantics + the real-pack sanity check that ≥1 pack is flagged).

    FIX path post-fix:
    - setup.sh succeeds against an audit-harness namespace on a 1+2 kubeadm cluster (verified in Phase 24 UAT).
    - metadata.yaml is unchanged (preserves Phase 17 BLG-02's kind-skip flag).
    - No new helper, no new gate, no new test case file (the placeholder file `symptom-diff-unsupported-in-audit.sh` is created as a brief no-op stub documenting that no audit-skip flag was needed).
    - Drill-mode and exam-mode unchanged.
  </behavior>
  <action>
    Read the `# AUDIT-W&S06 decision:` comment in setup.sh from Task 1 to pick the branch.

    --- IF SKIP PATH ---

    Step 1. Edit `cka-sim/packs/workloads-scheduling/06-static-pod/metadata.yaml`: add a new top-level line `unsupported-in-audit-mode: true` immediately below the existing `unsupported-on-kind: true` line (line 6). Keep `unsupported-on-kind: true` intact (Phase 17 BLG-02 semantics, narrow scope per CONTEXT D-AUDIT-W&S06 deferred). Add a single-line comment above the new flag explaining the reason: `# Skipped by audit/lint harness (AUDIT-W&S06): static-pod questions live outside any namespace; audit-harness symptom-diff model cannot simulate /etc/kubernetes/manifests mutations.` Remove the temporary `# AUDIT-W&S06 decision:` comment from setup.sh (Task 1 added it; not needed once metadata.yaml carries the formal flag).

    Step 2. Edit `cka-sim/lib/symptom-diff.sh`: add a new helper `cka_sim::symptom_diff::is_unsupported_in_audit_mode` immediately after `is_unsupported_on_kind` (insert below current line 78, before the `compute_ns` helper). Mirror the existing helper exactly: 5-line body — function header, `local q_dir="$1"`, `local meta="$q_dir/metadata.yaml"`, `[[ -f "$meta" ]] || return 1`, `grep -qE '^unsupported-in-audit-mode:[[:space:]]*true[[:space:]]*(#.*)?$' "$meta"`. Add a 2-line comment above the new helper: `# --- AUDIT-W&S06: audit-mode-only harness skip flag ---` and `# Returns 0 (true; skip) if metadata.yaml declares unsupported-in-audit-mode=true. Args: q_dir.`

    Step 3. Edit `cka-sim/lib/cmd/audit.sh`: in the per-question loop, ADD a new gate immediately after the existing `is_unsupported_on_kind` gate (after the current line 251 `continue` and before line 253 `tsv_tmp="$(mktemp)"`). Mirror the kind-skip block shape exactly: `if cka_sim::symptom_diff::is_unsupported_in_audit_mode "$q_dir"; then info "$pack/$q_name: skipped (unsupported-in-audit-mode)"; _AUDIT_SKIPPED=$(( _AUDIT_SKIPPED + 1 )); _AUDIT_REPORT_BUFFER+="ⓘ $pack/$q_name: SKIPPED (unsupported-in-audit-mode)"$'\n---\n'; continue; fi`. Format as a multi-line block matching the existing kind-gate's indentation.

    Step 4. Edit `cka-sim/scripts/lint-question-symptom.sh`: in the per-question driver loop, ADD a new gate immediately after the existing `is_unsupported_on_kind` gate (after current line 59 `continue`, before line 60 `checked=$(( checked + 1 ))`). Mirror the kind-skip block shape: `if cka_sim::symptom_diff::is_unsupported_in_audit_mode "$q_dir"; then warn "$pack/$q_name: skipped (unsupported-in-audit-mode)"; continue; fi`.

    Step 5. Create `cka-sim/tests/cases/symptom-diff-unsupported-in-audit.sh`: mirror `cka-sim/tests/cases/symptom-diff-unsupported-on-kind.sh` exactly, swapping the helper name (`is_unsupported_on_kind` → `is_unsupported_in_audit_mode`), the flag string (`unsupported-on-kind` → `unsupported-in-audit-mode`), and the real-packs sanity threshold (`>=3` → `>=1`, since at most 1 question carries the new flag at the end of P22). Keep the same 5 cases: flag=true → returns 0; flag missing → returns 1; flag=false → returns 1; metadata.yaml absent → returns 1; real-pack walk finds ≥1 flagged pack.

    --- IF FIX PATH ---

    Step 1. Edit `cka-sim/packs/workloads-scheduling/06-static-pod/setup.sh`: apply the minimal tweak identified in Task 1's investigation. The exact change depends on the root cause Task 1 surfaced — examples (the executor picks the one that matches the documented decision):
    - Soft-skip the SSH preflight when audit-mode env detected (e.g., `[[ -n "${CKA_SIM_AUDIT_MODE:-}" ]] && skip_ssh_preflight=1`). Requires audit harness to export `CKA_SIM_AUDIT_MODE=1` — which means a parallel edit to `cka-sim/lib/cmd/audit.sh` to set that variable before invoking `run_one`.
    - Add a kubectl-apply seed for a non-static placeholder pod that lets the audit's symptom-diff have a target. Requires a parallel edit to `expected-symptom.yaml`.
    - Other narrow tweak per Task 1 evidence.
    Remove the temporary `# AUDIT-W&S06 decision:` comment from setup.sh.

    Step 2. metadata.yaml unchanged. symptom-diff.sh unchanged. audit.sh unchanged. lint-question-symptom.sh unchanged.

    Step 3. Create `cka-sim/tests/cases/symptom-diff-unsupported-in-audit.sh` as a brief no-op stub: 5-line file with `set -uo pipefail`, a comment block stating `# AUDIT-W&S06: FIX path landed; no audit-skip flag introduced. This file documents the decision.`, and `exit 0`. The stub keeps the file path stable for future SUMMARY references and tooling that lists the case file as part of P22 deliverables.

    --- COMMON to both paths ---

    Run the full unit suite via `bash cka-sim/scripts/test.sh` to confirm no regression:
    - SKIP path: existing `symptom-diff-unsupported-on-kind` still passes (orthogonal flag); new `symptom-diff-unsupported-in-audit` passes; existing `symptom-diff-emit-row-fd-safe` (Plan 22-02) still passes; no new reds.
    - FIX path: same except the new test is a stub no-op.

    Do NOT touch `cka-sim/lib/cmd/drill.sh` or `cka-sim/lib/cmd/exam.sh` (drill/exam mode behavior must be preserved per ROADMAP P22 success criterion 2).
  </action>
  <verify>
    <automated>cd C:\Users\thien\IdeaProjects\CKA-Certified-Kubernetes-Administrator && bash -n cka-sim/lib/symptom-diff.sh && bash -n cka-sim/lib/cmd/audit.sh && bash -n cka-sim/scripts/lint-question-symptom.sh && bash -n cka-sim/packs/workloads-scheduling/06-static-pod/setup.sh && bash cka-sim/tests/cases/symptom-diff-unsupported-in-audit.sh && bash cka-sim/tests/cases/symptom-diff-unsupported-on-kind.sh && grep -F 'AUDIT-W&S06 decision:' cka-sim/packs/workloads-scheduling/06-static-pod/setup.sh; if [ $? -eq 0 ]; then echo "ERR: temporary decision comment still present" >&2; exit 1; fi; echo "all-checks-ok"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 declared `files_modified` paths exist after this task; their contents match the chosen path's branch from the action block.
    - SKIP path: `cka-sim/tests/cases/symptom-diff-unsupported-in-audit.sh` exits 0; the helper `cka_sim::symptom_diff::is_unsupported_in_audit_mode` is declared in symptom-diff.sh; both audit.sh and lint-question-symptom.sh consult it; metadata.yaml carries `unsupported-in-audit-mode: true` AND keeps the existing `unsupported-on-kind: true`.
    - FIX path: setup.sh tweak is minimal and self-contained (≤10 lines added/changed); metadata.yaml unchanged; symptom-diff.sh, audit.sh, lint-question-symptom.sh unchanged; the stub test file at `cka-sim/tests/cases/symptom-diff-unsupported-in-audit.sh` exits 0 (no-op).
    - The temporary `# AUDIT-W&S06 decision:` comment from Task 1 is REMOVED from setup.sh — verified by `grep -F 'AUDIT-W&S06 decision:' setup.sh` returning non-zero (no hit).
    - Drill-mode and exam-mode runners (`cka-sim/lib/cmd/drill.sh`, `cka-sim/lib/cmd/exam.sh`) are unchanged — verified via git diff showing zero modifications to those two files.
    - `bash cka-sim/scripts/test.sh` shows no NEW red cases attributable to this plan; in particular, all 5 existing `symptom-diff-*` cases plus the new `symptom-diff-emit-row-fd-safe` (from Plan 22-02) plus `symptom-diff-unsupported-in-audit` are GREEN.
    - Lab-cluster verification (`cka-sim audit workloads-scheduling/06-static-pod` produces SKIPPED or PASS) is the explicit ROADMAP P22 success criterion 2 — verified in Phase 24 UAT batch, NOT in this plan.
  </acceptance_criteria>
  <done>
    AUDIT-W&S06 closed in code per the chosen path; metadata.yaml + symptom-diff.sh helpers + audit.sh/lint-question-symptom.sh gates (SKIP) OR setup.sh tweak (FIX) are committed; new test case locks the helper's contract (SKIP) or documents the decision (FIX); drill/exam paths untouched. Lab-cluster verification deferred to Phase 24.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| metadata.yaml → audit/lint harness | metadata.yaml is repo content; trusted. Flag is read by `grep -qE` — bounded regex match, no eval. |
| audit harness → setup.sh subshell | setup.sh runs in a subshell with `CKA_SIM_LAB_NS` and `CKA_SIM_ROOT` exported; no other env passthrough. Bounded. |
| static-pod question → kubelet manifest dir on node-01 | Out of scope for audit; the audit harness deliberately does not exercise `/etc/kubernetes/manifests/` writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03-01 | Tampering | drill-mode invariant | mitigate | The audit-skip flag is consulted ONLY by `cka-sim/lib/cmd/audit.sh` and `cka-sim/scripts/lint-question-symptom.sh` per the gate placement in Step 3-4. Drill-mode and exam-mode runners (`cka-sim/lib/cmd/drill.sh`, `cka-sim/lib/cmd/exam.sh`) do not import `is_unsupported_in_audit_mode`. Verified by git-diff acceptance criterion. |
| T-22-03-02 | Denial of Service | regex match on metadata.yaml | accept | `grep -qE` with anchored pattern `^unsupported-in-audit-mode:[[:space:]]*true[[:space:]]*(#.*)?$` is bounded and runs once per question per audit invocation; no DoS surface. |
| T-22-03-03 | Repudiation | audit-mode skip rationale not surfaced | mitigate | Skip emits `ⓘ <pack>/<q_name>: SKIPPED (unsupported-in-audit-mode)` to both stdout and the report buffer (Step 3); operator can audit-trail the decision via `_AUDIT_REPORT_BUFFER` capture. |
</threat_model>

<verification>
- All 6 files in `files_modified` exist after Task 2 completes.
- `bash -n` on every modified shell file exits 0.
- `bash cka-sim/tests/cases/symptom-diff-unsupported-in-audit.sh` exits 0 (SKIP path: real test; FIX path: stub no-op).
- `bash cka-sim/tests/cases/symptom-diff-unsupported-on-kind.sh` still exits 0 (Phase 17 BLG-02 invariant preserved).
- `bash cka-sim/scripts/test.sh` adds no NEW reds attributable to this plan.
- Lab-cluster verification of the audit outcome (SKIPPED or PASS) is deferred to Phase 24 UAT batch — `cka-sim/scripts/uat-v103.sh` (or equivalent) will run `cka-sim audit workloads-scheduling/06-static-pod` on the v1.0.1 lab cluster and assert the verdict.
</verification>

<success_criteria>
- AUDIT-W&S06 closed in code via FIX path or SKIP path per Task 1 investigation.
- Drill-mode and exam-mode preserved (`cka-sim/lib/cmd/drill.sh`, `cka-sim/lib/cmd/exam.sh` unchanged).
- New unit test case locks the helper contract (SKIP path) or documents the decision (FIX path).
- Plan SUMMARY.md captures the decision rationale (FIX vs SKIP) with evidence references to Task 1 investigation findings.
</success_criteria>

<output>
After completion, create `.planning/phases/22-surgical-tech-debt-fixes/22-03-SUMMARY.md`. The SUMMARY records:
1. Task 1's decision (FIX or SKIP) plus 3-bullet evidence.
2. Task 2's actual file changes (diff summary per file).
3. Pointer to Phase 24 UAT for lab-cluster verification.
</output>
